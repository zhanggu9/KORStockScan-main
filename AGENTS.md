KORStockScan working rules:

## 1. Required References

- Before starting work, read `docs/plan-korStockScanPerformanceOptimization.rebase.md` §1 through §8 and the two top summary sections of the current daily `docs/checklists/YYYY-MM-DD-stage2-todo-checklist.md`: the daily objective and the daily mandatory rules.
- Use `Plan Rebase` as the source of truth for tuning principles, decision axes, rollback guards, and active/open state. The daily checklist owns executable work items.
- The clean tuning data decision baseline is `clean_tuning_baseline_date=2026-06-05` and `clean_tuning_baseline_ts_kst=2026-06-05T00:00:00+09:00`. Pre-baseline raw/report/analytics artifacts are archive/audit evidence only and must not be used for EV, rolling/MTD/cumulative tuning, live-auto promotion, runtime approval, pattern lab promotion, or real execution quality approval. The policy artifact is `data/source_quality/clean_baseline_policy.json`.
- Read the current active/open state from `Plan Rebase` §7 through §8. Do not treat completed `[x]` items in older checklists as current OPEN owners. Completed items are evidence links. The current owner is the runtime owner named on the same row or the open item in the current checklist.
- `docs/plan-korStockScanPerformanceOptimization.prompt.md` is a lightweight session-entry pointer. Do not read it for every routine task. Check it only when the user explicitly asks, or when the Plan Rebase location, Source of Truth document map, or current execution table is unclear.

## 1.1 Current State Baseline (`2026-08-11 KST`)

- The current phase is the automation-chain tuning phase in `Plan Rebase`. The objective is not loss suppression, but expected value and net-profit maximization. Target-date PREOPEN runtime env or exact-date policy artifacts own selected runtime families. Active operating families include `soft_stop_whipsaw_confirmation`, `scalp_sim_candidate_window_expansion`, `scalp_sim_ai_budget_manager`, `lifecycle_decision_matrix_runtime`, `scalp_sim_auto_approval`, `swing_sim_auto_approval`, and the separate `samsung_machine_entry_policy` bounded PREOPEN family. The retired WAIT6579 entry-only bridge has no producer, approval contract, PREOPEN consumer, or reactivation path; `wait6579_ev_cohort` remains LDM/source-quality provenance only. Samsung morning remains baseline-only, while midday/afternoon can tighten at most one machine and one entry axis per next-session candidate without changing target, validity, no-stop holding, provider/bot/cap, or broker guards. By explicit operator direction on 2026-08-13, all new independent episode-machine entries use two separate 10-share legs (20 shares maximum); legacy owned one-share state remains custody-compatible and is never resized, and quantity is not a postclose tuning axis.
- Tuning data decisions now start from the clean baseline `2026-06-05 KST`, with full-day start timestamp `2026-06-05T00:00:00+09:00`. Raw/report/analytics data before that baseline is archive-only and cannot support EV, rolling/MTD/cumulative tuning, live-auto promotion, runtime approval, pattern lab promotion, or real execution quality approval. `threshold_cycle_preopen_status` and `threshold_cycle_postclose_status` are operational freshness status artifacts, not tuning decision reports, and are excluded from clean report quarantine.
- Postclose `observation_source_quality_audit` is now the contract-gap preflight hard gate for tuning inputs. Contract warning/fail, required-field missing, invalid label, or high-volume no-contract gaps set `tuning_input_allowed=false`; EV, rolling/MTD/cumulative tuning, live-auto promotion, and runtime approval must become `source_quality_blocked` with `runtime_effect=false` and a `source_quality_gap` code-improvement workorder handoff. Unknown-token findings alone are review warnings, not immediate tuning stops.
- The central loop is `R0_collect -> R1_daily_report -> R2_cumulative_report -> R3_manifest_only -> R4_preopen_apply_candidate -> R5_bounded_calibrated_apply -> R6_post_apply_attribution`. Artifact, consumer, and apply contracts are owned by `docs/report-based-automation-traceability.md`.
- The scheduled automation-chain path is postclose artifact plus AI/deterministic guards and PREOPEN `auto_bounded_live` runtime env. An explicit user-directed intraday override may change one existing `bounded_tunable` axis for one explicit KRX or NXT cohort only with fresh conflict-free source quality, effective price, target-first or sufficient post-block MFE, a single-blocker causal link, no same-stage owner/canary conflict, recorded before/after values and runtime provenance, rollback, and immediate attribution. It must not alter hard safety, stale/conflict, price freshness, broker/account/order/quantity/cooldown, provider, bot, cap, requested quantity, or a common/KRX surface from NXT evidence. Closed operational override records are archive evidence only; current owner decisions retain only selected family, provenance, and post-apply attribution.
- `lifecycle_decision_matrix_runtime` is the ADM expansion umbrella owner. It wraps existing Entry ADM, Holding/Exit ADM, submit observation, and scale-in bias adapter as `entry`, `submit`, `holding`, `scale_in`, and `exit` arms. Runtime structure and lifecycle bucket discovery are implemented; scheduled auto application uses PREOPEN `auto_bounded_live`, while an explicit intraday override follows the same-stage `runtime_mutation_guard` contract and cannot bypass hard safety. LDM `entry_bucket_attribution`, `scale_in_bucket_attribution`, `overnight_bucket_attribution`, and stage policy buckets automatically generate sim-auto/live-auto candidates or source-only `code_improvement_workorders`; missing downstream handoff is a postclose verifier FAIL.
- Bucket tuning is meant to accelerate validated tuning, not to create thinner combo buckets. Daily buckets detect new patterns, source-quality gaps, and conflict children; rolling/MTD parent buckets confirm EV and own sim/live candidate narrowing. Child combo detail must remain as dimensions/provenance, dominant/conflicting patterns, and exclusion candidates, but daily-only child EV or a thin child sample must not become live authority.
- Existing fixed thresholds are not deleted; their roles are changed. Broker submit guard, stale quote submit block, price freshness, hard/protect/emergency stops, account/order/cooldown/quantity guards are `hard_safety`; `BUY_SCORE_THRESHOLD`, VPW/strength/momentum, and entry score cutoffs are `baseline_prior`; latency caution, score65_74 probe, soft stop/holding flow, and scale-in price guard are `bounded_tunable`; fallback scout/main, fallback single, latency fallback split-entry, legacy latency composite, and closed shadow axes are `legacy_archive`.
- Lifecycle matrix and ADM priority is `hard safety veto -> account/order/broker guard -> lifecycle matrix runtime policy -> existing ADM adapter -> baseline fixed threshold fallback`. Matrix/ADM cannot bypass broker submit guard, stale/price freshness, stop, or quantity guards.
- `greenfield_real_environment_authority` is implemented as a PREOPEN-only runtime env candidate but is currently blocked by bridge/policy contract gaps and missing policy file. Do not manually enable `KORSTOCKSCAN_GREENFIELD_REAL_ENV_AUTHORITY_*`; missing policy or promoted bucket is a blocked-action condition, not a fallback to legacy live authority.
- Sim-first lifecycle is not a new independent chain; it is the input and interpretation scope of the existing threshold-cycle automation chain. Bucket classification, typing, sim-only/pre-final candidate generation, and code-improvement workorder implementation within source-quality, parser/schema, report, test, documentation, instrumentation, or sim/source-only scope are unattended automation-chain duties. User explicit action is reserved for converting sim/probe/source-only evidence into real order authority, PREOPEN live env selection, provider route, bot process state, cap release, broker/order guard behavior, hard/protect/emergency safety behavior, or final full-live approval artifacts. Scalping and swing BUY/selection candidates are broadly run through a virtual `selection -> entry -> submit -> holding -> scale_in -> exit -> overnight/carry` lifecycle to find optimal threshold candidates and code-improvement workorders, but this is never standalone evidence for full real-order enablement, cap release, provider change, or bot restart. `scalp_sim_candidate_window_expansion` widens score 55-100 WAIT/blocked candidates as sim-only observation. `scalp_sim_ai_budget_manager` controls AI calls for the expanded sim holding set based on state changes and budget, as sim-only. `scalp_sim_scale_in_window_expansion` remains sim-only source widening when enabled by runtime env. These axes keep `actual_order_submitted=false`, `broker_order_forbidden=true`, and `decision_authority=sim_observation_only`. Sim-only candidates do not send BUY analysis or order Telegram alerts and leave provenance only; sim AI budget does not change provider route, BUY/submit threshold, or broker guard.
- The default live scalping AI route remains OpenAI except endpoint-specific overrides. `entry_price` uses Bedrock Qwen3 32B primary with Nova Lite v2 failback and no OpenAI third fallback; if both Bedrock calls fail, the defensive entry-price fallback closes the call. `holding_flow` uses Bedrock Nova Lite v2 primary with OpenAI failback. Removed Bedrock shadow/comparison surfaces are not active report inventory; current Bedrock evidence is primary/failback provider audit rows. Provider transport/provenance confirmation is separate from threshold value, order price/quantity guard, and swing dry-run guard changes. Gemini fallback caused by OpenAI initialization failure is a runtime incident.
- Scalping entry, price, and holding are judged through automation-chain attribution.
  - `entry_cancel_wait_runtime` is a standalone operational family from `2026-06-15 PREOPEN`. It is excluded from ADM/LDM, lifecycle buckets, general threshold EV, and runtime apply bridge. It tunes standard/breakout/pullback/reserve BUY cancel waits through a dedicated postclose deterministic counterfactual EV report and remains ON unless an explicit operator lock sets enabled=false. Entry-price AI `max_wait_sec` is advisory only and cannot directly override the live cancel timeout.
  - Score 50 fallback/neutral must not flow into new BUY submission; hold it as `blocked_ai_score`.
  - Score bucket is a baseline prior and feature only. Score alone must not decide BUY/WAIT/DROP.
  - `wait6579_ev_cohort` remains a score 66-69 WAIT/recovery entry provenance and source-quality dimension. Its retired standalone bridge family has no PREOPEN/runtime path. Current `WAIT6579_PROBE_CANARY` budget/quantity cap values of `0` mean separate probe caps are disabled and normal new-buy sizing applies. It is not broad BUY threshold relaxation.
  - `dynamic_entry_price_resolver_p1` and `dynamic_entry_ai_price_canary_p2` own entry price. If passive probe submit revalidation is `stale_context_or_quote`, block before broker submission.
  - `soft_stop_micro_grace`, `holding_flow_override`, Entry/Holding ADM, and lifecycle matrix cannot bypass hard/protect/emergency/order safety. `soft_stop_whipsaw_confirmation` is currently OFF/source-only until exact-path rolling evidence closes; `holding_flow_ofi_smoothing` remains ON inside its bounded holding-flow guard.
  - Keep scale-in price resolver and dynamic quantity safety. `position_sizing_dynamic_formula` is the single sizing owner; new-buy/additional-buy always use the 10-30% orderable-budget formula with minimum 1-share floor. `position_sizing_cap_release` family is removed; rollback uses more conservative formula candidates.
- `scalp_ai_buy_all_live_simulator`, swing dry-run, and probe/counterfactual samples have `actual_order_submitted=false` authority. They may support source bundles and approval requests, but they are not standalone evidence for real execution quality or real-order conversion. In sim-first exploration, insufficient cash and no real submission are provenance tags, not candidate exclusion reasons.
- Tuning performance must be reported from the user's operating viewpoint, not as a raw headline count. For LDM, bucket, active-sim-priority, scalp sim, and swing sim results, answer in this order: `decision -> evidence -> next action`. The decision must state whether the flow/bucket/arm was identified with valid source quality, whether it was actually applied to sim through policy catalog/PREOPEN env/runtime observation, and what remains before real runtime reflection. Evidence must separate complete lifecycle flows, stage-only buckets, sim-auto approvals, active priority matches, EV fields, sample floors, source-quality warnings, submit drought, bridge/live-auto contract status, AI review status, and post-apply attribution. A low `complete lifecycle flow` count is not automatically a failure when catalog/PREOPEN handoff and natural-match semantics are intact; report it as early evidence with sim application status and real-runtime blockers. Source-quality unknowns are evidence-quality warnings, not a current real-trading blocker for a candidate that has not reached real-trading authority; they must be resolved, excluded, or explicitly marked not-applicable before the same data can support live-auto, real runtime reflection, or full-live approval. Bridge/live-auto reports must not stop at "contract not closed"; enumerate each blocker by class, owner artifact, observed evidence, next repair action, and acceptance test.
- Swing is primarily a dry-run self-improvement chain. `swing_lifecycle_audit`, `swing_threshold_ai_review`, `swing_improvement_automation`, `swing_runtime_approval`, and the swing auto-approval control tower may create postclose source bundles. General swing dry-run env changes are AI Tier2 pre-final auto approvals when hard floors, source-quality gates, and parsed Tier2 review pass; Tier2 missing/unavailable/parse-rejected is fail-closed. There is no current swing one-share or scale-in broker-submit trial path; broker-submit authority opens only through final full-live conversion approval after complete Swing LDM parent bucket evidence is closed. User approval is reserved for final full-live conversion, cap release beyond bounded limits, provider/bot changes, or hard/protect/emergency safety relaxation. `swing_strategy_discovery_sim` expands safe-pool candidates into sim-only candidates and 8-arm variants, while `swing_strategy_discovery_label_builder` and `swing_strategy_discovery_ev_report` produce label/EV/source-only analysis. This chain keeps `actual_order_submitted=false`, `broker_order_forbidden=true`, `runtime_effect=false`, and `decision_authority=swing_sim_exploration_only`; it does not replace existing swing runtime or `recommendation_history`.
- `BUY Funnel Sentinel`, `HOLD/EXIT Sentinel`, `panic_sell_defense`, and `System Error Detector` are report-only/source-quality/incident inputs. The panic-buying detector/report, wrapper, postclose source, approval/workorder bridge, sim euphoria actuator, and real-entry price modifier were removed on `2026-08-08`; historical panic-buying artifacts are archive/audit evidence only and have no producer, consumer, runtime, or reactivation path. `panic_regime_mode` is the panic-sell `NORMAL -> PANIC_DETECTED -> STABILIZING -> RECOVERY_CONFIRMED` risk-regime interpretation layer; the V2.0 candidate is scalping `entry_pre_submit` only `panic_entry_freeze_guard`. Without approval artifact, runtime env key, rollback guard, and same-stage owner rule, do not change score/stop/TP/trailing/threshold/provider/bot restart, automated selling, open-order cancellation, forced holding-exit change, chase buying, or market full liquidation.
- The previous-limit-up rotation observation family (`upper_limit_watch`, its bounded-live promotion chain, and `UPPER_LIMIT_LIVE_RECLAIM`) was removed on `2026-08-14` by explicit operator direction. Historical artifacts are archive/audit evidence only; there is no producer, verifier, runtime slot, scanner handoff, live-auto authority, or reactivation path. General upper-limit proximity entry protection and owned-position limit-up exit safety remain separate active safeguards.
- Opening Rotation one-share was permanently retired on `2026-08-14` under `opening_rotation_full_retirement_20260814`. Scanner ownership/protected slots, new BUY and margin-exception authority, postclose tuning, and PREOPEN policy apply/verify have no current producer or runtime authority. Historical artifacts are archive/audit only; legacy receipt/exit parsing remains custody compatibility and cannot reactivate entry.
- The previous-limit-up rotation observation family (`upper_limit_watch`, its bounded-live promotion chain, and `UPPER_LIMIT_LIVE_RECLAIM`) was removed on `2026-08-14` by explicit operator direction. Historical artifacts are archive/audit evidence only; there is no producer, verifier, runtime slot, scanner handoff, live-auto authority, or reactivation path. General upper-limit proximity entry protection and owned-position limit-up exit safety remain separate active safeguards.
- New observation metrics must declare `metric_role`, `decision_authority`, `window_policy`, `sample_floor`, `primary_decision_metric`, `source_quality_gate`, and `forbidden_uses` at creation time. Without that contract, route them only as `instrumentation_gap` or `source_quality_blocker`.
- Win rate is `diagnostic_win_rate`; EV is `primary_ev`. `simple_sum_profit_pct` is not EV. EV fields must be named one of `equal_weight_avg_profit_pct`, `notional_weighted_ev_pct`, or `source_quality_adjusted_ev_pct`.
- `fallback_scout/main`, `fallback_single`, latency fallback split-entry, legacy latency composite, and closed shadow axes are historical/reference per archive. Reopening them requires a new workorder, new rollback guard, and new checklist.

## 1.2 AGENTS.md Daily Revision Procedure

- AGENTS.md is a daily-updatable working-instruction snapshot. The original owner of executable work items is always the daily checklist, and the original owner of tuning principles and active/open decisions is always `Plan Rebase`.
- At preopen or task start, read `Plan Rebase` §1 through §8 and the current daily checklist top summary, then confirm that the date and active-owner summary in AGENTS.md `Current State Baseline` still match.
- If owner, live/observe/off state, rollback guard, or the next decision checklist changes intraday or postclose, update `Plan Rebase` or the daily checklist first, then briefly update only AGENTS.md `1.1 Current State Baseline`.
- Baseline documents such as README, runbook, Plan Rebase, prompt, and AGENTS are updated only when the user explicitly asks.
- Keep those document updates separate from runtime/order/provider/bot/threshold changes. When needed, close the update through first edit, review, final supplement, and parser validation.
- Do not add new auto-parsed `- [ ]` work items to AGENTS.md. Future work, time-specific work, and recheck work belong only in the daily checklist as checkboxes with `Due`, `Slot`, `TimeWindow`, and `Track`.
- When linking old checklist IDs from AGENTS.md, distinguish `completed record`, `current owner`, and `next check owner`. Do not present completed `[x]` items as current OPEN owners.
- Daily revision must at least check `entry owner`, `holding/exit owner`, `operating override`, `observe/report-only axes`, `OFF/deprecated axes`, and `Project/Calendar sync rules`.
- After document updates, run parser validation. AI must not run Project/Calendar sync directly; leave exactly one standard sync command for the user.

## 2. Decision Principles

- The goal is not loss suppression, but expected value and net-profit maximization.
- The Plan Rebase period baseline is `main-only`, `normal_only`, and `post_fallback_deprecation`.
- The tuning data decision baseline is `2026-06-05T00:00:00+09:00 KST`; pre-baseline raw/report/analytics data is forbidden for current tuning decision inputs and remains archive/audit evidence only.
- Live changes allow only one canary axis within the same stage. Stage-disjoint concurrent canaries can be reviewed in parallel only when stage, manipulation point, apply timing, cohort tag, and rollback guard are separated, such as entry-bottleneck versus holding/exit axes.
- New or supplemental alpha tuning axes are treated as `canary-only` without shadow. Operational/support axes explicitly documented as observe/report-only, such as AI transport/schema, statistical action weight, and decision matrix, must not change real order or exit decisions.
- Remote comparison values are excluded from Plan Rebase decision inputs.
- Profit/loss uses only `COMPLETED + valid profit_rate`. Exclude `NULL`, incomplete, and fallback-normalized values from PnL criteria.
- Do not merge `full fill` and `partial fill`.
- Win rate is a supporting diagnostic; EV primary metric owns expected value and net-profit decisions.
- Daily-only metrics may be used for safety/source-quality/operational triggers, but live/canary/threshold apply approval must use rolling/cumulative or post-apply version windows as well.
- Tuning performance reports must answer `identified?`, `applied to sim?`, and `remaining for real runtime?` before interpreting whether a count is good or bad. Do not treat a low complete-flow count, high bucket count, or sim-auto approval count as standalone success/failure without source-quality, catalog/PREOPEN handoff, runtime match, EV, and bridge/live-auto status. If bridge/live-auto is blocked, report the blocker taxonomy and the next concrete closure test instead of leaving a vague wait state.
- Split post-BUY non-entry into `latency guard miss`, `liquidity gate miss`, `AI threshold miss`, and `overbought gate miss`.
- If attribution is unclear, check report consistency, event reconstruction, and aggregation quality before changing strategy thresholds.
- Operational automation evidence such as cron, runbook, manifest, report, and parser normalization is separate from strategy effect or live approval evidence.
- When possible, structure answers as `decision -> evidence -> next action`.

## 3. Documentation And Automation

- Update related documents together when relevant.
- Do not duplicate long `objective/term legend/operating rules` boilerplate at the top of daily checklists. Keep only short `Today objective` and `Today mandatory rules`; detailed terms, policies, and guards are referenced from `Plan Rebase` or related companion documents.
- Runtime-loaded AI system/user prompts, reviewer prompts, JSON classifier prompts, and schema instructions must be written and edited in English ASCII by default. Korean user-facing briefing/Telegram text and contract-required canonical labels can be exceptions, but internal decision-prompt role descriptions, rules, JSON field descriptions, and fallback reasons must remain English.
- When editing that prompt-contract area later, do not restore old Korean wording. Supplement it only in English. If Korean output is needed, separate it into a user-facing formatting contract instead of internal prompt instructions.
- Personal notes (`docs/personal-decision-flow-notes.md`) are not referenced as `Source` or decision evidence in other documents. Execution decisions must cite checklist/report/plan baseline documents directly.
- Future work, time-specific work, and recheck work must not remain only in an answer. Record them as auto-parsable `- [ ]` items in the daily checklist.
- If checklist work items are created or modified, run parser validation, and do not check Project/Calendar token existence. Leave exactly one command for the user to run.
- AI runs parser validation after document changes. AI does not directly run GitHub Project or Google Calendar sync; the user runs it manually.
- Use exactly this one sync command when leaving a sync instruction for the user:
  - `PYTHONPATH=. .venv/bin/python -m src.engine.sync_docs_backlog_to_project && PYTHONPATH=. .venv/bin/python -m src.engine.sync_github_project_calendar`
- If automation rules, cron, workflows, or wrappers change, update the operating documents and checklist in the same change set.

## 4. Execution Environment

- Use the project `.venv` by default for Python work.
- Ask for user decision before installing, upgrading, or removing packages.
- Prefer reproducible commands and project-standard execution paths over temporary scripts.

### 4.1 Kiwoom Official API Reference Gate

- Before writing or modifying any Kiwoom REST/WebSocket request, response parser, realtime FID mapping, REG/REMOVE or recovery flow, authentication flow, account/order call, or continuation handler, inspect the current official [`Kiwoom-Securities/Kiwoom-REST-API`](https://github.com/Kiwoom-Securities/Kiwoom-REST-API) revision and the relevant upstream files.
- Use [Kiwoom API Data Contract](./docs/kiwoom-api-data-contract.md) `Official Kiwoom Reference Gate` as the local owner for source precedence and the mandatory verification checklist. Record the upstream commit SHA, inspected paths, and retrieval time in change/review evidence.
- Treat upstream `kiwoom_docs` as the protocol documentation owner; cross-check `kiwoom/specs.py`, `kiwoom/core`, `kiwoom/realtime`, and Postman. Treat `examples` as samples only, never as authority to execute real orders or bypass local guards.
- Verify REST path, `api-id`, headers, fields, sign/unit/time semantics, continuation, errors, and real/demo separation. Verify WebSocket URL, login/control packets, realtime type/FIDs, item/suffix/route, REG/REMOVE, reconnect/resubscribe, and documented limits.
- If official repository documents, SDK/specs, portal guidance, or observed packets conflict or leave semantics undefined, do not guess. Preserve raw provenance, fail closed for semantic/runtime promotion, and open a source-quality or contract gap with tests.
- Official protocol evidence does not override KORStockScan hard safety. Never relax stale/conflict, broker/account/order/quantity/cooldown, provider, threshold, cap, bot-state, or hard/protect/emergency guards solely because an upstream example permits a call.

## 5. Codebase Work Review And Fix Loop

- Every codebase modification must close through `implementation -> self code review -> supplemental fixes -> revalidation -> result report`.
- After any KORStockScan codebase modification, use `$korstockscan-review-gate` as the default review gate when the skill is available. The gate must repeat `review -> fix defects -> re-review -> targeted validation` until there are no unresolved findings before final reporting.
- Do not restart the bot, regenerate expensive reports, or run broad automation after a code change until `$korstockscan-review-gate` has closed with no remaining defects, relevant targeted validation has passed, and the user or runbook/checklist explicitly allows that runtime follow-up.
- Before creating or moving a source file, package, CLI module, report producer, or test, enforce a mandatory location gate. `src/engine` root is closed to new Python modules by default; new `.py` files there are allowed only when the engine-root allowlist test is deliberately updated in the same change with a written ownership reason. First inspect the repository structure and nearby producers/consumers, then choose the directory that matches the work's role: live engine/runtime, swing dry-run/simulation, scanner, model/training, report producer, test, document, or offline/archive analysis. Prefer existing role packages such as `src/engine/automation`, `src/engine/swing`, `src/engine/scalping`, `src/engine/lifecycle`, `src/engine/monitoring`, `src/engine/risk`, `src/engine/error_detectors`, `src/engine/infrastructure`, or `src/engine/ai` over a root-level module. If multiple locations look plausible, state the chosen ownership boundary and keep compatibility wrappers only when an existing caller or documented command requires them. Wrapper modules must be temporary compatibility surfaces, not a way to leave duplicate implementation residue.
- Self-review must not stop at skimming changed files. Also check the changed path's producer/consumer contracts, silent-fail possibilities, runtime/order/provider/threshold authority leaks, and regressions existing tests may miss.
- Fix issues found during review immediately, without waiting for the user to separately ask for supplementation. If the fix scope goes beyond the original request or leads to runtime/order/provider/bot/threshold changes, separate the scope and report it first.
- After supplemental fixes, run relevant targeted tests, syntax/compile checks, and `git diff --check` by default. Include parser validation for document/checklist/parser changes.
- If tests were not run or only partially run, state why and name the remaining risk in the final response.
- Simple lookup, explanation, and no-code review requests are exceptions. Once code or automation files are modified, this loop applies.
