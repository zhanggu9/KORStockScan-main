# 2026-07-14 Stage2 To-Do Checklist

## 오늘 목적

- 전일 postclose 자동화가 만든 장전 apply 후보와 사용자 개입 요구사항을 산출물 기준으로 확인한다.
- 실주문, threshold, provider, sim/probe 관련 변경은 approval artifact와 checklist 기준 없이 열지 않는다.
- code-improvement workorder는 자동 repo 수정이 아니라 사용자가 Codex에 구현을 지시한 경우에만 실행한다.

## 오늘 강제 규칙

- 장중 runtime threshold mutation은 금지한다. 적용은 PREOPEN `threshold_cycle_preopen_apply`가 생성한 runtime env만 source로 본다.
- 튜닝 데이터 기준은 `clean_tuning_baseline_date=2026-06-04`, `clean_tuning_baseline_ts_kst=2026-06-04T14:29:09+09:00`이다. 기준 이전 raw/report/analytics artifact는 archive/audit evidence로만 보고 EV/rolling/MTD/cumulative tuning, live-auto promotion, runtime approval, pattern lab promotion, real execution quality approval 입력으로 쓰지 않는다.
- Baseline 이후 raw source-quality contract 결손은 날짜 전체 차단이 아니라 결손 row/window를 `raw_row_exclusion`으로 제외하는 것이 기본이다. 전체 block은 preflight missing/invalid, row/window exclusion 실패, 또는 결손을 안정적으로 특정할 수 없는 high-volume no-contract 상황에만 사용한다.
- 장중과 장후에는 `observation_source_quality_audit --write` 또는 최신 artifact로 raw source-quality를 반복 확인한다. Hard contract gap은 결손 row/window 제외 또는 `source_quality_blocked` 없이는 튜닝 입력에 들어갈 수 없고, unknown-token warning은 hard block이 아니더라도 code-improvement workorder handoff 확인 대상이다.
- provider transport/provenance 확인은 threshold 값, 주문가/수량 guard, 스윙 dry-run guard 변경과 분리한다.
- `actual_order_submitted=false`인 sim/probe 표본은 EV/source-quality 입력이며 실주문 전환 근거가 아니다.
- Project/Calendar 동기화는 사용자가 표준 동기화 명령으로 수행한다.

<!-- AUTO_NEXT_STAGE2_CHECKLIST_START -->
## 자동 생성 체크리스트 (`2026-07-13` postclose -> `2026-07-14`)

- 이 블록은 postclose 자동화 산출물에서 생성된다.
- `codex_daily_workorder_*.md`는 downstream 전달물이라 입력 source로 사용하지 않는다.
- RunbookOps 반복 확인은 `build_codex_daily_workorder`와 Project/Calendar 동기화 경로가 별도로 소유한다.

## 장전 체크리스트 (08:45~09:00)

- [x] `[ThresholdEnvAutoApplyPreopen0714] threshold env 자동 apply 산출물 및 사용자 개입 여부 확인` (`Due: 2026-07-14`, `Slot: PREOPEN`, `TimeWindow: 08:50~08:55`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-07-13.json), [threshold_cycle_preopen_apply.py](/home/ubuntu/KORStockScan/src/engine/threshold_cycle_preopen_apply.py), [run_bot.sh](/home/ubuntu/KORStockScan/src/run_bot.sh)
  - 판정 기준: 전일 postclose EV와 당일 apply plan/runtime env를 확인하고 `auto_bounded_live` guard 통과분만 runtime env로 인정한다.
  - 금지: blocked family, approval artifact missing, same-stage owner conflict를 수동 env override로 우회하지 않는다.
  - 다음 액션: `applied_guard_passed_env`, `blocked_no_env`, `partial_apply_with_blocked_families`, `failed_preopen_wrapper`, `not_yet_due` 중 하나로 닫는다.
  - 실행 결과 (`2026-07-14 08:54 KST`): 판정=`applied_guard_passed_env`. `threshold_apply_2026-07-14.json`은 status=`auto_bounded_live_ready`, apply_mode=`auto_bounded_live`, runtime_change=`true`, source_phase_auto_apply_blocked=`false`다. `threshold_runtime_env_2026-07-14.json`과 `threshold_runtime_env_verify_2026-07-14.json` 기준 selected family는 `25`개이고, verify는 status=`pass`, passed=`true`, missing_family_count=`0`, pid_passed=`true`, pid_missing/pid_mismatches=`[]`다. blocked family/approval gap/same-stage conflict를 수동 env override로 우회하지 않았다.

- [x] `[RisingMissedScoutRuntimePreopen0714] rising_missed_scout_workorder 구현분 다음 장전 runtime 반영 여부 확인` (`Due: 2026-07-14`, `Slot: PREOPEN`, `TimeWindow: 08:55~09:00`, `Track: ScalpingLogic`)
  - Source: [rising_missed_scout_workorder_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/rising_missed_scout_workorder/rising_missed_scout_workorder_2026-07-13.json), [rising_missed_normal_buy_bridge_candidate_discovery_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/rising_missed_normal_buy_bridge_candidate_discovery/rising_missed_normal_buy_bridge_candidate_discovery_2026-07-13.json), [code_improvement_workorder_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-07-13.json), [threshold_apply_2026-07-14.json](/home/ubuntu/KORStockScan/data/threshold_cycle/apply_plans/threshold_apply_2026-07-14.json), [threshold_runtime_env_2026-07-14.json](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_2026-07-14.json), [threshold_runtime_env_verify_2026-07-14.json](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_verify_2026-07-14.json)
  - 2026-07-14 user-approved supplement: scout/normal bridge 공통 `resolve_rising_missed_decision_input`과 date-bounded TP1 selector를 구현하고, `operator_runtime_overrides.env`에 `KORSTOCKSCAN_RISING_MISSED_TP1_SELECTOR_ENABLED=true`, `KORSTOCKSCAN_RISING_MISSED_TP1_SELECTOR_ACTIVE_DATE=2026-07-14`를 추가했다. rollback은 selector `false`이며 sizing/cap/provider/TP/exit/hard safety는 변경하지 않는다. review gate와 graceful restart/PID env 검증 전에는 runtime 반영 완료로 판정하지 않는다.
  - 2026-07-14 08:46 KST 적용 판정: `$korstockscan-review-gate` post-fix 재리뷰와 targeted validation `797 passed`, compile, `git diff --check`, checklist parser 통과 후 `./restart.sh`로 PID `8378 -> 26504` graceful restart를 완료했다. `threshold_runtime_env_verify_2026-07-14.json`은 status=`pass`, selected_family_count=`25`, missing_family_count=`0`, pid_passed=`true`, pid_missing/pid_mismatches=`[]`였고 새 PID env에서 TP1 selector enabled/date, one-share entry, normal bridge가 모두 확인됐다. 다음 액션=`runtime_env_reflected_and_verified`.
  - 2026-07-14 09:42 KST 장중 보완: 09:00~09:20 관찰에서 확인된 WS nested orderbook spread 기본값, reversal current-delta/상관 micro 중복점수, reversal-up recheck 중복 enqueue, REST reserve 소진 cache 소비 문제를 보완했다. review/fix/re-review 후 관련 targeted pytest `722 passed`, compile, `git diff --check`, checklist parser를 통과했고 `./restart.sh`로 PID `26504 -> 45019` graceful restart를 완료했다. runtime env handoff는 selected family `25`, status=`pass`, missing family `0`, pid_passed=`true`, pid_missing/pid_mismatches=`[]`이며 새 PID env의 TP1 selector enabled/date, one-share entry, normal bridge를 재확인했다. 재기동 후 첫 TP1 평가에서 input=`ready`, source=`ws`, quote level basis=`normalized_ws_orderbook`, spread=`0.00906`이 기록되어 기존 spread 기본값 `1.0` 병목 제거를 확인했다.
  - 2026-07-14 09:53 KST 재리뷰 보완: normal bridge block/defer provenance 누락, TP1 marker 부분문자열 오탐과 source-family 과대계수, resolver effective-price/anchor provenance 누락, 20분 first-hit label의 후속 fee/tax 누락을 수정했다. 최종 재리뷰에서 selector가 submit 권한을 얻거나 기존 stale/tick-speed/reversal/broker/account/order/quantity/cooldown veto를 우회하는 문제는 없었다. 관련 targeted pytest `726 passed`, compile, `git diff --check`, checklist parser 통과 후 `./restart.sh`로 PID `45019 -> 48934` graceful restart를 완료했다. runtime env handoff는 selected family `25`, status=`pass`, missing family `0`, pid_passed=`true`, pid_missing/pid_mismatches=`[]`이며 새 프로세스의 계좌 동기화, WS 연결/로그인, 0B/0D 첫 수신, 스캐너 루프 진입을 확인했다.
  - 판정 기준: 전일 `rising_missed_scout_workorder` 요약(code_improvement_order_count=`4`, forced_scout_with_post_sell_count=`4`, profitable_forced_scout_count=`2`, loss_or_flat_forced_scout_count=`2`, current_missed_count=`66`)과 `rising_missed_normal_buy_bridge_candidate_discovery` 요약(status=`hold_no_candidate`, bridge_candidate_count=`0`, code_improvement_order_count=`0`, runtime_env_key=`KORSTOCKSCAN_RISING_MISSED_NORMAL_BUY_BRIDGE_ENABLED`)을 함께 보고 구현 완료된 mapped family가 당일 PREOPEN apply plan/runtime env/verify에 반영됐는지 확인한다. source-only order는 별도 runtime family/env mapping과 guard 통과가 있을 때만 반영으로 인정한다.
  - 금지: `rising_missed_scout_workorder`/bridge discovery 생성 또는 forced 1-share scout 손익만으로 runtime threshold mutation, stale submit bypass, broker/order guard 완화, provider/bot/cap 변경, real execution quality approval을 열지 않는다.
  - 다음 액션: `runtime_env_reflected_and_verified`, `implemented_but_runtime_not_selected`, `source_only_no_runtime_authority`, `blocked_by_apply_guard`, `report_missing_or_stale`, `verify_missing_or_failed` 중 하나로 닫는다.

## 장중 체크리스트 (09:05~15:20)

- [x] `[RuntimeEnvIntradayObserve0714] 전일 selected runtime family 장중 provenance 및 rollback guard 확인` (`Due: 2026-07-14`, `Slot: INTRADAY`, `TimeWindow: 09:05~09:20`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-07-13.json), [pipeline_events_2026-07-14.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-07-14.jsonl)
  - 판정 기준: selected_families=soft_stop_whipsaw_confirmation, entry_split_order_plan, scale_in_split_order_plan, score65_74_recovery_probe, scalping_scanner_real_source_guard_runtime, score65_74_recovery_probe_strong_micro_override_runtime, entry_price_gap_profile_runtime, profit_stagnation_exit_runtime, latency_spread_relief_real_operator_override, quote_consistency_normalization, scalp_sim_candidate_window_expansion, scalp_sim_ai_budget_manager, ai_watching_score_smoothing_report_only, weak_pullback_entry_block_runtime, early_accel_recheck_runtime, real_pyramid_scale_in_quality_guard_runtime, sell_side_open_time_block_runtime, pre_submit_liquidity_relief_runtime, entry_opportunity_recheck_runtime, weak_context_late_entry_guard_runtime, rising_missed_normal_buy_bridge, persistent_operator_overrides_2026_06_26가 runtime event provenance에 찍히는지 확인한다.
  - 금지: 장중 관찰 결과로 runtime threshold mutation을 수행하지 않는다.
  - 실행 결과 (`2026-07-14 15:27 KST`): pipeline event `68,686`건 기준 selected/runtime family provenance는 `entry_split_order_plan=17`, `score65_74_recovery_probe=7`, `quote_consistency_normalization=130`, `scalp_sim_candidate_window_expansion=632`, `real_pyramid_scale_in_quality_guard_runtime=83`, `sell_side_open_time_block_runtime=2`, `rising_missed_normal_buy_bridge=24`, `scalp_sim_auto_approval=632`, `entry_cancel_wait_runtime=5`로 관측됐다. 나머지 selected family는 장중 자연 표본이 없거나 stage/event provenance에 family literal이 직접 찍히지 않은 상태다. 실제 rollback guard breach 계열(`rollback_guard_breach`, `rollback_triggered`)은 `0`건이며, 관찰 결과만으로 threshold/env/provider/bot/cap 변경은 수행하지 않았다.
  - 다음 액션: `provenance_partial_present_no_rollback_breach`. 자연 표본이 없는 selected family는 장후 post-apply attribution에서 적용 여부와 미관측 상태를 분리 확인한다.

- [x] `[SimProbeIntradayCoverage0714] sim/probe 관찰축 actual_order_submitted=false 및 source-quality 확인` (`Due: 2026-07-14`, `Slot: INTRADAY`, `TimeWindow: 09:35~09:50`, `Track: ScalpingLogic`)
  - Source: [threshold_cycle_ev_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-07-13.json), [pipeline_events_2026-07-14.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-07-14.jsonl), [observation_source_quality_audit_2026-07-14.json](/home/ubuntu/KORStockScan/data/report/observation_source_quality_audit/observation_source_quality_audit_2026-07-14.json)
  - 판정 기준: sim/probe 표본이 real execution과 분리되고 `actual_order_submitted=false` provenance가 유지되는지 확인한다.
  - 금지: sim/probe EV를 broker execution 품질이나 실주문 전환 근거로 단독 사용하지 않는다.
  - 실행 결과 (`2026-07-14 15:27 KST`): sim/probe/counterfactual 키워드 이벤트는 `28,191`건이지만, 이 중에는 `rising_missed_one_share_entry`와 실제 broker submit 경로가 섞여 있어 권한별로 재분리했다. sim-only/counterfactual 권한 표본 `1,499`건 중 `actual_order_submitted=false`는 `1,489`건, `broker_order_forbidden=true`는 `1,475`건이며, 누수 의심 `actual_order_submitted=true` `10`건과 `broker_order_forbidden=false` `24`건은 `order_bundle_submitted`, `holding_started`, `entry_advisory_prompt_context_only` 등 실제 주문/보유 또는 advisory 경로가 keyword filter에 섞인 것으로 분리했다. 순수 sim stage의 주요 표본은 `scalp_sim_panic_scale_in_blocked=458`, `scalp_sim_panic_action_deduped=244`, `scalp_sim_ai_holding_live_call=168`, `rising_missed_tp1_counterfactual_submit_safety=139`, `score65_74_recovery_probe_blocked=7`이다.
  - 다음 액션: `sim_probe_authority_split_verified`. sim/probe EV는 계속 source-only/observation evidence로만 사용하고, 실제 주문 경로와 섞인 키워드 결과는 authority 기준으로 재분리해 장후 report에 반영한다.

- [x] `[RisingMissedTP1PostRestartMonitor0714] Rising Missed TP1/Freshness Envelope 재기동 후 가동현황 및 반전 가드 점검` (`Due: 2026-07-14`, `Slot: INTRADAY`, `TimeWindow: 09:53~11:15`, `Track: ScalpingLogic`)
  - Source: [pipeline_events_2026-07-14.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-07-14.jsonl), [rising_missed_intraday_feedback_2026-07-14.json](/home/ubuntu/KORStockScan/data/report/rising_missed_intraday_feedback/rising_missed_intraday_feedback_2026-07-14.json), [rising_missed_intraday_feedback_2026-07-14.md](/home/ubuntu/KORStockScan/data/report/rising_missed_intraday_feedback/rising_missed_intraday_feedback_2026-07-14.md)
  - 실행 결과 (`2026-07-14 10:05 KST`): PID `48934`는 정상 유지됐고 TP1 평가는 block=`44`, pass=`0`, defer=`0`, input ready=`44/44`, trusted WS micro 결측=`0/44`, REST budget defer=`0`, `ka10004` fetch ok=`44/44`였다. freshest-age 선택은 WS=`40`, REST=`4`이고 conflict=`0`, WS/REST gap max=`54.407bp`다. lane 적격은 8회/5종목이며 lane 미충족=`36`, micro 조건 실패=`3`, WAIT bid-imbalance 미충족=`5`다. 적격 8회 모두 spread `21.77~107.14bp`로 `20bp` 상한을 넘고 fresh tick acceleration/micro-VWAP support가 `0/8`이었다. 10:05까지 종목별 관측 MFE 최대는 `+0.2191%`로 gross `+1.30%` 과차단 증거는 없지만, 각 20분 horizon은 미완료라 edge 판정에는 사용하지 않는다.
  - 반전 가드 판정: selector pass가 없어 `rising_missed_reversal_pre_submit_guard` 평가/block은 `0/0`이고, 재기동 이후 `reversal_up_watch`와 `reversal_up_volatile_watch`도 각각 `0`이라 과도 차단/제출 적정성은 `hold_sample`이다. 주문·threshold·provider·cap 변경이나 추가 재기동 근거로 사용하지 않는다.
  - source-quality handoff: `001820`에서 `first_seen_price=10662` 대 실제 호가 약 `89,000`의 anchor 오염이 18회 반복됐고, TP1 평가 44회 모두 tick acceleration/micro-VWAP source가 `missing`이었다. 오늘 `CodeImprovementWorkorderReview0714`에서 producer fix와 회귀 테스트 대상으로 재확인한다.
  - 2026-07-14 10:25 KST 사용자 지시 장중 source-quality hotfix: 09:24 잘못된 `001820/1Q K반도체TOP2+` source payload는 downstream identity guard에서 차단됐지만 scanner `recent_picks`에 먼저 저장되어 09:39 정상 `001820/삼화콘덴서`의 `first_seen_price=10662` anchor를 만든 것으로 확인했다. scanner promotion 직전 `daily_stock_quotes` 최신 code-name mapping 불일치를 차단하고 불일치 payload를 DB/WS/recent state에 기록하지 않으며, 레거시 name/가격 불연속 state와 TP1/reversal anchor는 symbol-local quote ratio로 복구한다. momentum은 signed FID 15가 확인된 fresh WS 0B 최근 10-tick의 수신시각 가속도와 recent-tick VWAP만 공급하고 REST/unsigned/future tick은 positive evidence에서 제외한다. selector threshold, sizing/cap/provider/TP/exit 및 기존 submit-safety는 변경하지 않으며 `$korstockscan-review-gate`, targeted validation, graceful restart/PID env 검증 전에는 runtime 반영 완료로 판정하지 않는다.
  - 2026-07-14 10:30 KST 적용 결과: initial review gate는 scanner/DB `102`, TP1/Freshness `729`, checklist parser `58`의 합계 `889 passed`와 compile/`git diff --check`를 통과했고, 운영 pass provenance 보완 후 scanner `99`와 TP1/Freshness `729`의 합계 `828 passed`를 재통과했다. 커밋 `074d265d`, `ef872cbe`를 main에 push하고 PID `48934 -> 63893 -> 65100` graceful restart를 완료했다. 최종 PID runtime env handoff는 status=`pass`, selected family=`25`, missing/mismatch=`0`이며 TP1 selector enabled/date와 one-share/normal bridge env를 유지했다. 최종 재기동 후 10:28:53~10:30 TP1 6건은 block=`6`, pass/defer=`0/0`, input ready=`6/6`, anchor state valid=`6/6`, effective source WS=`6/6`, REST budget defer=`0`이었다. signed WS 0B sample이 확보된 3건은 tick acceleration/micro-VWAP source가 trusted WS로 채워졌고, 재기동 직후 sample count 0인 3건은 기존대로 `missing`을 유지했다. scanner promotion 7건은 authoritative identity=`scanner_identity_ok`와 기존 promotion reason을 함께 기록했다. `001820` 직접 recovery runtime sample은 아직 없으므로 회귀 테스트와 다음 자연 재평가에서 provenance를 계속 확인한다.
  - 2026-07-14 10:50 KST post-restart 재점검: PID `71846`의 `10:42:14~10:50:30` cohort에서 TP1은 block/pass/defer=`34/0/0`, unique symbol=`9`, input ready=`34/34`, trusted WS micro fresh=`34/34`, REST budget defer=`0`, freshest source WS=`34/34`, anchor valid=`34/34`였다. block은 WAIT without `BID_IMBALANCE_SURGE`=`14`, micro conditions=`13`, lane ineligible=`7`이다. lane eligible 27건은 모두 spread `>20bp`였고, 추가 실패는 pressure/depth/top-depth 각 `15`, OFI `11`, positive momentum `9`, sell tape `1`이었다. signed WS momentum sample zero는 `3/34`, tick acceleration과 micro-VWAP가 모두 unavailable인 평가는 `9/34`였지만 TP1 trusted micro defer는 없었다. 관찰 가격 first-hit은 gross `+1.30%`=`1`, adverse `-0.70%`=`3`, neither=`30`이며 20분 horizon은 미완료다. gross target 1건 `011090/에넥스`는 최초 평가에서 lane delta `0.9321%`로 acceleration floor 미달, 이후 lane 진입 시에도 spread `221.64bp`와 momentum missing이 함께 남아 체결 가능한 명백한 과차단으로 보지 않았다. `rising_missed_reversal_pre_submit_guard`와 `reversal_up_watch`/`reversal_up_volatile_watch`는 평가·watch·submit 표본이 모두 `0`이므로 `hold_sample`이다. 오늘 강제 규칙에 따라 장중 threshold mutation과 추가 재기동은 수행하지 않고, 현행 selector/submit-safety를 유지한다.
  - 2026-07-14 11:00 KST 고정 cohort 재점검: PID `71846`의 `10:42:14~11:00:30` TP1 평가는 block/pass/defer=`60/0/1`, unique symbol=`12`, input ready=`60/61`, trusted WS micro fresh=`61/61`, anchor valid=`61/61`이었다. block은 micro conditions=`23`, WAIT without `BID_IMBALANCE_SURGE`=`20`, lane ineligible=`17`이고, freshest source는 WS=`59`, REST=`1`, conflict=`1`이다. 유일한 defer `365660/레몬헬스케어`는 WS/REST gap `158.328bp`가 `120bp` 상한을 넘어 fail-closed 및 recheck enqueue됐으며 REST budget defer는 `0`이다. signed WS momentum sample zero는 `7/61`, tick acceleration과 micro-VWAP가 모두 unavailable인 평가는 `15/61`이지만 trusted WS micro 자체 결측은 없었다. lane eligible 43건은 모두 spread `>20bp`였고 spread-only 실패 10건의 gross `+1.30%` first-hit은 `0`, 최대 관찰 MFE는 `+1.06996%`라 spread 완화 근거가 없다. 전체 관찰 first-hit은 gross `+1.30%`=`6`, adverse `-0.70%`=`8`, neither=`47`이고 target은 4종목이지만 lane 미충족, 복수 micro 실패 또는 하락 중 평가 뒤 반등한 표본이어서 명백한 단일 threshold 과차단으로 판정하지 않는다. selector pass가 없어 `rising_missed_reversal_pre_submit_guard`는 평가/block=`0/0`, `reversal_up_watch`/`reversal_up_volatile_watch`와 broker submit도 모두 `0`이므로 과도 제출 증거는 없다. 다만 TP1 block/defer는 현재 quality-guard 전용 reversal-up recheck 호출 경로로 전달되지 않아, 특히 하락 평가 뒤 급반등한 `365660` 같은 표본을 watch provenance로 남기지 못하는 source-only coverage gap이 확인됐다. 장중 threshold mutation이나 추가 재기동 없이 현행 selector/submit-safety를 유지하고, 이 handoff를 `CodeImprovementWorkorderReview0714`에서 broker authority가 없는 TP1-block-to-reversal 관측 보완으로 검토한다.
  - 2026-07-14 11:15 KST 사용자 승인 장중 mutation 예외 검토: PID `71846`의 고정 `10:42:14~11:15:30` cohort는 TP1 block/pass/defer=`110/0/1`, unique symbol=`17`, input ready=`110/111`, trusted WS micro fresh=`111/111`, anchor valid=`111/111`, REST budget defer=`0`이었다. freshest source는 WS=`96`, REST=`14`, conflict=`1`이며 REST가 `12.6%`의 quote를 실제 복구했다. momentum 동시 unavailable=`28/111`, signed WS momentum sample zero=`17/111`이지만 trusted micro 결측은 없었다. block은 lane ineligible=`45`, WAIT without `BID_IMBALANCE_SURGE`=`34`, micro conditions=`31`이다. 고정 cutoff 내 관찰 first-hit은 gross `+1.30%`=`7`, adverse `-0.70%`=`28`, neither=`76`이고 target은 5종목이다. target 7건 중 5건은 평가 당시 lane 미충족, 1건은 spread+OFI+momentum 복수 실패, 1건은 WS/REST `158.328bp` conflict fail-closed 뒤 반등이라 단일 조건 과차단이 아니다. 명백한 상승 후보 `228760/지노믹트리`는 actual watch delta `+5.04%`에서 target-first였지만 spread `115.43bp`와 OFI 음수가 함께 있었고, `+3%` 초과 lane-none 전체 9건은 target/adverse/neither=`1/7/1`이었다. spread-only 실패 13건도 target/adverse/neither=`0/1/12`, 최대 MFE=`+1.124063%`이므로 acceleration `3%` 상한, spread `20bp`, OFI 등 공통 threshold를 장중 완화하지 않는다. selector pass가 없어 `rising_missed_reversal_pre_submit_guard` 평가/block=`0/0`, `reversal_up_watch`/`reversal_up_volatile_watch`/rising-missed submit도 모두 `0`으로 과도 제출 증거는 없고, TP1 block/defer가 reversal-up recheck로 전달되지 않는 source-only coverage gap 판정은 유지한다. threshold/code/env 변경과 재기동 없이 현재 runtime을 유지하며 장후 `CodeImprovementWorkorderReview0714`에서 broker authority 없는 관측 handoff만 검토한다.

- [x] `[RisingMissedTP1ProbabilityGateRuntimeApply0714] TP1 probability-support gate 및 counterfactual submit-safety runtime 반영` (`Due: 2026-07-14`, `Slot: INTRADAY`, `TimeWindow: 11:31~12:00`, `Track: ScalpingLogic`)
  - Source: [rising_missed_one_share_entry.py](/home/ubuntu/KORStockScan/src/engine/scalping/rising_missed_one_share_entry.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [rising_missed_intraday_feedback.py](/home/ubuntu/KORStockScan/src/engine/monitoring/rising_missed_intraday_feedback.py), [pipeline_events_2026-07-14.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-07-14.jsonl)
  - 구현 결과 (`2026-07-14 11:31 KST`): selector policy를 `probability_support_v2`로 분리해 identity/lane/Freshness/AI explicit veto/fresh sell-dominated만 hard gate로 유지하고, bid imbalance/order flow/depth/momentum 4개 독립 family 중 2개 이상을 candidate pass로 사용한다. spread `20bp`, OFI·pressure 개별값, momentum 단독 결측, acceleration `3%` 초과는 candidate hard veto가 아니라 downstream submit-safety recheck risk로 기록한다. selector block/defer에는 `actual_order_submitted=false`, `broker_order_forbidden=true`, `runtime_effect=false`인 `rising_missed_tp1_counterfactual_submit_safety` 이벤트를 남기며 intraday feedback이 action/risk/reason/unique symbol을 별도 집계한다. 기존 quality/latency/micro/tick-speed/reversal/liquidity/overbought/price/broker/account/order/quantity/cooldown guard와 sizing/cap/provider/TP/exit는 변경하지 않았다. 고정 `10:42:14~11:15:30` 111건 replay는 기존 pass `0` 대비 v2 pass/block=`62/49`, unique pass symbol=`9`였고 pass 전부 downstream recheck required였다. core/report 커밋은 `62f030d1`, `cd8c575d`이며 최종 review gate는 관련 pytest `754 passed`, compile, live report build, parser, `git diff --check`를 통과했다. 현재 PID `71846`에는 아직 반영하지 않았다.
  - runtime 적용 결과 (`2026-07-14 11:36 KST`): 최종 no-defect review와 관련 pytest `754 passed`, compile, checklist parser, `git diff --check`를 재확인한 뒤 `./restart.sh`로 PID `71846 -> 92902` graceful restart를 완료했다. runtime env handoff는 status=`pass`, target date=`2026-07-14`, selected family=`25`, missing family/PID mismatch=`0`이었다. 새 PID 자연 표본 `006340/대원전선`은 policy=`probability_support_v2`, lane=`acceleration`, support=`depth,order_flow` (`2/2`)로 selector pass했고, counterfactual action=`RECHECK_REQUIRED` 및 risk=`spread_above_candidate_caution,true_ofi_nonpositive,momentum_support_weak,wait_without_bid_imbalance`를 source-only로 남겼다. 이후 기존 submit-safety는 별도 `ALLOW_NORMAL`로 판정했고 `order_leg_sent`와 `order_bundle_submitted actual_order_submitted=true`가 순서대로 관측되어 candidate pass가 broker 권한을 직접 부여하지 않고 기존 제출 가드를 통과한 경우에만 제출되는 계약을 확인했다.
  - `12:00 KST` post-restart 고정 cohort (`11:35:13~12:00:30`) 판정: TP1 pass/block/defer=`7/0/0`, unique pass symbol=`5`, trusted WS micro 결측=`0/7`, REST selected=`0`, REST budget defer=`0`이었다. pass 후 broker submit은 `1`, `real_weak_ai_micro_entry_block(source_quality_unknown)`은 `6`이었고, block 시점 이후 MFE 최대는 `+0.1627%`, gross `+1.30%` 도달은 `0`이라 submit-safety threshold를 완화하지 않았다. 유일한 submit `006340`은 defensive `12,160` 주문이 미체결 취소됐으며 pass 후보의 Freshness 가격 기준 20분 label은 target/adverse/pending=`1/4/2`였다. counterfactual은 `27`건/`14`종목, reason은 lane-not-eligible=`26`, insufficient-support=`1`, action은 recheck/hard-veto=`25/2`였고 source-quality가 닫힌 label은 target/no-hit/pending=`3/4/20`이다. `240810` target 1건은 후속 정상 lane pass로 재평가됐지만 `365660` 2건은 candidate block이 reversal-up recheck로 전달되지 않아 `+3.937%/+3.3943%` 반등 관측만 남았으며, reversal pre-submit block과 reversal-up normal/volatile watch/recheck는 모두 `0`이었다. 성숙 표본이 7건뿐이라 lane/runtime threshold는 유지하고 candidate-block-to-reversal source-only coverage gap은 `[CodeImprovementWorkorderReview0714]`가 후속 검토한다. 분석 중 feedback producer가 stale `current_price_observed`를 Freshness Envelope effective price보다 우선해 label을 왜곡하는 결함을 확인해 effective candidate price, fresh submit mark, signed 0B age `<=3초` scanner price만 사용하는 provenance와 counterfactual 20분 first-hit source-only label을 보완했다. 이 report-only 변경은 broker/runtime 권한이 없으며 bot 재기동 대상이 아니다.
  - `13:05 KST` market-first split 재기동 PID `120900` 고정 cohort (`12:43:57~13:05:00`) 판정: TP1 pass/block/defer=`0/6/0`, reason은 insufficient positive support=`4`, lane not eligible=`2`이고, input ready/trusted WS micro/`ka10004`/`ka10084` fetch ok=`6/6`, effective source WS=`6/6`, conflict 및 REST budget defer=`0`이었다. momentum sample zero는 `5/6`이지만 trusted micro 결측은 없었다. selector pass가 없어 실제 submit-safety, broker, reversal pre-submit guard, reversal-up normal/volatile watch는 모두 exposure=`0`이고 counterfactual 6건은 전부 source-only `RECHECK_REQUIRED`였다. 공식 `ka10080` 1분봉의 후보 다음 full-minute proxy로 보강하면 lane-not-eligible `002990/금호건설`, `049080/기가레인`은 각각 target-first MFE=`+2.964%`, `+1.820%`였고, support 1개 부족 4건은 target `0`, adverse-first `4`였다. 당일 전체 counterfactual earliest-per-symbol에서도 lane-none+support `>=2`는 13종목 target/adverse/no-hit=`4/0/9`여서 support 최소값 `2`는 유지하고 lane 절대차단만 완화하는 근거로 사용한다. 분봉 bar는 sub-second freshness/micro 근거가 아니라 사후 경로 proxy로만 사용했다.
  - 사용자 승인 장중 mutation (`probability_support_v3`): identity lane이 없더라도 trusted bid-imbalance/order-flow/depth/momentum 중 독립 support가 2개 이상이면 `support_reversal` candidate lane으로 승격해 기존 submit-safety에 전달한다. AI explicit veto, sell-dominated tape, Freshness/conflict/defer, support 최소 2개와 기존 quality/latency/tick-speed/reversal/liquidity/overbought/price/broker/account/order/quantity/cooldown veto는 유지한다. 고정 cohort replay는 기존 pass/block=`0/6`에서 `support_reversal` pass=`2`, 기존 acceleration/low-rebound support 부족 block=`4`가 되고, runtime 반영은 review gate와 graceful restart 뒤 새 PID provenance 확인 전까지 미완료다.
  - v3 runtime 적용 결과 (`2026-07-14 13:14~13:16 KST`): 관련 pytest `746 passed`, selector 재검증 `10 passed`, compile, parser, `git diff --check`와 최종 no-defect review 뒤 `./restart.sh`로 PID `120900 -> 131537` graceful restart를 완료했다. runtime env handoff는 status=`pass`, missing family/PID mismatch=`0`, runtime policy fail=`0`이고 WS login 및 0B/0D 수신이 정상이다. 새 PID 자연 표본 `049080/기가레인`에서 policy=`probability_support_v3`가 기록됐으며 해당 시점 support=`0`이라 lane-none block을 유지해 2개 support 하한도 확인했다. `support_reversal` 자연 pass와 downstream 실제 submit-safety 분포는 이후 표본으로 계속 관찰한다.
  - `13:40 KST` 고정 cohort (`13:14:45~13:40:00`) 판정: TP1 pass/block/defer=`21/13/0`, unique pass/block symbol=`14/10`, input ready와 trusted WS micro=`34/34`, effective quote source WS=`34/34`, `ka10004/ka10084` fetch ok=`34/34`, REST budget defer/conflict=`0/0`이었다. block은 lane-not-eligible=`7`, insufficient-support=`6`이며 support count는 `0` 8건, `1` 5건이라 support 최소 2를 완화할 근거는 없다. pass 21건의 terminal은 `real_weak_ai_micro_entry_block(source_quality_unknown)=16`, `latency_state_danger/true_ofi_below_floor=4`, broker submit=`1`이다. pass first-hit은 target/adverse/pending=`4/9/8`; `144960/뉴파워프라즈마`, `365660/레몬헬스케어`, `024060/흥구석유`의 source-quality block target-first와 `477850/마키나락스` latency block target-first가 확인됐지만, 대원전선/흥구석유/감성코퍼레이션 adverse-first도 있어 광범위 submit-safety bypass는 부적정하다. block target-first 2건은 모두 `365660` lane-none support 0에서 후속 support-reversal support 3 pass로 재평가되어 selector v3 재검사는 작동했고, 최종 병목은 legacy weak-AI micro `source_quality_unknown`이었다.
  - 주문/반전 판정: 실제 broker submit은 `097230/HJ중공업` 1건이다. first leg 13주 시장가는 후보 effective price `15,180` 대비 `+32.94bp`에 체결됐고 residual 13주 `15,090`은 미체결 취소됐으며, 포지션은 trailing exit 후 실현 `+0.43%`로 종료됐다. 시장가 first leg는 기회를 포착했으나 약 `0.30%` cost reserve만큼 slippage를 소모해 추적 대상이다. `rising_missed_reversal_pre_submit_guard` block=`0`이고 broker 경로 exposure는 이 1건뿐이라 과차단 근거는 없다. `reversal_up_watch=0`, `reversal_up_volatile_watch` recheck enqueue=`8`/2종목, direct submit=`0`, `broker_order_forbidden=true`로 과도 제출은 없었다.
  - 사용자 승인 장중 완화 (`13:40 KST` cohort 기반): TP1 v3 `support_reversal`, independent support `>=3`, trusted WS momentum, fresh TP1 input을 모두 만족하고 legacy weak-AI micro의 결측만이 유일한 차단 사유인 경우 `source_quality_unknown`을 bounded relief로 분리한다. context TTL은 `10초`, active date는 selector의 `2026-07-14`를 공유한다. 실제 weak buy-pressure/micro, pre-submit micro unavailable, latency/OFI/tick-speed/reversal/liquidity/price/broker/account/order/quantity/cooldown 및 sizing/cap/provider/TP/exit/hard safety는 계속 차단한다. rollback은 `KORSTOCKSCAN_RISING_MISSED_TP1_SOURCE_GAP_RELIEF_ENABLED=false`이며 review gate와 graceful restart/PID provenance 확인 전에는 runtime 반영 완료로 판정하지 않는다.
  - source-gap relief runtime 적용 결과 (`2026-07-14 13:53~13:54 KST`): initial review에서 measured weak pressure/micro 동시 관측 시 relief가 열릴 수 있는 권한 누수를 찾아 결측만이 유일한 차단 사유인 경우로 축소하고 재리뷰했다. 최종 관련 pytest `987 passed`, compile, checklist parser, env parse, `git diff --check`를 통과한 뒤 PID `131537 -> 144265` graceful restart를 완료했다. runtime env handoff는 status=`pass`, selected family=`25`, missing/mismatch=`0`, policy fail=`0`이고 새 PID env에서 relief enabled=`true`, TTL=`10`, support min=`3`, selector active date=`2026-07-14`를 확인했다. 계좌 동기화, WS login, 0B/0D 첫 수신도 정상이며 자연 relief/후속 submit 표본은 이후 runtime event로 관찰한다.
  - commit 전 최종 review supplement (`2026-07-14 13:58~13:59 KST`): 새 relief event에 Metric Decision Contract와 `observation_source_quality_audit` 명시적 stage contract가 없던 instrumentation gap을 보완했다. runtime/order 판단은 변경하지 않았고 관련 pytest `988 passed`, compile, parser, `git diff --check` 재통과 후 PID `144265 -> 146397` graceful restart를 완료했다. runtime env handoff는 status=`pass`, selected family=`25`, missing/mismatch=`0`, policy fail=`0`이며 새 PID의 계좌 동기화, WS login, 0B/0D 수신과 relief env enabled/TTL/support/date 일치를 확인했다.
  - `14:10 KST` 최종 재기동 PID `146397` 고정 cohort (`13:58:01~14:10:00`) 판정: TP1 evaluation은 pass/block/defer=`4/3/0`, 6종목이며 input ready, trusted WS micro, Freshness Envelope `ka10004` fetch ok가 모두 `7/7`이다. effective source는 WS=`7`, REST budget defer/conflict=`0/0`이고 source-gap relief 적용은 `0`이다. block 3건은 support `0/1/0`의 복합 약세였고 `105560/KB금융`은 14:05 block 뒤 14:07 support 2 pass로 재평가되어 실제 2-leg broker submit까지 진행됐다. pass terminal은 broker submit=`1`, latency DANGER=`2`, legacy weak-AI micro source-quality block=`1`이다. latency block `034020/두산에너빌리티`는 MFE/MAE=`0.00/-0.28%`, `049080/기가레인`은 `adverse_stop_first -1.82%`였고, weak-AI block `024060/흥구석유`는 effective price 기준 MFE=`+1.2048%`로 gross `+1.30%` 미달이며 submit 예정가 `12,480` 기준 edge는 더 작았다. counterfactual block 3건도 gross target first-hit=`0`이고 KB금융은 후속 pass로 회복됐다. KB금융 split은 first leg 1주 시장가가 평균 `183,100`에 즉시 체결되고 residual 1주 `181,900`은 TTL 취소돼 market-first/resolver-limit 정책과 broker 경로가 정상 작동했다. `rising_missed_reversal_pre_submit_block`, `reversal_up_watch`, `reversal_up_volatile_watch`는 모두 `0`이고 direct submit 권한 노출도 없다. 명백한 상승 과차단이나 과도 제출 증거가 없어 selector/source-gap/latency/reversal threshold와 runtime env를 유지하고 추가 재기동은 수행하지 않는다.
  - `14:25 KST` 고정 cohort (`13:58:01~14:25:00`) 재판정: TP1 pass/block/defer=`18/6/1`, 25 evaluations/18종목이며 input ready와 trusted WS micro는 `24/25`, effective quote source WS=`25`, REST budget defer/conflict=`0/0`이다. 유일한 micro defer `005860/한일사료`는 WS 0B age `6.47초`로 fail-closed됐지만 39초 뒤 fresh signed WS가 회복돼 support 3 pass로 재평가됐다. pass terminal은 latency DANGER=`13`, weak-AI source-quality=`3`, tick-speed=`1`, broker submit=`1`이다. latency 중 한일사료는 spread=`118.343bp`, WS age=`173.798ms`, delta=`3.79%`, true OFI=`0.0412`, signed WS tape buy ratio=`74.922%`, support=`3`인데 기존 direct-canary `90bp` cap에서 차단된 뒤 target-first MFE=`+8.3004%`였다. 반면 `095610/테스` `120.773bp`와 `270520/앱튼` `131.184bp`는 adverse-first여서 일괄 spread 완화는 부적정하다. weak-AI block 중 테스 acceleration pass는 support=`4`, trusted momentum/fresh micro, measured weak context=`false`인데 lane이 support-reversal이 아니라는 이유로 relief가 닫힌 뒤 target-first MFE=`+4.9696%`였다. 이를 근거로 날짜 제한 `90~120bp` 확장 latency tier를 추가해 support>=3, delta>=3%, true OFI>=0, WS age<=180ms, signed WS-only tape buy ratio>=60%, positive net volume/latest BUY를 모두 요구하고, >120bp/stale/non-spread/sell-dominated/explicit AI veto와 후속 submit-safety/broker guard는 유지한다. source-gap relief는 동일 support>=3/momentum/fresh trusted WS/결측-only 계약을 유지한 채 acceleration/low-rebound lane에도 적용한다. rollback은 `KORSTOCKSCAN_LATENCY_TRUE_OFI_DIRECT_CANARY_EXTENDED_SPREAD_ENABLED=false` 및 기존 source-gap relief env false다. shallow avg down은 KB금융이 `-0.34~-0.56%` 구간에서 hold/freshness 조건으로 미실행된 뒤 `-0.83%`까지 악화되고 cutoff `-0.56%`, HMM은 `-1.22~-1.66%`로 shallow PnL 범위 밖이어서 수익확대 과차단 근거가 없다. multi-leg 신규 submit/reprice는 없고 기존 KB금융 residual 미체결 취소 뒤 추가 진입이 유리했다는 증거도 없다. `rising_missed_reversal_pre_submit_block=0`, reversal-up normal watch=`0`, volatile recheck enqueue=`2`/테스 1종목, direct submit=`0`이다. runtime 반영은 review gate와 graceful restart/PID provenance 확인 전까지 미완료다.
  - 동적 latency/source-gap runtime 적용 결과 (`2026-07-14 14:36~14:38 KST`): review 중 REST `ka10084` BUY 결과가 확장 tier positive tape로 섞일 수 있는 provenance 누수를 찾아 signed WS-only 조건으로 축소하고 재리뷰했다. 관련 pytest `1,015 passed`, compile, env syntax, parser, `git diff --check` 통과 후 PID `146397 -> 159225` graceful restart를 완료했다. runtime env handoff는 status=`pass`, selected family=`25`, missing/mismatch=`0`, policy fail=`0`이고 새 PID env에서 extended enabled/date/max spread/support/delta/OFI/tape ratio와 source-gap env가 모두 일치한다. 계좌 동기화, WS login, 0B/0D 수신이 정상이며 자연 표본 `477850/마키나락스`에서 `extended_spread_active=true`가 기록됐다. 이 표본은 기존 <=90bp base tier가 latency를 허용해 `extended_tier_applied=false`였고, 후속 `pre_submit_entry_ai_authority_guard_block`이 유지돼 latency relief가 broker 권한이나 downstream guard 우회가 아님을 확인했다.
  - commit 전 최종 review supplement (`2026-07-14 14:44~14:45 KST`): 확장 tier의 signed WS-only 판정이 REST fresh count 부재만 확인해 provenance 미상 tick을 허용할 수 있고 source-gap relief `sample_floor`가 기존 support-reversal 전용 문구로 남은 결함을 확인했다. 확장 tier에 명시적 `kiwoom_0b_signed_trade_volume` 표본 `>=3`, REST 표본 `0`, unknown-source 표본 `0`을 요구하고 sample-floor 문구를 eligible lane 공통 계약으로 수정했다. 기존 <=90bp base tier와 submit-safety/broker guard는 변경하지 않았다. 관련 pytest `1,016 passed`, compile, env syntax, parser, `git diff --check` 통과 후 PID `159225 -> 161871` graceful restart를 완료했다. runtime env handoff는 status=`pass`, selected family=`25`, missing/mismatch=`0`, policy fail=`0`이며 새 PID env 일치, 계좌 동기화, WS login, 0B/0D 첫 수신을 확인했다.
  - 재기동 후 `15:00 KST` 고정 cohort (`14:44:22~15:00:00`) 판정: TP1 pass/block/defer=`8/4/0`, 12 evaluations/9종목이며 input ready와 trusted WS micro=`12/12`, effective quote source WS=`12`, REST budget defer/conflict=`0/0`이다. pass 8건은 latency=`4`, legacy weak-AI source-quality=`2`, tick-speed=`2`에서 모두 멈춰 broker/multi-leg/reprice 제출은 `0`이다. selector block 4건은 target-first=`0`으로 HLB no-hit, 현대차/SK이터닉스 pending MFE=`0.35/0.21%`, 기가레인 adverse-first=`-2.28%`였다. submit blocker의 freshness-aware event-price 재산출 결과 latency MFE는 레몬헬스케어 `0~0.42%`, 선도전기 `0%`, weak-AI는 두산 `0%`, 현대차 `0.83%`, tick-window는 시지메드텍 `0.39%`였고 추가 완화 근거가 없다. 씨피시스템 tick-speed block만 제출 mark `2,308원` 기준 MFE/MAE=`+1.386/-0.13%` target-first였으나 당일 tick-speed 4건 중 유일한 표본이고 당시 OFI=`-3.55`, micro bearish, 10-tick 감속=`0.357`이어서 즉시 runtime relief를 열지 않고 동적 허용 후보로 누적 관찰한다. shallow avg-down은 KB금융이 `-0.56% -> -0.88%`로 악화했고 buy pressure `40~60<85`, momentum/micro 조건도 약해 미실행이 적정하다. reversal pre-submit/normal/volatile와 broker submit은 모두 `0`으로 과도 제출·과차단 판정 표본이 없다. monitoring 중 report가 fresh TP1 `7,120~7,160원` 대신 stale scanner `7,920원`을 소비해 레몬헬스케어를 false target-first/MFE `+11%`로 만들던 source-quality 결함을 발견해, TP1/submit-block outcome이 effective TP1 price, explicit fresh submit mark, fresh scanner 0B price만 소비하도록 report-only 보완했다. runtime threshold/env와 PID는 변경하지 않는다.
  - hard-stop 수동관리 이관 runtime 적용 (`2026-07-14 15:19~15:21 KST`): 사용자 명시 승인에 따라 기본 OFF인 `KORSTOCKSCAN_HARD_STOP_MANUAL_HANDOFF_ENABLED`를 구현하고 operator override에서 `true`, 등록 실패 retry=`5초`로 적용했다. 실보유 SCALPING의 `scalp_hard_stop_pct`는 최종 quote 재검증 뒤 avg-down/broker sell 전, `scalp_preset_hard_stop_pct`는 REST-only 확인 뒤 emergency line 미도달 시 수동관리 exclusion 파일에 등록하고 해당 loop의 신규 hard-stop sell을 금지한다. 등록 실패도 sell fail-closed 후 retry하며, sim position, preset emergency, protect, daily-limit exit와 다른 손절/청산 rule은 기존 권한을 유지한다. review 중 기존 env exclusion도 file-persisted로 오기록할 수 있던 provenance를 `registered/active`로 보완했다. pytest=`889 passed`, compile, env syntax, `git diff --check` 통과 후 PID `161871 -> 174239` graceful restart를 완료했고 runtime env handoff status=`pass`, selected family missing/mismatch=`0/0`, PID env `enabled=true`, `retry=5`를 확인했다. 계좌 동기화, WS login, 0D 첫 수신, HOLDING pipeline 지속은 정상이다. 재기동 직후 자연 hard-stop 표본은 `0`이므로 실제 file 등록 event는 다음 발생 시 `hard_stop_manual_control_handoff`/`deferred`로 관찰한다. rollback은 enabled=`false`이며 수동관리 파일의 기존 사용자 변경은 커밋 대상에서 제외한다.
  - hard-stop 이관 post-commit 재리뷰 보완 (`2026-07-14 15:23~15:26 KST`): 최초 exclusion file 등록 실패 뒤 가격이 hard-stop 위로 회복되면 기존 구현은 helper에 재진입하지 않아 pending 이관이 끊길 수 있는 결함을 확인했다. 실패 시 exit rule/가격/손익/source context를 stock runtime state에 보존하고, 다음 HOLDING loop 시작점에서 quote/AI/가격 재평가보다 먼저 5초 backoff 재등록을 수행하도록 보완했다. retry 중에는 계속 broker sell을 금지하고 성공 시 수동관리 exclusion으로 전환하며, runtime OFF rollback은 pending context를 제거해 기존 흐름으로 복귀한다. targeted/full state-handler pytest=`24/891 passed`, compile, `git diff --check` 통과 후 PID `174239 -> 176313` graceful restart와 runtime env handoff status=`pass`, missing/mismatch=`0/0`, PID env enabled=`true`/retry=`5`를 확인했다. 계좌 동기화, WS login, 0D 첫 수신이 정상이며 수동관리 판정은 `005930=false`, `038500/010120=true`다. `005930` 수동관리 해제는 별도 config commit으로 기록했다.
  - 판정 기준: `$korstockscan-review-gate` 최종 no-defect와 parser validation을 통과하고 runtime apply가 승인된 경우에만 graceful restart 후 새 PID에서 policy=`probability_support_v3`, TP1 pass/block/defer, `support_reversal` lane, 실제 submit-safety veto 분포, counterfactual source-only provenance를 확인한다.
  - 금지: candidate pass를 broker 제출 권한으로 해석하거나 stale/conflict, quality, latency, tick-speed, reversal, liquidity, overbought, price, broker/account/order/quantity/cooldown veto를 우회하지 않는다. counterfactual event는 실주문·threshold auto-apply·provider/cap 변경 근거가 아니다.
  - 다음 액션: `runtime_applied_and_verified`. 이후 pass/block/defer, `support_reversal`, counterfactual risk와 실제 submit-safety veto 분포를 자연 표본으로 누적 관찰하며, rollback 필요 시 selector env를 `false`로 전환한다.

- [x] `[EntryScaleSplitRuntimeRepair0714] entry market-first multi-leg 및 split runtime 미적용 복구` (`Due: 2026-07-14`, `Slot: INTRADAY`, `TimeWindow: 12:05~12:45`, `Track: ScalpingLogic`)
  - Source: [entry_split_order_plan.py](/home/ubuntu/KORStockScan/src/engine/scalping/entry_split_order_plan.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [threshold_cycle_preopen_apply.py](/home/ubuntu/KORStockScan/src/engine/threshold_cycle_preopen_apply.py), [threshold_runtime_env_verify_2026-07-14.json](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_verify_2026-07-14.json)
  - 원인 판정: PID `92902`는 operator override의 2026-07-07 entry/scale split policy pin을 사용해 entry policy는 stale로 skip되고, scale-in policy도 당일 PREOPEN이 선택한 2026-07-13 artifact 대신 구 artifact를 소비했다. effective env `371`개 전수 대조에서 missing=`5`, mismatch=`4`가 모두 entry/scale split 계열이었고, 그 외 runtime env는 PID와 일치했다.
  - 구현 결과: SCALPING entry split first leg는 WAIT/spread 경고로 재축소하지 않고 policy first-leg weight를 시장가로 제출하며 residual leg는 기존 resolver limit price를 유지한다. 현재 fallback policy 기준 32주는 `16주 market + 16주 resolver limit`이다. total quantity, sizing/cap, shallow avg-down/scale-in, freshness/conflict, quality/latency/reversal/liquidity/overbought/price, broker/account/order/quantity/cooldown, provider/TP/exit 권한은 변경하지 않았다. `runtime_apply_allowed=false`인 entry artifact는 2026-07-14 한정 operator fallback 권한이 없으면 fail-closed하고, 시장가 broker price `0`은 별도 guard/reference price로 safety와 counterfactual 분석을 유지한다.
  - 검증/적용 결과 (`2026-07-14 12:46 KST`): 최초 targeted pytest `891 passed`, compile, `git diff --check` 후 PID `92902 -> 119698` graceful restart와 runtime handoff/effective env `371/371` 일치를 확인했다. 첫 자연 entry split 후보는 broker 호출 전 `order_leg_request` provenance에서 bundle/leg 중복 key 예외를 노출했으며 실제 주문은 제출되지 않았다. common+leg를 먼저 단일 dict로 병합하고 residual leg 값이 우선하도록 보완한 뒤 review/fix/re-review와 targeted pytest `892 passed`, compile, `git diff --check`를 통과하고 PID `119698 -> 120900`으로 다시 graceful restart했다. 최종 commit 전 리뷰에서 selected split `ENABLED=false`와 market-first active-date 불일치를 verifier fail 조건으로 보완해 targeted pytest `894 passed`와 parser를 재통과했다. 최종 handoff는 status=`pass`, selected family=`25`, pid_missing/pid_mismatches=`0`, entry/scale policy audit=`pass/pass`, effective env=`371/371`이며 계좌 동기화, WS 로그인, 0B/0D 수신, 스캐너 루프 재개 후 한 scanner 주기 이상 추가 오류가 없었다.
  - 미적용 runtime 추가 점검: 추가 미적용 env는 `0`이다. `persistent_operator_overrides_2026_06_26`은 개별 env mapping이 없는 manifest rollup 이름이지만 operator+PREOPEN effective env 전수 대조로 실제 PID 반영을 확인했다. `lifecycle_bucket_discovery_sim_auto_approval`의 live-auto apply OFF는 sim-only 계약에 따른 의도된 상태이며 미적용 결함이 아니다. 최종 PID의 정상 entry/scale split 자연 주문 표본은 아직 없으므로 env/hook 반영과 자연 실행 관측을 구분한다.
  - rollback: `KORSTOCKSCAN_ENTRY_SPLIT_MARKET_FIRST_LEG_ENABLED=false`, `KORSTOCKSCAN_ENTRY_SPLIT_OPERATOR_FALLBACK_ENABLED=false`, `KORSTOCKSCAN_ENTRY_SPLIT_ORDER_POLICY_ENABLED=false`. 날짜 제한 키는 2026-07-15부터 자동 비활성화된다.
  - 3-leg 경로 보완 (`2026-07-14 17:15 KST`): entry 빈 policy bucket은 `passive_wide_or_weak`이면서 요청수량 `>=6`일 때만 50% market-first + 25/25% resolver residual의 최대 3-leg fallback을 사용하고 urgent/balanced 및 3~5주는 기존 2-leg를 유지한다. AVG_DOWN allocator의 2-leg 하드캡은 최대 3-leg로 열되, 자동 정책 승격은 real post-submit observed sample `>=20`, 1-tick touch `>=70%`, 2-tick touch `>=40%`, missed-upside proxy `<40%`를 모두 요구하며 얇거나 즉시상승 비중이 높은 표본은 diagnostic-only다. policy requested leg, qty별 max leg, clipped 여부와 execution variant를 분리해 outcome attribution 오염을 막았고 `runtime_three_leg_candidate_count`를 daily/EV/runtime approval consumer까지 전달했다. 수량 보존, 최소 1주, PYRAMID 제외, stale/broker/account/order/quantity/cooldown guard는 유지한다. targeted pytest=`35+1+1+23+7 passed`, compile 및 `git diff --check` 통과 상태이며 현재 PID에는 미적용이고 재기동은 수행하지 않았다.
  - 3-leg review/apply (`2026-07-14 17:32 KST`): producer/consumer와 submit loop를 재리뷰해 entry 각 leg가 기존 late-drift/liquidity/overbought/submit revalidation을 거치고 AVG_DOWN runtime 3-leg 승격이 `actual_order_submitted=true` 실주문 anchor와 source-quality gate를 요구함을 확인했다. targeted pytest=`181+869 passed`, compile, checklist parser, `git diff --check`를 통과한 뒤 commit=`0d73bc46`으로 묶고 PID `206471 -> 219527` graceful restart를 완료했다. runtime env handoff status=`pass`, selected family missing/mismatch=`0/0`, entry/scale policy audit fail=`0`이며 새 PID에서 HOLDING 4종목 복원, WS 연결/로그인, 0B/0D 실수신과 scanner loop 재개를 확인했다. 신규 자연 3-leg 주문 표본은 아직 없으므로 적용 확인과 execution outcome 판정은 분리한다.

- [x] `[IntradaySourceQualityGateCheck0714] 장중 raw source-quality 결손/unknown 조기 경보 및 튜닝 입력 차단 준비 확인` (`Due: 2026-07-14`, `Slot: INTRADAY`, `TimeWindow: 14:20~14:35`, `Track: RuntimeStability`)
  - Source: [pipeline_events_2026-07-14.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-07-14.jsonl), [threshold_events_2026-07-14.jsonl](/home/ubuntu/KORStockScan/data/threshold_cycle/threshold_events_2026-07-14.jsonl), [observation_source_quality_audit_2026-07-14.json](/home/ubuntu/KORStockScan/data/report/observation_source_quality_audit/observation_source_quality_audit_2026-07-14.json), [observation_source_quality_audit.py](/home/ubuntu/KORStockScan/src/engine/observation_source_quality_audit.py)
  - 판정 기준: 장중 `PYTHONPATH=. .venv/bin/python -m src.engine.observation_source_quality_audit --target-date 2026-07-14 --write` 재감사를 실행하거나 최신 산출물을 확인해 `hard_blocking_contract_gap_count`, `hard_blocking_excluded_row_count`, `tuning_input_allowed`, `raw_row_exclusion_applied`, `unknown_token_stage_count`, `review_warning_count`를 기록한다.
  - 금지: hard contract gap 또는 unknown-token warning을 답변에만 남기지 않는다. 결손 row/window는 튜닝 입력 제외 또는 workorder handoff 대상으로 고정하고, broker/order/provider/cap/bot/threshold 변경 근거로 사용하지 않는다.
  - 실행 결과 (`2026-07-14 15:28 KST`): `observation_source_quality_audit --target-date 2026-07-14 --write`를 재실행해 JSON/MD 산출물을 생성했다. audit status=`pass`, `tuning_input_allowed=true`, event_count=`68,630`, stage_count=`106`, `hard_blocking_contract_gap_count=0`, `hard_blocking_excluded_row_count=0`, `unknown_token_stage_count=0`, `review_warning_count=0`, `warning_stage_count=0`이다. `raw_row_exclusion_applied=true`이며 exclusion manifest는 `data/source_quality/raw_row_exclusion/2026-07-14_20260714T152745427972+0900/manifest.json`이다.
  - 다음 액션: `defective_rows_excluded`. hard gap/unknown warning은 없으므로 tuning input은 장중 기준 허용하되, postclose 산출물 생성 후 `PostcloseSourceQualityGateReview0714`에서 동일 gate를 재확인한다.

## 장후 체크리스트 (20:05~21:55)

- [ ] `[RisingMissedNxtSessionObservation0714] NXT window TP1/WS route/micro/order resolution 관측 분리` (`Due: 2026-07-14`, `Slot: POSTCLOSE`, `TimeWindow: 16:00~20:00`, `Track: ScalpingLogic`)
  - Source: [kiwoom_websocket.py](/home/ubuntu/KORStockScan/src/engine/kiwoom_websocket.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [rising_missed_intraday_feedback.py](/home/ubuntu/KORStockScan/src/engine/monitoring/rising_missed_intraday_feedback.py), [pipeline_events_2026-07-14.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-07-14.jsonl)
  - 구현 범위: 실제 WS item 기준 type별 `_AL/_NX` route, NXT session bucket, 절대 0B/0D receive age, `fresh_active/fresh_trade_quiet/stale_or_conflicted`, Freshness Envelope effective quote source, NXT eligibility, 요청/유효 주문유형과 market-to-best remap을 observation-only provenance로 기록한다. 기존 TP1 selector, freshness/micro authority, submit-safety, sizing/cap/provider/TP/exit/hard safety는 변경하지 않는다.
  - 판정 기준: review gate와 targeted test/parser 통과 후 graceful restart하고 새 PID에서 0B/0D route가 실제 수신 item과 일치하는지 확인한다. NXT 자연 표본은 KRX와 분리해 TP1 pass/block/defer, input ready, `fresh_trade_quiet`, REST quote 선택, 주문유형 remap과 fillability를 관찰한다.
  - 금지: `fresh_trade_quiet`를 positive OFI로 변환하거나 이 단일 장중 표본으로 threshold 완화, broker/order guard 우회, provider/cap 변경, full-live 승인 근거로 사용하지 않는다.
  - 적용 결과 (`2026-07-14 15:51 KST`): WS canonical snapshot에 type별 실제 item/suffix/route를 보존하고 TP1 resolver가 NXT session bucket, absolute 0B/0D age, NXT eligibility, `fresh_active/fresh_trade_quiet/stale_or_conflicted`, effective quote source를 observation-only로 기록하도록 구현했다. TP1 pass context는 60초 이내 submit/broker event에만 전달하며 requested/effective order type과 NXT market-to-best remap을 기록한다. intraday feedback JSON/MD에는 NXT session/micro/route/REST quote/order remap 집계를 추가했다. 첫 재기동 후 persisted WS snapshot allowlist가 route를 제거하는 결함을 발견해 producer를 보완했고, review/fix/re-review 후 targeted pytest `1,109 passed`, compile, parser, `git diff --check`를 통과했다. 최종 PID `184522`의 runtime env handoff는 status=`pass`, selected family missing/mismatch=`0/0`이며 계좌 동기화, WS login, 0B/0D 수신이 정상이다. persisted snapshot에서 `010120/105560`의 실제 item=`*_AL`, suffix=`_AL`, 0B/0D route=`krx_nxt_integrated`를 확인했다. selector/threshold/sizing/cap/provider/TP/exit/hard safety 권한은 변경하지 않았다.
  - NXT open 관측 (`2026-07-14 16:00~16:10 KST`): PID `184522`(시작 `15:50:50`) 기준 unique evaluation/symbol=`9/5`, TP1 pass/block/defer=`4/4/1`, input ready=`8/9`, micro state fresh-active/trade-quiet=`8/1`, effective quote WS/REST=`9/0`, `ka10004/ka10084` fetch ok=`9/9`, REST budget defer/conflict=`0/0`이다. 0B/0D route는 모두 `krx_nxt_integrated`, effective venue는 모두 `NXT`였다. 유일 micro defer `319660/피에스케이`는 0B age=`4039ms`, 0D age=`0ms`의 `fresh_trade_quiet`에서 `tp1_micro_ws_unavailable`로 fail-closed하고 recheck를 예약했다. selector block은 lane-none `3`, support 부족 `1`이며 `399720/가온칩스`, `095610/테스`는 각각 36초, 29초 후 자연 pass로 재평가됐다. pass 4건의 terminal은 latency DANGER=`3`, tick-speed=`1`, broker/order/multi-leg/reprice=`0`이다. 16:10 시점 submit blocker 사후 MFE/MAE는 두산에너빌리티=`0/0%`, 가온칩스=`0/0%`, HD현대중공업=`+0.0536/+0.0536%`, 테스=`0/-2.5287%`로 명백한 missed winner는 없었다. shallow avg-down은 KB금융 `-1.43~-1.59%`로 configured shallow PnL 범위를 벗어나 미실행이 적정했다. reversal pre-submit/normal watch/direct submit=`0`, volatile recheck enqueue=`1`이고 broker forbidden이 유지됐다. NXT에서 wide spread와 trade-quiet가 관찰됐지만 20분 first-hit label과 실제 fill/multi-leg 표본이 없으므로 KRX 공통 threshold를 변경하지 않고, 후속 근거가 쌓일 경우에만 NXT 전용 runtime family로 분기한다.
  - NXT entry 연장 관측 (`2026-07-14 16:00~16:25 KST`): 누적 unique evaluation/symbol=`14/9`, TP1 pass/block/defer=`7/4/3`, input ready=`11/14`, micro 결측률=`3/14(21.43%)`, micro state fresh-active/trade-quiet=`12/2`, effective quote WS/REST=`14/0`, `ka10004/ka10084` fetch ok=`14/14`, REST budget defer/conflict=`0/0`이다. 추가 defer `003540/대신증권`은 trusted true OFI sample=`1`, `459510/나우로보틱스`는 0B/0D age=`3019.874/2915.768ms`의 warm-up/trade-quiet 결측이었다. 나우로보틱스는 25초 후 자연 pass로 회복돼 recheck 경로가 작동했다. pass 7건의 terminal은 latency DANGER=`4`, tick-speed=`3`, broker/order/multi-leg/reprice=`0`이다. submit blocker provisional MFE는 최대 `+0.0742%`(코리아써키트)이고 테스 MAE=`-2.5287%`, 나우로보틱스=`-0.0409%`로 명백한 상승 과차단은 없었다. latency 4건은 spread=`70.423~116.054bp`에서 stale/low true-OFI가 동반됐고, tick-speed 3건도 MFE=`-0.0409~+0.0742%`로 NXT 동적 band 완화 근거가 없다. shallow avg-down은 KB금융이 `-1.43% -> -1.76%`로 악화해 미실행이 적정했다. reversal pre-submit/normal watch/direct submit=`0`, volatile recheck enqueue=`1`이며 과도 제출은 없었다. 다만 scanner watch eviction 후 NXT blocked/defer 종목의 후속 가격 수집이 끊겨 16:01 평가도 20분 first-hit label이 생성되지 않은 instrumentation gap을 확인했다. 이 결손은 threshold 완화 근거가 아니며, NXT cohort만 대상으로 하는 post-block bounded price sampler/라벨 회수 보완 후 판정해야 한다.
  - NXT post-block sampler 적용 (`2026-07-14 16:45 KST`): selector block/defer의 NXT session/evaluation ID만 등록하고 scanner target 종료 뒤에도 WS 구독을 최대 20분 유지하는 source-only sampler를 구현했다. 표본은 REST를 호출하지 않으며 absolute 0B receive age `<=3000ms`, 실제 item suffix `_AL/_NX`, route `krx_nxt_integrated|nxt_only`를 모두 만족할 때만 `current_price_observed`를 기록한다. per-evaluation dedupe, active cap=`24`, interval=`15초`, horizon=`1200초`, source-gap/completion/MFE/MAE/first-hit provenance와 source-quality stage contract를 추가했다. review 과정에서 loop 시작시각을 freshness 기준으로 재사용하면 장시간 loop 뒤 새 tick이 `0ms`로 오판될 수 있는 결함을 발견해 sampler 호출 순간의 실제 시각을 사용하도록 보완했고, active-cap skip event I/O를 lock 밖으로 이동했다. targeted pytest는 focused=`8 passed`, report full=`22 passed`, TP1/NXT bundle=`13 passed`, prune=`1 passed`, source-quality contract=`3 passed`이고 compile, parser, `git diff --check`를 통과했다. 사용자 승인으로 `.venv`에 `black 26.5.1`을 설치했으며 신규 runtime/report/test 블록의 독립 Black check는 통과했다. 저장소 전체 check는 기존 6개 파일의 pre-existing formatting debt 때문에 실패해 무관한 전체 파일 포맷 churn은 만들지 않았다. graceful restart 후 PID=`201784`, env handoff status=`pass`, selected family missing/mismatch=`0/0`, sampler env 6개가 실제 process env에 반영됐고 WS login 및 0B/0D 수신이 정상이다. 재기동 후 첫 48초에는 자연 TP1 block/defer가 없어 sampler event는 아직 `0`건이다. selector/submit-safety/broker/order/sizing/cap/provider/TP/exit/hard safety와 KRX runtime은 변경하지 않았다.
  - 최종 review gate 재기동 (`2026-07-14 16:57 KST`): active-cap event I/O lock 분리와 신규 블록 formatter 검증까지 재리뷰한 뒤 graceful restart를 다시 수행했다. 새 PID=`206471`, runtime env handoff status=`pass`, selected family missing/mismatch=`0/0`, NXT sampler env 6개 process 반영을 확인했다.
  - NXT 17:20 관측 (`2026-07-14 16:00~17:20 KST`): evaluation/symbol=`40/15`, TP1 pass/block/defer=`19/17/4`, input ready=`36/40`로 WS micro 결측률은 `10.00%`다. micro state fresh-active/trade-quiet=`38/2`, effective quote WS/REST=`40/0`, `ka10004/ka10084` fetch ok=`40/40`, REST budget defer/conflict=`0/0`이며 모든 0B/0D route는 `krx_nxt_integrated`, effective venue는 `NXT`였다. selector block은 lane-none=`11`, positive support 부족=`5`, hard-negative=`1`; submit terminal은 latency=`13`, tick-speed=`3`, weak-AI source-quality unknown=`2`, broker bundle=`1`이다. 차단 사후 최대 MFE는 weak-AI HLB=`+0.8341%`, latency 가온칩스=`+0.5803%`, 로킷헬스케어=`+0.4484%`로 gross TP1 `+1.30%`에 미달했고, 테스는 MFE=`+0.2299%`/MAE=`-2.5287%`였다. 따라서 명백한 상승 과차단 근거가 없어 KRX 공통 threshold와 NXT threshold를 변경하지 않았다. latency 표본 spread=`55.804~122.850bp`와 WS age/true-OFI/tape 조건이 혼재했고 target-first 완료 라벨이 없어 동적 band도 유지했다. 마녀공장 24주는 NXT market-to-best 12주가 평균 `16,400`에 즉시 체결되고 residual 12주 `16,330`은 98.5초 뒤 미체결 취소됐으며 reprice event는 없었다. 이는 단일 자연 표본이므로 NXT 전용 residual reprice 후보로만 남긴다. shallow avg-down은 KB금융 `-1.43~-2.03%`로 shallow PnL 범위 밖이었고, 마녀공장은 `-0.35~-0.66%` 구간에서도 보유시간 상한과 buy pressure가 함께 미달해 미실행이 적정했다. reversal pre-submit/normal watch/direct submit=`0`, volatile recheck enqueue=`3`으로 과도 제출은 없었다. sampler는 registration/evaluation=`7/7`, samples fresh/source-gap=`44/85`, completion=`0`; 16:50~16:54 registration은 16:57 재기동으로 in-memory horizon이 초기화됐고 새 PID의 첫 registration이 17:03이라 17:20 cutoff에는 20분 완료가 불가능했다.
  - NXT 17:50 관측 (`2026-07-14 16:00~17:50 KST`): PID `219527`는 cutoff까지 정상 유지됐다. evaluation/symbol=`55/20`, TP1 pass/block/defer=`28/23/4`, input ready=`51/55`로 WS micro 결측률은 `7.27%`다. micro fresh-active/trade-quiet=`53/2`, effective quote WS/REST=`55/0`, `ka10004/ka10084` fetch ok=`55/55`, REST budget defer/conflict=`0/0`, 0B/0D route는 전부 `krx_nxt_integrated`였다. pass 28건의 terminal은 latency=`20`, tick-speed=`3`, weak-AI source-quality unknown=`4`, broker bundle=`1`이다. 완료된 NXT post-block sampler 2건은 가온칩스 `MFE=+0.1934%/MAE=-0.3868%`, 피에스케이 `MFE=0%/MAE=-0.4926%`로 모두 `no_hit_within_20m`; 누적 sample fresh/source-gap=`117/166`, completion=`2/13`이다. submit blocker 최대 MFE는 테스=`+1.1682%`, 로킷헬스케어=`+1.1211%`, HLB제약=`+1.0145%`로 모두 gross TP1 `+1.30%` 미달이다. 테스는 true-OFI sample=`50`/tape buy=`10.714%`, 로킷은 WS age=`440.948ms`/extended support=`2`, HLB제약은 signal=`0`/extended support=`2`여서 NXT latency dynamic band 완화 근거가 없다. 신규 PID 이후 broker/multi-leg/reprice 표본은 `0`이고, 누적 주문은 기존 마녀공장 2-leg 1건뿐이다. shallow avg-down은 KB금융 PnL=`-1.43~-2.03%`로 shallow 범위 밖이고, 마녀공장은 shallow PnL 적합 50회가 있었지만 hold/buy-pressure/기타 조건 동시충족=`0`이라 미실행이 적정했다. reversal pre-submit/normal watch/direct submit=`0`, volatile recheck enqueue=`4`이며 과도 제출은 없었다. KRX 공통 threshold와 NXT runtime은 변경하지 않았다. source-only gap으로 terminal blocker `27`건 중 `3`건의 direct NXT venue provenance 누락, sampler completion의 raw max/min move가 feedback MFE/MAE로 매핑되지 않는 문제, 재기동 시 active sampler horizon 미복구를 확인했으며 전략 완화와 분리한 NXT observation 보완 후보로 남긴다.
  - NXT source-only gap 보완 (`2026-07-14 18:18 KST`): TP1 pass context를 symbol+scanner promotion ID로 격리한 process cache에 보존해 후속 stock 객체가 교체돼도 terminal event가 evaluation/session/venue provenance를 회수하도록 했다. 판정 freshness는 기존 `60초`를 유지하고 logging provenance만 동일 promotion에서 최대 `20분` 보존하며, 60초 초과는 `submit_context_fresh=false`로 기록한다. active NXT post-block sampler는 `data/runtime/rising_missed_nxt_post_block_sampler_state.json`에 source-only 상태를 원자 저장하고 동일 active date, NXT route/session, 미만료 horizon만 복구한 뒤 WS 재구독을 요청한다. 만료 sampler는 horizon 외 가격 오염을 막기 위해 복구하지 않는다. completion의 raw first-hit/max/min move는 feedback row의 `first_hit_move_pct`, `mfe_after_block_pct`, `mae_after_block_pct`로 직접 매핑했다. selector/submit-safety/broker/order/sizing/cap/provider/threshold 및 KRX/NXT runtime 값은 변경하지 않았고, `18:18 KST` 당시 PID에는 미반영 상태였다.
  - NXT source-only gap review gate (`2026-07-14 18:24 KST`): producer/consumer 재리뷰에서 만료 직후 0B tick이 20분 horizon에 포함될 수 있는 경계와 completion aggregate가 counterfactual 대표 label row에 소비되지 않는 결함을 찾아 보완했다. 최종 targeted pytest는 feedback report=`22 passed`, TP1/NXT context·sampler=`7 passed`, WS subscription retention=`1 passed`, source-quality contract=`3 passed`이며 compile, checklist parser count=`26`, `git diff --check`를 통과했다. 미해결 코드리뷰 finding은 없다. `18:24 KST` 당시에는 재기동 전이라 신규 source-only 계측이 runtime 미반영이었다.
  - NXT source-only gap graceful restart (`2026-07-14 18:28 KST`): `./restart.sh`의 restart flag/supervisor 경로로 PID `219527 -> 235884` 교체를 완료했다. runtime env handoff는 status=`pass`, selected family missing/mismatch=`0/0`, runtime policy fail=`0`이며 새 PID 환경에서 TP1 selector enabled/date와 NXT post-block sampler 6개 env를 확인했다. 계좌 동기화, WS 로그인, 0B/0D 첫 실수신과 main loop 진입이 정상이며 source-only gap 보완이 runtime에 반영됐다.
  - NXT 18:45 재기동 후 관측 (`2026-07-14 18:28:38~18:45:00 KST`): PID `235884`는 cutoff까지 정상 유지됐다. 독립 NXT cohort evaluation=`11`, TP1 pass/block/defer=`5/5/1`, input ready=`10/11`로 WS micro 결측률은 `9.09%`다. micro state는 전부 `fresh_active`, effective quote WS/REST=`10/1`, WS/REST gap 최대=`28.249bp`, `ka10004/ka10084` bounded envelope 입력=`11/11`, final reserve allowed=`11/11`, REST budget defer/conflict=`0/0`이며 모든 0B/0D route와 effective venue가 `krx_nxt_integrated`/`NXT`였다. terminal은 weak-AI source-quality unknown=`3`, latency=`1`, tick-speed=`1`, broker/order/multi-leg/reprice=`0`; 신규 provenance 보완 후 terminal `5/5`가 direct NXT venue와 evaluation ID를 보존했다. block/defer sampler registration=`6`, sample fresh/source-gap=`43/24`, completion=`0`이며 active state JSON이 원자 갱신됐다. strict cutoff counterfactual은 미래나노텍 lane-none 3건이 adverse-stop-first이고 MFE/MAE가 각각 `+0.399/-0.798%`, `+0.133/-1.061%`, `0/-0.930%`; 나머지 lane-none/defer의 cutoff 최대 MFE는 `0%` 또는 fresh sample 없음이었다. submit terminal 사후 최대 MFE는 HLB제약 weak-AI=`+0.367%`, 미래나노텍 latency=`+0.266%`, 테스 weak-AI=`+0.116%`, 가온칩스 tick-speed=`+0.096%`다. latency 표본은 spread=`66.578bp`, true OFI=`-0.0001`, top-depth ratio=`0.276`, signed tape buy ratio=`40%`와 sell-dominated가 동반돼 동적 band 미허용이 적정했다. weak-AI source-quality unknown은 TP1 pass 뒤 micro consumer provenance가 끊기는 동적 recheck 후보지만 3건 모두 MFE가 낮아 direct 허용이나 threshold 완화 근거는 없다. 신규 submit이 없어 entry price/multi-leg/reprice 적정성은 판정 보류다. shallow avg-down은 KB금융 `-1.92~-1.97%`로 shallow PnL 범위 밖이고, 마녀공장 `-0.23~-0.66%`는 PnL 범위 진입이 있었으나 hold=`6793~7705초`가 max `240초`를 초과하고 buy pressure도 주로 기준 `85` 미달이라 동시 충족이 없었다. rising-missed reversal pre-submit/normal/direct submit=`0`, volatile recheck enqueue=`1`(테스)이며 해당 표본 MFE=`+0.116%`로 과도 제출이나 과도 차단 근거가 없다. 따라서 KRX 공통 로직과 기존 NXT runtime을 변경하지 않고, NXT 전용 후보는 완료 label이 축적될 때까지 source-only 관측으로 유지한다.
  - NXT 19:15 재기동 후 누적 관측 (`2026-07-14 18:28:38~19:15:00 KST`, 증분 `18:45~19:15`): PID `235884`는 cutoff까지 정상 유지됐다. 누적 evaluation=`16`, TP1 pass/block/defer=`7/8/1`, input ready=`15/16`로 WS micro 결측률은 `6.25%`; 증분은 evaluation=`5`, pass/block/defer=`2/3/0`, input ready=`5/5`다. 누적 micro state는 전부 `fresh_active`, effective quote WS/REST=`15/1`, `ka10004/ka10084` envelope 입력과 final reserve allowed=`16/16`, REST budget defer/conflict=`0/0`이며 terminal direct NXT venue/evaluation provenance는 `7/7` 보존됐다. 누적 post-block sampler registration/sample fresh/source-gap/completion=`9/79/226/6`; completion outcome은 adverse-stop-first=`3`, no-hit=`2`, source-gap-no-fresh=`1`, gross-target-first=`0`이고 source-quality pass/insufficient=`4/2`다. 완료 MFE/MAE는 미래나노텍 adverse 3건=`+0.399/-0.798%`, `+0.133/-1.061%`, `0/-0.930%`, no-hit=`-0.133/-0.666%`; 칩스앤미디어 no-hit=`+0.189/+0.189%`이며 원익머트리얼즈는 fresh 0건 source-gap이다. 증분 block 3건의 provisional 최대 MFE는 피에스케이=`+0.244%`, 테스=`+0.231%`, 가온칩스=`0%`로 명백한 상승 과차단은 없다. 누적 terminal은 weak-AI=`3`, latency=`2`, tick-speed=`2`, broker/order/multi-leg/reprice=`0`; 최대 사후 MFE는 HLB제약 weak-AI=`+0.367%`이고 증분 한국피아이엠 tick-speed=`+0.159%`, 두산에너빌리티 latency=`0%`다. 미래나노텍 latency는 sell-dominated·top-depth `0.276`이었고, 두산에너빌리티 latency는 top-depth=`1.4104`, extended support=`4`, non-sell-dominated였으나 true OFI sample=`67<75`, price delta=`0%`, 사후 MFE=`0%`여서 direct 동적 band 허용 대신 기존 volatile recheck가 적정하다. tick-speed 두 표본은 ratio=`0.338/0.327`, recent 5-tick span=`74/196초`, stale tick context와 낮은 MFE가 동반됐다. shallow avg-down은 KB금융 PnL=`-2.14~-1.92%`로 shallow 범위 충족 `0/56`; 마녀공장은 PnL 충족 `47/55`였지만 hold 충족=`0`, pressure 충족=`1`, 전체 동시 충족=`0`이라 미실행이 적정했다. reversal pre-submit/normal/direct submit=`0`, volatile recheck enqueue=`2`(테스, 두산에너빌리티)이며 직접 제출은 없었다. 따라서 strategy/threshold/runtime mutation은 하지 않는다. 다만 sampler WS 0B source-gap=`74.10%`는 selector 평가 micro 결측과 별개인 NXT trade-quiet 관측 특성으로, KRX 공통 sampler를 바꾸지 않고 `nxt_post_block_trade_quiet_price_observation` source-only family 후보에서 0D/REST 가격 관측의 provenance·budget 계약을 별도 설계한다.
  - NXT sampler persistence packaging 보완 (`2026-07-14 19:20 KST`): runtime에서 생성되는 `data/runtime/rising_missed_nxt_post_block_sampler_state.lock`이 untracked artifact로 노출되는 것을 확인해 exact path를 `.gitignore`에 추가했다. persistence·sampler·주문 runtime 동작은 변경하지 않았다.
  - 다음 액션: `nxt_sampler_first_20m_label_then_hold_order_sample`. 첫 자연 sampler registration의 fresh/source-gap 비율과 20분 completion/first-hit label을 확인하고, 20:00까지의 자연 broker/order remap/multi-leg/fillability 표본을 NXT cohort로만 재판정한다. NXT 고유 차이가 반복되면 기존 KRX family를 변경하지 않고 독립 env/rollback guard를 갖는 NXT runtime family candidate로 넘긴다.

- [ ] `[PostcloseSourceQualityGateReview0714] 장후 source-quality gate 결과 및 튜닝 입력 허용/제외 확인` (`Due: 2026-07-14`, `Slot: POSTCLOSE`, `TimeWindow: 16:25~16:35`, `Track: RuntimeStability`)
  - Source: [observation_source_quality_audit_2026-07-14.json](/home/ubuntu/KORStockScan/data/report/observation_source_quality_audit/observation_source_quality_audit_2026-07-14.json), [threshold_cycle_ev_2026-07-14.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-07-14.json), [code_improvement_workorder_2026-07-14.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-07-14.json), [threshold_cycle_postclose_verification_2026-07-14.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_postclose_verification/threshold_cycle_postclose_verification_2026-07-14.json)
  - 판정 기준: postclose EV/report 소비 전후 `observation_source_quality_audit`의 hard block, row exclusion, clean baseline, unknown-token review warning을 확인한다. `hard_blocking_contract_gap_count>0`이면 결손 row/window 제외 또는 `source_quality_blocked` 산출 여부를 확인하고, `unknown_token_stage_count>0`이면 source-quality producer-fix workorder가 생성됐는지 확인한다.
  - 금지: source-quality preflight missing/stale, row exclusion 실패, hard block candidate 생성, unknown-token workorder handoff 누락을 정상 postclose 완료로 처리하지 않는다. sim/combined EV, live-auto promotion, runtime approval, LDM, threshold apply candidate에 결손 row/window가 섞이면 fail로 닫는다.
  - 다음 액션: `source_quality_gate_pass`, `defective_rows_excluded_and_ev_allowed`, `source_quality_blocked`, `unknown_warning_workorder_created`, `handoff_missing_fix_automation_first` 중 하나로 닫는다.

- [ ] `[ThresholdDailyEVReport0714] daily EV real/sim/combined split 및 자동 반영 결과 확인` (`Due: 2026-07-14`, `Slot: POSTCLOSE`, `TimeWindow: 16:30~16:45`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-07-13.json)
  - 판정 기준: threshold cycle EV를 보고 `live_auto_apply_ready`, `sim_auto_approved`, post-apply attribution, EV authority를 분리해 확인한다.
  - 금지: sim/combined EV만으로 broker execution 품질이나 live 전환을 확정하지 않는다.
  - 다음 액션: 다음 장전 apply 입력으로 쓸 수 있는 항목과 hold_sample/freeze 항목을 분리한다.

- [ ] `[HumanInterventionSummary0714] 자동화체인 사용자 개입 요구사항 분류 및 누락 확인` (`Due: 2026-07-14`, `Slot: POSTCLOSE`, `TimeWindow: 17:00~17:15`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-07-13.json), [time-based-operations-runbook.md](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md)
  - 판정 기준: 개입사항을 `approval_artifact_required|created|missing|blocked_by_policy|observe_only`, `Codex 구현 필요`, `수동 동기화 필요`, `관찰만`으로 분류한다.
  - 금지: approval request만 보고 env 파일을 직접 수정하지 않고, 자동화 산출물에 있는 요청을 답변에만 남기고 checklist/Project 대상에서 누락하지 않는다.
  - 다음 액션: approval request가 있으면 `approval_id`, 후보/대상, artifact path, 승인 여부, 다음 PREOPEN 적용 확인 항목을 남긴다. 누락된 항목이 있으면 다음 영업일 checklist에 parser-friendly checkbox로 추가한다.

- [ ] `[CodeImprovementWorkorderReview0714] code improvement workorder 구현 필요 여부 및 Codex 지시 대상 확인` (`Due: 2026-07-14`, `Slot: POSTCLOSE`, `TimeWindow: 21:15~21:25`, `Track: ScalpingLogic`)
  - Source: [code_improvement_workorder_2026-07-13.md](/home/ubuntu/KORStockScan/docs/code-improvement-workorders/code_improvement_workorder_2026-07-13.md), [code_improvement_workorder_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-07-13.json), [pipeline_events_2026-07-14.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-07-14.jsonl), [rising_missed_intraday_feedback_2026-07-14.json](/home/ubuntu/KORStockScan/data/report/rising_missed_intraday_feedback/rising_missed_intraday_feedback_2026-07-14.json)
  - 판정 기준: selected_order_count=117와 `implement_now`, `attach_existing_family`, `design_family_candidate`, `reject` 분류를 확인하고, 비-implement 반복 항목이 `terminal_non_implement_longstanding`, `repeat_unresolved_structural_blocker`, `keep_visible_by_design` 중 무엇으로 닫혀야 하는지 분리한다.
  - Rising Missed 10:05 handoff: `first_seen_price`와 fresh quote의 비정상 배율을 fail-closed하는 symbol-local anchor contract, 그리고 trusted 0B/0D micro에서 TP1 tick acceleration 또는 micro-VWAP freshness를 공급하는 producer contract를 `instrumentation_gap`으로 검토한다. acceptance test는 cross-symbol/stale anchor가 lane/REST 평가를 만들지 않고, 정상 WS sample에서 momentum source가 `missing`이 아니며, 결측 시 기존 defer/block과 submit-safety가 유지되는 것이다.
  - 금지: code-improvement workorder를 자동 repo 수정으로 취급하지 않는다. 사용자가 Codex 구현을 지시한 경우에만 실행한다.
  - 다음 액션: `implement_now`, `terminal_non_implement_longstanding`, `repeat_unresolved_structural_blocker`, `keep_visible_by_design`, `already_implemented`, `defer_design`, `reject` 중 하나로 닫는다.

- [ ] `[NonImplementLongstandingRejudge0714] non-implement 장기 항목 재판정 및 처리방안 재확인` (`Due: 2026-07-14`, `Slot: POSTCLOSE`, `TimeWindow: 21:25~21:35`, `Track: ScalpingLogic`)
  - Source: [code_improvement_workorder_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-07-13.json), [code_improvement_workorder_2026-07-13.md](/home/ubuntu/KORStockScan/docs/code-improvement-workorders/code_improvement_workorder_2026-07-13.md)
  - 판정 기준: `selected_terminal_non_implement_longstanding_order_ids`, `selected_longstanding_non_implement_disposition_counts`, `non_selected_longstanding_non_implement_disposition_counts`, `repeat_unresolved_escalation_count`, `needs_followup_workorder_count`를 산출물 기준으로 확인한다. `keep_visible_by_design`, `review_required`, `implemented_with_provenance`, `action_required`를 분리하고 action_required가 있으면 별도 구현 워크오더로 승격할지 판단한다.
  - 금지: `keep_visible_by_design` 또는 source-only visibility rollup을 `implement_now`로 임의 승격하지 않는다. runtime/order/provider/bot/threshold/cap 변경 근거로 사용하지 않는다.
  - 다음 액션: `no_action_required_keep_visible`, `review_required_defer_evidence`, `implemented_with_provenance`, `new_followup_workorder_required`, `repeat_unresolved_escalation_required` 중 하나로 닫는다.

- [ ] `[ExitOutcomeUnknownEvidenceReview0714] exit_outcome=outcome_unknown deferred evidence 처리방안 확인` (`Due: 2026-07-14`, `Slot: POSTCLOSE`, `TimeWindow: 21:35~21:45`, `Track: ScalpingLogic`)
  - Source: [code_improvement_workorder_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-07-13.json), [lifecycle_decision_matrix_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/lifecycle_decision_matrix/lifecycle_decision_matrix_2026-07-13.json), [lifecycle_bucket_discovery_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/lifecycle_bucket_discovery/lifecycle_bucket_discovery_2026-07-13.json)
  - 판정 기준: `order_lifecycle_exit_bucket_exit_outcome_outcome_unknown_40c2ecc3`가 계속 `defer_evidence`인지 확인하고, 파일 후보(`files_likely_touched`)와 실행 가능한 acceptance test가 생겼는지 확인한다. 둘 중 하나라도 없으면 evidence 대기로 닫고, 둘 다 있으면 별도 instrumentation workorder 후보로 분리한다.
  - 금지: exit outcome unknown만으로 exit threshold, TP/trailing, sell automation, provider, bot restart를 변경하지 않는다.
  - 다음 액션: `defer_evidence_no_code_action`, `candidate_ready_needs_workorder`, `covered_by_new_provenance`, `reject_not_applicable` 중 하나로 닫는다.

- [ ] `[PerformanceParityHarnessScope0714] deferred performance review 항목 parity harness 범위 검토` (`Due: 2026-07-14`, `Slot: POSTCLOSE`, `TimeWindow: 21:45~21:55`, `Track: RuntimeStability`)
  - Source: [code_improvement_workorder_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-07-13.json), [codebase_performance_workorder_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/codebase_performance_workorder/codebase_performance_workorder_2026-07-13.json)
  - 판정 기준: `order_perf_kiwoom_orders_http_session_review`, `order_perf_config_cache_scope_review`, `order_perf_sentinel_event_cache_incremental_review`의 broker request lifecycle, runtime config reload semantics, incremental cache semantics를 분리해 parity harness 선행 필요 여부를 확인한다. 범위가 닫히지 않으면 deferred performance backlog로 유지한다.
  - 금지: parity harness 없이 broker request/session, runtime config cache, sentinel incremental cache 구현을 시작하지 않는다. provider/order/bot/threshold 변경과 묶지 않는다.
  - 다음 액션: `defer_until_parity_harness`, `parity_harness_workorder_required`, `reject_out_of_scope`, `covered_by_existing_test` 중 하나로 닫는다.

- [ ] `[LifecycleQuietGapReview0714] lifecycle quiet gap rollup 자동 표면화 및 처리 확인` (`Due: 2026-07-14`, `Slot: POSTCLOSE`, `TimeWindow: 21:25~21:40`, `Track: ScalpingLogic`)
  - Source: [runtime_apply_gap_audit_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/runtime_apply_gap_audit/runtime_apply_gap_audit_2026-07-13.json), [runtime_apply_gap_audit_2026-07-13.md](/home/ubuntu/KORStockScan/data/report/runtime_apply_gap_audit/runtime_apply_gap_audit_2026-07-13.md)
  - 판정 기준: quiet gap summary의 quiet_gap_count=`268`, rollup_required_count=`268`, sim_live_connected_quiet_gap_count=`0`, observation_source_quality_warning_count=`0`, quiet_gap_type_counts=`{'ai_review_parsed_low_coverage': 1, 'positive_source_only_keep_collecting': 267}`를 확인하고 parent conflict/exclusion, positive source-only, source-quality warning, AI coverage 누락을 닫는다.
  - 금지: quiet gap을 threshold/env/provider/order/bot 변경 근거로 사용하지 않는다.
  - 다음 액션: `rollup_only`, `implement_now`, `already_covered_by_parent_policy`, `defer_until_more_sample`, `reject_not_applicable` 중 하나로 닫는다.

- [ ] `[AutomationTriggerDecisionSummary0714] 자동화체인 trigger decision run/skip 요약 및 wrapper marker 대조 확인` (`Due: 2026-07-14`, `Slot: POSTCLOSE`, `TimeWindow: 21:40~21:55`, `Track: RuntimeStability`)
  - Source: [automation_chain_trigger_decision_2026-07-13.json](/home/ubuntu/KORStockScan/data/report/automation_chain_trigger_decision/automation_chain_trigger_decision_2026-07-13.json), [run_threshold_cycle_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh)
  - 판정 기준: trigger decision summary의 total_steps=`16`, run_count=`16`, skip_count=`0`, source_missing_count=`7`, force_override_count=`0`, run_steps_sample=`lifecycle_window_rolling5d, lifecycle_window_rolling10d, lifecycle_window_mtd, scalp_sim_ai_deferred_review, pattern_lab_currentness_audit`, skip_steps_sample=`-`, top_reasons=`output_missing_or_unreadable:15, source_missing_or_unreadable:7, upstream_drift_signal:7, upstream_artifact_newer:1`를 확인하고 wrapper 로그의 `[SKIP] threshold-cycle postclose ... trigger_decision=skip` marker와 대조한다.
  - 금지: trigger decision을 PREOPEN apply, final verifier, broker/order/provider/cap/bot/threshold, hard-safety/source-quality fail-closed 경계 변경 근거로 사용하지 않는다.
  - 다음 액션: `trigger_contract_pass`, `unexpected_all_run`, `skip_marker_missing`, `source_missing_run_required`, `force_override_detected`, `needs_followup_patch` 중 하나로 닫는다.

<!-- AUTO_NEXT_STAGE2_CHECKLIST_END -->

## Project/Calendar 동기화

문서/checklist를 수정했으면 parser 검증은 실행하고, Project/Calendar 동기화는 사용자가 아래 명령으로 수동 실행한다.

```bash
PYTHONPATH=. .venv/bin/python -m src.engine.sync_docs_backlog_to_project && PYTHONPATH=. .venv/bin/python -m src.engine.sync_github_project_calendar
```

<!-- AUTO_SERVER_COMPARISON_START -->
### 본서버 vs songstockscan 자동 비교 (`2026-07-14 15:45:51`)

- 기준: `profit-derived metrics are excluded by default because fallback-normalized values such as NULL -> 0 can distort comparison`
- 상세 리포트: `data/report/server_comparison/server_comparison_2026-07-14.md`
- `Trade Review`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Performance Tuning`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Post Sell Feedback`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Entry Pipeline Flow`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
<!-- AUTO_SERVER_COMPARISON_END -->
