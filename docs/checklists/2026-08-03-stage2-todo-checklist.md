# 2026-08-03 Stage2 To-Do Checklist

## 오늘 목적

- 전일 postclose 자동화가 만든 장전 apply 후보와 사용자 개입 요구사항을 산출물 기준으로 확인한다.
- 실주문, threshold, provider, sim/probe 관련 변경은 approval artifact와 checklist 기준 없이 열지 않는다.
- code-improvement workorder는 자동 repo 수정이 아니라 사용자가 Codex에 구현을 지시한 경우에만 실행한다.

## 오늘 강제 규칙

- 장중 runtime 변경은 사용자 명시 지시가 있을 때만 기존 `bounded_tunable` 단일 축에 한해 허용한다. fresh/conflict-free source, 유효 effective price, 단일 blocker 인과, same-stage owner 비충돌, before/after·PID/env provenance·rollback·즉시 attribution을 모두 남긴다. hard safety, stale/conflict, price freshness, broker/account/order/quantity/cooldown, provider, bot, cap, 요청수량은 변경하거나 우회하지 않는다.
- 튜닝 데이터 기준은 `clean_tuning_baseline_date=2026-06-05`, `clean_tuning_baseline_ts_kst=2026-06-05T00:00:00+09:00`이다. 기준 이전 raw/report/analytics artifact는 archive/audit evidence로만 보고 EV/rolling/MTD/cumulative tuning, live-auto promotion, runtime approval, pattern lab promotion, real execution quality approval 입력으로 쓰지 않는다.
- Baseline 이후 raw source-quality contract 결손은 날짜 전체 차단이 아니라 결손 row/window를 `raw_row_exclusion`으로 제외하는 것이 기본이다. 전체 block은 preflight missing/invalid, row/window exclusion 실패, 또는 결손을 안정적으로 특정할 수 없는 high-volume no-contract 상황에만 사용한다.
- 장중과 장후에는 `observation_source_quality_audit --write` 또는 최신 artifact로 raw source-quality를 반복 확인한다. Hard contract gap은 결손 row/window 제외 또는 `source_quality_blocked` 없이는 튜닝 입력에 들어갈 수 없고, unknown-token warning은 hard block이 아니더라도 code-improvement workorder handoff 확인 대상이다.
- provider transport/provenance 확인은 threshold 값, 주문가/수량 guard, 스윙 dry-run guard 변경과 분리한다.
- `actual_order_submitted=false`인 sim/probe 표본은 EV/source-quality 입력이며 실주문 전환 근거가 아니다.
- Project/Calendar 동기화는 사용자가 표준 동기화 명령으로 수행한다.

- [x] `[SmoothingOutcomeReadinessContract0803] OFI 유효 액션 성과장부·protect trailing EV readiness 계약 보완` (`Due: 2026-08-03`, `Slot: POSTCLOSE`, `TimeWindow: 16:50~17:10`, `Track: ScalpingLogic`)
  - Source: [ai_action_outcome_calibration.py](/home/ubuntu/KORStockScan/src/engine/scalping/ai_action_outcome_calibration.py), [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md)
  - 판정 기준: OFI exact-trace 누적 장부는 raw/final action이 실제로 달라진 행만 learning floor와 `source_quality_adjusted_ev_delta_pct`에 포함하고 `NO_CHANGE`는 별도 control로 유지한다. Protect trailing은 raw-event sample readiness와 holding-exit outcome EV edge readiness를 분리하며 `eligible_for_live_review=false` 또는 qualifying cohort 0이면 `sample_ready_but_no_ev_edge`/`hold_no_edge` report-only로 닫아 PREOPEN apply 후보를 만들지 않는다.
  - 금지: report/instrumentation 보완을 smoothing threshold, emergency/protect stop, AVG_DOWN, 주문·수량·provider·bot 상태 변경 또는 runtime authority 확대로 사용하지 않는다.
  - 다음 액션: 당일 postclose 표준 체인에서 schema v2 effective/control 분리, protect trailing readiness와 PREOPEN 비선택을 재확인한다.
  - 실행 결과 (`2026-08-03 16:50~17:00 KST`): OFI ledger schema v2는 actual action transition만 cumulative learning/EV에 사용하고 exact `NO_CHANGE`를 control로 분리한다. 당일 실제 2건은 `effective_transition=0`, `no_change_control=2`, `learning pass=false`, `sample_floor_keep_collecting`으로 확인됐다. Protect trailing은 `sample_ready`, `ev_edge_ready`, `candidate_readiness`를 분리하고 no-edge 상태를 `hold_no_edge`/`report_only_calibration`로 고정했다. rolling primary 재평가 시 readiness 동기화 결함도 재리뷰에서 보완했다. targeted pytest `286 passed`, Ruff, Black, py_compile, checklist parser, `git diff --check`를 통과했으며 runtime/threshold/order/provider/bot 변경과 리포트 write는 수행하지 않았다.

- [x] `[LimitDownWatchPersistentConversionGate0803] 연속 하한가 관찰 영속 ON·장후 실매매 전환 준비도 자동점검 적용` (`Due: 2026-08-03`, `Slot: POSTCLOSE`, `TimeWindow: 16:00~18:30`, `Track: ScalpingLogic`)
  - Source: [run_bot.sh](/home/ubuntu/KORStockScan/src/run_bot.sh), [operator_runtime_overrides.env](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/operator_runtime_overrides.env), [limit_down_watch_report.py](/home/ubuntu/KORStockScan/src/engine/monitoring/limit_down_watch_report.py), [run_threshold_cycle_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh), [verify_threshold_cycle_postclose_chain.py](/home/ubuntu/KORStockScan/src/engine/verify_threshold_cycle_postclose_chain.py)
  - 판정 기준: 모든 향후 봇 시작에서 `KORSTOCKSCAN_LIMIT_DOWN_WATCH_ENABLED=true`가 기본 적용되고 persistent operator override가 이를 재확인하며, 명시적 false rollback만 우선할 수 있어야 한다. 장후 리포트는 observer/candidate 유무와 무관하게 기본 실행되어 당일 observer 활성 증거, clean-baseline 다일 ordered-path 표본, `2회 이상/1회` cohort floor, counterfactual `source_quality_adjusted_ev_pct`, sim policy, post-sim attribution, 별도 사용자 승인 artifact를 단계별로 검증해야 한다. 모든 증거가 닫혀도 `automatic_live_conversion_performed=false`, `allowed_runtime_apply=false`를 유지하고 별도 PREOPEN 적용 준비 상태만 표면화한다.
  - 금지: 장후 리포트가 실주문, BUY 분석, threshold/provider/order-price/quantity/cap/broker guard/bot 상태를 직접 변경하거나 사용자 승인 없이 실매매로 자동 전환하지 않는다.
  - 다음 액션: `keep_observing_and_build_evidence`, `operator_live_conversion_approval_required`, `approved_for_separate_preopen_apply`, `observer_activation_not_observed` 중 하나로 매일 닫고, 별도 PREOPEN apply는 명시적 승인 artifact와 rollback guard가 모두 있을 때만 검토한다.
  - 실행 결과 (`2026-08-03 16:00~16:25 KST`): 판정=`implementation_complete_ready_for_scheduled_preopen_activation`. launcher 기본값과 persistent operator overlay를 ON으로 고정했고 실제 daily env→persistent→dated 합성값은 `true`, PREOPEN handoff verify는 `status=pass`, finding/runtime-policy/datetime-override fail은 모두 0건이다. 장후 producer는 매일 실행되며 observer 활성, rolling `5일/20 path/2회+ 5 path/1회 10 path/capture 80%`, 완전한 EV metric contract, sim catalog, post-sim attribution, 사용자 승인·rollback을 단계별로 검증한다. 모든 단계가 닫혀도 자동 apply는 금지된다. 관련 targeted pytest `279 passed`, Ruff, py_compile, Bash syntax, checklist parser, `git diff --check`를 통과했고 review-gate 재리뷰 finding은 0건이다. 현재 PID는 변경 전 OFF로 시작했고 PID 이후 다른 세션의 미완료 runtime-source 수정이 있어 즉시 재기동하지 않았다. 설치된 스케줄의 `20:10 stop -> 다음 영업일 07:55 fresh start`에서 안전하게 로드한다.

- [x] `[LimitDownOpportunityRiskBridge0803] 연속 하한가 유형별 탐색·sim·bounded-live 후보 자동 생산체인 구현` (`Due: 2026-08-03`, `Slot: POSTCLOSE`, `TimeWindow: 18:30~20:00`, `Track: ScalpingLogic`)
  - Source: `src/engine/monitoring/limit_down_watch_research.py`, [limit_down_watch_report.py](/home/ubuntu/KORStockScan/src/engine/monitoring/limit_down_watch_report.py), [limit_down_watch.py](/home/ubuntu/KORStockScan/src/engine/scalping/limit_down_watch.py), [run_threshold_cycle_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh)
  - 판정 기준: ordered unlock 경로에서 비용 반영 MFE/MAE와 시간축 label을 생성하고, cohort×가격대별 독립 counterfactual 및 sim policy, prior-policy post-sim attribution, bounded-live 후보 계약을 매일 원자적으로 생성한다. 전체 cohort 동시충족을 요구하지 않고 유형별 표본·EV로 독립 판정하며, 회전은 미관찰 후보와 부족한 cohort×가격대 coverage를 먼저 채운 뒤 유동성·unlock 증거를 활용한다. primary EV와 `expected_daily_net_profit_per_watch_slot_hour_pct`를 함께 기록한다.
  - 금지: 연구 producer 또는 후보 artifact가 Recommendation/ACTIVE_TARGET/BUY/order를 생성하거나 runtime/provider/threshold/order-price/quantity/cap/broker guard/bot 상태를 변경하지 않는다. bounded-live 후보는 `runtime_effect=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`, `allowed_runtime_apply=false`이며 사용자 승인과 별도 PREOPEN 소비자 없이는 적용되지 않는다.
  - 다음 액션: `source_observation_building`, `cell_sim_policy_ready`, `post_sim_evidence_building`, `operator_bounded_live_approval_required` 중 하나로 닫는다.
  - 실행 결과 (`2026-08-03 18:30~19:35 KST`): 판정=`source_observation_building`. 실제 producer 4종을 장후 report write에 연결했고, raw-tick 2회 해제 확인 이벤트, 30/60/180/300/600초 비용 반영 label, 90일 rolling cohort×가격대 독립 sim policy, 최신 전일 catalog runtime observation match, prior-policy post-sim, 90% EV lower confidence bound와 BBO/tail/relock 조건을 갖춘 bounded-live 후보 계약을 구현했다. 회전은 unseen/cell coverage debt 이후 sim-policy·2회 이상·유동성 evidence를 적용한다. bounded 후보는 기존 `position_sizing_dynamic_formula` 수량 소유권과 hard safety를 유지하며 PREOPEN consumer가 없어 실주문 적용되지 않는다. 과거 2026-07-29 원천 임시 smoke는 네 artifact를 모두 생성했고 표본 부족으로 `counterfactual/sim/post-sim=insufficient_sample`, `bounded=blocked`, 네 artifact 모두 `runtime_effect=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`, `allowed_runtime_apply=false`였다. targeted pytest `287 passed`, Black/Ruff/py_compile/checklist parser/diff check를 통과했고 review-gate 최종 finding은 0건이다.

- [x] `[LimitDownOnePathLiveAuto0803] 유형별 1개 검증표본 live-auto 적용·후단 보호장치 연결` (`Due: 2026-08-03`, `Slot: POSTCLOSE`, `TimeWindow: 20:00~21:00`, `Track: ScalpingLogic`)
  - Source: [limit_down_watch_research.py](/home/ubuntu/KORStockScan/src/engine/monitoring/limit_down_watch_research.py), [limit_down_watch.py](/home/ubuntu/KORStockScan/src/engine/scalping/limit_down_watch.py), [scalping_scanner.py](/home/ubuntu/KORStockScan/src/scanners/scalping_scanner.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [limit_down_watch_report.py](/home/ubuntu/KORStockScan/src/engine/monitoring/limit_down_watch_report.py)
  - 판정 기준: 사용자 명시 authority에 따라 cohort×가격대별 ordered path 1건부터 비용반영 EV 양수, downside 양수, MAE `>=-5%`, 재잠김 0%, BBO 100%를 만족하면 `live_auto_apply_ready`를 생성하고, 다음 날짜 runtime이 별도 사용자 승인 없이 최신 유효 전일 정책을 로드한다. 당일 종목은 2회 연속 해제 raw tick, tick age `<=5s`, 현재 하한가 상회, 유효 BBO, spread `<=1.5%`를 다시 통과한 경우에만 정상 SCALPING WATCHING/AI/submit 경로로 넘긴다.
  - 금지: 관찰/장후 producer의 직접 broker 주문, same-day artifact 소비, 하루 2개 이상 limit-down live promotion, scale-in, 같은 날 재진입, overnight, `position_sizing_dynamic_formula` 우회, stale/account/order/quantity/cooldown/hard safety 우회, provider/bot 변경을 금지한다.
  - 다음 액션: `live_auto_policy_ready`, `one_path_quality_blocked`, `runtime_policy_invalid_fail_closed`, `daily_cap_consumed`, `fresh_unlock_revalidation_blocked` 중 하나로 닫고 실제 적용은 다음 날짜 자연 후보와 정상 스캘핑 후단 결과로 귀속한다.
  - 실행 결과 (`2026-08-03 17:17 KST`): 판정=`implemented_reviewed_live_auto_consumer_ready`. cohort×가격대별 ordered path 1건부터 비용반영 EV/downside 양수, MAE `>=-5%`, relock `0%`, BBO `100%`를 만족하면 승인 artifact 없이 다음 날짜 최신 정책을 자동 로드한다. 당일에는 2회 해제 raw tick·5초 tick freshness·공식 하한가 상회·BBO·artifact spread cap을 재검증한 뒤 기존 SCALPING WATCHING/AI/submit 경로에만 인수한다. 리뷰에서 artifact spread cap의 scanner 재검증 누락, limit-down owner가 general slot/floor에 막히는 용량 결함, unlock 후 재잠김 시 confirmation 잔존, 주문 직전 재잠김 차단 누락을 찾아 보완했다. broker-order leg마다 최신 WS quote를 다시 확인하며 stale/relock/BBO/spread 실패는 safety veto로 닫는다. immutable promotion provenance로 일 1개를 유지하고 scale-in/reentry/overnight를 차단했으며 정상 수량 owner와 account/order/quantity/cooldown/hard safety는 유지했다. 관련 확대 pytest `1680 passed`, Black, targeted Ruff, py_compile, checklist parser, `git diff --check`를 통과했고 review-gate 최종 finding은 0건이다. 봇/provider/일반 threshold는 변경하거나 재기동하지 않았다.

- [ ] `[LimitDownWatchPersistentPreopenVerify0804] 연속 하한가 영속 ON 최초 다음날 PID·예산·후보소스 확인` (`Due: 2026-08-04`, `Slot: PREOPEN`, `TimeWindow: 07:55~08:10`, `Track: ScalpingLogic`)
  - Source: [run_bot.sh](/home/ubuntu/KORStockScan/src/run_bot.sh), [operator_runtime_overrides.env](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/operator_runtime_overrides.env), [limit_down_watch_state_2026-08-03.json](/home/ubuntu/KORStockScan/data/runtime/limit_down_watch_state_2026-08-03.json), `data/runtime/limit_down_watch_state_2026-08-04.json`, `data/report/limit_down_watch_candidate_source/limit_down_watch_candidate_source_2026-08-04.json`
  - 판정 기준: 07:55 신규 PID가 `KORSTOCKSCAN_LIMIT_DOWN_WATCH_ENABLED=true`를 로드하고 PREOPEN `--verify --pid`가 pass이며, logical watch budget이 `general=1, opening=2, limit_down=1, rising=12, total=16`인지 확인한다. 후보가 없더라도 target-date candidate-source가 valid `no_candidate`로 생성되어야 하며, 후보가 있으면 runtime state heartbeat와 observation registry 1개 이하를 확인한다.
  - 금지: 확인 과정에서 threshold/provider/order/price/quantity/cap/broker guard를 변경하거나 관찰 종목을 Recommendation/ACTIVE_TARGET/BUY/order로 승격하지 않는다.
  - 다음 액션: `persistent_on_verified`, `valid_no_candidate`, `observer_activation_not_observed`, `pid_env_handoff_failed`, `watch_budget_contract_failed` 중 하나로 닫고, 실패 시 limit-down flag만 false rollback한 뒤 review-gate를 다시 수행한다.

- [x] `[SamsungWidgetAdvisoryPreopen0803] 삼성전자 전 세션 위젯 수집기·조언 API 최초 운영 귀속 확인` (`Due: 2026-08-03`, `Slot: PREOPEN`, `TimeWindow: 07:55~08:05`, `Track: RuntimeStability`)
  - Source: [samsung_widget_advisory.py](/home/ubuntu/KORStockScan/src/engine/monitoring/samsung_widget_advisory.py), [samsung_price_widget_routes.py](/home/ubuntu/KORStockScan/src/web/samsung_price_widget_routes.py), [Windows 위젯 운영 안내](/home/ubuntu/KORStockScan/tools/windows/README.md), [외부 감리 설명서](/home/ubuntu/KORStockScan/docs/audit-reports/2026-08-02-samsung-widget-advisory-external-audit-brief.md)
  - 판정 기준: 전용 collector snapshot이 25초 이내 freshness, `shared_cache_only`, 정확한 NXT/KRX venue·request code, `authority=widget_advisory_only`, `runtime_effect=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`를 유지하는지 확인한다. quote-BBO coherence, 세션별 cache scope, 외국인·프로그램 독립 source age, 당일 KRX 장후 anchor가 기록되는지 확인한다. 1·3·5분은 고정 5bp가 아니라 세션별 tick·최근 변동성 band와 slope/R²/방향일치율로 판정하고, `TREND_UP|STABLE|MIXED|DOWN`을 setup 상태와 분리해야 한다. SK하이닉스·KOSPI `ka10080|ka20005` 동일 timestamp 3·5·15분 비교는 negative weakness veto만 가지며 결측이 새 hard block/positive authority를 만들지 않아야 한다. 현재가 하락 impulse와 매도호가 압력 결합은 즉시 veto하되 상승 impulse는 승격하지 않는다. 외부 Yahoo 원천은 `BEST_EFFORT_DELAYED|STALE|MARKET_CLOSED`를 구분하고 실시간으로 오표기하지 않으며, 보조 원천 결측은 개별 `DATA_LIMITED`로 격리되고 필수 quote/BBO/분봉/daily anchor 결손만 advisory를 `DATA_WAIT`로 닫는지 확인한다. collector `cycle_elapsed_ms`, 분당 request budget/잔량/429와 동시간대 실매매 봇 API latency·429를 함께 확인해 계정 quota 경합이 없음을 검증한다. Windows client는 통신 실패 즉시 조언을 제거하고 마지막 성공 25초 이후 stale watchdog이 발동해야 한다.
  - 금지: 신규 Kiwoom 토큰 발급·갱신, 계좌/주문 API, 실매매 runtime/threshold/provider/bot 변경, AI 점수 또는 BUY hard gate 연결, widget 관측 EV와 실현손익 합산을 수행하지 않는다.
  - 다음 액션: `collector_and_api_observed`, `collector_not_deployed`, `snapshot_or_provenance_defect`, `forbidden_authority_leak` 중 하나로 닫고, 배포가 필요하면 trading bot이 아닌 collector와 Gunicorn만 별도로 반영한다.
  - 실행 결과 (`2026-08-03 08:10~08:12 KST`): 판정=`collector_and_api_observed`. 전용 collector PID `22148`은 NXT PREMARKET `005930_NX` snapshot을 10초 주기로 생성했고 source-quality/auxiliary status=`PASS`, request steady-state=`22/36`, 429/경고=`0`, `runtime_effect=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`를 유지했다. Gunicorn master PID `629`는 유지한 채 HUP으로 worker만 교체했으며 Windows client의 HTTPS 요청은 10초마다 `200`과 full advisory payload를 수신했다. 평가 timer는 enabled/waiting이며 다음 실행은 당일 `20:10 KST`다. 관련 pytest `116 passed`, Black, Ruff, py_compile, systemd verify, checklist parser, `git diff --check`를 통과했고 실매매 봇은 재기동하지 않았다.

- [ ] `[ExactV2DailyQualityMaterialization0803] Exact V2 일별 판단품질 생산체인 최초 장후 귀속 확인` (`Due: 2026-08-03`, `Slot: POSTCLOSE`, `TimeWindow: 20:05~20:10`, `Track: ScalpingLogic`)
  - Source: [run_threshold_cycle_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh), [ai_decision_quality.py](/home/ubuntu/KORStockScan/src/engine/scalping/ai_decision_quality.py), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md)
  - 판정 기준: 동일 target date의 `ai_decision_quality_control`, `ai_decision_outcome_labels`, `ai_decision_quality_baseline`, `ai_prompt_paired_replay`가 `control -> mature -> baseline -> paired request preparation` 순서로 생성되고 네 산출물 모두 `runtime_effect=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`를 유지하는지 확인한다. Control은 버전명만이 아니라 당일 마지막 eligible Exact V2의 prompt hash/provider/model/request 설정 전체 서명을 고정해야 하며, 동일 버전의 과거 hash는 `control_signature_not_selected`로 제외한다. Eligible exact mature 표본 1건부터 cumulative learning request에 포함하면서 runtime 승격용 `promotion_evidence_floor`는 별도 유지하고, paired report의 `candidate_execution_performed=false`를 확인한다. 성과 기준은 무조건적 DROP/WAIT 또는 raw adverse-first 절대회피가 아니라 missed-upside 감소, 비용조정 1주 probe EV, 제한손실, severe-tail adverse 비증가, bounded adverse 이후 recovery 포착과 후단 보호장치의 결합이다.
  - 금지: 산출물 생성 성공을 AI 판단품질 또는 순이익 개선으로 간주하지 않는다. 장후 자동 경로에서 `--execute-candidate`, live prompt/provider/model/threshold/order/price/quantity/bot 변경을 수행하지 않는다.
  - 다음 액션: `daily_quality_chain_materialized`, `source_or_maturity_gap`, `artifact_order_or_freshness_defect`, `candidate_execution_authority_leak` 중 하나로 닫는다.

- [ ] `[DecisionQualityV211CumulativeReplay0803] V2.11 clean-continuation 누적 Exact V2 paired replay` (`Due: 2026-08-03`, `Slot: POSTCLOSE`, `TimeWindow: 20:25~20:40`, `Track: ScalpingLogic`)
  - Source: [ai_decision_quality.py](/home/ubuntu/KORStockScan/src/engine/scalping/ai_decision_quality.py), [ai_prompt_contracts.py](/home/ubuntu/KORStockScan/src/engine/ai_prompt_contracts.py), [ai_prompt_detailed_paired_replay_2026-07-29_decision_quality_v2_11_clean_continuation_probe.json](/home/ubuntu/KORStockScan/data/report/ai_prompt_detailed_paired_replay/ai_prompt_detailed_paired_replay_2026-07-29_decision_quality_v2_11_clean_continuation_probe.json), [ai_prompt_detailed_paired_replay_2026-07-31_decision_quality_v2_11_clean_continuation_probe.json](/home/ubuntu/KORStockScan/data/report/ai_prompt_detailed_paired_replay/ai_prompt_detailed_paired_replay_2026-07-31_decision_quality_v2_11_clean_continuation_probe.json)
  - 현재 증거: 동일 date/trace/exact payload hash/venue/session과 route-qualified Kiwoom completed-1m 원천을 검증한 7월 29일 219건 및 7월 31일 12건을 실제 OpenAI `gpt-5.4-nano`로 재실행했고 provider failure/schema reject/provider none은 모두 0건이다. V2.11은 7월 29일 전체 후보에서 BUY/exposure 2건·1종목, 비용조정 EV `+0.002898%`, Control 대비 `+0.041513%p`, adverse-first 0건을 기록했다. 좁은 clean-continuation cohort는 6건·2종목, 전부 노출 가정 비용조정 EV `+0.317066%`였으나 Candidate가 2건·1종목만 노출해 coverage `33.33%`, 선택분 EV `+0.317369%`였다. 7월 31일 신규 12건에는 clean cohort와 Candidate exposure가 없었다. 누적 231건은 Candidate exposure 2건·1종목, 비용조정 EV `+0.002748%`, adverse-first 0건이다. 신규 missed-upside 1건과 promotion evidence floor 10건/3종목 미달로 `paired_replay_complete_candidate_quality_rejected`이며 runtime 승격 근거가 아니다.
  - 판정 기준: 신규 mature Exact V2 전체 eligible cohort에 동일 prompt/provider/model로 누적 replay하고, `control_entry_probe_intent`를 무노출 WAIT로 버리지 않으며 Control/Candidate 모두 같은 conservative execution cost를 적용한다. V2.11 clean cohort는 fresh dual source, normal spread, blocker 없음, completed 3/5/10분 양수, structural edge, 얕은 drawdown, reference reclaim, 독립 precursor 2개 이상, 실행비용 `<=0.25%`를 모두 요구한다. Candidate가 정직하게 WAIT/DROP한 clean 행은 schema reject로 강제하지 않고 no-exposure/missed-opportunity로 귀속한다. `blocking`, failed structure, large sell print, source unusable, stale/conflict, venue/session mismatch는 계속 차단한다.
  - 금지: 현재 positive exposure와 clean cohort EV를 live prompt 적용 근거로 사용하거나, BUY 생성을 위해 action·risk label·expected upside/downside·source facts를 사후 수정하지 않는다. 주문·provider/model·threshold·가격·수량·bot 상태는 변경하지 않는다.
  - 다음 액션: `candidate_positive_exposure_ev_ready`, `quality_rejected_keep_learning`, `semantic_rejected_fix_contract`, `source_or_maturity_gap` 중 하나로 닫는다.
  - 장중 추가 증거 (`2026-08-03 16:20 KST`): 나우로보틱스 `analyze_target:459510:1785740479314:c5380333`를 exact trace/evaluation 기준으로 독립 귀속했다. post-block 기준가 16,520원에서 gross `+1.30%`를 adverse `-0.70%`보다 먼저 달성했고 최대 `+1.694915%`, 최초 target hit=`16:05:16`이었다. Control V2.7과 실제 OpenAI V2.11 targeted replay는 모두 DROP이었다. V2.11 입력은 fresh dual source, normal spread, hard blocker 0, 독립 precursor 3개였지만 NXT 장후 시작 직후 completed bar 1개라 3·5·10분 continuation 조건을 충족하지 못했다. targeted artifact는 `diagnostic_case_analysis_only`, `cohort_performance_authority=false`, `runtime_effect=false`이며 live prompt나 BUY guard 변경 근거가 아니다.
  - 후속 보완 기준: 기존 전체 eligible cohort를 유지한 채 early-session/opening-flow 전용 후보 규칙을 설계한다. completed bar 1개만으로 BUY를 허용하지 않고, fresh dual source, normal observable spread, hard blocker 0, bounded execution cost, reference reclaim, micro absorption, trusted tape acceleration 등 독립 precursor를 결합해 one-share passive-probe 후보를 재평가한다. 동일 payload paired replay에서 missed-upside 감소, 비용조정 probe EV, 제한손실, severe-tail adverse, bounded adverse 이후 recovery 포착을 함께 확인하기 전 runtime 승격은 금지한다. raw adverse-first 증가는 별도 진단값으로 유지하되 단독 veto로 사용하지 않는다.

<!-- AUTO_NEXT_STAGE2_CHECKLIST_START -->
## 자동 생성 체크리스트 (`2026-07-31` postclose -> `2026-08-03`)

- 이 블록은 postclose 자동화 산출물에서 생성된다.
- `codex_daily_workorder_*.md`는 downstream 전달물이라 입력 source로 사용하지 않는다.
- RunbookOps 반복 확인은 `build_codex_daily_workorder`와 Project/Calendar 동기화 경로가 별도로 소유한다.

## 장전 체크리스트 (08:45~09:00)

- [x] `[ThresholdEnvAutoApplyPreopen0803] threshold env 자동 apply 산출물 및 사용자 개입 여부 확인` (`Due: 2026-08-03`, `Slot: PREOPEN`, `TimeWindow: 08:50~08:55`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-07-31.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-07-31.json), [threshold_cycle_preopen_apply.py](/home/ubuntu/KORStockScan/src/engine/threshold_cycle_preopen_apply.py), [run_bot.sh](/home/ubuntu/KORStockScan/src/run_bot.sh)
  - 판정 기준: 전일 postclose EV와 당일 apply plan/runtime env를 확인하고 `auto_bounded_live` guard 통과분만 runtime env로 인정한다. 고효용 entry 보강은 clean baseline cumulative evidence를 담은 `single_cumulative_quality_update` 계약과 `max_runtime_apply_count=1`을 충족하는 `entry_opportunity_recheck_runtime` 최대 1건만 신규 runtime 품질갱신으로 인정한다. 해당 runtime은 canonical `EDGE/WAIT/recovery_required/eligible_wait_probe`와 fresh strong micro가 일치할 때만 1주 probe를 허용하고, probe-first active date·qty=1·post-probe resolver 의존성이 없으면 적용하지 않는다.
  - 금지: blocked family, approval artifact missing, same-stage owner conflict, cumulative 계약 누락/불일치, 복수 entry 품질갱신 후보를 수동 env override로 우회하지 않는다.
  - 다음 액션: `applied_guard_passed_env`, `blocked_no_env`, `partial_apply_with_blocked_families`, `failed_preopen_wrapper`, `not_yet_due` 중 하나로 닫는다.
  - 실행 결과 (`2026-08-03 07:35~08:59 KST`): 판정=`applied_guard_passed_env`. PREOPEN wrapper는 `auto_bounded_live_ready`, `runtime_change=true`, exit code `0`으로 끝났고 runtime manifest는 25개 family와 318개 env override를 기록했다. 제거된 `ai_watching_score_smoothing_report_only`는 raw 선택에서 유입됐지만 `removed_selected_families_ignored`로 제외됐다. `scalping_pyramid_quality_gate`의 필수 env 계약을 보완한 뒤 runtime handoff verifier와 실제 봇 PID `55959` 검증은 `status=pass`, `pid_passed=true`, finding/unverified family/runtime-policy/dated-override fail=`0`이었다. `entry_opportunity_recheck_runtime`은 canonical EDGE/WAIT recovery, fresh strong-micro, 1주 probe-first 계약을 요구하며 stale/broker/order/quantity/hard-safety guard는 유지한다. PREOPEN은 이미 성공했으므로 중복 apply와 봇 재기동은 수행하지 않았다.
  - 재기동 후 확인 (`2026-08-03 09:07 KST`): 코드리뷰와 관련 테스트 통과 후 표준 [restart.sh](/home/ubuntu/KORStockScan/restart.sh) `restart.flag` 경로로 graceful restart를 수행해 bot PID가 `55959 -> 79017`로 교체됐고 flag는 소비됐다. 새 PID는 `RUNTIME_GIT_COMMIT=5bcedeb7`, `RUNTIME_SOURCE_DIRTY=false`, `RUNTIME_STARTED_AT_KST=2026-08-03T09:07:10+09:00`를 기록했다. 장시간 유지되는 `run_bot.sh` supervisor의 launcher commit은 최초 세션 시작 시점 `f7209096`을 유지하지만 launcher SHA256=`3b26f67be0868a33d2c1c2fc28fba0eda1f105cb55461cf9016cff84e672b8ce`은 리뷰·커밋된 현재 파일과 일치한다. PID runtime handoff verifier는 `status=pass`, `pid_passed=true`, finding/unverified family/runtime-policy/dated-override fail=`0`이고 제거된 WATCHING smoothing provenance도 보존했다. 메인 루프, Kiwoom WS 연결·로그인, 조건식 초기화와 첫 실시간 수신이 복귀했으며 threshold/provider/order/quantity/broker/hard-safety 값은 추가 변경하지 않았다.

- [x] `[RisingMissedScoutRuntimePreopen0803] rising_missed_scout_workorder 후속 구현 및 귀속 확인` (`Due: 2026-08-03`, `Slot: PREOPEN`, `TimeWindow: 08:55~09:00`, `Track: ScalpingLogic`)
  - Source: [rising_missed_scout_workorder_2026-07-31.json](/home/ubuntu/KORStockScan/data/report/rising_missed_scout_workorder/rising_missed_scout_workorder_2026-07-31.json), [code_improvement_workorder_2026-07-31.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-07-31.json), [threshold_apply_2026-08-03.json](/home/ubuntu/KORStockScan/data/threshold_cycle/apply_plans/threshold_apply_2026-08-03.json), [threshold_runtime_env_2026-08-03.json](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_2026-08-03.json), [threshold_runtime_env_verify_2026-08-03.json](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_verify_2026-08-03.json)
  - 판정 기준: 전일 `rising_missed_scout_workorder`의 forced scout 대비 post-sell outcome join coverage와 code-improvement order를 보고 구현 완료된 mapped family가 당일 PREOPEN apply plan/runtime env/verify에 반영됐는지 확인한다. source-only order는 별도 runtime family/env mapping과 guard 통과가 있을 때만 반영으로 인정한다.
  - 금지: `rising_missed_scout_workorder` 생성 또는 forced 1-share scout 손익만으로 runtime threshold mutation, stale submit bypass, broker/order guard 완화, provider/bot/cap 변경, real execution quality approval을 열지 않는다.
  - 다음 액션: `runtime_env_reflected_and_verified`, `implemented_but_runtime_not_selected`, `source_only_no_runtime_authority`, `blocked_by_apply_guard`, `report_missing_or_stale`, `verify_missing_or_failed` 중 하나로 닫는다.
  - 실행 결과 (`2026-08-03 08:55~08:59 KST`): 판정=`runtime_env_reflected_and_verified`. 전일 forced scout 472건 중 post-sell join 5건은 모두 양수였고 equal-weight EV=`+0.564%`, notional-weighted EV=`+0.520383%`였으나 fee/tax 결손으로 net PnL은 확정하지 않았다. 네 mapped code-improvement order는 구현 완료 상태지만 모두 `runtime_effect=false`, `allowed_runtime_apply=false`인 source-only 계측 계약을 유지한다. 별도 기존 runtime family인 `rising_missed_normal_buy_bridge`만 PREOPEN manifest에 선택되고 실제 PID env에서 enabled=`true`로 확인됐으며 verifier는 pass했다. forced scout 성과를 normal BUY 성공이나 실체결 품질로 승격하지 않았다.

## 장중 체크리스트 (09:05~15:20)

- [ ] `[RuntimeEnvIntradayObserve0803] 전일 selected runtime family 장중 provenance 및 rollback guard 확인` (`Due: 2026-08-03`, `Slot: INTRADAY`, `TimeWindow: 09:05~09:20`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-07-31.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-07-31.json)
  - 판정 기준: 당일 runtime manifest의 selected_families=soft_stop_whipsaw_confirmation, entry_split_order_plan, scale_in_split_order_plan, score65_74_recovery_probe, scalping_scanner_real_source_guard_runtime, score65_74_recovery_probe_strong_micro_override_runtime, entry_price_gap_profile_runtime, profit_stagnation_exit_runtime, latency_spread_relief_real_operator_override, quote_consistency_normalization, scalp_sim_candidate_window_expansion, scalp_sim_ai_budget_manager, scalping_pyramid_quality_gate, holding_decision_context_v1, weak_pullback_entry_block_runtime, early_accel_recheck_runtime, real_pyramid_scale_in_quality_guard_runtime, sell_side_open_time_block_runtime, pre_submit_liquidity_relief_runtime, entry_opportunity_recheck_runtime, weak_context_late_entry_guard_runtime, rising_missed_normal_buy_bridge, persistent_operator_overrides_2026_06_26, scalp_sim_scale_in_window_expansion, entry_cancel_wait_runtime가 runtime event provenance에 찍히는지 확인한다. 제거된 `ai_watching_score_smoothing_report_only`는 provenance 기대 대상이 아니며 `removed_selected_families_ignored`로만 확인한다.
  - 누적 품질 갱신 기준: PYRAMID/AVG_DOWN 고효용 calibration은 clean-baseline `single_cumulative_quality_update` 중 최대 1건만 선택하고, `scale_in_split_order_plan`과 후단 real-pyramid 보호장치는 유지하며 post-apply attribution이 없는 후보는 fail-closed한다.
  - 금지: 관찰 결과만으로 장중 runtime을 변경하지 않는다. 사용자 명시 override는 fresh/conflict-free source, 단일 blocker 인과, 기존 bounded_tunable 단일 축, rollback과 즉시 attribution 계약을 모두 충족해야 한다.
  - 다음 액션: provenance present/missing, rollback guard breach 여부를 분리 기록한다.
  - dated override 보완 (`2026-08-03`): 2026-07-31 operator overlay와 handoff verifier에서 21개 canary/relief member가 모두 enabled/pass였고, 이후 개별 rollback·retirement 근거는 발견되지 않았다. 2026-08-03 target-date overlay 부재와 persistent false가 결합해 전부 OFF가 된 날짜 rollover 결함을 보완했다. Persistent operator가 `KORSTOCKSCAN_DATED_RUNTIME_AUTO_RENEW_ENABLED=true`를 명시한 경우에만 이미 enabled인 21개 member의 active date를 target date로 갱신하며, 개별 false는 명시적 rollback으로 보존한다. 만료된 early-volatility TP 3종, rising-missed AI action guard, scalp fast-exit guard는 갱신 allowlist 밖에 두고 launcher safe-disable을 유지한다. TP1 source-gap relief는 selector 날짜를 공유하지 않고 자체 active-date key를 사용하도록 수정했다. 재기동 전 verifier에서 21개 pass/fail 0을 확인했으며 실행 PID 반영은 최종 리뷰·재기동 후 별도 귀속한다.
  - 재기동 귀속 (`2026-08-03 09:29 KST`): commit `1cc47841`을 만든 뒤 기존 bot PID `79017`이 restart flag를 소비해 정상 종료했고, 오래된 supervisor를 종료한 다음 같은 commit의 새 `run_bot.sh`로 supervisor와 bot PID `100734`를 기동했다. 새 PID는 runtime/launcher commit=`1cc47841`, `source_dirty=false`, dated auto-renew policy=`v2`, active count=`21`이며 21개 member 모두 active date `2026-08-03`/handoff pass, 만료 guard 5개 모두 false다. PID verifier는 `status=pass`, `pid_passed=true`, finding/mismatch/missing/dated fail=`0`; 계좌 reconciliation, Kiwoom WS 연결·로그인·첫 실시간 수신, OpenAI 엔진 초기화가 정상 완료됐고 `provider=none`은 관측되지 않았다. 09:29:53 KST 신규 1주 BUY 체결 1건은 최초 post-apply 자연 표본으로만 기록하며, 21개 bundle 효과로 인과 귀속하지 않고 후속 lifecycle attribution에서 판정한다.
  - WAIT residual 결함 보완 (`2026-08-03 11:26 KST`, 사용자 명시): 09:29 재기동 후 `residual_revalidation_timeout` 4건을 재구성한 결과 케이엔알시스템은 3초 TTL 안에서 250ms 이상 떨어진 두 번의 `price_tick+orderbook` 양수·음수 0 확인을 충족했지만, persistent override의 `rising_missed_ai_action_guard`가 launcher auto-renew registry에서 누락되어 실제 PID에서 OFF가 되면서 confirmation count가 0으로 남은 결함을 확인했다. 명시적으로 enabled인 `rising_missed_ai_action_guard`와 같은 누락을 가진 `scalp_fast_exit_guard`를 dated auto-renew/handoff registry에 포함하고, WAIT probe 제출 시점의 검증된 probe 계약을 bundle에 불변 저장하여 후단 revalidation이 날짜 환경 재평가로 권한을 잃지 않도록 보완했다. timeout 알림은 기존 canonical reason을 유지하면서 AI authority 만료·quote source conflict·negative group 지속·confirmation 미충족 세부 원인을 분리한다. 기존 3초 TTL, 250ms 간격, 2회 확인, fresh BBO, account/order/quantity/cooldown, hard/protect/emergency와 broker guard는 변경하지 않는다. 현재 PID에는 미반영이며 별도 재기동 지시 전에는 runtime을 변경하지 않는다.

- [ ] `[SimProbeIntradayCoverage0803] sim/probe 관찰축 actual_order_submitted=false 및 source-quality 확인` (`Due: 2026-08-03`, `Slot: INTRADAY`, `TimeWindow: 09:35~09:50`, `Track: ScalpingLogic`)
  - Source: [threshold_cycle_ev_2026-07-31.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-07-31.json)
  - 판정 기준: sim/probe 표본이 real execution과 분리되고 `actual_order_submitted=false` provenance가 유지되는지 확인한다. one-share source는 미적격 skip을 intent로 세지 않고 actual submit/probe-first/split bundle·variant/post-sell join을 분리하며, probe-first submit 이후 `residual_submitted` 이벤트와 `residual_not_submitted` terminal outcome을 record lineage로 결합해 누적 및 `target_date_probe_to_residual` 각각의 `observed|no_natural_sample|instrumentation_gap`, resolution coverage, submitted/blocked/not-submitted/unresolved 건수를 확인한다. terminal outcome 도입 전 artifact는 `residual_blocked + phase=aborted + residual_submitted 미관측`인 경우만 `legacy_aborted_phase_fallback`으로 복원하고 source count를 분리한다. 과거 instrumentation gap을 당일 표본 결함으로 간주하지 않는다. Entry ADM은 평가 원천 underproduction과 join-contract gap을 분리하고, overnight는 `observed|no_natural_sample|instrumentation_gap`을 gzip artifact까지 포함해 판정한다.
  - 금지: sim/probe EV를 broker execution 품질이나 실주문 전환 근거로 단독 사용하지 않는다.
  - 다음 액션: source-quality split, active state 복원, open/closed count를 같이 기록한다.

- [ ] `[IntradaySourceQualityGateCheck0803] 장중 raw source-quality 결손/unknown 조기 경보 및 튜닝 입력 차단 준비 확인` (`Due: 2026-08-03`, `Slot: INTRADAY`, `TimeWindow: 14:20~14:35`, `Track: RuntimeStability`)
  - Source: [pipeline_events_2026-08-03.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-08-03.jsonl), [threshold_events_2026-08-03.jsonl](/home/ubuntu/KORStockScan/data/threshold_cycle/threshold_events_2026-08-03.jsonl), [observation_source_quality_audit_2026-08-03.json](/home/ubuntu/KORStockScan/data/report/observation_source_quality_audit/observation_source_quality_audit_2026-08-03.json), [observation_source_quality_audit.py](/home/ubuntu/KORStockScan/src/engine/observation_source_quality_audit.py)
  - 판정 기준: 장중 `PYTHONPATH=. .venv/bin/python -m src.engine.observation_source_quality_audit --target-date 2026-08-03 --write` 재감사를 실행하거나 최신 산출물을 확인해 `hard_blocking_contract_gap_count`, `hard_blocking_excluded_row_count`, `tuning_input_allowed`, `raw_row_exclusion_applied`, `unknown_token_stage_count`, `review_warning_count`를 기록한다.
  - 금지: hard contract gap 또는 unknown-token warning을 답변에만 남기지 않는다. 결손 row/window는 튜닝 입력 제외 또는 workorder handoff 대상으로 고정하고, broker/order/provider/cap/bot/threshold 변경 근거로 사용하지 않는다.
  - 다음 액션: `source_quality_clean_intraday`, `defective_rows_excluded`, `hard_block_requires_producer_fix`, `unknown_warning_workorder_required`, `audit_missing_or_stale` 중 하나로 닫는다. hard gap/unknown warning이 있으면 장후 `PostcloseSourceQualityGateReview`와 `CodeImprovementWorkorderReview`에서 누락 없이 재확인한다.

## 장후 체크리스트 (20:05~21:55)

- [ ] `[EntryAiGateCumulativeSourceJoin0803] entry AI 누적 품질갱신 source join 및 손상일 provenance 보완` (`Due: 2026-08-03`, `Slot: POSTCLOSE`, `TimeWindow: 20:10~20:25`, `Track: ScalpingLogic`)
  - Source: [entry_ai_gate_backtest_2026-07-31.json](/home/ubuntu/KORStockScan/data/report/entry_ai_gate_backtest/entry_ai_gate_backtest_2026-07-31.json), [entry_ai_gate_backtest.py](/home/ubuntu/KORStockScan/src/engine/scalping/entry_ai_gate_backtest.py), [missed_entry_counterfactual_2026-07-27.json.gz](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/missed_entry_counterfactual_2026-07-27.json.gz)
  - 판정 기준: clean baseline 누적 replay에서 missed-entry counterfactual을 동일 `record_id`/candidate lineage의 entry decision snapshot과 결합해 canonical AI action, fresh tick/micro-VWAP, source-quality provenance를 복원한다. producer별 실제 소비일과 의도한 trading-day window를 분리하고, JSON 손상·artifact missing 날짜는 누적 source count에서 제외 사유와 함께 표면화한다. Entry ADM의 `entry_price_skip_followup_cumulative`은 exact same-symbol lineage로 결합된 finite 90초 MFE/MAE가 20건 이상일 때만 offline counterfactual 재검토 준비로 판정하며, 해당 상태만으로 runtime apply나 실현 EV를 승인하지 않는다. 결합 가능한 원천이 없으면 0표본을 전략 부재가 아니라 `source_contract_not_evaluable`로 닫는다.
  - 금지: score-only diagnostic EV, action/micro provenance가 없는 counterfactual, 손상 artifact를 `entry_opportunity_recheck_runtime` 적용 근거로 사용하지 않는다. hard safety, stale/quote, broker/account/order/quantity/cooldown, provider, bot, cap을 변경하거나 우회하지 않는다.
  - 다음 액션: `joined_replay_apply_candidate_0_or_1`, `source_contract_not_evaluable`, `corrupt_date_excluded_with_provenance`, `source_quality_blocked`, `needs_producer_patch` 중 하나로 닫는다.

- [x] `[PostcloseSourceQualityGateReview0803] 장후 source-quality gate 결과 및 튜닝 입력 허용/제외 확인` (`Due: 2026-08-03`, `Slot: POSTCLOSE`, `TimeWindow: 16:25~16:35`, `Track: RuntimeStability`)
  - Source: [observation_source_quality_audit_2026-08-03.json](/home/ubuntu/KORStockScan/data/report/observation_source_quality_audit/observation_source_quality_audit_2026-08-03.json), [threshold_cycle_ev_2026-08-03.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-08-03.json), [code_improvement_workorder_2026-08-03.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-08-03.json), [threshold_cycle_postclose_verification_2026-08-03.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_postclose_verification/threshold_cycle_postclose_verification_2026-08-03.json)
  - 판정 기준: postclose EV/report 소비 전후 `observation_source_quality_audit`의 hard block, row exclusion, clean baseline, unknown-token review warning을 확인한다. `hard_blocking_contract_gap_count>0`이면 결손 row/window 제외 또는 `source_quality_blocked` 산출 여부를 확인하고, `unknown_token_stage_count>0`이면 source-quality producer-fix workorder가 생성됐는지 확인한다.
  - 금지: source-quality preflight missing/stale, row exclusion 실패, hard block candidate 생성, unknown-token workorder handoff 누락을 정상 postclose 완료로 처리하지 않는다. sim/combined EV, live-auto promotion, runtime approval, LDM, threshold apply candidate에 결손 row/window가 섞이면 fail로 닫는다.
  - 다음 액션: `source_quality_gate_pass`, `defective_rows_excluded_and_ev_allowed`, `source_quality_blocked`, `unknown_warning_workorder_created`, `handoff_missing_fix_automation_first` 중 하나로 닫는다.
  - Result: `source_quality_gate_pass`. 480,717 events/174 stages 재감사에서 hard block·warning stage·unknown-token stage 모두 0, reviewed unknown stage 98, `tuning_input_allowed=true`로 확인했다. producer-reviewed fail-closed/source-only provenance만 엄격히 인정하며 신규 runtime 권한은 생성하지 않았다.

- [ ] `[ThresholdDailyEVReport0803] daily EV real/sim/combined split 및 자동 반영 결과 확인` (`Due: 2026-08-03`, `Slot: POSTCLOSE`, `TimeWindow: 16:30~16:45`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-07-31.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-07-31.json)
  - 판정 기준: threshold cycle EV를 보고 `live_auto_apply_ready`, `sim_auto_approved`, post-apply attribution, EV authority를 분리해 확인한다. wrapper에서 OFF인 swing/deep-audit source는 `warning_contract.disabled_not_applicable`로 분리되고, active warning은 중복 제거된 required-missing/quality warning만 남는지 확인한다.
  - 금지: sim/combined EV만으로 broker execution 품질이나 live 전환을 확정하지 않는다.
  - 다음 액션: 다음 장전 apply 입력으로 쓸 수 있는 항목과 hold_sample/freeze 항목을 분리한다.
  - Calibration guard 보완 (`2026-08-03`): 정책 seed 1건을 실표본으로 오인하던 `scale_in_split_order_plan`을 직접 AVG_DOWN 또는 real+sim 3건 floor로 수정했다. broad completed count가 `soft_stop_whipsaw_confirmation` rolling 분모를 대신하지 못하도록 직접 grace/confirmation/post-sell denominator를 선언했고, 모든 candidate에 current/recommended/PREOPEN-selected/actual-runtime 상태 분리와 단일 stage owner·post-apply 귀속 계약을 추가했다. Score recovery가 `adjust_up`이면 `recommended.enabled=true`가 되도록 deterministic consistency를 고정하되, confirmed panic/source-quality/hard safety와 다음 PREOPEN selection은 그대로 별도 guard가 소유한다. 기존 8/3 scale-in artifact read-only 재판정은 직접 표본 `0/3`, `hold_sample`, `report_only_calibration`이며 seed 관측은 계속한다.
  - Review gate (`2026-08-03`): 1차 리뷰에서 PREOPEN consumer가 신규 handoff 계약을 검증하지 않는 결함을 찾아 권한/runtime-effect/단일-stage/post-apply/selection-state fail-closed 검증과 결정 provenance 복사를 추가했다. 관련 report·scale-in·PREOPEN 전체 `298 passed`, checklist parser `60 passed`, Black, Ruff, py_compile, `git diff --check`를 통과했고 미해결 finding은 0건이다. 리포트 재생성·PREOPEN apply·봇 재기동은 이 코드리뷰 범위에서 수행하지 않았다.

- [ ] `[HumanInterventionSummary0803] 자동화체인 사용자 개입 요구사항 분류 및 누락 확인` (`Due: 2026-08-03`, `Slot: POSTCLOSE`, `TimeWindow: 17:00~17:15`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-07-31.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-07-31.json), [time-based-operations-runbook.md](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md)
  - 판정 기준: 개입사항을 `approval_artifact_required|created|missing|blocked_by_policy|observe_only`, `Codex 구현 필요`, `수동 동기화 필요`, `관찰만`으로 분류한다.
  - 금지: approval request만 보고 env 파일을 직접 수정하지 않고, 자동화 산출물에 있는 요청을 답변에만 남기고 checklist/Project 대상에서 누락하지 않는다.
  - 다음 액션: approval request가 있으면 `approval_id`, 후보/대상, artifact path, 승인 여부, 다음 PREOPEN 적용 확인 항목을 남긴다. 누락된 항목이 있으면 다음 영업일 checklist에 parser-friendly checkbox로 추가한다.

- [x] `[CodeImprovementWorkorderReview0803] code improvement workorder 구현 필요 여부 및 Codex 지시 대상 확인` (`Due: 2026-08-03`, `Slot: POSTCLOSE`, `TimeWindow: 21:15~21:25`, `Track: ScalpingLogic`)
  - Source: [code_improvement_workorder_2026-08-03.md](/home/ubuntu/KORStockScan/docs/code-improvement-workorders/code_improvement_workorder_2026-08-03.md), [code_improvement_workorder_2026-08-03.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-08-03.json)
  - 판정 기준: 최신 재생성 산출물의 selected order와 `implement_now`, `attach_existing_family`, `design_family_candidate`, `reject` 분류를 확인하고, `duplicate_order_warnings=[]`이며 모든 `handoff_closed_root_cause_open` order에 non-null `root_cause_followup_contract.root_cause_signal`, `acceptance_test`, `next_repair_action`, `implementation_only_closure_allowed=false`가 있는지 검증한다. `threshold_cycle_postclose_verification.code_improvement_workorder_contract.status=pass`, `contract_state=declared_and_verified`, required/complete count 일치도 함께 확인한다. 비-implement 반복 항목은 `terminal_non_implement_longstanding`, `repeat_unresolved_structural_blocker`, `keep_visible_by_design` 중 무엇으로 닫혀야 하는지 분리한다.
  - 금지: code-improvement workorder를 자동 repo 수정으로 취급하지 않는다. 사용자가 Codex 구현을 지시한 경우에만 실행한다.
  - 다음 액션: `implement_now`, `terminal_non_implement_longstanding`, `repeat_unresolved_structural_blocker`, `keep_visible_by_design`, `already_implemented`, `defer_design`, `reject` 중 하나로 닫는다.
  - Result: 2-pass 구현·리뷰·재생성 후 generation `2026-08-03-7971b49e03ce`에서 신규 `implement_now=0`, `runtime_effect_true=0`, duplicate warning 0, root-cause follow-up contract `20/20` pass로 닫았다. 최초 runtime-effect-false 3건은 lineage `removed_order_ids`로 해소됐고, 장기 non-implement 2건은 `terminal_non_implement_longstanding` 관찰 항목으로 유지한다.

- [ ] `[LifecycleQuietGapReview0803] lifecycle quiet gap rollup 자동 표면화 및 처리 확인` (`Due: 2026-08-03`, `Slot: POSTCLOSE`, `TimeWindow: 21:25~21:40`, `Track: ScalpingLogic`)
  - Source: [runtime_apply_gap_audit_2026-07-31.json](/home/ubuntu/KORStockScan/data/report/runtime_apply_gap_audit/runtime_apply_gap_audit_2026-07-31.json), [runtime_apply_gap_audit_2026-07-31.md](/home/ubuntu/KORStockScan/data/report/runtime_apply_gap_audit/runtime_apply_gap_audit_2026-07-31.md)
  - 판정 기준: quiet gap summary의 quiet_gap_count=`246`, rollup_required_count=`246`, sim_live_connected_quiet_gap_count=`0`, observation_source_quality_warning_count=`0`, quiet_gap_type_counts=`{'ai_review_parsed_low_coverage': 1, 'positive_source_only_keep_collecting': 245}`를 확인하고 parent conflict/exclusion, positive source-only, source-quality warning, AI coverage 누락을 닫는다.
  - 금지: quiet gap을 threshold/env/provider/order/bot 변경 근거로 사용하지 않는다.
  - 다음 액션: `rollup_only`, `implement_now`, `already_covered_by_parent_policy`, `defer_until_more_sample`, `reject_not_applicable` 중 하나로 닫는다.

- [ ] `[AutomationTriggerDecisionSummary0803] 자동화체인 trigger decision run/skip 요약 및 wrapper marker 대조 확인` (`Due: 2026-08-03`, `Slot: POSTCLOSE`, `TimeWindow: 21:40~21:55`, `Track: RuntimeStability`)
  - Source: [automation_chain_trigger_decision_2026-07-31.json](/home/ubuntu/KORStockScan/data/report/automation_chain_trigger_decision/automation_chain_trigger_decision_2026-07-31.json), [run_threshold_cycle_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh)
  - 판정 기준: trigger decision summary의 total_steps=`16`, run_count=`15`, skip_count=`1`, source_missing_count=`7`, force_override_count=`0`, run_steps_sample=`lifecycle_window_rolling5d, lifecycle_window_rolling10d, lifecycle_window_mtd, pattern_lab_currentness_audit, pattern_lab_ai_review`, skip_steps_sample=`scalp_sim_ai_deferred_review`, top_reasons=`output_missing_or_unreadable:11, source_missing_or_unreadable:7, upstream_drift_signal:7, upstream_artifact_newer:4, fresh_outputs_no_trigger:1`를 확인하고 wrapper 로그의 `[SKIP] threshold-cycle postclose ... trigger_decision=skip` marker와 대조한다.
  - 2026-08-01 코드 정리: 위 수치는 2026-07-31 산출물의 역사적 실행 결과다. 소비자가 없던 `scalp_sim_ai_deferred_review` step과 actionable 후속 소비자가 없던 standalone `quote_consistency_report`는 producer/wrapper/verifier에서 제거됐으므로 다음 postclose부터 total step, skip sample, DONE marker 및 verifier artifact status에서 제외한다. sim AI budget-manager의 deferred runtime event와 실주문 안전용 `quote_consistency_normalization`은 유지한다. 기존 quote-consistency report 산출물은 archive evidence로만 취급한다.
  - 2026-08-01 후속 정리: source-only `limit_down_watch` runtime observer가 기본 OFF인 동안 report만 기본 ON으로 실행돼 빈 `source_blocked` 산출물을 만들던 불일치를 제거했다. 다음 postclose부터 report 기본값은 상속된 `KORSTOCKSCAN_LIMIT_DOWN_WATCH_ENABLED=ON` 또는 대상일 candidate-source 파일 존재를 따르고, `THRESHOLD_CYCLE_RUN_LIMIT_DOWN_WATCH_REPORT` 명시값이 자동 판정보다 우선한다. 이로써 postclose cron이 PREOPEN env를 상속하지 않는 경우에도 실제 observer 표본을 누락하지 않는다. observer/report가 모두 OFF이고 source도 없는 경우 DONE marker의 `limit_down_watch_report=false`와 verifier disabled 상태를 정상으로 판정한다.
  - 2026-08-01 성능 보완: 2026-07-31 raw 4.37GB 중 고빈도 diagnostic event가 약 90%를 차지하고 raw-derived parity summary가 322MB까지 증가한 병목을 분리했다. `producer_parity` 산출물은 `counts_only_v1`로 축소하고, 대형 Entry ADM/lifecycle bucket/latency CLI의 postclose stdout은 compact summary만 남긴다. 수정 코드로 2026-07-31 실데이터를 재생성한 결과 49,655 bucket과 364,634 원천 event count는 보존됐고 산출물은 337,590,650 bytes에서 35,499,217 bytes(33.85MiB)로 89.48% 감소했다. 상태는 기존과 같은 `v2_shadow_partial_coverage`, `suppress_eligibility=false`, `raw_suppression_enabled=false`다. `pipeline_event_verbosity`는 raw/producer/code input보다 검증된 JSON/Markdown이 새로울 때만 recovery rerun에서 skip한다. raw suppression은 2영업일 이상 parity 승인 전까지 OFF이며 실주문·threshold·provider·bot 권한 변화는 없다. 다음 postclose 판정은 wrapper log 단일행 크기와 pipeline report `[SKIP]` marker 또는 재생성 사유를 추가 확인한다.
  - 금지: trigger decision을 PREOPEN apply, final verifier, broker/order/provider/cap/bot/threshold, hard-safety/source-quality fail-closed 경계 변경 근거로 사용하지 않는다.
  - 다음 액션: `trigger_contract_pass`, `unexpected_all_run`, `skip_marker_missing`, `source_missing_run_required`, `force_override_detected`, `needs_followup_patch` 중 하나로 닫는다.

<!-- AUTO_NEXT_STAGE2_CHECKLIST_END -->

## Project/Calendar 동기화

문서/checklist를 수정했으면 parser 검증은 실행하고, Project/Calendar 동기화는 사용자가 아래 명령으로 수동 실행한다.

```bash
PYTHONPATH=. .venv/bin/python -m src.engine.sync_docs_backlog_to_project && PYTHONPATH=. .venv/bin/python -m src.engine.sync_github_project_calendar
```
