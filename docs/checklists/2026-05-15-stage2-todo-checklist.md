# 2026-05-15 Stage2 To-Do Checklist

## 오늘 목적

- 전일 postclose 자동화가 만든 장전 apply 후보와 사용자 개입 요구사항을 산출물 기준으로 확인한다.
- 실주문, threshold, provider, sim/probe 관련 변경은 approval artifact와 checklist 기준 없이 열지 않는다.
- 실주문 예수금/1주 cap/selected family 여부와 무관하게 스캘핑·스윙의 BUY/선정 가능 후보는 sim/probe 전주기 관찰 대상으로 최대한 남기고, 병목 해소·손실 축소 후보를 분리해 threshold-cycle 입력으로 보낸다.
- code-improvement workorder는 자동 repo 수정이 아니라 사용자가 Codex에 구현을 지시한 경우에만 실행한다.

## 오늘 강제 규칙

- 장중 runtime threshold mutation은 금지한다. 적용은 PREOPEN `threshold_cycle_preopen_apply`가 생성한 runtime env만 source로 본다.
- provider transport/provenance 확인은 threshold 값, 주문가/수량 guard, 스윙 dry-run guard 변경과 분리한다.
- `actual_order_submitted=false`인 sim/probe 표본은 EV/source-quality와 threshold 개선 입력이며, 실주문 전환·broker execution 품질 근거가 아니다.
- 실계좌 주문가능금액, real order guard, approval artifact 부재는 sim/probe 후보 생성 제외 사유가 아니다. 단, provenance는 real/sim/combined를 분리한다.
- 추가매수는 `ENABLE_SCALE_IN` 공통 master gate와 AVG_DOWN/PYRAMID arm별 gate를 분리해서 본다. 한쪽 arm만 observe/live-equivalent로 열 수 있으나, real canary 또는 cap 확대는 별도 approval artifact 없이는 열지 않는다.
- `panic_regime_mode`와 `panic_buy_regime_mode`는 report/approval source이며, approval artifact와 rollback guard 없이 신규 BUY 차단, 미체결 주문 취소, holding/exit 강제 변경, 자동매도, TP/trailing 변경, 추격매수 차단, 시장가 전량청산에 직접 쓰지 않는다.
- Project/Calendar 동기화는 사용자가 표준 동기화 명령으로 수행한다.

<!-- AUTO_NEXT_STAGE2_CHECKLIST_START -->
## 자동 생성 체크리스트 (`2026-05-14` postclose -> `2026-05-15`)

- 이 블록은 postclose 자동화 산출물에서 생성된다.
- `codex_daily_workorder_*.md`는 downstream 전달물이라 입력 source로 사용하지 않는다.
- RunbookOps 반복 확인은 `build_codex_daily_workorder`와 Project/Calendar 동기화 경로가 별도로 소유한다.

## 장전 체크리스트 (08:45~09:00)

- 운영 확인 기록 (`PreopenAutomationHealthCheck20260515`, `PVTI_lAHOAXZuE84BUTcPzgsxrk4`): 판정은 `warning`. `threshold_cycle_preopen`과 bot runtime env 적용은 pass, OpenAI `entry_price` WS 표본 0건은 장중 재확인으로 연결, 스윙 approval request 2건은 approval artifact missing으로 정상 차단.


- [x] `[ThresholdEnvAutoApplyPreopen0515] threshold env 자동 apply 산출물 및 사용자 개입 여부 확인` (`Due: 2026-05-15`, `Slot: PREOPEN`, `TimeWindow: 08:50~08:55`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-14.json), [threshold_cycle_preopen_apply.py](/home/ubuntu/KORStockScan/src/engine/threshold_cycle_preopen_apply.py), [run_bot.sh](/home/ubuntu/KORStockScan/src/run_bot.sh)
  - 판정 기준: 전일 postclose EV와 당일 apply plan/runtime env를 확인하고 `auto_bounded_live` guard 통과분만 runtime env로 인정한다.
  - 금지: blocked family, approval artifact missing, same-stage owner conflict를 수동 env override로 우회하지 않는다.
  - 판정 (`2026-05-15 KST`): `applied_guard_passed_env`.
  - 근거: `logs/threshold_cycle_preopen_cron.log`에 `[DONE] threshold-cycle preopen target_date=2026-05-15` marker가 있고, [threshold_apply_2026-05-15.json](/home/ubuntu/KORStockScan/data/threshold_cycle/apply_plans/threshold_apply_2026-05-15.json)은 status=`auto_bounded_live_ready`, apply_mode=`auto_bounded_live`, runtime_change=`true`다. runtime env는 [threshold_runtime_env_2026-05-15.env](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_2026-05-15.env)와 [threshold_runtime_env_2026-05-15.json](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_2026-05-15.json)으로 생성됐고 selected family는 `soft_stop_whipsaw_confirmation`, override는 `KORSTOCKSCAN_SCALP_SOFT_STOP_WHIPSAW_CONFIRMATION_ENABLED=true` 1개다. `bad_entry_refined_canary`는 same-stage owner conflict로 차단됐고, `protect_trailing_smoothing`은 `window_policy_blocks_single_case_live_candidate:18/20`, `score65_74_recovery_probe`는 `hold/no_runtime_env_override`로 수동 override 없이 제외됐다.
  - 검증: `tmux bot` 세션은 `2026-05-15 07:40:01 KST` 생성, `bot_main.py` PID `4779` 실행 중이며, [run_bot.sh](/home/ubuntu/KORStockScan/src/run_bot.sh)는 runtime env를 source한 뒤 봇을 기동하는 경로다.
  - 다음 액션: `applied_guard_passed_env`, `blocked_no_env`, `partial_apply_with_blocked_families`, `failed_preopen_wrapper`, `not_yet_due` 중 하나로 닫는다.

### Score6574OpenDecisionRecheck0515 확인 기록

- checked_at: `2026-05-15 KST`
- 판정: `not_opened_by_actual_0515_preopen_apply`.
- 근거: 사용자 기억처럼 [2026-05-14 checklist](/home/ubuntu/KORStockScan/docs/checklists/2026-05-14-stage2-todo-checklist.md)의 `Score6574ApplyGuardRecheck0514`는 5/13 source를 기준으로 `sample_count=16`, `source_sample_count=16`, `sample_floor_status=panic_adjusted_ready`, `calibration_state=adjust_up`, `decision_reason=ai_guard_accepted`, env override `KORSTOCKSCAN_SCORE65_74_RECOVERY_PROBE_ENABLED=true`가 selected 될 수 있다고 재검증했다. 그러나 실제 5/15 preopen apply는 5/14 source를 다시 소비했고, [threshold_apply_2026-05-15.json](/home/ubuntu/KORStockScan/data/threshold_cycle/apply_plans/threshold_apply_2026-05-15.json)의 `score65_74_recovery_probe`는 `sample_window=rolling_5d_with_daily_trigger`, `window_policy.primary=rolling_5d`, `sample_count=177`, `source_sample_count=19`, `sample_floor_status=ready`, `calibration_state=hold`, `decision_reason=no_runtime_env_override`로 닫혔다. [threshold_cycle_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_2026-05-14.json)의 `calibration_trigger_pack`도 `window_policy primary=rolling_5d 기준 재평가: score65~74 EV/close_10m 우위가 efficient trade-off gate에 미달해 값 유지`로 기록한다.
- 해석: daily 5/14는 score65~74 병목과 양의 EV/close 신호를 보여 후보로 남겼지만, Plan Rebase/threshold README의 current window policy는 daily trigger만으로 live/bounded apply를 확정하지 않고 rolling/cumulative primary로 재확인한다. 따라서 5/14 11:29의 `logic_fix_ready_not_runtime_applied`는 “5/13 source bug fix 검증 결과”이고, 5/15 실제 runtime env 적용 결과는 “5/14 rolling 재평가 후 미오픈”이다.
- 다음 액션: 장중 runtime threshold mutation은 하지 않는다. score65~74는 오늘 장후 `ThresholdDailyEVReport0515`와 `RuntimeEnvIntradayObserve0515`에서 `selected/applied/not-applied`, daily trigger, rolling primary를 분리해 다시 판정한다.


- [x] `[SwingApprovalArtifactPreopen0515] 스윙 approval request 및 별도 승인 artifact 존재 여부 확인` (`Due: 2026-05-15`, `Slot: PREOPEN`, `TimeWindow: 08:45~08:50`, `Track: RuntimeStability`)
  - Source: [swing_runtime_approval_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/swing_runtime_approval/swing_runtime_approval_2026-05-14.json), [threshold_cycle_ev_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-14.json)
  - 판정 기준: approval request가 있더라도 사용자 승인 artifact가 없으면 env apply 대상이 아니다.
  - 금지: 스윙 dry-run 해제, real canary, floor, scale-in real canary를 서로 자동 승인하지 않는다.
  - 판정 (`2026-05-15 KST`): `approval_artifact_missing / blocked_by_policy`.
  - 근거: [swing_runtime_approval_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/swing_runtime_approval/swing_runtime_approval_2026-05-14.json)은 `swing_model_floor`, `swing_gatekeeper_reject_cooldown` approval request 2건을 생성했지만 runtime_change=`false`다. `data/threshold_cycle/approvals/swing_runtime_approvals_2026-05-14.json`와 `data/threshold_cycle/approvals/swing_scale_in_real_canary_2026-05-14.json`은 존재하지 않는다. [threshold_apply_2026-05-15.json](/home/ubuntu/KORStockScan/data/threshold_cycle/apply_plans/threshold_apply_2026-05-15.json)의 `swing_runtime_approval`도 requested=`2`, approved=`0`, blocked=`approval_artifact_missing`, selected=`[]`, dry_run_forced=`false`로 차단했다.
  - 다음 액션: approval request는 사용자 승인 전까지 env apply/real canary/dry-run 해제 근거가 아니다. 승인할 경우 별도 approval artifact를 만든 뒤 다음 preopen apply에서만 소비한다.
  - 다음 액션: `approval_artifact_present`, `approval_artifact_missing`, `blocked_by_policy` 중 하나로 닫는다.

## 장중 체크리스트 (09:05~15:20)


- 운영 확인 기록 (`ErrorDetectionFail07250515`): 판정은 `source_quality_false_positive_fixed`. `2026-05-15 07:25:01 KST` full error detection fail은 `log_scanner`가 `macro_briefing_complete_error.log(+5)`를 `UNKNOWN(5)`로 분류한 것이다. 실제 5줄은 `[MACRO] live_bundle fetched`, `LIVE bundle_as_of`, `MACRO CACHE SAVE`, `applying LIVE bundle`, `collect_snapshot done` 정상 진행 로그였고, cron/process/artifact/resource/stale_lock 및 Kiwoom auth detector는 pass였다. 원인은 `macro_briefing_complete.py`가 정상 macro progress를 `log_error`로 남긴 로그 sink 오염이다. 정상 macro progress/cache 로그를 `log_info`로 내리도록 보정했으며 runtime threshold/provider/order/bot restart 변경은 없다. 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_error_detector_log_scanner.py` -> `14 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/macro_briefing_complete.py src/engine/error_detectors/log_scanner.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode log_only --dry-run` -> `summary_severity=pass`, `log_scanner=pass`.

- 운영 확인 기록 (`ProcessHealthStartupRace07400515`): 판정은 `startup_boundary_false_positive_fixed`. `2026-05-15 07:40:02 KST` full error detection fail은 `process_health`가 봇 expected runtime window 시작 초(`07:40`)에 오래된 heartbeat PID `48192`를 읽어 `pid_dead`로 fail 처리한 것이다. 같은 시각 `tmux bot` 세션은 `07:40:01 KST` 생성 중이었고, 이후 `bot_main.py` PID `13479`가 정상 기동해 `07:45` full detection부터 pass로 회복됐다. 현재 `health_only --dry-run`도 `main_loop_pid=13479`, `main_loop_status=ok`, thread status=`ok`다. 조치: `process_health`에 `ERROR_DETECTOR_BOT_STARTUP_GRACE_SEC=180` startup grace를 추가해 expected start 직후 heartbeat/PID가 아직 갱신되지 않은 경우 fail이 아니라 warning/recheck로 분류하도록 보정했다. 이 조치는 detector race 보정이며 runtime threshold/provider/order guard 변경이 아니다. 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_error_detector_process_health.py src/tests/test_constants.py -k 'process_health or error_detector'` -> `14 passed, 10 deselected`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/error_detectors/process_health.py src/utils/constants.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode health_only --dry-run` -> `summary_severity=pass`, `process_health=pass`.

- 운영 확인 기록 (`ProcessHealthRestartHandoff11300515`): 판정은 `restart_handoff_false_positive_fixed`. `2026-05-15 11:30:02 KST` full error detection fail은 `ScalpAIInputFreshnessRecheck0515`의 `restart.flag` 기반 graceful restart 중 detector가 이전 heartbeat PID `47043`을 읽은 시점과 새 PID `47852`가 아직 heartbeat를 갱신하기 전 시점이 겹친 것이다. `logs/bot_history.log` 기준 새 프로세스는 `11:30:10` 초기화, `11:30:12` Kiwoom WS 연결/로그인, `11:30:15` OpenAI route 및 첫 실시간 데이터 수신을 완료했고, `11:35:01` full detector는 pass로 회복됐다. 현재 PID는 `49065`이며 `health_only --dry-run`과 `log_only --dry-run`은 pass다. 조치: [process_health.py](/home/ubuntu/KORStockScan/src/engine/error_detectors/process_health.py)에 `ERROR_DETECTOR_PROCESS_RESTART_GRACE_SEC=30` handoff grace를 추가해, expected runtime window 안에서도 `pid_dead`이지만 마지막 main heartbeat age가 restart grace 이내면 fail이 아니라 `restart_grace_pid_handoff` warning/recheck로 분류한다. grace가 지나도 PID/heartbeat가 회복되지 않으면 기존 fail 경로를 유지한다. 이 조치는 detector false-positive 보정이며 runtime threshold/provider/order guard 변경이 아니다. 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_error_detector_process_health.py src/tests/test_constants.py -k 'process_health or error_detector'` -> `15 passed, 10 deselected`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/error_detectors/process_health.py src/utils/constants.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode health_only --dry-run` -> `summary_severity=pass`, `main_loop_pid=49065`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode log_only --dry-run` -> `summary_severity=pass`.

- 운영 확인 기록 (`OpenAITransportFixtureNoise07550515`): 판정은 `test_fixture_log_noise_fixed`. `2026-05-15 07:55:02 KST` full error detection fail은 `log_scanner`가 `ai_engine_openai_error.log(+10)`을 `UNKNOWN/API_ERROR/TIMEOUT_ERROR/WEBSOCKET_ERROR`로 분류한 것이다. 실제 10줄은 `테스트`, `test: ws timeout`, `OpenAI WS fail-closed ... 테스트`, `request_id mismatch`, `invalid_prompt retry` 등 OpenAI transport pytest/self-test fixture signature였고, 실제 운영 종목 오류가 아니다. 원인은 `log_scanner` ignore rule이 영문 `TEST`만 제외하고 한국어 `테스트` 및 `test:` context를 제외하지 못한 것이다. `테스트`/`test:` fixture signature를 scanner ignore rule에 추가했으며 runtime provider/threshold/order guard 변경은 없다. 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_error_detector_log_scanner.py` -> `15 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/error_detectors/log_scanner.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode log_only --dry-run` -> `summary_severity=pass`, `log_scanner=pass`.

- 운영 확인 기록 (`AIInputFreshnessProvenance0515`): 판정은 `source_quality_operational_override_applied`. 스캘핑은 장중 즉시성 데이터이므로 BUY drought의 AI 입력 stale/flat 여부를 장후까지 미루지 않고 1~2시간 내 운영 오버라이드 대상으로 확인한다. 새 report/cron/threshold 축은 만들지 않고 기존 `ai_confirmed`/`blocked_ai_score` provenance를 보강해 `tick_sample_count`, `tick_latest_time`, `tick_latest_age_ms`, `tick_window_span_sec`, `tick_accel_source`, `tick_accel_effective_recent_5tick_seconds`, `tick_acceleration_ratio_raw`, `tick_context_stale`, `tick_context_quality`, `quote_age_ms`, `quote_age_source`, `quote_stale`를 JSONL에 남기도록 했다. 10:35 이후 재기동 표본 29건 확인 결과 `ai_confirmed` 13건 중 `accel_zero_recent_window` 5건이 있었고, tick latest age는 대체로 0~2초라 missing/stale보다 같은 초에 5틱이 몰리며 `recent_5tick_seconds=0`으로 가속도가 0 처리되는 feature flatness 문제로 판단했다. [scalping_feature_packet.py](/home/ubuntu/KORStockScan/src/engine/scalping_feature_packet.py)는 같은 초 10틱 burst에서 이전 5틱 window가 있으면 effective recent window를 1초로 두고 `tick_accel_source=same_second_burst_10ticks`, `tick_context_quality=fresh_computed`로 산출하도록 보정했다. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 `ai_confirmed`, `blocked_ai_score`, wait65/79 EV 후보, recovery probe 이벤트에 같은 source-quality 필드를 전달한다. 이 조치는 AI 입력 데이터 품질 보정이며 score threshold, selected runtime family, 주문 guard, provider route를 바꾸는 장중 threshold mutation이 아니다. `restart.flag` 기반 우아한 재기동으로 `bot_main.py` PID가 `13479 -> 34781 -> 36360 -> 37367`로 교체됐고 새 PID env는 `KORSTOCKSCAN_THRESHOLD_RUNTIME_APPLY_DATE=2026-05-15`, `KORSTOCKSCAN_SCALP_SOFT_STOP_WHIPSAW_CONFIRMATION_ENABLED=true`, `KORSTOCKSCAN_SCALPING_AI_ROUTE=openai`, `KORSTOCKSCAN_OPENAI_TRANSPORT_MODE=responses_ws`, `KORSTOCKSCAN_OPENAI_RESPONSES_WS_ENABLED=true`를 유지한다. 재기동 후 10:42 KST 표본에는 `same_second_burst_10ticks`가 `fresh_computed`로 기록됐고, 10:46 KST health check는 `summary_severity=pass`, `main_loop_pid=37367`, thread status=`ok`다. 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_scalping_feature_packet.py src/tests/test_ai_engine_openai_v2_audit_fields.py::test_openai_scalping_analyze_target_returns_feature_audit_fields` -> `5 passed, 1 warning`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/scalping_feature_packet.py src/engine/sniper_state_handlers.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode health_only --dry-run` -> `summary_severity=pass`.

- 운영 확인 기록 (`AIInputFreshnessDuplicateKwarg10510515`): 판정은 `runtime_error_fixed_and_restarted`. `2026-05-15 10:43~10:51 KST` log scanner UNKNOWN burst는 [sniper_state_handlers_error.log](/home/ubuntu/KORStockScan/logs/sniper_state_handlers_error.log)의 `src.engine.sniper_state_handlers._log_entry_pipeline() got multiple values for keyword argument 'tick_source_quality_fields_sent'` 반복이다. 원인은 `AIInputFreshnessProvenance0515` 보강 직후 `ai_confirmed`와 non-50 `blocked_ai_score`가 `feature_probe`의 source-quality 필드와 `ai_decision`의 audit 필드를 동시에 kwargs로 펼친 것이다. 해당 예외는 AI 호출 자체가 아니라 이벤트 로깅 예외였지만, try 블록 전체가 실패하며 해당 종목은 `Score 50 매수보류 override`로 처리됐다. 조치: [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)의 `ai_confirmed`와 non-50 `blocked_ai_score`에서는 `ai_decision` audit 필드만 사용하도록 중복 `feature_probe` kwargs를 제거했고, wait65/79 후보와 recovery probe처럼 AI ops audit 필드를 같이 펼치지 않는 이벤트는 source-quality 필드 전달을 유지했다. `restart.flag` 기반 우아한 재기동으로 `bot_main.py` PID가 `37367 -> 39117`로 교체됐고 새 PID env는 OpenAI WS route와 `soft_stop_whipsaw_confirmation` selected family를 유지한다. 검증: `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/sniper_state_handlers.py src/engine/scalping_feature_packet.py`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_scalping_feature_packet.py src/tests/test_ai_engine_openai_v2_audit_fields.py::test_openai_scalping_analyze_target_returns_feature_audit_fields` -> `5 passed, 1 warning`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode health_only --dry-run` -> `summary_severity=pass`, `main_loop_pid=39117`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode log_only --dry-run` -> `summary_severity=pass`, `No new error log entries detected`.

- [x] `[ScalpAIInputFreshnessRecheck0515] 스캘핑 AI 입력 freshness/flatness 운영 오버라이드 후속 재확인` (`Due: 2026-05-15`, `Slot: INTRADAY`, `TimeWindow: 11:30~12:30`, `Track: ScalpingLogic`) (`실행: 2026-05-15 11:25~11:36 KST`)
  - Source: [pipeline_events_2026-05-15.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-15.jsonl), [scalping_feature_packet.py](/home/ubuntu/KORStockScan/src/engine/scalping_feature_packet.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [kiwoom_websocket.py](/home/ubuntu/KORStockScan/src/engine/kiwoom_websocket.py)
  - 판정 기준: 운영 오버라이드 적용 후 `ai_confirmed`/`blocked_ai_score`에 `tick_source_quality_fields_sent=true`, `tick_accel_source`, `tick_accel_effective_recent_5tick_seconds`, `tick_context_quality`, `quote_age_source`가 찍히고, same-second burst가 `accel_zero_recent_window`가 아니라 `same_second_burst_10ticks`/`fresh_computed`로 분류되는지 확인한다. 5틱 외에 quote timestamp provenance(`quote_age_source=missing`)와 AI 전 단계 strength momentum 차단 로그의 candle/range 기본값(`distance_from_day_high_pct=0`, `intraday_range_pct=0`)도 source-quality gap으로 함께 본다.
  - 금지: 이 재확인을 score threshold, selected runtime family, 주문 guard, provider route 장중 변경 근거로 쓰지 않는다.
  - 중간점검 (`2026-05-15 11:01 KST`): `partial_ok_with_source_quality_gap`. `10:52` 이후 `ai_confirmed=25`, `blocked_ai_score=34`, `strength_momentum_observed=9494`, `blocked_strength_momentum=9494`를 확인했다. `ai_confirmed` 24/25건은 `tick_source_quality_fields_sent=true`, `tick_context_quality=fresh_computed`이고 `tick_accel_source`는 `computed_10ticks=18`, `same_second_burst_10ticks=6`으로 `accel_zero_recent_window` 재발은 없었다. `blocked_ai_score`도 감사필드가 붙은 표본 8건은 모두 `fresh_computed`였고 duplicate kwarg 오류 재발은 없으며 `log_only --dry-run`은 pass다. 남은 gap은 `quote_age_source=missing`이 AI 감사필드 표본 전부에서 계속 발생해 quote freshness 판정이 불가능한 점, 그리고 `blocked_strength_momentum` 9494건에서 `distance_from_day_high_pct=0`, `intraday_range_pct=0`이 전부 0으로 남는 점이다. 이 중간점검은 source-quality 확인이며 장중 threshold/env/provider/order guard 변경 근거가 아니다.
  - 최종판정 (`2026-05-15 11:36 KST`): `same_second_burst_fixed_with_quote_source_gap`. 공식 창 직전 `11:25` 재집계에서는 `10:52` 이후 `ai_confirmed=79`, `blocked_ai_score=102`, `blocked_strength_momentum=32621`였고, 감사필드가 붙은 `ai_confirmed` 74건은 모두 `fresh_computed`(`computed_10ticks=46`, `same_second_burst_10ticks=28`)라 `accel_zero_recent_window` 재발은 없었다. `quote_age_source=missing`은 계속 남아 quote freshness는 별도 source gap으로 유지한다. `blocked_strength_momentum`의 range 0 고정 원인은 WS 캐시가 Kiwoom 실시간 FID `17/18` 고가/저가를 보존하지 않고, AI overlap snapshot이 candle 없는 전단 차단 경로에서 `ws_data` 고가/저가를 쓰지 못한 것이었다. 조치로 [kiwoom_websocket.py](/home/ubuntu/KORStockScan/src/engine/kiwoom_websocket.py)는 `high/low`를 캐시에 저장하고, [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 candle 없이도 `ws_data`의 `high/low`로 `distance_from_day_high_pct`/`intraday_range_pct`를 산출한다. 또한 `blocked_ai_score` 후속 차단 로그가 직전 AI source-quality snapshot을 이어받도록 보강했다. `restart.flag` 재기동으로 PID는 `41288 -> 47043 -> 47852 -> 49065`로 교체됐고, 최종 PID env는 OpenAI WS route와 `soft_stop_whipsaw_confirmation` selected family를 유지한다. `11:30:02~11:32:29` 표본은 `blocked_strength_momentum=2165` 중 `intraday_range_pct/distance_from_day_high_pct` 비영(非0)이 각각 `2093`건이었고, `11:34:32~11:36:23` 최종 PID 표본은 `blocked_strength_momentum=1331` 중 비영 값이 각각 `1328`건이었다. 같은 최종 표본에서 `ai_confirmed=14`, `blocked_ai_score=14`이고 감사필드가 존재하는 8쌍은 모두 `tick_source_quality_fields_sent=true`, `tick_context_quality=fresh_computed`, `tick_accel_source=computed_10ticks/same_second_burst_10ticks`, `quote_age_source=missing`으로 기록됐다. 감사필드가 없는 score 50류 표본 6쌍은 AI audit payload 자체가 비어 있는 source-quality gap으로 남기며, BUY drought 판단에는 threshold 변경이 아니라 AI scoring/prompt/source-quality workorder 후보로 분리한다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_state_handler_fast_signatures.py src/tests/test_kiwoom_websocket.py src/tests/test_scalping_feature_packet.py src/tests/test_ai_engine_openai_v2_audit_fields.py::test_openai_scalping_analyze_target_returns_feature_audit_fields` -> `32 passed, 1 warning`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/sniper_state_handlers.py src/engine/scalping_feature_packet.py src/engine/kiwoom_websocket.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode health_only --dry-run` -> `summary_severity=pass`, `main_loop_pid=49065`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode log_only --dry-run` -> `summary_severity=pass`.
  - 다음 액션: `quote_age_source=missing`과 AI audit payload가 비는 score 50류 표본은 별도 source-quality workorder로 유지한다. 현재 항목은 threshold/env/provider/order guard 변경 없이 관찰 필드와 freshness/flatness 판정 가능성 보강으로 닫는다.
  - 재확인 (`2026-05-15 12:12 KST`): `post_followup_sample_ok_with_historical_warning`. [observation_source_quality_audit_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/observation_source_quality_audit/observation_source_quality_audit_2026-05-15.json)을 재생성한 결과 전체 누적은 `status=warning`, `event_count=531388`, warning stage=`ai_confirmed`, `blocked_ai_score`, `wait65_79_ev_candidate`, `blocked_strength_momentum`, `blocked_overbought`, high-volume no-source-field=`swing_probe_state_persisted` 1개다. 다만 이는 11:30 이전 pre-fix 누적이 섞인 전체 일중 판정이다. `12:01:21~12:12:50` 후속 표본만 분리하면 `ai_confirmed=24` 중 21건(87.5%)에 `tick_source_quality_fields_sent`, `tick_accel_source`, `tick_context_quality`, `quote_age_source`, OpenAI WS provenance가 붙었고 `tick_context_quality=fresh_computed`, `openai_ws_http_fallback=false`다. `blocked_ai_score=36`은 score50 `not_evaluated/ai_score_50_buy_hold_override_no_tick_audit` 15건과 실제 AI 평가 `fresh_computed` 21건으로 분리된다. `ai_cooldown_blocked=14`는 전부 `ai_input_source_quality_status=not_evaluated`, `reason=watching_ai_cooldown_active`로 찍혔다. 12:01 이후 `distance_from_day_high_pct`/`intraday_range_pct` zero-rate는 `ai_confirmed=0`, `blocked_ai_score=0`으로 회복됐고, 남은 `tick_acceleration_ratio_raw=0`은 10틱 window 내 실측 flatness로 기록되며 missing/stale로 보지 않는다.
  - 재확인 검증: `PYTHONPATH=. .venv/bin/python -m src.engine.observation_source_quality_audit --target-date 2026-05-15 --write` -> `status=warning`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode health_only --dry-run` -> `summary_severity=pass`, `main_loop_pid=56117`. 이 재확인은 source-quality 판정이며 threshold/env/provider/order/bot restart 근거가 아니다.
  - 즉시 보강 (`2026-05-15 12:20 KST`): `quote_age_source_gap_fixed_runtime_loaded`. `quote_age_source=missing`은 장후까지 미룰 필요가 없는 source-quality instrumentation gap으로 재분류했다. 원인은 [kiwoom_websocket.py](/home/ubuntu/KORStockScan/src/engine/kiwoom_websocket.py)가 WS cache에 `last_ws_update_ts`를 이미 저장하지만 [scalping_feature_packet.py](/home/ubuntu/KORStockScan/src/engine/scalping_feature_packet.py)의 quote age resolver가 이 키를 보지 않던 것이다. resolver에 `last_ws_update_ts`를 추가하고 `restart.flag`로 graceful restart해 PID `56117 -> 60947`로 교체했다. `12:19:00` 이후 신규 표본은 `ai_confirmed=6`, `blocked_ai_score=9`이며 `quote_age_source=last_ws_update_ts`가 10건, score50 not_evaluated 계열 `-`가 5건이다. 실제 AI 평가 표본의 `quote_age_ms`는 `346~622ms`, `tick_context_quality=fresh_computed`, `openai_ws_http_fallback=false`로 확인됐다. 이 조치는 source-quality provenance 보강이며 threshold/env/provider/order guard 변경이 아니다.
  - 즉시 보강 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_scalping_feature_packet.py src/tests/test_ai_engine_openai_v2_audit_fields.py::test_openai_scalping_analyze_target_returns_feature_audit_fields` -> `6 passed, 1 warning`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/scalping_feature_packet.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode health_only --dry-run` -> `summary_severity=pass`, `main_loop_pid=60947`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode log_only --dry-run` -> `summary_severity=pass`.

- 운영 확인 기록 (`ObservationSourceQualityAudit0515`): 판정은 `source_quality_audit_warning_report_created`. 관찰 필드 보강이 단일 AI tick 경로에 머물지 않도록 [observation_source_quality_audit.py](/home/ubuntu/KORStockScan/src/engine/observation_source_quality_audit.py)를 추가해 `data/pipeline_events/pipeline_events_YYYY-MM-DD.jsonl`의 stage별 source-quality field contract를 점검한다. 산출물은 [observation_source_quality_audit_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/observation_source_quality_audit/observation_source_quality_audit_2026-05-15.json) 및 [observation_source_quality_audit_2026-05-15.md](/home/ubuntu/KORStockScan/data/report/observation_source_quality_audit/observation_source_quality_audit_2026-05-15.md)이며 `metric_role=source_quality_gate`, `decision_authority=source_quality_only`, `runtime_effect=false`, forbidden uses=`runtime_threshold_apply/order_submit/provider_route_change/bot_restart/real_execution_quality_approval`이다. 전체 이벤트 기준 결과는 `status=warning`, `event_count=457732`, warning stage=`ai_confirmed`, `blocked_ai_score`, `wait65_79_ev_candidate`, `blocked_strength_momentum`, `blocked_overbought`다. 단, `blocked_strength_momentum`/`blocked_overbought`의 range zero-rate 경고는 11:30 이전 패치 전 표본이 대부분이며 `11:34:32` 이후 별도 스냅샷에서는 `blocked_strength_momentum` zero-rate가 `distance=0.0058`, `range=0.0019`로 회복됐다. 남은 실질 gap은 cooldown/score50 AI event가 실제 OpenAI 호출이 아닌데 `ai_confirmed`/`blocked_ai_score`에 남으며 `tick_source_quality_fields_sent`, `tick_accel_source`, `tick_context_quality`, `quote_age_source`가 비는 점, `wait65_79_ev_candidate` 일부가 tick audit을 상속하지 못하는 점, 그리고 `strength_momentum_observed`, `blocked_swing_score_vpw`, `blocked_swing_gap`, `strength_momentum_pass`, `blocked_liquidity`, `dynamic_vpw_override_pass`, `first_ai_wait`, `swing_probe_state_persisted` 같은 고빈도 진단 stage의 명시적 metric contract/source-quality 라벨 부족이다. 이 항목은 source-quality audit이며 threshold/env/provider/order guard 변경 근거가 아니다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_observation_source_quality_audit.py` -> `3 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/observation_source_quality_audit.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.observation_source_quality_audit --target-date 2026-05-15 --write` -> `status=warning`.
  - 다음 액션: cooldown/score50 이벤트에는 `ai_input_source_quality_status=not_evaluated`와 reason을 명시하고, wait65/79는 직전 AI source-quality snapshot 상속을 확인한다. 고빈도 진단 stage는 threshold 후보가 아니라 `ops_volume_diagnostic` 또는 `funnel_count` 계약으로 라벨링한 뒤 source-quality blocker로만 라우팅한다.
- 자동화체인 포함 확인 (`ObservationSourceQualityAutomationLink0515`): 판정은 `automation_gap_fixed_report_only`. 최초 확인 시 [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md)에는 artifact가 등록됐지만 [run_threshold_cycle_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh)와 [build_code_improvement_workorder.py](/home/ubuntu/KORStockScan/src/engine/build_code_improvement_workorder.py)에는 호출/consumer 연결이 없었다. 보강 후 postclose wrapper는 `THRESHOLD_CYCLE_RUN_OBSERVATION_SOURCE_QUALITY_AUDIT=true` 기본값으로 `src.engine.observation_source_quality_audit --target-date TARGET_DATE --write`를 `pipeline_event_verbosity` 뒤, code improvement workorder 앞에서 실행하고 JSON/Markdown artifact를 대기한다. code improvement workorder는 same-day `observation_source_quality_audit`를 source fingerprint와 source 목록에 포함하며 warning이면 `order_ai_source_quality_not_evaluated_provenance`, `order_high_volume_diagnostic_stage_contract_labels` 같은 `runtime_effect=false` instrumentation order로만 변환한다. 이 연결은 source-quality/report-only 자동화체인 포함이며 threshold/env/provider/order/bot restart 권한이 없다.
  - 검증: `bash -n deploy/run_threshold_cycle_postclose.sh`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_observation_source_quality_audit.py src/tests/test_build_code_improvement_workorder.py::test_build_code_improvement_workorder_adds_observation_source_quality_orders src/tests/test_build_code_improvement_workorder.py::test_build_code_improvement_workorder_adds_pipeline_event_verbosity_order` -> `5 passed`; `PYTHONPATH=. .venv/bin/python -m src.engine.build_code_improvement_workorder --date 2026-05-15 --max-orders 12` -> `observation_source_quality_source_order_count=2`, selected order=`order_ai_source_quality_not_evaluated_provenance`, `order_high_volume_diagnostic_stage_contract_labels`.
- 보강 실행 기록 (`ObservationSourceQualityFollowup0515`): 판정은 `followup_applied_and_restarted_report_only`. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 cooldown event에 `ai_input_source_quality_status=not_evaluated`, `ai_input_source_quality_reason=watching_ai_cooldown_active`, score50 buy-hold override에 `ai_score_50_buy_hold_override_no_tick_audit` reason을 남긴다. `wait65_79_ev_candidate`는 `feature_probe`에 tick audit이 없으면 `last_watching_ai_source_quality_fields`를 상속하고, 그래도 없으면 `wait65_79_ev_candidate_no_tick_audit`로 닫는다. 고빈도 diagnostic stage는 `metric_role=ops_volume_diagnostic|funnel_count`, `decision_authority=source_quality_only`, `runtime_effect=false`, `forbidden_uses=runtime_threshold_apply/order_submit/provider_route_change/bot_restart`, `source_quality_route=source_quality_blocker_or_workorder_only`를 명시한다. [observation_source_quality_audit.py](/home/ubuntu/KORStockScan/src/engine/observation_source_quality_audit.py)는 `metric_role`/`decision_authority`/`forbidden_uses`를 source-like contract field로 인식한다. `restart.flag`를 소모해 main PID `49065 -> 56117`로 교체했고, 새 PID env는 `KORSTOCKSCAN_THRESHOLD_RUNTIME_APPLY_DATE=2026-05-15`, `KORSTOCKSCAN_SCALPING_AI_ROUTE=openai`, `KORSTOCKSCAN_OPENAI_TRANSPORT_MODE=responses_ws`, `KORSTOCKSCAN_OPENAI_RESPONSES_WS_ENABLED=true`, `KORSTOCKSCAN_SCALP_SOFT_STOP_WHIPSAW_CONFIRMATION_ENABLED=true`를 유지한다. `12:01~12:02` 표본에서 `ai_cooldown_blocked`는 `not_evaluated/watch_cooldown` 4건, score50 `blocked_ai_score`는 `not_evaluated/ai_score_50_buy_hold_override_no_tick_audit`, non-score50 `blocked_ai_score`는 직전 tick audit 상속 후 `evaluated/fresh_computed`로 찍혔다. `strength_momentum_observed`, `blocked_swing_score_vpw`, `blocked_swing_gap`, `strength_momentum_pass`, `blocked_liquidity`, `dynamic_vpw_override_pass`, `first_ai_wait`에는 contract field가 찍혔고, `swing_probe_state_persisted`는 재기동 후 신규 persist 이벤트가 아직 없어 다음 발생 시 확인한다. 재생성 audit는 `event_count=497404`, `high_volume_no_source_field_stage_count=1`로 줄었으며 남은 1개는 pre-fix 누적 `swing_probe_state_persisted`다.
  - 검증: `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/sniper_state_handlers.py src/engine/observation_source_quality_audit.py src/engine/build_code_improvement_workorder.py`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_state_handler_fast_signatures.py src/tests/test_observation_source_quality_audit.py src/tests/test_build_code_improvement_workorder.py::test_build_code_improvement_workorder_adds_observation_source_quality_orders` -> `28 passed`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode health_only --dry-run` -> `summary_severity=pass`, `main_loop_pid=56117`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode log_only --dry-run` -> `summary_severity=pass`; `PYTHONPATH=. .venv/bin/python -m src.engine.observation_source_quality_audit --target-date 2026-05-15 --write` -> `status=warning`.
- 보강 실행 기록 (`SwingSourceQualityFollowup0515`): 판정은 `swing_source_quality_gap_reclassified_report_only`. 스윙 쪽 과소점검 우려에 따라 `swing_probe_state_persisted`와 `swing_scale_in_ofi_qi_confirmation` blocker를 재점검했다. [observation_source_quality_audit.py](/home/ubuntu/KORStockScan/src/engine/observation_source_quality_audit.py)는 `swing_probe_state_persisted`를 명시 contract stage로 등록해 고빈도 무계약 이벤트가 아니라 `simulation_book/simulation_owner/metric_role/decision_authority/runtime_effect/forbidden_uses` 기준으로 평가한다. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 스윙 micro context 로그에 `swing_micro_ws_quote_present`, `swing_micro_ws_quote_source`, `swing_micro_ws_quote_age_ms`, `swing_micro_ws_quote_stale`, `swing_micro_source_quality_status`를 추가해 `observer_unhealthy`가 실제 WS 호가 부재인지 observer feed gap인지 분리한다. source-quality 상태가 `observer_gap_with_fresh_ws_quote`여도 실주문/threshold 판단은 바꾸지 않고 workorder/provenance 입력으로만 쓴다. `restart.flag` 재기동으로 PID `60947 -> 63398`에 반영했고 OpenAI WS route와 selected family는 유지된다. 재생성 audit는 `event_count=596422`, `high_volume_no_source_field_stage_count=0`으로 개선됐고, `swing_probe_state_persisted`는 pre-fix 76건 때문에 contract warning으로 남으며 신규 1건에는 contract field가 붙었다. `scale_in_price_p2_observe`는 pre-fix 1/22건 누락으로 warning이나 `scale_in_price_resolved`와 스윙 probe scale-in 계열은 pass다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_observation_source_quality_audit.py src/tests/test_swing_model_selection_funnel_repair.py -k 'source_quality or swing_micro_context or ofi_qi or observation_source_quality'` -> `7 passed, 39 deselected, 2 warnings`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_sniper_scale_in.py -k 'swing_real_canary or scale_in or orderbook_micro'` -> `145 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/sniper_state_handlers.py src/engine/observation_source_quality_audit.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode health_only --dry-run` -> `summary_severity=pass`, `main_loop_pid=63398`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode log_only --dry-run` -> `summary_severity=pass`; `PYTHONPATH=. .venv/bin/python -m src.engine.observation_source_quality_audit --target-date 2026-05-15 --write` -> `status=warning`.
- 자동화체인 누락 재점검 (`SwingSourceQualityAutomationCoverage0515`): 판정은 `consumer_gap_fixed_report_only`. postclose wrapper, artifact freshness detector, detector coverage, traceability 문서는 이미 연결돼 있었지만 [build_code_improvement_workorder.py](/home/ubuntu/KORStockScan/src/engine/build_code_improvement_workorder.py)의 observation source-quality consumer가 AI `not_evaluated`와 high-volume diagnostic gap만 workorder로 변환하고, `swing_probe_state_persisted`/`scale_in_price_p2_observe` 같은 스윙 source-quality warning을 별도 order로 라우팅하지 않는 누락을 확인했다. 보강 후 `order_swing_source_quality_micro_context_provenance`가 생성되어 스윙 probe-state contract warning과 OFI/QI micro observer gap을 `runtime_effect=false`, `decision_authority=source_quality_only` workorder로만 전달한다. [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md)와 [time-based-operations-runbook.md](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md)는 `observer_gap_with_fresh_ws_quote`가 provenance/workorder 입력일 뿐 threshold/env/provider/order/bot restart 권한이 없다는 계약으로 보정했다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_build_code_improvement_workorder.py::test_build_code_improvement_workorder_adds_observation_source_quality_orders` -> `1 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/build_code_improvement_workorder.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.build_code_improvement_workorder --date 2026-05-15 --max-orders 12` -> `observation_source_quality_source_order_count=2`, 신규 order=`order_swing_source_quality_micro_context_provenance`, 제거 order=`order_high_volume_diagnostic_stage_contract_labels`.
- 자동화체인 누락 전수점검 (`AutomationCoverageAudit0515`): 판정은 `coverage_gap_fixed`. 오늘 코드베이스 수정 중 wrapper/report consumer/traceability에는 연결됐지만 detector artifact coverage에 빠진 항목을 점검한 결과 `market_panic_breadth_report`, `observation_source_quality_audit_report`, `codebase_performance_workorder_report`가 누락돼 있었다. [artifact_freshness.py](/home/ubuntu/KORStockScan/src/engine/error_detectors/artifact_freshness.py)에 세 artifact를 등록했고, [error_detector_coverage.py](/home/ubuntu/KORStockScan/src/engine/error_detector_coverage.py)의 required artifact set과 [test_error_detector_coverage.py](/home/ubuntu/KORStockScan/src/tests/test_error_detector_coverage.py)를 갱신했다. [test_threshold_cycle_wrappers.py](/home/ubuntu/KORStockScan/src/tests/test_threshold_cycle_wrappers.py)는 postclose wrapper 순서를 `pipeline_event_verbosity -> observation_source_quality_audit -> codebase_performance_workorder -> threshold_ev pre-pass -> code_improvement_workorder`로 고정한다. 이 조치는 자동화 누락 탐지 보강이며 runtime threshold/provider/order/bot mutation 권한이 없다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_error_detector_coverage.py src/tests/test_error_detector_artifact_freshness.py src/tests/test_threshold_cycle_wrappers.py` -> `32 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/error_detector_coverage.py src/engine/error_detectors/artifact_freshness.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode artifact_only --dry-run` -> `summary_severity=pass`, 신규 `market_panic_breadth_report=pass`, `observation_source_quality_audit_report=not_yet_due`, `codebase_performance_workorder_report=not_yet_due`.

- [x] `[RuntimeEnvIntradayObserve0515] 전일 selected runtime family 장중 provenance 및 rollback guard 확인` (`Due: 2026-05-15`, `Slot: INTRADAY`, `TimeWindow: 09:05~09:20`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-14.json)
  - 판정 기준: selected_families=soft_stop_whipsaw_confirmation가 runtime event provenance에 찍히는지 확인한다.
  - 금지: 장중 관찰 결과로 runtime threshold mutation을 수행하지 않는다.
  - 판정 (`2026-05-15 09:07 KST`): `runtime_env_present_event_sample_pending / rollback_guard_clear`.
  - 근거: [threshold_apply_2026-05-15.json](/home/ubuntu/KORStockScan/data/threshold_cycle/apply_plans/threshold_apply_2026-05-15.json)은 status=`auto_bounded_live_ready`, apply_mode=`auto_bounded_live`, runtime_change=`true`, runtime env override=`KORSTOCKSCAN_SCALP_SOFT_STOP_WHIPSAW_CONFIRMATION_ENABLED=true`를 기록한다. [threshold_runtime_env_2026-05-15.env](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_2026-05-15.env)와 [threshold_runtime_env_2026-05-15.json](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_2026-05-15.json)의 selected family도 `soft_stop_whipsaw_confirmation` 1개다. 실행 중인 `bot_main.py` PID `13479` env에는 `KORSTOCKSCAN_THRESHOLD_RUNTIME_APPLY_DATE=2026-05-15`, `KORSTOCKSCAN_SCALP_SOFT_STOP_WHIPSAW_CONFIRMATION_ENABLED=true`, `KORSTOCKSCAN_SCALPING_AI_ROUTE=openai`, `KORSTOCKSCAN_OPENAI_TRANSPORT_MODE=responses_ws`가 들어 있다.
  - 이벤트 확인: [pipeline_events_2026-05-15.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-15.jsonl)은 `2026-05-15 09:07 KST` 기준 JSONL 4,737건이며 `soft_stop_whipsaw_confirmation=0`, `rollback=0`, `safety_revert=0`, `threshold_runtime_mutation=0`, `runtime_mutation=0`, `unauthorized=0`, `guard_breach=0`이다. 따라서 env 적용은 present지만 holding/soft-stop runtime sample이 아직 없어 runtime event provenance는 `sample_pending`으로 분리한다.
  - 검증: `/proc/13479/environ` runtime env 확인, `threshold_runtime_env_2026-05-15.env/json` 확인, `threshold_apply_2026-05-15.json` 확인, `pipeline_events_2026-05-15.jsonl` JSONL 카운트 스캔.
  - 다음 액션: 장중 threshold mutation은 하지 않는다. 장후 `ThresholdDailyEVReport0515`/post-apply attribution에서 selected/applied/not-applied cohort와 실제 soft-stop runtime event provenance를 재확인한다.


- [x] `[SimProbeIntradayCoverage0515] sim/probe 관찰축 actual_order_submitted=false 및 source-quality 확인` (`Due: 2026-05-15`, `Slot: INTRADAY`, `TimeWindow: 09:35~09:50`, `Track: ScalpingLogic`)
  - Source: [threshold_cycle_ev_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-14.json), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [swing_daily_simulation_report.py](/home/ubuntu/KORStockScan/src/engine/swing_daily_simulation_report.py)
  - 판정 기준: 스캘핑·스윙 모두에서 BUY/선정 가능 후보가 실주문 예수금/real cap/approval 부재와 무관하게 sim/probe 후보로 남고, `actual_order_submitted=false` provenance가 유지되는지 확인한다.
  - 금지: sim/probe EV를 broker execution 품질이나 실주문 전환 근거로 단독 사용하지 않는다.
  - 판정 (`2026-05-15 KST`): `partial_gap_fixed_with_remaining_declared_proxy`.
  - 근거: `OpenAI entry_price 표본 0건` 원인과 같은 유형의 silent gap을 점검한 결과, 스캘핑 sim entry는 `entry_price` canary 미연결을 이미 보정했고 추가로 passive-probe `stale_context_or_quote` submit revalidation이 sim pending 전에 적용되지 않던 gap을 보정했다. 스캘핑/스윙 scale-in 공통 함수도 sim/probe 분기 전에 실계좌 `get_deposit()`를 읽어 실계좌 주문가능금액이 sim scale-in 후보를 막을 수 있었으므로, simulated position은 `SIM_VIRTUAL_BUDGET_KRW`를 deposit 입력으로 쓰고 `virtual_budget_override=true`, `budget_authority=sim_virtual_not_real_orderable_amount`를 남기도록 보정했다.
  - 현재 동등/비동등 분류:
    - 스캘핑 runtime sim: BUY trigger를 넓게 잡기 위해 real budget/real broker submit은 의도적으로 건너뛰지만, entry price canary, passive submit revalidation, holding/exit, scale-in price resolver/dynamic qty는 runtime 함수와 연결된다. 결과는 `actual_order_submitted=false`이며 broker execution 품질 근거가 아니다.
    - 스윙 intraday probe/dry-run: real order submit은 금지하고, blocked stage에서 current price 기반 virtual holding을 만든다. holding/exit와 scale-in은 runtime handler를 공유하되, probe open/per-symbol/daily quota 초과는 `swing_probe_discarded`로 명시 기록된다.
    - 스윙 daily simulation: `runtime_order_dry_run_daily_proxy`이며 gatekeeper AI/tick/orderbook 입력을 일봉으로 replay할 수 없어 `gatekeeper_mode=dry_run_assumed_pass`로 표시한다. 이는 broad source bundle/approval 후보 입력이지 실시간 gatekeeper transport 또는 broker-equivalent 실행 검증이 아니다.
  - 이벤트 확인: `pipeline_events_2026-05-14.jsonl` 기준 `scalp_sim_entry_armed=1`, `scalp_sim_buy_order_assumed_filled=1`, `scalp_sim_sell_order_assumed_filled=2`, `swing_probe_entry_candidate=15`, `swing_probe_sell_order_assumed_filled=15`, `swing_sim_scale_in_order_assumed_filled=1`, `swing_probe_scale_in_order_assumed_filled=1`이 있었고, `swing_probe_discarded=14898`은 quota/duplicate/cooldown 등 명시 discard로 남았다.
  - 리포트 범위 확인 (`2026-05-15 KST`): 시뮬레이션 변경으로 새 canonical report chain은 만들지 않는다. 다만 `scalp_sim_entry_ai_price_applied`, `scalp_sim_entry_ai_price_skip_order`, `scalp_sim_entry_submit_revalidation_warning`, `scalp_sim_entry_submit_revalidation_block`은 `pre_submit_price_guard`, `scalp_sim_scale_in_order_*`는 `scale_in_price_guard`의 sim-only 관찰 범위로 명시 registry/리포트 집계에 포함하도록 보정했다. 실주문 calibration 값과 broker execution 품질에는 섞지 않고, `threshold_cycle_ev`의 Scalp Simulator 섹션에서 applied/skip, revalidation warning/block, scale-in filled/unfilled를 별도로 보이게 한다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_scalp_live_simulator.py` -> `21 passed`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_sniper_scale_in.py -k 'entry_ai_price_canary or passive_probe_stale'` -> `4 passed, 141 deselected`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_swing_model_selection_funnel_repair.py -k 'swing_intraday_probe or swing_probe or swing_same_symbol'` -> `13 passed, 28 deselected`.
  - 다음 액션: 장후 `SimFirstLifecycleCoverageAudit0515`에서 `swing_probe_discarded` 사유별 분포와 quota가 후보 coverage를 과도하게 줄이는지 확인한다. daily swing simulation은 live-equivalent로 승격하지 않고 proxy 권한을 유지한다.

- 운영 확인 기록 (`MarketPanicBreadthCollector0515`): 판정은 `live_breadth_source_added_report_only`. 패닉 디텍션 freshness 보강은 fallback flag가 아니라 Kiwoom live 수집원 추가로 처리했다. [market_panic_breadth_collector.py](/home/ubuntu/KORStockScan/src/engine/market_panic_breadth_collector.py)는 Kiwoom REST `ka20003` `/api/dostk/sect`를 `inds_cd=001`, `inds_cd=101`로 조회해 KOSPI/KOSDAQ 지수 등락률, 업종 등락률, 상승/보합/하락 종목수, 상장종목수를 `data/report/market_panic_breadth/market_panic_breadth_YYYY-MM-DD.json`에 저장한다. 공식 Kiwoom 문서 기준 WS `0J` 업종지수와 `0U` 업종등락은 향후 저지연 소스로 문서화했지만 이번 변경은 기존 panic sell 2분 cadence wrapper의 best-effort REST pre-step으로 적용했다. [panic_sell_defense_report.py](/home/ubuntu/KORStockScan/src/engine/panic_sell_defense_report.py)는 이 산출물의 `risk_off_advisory`를 `microstructure_market_context`에 직접 소비하되, policy는 `report_only_no_mutation`, `decision_authority=source_quality_only`, forbidden uses=`runtime_threshold_apply/order_submit/auto_sell/bot_restart/provider_route_change`를 유지한다. `2026-05-15 11:22 KST` 실수집 결과 `source_quality.status=ok`, `sample_count=65`, KOSPI `change_pct=-3.56`, KOSDAQ `change_pct=-3.25`, 업종 하락 비율 `96.8%`, severe down 비율 `76.2%`, KOSPI/KOSDAQ 하락 종목 비율 `79.0%/77.6%`로 `risk_off_advisory=true`가 산출됐고, panic sell report는 `panic_state=PANIC_SELL`, `panic_regime_mode=PANIC_DETECTED`, `runtime_effect=report_only_no_mutation`으로 재생성됐다. 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_market_panic_breadth_collector.py src/tests/test_panic_sell_defense_report.py` -> `14 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/market_panic_breadth_collector.py src/engine/panic_sell_defense_report.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode log_only --dry-run` -> `summary_severity=pass`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode health_only --dry-run` -> `summary_severity=pass`.

- 운영 확인 기록 (`PanicBuyingSourceQualityCarryForward0515`): 판정은 `partial_source_quality_improved_report_only`. `panic_buying`의 orderbook/trade aggressor missing은 실주문 로직 문제가 아니라 `pipeline_events` source-quality/결합 문제로 확인했다. [panic_buying_state_detector.py](/home/ubuntu/KORStockScan/src/engine/panic_buying_state_detector.py)는 가격 candle row만 평가 대상으로 삼아 가격 없는 micro snapshot을 버릴 수 있었고, `buy_ratio/exec_buy_ratio`만 있는 row는 절대 volume이 없으면 trade flow로 쓰지 못했다. 보강 후 같은 종목의 직전 trade/orderbook micro snapshot을 `120s` 이내에서만 candle 평가에 carry-forward하고, `buy_ratio` 단독 row는 normalized trade flow로 소비하며, `swing_micro_ws_best_bid/ask`, `orderbook_micro_qi` alias도 orderbook source로 인정한다. 재생성한 [panic_buying_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/panic_buying/panic_buying_2026-05-15.json)은 `panic_buy_state=NORMAL`, `panic_buy_regime_mode=NORMAL`, `runtime_effect=report_only_no_mutation`을 유지했고, micro source-quality는 `carried_orderbook_snapshot_count=203`, `carried_trade_aggressor_snapshot_count=313`, `missing_trade_aggressor_count=13`, `missing_orderbook_count=13`다. 남은 orderbook missing은 평가 대상 종목에 120초 이내 orderbook snapshot 자체가 없는 collector coverage gap으로 분리한다. 이 조치는 source-quality/report-only 보강이며 threshold/env/provider/order/bot restart 근거가 아니다. 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_panic_buying_report.py` -> `6 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/panic_buying_state_detector.py src/engine/panic_buying_report.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.panic_buying_report --date 2026-05-15 --print-json` -> `status=success`.

- 후속 보강 (`PanicBuyingMarketBreadthContext0515`): 판정은 `market_wide_context_added_report_only`. 보유종목/후보종목 micro coverage만으로는 시장 전체 패닉바잉을 판정할 수 없으므로 [market_panic_breadth_collector.py](/home/ubuntu/KORStockScan/src/engine/market_panic_breadth_collector.py)에 상승 breadth 기반 `risk_on_advisory`를 추가하고 [panic_buying_report.py](/home/ubuntu/KORStockScan/src/engine/panic_buying_report.py)가 `market_breadth_context`로 소비하도록 했다. 전체 종목 orderbook을 수집하지 않고 Kiwoom REST `ka20003` 업종/지수 breadth를 source-quality gate로 쓰는 구조다. 재생성 결과 [market_panic_breadth_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/market_panic_breadth/market_panic_breadth_2026-05-15.json)은 `risk_on_advisory=false`, `risk_off_advisory=true`, 업종 상승 비율 `3.2%`, KOSPI/KOSDAQ 상승종목 비율 `17.7%/12.0%`이고, [panic_buying_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/panic_buying/panic_buying_2026-05-15.json)은 `market_wide_panic_buy_confirmed=false`로 닫혔다. 이 값은 source-quality/risk-regime context이며 자동매수, 추격매수 차단, TP/trailing 변경, provider/bot/runtime 변경 권한이 없다. 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_market_panic_breadth_collector.py src/tests/test_panic_buying_report.py` -> `11 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/market_panic_breadth_collector.py src/engine/panic_buying_report.py src/engine/panic_buying_state_detector.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.market_panic_breadth_collector --date 2026-05-15 --print-json`; `PYTHONPATH=. .venv/bin/python -m src.engine.panic_buying_report --date 2026-05-15 --print-json`.

- 후속 보강 (`PanicBuyingConsumerRouting0515`): 판정은 `consumer_routing_fixed_source_quality_blocker`. `panic_buying`의 market breadth/micro coverage 필드를 자동화체인 소비자까지 연결했다. [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py)는 `market_breadth_risk_on_advisory`, `market_wide_panic_buy_confirmed`, `missing_orderbook_count`, `missing_trade_aggressor_count`, carry-forward count와 `source_quality_blockers`를 calibration source bundle에 싣는다. [runtime_approval_summary.py](/home/ubuntu/KORStockScan/src/engine/runtime_approval_summary.py)는 blocker가 있는 panic-buy request를 `freeze`로 판정하고, [build_code_improvement_workorder.py](/home/ubuntu/KORStockScan/src/engine/build_code_improvement_workorder.py)는 blocker가 존재하면 TP counterfactual 표본이 있어도 `panic_buy_runner_tp_canary` 후보가 아니라 `source_quality_blocker` workorder로 라우팅한다. 이 조치는 자동화체인 해석 보강이며 threshold/env/provider/order/bot runtime mutation 근거가 아니다. 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_daily_threshold_cycle_report.py::test_calibration_source_bundle_includes_panic_buying_read_only src/tests/test_runtime_approval_summary.py::test_runtime_approval_summary_surfaces_panic_approval_requests src/tests/test_runtime_approval_summary.py::test_runtime_approval_summary_freezes_panic_request_on_source_quality_blocker src/tests/test_build_code_improvement_workorder.py::test_build_code_improvement_workorder_adds_panic_lifecycle_orders src/tests/test_build_code_improvement_workorder.py::test_build_code_improvement_workorder_routes_panic_buy_source_quality_only` -> `5 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/daily_threshold_cycle_report.py src/engine/runtime_approval_summary.py src/engine/build_code_improvement_workorder.py`.

- 자동화체인 누락 점검 (`PanicDetectionAutomationChainAudit0515`): 판정은 `partial_gap_fixed_report_only`. `panic_sell_defense`, `panic_buying`, `market_panic_breadth`, `threshold_cycle_ev`, `runtime_approval_summary`, `code_improvement_workorder`, Telegram transition wrapper, error detector artifact/cron 감시, postclose verification을 전수 대조했다. R1 report, R6 EV/approval/workorder 소비, forbidden uses 계약과 error detector 감시는 연결돼 있었지만, 장중 [run_panic_buying_intraday.sh](/home/ubuntu/KORStockScan/deploy/run_panic_buying_intraday.sh)는 `market_panic_breadth_collector`를 직접 실행하지 않아 같은 2분 offset에서 panic sell wrapper보다 먼저 실행되면 직전 breadth를 읽을 수 있었고, [run_threshold_cycle_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh)도 postclose panic report 재생성 직전 market breadth를 강제 refresh하지 않았다. 또한 [verify_threshold_cycle_postclose_chain.py](/home/ubuntu/KORStockScan/src/engine/verify_threshold_cycle_postclose_chain.py)의 explicit artifact status에는 panic 세 산출물이 빠져 있었고, postclose window 전 수동 실행은 `fail`이 아니라 `not_yet_due`로 구분해야 했다. 보강 후 panic buying wrapper와 postclose wrapper가 panic report 생성 전에 `market_panic_breadth_collector`를 best-effort/pre-report로 실행하고, postclose는 `market_panic_breadth_YYYY-MM-DD.json` JSON validity를 기다린 뒤 panic sell/buying report를 재생성한다. postclose verification은 `market_panic_breadth`, `panic_sell_defense`, `panic_buying` artifact status를 같이 표면화하고 window 전 수동 검증은 `not_yet_due`로 닫는다. 이 조치는 source-quality freshness/검증 보강이며 threshold/env/provider/order/bot runtime mutation 근거가 아니다. 검증: `bash -n deploy/run_panic_buying_intraday.sh deploy/run_panic_sell_defense_intraday.sh deploy/run_threshold_cycle_postclose.sh`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_threshold_cycle_wrappers.py src/tests/test_verify_threshold_cycle_postclose_chain.py`; parser 검증은 `sync_docs_backlog_to_project --print-backlog-only`로 수행한다.

- 운영 확인 기록 (`DataRootArchiveCleanup0515`): 판정은 `archive_cleanup_applied_verified`. `data` 루트 하위 용량 점검 결과 `data/threshold_cycle/snapshots`의 immutable source snapshot 5개가 4.7GB를 차지했고, [compress_db_backfilled_files.py](/home/ubuntu/KORStockScan/src/engine/compress_db_backfilled_files.py)는 문서 계약과 달리 `threshold_cycle/snapshots/pipeline_events_YYYY-MM-DD_*.jsonl` 패턴을 압축 대상으로 잡지 못했다. 해당 snapshot 날짜 `2026-05-08`, `2026-05-11`, `2026-05-12`, `2026-05-13`, `2026-05-14`는 모두 `data/threshold_cycle/date=YYYY-MM-DD/family=*/part-*.jsonl` backfill 산출물이 존재하므로 검증 완료 snapshot으로 분류했다. 스크립트를 보강해 threshold backfill 산출물이 있는 snapshot만 gzip 압축하도록 했고, 실제 실행 결과 `threshold_snapshots.scanned=5`, `verified=5`, `compressed=5`, `saved_bytes=4.6GB`, `skipped_unverified=17`로 완료했다. `data/threshold_cycle`은 `4.7GB -> 445MB`, 전체 `data`는 `8.8GB -> 4.6GB`, root filesystem 사용률은 `72% -> 61%`로 개선됐다. 당일 raw `data/pipeline_events/pipeline_events_2026-05-15.jsonl`과 미검증 raw/snapshot은 건드리지 않았다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_compress_db_backfilled_files.py` -> `3 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/compress_db_backfilled_files.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.compress_db_backfilled_files --days 1 --dry-run` -> threshold snapshot 5개 verified; 실제 run -> threshold snapshot 5개 compressed.
  - 다음 액션: 다음 disk warning 때는 percent 기준 경보 보강과 `pipeline_event_summaries` retention/재생성 가능성 검토를 별도 workorder로 분리한다. 이 정리는 source snapshot 보관/압축이며 threshold/env/provider/order/bot runtime 변경 근거가 아니다.
  - 후속 보강 (`2026-05-15 KST`): 압축 snapshot을 향후 포렌식/재분석에서 그대로 쓸 수 있도록 [backfill_threshold_cycle_events.py](/home/ubuntu/KORStockScan/src/engine/backfill_threshold_cycle_events.py)에 `.jsonl.gz` 스트리밍 reader를 추가했다. 완료 checkpoint가 있는 압축 snapshot은 재처리 없이 `completed`로 닫고, overwrite 재생성 시에는 압축 source chunk cap을 `5,000` lines로 낮추며 기존 IO wait/memory guard에 `max_cpu_busy_pct` guard를 추가한다. [run_threshold_cycle_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh)는 기존 snapshot 검색/cleanup에서 `.jsonl.gz`를 함께 인식하고 `THRESHOLD_CYCLE_MAX_CPU_BUSY_PCT`로 CPU guard를 조절할 수 있다. [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py)의 legacy compact JSONL reader도 gzip을 읽을 수 있게 했다.
  - 후속 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_backfill_threshold_cycle_events.py src/tests/test_threshold_cycle_wrappers.py src/tests/test_compress_db_backfilled_files.py` -> `23 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/backfill_threshold_cycle_events.py src/engine/daily_threshold_cycle_report.py src/engine/compress_db_backfilled_files.py`; `bash -n deploy/run_threshold_cycle_postclose.sh`; `PYTHONPATH=. .venv/bin/python -m src.engine.backfill_threshold_cycle_events --date 2026-05-14 --mode incremental --resume --source-path data/threshold_cycle/snapshots/pipeline_events_2026-05-14_20260514_161001.jsonl.gz --max-input-lines-per-chunk 20000 --max-cpu-busy-pct 95` -> `status=completed`, `processed=0`, `source_compressed=true`, `effective_max_input_lines_per_chunk=5000`.
  - gzip consumer 추가 점검: `data/pipeline_events/*.jsonl.gz`, `data/report/monitor_snapshots/*.json.gz`, `data/log_archive/*/*.log.gz` 소비 경로를 재검색했다. 과거 raw는 `dashboard_data_repository.load_pipeline_events`가 DB 우선 fallback이라 압축 파일을 놓칠 수 있었고, monitor snapshot gzip은 `dashboard_data_repository`, `log_archive_service`, `daily_threshold_cycle_report` source bundle에서 plain `.json`만 보는 누락이 있었다. 보강 후 pipeline events/monitor snapshots는 `.gz` fallback을 읽고, daily threshold source bundle은 gz monitor snapshot을 `exists/loaded`로 집계한다. intraday 당일 report 전용 reader는 live append 중인 plain raw만 보는 경로라 이번 압축 포렌식 보강 대상에서 제외했다.
  - gzip consumer 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_dashboard_data_repository.py src/tests/test_log_archive_service.py src/tests/test_daily_threshold_cycle_report.py::test_calibration_source_bundle_reads_gzip_monitor_snapshot src/tests/test_daily_threshold_cycle_report.py::test_default_pipeline_load_result_reads_legacy_compact_gzip` -> `22 passed`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_backfill_threshold_cycle_events.py src/tests/test_threshold_cycle_wrappers.py src/tests/test_compress_db_backfilled_files.py` -> `23 passed`.

## 장후 체크리스트 (16:30~18:55)

- 운영 확인 기록 (`PostcloseAutomationHealthCheck20260515`): 판정은 `warning_resolved_detector_registry_gap`. `threshold_cycle_postclose` 최신 marker는 `[DONE]`이며 [threshold_cycle_postclose_verification_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_postclose_verification/threshold_cycle_postclose_verification_2026-05-15.json) status=`pass`, full profile stage flag가 모두 true, predecessor integrity=`pass`, 필수 artifact JSON validity가 닫혔다. [tuning_monitoring_postclose_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/tuning_monitoring/status/tuning_monitoring_postclose_2026-05-15.json)은 status=`success`이고 20:05 dependency 재스케줄 후 재실행도 `[DONE]`으로 끝났다. 최초 [error_detection_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/error_detection/error_detection_2026-05-15.json)은 `summary_severity=warning`이었고 원인은 `artifact_freshness`가 legacy flat `data/threshold_cycle/threshold_events_2026-05-15.jsonl`만 확인해 partitioned compact 산출물 `data/threshold_cycle/date=2026-05-15/family=*/part-*.jsonl`과 checkpoint `completed=true`, `written_count=706`을 pass로 인정하지 못한 detector registry gap이다. [artifact_freshness.py](/home/ubuntu/KORStockScan/src/engine/error_detectors/artifact_freshness.py)를 보정해 legacy 파일이 없을 때 partitioned compact checkpoint와 family part 파일을 확인하도록 했고, 재검증 결과 `threshold_events_status=pass_partitioned_checkpoint`, `summary_severity=pass`로 해소됐다. 이 조치는 운영 detector 판정 보정이며 runtime threshold/provider/order/bot 변경 근거가 아니다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_error_detector_artifact_freshness.py` -> `22 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/error_detectors/artifact_freshness.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode full --dry-run` -> `summary_severity=pass`.

- 운영 확인 기록 (`NprocCpuAffinityProfile0515`): 판정은 `applied_and_restarted`. m8g.xlarge 4 vCPU 변경 후 기존 2 vCPU 전제의 CPU affinity를 [cpu_affinity_profile.sh](/home/ubuntu/KORStockScan/deploy/cpu_affinity_profile.sh) 공통 helper로 전환했다. `nproc=4` 기준 bot 기본 affinity는 `0-1`, report-only/diagnostic wrapper는 `2-3`, health/sampler는 `3`으로 분리된다. [run_bot.sh](/home/ubuntu/KORStockScan/src/run_bot.sh), error detection, sentinel, panic, monitor snapshot, threshold calibration wrapper가 동일 profile을 소비한다. tmux `bot` 세션을 새 wrapper로 재기동했고 `bot_main.py` PID `3693`의 실제 affinity는 `0,1`이다. threshold runtime env와 OpenAI WS 설정은 유지됐다. 검증: `bash -n` 대상 wrapper 전체 통과, profile override smoke test `n=1/2/3/4` 통과, `health_only --dry-run` pass, `full --dry-run`은 resource/process/cron pass이며 `threshold_events` sparse stream warning만 남았다. 이 조치는 CPU 배치/운영 안정성 보강이며 threshold, 주문 guard, provider route 변경이 아니다.

- 운영 확인 기록 (`PostcloseCronDependencyReschedule0515`): 판정은 `cron_schedule_dependency_fixed`. `threshold_cycle_postclose`가 full profile 기준 19:49까지 길어지면서 기존 18:00 `tuning_monitoring_postclose`가 선행 산출물을 덜 소비할 수 있음을 확인했다. 실제 crontab과 [install_tuning_monitoring_postclose_cron.sh](/home/ubuntu/KORStockScan/deploy/install_tuning_monitoring_postclose_cron.sh)를 20:05 실행으로 변경하고, [run_tuning_monitoring_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_tuning_monitoring_postclose.sh)에 같은 날짜 `threshold_cycle_postclose` 최신 terminal marker가 `[DONE]`일 때만 Parquet/DuckDB refresh를 시작하는 predecessor guard를 추가했다. [cron_completion.py](/home/ubuntu/KORStockScan/src/engine/error_detectors/cron_completion.py)의 `tuning_monitoring_postclose` 감시 window는 `20:05~20:45`, [build_codex_daily_workorder.py](/home/ubuntu/KORStockScan/src/engine/build_codex_daily_workorder.py)와 [sync_docs_backlog_to_project.py](/home/ubuntu/KORStockScan/src/engine/sync_docs_backlog_to_project.py)의 RunbookOps POSTCLOSE window는 `16:10~20:45`로 맞췄다. `swing_model_retrain`은 threshold postclose 산출물 소비자가 아니므로 17:30 유지한다. 이 조치는 cron dependency/운영 집계 순서 보강이며 threshold/env/order/provider/bot runtime mutation이 아니다.
  - 검증: `bash -n deploy/run_tuning_monitoring_postclose.sh deploy/install_tuning_monitoring_postclose_cron.sh`; 임시 status 파일 기반 `TUNING_MONITORING_DRY_RUN=1 ... deploy/run_tuning_monitoring_postclose.sh 2026-05-15`에서 predecessor done을 확인하고 dry-run 완료; `PYTHONPATH=. .venv/bin/python -m src.engine.error_detector --mode cron_only --dry-run` -> `summary_severity=pass`.

- 운영 확인 기록 (`SimToLiveAutomationContractAudit0515`): 판정은 `gap_fixed_default_off_artifact_required`. 시뮬레이션/프로브 결과를 라이브로 반영하는 계약을 `source artifact -> approval request -> approval artifact -> preopen env -> runtime guard -> rollback/provenance` 기준으로 전수 점검했다. 실제 누락은 스윙 `swing_one_share_real_canary_phase0`였고, 기존에는 정책 문서만 있어 1주 canary도 `approval request -> 별도 artifact -> env -> initial BUY runtime guard`로 이어지지 않았다. 보강 후 [swing_runtime_approval_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/swing_runtime_approval/swing_runtime_approval_2026-05-15.json)은 `swing_one_share_real_canary_phase0` approval request를 생성한다. 하지만 `data/threshold_cycle/approvals/swing_one_share_real_canary_2026-05-15.json`이 없으므로 현재 라이브 반영은 차단 상태가 맞고, `SWING_LIVE_ORDER_DRY_RUN_ENABLED=True`는 유지된다. [sim-to-live-automation-contract-audit-2026-05-15.md](/home/ubuntu/KORStockScan/docs/sim-to-live-automation-contract-audit-2026-05-15.md)에 `position_sizing_cap_release`, `panic_entry_freeze_guard`, `panic_buy_runner_tp_canary`까지 포함한 전수표를 남겼다.
  - 구현: [swing_lifecycle_audit.py](/home/ubuntu/KORStockScan/src/engine/swing_lifecycle_audit.py)는 one-share request를 생성하고, [threshold_cycle_preopen_apply.py](/home/ubuntu/KORStockScan/src/engine/threshold_cycle_preopen_apply.py)는 `data/threshold_cycle/approvals/swing_one_share_real_canary_YYYY-MM-DD.json` artifact가 있을 때만 `KORSTOCKSCAN_SWING_ONE_SHARE_REAL_CANARY_*` env를 생성한다. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 승인 code allowlist, qty 1, daily/open/notional cap, stale/bearish submit guard를 runtime에서 fail-closed로 확인한다.
  - 추가 보강: [approval_contracts.py](/home/ubuntu/KORStockScan/src/engine/approval_contracts.py)를 추가해 approval 후보마다 `approval_contract_status`, `approval_live_ready`, `approval_artifact_path`, `approval_contract_missing_components`를 강제로 남긴다. 현재 ready 계약은 스윙 dry-run env approval, `swing_one_share_real_canary_phase0`, `swing_scale_in_real_canary_phase0`이고, `position_sizing_cap_release`, `position_sizing_dynamic_formula`, `panic_entry_freeze_guard`, `panic_buy_runner_tp_canary`는 `contract_missing`이다. 즉 이 축들은 조건이 충족되어도 approval artifact만 만들어서는 바로 live 반영할 수 없고, loader/env/runtime guard/rollback 테스트 작업이 필요하다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_threshold_cycle_preopen_apply.py` -> `12 passed`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_sniper_scale_in.py -k "one_share_real_canary or swing_real_canary"` -> `4 passed, 143 deselected`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_swing_model_selection_funnel_repair.py -k "runtime_approval_report_emits_machine_readable_requests"` -> `1 passed, 40 deselected`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_threshold_cycle_ev_report.py` -> `5 passed`.
  - 다음 액션: 사용자가 1주 canary를 승인하려면 승인 종목과 request id를 포함한 `swing_one_share_real_canary_YYYY-MM-DD.json` artifact를 별도로 만든 뒤 다음 PREOPEN apply에서만 소비한다. `position_sizing_cap_release`, `panic_entry_freeze_guard`, `panic_buy_runner_tp_canary`는 현재 live 연결 누락이 아니라 의도적 미구현 계약이므로 실제 적용하려면 새 approval artifact loader/env mapping/runtime guard/rollback 테스트 작업으로 연다.

- [x] `[SimFirstLifecycleCoverageAudit0515] 스캘핑/스윙 적극 sim-first 전주기 실행 및 consumer 연결 확인` (`Due: 2026-05-15`, `Slot: POSTCLOSE`, `TimeWindow: 16:20~16:30`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-14.json), [swing_lifecycle_audit_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/swing_lifecycle_audit/swing_lifecycle_audit_2026-05-14.json), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md)
  - 판정 기준: 스캘핑 `scalp_ai_buy_all`/missed probe와 스윙 dry-run/probe가 entry->holding->scale-in->exit 관찰축을 생성하고, daily EV/threshold cycle/code-improvement workorder/runtime approval summary consumer에 누락 없이 들어갔는지 확인한다.
  - 금지: closed sample 부족, real order 불가, approval artifact 부재를 sim/probe 후보 생성 중단 사유로 쓰지 않는다. sim/probe 결과를 실주문 품질로 섞지도 않는다.
  - 중간점검 (`2026-05-15 12:51 KST`): `partial_coverage_ok_with_scalp_buy_drought_and_test_event_pollution`. [pipeline_events_2026-05-15.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-15.jsonl) 기준 sim/probe 관련 이벤트는 5,422건이다. 스윙 probe는 `swing_probe_entry_candidate=27`, `swing_probe_holding_started=27`, `swing_probe_sell_order_assumed_filled=27`, `swing_probe_scale_in_order_assumed_filled=21`, `swing_sim_scale_in_order_assumed_filled=21`로 entry->holding->scale-in->exit 관찰축이 동작 중이다. `swing_probe_sell_order_assumed_filled` 27건의 단순 중간 집계는 승률 8/27, `simple_sum_profit_pct=+23.48`, `equal_weight_avg_profit_pct=+0.8696`, median `-1.55`, min `-3.06`, max `+22.69`이나, 이는 intraday probe/sim authority이며 real execution 품질이나 실주문 전환 근거가 아니다. discard는 `origin_quota_reached=3025`, `same_symbol_loss_reentry_cooldown=1042`, `max_per_symbol_reached=1039`, `max_open_reached=48`가 주 병목이다. 스캘핑은 `ai_confirmed=628` 중 `BUY=1`, `WAIT=624`, `DROP=3`, `buy_signal=0`, `order_leg_sent=0`이라 BUY drought가 계속되며, `scalp_sim_*` 4건은 모두 `TEST/123456` 표본으로 확인되어 실적 표본에서 제외해야 하는 test event pollution 후보로 분리한다.
  - 장후 판정 (`2026-05-15 20:23 KST`): `coverage_ok_with_sample_floor_gap_and_source_quality_warning`.
  - 근거: [threshold_cycle_ev_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-15.json)은 스캘핑 `scalp_ai_buy_all` simulator가 `event_count=91`, `entry_armed=2`, `buy_filled=2`, `holding_started=2`, `sell_completed=1`, `entry_ai_price_applied=1`, `entry_submit_revalidation_warning=1`, `entry_submit_revalidation_block=0`으로 entry->holding->exit 관찰축을 생성했음을 기록한다. [swing_lifecycle_audit_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/swing_lifecycle_audit/swing_lifecycle_audit_2026-05-15.json)은 `observation_axis_coverage.axis_count=10`, `ready_count=9`, `instrumentation_gap_count=0`, stage coverage=`selection/db_load/entry/holding/scale_in/exit/holding_exit`로 전주기 축을 닫았다. pipeline event raw count는 `swing_probe_entry_candidate=42`, `swing_probe_holding_started=42`, `swing_probe_scale_in_order_assumed_filled=28`, `swing_sim_scale_in_order_assumed_filled=28`, `swing_probe_sell_order_assumed_filled=42`다. 다만 swing daily simulation은 `rows=75`, `closed_count=0`, `sample_state=hold_sample`이고 OFI/QI stale/missing ratio는 `16/127=0.126` warning으로 남는다.
  - consumer 확인: daily EV, threshold calibration, runtime approval summary, code-improvement workorder가 모두 5/15 source bundle에 들어갔다. `actual_order_submitted=false` sim/probe는 threshold 개선/source-quality 입력으로만 사용하고 broker execution 품질이나 실주문 전환 근거로 섞지 않는다.
  - 다음 액션: `coverage_ok`, `consumer_gap`, `lifecycle_arm_gap`, `source_quality_blocker`, `sample_floor_gap` 중 하나 이상으로 닫고 gap은 workorder 후보로 연결한다.

- [x] `[WindowPolicyRegistryConsistency0515] threshold family window_policy 레지스트리와 daily/rolling/cumulative consumer 일관성 확인` (`Due: 2026-05-15`, `Slot: POSTCLOSE`, `TimeWindow: 16:30~16:40`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_2026-05-14.json), [threshold_cycle_cumulative_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_cumulative/threshold_cycle_cumulative_2026-05-14.json), [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [threshold_cycle_README.md](/home/ubuntu/KORStockScan/data/threshold_cycle/README.md)
  - 판정 기준: 각 family의 `window_policy.primary/secondary/daily_only_allowed`가 calibration candidate, `calibration_source_bundle_by_window`, AI correction input, preopen apply, EV summary, cumulative markdown sample denominator에 동일하게 반영됐는지 확인한다.
  - 금지: daily trigger 표본을 rolling/cumulative primary 표본처럼 표시하거나, registry와 다른 window 기준으로 threshold/code-improvement 결론을 내리지 않는다.
  - 실행 메모 (`2026-05-15 KST`): `daily_threshold_cycle_report.apply_window_policy_registry_to_report`가 `threshold_cycle_cumulative`의 `threshold_snapshot_by_window`와 `calibration_source_bundle_by_window`를 읽어 `window_policy_resolution`을 생성하고, non-daily primary family는 rolling/cumulative ready 표본으로 candidate를 재평가하는 것을 확인했다. `threshold_cycle_2026-05-14.json` 기준 `daily_only_leak_blocked=0`, `rolling_consumer_gap=0`, `sample_denominator_mismatch=0`이며 `rolling_source_snapshot_mismatch=4`는 source metric denominator가 snapshot denominator보다 큰 alignment warning으로 감사 필드에 표시된다. `threshold_apply_2026-05-15.json`은 selected family를 `soft_stop_whipsaw_confirmation` 1개로 제한했고, `score65_74_recovery_probe`는 rolling_5d decision window에서 `hold/no_runtime_env_override`로 남아 daily-only 승격 누수는 없었다.
  - 문서 보정: `data/threshold_cycle/README.md`의 `combined` 설명을 Plan Rebase와 코드의 `diagnostic_only_not_family_candidate_input` 계약에 맞게 수정했다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_daily_threshold_cycle_report.py -k 'window_policy or cumulative_threshold_cycle_report'` -> `7 passed, 33 deselected`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_threshold_cycle_preopen_apply.py` -> `10 passed`.
  - 다음 액션: `registry_consistent`, `daily_only_leak`, `rolling_consumer_gap`, `sample_denominator_mismatch`, `report_rendering_gap` 중 하나 이상으로 닫고 gap은 code-improvement workorder 후보로 연결한다.

- [x] `[ThresholdDailyEVReport0515] daily EV real/sim/combined split 및 자동 반영 결과 확인` (`Due: 2026-05-15`, `Slot: POSTCLOSE`, `TimeWindow: 16:40~16:55`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-14.json)
  - 판정 기준: real/sim/combined split, selected/blocked family, runtime_change, warning을 분리하고 sim 결과가 threshold 개선 후보 또는 workorder 후보로 소비됐는지 확인한다.
  - 금지: sim/combined EV만으로 broker execution 품질이나 live 전환을 확정하지 않는다.
  - 판정 (`2026-05-15 20:23 KST`): `pass_with_warning / next_preopen_input_ready`.
  - 근거: [threshold_cycle_ev_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-15.json)의 real/sim/combined split은 `real sample=1 avg=-1.55`, `sim sample=13 avg=+2.7815`, `combined sample=14 avg=+2.4721`이고, `combined_authority=diagnostic_only_not_family_candidate_input`로 broker execution 품질과 분리됐다. 당일 runtime apply는 `status=auto_bounded_live_ready`, selected family=`soft_stop_whipsaw_confirmation`, runtime env file=`threshold_runtime_env_2026-05-15.env`이며 `runtime_change=true`는 장전 apply 결과다. 장후 calibration 자체는 `runtime_change=false`이고 다음 장전 입력으로만 소비된다. `score65_74_recovery_probe`는 `adjust_up`, `sample_count=196`, `source_sample_count=19`, `sample_floor_status=ready`지만 `threshold_relaxation_approved=false`와 `panic_regime_mode=PANIC_DETECTED`를 동반하므로 장후 후보 입력이지 장중 수동 변경 근거가 아니다. `position_sizing_cap_release`와 `position_sizing_dynamic_formula`는 `hold_sample`, `pre_submit_price_guard`/`trailing_continuation`은 `freeze`로 분리됐다.
  - 다음 액션: 5/18 PREOPEN apply가 deterministic guard, approval artifact, same-stage owner rule을 다시 확인한다. sim/combined EV는 후보 산출용으로만 유지한다.

- [x] `[CodeImprovementWorkorderReview0515] code improvement workorder 구현 필요 여부 및 Codex 지시 대상 확인` (`Due: 2026-05-15`, `Slot: POSTCLOSE`, `TimeWindow: 16:55~17:10`, `Track: ScalpingLogic`)
  - Source: [code_improvement_workorder_2026-05-15.md](/home/ubuntu/KORStockScan/docs/code-improvement-workorders/code_improvement_workorder_2026-05-15.md), [code_improvement_workorder_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-05-15.json)
  - 판정 기준: selected_order_count=12와 `implement_now`, `attach_existing_family`, `design_family_candidate`, `reject` 분류를 확인하고, sim-first 전주기 실행/consumer 연결을 막는 성능·계측·source-quality 병목이 우선순위에 반영됐는지 확인한다.
  - 금지: code-improvement workorder를 자동 repo 수정으로 취급하지 않는다. 사용자가 Codex 구현을 지시한 경우에만 실행한다.
  - 실행 메모 (`2026-05-15 16:56 KST`): 2-pass로 처리했다. Pass1에서 `order_ai_source_quality_not_evaluated_provenance`, `order_swing_source_quality_micro_context_provenance`, `order_threshold_window_policy_source_snapshot_alignment`를 instrumentation/report/provenance 범위로 구현했고, 관련 postclose report/workorder를 재생성했다. 재생성 lineage는 `generation_id=2026-05-15-e7c091bff99f`, `source_hash=e7c091bff99f0fdc24e52ea07359feccac2d04e77c180eb7d7605fbb7d344567`, 신규 selected order는 `order_high_volume_diagnostic_stage_contract_labels`, `order_swing_gatekeeper_reject_threshold_review`, `order_swing_ofi_qi_stale_or_missing_context`, 제거 selected order는 `order_panic_buying_source_quality_market_breadth_micro_coverage`, `order_panic_sell_defense_lifecycle_transition_pack`, `order_threshold_window_policy_source_snapshot_alignment`, 판정 변경은 `0`건이었다. Pass2에서는 신규 `implement_now/runtime_effect=false`인 `order_high_volume_diagnostic_stage_contract_labels`만 추가 구현했다.
  - 검증: `PYTHONPATH=. .venv/bin/python -m pytest src/tests/test_state_handler_fast_signatures.py src/tests/test_sniper_scale_in.py::test_p2_observe_skip_does_not_change_live_order src/tests/test_sniper_scale_in.py::test_scalp_soft_stop_micro_grace_delays_exit src/tests/test_holding_flow_override.py src/tests/test_daily_threshold_cycle_report.py::test_window_policy_registry_consumes_rolling_source_metrics_when_snapshot_sample_is_empty src/tests/test_observation_source_quality_audit.py src/tests/test_build_code_improvement_workorder.py -q` -> `56 passed`; `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/sniper_state_handlers.py src/engine/daily_threshold_cycle_report.py src/engine/observation_source_quality_audit.py src/engine/build_code_improvement_workorder.py`; `PYTHONPATH=. .venv/bin/python -m src.engine.sync_docs_backlog_to_project --print-backlog-only --limit 500`; `git diff --check`.
  - 다음 액션: 신규 `attach_existing_family` 2건은 오늘 요청 범위 밖이라 보류한다. diagnostic label과 source-quality provenance는 다음 postclose source-quality audit에서 신규 이벤트 기준 warning 감소 여부를 확인한다.

- [x] `[HumanInterventionSummary0515] 자동화체인 사용자 개입 요구사항 분류 및 누락 확인` (`Due: 2026-05-15`, `Slot: POSTCLOSE`, `TimeWindow: 17:10~17:25`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-14.json), [time-based-operations-runbook.md](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md)
  - 판정 기준: 개입사항을 `승인 artifact 필요`, `Codex 구현 필요`, `수동 동기화 필요`, `관찰만`으로 분류한다.
  - 금지: 자동화 산출물에 있는 요청을 답변에만 남기고 checklist/Project 대상에서 누락하지 않는다.
  - 판정 (`2026-05-15 20:23 KST`): `classified_no_same_day_missing`.
  - 근거: [runtime_approval_summary_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/runtime_approval_summary/runtime_approval_summary_2026-05-15.json)은 scalping selected auto-bounded live `1`, swing requested `3`, swing approved `0`, panic approval requested `0`으로 닫았다. 사용자 승인 artifact 필요 항목은 `swing_model_floor`, `swing_gatekeeper_reject_cooldown`, `swing_one_share_real_canary_phase0`이며 artifact 없이는 다음 PREOPEN env/live order에 반영하지 않는다. Codex 구현 검토 항목은 [code_improvement_workorder_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-05-15.json)의 `selected_order_count=12`이고, 오늘 구현 지시 범위에서는 instrumentation/report/provenance 성격의 일부만 처리됐다. 수동 동기화 필요 항목은 Project/Calendar 표준 명령 1개다. 관찰만 항목은 panic sell/buy, sim/probe EV, pipeline event verbosity, source-quality warning이다.
  - 사용자 후속 승인 기록 (`2026-05-15 KST`): 사용자가 3건 모두 승인했다. 생성된 artifact는 [swing_runtime_approvals_2026-05-15.json](/home/ubuntu/KORStockScan/data/threshold_cycle/approvals/swing_runtime_approvals_2026-05-15.json)과 [swing_one_share_real_canary_2026-05-15.json](/home/ubuntu/KORStockScan/data/threshold_cycle/approvals/swing_one_share_real_canary_2026-05-15.json)이다. 승인 id는 `swing_runtime_approval:2026-05-15:swing_model_floor`, `swing_runtime_approval:2026-05-15:swing_gatekeeper_reject_cooldown`, `swing_one_share_real_canary:2026-05-15:phase0`이고 target_date는 `2026-05-18`이다.
  - 보고 보정: 최종 사용자 보고에는 `HumanInterventionSummary` 판정명만 쓰지 않고, `승인 필요/승인 완료` 섹션에 approval_id, artifact path, 후보/대상, 다음 PREOPEN 적용 여부를 명시한다.
  - 다음 액션: 다음 영업일 체크리스트 [2026-05-18-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-05-18-stage2-todo-checklist.md)에 PREOPEN approval 확인, INTRADAY sim/probe coverage, POSTCLOSE EV/workorder/human intervention/shadow-canary review가 이미 parser-friendly 체크박스로 생성됐다. Project/Calendar 동기화는 사용자가 표준 명령으로 수행한다.

- [x] `[PanicLifecycleModePolicyReview0515] 패닉셀/패닉바잉 mode policy와 V2 owner 분리 확인` (`Due: 2026-05-15`, `Slot: POSTCLOSE`, `TimeWindow: 17:25~17:40`, `Track: RuntimeStability`)
  - Source: [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md), [panic_entry_freeze_guard_v2_2026-05-13.md](/home/ubuntu/KORStockScan/docs/code-improvement-workorders/panic_entry_freeze_guard_v2_2026-05-13.md), [panic_buying_regime_mode_v2_2026-05-14.md](/home/ubuntu/KORStockScan/docs/code-improvement-workorders/panic_buying_regime_mode_v2_2026-05-14.md), [panic_sell_defense_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/panic_sell_defense/panic_sell_defense_2026-05-14.json), [panic_buying_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/panic_buying/panic_buying_2026-05-14.json), [runtime_approval_summary_2026-05-14.json](/home/ubuntu/KORStockScan/data/report/runtime_approval_summary/runtime_approval_summary_2026-05-14.json)
  - 판정 기준: `panic_regime_mode=NORMAL|PANIC_DETECTED|STABILIZING|RECOVERY_CONFIRMED`와 `panic_buy_regime_mode=NORMAL|PANIC_BUY_DETECTED|PANIC_BUY_CONTINUATION|PANIC_BUY_EXHAUSTION|COOLDOWN` 해석이 report-only/source-quality 계약으로 유지되는지 확인한다. 패닉셀 V2.0 `panic_entry_freeze_guard`, V2.1 미체결 진입 주문 cancel, V2.2 holding/exit context, V2.3 강제 축소/청산과 패닉바잉 V2.0 `panic_buy_runner_tp_canary`, V2.1 추격매수 차단, V2.2 continuation trailing, V2.3 exhaustion cleanup, V2.4 cooldown reentry guard owner를 섞지 않는다.
  - 금지: approval artifact, env key, rollback guard, same-stage owner rule 없이 신규 BUY 차단, 주문 취소, 자동매도, 시장가 전량청산, stop/TP/trailing/threshold/provider/bot restart 변경을 수행하지 않는다.
  - 판정 (`2026-05-15 20:23 KST`): `hold_report_only / source_quality_blocker / approval_contract_missing`.
  - 근거: [panic_sell_defense_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/panic_sell_defense/panic_sell_defense_2026-05-15.json)은 `panic_state=PANIC_SELL`, `panic_regime_mode=PANIC_DETECTED`지만 `panic_regime_contract.runtime_effect=report_only_no_mutation`, `allowed_runtime_apply=false`로 닫았다. V2.0 `panic_entry_freeze_guard`는 runtime approval summary에서 `approval_contract_missing`이며 누락 구성요소는 `approval_artifact_loader`, `preopen_env_mapping`, `entry_pre_submit_runtime_guard`, `rollback_tests`다. [panic_buying_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/panic_buying/panic_buying_2026-05-15.json)은 `panic_buy_state=NORMAL`, `panic_buy_regime_mode=NORMAL`, `market_wide_panic_buy_confirmed=false`, `risk_on_advisory=false`, `risk_off_advisory=true`이고 V2.0 `panic_buy_runner_tp_canary`는 `panic_buy_orderbook_collector_coverage_gap` source-quality blocker와 approval contract missing으로 freeze다.
  - 다음 액션: 패닉셀 V2.0과 패닉바잉 runner TP는 separate workorder/approval contract가 생기기 전까지 report-only다. V2.1 이후 order cancel, holding/exit context, forced liquidation, chase freeze, continuation trailing, exhaustion cleanup, cooldown reentry는 서로 다른 owner로 유지한다.

- [x] `[ShadowCanaryCohortReview0515] shadow/canary/cohort 런타임 분류 및 정리 판정` (`Due: 2026-05-15`, `Slot: POSTCLOSE`, `TimeWindow: 18:40~18:55`, `Track: Plan`)
  - Source: [workorder-shadow-canary-runtime-classification.md](/home/ubuntu/KORStockScan/docs/workorder-shadow-canary-runtime-classification.md)
  - 판정 기준: 당일 변경/관찰 결과를 기준으로 `remove`, `observe-only`, `baseline-promote`, `active-canary` 상태 변동 여부를 닫는다.
  - 금지: shadow 금지, canary-only, baseline 승격 원칙을 코드/문서 상태와 분리하지 않는다.
  - 판정 (`2026-05-15 20:23 KST`): `no_runtime_classification_change_with_snapshot_update`.
  - 근거: [runtime_approval_summary_2026-05-15.json](/home/ubuntu/KORStockScan/data/report/runtime_approval_summary/runtime_approval_summary_2026-05-15.json)은 `soft_stop_whipsaw_confirmation`만 selected auto-bounded-live로 유지하고, `score65_74_recovery_probe`/`bad_entry_refined_canary`는 후보 또는 observe/live-off, `pipeline_event_compaction_v2_shadow`는 report-only diagnostic aggregation, `panic_entry_freeze_guard`와 `panic_buy_runner_tp_canary`는 approval contract missing 또는 source-quality blocker로 runtime 권한 없음, 스윙 approval request 3건은 approval artifact missing으로 차단했다.
  - 문서 반영: [workorder-shadow-canary-runtime-classification.md](/home/ubuntu/KORStockScan/docs/workorder-shadow-canary-runtime-classification.md)에 `2026-05-15 POSTCLOSE Snapshot Addendum`을 추가해 cohort 분류를 잠갔다.
  - 다음 액션: 신규 `remove`, `baseline-promote`, 추가 `active-canary` 전환 없음. 다음 5/18 장후 review에서 same-day 변경 여부만 재확인한다.

- 운영 변경 기록 (`EODTop5AutoRunDisabled0515`): 판정은 `disabled_no_ai_call`. AI 호출 절감을 위해 `22:30` `eod_analyzer.py` cron 라인을 현재 crontab에서 제거했고, [eod_analyzer.py](/home/ubuntu/KORStockScan/src/scanners/eod_analyzer.py)는 `KORSTOCKSCAN_EOD_TOP5_ENABLED=true`가 명시되지 않으면 `[DONE] status=disabled_no_ai_call`로 종료하도록 막았다. [cron_completion.py](/home/ubuntu/KORStockScan/src/engine/error_detectors/cron_completion.py)와 [error_detector_coverage.py](/home/ubuntu/KORStockScan/src/engine/error_detector_coverage.py)에서도 `eod_analyzer` 반복 cron 기대값을 제거해 22:45 이후 disabled 상태를 장애로 보지 않게 했다. 이 변경은 내일의 주도주 TOP5 리포트/DB 예약 자동 실행 OFF이며, threshold daily EV, 스윙 dry-run/lifecycle, final ensemble scanner, update_kospi, 실주문 runtime guard는 변경하지 않는다. 재개하려면 cron 등록, `KORSTOCKSCAN_EOD_TOP5_ENABLED=true`, detector registry 복구, runbook 갱신을 같은 변경 세트로 처리한다.

<!-- AUTO_NEXT_STAGE2_CHECKLIST_END -->


## Project/Calendar 동기화

문서/checklist를 수정했으면 parser 검증은 실행하고, Project/Calendar 동기화는 사용자가 아래 명령으로 수동 실행한다.

```bash
PYTHONPATH=. .venv/bin/python -m src.engine.sync_docs_backlog_to_project && PYTHONPATH=. .venv/bin/python -m src.engine.sync_github_project_calendar
```

<!-- AUTO_SERVER_COMPARISON_START -->
### 본서버 vs songstockscan 자동 비교 (`2026-05-15 15:48:21`)

- 기준: `profit-derived metrics are excluded by default because fallback-normalized values such as NULL -> 0 can distort comparison`
- 상세 리포트: `data/report/server_comparison/server_comparison_2026-05-15.md`
- `Trade Review`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Performance Tuning`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Post Sell Feedback`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Entry Pipeline Flow`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
<!-- AUTO_SERVER_COMPARISON_END -->
