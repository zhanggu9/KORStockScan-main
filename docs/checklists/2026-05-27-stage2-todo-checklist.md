# 2026-05-27 Stage2 To-Do Checklist

## 오늘 목적

- 전일 postclose 자동화가 만든 장전 apply 후보와 사용자 개입 요구사항을 산출물 기준으로 확인한다.
- 실주문, threshold, provider, sim/probe 관련 변경은 approval artifact와 checklist 기준 없이 열지 않는다.
- code-improvement workorder는 자동 repo 수정이 아니라 사용자가 Codex에 구현을 지시한 경우에만 실행한다.

## 오늘 강제 규칙

- 장중 runtime threshold mutation은 금지한다. 적용은 PREOPEN `threshold_cycle_preopen_apply`가 생성한 runtime env만 source로 본다.
- provider transport/provenance 확인은 threshold 값, 주문가/수량 guard, 스윙 dry-run guard 변경과 분리한다.
- `actual_order_submitted=false`인 sim/probe 표본은 EV/source-quality 입력이며 실주문 전환 근거가 아니다.
- Project/Calendar 동기화는 사용자가 표준 동기화 명령으로 수행한다.

## 당일 운영 보완 기록

- [x] `[ScalpingOvernightDecisionTimeAdvance0527] 오버나이트 판정 시각 15:10 조정` (`Due: 2026-05-27`, `Slot: INTRADAY`, `TimeWindow: 15:45~16:05`, `Track: RuntimeStability`)
  - Source: [bot_history.log](/home/ubuntu/KORStockScan/logs/bot_history.log), [sniper_overnight_gatekeeper.py](/home/ubuntu/KORStockScan/src/engine/sniper_overnight_gatekeeper.py), [time-based-operations-runbook.md](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md)
  - 근거: `2026-05-27 15:20:06` `TIME K바이오액티브(463050)` SELL_TODAY 주문은 접수됐지만 40초 미체결 후 `15:20:47` 취소 및 HOLDING 롤백됐다. 15:20 판정은 청산 주문 체결/복구 여유가 부족하므로 기본 오버나이트 판정 시각을 `15:10:00`으로 당긴다.
  - 처리 결과: `SCALPING_OVERNIGHT_DECISION_TIME` 기본값과 sim overnight preclose cron을 `15:10`으로 맞췄고, runbook final window를 `15:10~15:30`으로 보정했다. `deploy/install_threshold_cycle_cron.sh`를 실행해 실제 crontab의 `SCALP_SIM_OVERNIGHT_PRECLOSE`도 `10 15 * * 1-5`로 반영했다. threshold/provider/order guard, cap, broker safety는 변경하지 않는다.
  - 다음 액션: 다음 장전에는 bot 시작 후 runtime 기준 `SCALPING_OVERNIGHT_DECISION_TIME=15:10:00`이 반영됐는지 확인한다.

- [x] `[PostcloseCronAdvance1600_0527] 장후 자동화체인 cron 16:00 조정` (`Due: 2026-05-27`, `Slot: INTRADAY`, `TimeWindow: 16:00~16:10`, `Track: RunbookOps`)
  - Source: [install_threshold_cycle_cron.sh](/home/ubuntu/KORStockScan/deploy/install_threshold_cycle_cron.sh), [time-based-operations-runbook.md](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md)
  - 근거: 15:10 오버나이트 판정과 15:10~15:30 미체결 청산 복구 window 이후 15:45 lightweight swing report를 유지하면 postclose chain은 16:00부터 시작해도 artifact wait/resource guard로 선행 지연을 흡수할 수 있다.
  - 처리 결과: postclose cron, runbook, daily workorder/backlog 기본 window를 `16:00~20:45`로 보정했고 actual crontab의 `THRESHOLD_CYCLE_POSTCLOSE`도 `0 16 * * 1-5`로 반영했다. threshold/provider/order guard, cap, broker safety는 변경하지 않는다.
  - 다음 액션: 다음 postclose에서 `[START]/[DONE] threshold-cycle postclose` marker, artifact wait, postclose isolation marker, verifier status를 확인한다.

## Runbook 운영 확인 완료 기록

- `PreopenAutomationHealthCheck20260527` (`Slot: PREOPEN`, `TimeWindow: 08:00~09:00`, `Track: RunbookOps`) 판정: `pass`
  - Source: [time-based-operations-runbook.md](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md) `장전 확인 절차`
  - 다음 액션: `RuntimeEnvIntradayObserve0527`에서 selected runtime family provenance, runtime apply bridge post-apply verification, Bedrock `entry_price` primary/OpenAI failback provenance를 재확인한다.

- `IntradayAutomationHealthCheck20260527` (`Slot: INTRADAY`, `TimeWindow: 09:05~15:30`, `Track: RunbookOps`) 판정: `warning`
  - Source: [time-based-operations-runbook.md](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md) `장중 확인 절차`
  - 근거: `2026-05-27 11:43 KST` `error_detector --mode full --dry-run`은 `summary_severity=warning`이지만 warning은 disk free `3707MB` resource watch다. `cron_completion`, `log_scanner`, `kiwoom_auth_8005_restart`, `process_health`, `artifact_freshness`, `stale_lock`은 pass이고 bot PID `87049`는 alive다. pipeline/threshold events와 sentinel/panic artifacts는 fresh다.
  - 다음 액션: threshold/provider/order guard는 변경하지 않고, postclose 전까지 disk free warning과 신규 8005/log burst만 관찰한다.
  - 디스크 정리 보완 (`2026-05-27 11:46 KST`): 판정=`resource_warning_cleared`. 근거: 당일 append 파일은 제외하고 과거 `pipeline_events`, `threshold_cycle/snapshots`, `pipeline_event_summaries`, `runtime/sentinel_event_cache`, rotate log 원본 `*.jsonl`/`*.log.1`만 gzip 압축했다. 루트 가용량은 `3683MB -> 11244MB`, `data`는 `15GB -> 7.8GB`, `logs`는 `311MB -> 135MB`로 감소했다. 재검증에서 `error_detector --mode full --dry-run`은 `summary_severity=pass`, `resource_usage=pass`, `disk_free_mb=11242.1`이다. 다음 액션: 현재 당일 append 파일은 유지하고 postclose 전까지 신규 대용량 증가만 관찰한다.
  - gzip reader 호환성 보완 (`2026-05-27 intraday`): 판정=`gzip_source_compatibility_supplemented`. 근거: 공통 [jsonl_io.py](/home/ubuntu/KORStockScan/src/utils/jsonl_io.py)는 `.jsonl -> .jsonl.gz` fallback을 지원하고 다수 report가 이를 사용한다. 추가 점검에서 [lifecycle_decision_matrix.py](/home/ubuntu/KORStockScan/src/engine/lifecycle_decision_matrix.py)와 [lifecycle_ai_context.py](/home/ubuntu/KORStockScan/src/engine/lifecycle_ai_context.py)의 로컬 `_iter_jsonl`은 gzip 파일 자체는 열지만 sibling fallback이 없어 과거 압축 파일을 놓칠 수 있어 보완했다. [pipeline_event_verbosity_report.py](/home/ubuntu/KORStockScan/src/engine/pipeline_event_verbosity_report.py)는 gzip raw를 읽을 때 압축 파일 크기를 raw byte denominator로 써 byte share가 100%를 넘고 producer summary `.jsonl.gz` 존재 판정이 틀어질 수 있어 보완했다. 코드리뷰 보완으로 raw denominator는 빈 줄/비JSON 라인까지 포함한 압축해제 stream byte로 고정했고, gzip storage size는 별도 `raw_storage_size_bytes`로 분리했다. 검증: 관련 pytest `25 passed`, py_compile pass, 실제 `2026-05-26` gzip 산출물 smoke에서 lifecycle AI entry eligible `3860`, lifecycle matrix scalp sim holding rows `274`, verbosity report `state=v2_shadow_pending_flush`, `producer_summary.exists=true`, `high_volume_byte_share_pct=36.89`다. 다음 액션: threshold/provider/order guard는 변경하지 않고 postclose 산출물이 gzip source를 정상 소비하는지 확인한다.
  - gzip reader 전수 보완 (`2026-05-27 intraday`): 판정=`remaining_reader_targets_supplemented`. 근거: 추가 전수 점검에서 이미 공통 reader/명시 `.jsonl.gz` 경로를 타는 consumer는 보완 대상에서 제외했다. 평문 sibling만 보고 source-only/rolling 표본을 놓칠 수 있던 [institutional_flow_context.py](/home/ubuntu/KORStockScan/src/engine/institutional_flow_context.py), [producer_gap_discovery.py](/home/ubuntu/KORStockScan/src/engine/automation/producer_gap_discovery.py), [time_window_regime_counterfactual.py](/home/ubuntu/KORStockScan/src/engine/automation/time_window_regime_counterfactual.py), [holding_exit_observation_report.py](/home/ubuntu/KORStockScan/src/engine/holding_exit_observation_report.py), [scalp_entry_action_decision_matrix.py](/home/ubuntu/KORStockScan/src/engine/scalp_entry_action_decision_matrix.py)를 공통 gzip fallback으로 보완했다. 코드리뷰 보완으로 rolling post-sell date discovery glob은 `.jsonl`/`.jsonl.gz`로만 제한해 임시 파일 오탐을 막았다. 검증: 관련 pytest `29 passed`, py_compile pass. 다음 액션: threshold/provider/order guard는 변경하지 않고 postclose 산출물에서 `.jsonl.gz` source 누락 여부만 관찰한다.

- `PostcloseAutomationHealthCheck20260527` (`Slot: POSTCLOSE`, `TimeWindow: 16:00~20:45`, `Track: RunbookOps`) 판정: `pass`
  - Source: [time-based-operations-runbook.md](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md) `장후 확인 절차`, [threshold_cycle_postclose_verification_2026-05-27.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_postclose_verification/threshold_cycle_postclose_verification_2026-05-27.json), [threshold_cycle_ev_2026-05-27.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-27.json), [runtime_approval_summary_2026-05-27.json](/home/ubuntu/KORStockScan/data/report/runtime_approval_summary/runtime_approval_summary_2026-05-27.json)
  - 근거: postclose verifier는 `status=pass`, predecessor integrity `pass`, missing/stale downstream links `0`, producer-gap handoff `pass`다. tuning control tower는 `primary_verdict=live_bucket_ready`, `source_artifact_warning_count=0`, EV warning `3`건만 남겼고 이는 `pending_future_quotes` 2건과 `pattern_lab_ai_review_warning` 1건으로 runtime/order/provider/bot 변경 권한이 없다.
  - 다음 액션: `runtime_apply_gap_audit`의 retry queue 2건(`entry_wait6579_score66_69_recovery_gate_v1:2026-05-27`, `greenfield_real_environment_authority:2026-05-27`)은 2026-05-28 PREOPEN apply 후보 소비 여부로 확인한다. 장후 bot은 resource isolation 정지 상태로 두며 다음 PREOPEN 전까지 재기동하지 않는다.

<!-- AUTO_NEXT_STAGE2_CHECKLIST_START -->
## 자동 생성 체크리스트 (`2026-05-26` postclose -> `2026-05-27`)

- 이 블록은 postclose 자동화 산출물에서 생성된다.
- `codex_daily_workorder_*.md`는 downstream 전달물이라 입력 source로 사용하지 않는다.
- RunbookOps 반복 확인은 `build_codex_daily_workorder`와 Project/Calendar 동기화 경로가 별도로 소유한다.

## 장전 체크리스트 (08:45~09:00)

- [x] `[ThresholdEnvAutoApplyPreopen0527] threshold env 자동 apply 산출물 및 사용자 개입 여부 확인` (`Due: 2026-05-27`, `Slot: PREOPEN`, `TimeWindow: 08:50~08:55`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-05-26.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-26.json), [threshold_cycle_preopen_apply.py](/home/ubuntu/KORStockScan/src/engine/threshold_cycle_preopen_apply.py), [run_bot.sh](/home/ubuntu/KORStockScan/src/run_bot.sh)
  - 판정 기준: 전일 postclose EV와 당일 apply plan/runtime env를 확인하고 `auto_bounded_live` guard 통과분만 runtime env로 인정한다.
  - 금지: blocked family, approval artifact missing, same-stage owner conflict를 수동 env override로 우회하지 않는다.
  - 다음 액션: `applied_guard_passed_env`, `blocked_no_env`, `partial_apply_with_blocked_families`, `failed_preopen_wrapper`, `not_yet_due` 중 하나로 닫는다.
  - 처리 결과 (`2026-05-27 07:49 KST`): 판정=`applied_guard_passed_env`. 근거: [threshold_cycle_preopen_2026-05-27.status.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_preopen_status/threshold_cycle_preopen_2026-05-27.status.json)은 `status=succeeded`, `apply_mode=auto_bounded_live`, `exit_code=0`, `runtime_effect=preopen_runtime_env_apply_only`다. [threshold_apply_2026-05-27.json](/home/ubuntu/KORStockScan/data/threshold_cycle/apply_plans/threshold_apply_2026-05-27.json)은 `status=auto_bounded_live_ready`, `runtime_change=true`, `warnings=[]`이고, [threshold_runtime_env_2026-05-27.env](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_2026-05-27.env)은 `KORSTOCKSCAN_THRESHOLD_RUNTIME_AUTO_APPLY_ENABLED=true`, `KORSTOCKSCAN_SCORE65_74_RECOVERY_PROBE_THRESHOLD_VERSION=entry_wait6579_score66_69_recovery_gate_v1:2026-05-26`를 포함한다. 다음 액션: runtime apply bridge의 `post_apply_verification_required=true` 후보는 장중 provenance 확인으로 넘기고, blocked/hold_sample family는 수동 env override로 열지 않는다.


- [x] `[SwingPreFinalAutoAndFinalApprovalPreopen0527] 스윙 pre-final auto state 및 final approval artifact 확인` (`Due: 2026-05-27`, `Slot: PREOPEN`, `TimeWindow: 08:45~08:50`, `Track: RuntimeStability`)
  - Source: [swing_runtime_approval_2026-05-26.json](/home/ubuntu/KORStockScan/data/report/swing_runtime_approval/swing_runtime_approval_2026-05-26.json), [threshold_cycle_ev_2026-05-26.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-26.json)
  - 판정 기준: pre-final은 parsed AI Tier2 auto state가 있어야 하고, final-stage는 사용자 승인 artifact가 있어야 한다.
  - 금지: 스윙 full-live 전환, cap release, provider/bot 변경, hard-safety 완화를 pre-final auto state로 처리하지 않는다.
  - 다음 액션: `pre_final_auto_selected`, `final_approval_artifact_present`, `blocked_by_policy` 중 하나로 닫는다.
  - 처리 결과 (`2026-05-27 07:49 KST`): 판정=`blocked_by_policy`. 근거: [swing_runtime_approval_2026-05-26.json](/home/ubuntu/KORStockScan/data/report/swing_runtime_approval/swing_runtime_approval_2026-05-26.json)은 `runtime_change=false`, requested/approved `0/0`, blocked `14`, `broker_order_submission=false`, `swing_live_order_dry_run_required=true`다. apply plan의 `swing_runtime_approval`도 `requested=0`, `approved=0`, `selected=[]`, `dry_run_forced=false`로 final approval artifact 소비가 없다. 다음 액션: 스윙 full-live 전환, cap release, provider/bot 변경, hard-safety 완화는 열지 않고 장후 rolling/source-quality 개선으로 재평가한다.

## 장중 체크리스트 (09:05~15:20)

- [x] `[GatekeeperTelegramApprovalSpam0527] Gatekeeper 승인 Telegram 반복 발송 억제 보완` (`Due: 2026-05-27`, `Slot: INTRADAY`, `TimeWindow: 09:05~15:20`, `Track: RuntimeStability`)
  - Source: [kiwoom_sniper_v2.py](/home/ubuntu/KORStockScan/src/engine/kiwoom_sniper_v2.py), [gatekeeper_snapshots_2026-05-27.jsonl](/home/ubuntu/KORStockScan/data/gatekeeper/gatekeeper_snapshots_2026-05-27.jsonl), [time-based-operations-runbook.md](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md)
  - 판정 기준: sim/probe/dry-run Gatekeeper 승인 메시지는 Telegram으로 보내지 않고 source-only 로그/산출물로만 남기며, 실제 live 승인 메시지도 동일 종목/전략/액션/report preview 기준 중복 발송을 제한한다.
  - 금지: 승인 알림 억제를 Gatekeeper 판단, 후보 제외, threshold/runtime env 변경, provider route, bot restart, broker 주문 제출 변경으로 확장하지 않는다.
  - 처리 결과 (`2026-05-27 intraday`): 판정=`notification_throttle_patch_ready`. 근거: 09:05 이후 [gatekeeper_snapshots_2026-05-27.jsonl](/home/ubuntu/KORStockScan/data/gatekeeper/gatekeeper_snapshots_2026-05-27.jsonl)에 같은 스윙 후보군 평가가 반복 기록됐고, Gatekeeper 승인 리포트 발송부가 sim/dry-run도 ADMIN_ONLY Telegram으로 내보낼 수 있었다. [kiwoom_sniper_v2.py](/home/ubuntu/KORStockScan/src/engine/kiwoom_sniper_v2.py)는 sim/dry-run 승인 리포트를 `source_only` 로그로만 남기고, live 승인 리포트는 10분 TTL dedupe를 적용하도록 보완했다. 다음 액션: 운영 프로세스 반영 시 알림량만 재확인하고, bot restart/provider/threshold/order guard는 변경하지 않는다.
  - 재기동 확인 (`2026-05-27 09:14 KST`): 판정=`restart_pass_runtime_env_loaded`. 근거: `restart.flag` 기반 우아한 재기동으로 bot PID가 `12097 -> 29538`로 교체됐고 `restart.flag`는 소비됐다. 새 프로세스 환경에서 [threshold_runtime_env_2026-05-27.env](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_2026-05-27.env)의 `KORSTOCKSCAN_THRESHOLD_RUNTIME_AUTO_APPLY_ENABLED=true`, `KORSTOCKSCAN_SCORE65_74_RECOVERY_PROBE_THRESHOLD_VERSION=entry_wait6579_score66_69_recovery_gate_v1:2026-05-26`, lifecycle decision matrix/scalp sim/Bedrock provider env가 로드됐다. `error_detector --mode health_only`는 `summary_severity=pass`, `main_loop_pid=29538`, thread status `ok`다. 다음 액션: 알림량과 runtime provenance만 관찰하고 threshold/provider/order guard는 추가 변경하지 않는다.
  - 코드리뷰 보완 (`2026-05-27 intraday`): 판정=`supplemented_notification_surface_only`. 근거: `NOT_EVALUATED_SCORE_VPW_PRIOR`/`cache_mode=not_evaluated`는 Telegram과 반복 생략 로그 모두 만들지 않도록 조용히 반환하고, `actual_order_submitted=false` probe성 payload도 source-only로 유지한다. live 승인 dedupe 상태는 lock으로 보호했고 `stock.name`/`report` 누락 payload도 함수 실패 없이 처리한다. 다음 액션: 반영 전까지 현재 PID에는 보완 코드가 로드되지 않으므로, 운영 반영은 별도 우아한 재기동으로만 진행한다.
  - AI 호출 구조 보완 (`2026-05-27 intraday`): 판정=`reject_cache_throttle_patch_ready`. 근거: fast signature가 조금만 바뀌거나 score boundary 근처에 있으면 같은 최근 reject도 모델 재호출로 이어질 수 있었다. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 최근 reject만 짧은 TTL(`AI_GATEKEEPER_REJECT_CACHE_SEC`, 기본 90초, 최소 30초)로 재사용하고, allow 결과는 이 경로로 재사용하지 않는다. score/가격/gap이 의미 있게 개선되면 cache를 쓰지 않고 새 평가로 넘어간다. 다음 액션: bot 재기동 전까지 현재 PID에는 로드되지 않으며, 운영 반영 후 `gatekeeper_reject_cache_reuse`와 model call 감소를 관찰한다.
  - 운영 반영 재기동 확인 (`2026-05-27 09:26 KST`): 판정=`restart_pass_runtime_env_loaded`. 근거: `restart.flag` 생성 후 bot PID가 `35888 -> 36481`로 교체됐고 flag는 소비됐다. 새 PID의 `/proc/36481/environ`에서 `KORSTOCKSCAN_THRESHOLD_RUNTIME_AUTO_APPLY_ENABLED=true`, `KORSTOCKSCAN_SCORE65_74_RECOVERY_PROBE_THRESHOLD_VERSION=entry_wait6579_score66_69_recovery_gate_v1:2026-05-26`, lifecycle decision matrix, scalp sim, Bedrock Nova Lite v2 primary/OpenAI failback env가 로드됐다. `KORSTOCKSCAN_AI_GATEKEEPER_REJECT_CACHE_SEC`는 env override 없이 코드 기본값 90초를 사용한다. `error_detector --mode full --dry-run`은 `summary_severity=pass`, process/artifact/resource/stale_lock/log scanner 모두 pass다. 다음 액션: threshold/provider/order guard는 변경하지 않고 `gatekeeper_reject_cache_reuse` 발생과 Telegram 알림량만 관찰한다.
  - 오전 로직 로그 점검 및 스윙 주문경로 코드리뷰 (`2026-05-27 intraday`): 판정=`pass_cache_functioning_provenance_supplemented`. 근거: 09:26 재기동 이후 [pipeline_events_2026-05-27.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-27.jsonl)에 `gatekeeper_reject_cache_reuse=64`, `blocked_gatekeeper_reject`의 `gatekeeper_cache=reject_cache` 68건/`miss` 20건이 기록되어 reject cache가 의도대로 AI 재호출을 줄였다. 같은 구간 스윙 source-only/sim/probe 이벤트는 `source_only_missing_broker_forbidden_count=0`이었다. 코드리뷰에서 스윙 dry-run entry leg/bundle provenance가 실제 브로커 주문 금지 상태를 더 명확히 남기도록 [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)를 보완하고, [test_swing_model_selection_funnel_repair.py](/home/ubuntu/KORStockScan/src/tests/test_swing_model_selection_funnel_repair.py)에 dry-run `broker_order_forbidden=true` 회귀 테스트를 추가했다. 다음 액션: runtime/order/provider/threshold는 변경하지 않고, 다음 bot 재기동 이후 신규 dry-run 로그의 provenance만 관찰한다.
  - 봇 에러 긴급 처리 (`2026-05-27 09:34~09:40 KST`): 판정=`runtime_error_recovered`. 근거: [sniper_state_handlers_error.log](/home/ubuntu/KORStockScan/logs/sniper_state_handlers_error.log)에 스윙 보유기간 timeout 계산이 `datetime.date`를 문자열로만 파싱해 `KOSPI_TIMEOUT` 에러를 반복 기록했다. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 `str`/`datetime`/`date`를 모두 허용하는 보유일 parser로 보완했다. 이어서 8005 인증 실패가 stale token cache 재사용과 결합해 재기동 반복 위험을 만들 수 있어 [kiwoom_utils.py](/home/ubuntu/KORStockScan/src/utils/kiwoom_utils.py), [sniper_sync.py](/home/ubuntu/KORStockScan/src/engine/sniper_sync.py), [kiwoom_auth_8005_restart.py](/home/ubuntu/KORStockScan/src/engine/error_detectors/kiwoom_auth_8005_restart.py)를 보완해 auth restart 전에 token cache를 무효화하도록 했다. `restart.flag` 기반 우아한 재기동 후 bot PID는 `43072`, 새 token cache는 09:39에 발급/갱신됐고, `error_detector --mode full --dry-run`은 `summary_severity=pass`, log scanner/auth/process 모두 pass다. 09:39 이후 `8005`, `KOSPI_TIMEOUT` 신규 로그는 0건이다. 다음 액션: threshold/provider/order guard는 변경하지 않고, auth 8005 재발 여부만 다음 health cycle에서 관찰한다.
  - 스윙 AI 호출/blocker 재점검 및 코드리뷰 보완 (`2026-05-27 intraday`): 판정=`pass_no_swing_ai_overcall_quota_log_supplemented`. 근거: 09:39 이후 Bedrock Nova Lite primary/shadow recent 24건은 모두 scalping sim `entry_price` provenance였고, 스윙 Gatekeeper는 `miss=9`, `reject_cache=6`으로 reject cache가 재호출을 줄이고 있었다. 스윙 쪽 반복 상위 이벤트는 `swing_probe_discarded`였으나 원인은 `max_open_reached`, `max_per_symbol_reached`, `origin_quota_reached` 등 source-only probe quota 관찰값이었다. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 global quota reason을 종목별로 반복 발행하지 않도록 dedupe하고, `blocker_authority=probe_capacity_only`, `quota_observation_scope`, `hard_gate=false`, `allowed_runtime_apply=false`를 남기도록 보완했다. [sniper_sync.py](/home/ubuntu/KORStockScan/src/engine/sniper_sync.py)와 [kiwoom_auth_8005_restart.py](/home/ubuntu/KORStockScan/src/engine/error_detectors/kiwoom_auth_8005_restart.py)는 auth restart cooldown 중에도 token cache invalidation을 수행하도록 보완했다. 다음 액션: 스윙 후보/주문/threshold/provider/dry-run guard는 변경하지 않고, 다음 graceful restart 이후 `swing_probe_discarded` raw count와 `gatekeeper_reject_cache_reuse`만 관찰한다.
  - 보완 후 health 재확인 (`2026-05-27 09:46 KST`): 판정=`pass_after_auth_cache_invalidation`. 근거: 09:45 fresh 8005가 한 차례 더 감지됐으나 09:46:20 `TOKEN CACHE invalidated stale Kiwoom token cache. reason=error_detector_auth_8005`가 기록됐고, 이후 `error_detector --mode full --dry-run`은 `summary_severity=pass`, log scanner/auth/process/artifact/resource 모두 pass다. 현재 bot PID는 `44999`, `restart.flag`는 없다. 다음 액션: 추가 재기동 없이 8005 재발과 스윙 quota observation raw count만 관찰한다.
  - 8005 Telegram 알림 후 재확인 (`2026-05-27 09:50~09:52 KST`): 판정=`pass_restarted_message_wording_supplemented`. 근거: 사용자가 받은 `report-only, no runtime mutation performed` 문구는 [telegram_manager.py](/home/ubuntu/KORStockScan/src/notify/telegram_manager.py)의 `SYSTEM_HEALTH_ALERT` footer가 모든 health alert에 고정으로 붙인 문구였고, 8005 detector의 token-cache/restart.flag 복구 가능성과 맞지 않아 오해 소지가 있었다. 우아한 재기동으로 bot PID가 `44999 -> 46789`로 교체됐고, 알림 문구 보완 반영을 위해 다시 `46789 -> 47642`로 교체됐다. 새 프로세스는 Bedrock Lite v2 primary/OpenAI WS/threshold runtime env를 정상 로드했다. 09:52 `error_detector --mode full --dry-run`은 `summary_severity=pass`, auth/log/process 모두 pass이며 09:45:36 이후 신규 8005 error line은 없다. 알림 footer는 `Telegram alert only; detector recovery actions are reported in the health artifact.`로 보완했다. 다음 액션: threshold/provider/order/swing guard는 변경하지 않고, 8005 재발 시 health artifact의 `token_cache_invalidated`, `restart_requested`, `restart_suppressed_by_cooldown` 필드로 판정한다.
  - 8005 restart loop cap 보완 (`2026-05-27 09:56~10:01 KST`): 판정=`fail_repeated_auth_8005_restart_loop_capped_runtime_loaded`. 근거: 09:56:08 fresh `kt00008` 8005 이후 detector가 count=`8`까지 `restart.flag`를 만들며 graceful restart는 동작했으나, 반복 auth 실패가 계속되어 restart loop로 전이될 위험이 확인됐다. [kiwoom_auth_8005_restart.py](/home/ubuntu/KORStockScan/src/engine/error_detectors/kiwoom_auth_8005_restart.py)는 하루 3회까지 restart 복구를 허용하고, 이후 fresh 8005는 token cache invalidation과 fail artifact/Telegram만 남기며 추가 `restart.flag`를 만들지 않도록 보완했다. [time-based-operations-runbook.md](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md)에도 동일 운영 계약을 반영했다. `restart.flag` 기반 우아한 재기동 후 bot PID는 `52283`, `restart.flag`는 없고, `2026-05-27 10:01 KST` `error_detector --mode full --dry-run`은 `summary_severity=pass`, `kiwoom_auth_8005_restart=pass`, `process_health=pass`, `main_loop_pid=52283`이다. 다음 액션: threshold/provider/order guard는 변경하지 않고, 추가 8005 발생 시 `restart_suppressed_by_daily_cap=true`, `restart_requested=false` 계약과 Kiwoom token 발급/REST auth 회복 여부를 확인한다.
  - 8005 조용한 fail 방지 코드리뷰 보완 (`2026-05-27 10:02~10:06 KST`): 판정=`notification_surface_supplemented_runtime_loaded`. 근거: [bot_main.py](/home/ubuntu/KORStockScan/src/bot_main.py)의 daemon alert loop와 [notify_error_detection_admin.py](/home/ubuntu/KORStockScan/src/engine/notify_error_detection_admin.py)의 standalone notifier가 `fail`만 Telegram 대상으로 삼아, daily cap 전 `kiwoom_auth_8005_restart` warning은 artifact에는 남지만 조용히 넘어갈 수 있었다. 8005 detector warning만 alert 대상으로 추가하고, detector state 손상 시 quiet pass 대신 warning으로 닫으며, token cache invalidation 예외가 restart.flag 생성 자체를 막지 않도록 [kiwoom_auth_8005_restart.py](/home/ubuntu/KORStockScan/src/engine/error_detectors/kiwoom_auth_8005_restart.py)를 보완했다. 우아한 재기동 후 bot PID는 `54149`, `restart.flag`는 없고, `10:04:33` 신규 8005는 daily cap으로 `restart_requested=false`/추가 flag 없음 상태에서 `ERROR_DETECTION` fail log로 표면화됐다. `10:06 KST` 재확인에서는 `summary_severity=pass`, `kiwoom_auth_8005_restart=pass`, `process_health=pass`이며 `10:05` 이후 신규 8005는 없다. 다음 액션: threshold/provider/order guard는 변경하지 않고, 이후 8005가 재발하면 warning/fail Telegram alert와 artifact 필드가 같이 남는지 관찰한다.
  - 8005 호출 지점별 refresh/retry 보완 (`2026-05-27 intraday`): 판정=`auth_retry_supplemented_guard_kept`. 근거: [kiwoom_utils.py](/home/ubuntu/KORStockScan/src/utils/kiwoom_utils.py)의 공통 Kiwoom REST wrapper는 `return_code/return_msg` 8005 응답을 받으면 token cache invalidation, `force_refresh=True`, same-request 1회 retry를 수행하도록 보완했다. [kiwoom_orders.py](/home/ubuntu/KORStockScan/src/engine/kiwoom_orders.py)의 예수금/잔고/매수/매도/취소 직접 POST 경로도 동일하게 8005 한정 1회 retry를 수행한다. 반복 8005는 retry loop로 확장하지 않고 auth incident/source-quality로 남긴다. 다음 액션: threshold/provider/order guard 자체는 변경하지 않고, 운영 반영 후 `api_8005_retry:*` 또는 `order_api_8005_retry:*` token cache invalidation 로그와 실제 REST 회복 여부를 확인한다.
  - 8005 refresh storm 보완 (`2026-05-27 14:18~14:22 KST`): 판정=`auth_refresh_singleflight_patch_ready`. 근거: daily restart cap 도달 후 health artifact는 `fresh_auth_8005_count=23`, `restart_suppressed_by_daily_cap=true`, `token_cache_invalidated=true`, `restart_requested=false`로 닫혔고 bot process health는 pass였다. 로그상 `kt00001`/`ka10003`/`ka10080` 호출 지점이 각자 8005를 맞고 새 token cache를 반복 invalidation하는 refresh storm이 확인됐다. [kiwoom_utils.py](/home/ubuntu/KORStockScan/src/utils/kiwoom_utils.py)는 실패한 token과 현재 cache token을 비교해, 이미 다른 thread가 새 token을 발급한 경우 cache를 재사용하고 추가 invalidation/force refresh를 생략하도록 보완했다. [kiwoom_orders.py](/home/ubuntu/KORStockScan/src/engine/kiwoom_orders.py)는 주문/계좌 API retry 경로에 실패 token provenance를 전달한다. 검증: `test_kiwoom_token_cache.py`, `test_kiwoom_orders.py` 16 passed, py_compile pass, diff check pass. 다음 액션: threshold/provider/order guard는 변경하지 않고, 운영 반영은 별도 우아한 재기동으로만 수행하며 반영 후 `이미 갱신된 Kiwoom token 캐시 재사용` 로그와 신규 8005 count 감소를 관찰한다.
  - 8005 refresh storm 운영 반영 (`2026-05-27 14:23~14:25 KST`): 판정=`runtime_loaded_auth_health_pass`. 근거: 코드리뷰 보강 후 관련 테스트 54 passed, py_compile pass, diff check pass다. `restart.flag` 기반 우아한 재기동으로 bot PID가 `111558 -> 149845`로 교체됐고 flag는 소비됐다. 새 PID는 threshold runtime env, Bedrock Lite v2 primary, OpenAI WS 설정을 로드했다. 재검증에서 `error_detector --mode full --dry-run`은 `summary_severity=pass`, `kiwoom_auth_8005_restart=pass`, `process_health=pass`이며 14:25 KST 구간 신규 8005/token refresh storm 로그는 0건이다. 다음 액션: threshold/provider/order guard는 변경하지 않고, `이미 갱신된 Kiwoom token 캐시 재사용` 또는 신규 8005 count만 관찰한다.
  - BUY time-block Telegram noise 보완 (`2026-05-27 intraday`): 판정=`notification_suppressed_guard_kept`. 근거: `BUY_TIME_BLOCK buy order blocked ... KST 10:00 전`은 1주 cap처럼 주문/수량 안전 guard provenance에 가깝고, 반복 Telegram이 필요하지 않다. [kiwoom_orders.py](/home/ubuntu/KORStockScan/src/engine/kiwoom_orders.py)는 `BUY_TIME_BLOCKED` broker submit 차단과 log 기록은 유지하되 `TELEGRAM_ADMIN_NOTIFY` 발송만 제거했다. 수동 `TRADING_PAUSED_BLOCK` 운영 알림은 유지한다. 운영 반영 후 bot PID는 `49117`, `restart.flag`는 없고 09:55 `error_detector --mode full --dry-run`은 `summary_severity=pass`, auth/log/process 모두 pass다. 다음 액션: threshold/provider/order guard 자체는 변경하지 않고, time-block은 pipeline/log source로만 확인한다.
  - pytest 로그 오염 보완 (`2026-05-27 intraday`): 판정=`source_quality_false_positive_fixed`. 근거: 10:46~10:47 `log_scanner UNKNOWN(8)` burst는 [kiwoom_orders_error.log](/home/ubuntu/KORStockScan/logs/kiwoom_orders_error.log)의 `temporary deposit failure`/`network down` fixture성 예수금 테스트 로그와 8005 retry 테스트 로그가 runtime `logs/`에 섞인 source-quality 오염이었다. [conftest.py](/home/ubuntu/KORStockScan/src/tests/conftest.py)는 pytest 실행 중 module logger를 per-test temp logs로 격리하고, [log_scanner.py](/home/ubuntu/KORStockScan/src/engine/error_detectors/log_scanner.py)는 `/tmp/pytest-of-*` fixture 경로가 runtime log에 누출된 경우만 무시한다. 실제 8005 인증 실패는 계속 `kiwoom_auth_8005_restart`가 판정한다. 검증: targeted pytest 28 passed, py_compile, `git diff --check`, `error_detector --mode full --dry-run`에서 `log_scanner=pass`, `kiwoom_auth_8005_restart=pass`. 다음 액션: threshold/provider/order guard와 bot restart 정책은 변경하지 않고, test fixture 로그가 runtime detector에 재유입되는지만 관찰한다.
  - 오전 변경 코드리뷰 보완 (`2026-05-27 intraday`): 판정=`supplemented_no_runtime_policy_change`. 근거: 8005 호출지점 retry는 token cache invalidation/force-refresh 예외가 나면 원래 8005 응답을 안전하게 반환하지 못할 수 있었고, 공통 Kiwoom API wrapper는 `rt_cd=8005`만 온 응답을 놓칠 수 있었다. [kiwoom_orders.py](/home/ubuntu/KORStockScan/src/engine/kiwoom_orders.py)와 [kiwoom_utils.py](/home/ubuntu/KORStockScan/src/utils/kiwoom_utils.py)는 refresh 예외를 fail-closed auth incident로 반환하고 `return_code`/`rt_cd` 모두 8005 retry 대상으로 본다. [kiwoom_sniper_v2.py](/home/ubuntu/KORStockScan/src/engine/kiwoom_sniper_v2.py)는 live Gatekeeper reject report가 future caller를 통해 VIP Telegram으로 나가지 않도록 approval-only 경계를 함수 내부에서도 보강했다. 검증: targeted pytest 44 passed + 252 passed, py_compile, `git diff --check`, checklist parser, `error_detector --mode full --dry-run`에서 `log_scanner=pass`, `kiwoom_auth_8005_restart=pass`. 다음 액션: threshold/provider/order guard, bot restart 정책, swing dry-run guard는 변경하지 않는다.
  - 스윙 RISK_OFF prior 관측화 (`2026-05-27 intraday`): 판정=`risk_off_prior_observation_patch_ready`. 근거: 단독 `RISK_OFF`/`allow_swing_entry=false`/`single_market_risk_off_advisory`는 더 이상 `market_regime_block` hard/blocker stage로 보지 않고 `market_regime_prior_observed`로 기록한다. `panic_state != NORMAL`, `confirmed_risk_off_advisory=true`, broad `risk_off_advisory=true`만 confirmed risk block으로 `market_regime_block`에 남긴다. 스윙 sim/probe는 `actual_order_submitted=false`, `broker_order_forbidden=true`로 계속 돌려 RISK_OFF prior의 진입/미진입 EV를 비교한다. 다음 액션: threshold/provider/order/cap/bot 정책은 변경하지 않고 postclose `swing_market_regime_sensitivity`에서 `market_regime_prior_observed`와 confirmed block을 분리 확인한다.
  - market regime recovery prior 계약 (`2026-05-27 intraday`): oil-only `35/70` 또는 recovery score 부족은 명확한 하락장 hard block 근거가 아니라 `market_regime_prior_observed`의 `oil_only_recovery_signal_insufficient`/`recovery_gate_signal_insufficient` prior로 기록한다. continuous regime label은 기본 시장 상태 해석값으로 보고, confirmed panic/breadth risk만 `market_regime_block` 권한을 가진다.
  - 8005 및 스윙 market regime 재점검 (`2026-05-27 12:39 KST`): 판정=`pass_auth_no_fresh_8005_market_regime_prior_guard_supplemented`. 근거: `error_detector --mode full --dry-run`은 `summary_severity=pass`, `kiwoom_auth_8005_restart=pass`, `log_scanner=pass`, bot PID `87049` alive로 신규 8005를 감지하지 않았다. 당일 pipeline 누적에서 스윙 sim/probe 체결성 이벤트는 `swing_sim_buy_order_assumed_filled=70`, `swing_sim_order_bundle_assumed_filled=70`, `swing_probe_entry_candidate=127`이고 `actual_order_submitted=false`, `broker_order_forbidden=true`를 유지한다. 코드리뷰 보완으로 [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 `confirmed_risk_block` 없는 `blocked=True` 호환 신호를 `market_regime_prior_observed`로 downgrade하고 `market_regime_block_downgraded_to_prior=true`를 남긴다. 검증: swing market-regime 관련 pytest `64 passed`, py_compile pass, `git diff --check` pass. 다음 액션: 운영 반영 전까지 현재 PID에는 새 downgrade guard가 로드되지 않으므로, 필요 시 별도 우아한 재기동 후 신규 `market_regime_block_downgraded_to_prior`와 confirmed block 분리를 관찰한다. threshold/provider/order/cap 정책은 변경하지 않는다.
  - 우아한 재기동 반영 (`2026-05-27 12:40 KST`): 판정=`restart_pass_runtime_env_loaded`. 근거: `restart.flag` 기반 재기동으로 bot main PID가 `87049 -> 111558`로 교체됐고 `restart.flag`는 소비됐다. `error_detector --mode full --dry-run`은 `summary_severity=pass`, `kiwoom_auth_8005_restart=pass`, `log_scanner=pass`, `process_health=pass`이며 새 프로세스 env는 `KORSTOCKSCAN_THRESHOLD_RUNTIME_AUTO_APPLY_ENABLED=true`, `KORSTOCKSCAN_SCORE65_74_RECOVERY_PROBE_THRESHOLD_VERSION=entry_wait6579_score66_69_recovery_gate_v1:2026-05-26`, `KORSTOCKSCAN_OPENAI_TRANSPORT_MODE=responses_ws`, `KORSTOCKSCAN_OPENAI_RESPONSES_WS_ENABLED=true`, `KORSTOCKSCAN_BEDROCK_NOVA_LITE_PRIMARY_ENDPOINTS=entry_price,holding_flow`를 로드했다. 다음 액션: 신규 8005/log burst와 새 `market_regime_block_downgraded_to_prior` 관측만 확인하고 threshold/provider/order/cap 정책은 변경하지 않는다.

- [x] `[RuntimeEnvIntradayObserve0527] 전일 selected runtime family 장중 provenance 및 rollback guard 확인` (`Due: 2026-05-27`, `Slot: INTRADAY`, `TimeWindow: 09:05~09:20`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-05-26.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-26.json)
  - 판정 기준: selected_families=soft_stop_whipsaw_confirmation, score65_74_recovery_probe, scalp_sim_candidate_window_expansion, scalp_sim_ai_budget_manager, lifecycle_decision_matrix_runtime, swing_one_share_real_canary_phase0, swing_gatekeeper_reject_cooldown, entry_wait6579_score66_69_recovery_gate_v1가 runtime event provenance에 찍히는지 확인한다.
  - 금지: 장중 관찰 결과로 runtime threshold mutation을 수행하지 않는다.
  - 다음 액션: provenance present/missing, rollback guard breach 여부를 분리 기록한다.
  - 처리 결과 (`2026-05-27 11:40 KST`): 판정=`pass_with_resource_watch`. 근거: 현재 bot PID `87049` 환경에 [threshold_runtime_env_2026-05-27.env](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_2026-05-27.env)의 `KORSTOCKSCAN_THRESHOLD_RUNTIME_AUTO_APPLY_ENABLED=true`, `KORSTOCKSCAN_SCORE65_74_RECOVERY_PROBE_THRESHOLD_VERSION=entry_wait6579_score66_69_recovery_gate_v1:2026-05-26`, `KORSTOCKSCAN_SCORE65_74_RECOVERY_PROBE_ENABLED=true`, `KORSTOCKSCAN_SCALP_SIM_CANDIDATE_WINDOW_EXPANSION_ENABLED=true`, `KORSTOCKSCAN_SCALP_SIM_AI_BUDGET_ENABLED=true`, `KORSTOCKSCAN_LIFECYCLE_DECISION_MATRIX_ENABLED=true`, `KORSTOCKSCAN_LIFECYCLE_DECISION_MATRIX_RUNTIME_EFFECT_ENABLED=false`, Bedrock Nova Lite v2 primary/OpenAI WS failback env가 로드되어 있다. [threshold_apply_2026-05-27.json](/home/ubuntu/KORStockScan/data/threshold_cycle/apply_plans/threshold_apply_2026-05-27.json)은 `auto_bounded_live_ready`, warnings `[]`, selected family와 runtime apply bridge selected `entry_wait6579_score66_69_recovery_gate_v1:2026-05-26`를 남겼고, 장중 [pipeline_events_2026-05-27.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-27.jsonl)에는 `lifecycle_decision_matrix_runtime_policy`와 `score65_74`/sim candidate provenance가 기록됐다. `error_detector --mode full --dry-run`은 auth/log/process/artifact/stale lock은 pass이고 disk free `3731MB` resource warning만 있다. 다음 액션: 장중 threshold/provider/order guard는 변경하지 않고, 디스크 여유와 post-apply verification은 장후 EV/verification에서 재확인한다.

- [x] `[SimProbeIntradayCoverage0527] sim/probe 관찰축 actual_order_submitted=false 및 source-quality 확인` (`Due: 2026-05-27`, `Slot: INTRADAY`, `TimeWindow: 09:35~09:50`, `Track: ScalpingLogic`)
  - Source: [threshold_cycle_ev_2026-05-26.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-26.json)
  - 판정 기준: sim/probe 표본이 real execution과 분리되고 `actual_order_submitted=false` provenance가 유지되는지 확인한다.
  - 금지: sim/probe EV를 broker execution 품질이나 실주문 전환 근거로 단독 사용하지 않는다.
  - 다음 액션: source-quality split, active state 복원, open/closed count를 같이 기록한다.
  - 처리 결과 (`2026-05-27 11:42 KST`): 판정=`pass_source_split_kept`. 근거: [pipeline_events_2026-05-27.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-27.jsonl) sim/probe 관련 stage `25,464`건 중 fields 기준 `actual_order_submitted=False` `25,037`건, `actual_order_submitted=True` `0`건, `broker_order_forbidden=True` `24,940`건, `broker_order_forbidden=False` `0`건이다. 주요 coverage는 `scalp_sim_buy_order_assumed_filled=251`, `scalp_sim_buy_order_virtual_pending=251`, `swing_probe_entry_candidate=120`, `swing_probe_holding_started=120`, `swing_sim_buy_order_assumed_filled=69`, `swing_sim_holding_started=69`, `swing_probe_state_restored=88`, `swing_probe_state_persisted=244`다. Bedrock primary provider artifact는 `605` rows 모두 `global.amazon.nova-2-lite-v1:0`, failback `0`이고, Lite v1 shadow artifact는 `602` rows 모두 `actual_order_submitted=false`, `runtime_effect=false`다. 다음 액션: missing provenance가 있는 lower-level observation rows는 source-quality 관찰로 남기고, sim/probe EV는 broker execution 품질이나 실주문 전환 근거로 단독 사용하지 않는다.

- [x] `[GreenfieldRealEnvironmentAuthority0527] promoted lifecycle bucket real authority 구현` (`Due: 2026-05-27`, `Slot: INTRADAY`, `TimeWindow: 14:30~15:20`, `Track: ScalpingLogic`)
  - Source: [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md), [runtime_apply_bridge.py](/home/ubuntu/KORStockScan/src/engine/runtime_apply_bridge.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)
  - 판정 기준: `greenfield_real_environment_authority`가 다음 PREOPEN env 후보로만 생성되고, 활성화 시 real `entry/submit/holding/scale_in/exit`는 promoted bucket allowlist만 authority로 삼는다.
  - 금지: 장중 env mutation, hard/protect/emergency/broker/order/qty/stale/price freshness guard 우회, provider route/bot restart/cap release 변경.
  - 처리 결과 (`2026-05-27 intraday`): 판정=`implementation_ready_preopen_only`. 근거: `runtime_apply_bridge`는 parsed Tier2 + source-quality pass `live_auto_apply_ready` lifecycle rows에서 `greenfield_real_env_policy_YYYY-MM-DD.json`을 만들고, `threshold_cycle_preopen_apply`는 `KORSTOCKSCAN_GREENFIELD_REAL_ENV_AUTHORITY_*` env만 다음 PREOPEN으로 쓴다. Runtime은 활성화 시 unpromoted entry/submit/scale-in/exit real action과 policy 누락 상태를 fail-closed로 차단하고 hard safety exit는 passthrough한다. Liquidity/overbought submit block은 greenfield active 상태에서 `greenfield_tunable_prior`로 관측되며 submit allowlist가 최종 권한을 가진다. Stage Telegram은 sim/probe/source-only를 제외하고 `entry`, `submit`, `holding`, `scale_in`, `exit` 발생 시 `VIP_ALL`로 1회 발송하도록 연결했다. 다음 액션: postclose discovery/bridge 산출물이 실제 promoted stage row를 생성하는지 확인하고, 다음 PREOPEN apply manifest에서 env 선택 여부를 검토한다.

## 장후 체크리스트 (16:30~18:55)

- [x] `[ThresholdDailyEVReport0527] daily EV real/sim/combined split 및 자동 반영 결과 확인` (`Due: 2026-05-27`, `Slot: POSTCLOSE`, `TimeWindow: 16:30~16:45`, `Track: RuntimeStability`)
  - Source: [tuning_performance_control_tower_2026-05-26.json](/home/ubuntu/KORStockScan/data/report/tuning_performance_control_tower/tuning_performance_control_tower_2026-05-26.json), [threshold_cycle_ev_2026-05-26.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-26.json)
  - 판정 기준: tuning performance control tower를 먼저 보고 `live_auto_apply_ready`, `sim_auto_approved`, post-apply attribution, EV authority를 분리해 확인한다.
  - 금지: sim/combined EV만으로 broker execution 품질이나 live 전환을 확정하지 않는다.
  - 다음 액션: 다음 장전 apply 입력으로 쓸 수 있는 항목과 hold_sample/freeze 항목을 분리한다.
  - 처리 결과 (`2026-05-27 POSTCLOSE`): 판정=`pass_live_bucket_ready_with_source_warnings`. 근거: [threshold_cycle_ev_2026-05-27.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-27.json)은 `status=auto_bounded_live_ready`, `runtime_change=true`, selected families `6`개와 lifecycle discovery `live_auto_apply_ready=1`, `sim_auto_approved=185`를 남겼다. [tuning_performance_control_tower_2026-05-27.json](/home/ubuntu/KORStockScan/data/report/tuning_performance_control_tower/tuning_performance_control_tower_2026-05-27.json)은 `primary_verdict=live_bucket_ready`, `real_pnl_is_tuning_performance=false`, `source_artifact_warning_count=0`이다. EV warning 3건은 `pending_future_quotes`/pattern lab AI review source warning이며 broker execution 품질이나 live 전환 단독 근거가 아니다. 다음 액션: 2026-05-28 PREOPEN에서 `entry_wait6579_score66_69_recovery_gate_v1:2026-05-27`와 greenfield authority의 apply 소비 여부만 확인한다.

- [x] `[CodeImprovementWorkorderReview0527] code improvement workorder 구현 필요 여부 및 Codex 지시 대상 확인` (`Due: 2026-05-27`, `Slot: POSTCLOSE`, `TimeWindow: 16:45~17:00`, `Track: ScalpingLogic`)
  - Source: [code_improvement_workorder_2026-05-26.md](/home/ubuntu/KORStockScan/docs/code-improvement-workorders/code_improvement_workorder_2026-05-26.md), [code_improvement_workorder_2026-05-26.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-05-26.json)
  - 판정 기준: selected_order_count=64와 `implement_now`, `attach_existing_family`, `design_family_candidate`, `reject` 분류를 확인한다.
  - 금지: code-improvement workorder를 자동 repo 수정으로 취급하지 않는다. 사용자가 Codex 구현을 지시한 경우에만 실행한다.
  - 다음 액션: 구현 필요, 설계 보류, reject, already_implemented 중 하나로 닫는다.
  - 처리 결과 (`2026-05-27 POSTCLOSE`): 판정=`already_implemented_no_new_codex_implement_now`. 근거: [code_improvement_workorder_2026-05-27.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-05-27.json)은 selected order `57`, selected decision `attach_existing_family=57`, selected `implement_now=0`, selected unimplemented runtime_effect=false `0`이다. implementation status는 `implemented=31`, `implemented_but_waiting_sample=5`, `implemented_but_hold_sample=2`, `implemented_source_quality_contract_waiting_sample=18`로 거짓 완료 없이 sample/source-quality provenance로 닫혔다. 다음 액션: 오늘 추가 Codex 구현은 열지 않고, sample 대기 항목은 다음 postclose workorder에서 자동 재표면화 여부를 확인한다.

- [x] `[HumanInterventionSummary0527] 자동화체인 사용자 개입 요구사항 분류 및 누락 확인` (`Due: 2026-05-27`, `Slot: POSTCLOSE`, `TimeWindow: 17:00~17:15`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-05-26.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-05-26.json), [time-based-operations-runbook.md](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md)
  - 판정 기준: 개입사항을 `approval_artifact_required|created|missing|blocked_by_policy|observe_only`, `Codex 구현 필요`, `수동 동기화 필요`, `관찰만`으로 분류한다.
  - 금지: approval request만 보고 env 파일을 직접 수정하지 않고, 자동화 산출물에 있는 요청을 답변에만 남기고 checklist/Project 대상에서 누락하지 않는다.
  - 다음 액션: approval request가 있으면 `approval_id`, 후보/대상, artifact path, 승인 여부, 다음 PREOPEN 적용 확인 항목을 남긴다. 누락된 항목이 있으면 다음 영업일 checklist에 parser-friendly checkbox로 추가한다.
  - 처리 결과 (`2026-05-27 POSTCLOSE`): 판정=`pass_no_operator_approval_artifact_required_today`. 근거: [runtime_approval_summary_2026-05-27.json](/home/ubuntu/KORStockScan/data/report/runtime_approval_summary/runtime_approval_summary_2026-05-27.json)은 `runtime_mutation_allowed=false`, `swing_requested=0`, `swing_approved=0`, `panic_approval_requested=0`, `warnings=[]`다. [lifecycle_bucket_discovery_2026-05-27.json](/home/ubuntu/KORStockScan/data/report/lifecycle_bucket_discovery/lifecycle_bucket_discovery_2026-05-27.json)은 `human_intervention_required=false`다. 수동 대상은 Project/Calendar 동기화뿐이며, runtime env 직접 수정/approval artifact 생성은 하지 않는다. 다음 액션: 2026-05-28 PREOPEN retry queue 소비 확인은 자동 생성된 다음 checklist의 PREOPEN 항목으로 처리한다.

- [x] `[RuntimeApplyGapDirectiveReview0527] runtime apply gap Codex 작업지시 표면화 및 구현 여부 확인` (`Due: 2026-05-27`, `Slot: POSTCLOSE`, `TimeWindow: 17:15~17:30`, `Track: ScalpingLogic`)
  - Source: [runtime_apply_gap_audit_2026-05-26.json](/home/ubuntu/KORStockScan/data/report/runtime_apply_gap_audit/runtime_apply_gap_audit_2026-05-26.json), [runtime_apply_gap_audit_2026-05-26.md](/home/ubuntu/KORStockScan/data/report/runtime_apply_gap_audit/runtime_apply_gap_audit_2026-05-26.md), [runtime-apply-gap-audit-user-guide.md](/home/ubuntu/KORStockScan/docs/runtime-apply-gap-audit-user-guide.md)
  - 판정 기준: runtime apply gap audit의 Codex 작업지시 `IMPLEMENT_RUNTIME_BRIDGE_FOR_ENTRY_BUCKET`:entry_wait6579_score66_69_recovery_gate_v1:2026-05-26(block=env_mapping_contract), `IMPLEMENT_SCALE_IN_POLICY_CONTRACT`:scale_in_bucket_runtime_policy_v1:2026-05-26(block=runtime_bridge_contract)를 구현 필요, 이미 해결, 설계 보류, reject로 분류한다.
  - 금지: 작업지시를 approval artifact나 즉시 runtime env 수정으로 해석하지 않는다. broker/order/provider/cap guard 우회와 장중 threshold mutation은 금지한다.
  - 다음 액션: `implement_now`, `already_implemented`, `defer_design`, `reject`, `needs_new_workorder` 중 하나로 닫고, 구현 시 테스트와 postclose verifier handoff를 같이 확인한다.
  - 처리 결과 (`2026-05-27 POSTCLOSE`): 판정=`warning_retry_queue_no_codex_directive`. 근거: [runtime_apply_gap_audit_2026-05-27.json](/home/ubuntu/KORStockScan/data/report/runtime_apply_gap_audit/runtime_apply_gap_audit_2026-05-27.json)은 `status=warning`, AI review `parsed`, critical failure `0`, `codex_directive_count=0`, retry queue `2`다. retry 대상은 `entry_wait6579_score66_69_recovery_gate_v1:2026-05-27`와 `greenfield_real_environment_authority:2026-05-27`의 다음 PREOPEN 소비 확인이며 코드 작업지시가 아니다. 다음 액션: 2026-05-28 PREOPEN apply에서 `ready_but_not_applied`가 해소되는지 확인하고, 해소되지 않으면 새 runtime uptake workorder로 표면화한다.

- [x] `[ShadowCanaryCohortReview0527] shadow/canary/cohort 런타임 분류 및 정리 판정` (`Due: 2026-05-27`, `Slot: POSTCLOSE`, `TimeWindow: 18:40~18:55`, `Track: Plan`)
  - Source: [workorder-shadow-canary-runtime-classification.md](/home/ubuntu/KORStockScan/docs/workorder-shadow-canary-runtime-classification.md)
  - 판정 기준: 당일 변경/관찰 결과를 기준으로 `remove`, `observe-only`, `baseline-promote`, `active-canary` 상태 변동 여부를 닫는다.
  - 금지: shadow 금지, canary-only, baseline 승격 원칙을 코드/문서 상태와 분리하지 않는다.
  - 다음 액션: 변경이 있으면 기준문서와 checklist를 함께 갱신하고 cohort 잠금 필드를 남긴다.
  - 처리 결과 (`2026-05-27 POSTCLOSE`): 판정=`no_baseline_promote_with_greenfield_and_provider_shadow_locked`. 근거: [workorder-shadow-canary-runtime-classification.md](/home/ubuntu/KORStockScan/docs/workorder-shadow-canary-runtime-classification.md)에 2026-05-27 addendum을 추가했다. Greenfield real authority는 다음 PREOPEN promoted-bucket allowlist 후보이며 legacy live fallback이나 baseline-promote가 아니다. Bedrock Lite v1은 v2 primary 비교용 transport shadow로만 남고 `runtime_effect=false`, `actual_order_submitted=false`다. 다음 액션: 신규 shadow alpha는 열지 않고, provider shadow mismatch는 source-quality 관찰로만 누적한다.

- [x] `[LiteV2PrimaryVsLiteV1ShadowPostclose0527] Bedrock Lite v2 primary와 Lite v1 shadow 결과 비교검증` (`Due: 2026-05-27`, `Slot: POSTCLOSE`, `TimeWindow: 18:55~19:05`, `Track: RuntimeStability`)
  - 판정 기준: `entry_price`와 `holding_flow`를 분리해 v2 primary parse rate, failback/error, latency p50/p90, v1 shadow action match, top mismatch pair, source-only authority를 확인한다. 특히 `entry_price`는 `USE_REFERENCE`/`IMPROVE_LIMIT` vs v1 `USE_DEFENSIVE` 차이를 fill quality, missed upside, stale/price freshness guard와 연결해 다음 관찰 입력으로만 남긴다.
  - 금지: 비교검증 결과만으로 threshold/order price/quantity guard, provider route, bot restart, 스윙 dry-run guard, real canary approval을 변경하지 않는다.
  - 다음 액션: `provider_split_pass`, `entry_price_mismatch_observe`, `holding_flow_match_pass`, `source_quality_gap`, `failback_or_parse_issue` 중 하나 이상으로 닫고, 필요 시 다음 영업일 장중 provenance 재확인 항목으로 연결한다.
  - 처리 결과 (`2026-05-27 POSTCLOSE`): 판정=`provider_split_pass_entry_price_mismatch_observe_holding_flow_match_pass`. 근거: [bedrock_nova_primary_provider_2026-05-27.jsonl](/home/ubuntu/KORStockScan/data/report/bedrock_nova_primary_provider/bedrock_nova_primary_provider_2026-05-27.jsonl)은 v2 primary `1169`행, parse ok `1168`, error `1`, entry_price p50/p90 `1390/2209ms`, holding_flow p50/p90 `1167/1391ms`다. [bedrock_nova_lite_shadow_2026-05-27.jsonl](/home/ubuntu/KORStockScan/data/report/bedrock_nova_lite_shadow/bedrock_nova_lite_shadow_2026-05-27.jsonl)은 v1 shadow `1165`행, parse ok `1165`, `runtime_effect=false`, `actual_order_submitted=false`다. action match는 entry_price `374/521`, holding_flow `581/644`; top mismatch는 entry_price v2 `USE_REFERENCE`/`SKIP`/`IMPROVE_LIMIT` vs v1 `USE_DEFENSIVE`다. 다음 액션: provider route/order price/quantity guard는 변경하지 않고, entry_price mismatch를 fill quality/missed upside/stale guard source-quality로만 관찰한다.

- [x] `[EngineRefactorMonitoringSamplerPlan0527] Greenfield 이후 src.engine Phase 2 Slice 2 계획 보정` (`Due: 2026-05-27`, `Slot: POSTCLOSE`, `TimeWindow: 18:55~19:10`, `Track: Plan`)
  - Source: [src-engine-refactor-inventory.md](/home/ubuntu/KORStockScan/docs/proposals/src-engine-refactor-inventory.md), [GreenfieldRealEnvironmentAuthority0527](#장중-체크리스트-09051520)
  - 판정 기준: `greenfield_real_environment_authority` 구현으로 오늘은 `src.engine.system_metric_sampler -> src.engine.monitoring.system_metric_sampler` 실제 이동을 하지 않고, refactor inventory의 consumer/risk/재개 조건을 문서로 보정한다.
  - 금지: runtime/order/provider/threshold/bot restart 경로 이동, cron/job id 의미 변경, output path/JSON schema/metric semantics 변경, wrapper 제거, `src/trading` 또는 `src/utils` 전체 이동, direct import bulk rewrite, Greenfield authority/env/policy/Telegram 경로와 리팩터링 변경을 같은 구현 slice로 묶는 것을 금지한다.
  - 다음 액션: `document_plan_updated`, `defer_after_greenfield_verification`, `consumer_inventory_refresh_needed`, `blocked_by_runtime_path` 중 하나로 닫는다.
  - 처리 결과 (`2026-05-27 POSTCLOSE`): 판정=`document_plan_updated_defer_after_greenfield_verification`. 근거: [src-engine-refactor-inventory.md](/home/ubuntu/KORStockScan/docs/proposals/src-engine-refactor-inventory.md)는 Phase 2 Slice 2를 실제 이동이 아니라 Greenfield 이후 consumer/risk/재개 조건 보정으로 잠갔다. `system_metric_sampler` 이동은 다음 영업일 이후 Greenfield post-apply/preopen provenance가 안정적으로 확인된 뒤 `consumer_inventory_refreshed`부터 다시 연다. 다음 액션: 오늘은 파일 이동, wrapper 제거, deploy/cron 변경, runtime/order/provider/bot 경로 이동을 하지 않는다.

<!-- AUTO_NEXT_STAGE2_CHECKLIST_END -->





## Project/Calendar 동기화

문서/checklist를 수정했으면 parser 검증은 실행하고, Project/Calendar 동기화는 사용자가 아래 명령으로 수동 실행한다.

```bash
PYTHONPATH=. .venv/bin/python -m src.engine.sync_docs_backlog_to_project && PYTHONPATH=. .venv/bin/python -m src.engine.sync_github_project_calendar
```

<!-- AUTO_SERVER_COMPARISON_START -->
### 본서버 vs songstockscan 자동 비교 (`2026-05-27 15:48:10`)

- 기준: `profit-derived metrics are excluded by default because fallback-normalized values such as NULL -> 0 can distort comparison`
- 상세 리포트: `data/report/server_comparison/server_comparison_2026-05-27.md`
- `Trade Review`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Performance Tuning`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Post Sell Feedback`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Entry Pipeline Flow`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
<!-- AUTO_SERVER_COMPARISON_END -->
