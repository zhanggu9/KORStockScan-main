# 2026-08-11 Stage2 To-Do Checklist

## 오늘 목적

- 전일 postclose 자동화가 만든 장전 apply 후보와 사용자 개입 요구사항을 산출물 기준으로 확인한다.
- 실주문, threshold, provider, sim/probe 관련 변경은 approval artifact와 checklist 기준 없이 열지 않는다.
- code-improvement workorder는 자동 repo 수정이 아니라 사용자가 Codex에 구현을 지시한 경우에만 실행한다.

## 오늘 강제 규칙

- 장중 runtime 변경은 사용자 명시 지시가 있을 때만 기존 `bounded_tunable` 단일 축에 한해 허용한다. fresh/conflict-free source, 유효 effective price, 단일 blocker 인과, same-stage owner 비충돌, before/after·PID/env provenance·rollback·즉시 attribution을 모두 남긴다. hard safety, stale/conflict, price freshness, broker/account/order/quantity/cooldown, provider, bot, cap, 요청수량은 변경하거나 우회하지 않는다.
- 튜닝 데이터 기준은 `clean_tuning_baseline_date=2026-06-05`, `clean_tuning_baseline_ts_kst=2026-06-05T00:00:00+09:00`이다. 기준 이전 raw/report/analytics artifact는 archive/audit evidence로만 보고 EV/rolling/MTD/cumulative tuning, live-auto promotion, runtime approval, pattern lab promotion, real execution quality approval 입력으로 쓰지 않는다.
- Baseline 이후 raw source-quality contract 결손은 날짜 전체 차단이 아니라 결손 row/window를 `raw_row_exclusion`으로 제외하는 것이 기본이다. 전체 block은 preflight missing/invalid, row/window exclusion 실패, 또는 결손을 안정적으로 특정할 수 없는 high-volume no-contract 상황에만 사용한다.
- 장중과 장후에는 `observation_source_quality_audit --write` 또는 최신 artifact로 raw source-quality를 반복 확인한다. Hard contract gap은 결손 row/window 제외 또는 `source_quality_blocked` 없이는 튜닝 입력에 들어갈 수 없고, unknown-token warning은 hard block이 아니더라도 code-improvement workorder handoff 확인 대상이다.
- provider transport/provenance 확인은 threshold 값, 주문가/수량 guard, 스윙 dry-run guard 변경과 분리한다.
- `actual_order_submitted=false`인 sim/probe 표본은 EV/source-quality 입력이며 실주문 전환 근거가 아니다.
- Project/Calendar 동기화는 사용자가 표준 동기화 명령으로 수행한다.

<!-- AUTO_NEXT_STAGE2_CHECKLIST_START -->
## 자동 생성 체크리스트 (`2026-08-10` postclose -> `2026-08-11`)

- 이 블록은 postclose 자동화 산출물에서 생성된다.
- `codex_daily_workorder_*.md`는 downstream 전달물이라 입력 source로 사용하지 않는다.
- RunbookOps 반복 확인은 `build_codex_daily_workorder`와 Project/Calendar 동기화 경로가 별도로 소유한다.

## 장전 체크리스트 (08:45~09:00)

- [ ] `[ThresholdEnvAutoApplyPreopen0811] threshold env 자동 apply 산출물 및 사용자 개입 여부 확인` (`Due: 2026-08-11`, `Slot: PREOPEN`, `TimeWindow: 08:50~08:55`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-08-10.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-08-10.json), [threshold_cycle_preopen_apply.py](/home/ubuntu/KORStockScan/src/engine/threshold_cycle_preopen_apply.py), [run_bot.sh](/home/ubuntu/KORStockScan/src/run_bot.sh)
  - 판정 기준: 전일 postclose EV와 당일 apply plan/runtime env를 확인하고 `auto_bounded_live` guard 통과분만 runtime env로 인정한다.
  - 금지: blocked family, approval artifact missing, same-stage owner conflict를 수동 env override로 우회하지 않는다.
  - 다음 액션: `applied_guard_passed_env`, `blocked_no_env`, `partial_apply_with_blocked_families`, `failed_preopen_wrapper`, `not_yet_due` 중 하나로 닫는다.

- [ ] `[RisingMissedScoutRuntimePreopen0811] rising_missed_scout_workorder 후속 구현 및 귀속 확인` (`Due: 2026-08-11`, `Slot: PREOPEN`, `TimeWindow: 08:55~09:00`, `Track: ScalpingLogic`)
  - Source: [rising_missed_scout_workorder_2026-08-10.json](/home/ubuntu/KORStockScan/data/report/rising_missed_scout_workorder/rising_missed_scout_workorder_2026-08-10.json), [code_improvement_workorder_2026-08-10.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-08-10.json), [threshold_apply_2026-08-11.json](/home/ubuntu/KORStockScan/data/threshold_cycle/apply_plans/threshold_apply_2026-08-11.json), [threshold_runtime_env_2026-08-11.json](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_2026-08-11.json), [threshold_runtime_env_verify_2026-08-11.json](/home/ubuntu/KORStockScan/data/threshold_cycle/runtime_env/threshold_runtime_env_verify_2026-08-11.json)
  - 판정 기준: 전일 `rising_missed_scout_workorder` 요약(code_improvement_order_count=`1`, forced_scout_with_post_sell_count=`0`, post_sell_join_coverage_pct=`0`, outcome_coverage_state=`no_closed_outcome`, profitable_forced_scout_count=`0`, loss_or_flat_forced_scout_count=`0`, current_missed_count=`0`)의 outcome join coverage와 code-improvement order를 보고 구현 완료된 mapped family가 당일 PREOPEN apply plan/runtime env/verify에 반영됐는지 확인한다. source-only order는 별도 runtime family/env mapping과 guard 통과가 있을 때만 반영으로 인정한다.
  - 금지: `rising_missed_scout_workorder` 생성 또는 forced 1-share scout 손익만으로 runtime threshold mutation, stale submit bypass, broker/order guard 완화, provider/bot/cap 변경, real execution quality approval을 열지 않는다.
  - 다음 액션: `runtime_env_reflected_and_verified`, `implemented_but_runtime_not_selected`, `source_only_no_runtime_authority`, `blocked_by_apply_guard`, `report_missing_or_stale`, `verify_missing_or_failed` 중 하나로 닫는다.

## 장중 체크리스트 (09:05~15:20)

- [ ] `[RuntimeEnvIntradayObserve0811] 전일 selected runtime family 장중 provenance 및 rollback guard 확인` (`Due: 2026-08-11`, `Slot: INTRADAY`, `TimeWindow: 09:05~09:20`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-08-10.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-08-10.json)
  - 전일 postclose candidate_selected_families=entry_split_order_plan, score65_74_recovery_probe, scalping_scanner_real_source_guard_runtime, score65_74_recovery_probe_strong_micro_override_runtime, entry_price_gap_profile_runtime, profit_stagnation_exit_runtime, latency_spread_relief_real_operator_override, quote_consistency_normalization, scalp_sim_candidate_window_expansion, scalp_sim_ai_budget_manager, scalping_pyramid_quality_gate, holding_decision_context_v1, weak_pullback_entry_block_runtime, early_accel_recheck_runtime, real_pyramid_scale_in_quality_guard_runtime, sell_side_open_time_block_runtime, pre_submit_liquidity_relief_runtime, entry_opportunity_recheck_runtime, weak_context_late_entry_guard_runtime, rising_missed_normal_buy_bridge, persistent_operator_overrides_2026_06_26이며 실제 기동 기대 목록으로 직접 사용하지 않는다.
  - 판정 기준: 당일 PREOPEN verify가 통과한 threshold_runtime_env의 selected_families와 selection_change_summary(신규 ON/정책 갱신/carry-forward·operator lock 유지/OFF·제외)를 기준으로 runtime event provenance를 확인한다.
  - 금지: 관찰 결과만으로 장중 runtime을 변경하지 않는다. 사용자 명시 override는 fresh/conflict-free source, 단일 blocker 인과, 기존 bounded_tunable 단일 축, rollback과 즉시 attribution 계약을 모두 충족해야 한다.
  - 다음 액션: provenance present/missing, rollback guard breach 여부를 분리 기록한다.

- [ ] `[SimProbeIntradayCoverage0811] sim/probe 관찰축 actual_order_submitted=false 및 source-quality 확인` (`Due: 2026-08-11`, `Slot: INTRADAY`, `TimeWindow: 09:35~09:50`, `Track: ScalpingLogic`)
  - Source: [threshold_cycle_ev_2026-08-10.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-08-10.json)
  - 판정 기준: sim/probe 표본이 real execution과 분리되고 `actual_order_submitted=false` provenance가 유지되는지 확인한다.
  - 금지: sim/probe EV를 broker execution 품질이나 실주문 전환 근거로 단독 사용하지 않는다.
  - 다음 액션: source-quality split, active state 복원, open/closed count를 같이 기록한다.

- [ ] `[IntradaySourceQualityGateCheck0811] 장중 raw source-quality 결손/unknown 조기 경보 및 튜닝 입력 차단 준비 확인` (`Due: 2026-08-11`, `Slot: INTRADAY`, `TimeWindow: 14:20~14:35`, `Track: RuntimeStability`)
  - Source: [pipeline_events_2026-08-11.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-08-11.jsonl), [threshold_events_2026-08-11.jsonl](/home/ubuntu/KORStockScan/data/threshold_cycle/threshold_events_2026-08-11.jsonl), [observation_source_quality_audit_2026-08-11.json](/home/ubuntu/KORStockScan/data/report/observation_source_quality_audit/observation_source_quality_audit_2026-08-11.json), [observation_source_quality_audit.py](/home/ubuntu/KORStockScan/src/engine/observation_source_quality_audit.py)
  - 판정 기준: 장중 `PYTHONPATH=. .venv/bin/python -m src.engine.observation_source_quality_audit --target-date 2026-08-11 --write` 재감사를 실행하거나 최신 산출물을 확인해 `hard_blocking_contract_gap_count`, `hard_blocking_excluded_row_count`, `tuning_input_allowed`, `raw_row_exclusion_applied`, `unknown_token_stage_count`, `review_warning_count`를 기록한다.
  - 금지: hard contract gap 또는 unknown-token warning을 답변에만 남기지 않는다. 결손 row/window는 튜닝 입력 제외 또는 workorder handoff 대상으로 고정하고, broker/order/provider/cap/bot/threshold 변경 근거로 사용하지 않는다.
  - 다음 액션: `source_quality_clean_intraday`, `defective_rows_excluded`, `hard_block_requires_producer_fix`, `unknown_warning_workorder_required`, `audit_missing_or_stale` 중 하나로 닫는다. hard gap/unknown warning이 있으면 장후 `PostcloseSourceQualityGateReview`와 `CodeImprovementWorkorderReview`에서 누락 없이 재확인한다.

## 장후 체크리스트 (20:05~21:55)

- [ ] `[PostcloseSourceQualityGateReview0811] 장후 source-quality gate 결과 및 튜닝 입력 허용/제외 확인` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 16:25~16:35`, `Track: RuntimeStability`)
  - Source: [observation_source_quality_audit_2026-08-11.json](/home/ubuntu/KORStockScan/data/report/observation_source_quality_audit/observation_source_quality_audit_2026-08-11.json), [threshold_cycle_ev_2026-08-11.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-08-11.json), [code_improvement_workorder_2026-08-11.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-08-11.json), [threshold_cycle_postclose_verification_2026-08-11.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_postclose_verification/threshold_cycle_postclose_verification_2026-08-11.json)
  - 판정 기준: postclose EV/report 소비 전후 `observation_source_quality_audit`의 hard block, row exclusion, clean baseline, unknown-token review warning을 확인한다. `hard_blocking_contract_gap_count>0`이면 결손 row/window 제외 또는 `source_quality_blocked` 산출 여부를 확인하고, `unknown_token_stage_count>0`이면 source-quality producer-fix workorder가 생성됐는지 확인한다.
  - 금지: source-quality preflight missing/stale, row exclusion 실패, hard block candidate 생성, unknown-token workorder handoff 누락을 정상 postclose 완료로 처리하지 않는다. sim/combined EV, live-auto promotion, runtime approval, LDM, threshold apply candidate에 결손 row/window가 섞이면 fail로 닫는다.
  - 다음 액션: `source_quality_gate_pass`, `defective_rows_excluded_and_ev_allowed`, `source_quality_blocked`, `unknown_warning_workorder_created`, `handoff_missing_fix_automation_first` 중 하나로 닫는다.

- [ ] `[ThresholdDailyEVReport0811] daily EV real/sim/combined split 및 자동 반영 결과 확인` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 16:30~16:45`, `Track: RuntimeStability`)
  - Source: [tuning_performance_control_tower_2026-08-10.json](/home/ubuntu/KORStockScan/data/report/tuning_performance_control_tower/tuning_performance_control_tower_2026-08-10.json), [threshold_cycle_ev_2026-08-10.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-08-10.json)
  - 판정 기준: tuning performance control tower를 먼저 보고 `live_auto_apply_ready`, `sim_auto_approved`, post-apply attribution, EV authority를 분리해 확인한다.
  - 금지: sim/combined EV만으로 broker execution 품질이나 live 전환을 확정하지 않는다.
  - 다음 액션: 다음 장전 apply 입력으로 쓸 수 있는 항목과 hold_sample/freeze 항목을 분리한다.

- [ ] `[HumanInterventionSummary0811] 자동화체인 사용자 개입 요구사항 분류 및 누락 확인` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 17:00~17:15`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_2026-08-10.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_ev/threshold_cycle_ev_2026-08-10.json), [time-based-operations-runbook.md](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md)
  - 판정 기준: 개입사항을 `approval_artifact_required|created|missing|blocked_by_policy|observe_only`, `Codex 구현 필요`, `수동 동기화 필요`, `관찰만`으로 분류한다.
  - 금지: approval request만 보고 env 파일을 직접 수정하지 않고, 자동화 산출물에 있는 요청을 답변에만 남기고 checklist/Project 대상에서 누락하지 않는다.
  - 다음 액션: approval request가 있으면 `approval_id`, 후보/대상, artifact path, 승인 여부, 다음 PREOPEN 적용 확인 항목을 남긴다. 누락된 항목이 있으면 다음 영업일 checklist에 parser-friendly checkbox로 추가한다.

- [ ] `[CodeImprovementWorkorderReview0811] code improvement workorder 구현 필요 여부 및 Codex 지시 대상 확인` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 21:15~21:25`, `Track: ScalpingLogic`)
  - Source: [code_improvement_workorder_2026-08-10.md](/home/ubuntu/KORStockScan/docs/code-improvement-workorders/code_improvement_workorder_2026-08-10.md), [code_improvement_workorder_2026-08-10.json](/home/ubuntu/KORStockScan/data/report/code_improvement_workorder/code_improvement_workorder_2026-08-10.json)
  - 판정 기준: selected_order_count=66와 `implement_now`, `attach_existing_family`, `design_family_candidate`, `reject` 분류를 확인하고, 비-implement 반복 항목이 `terminal_non_implement_longstanding`, `repeat_unresolved_structural_blocker`, `keep_visible_by_design` 중 무엇으로 닫혀야 하는지 분리한다.
  - 금지: code-improvement workorder를 자동 repo 수정으로 취급하지 않는다. 사용자가 Codex 구현을 지시한 경우에만 실행한다.
  - 다음 액션: `implement_now`, `terminal_non_implement_longstanding`, `repeat_unresolved_structural_blocker`, `keep_visible_by_design`, `already_implemented`, `defer_design`, `reject` 중 하나로 닫는다.

- [ ] `[LifecycleQuietGapReview0811] lifecycle quiet gap rollup 자동 표면화 및 처리 확인` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 21:25~21:40`, `Track: ScalpingLogic`)
  - Source: [runtime_apply_gap_audit_2026-08-10.json](/home/ubuntu/KORStockScan/data/report/runtime_apply_gap_audit/runtime_apply_gap_audit_2026-08-10.json), [runtime_apply_gap_audit_2026-08-10.md](/home/ubuntu/KORStockScan/data/report/runtime_apply_gap_audit/runtime_apply_gap_audit_2026-08-10.md)
  - 판정 기준: quiet gap summary의 quiet_gap_count=`340`, rollup_required_count=`340`, sim_live_connected_quiet_gap_count=`0`, observation_source_quality_warning_count=`0`, quiet_gap_type_counts=`{'positive_source_only_keep_collecting': 340}`를 확인하고 parent conflict/exclusion, positive source-only, source-quality warning, AI coverage 누락을 닫는다.
  - 금지: quiet gap을 threshold/env/provider/order/bot 변경 근거로 사용하지 않는다.
  - 다음 액션: `rollup_only`, `implement_now`, `already_covered_by_parent_policy`, `defer_until_more_sample`, `reject_not_applicable` 중 하나로 닫는다.

- [ ] `[AutomationTriggerDecisionSummary0811] 자동화체인 trigger decision run/skip 요약 및 wrapper marker 대조 확인` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 21:40~21:55`, `Track: RuntimeStability`)
  - Source: [automation_chain_trigger_decision_2026-08-10.json](/home/ubuntu/KORStockScan/data/report/automation_chain_trigger_decision/automation_chain_trigger_decision_2026-08-10.json), [run_threshold_cycle_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh)
  - 판정 기준: trigger decision summary의 total_steps=`15`, run_count=`15`, skip_count=`0`, source_missing_count=`4`, force_override_count=`0`, run_steps_sample=`lifecycle_window_rolling5d, lifecycle_window_rolling10d, lifecycle_window_mtd, pattern_lab_currentness_audit, pattern_lab_ai_review`, skip_steps_sample=`-`, top_reasons=`upstream_drift_signal:10, output_missing_or_unreadable:6, upstream_artifact_newer:6, source_missing_or_unreadable:4`를 확인하고 wrapper 로그의 `[SKIP] threshold-cycle postclose ... trigger_decision=skip` marker와 대조한다.
  - 구현 보완 (`2026-08-11`): trigger decision이 wrapper enable flag를 받아 OFF step을 `disabled_success`로 분리하고 force가 OFF를 재활성화하지 않도록 했다. 동일 날짜 `failed` 복구 실행은 target/report/status 계약이 유효한 opening-rotation·avg-down recovery·one-share 완료 artifact만 재사용하며, verbosity는 완료된 common-minute watermark와 미완료 tail을 분리한다. 실제 당일 postclose 재생성 전이므로 이 항목은 새 summary/marker 대조가 끝날 때까지 OPEN을 유지한다.
  - 금지: trigger decision을 PREOPEN apply, final verifier, broker/order/provider/cap/bot/threshold, hard-safety/source-quality fail-closed 경계 변경 근거로 사용하지 않는다.
  - 다음 액션: `trigger_contract_pass`, `unexpected_all_run`, `skip_marker_missing`, `source_missing_run_required`, `force_override_detected`, `needs_followup_patch` 중 하나로 닫는다.

- [ ] `[EntrySetupV214PredecessorRecovery0811] Entry Setup V2.14 predecessor 복구 대기 결함 보완 및 재확인` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 21:05~22:05`, `Track: ScalpingLogic`)
  - Source: [entry_setup_paired_replay_batch.py](/home/ubuntu/KORStockScan/src/engine/scalping/entry_setup_paired_replay_batch.py), [run_ai_entry_setup_paired_replay_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_ai_entry_setup_paired_replay_postclose.sh), [threshold_cycle_postclose_2026-08-11.status.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_postclose_status/threshold_cycle_postclose_2026-08-11.status.json)
  - 판정 기준: 현재 구현은 predecessor의 일시적 `failed|error|blocked` 상태에서 최대 4시간 대기 계약과 달리 즉시 종료한다. 상태 전환을 복구 가능하게 처리하는 최소 보완과 회귀 테스트를 별도 승인 범위에서 닫은 뒤, 21:05 runner가 최대 4시간 안의 `succeeded` 전환을 기다리는지 확인한다. 이후 replay batch와 activation artifact를 분리해 `succeeded`, `blocked_predecessor_timeout`, `blocked_promotion_guard`, `inactive_fallback_v2_13` 중 실제 상태를 기록한다.
  - 금지: predecessor 복구 대기 통과만으로 V2.14를 live activation으로 간주하거나 promotion/source-quality/누적 EV/risk guard를 우회하지 않는다.
  - 다음 액션: `recovered_then_evaluated`, `predecessor_timeout`, `promotion_guard_blocked`, `activated_and_preopen_pending`, `runner_failed_needs_fix` 중 하나로 닫는다.

- [x] `[SmoothingRollingDecisionConsumerImplementation0811] exact-path rolling 의사결정 consumer 구현·리뷰` (`Due: 2026-08-11`, `Slot: PREOPEN`, `TimeWindow: 08:20~09:10`, `Track: TuningAutomation`)
  - Source: [daily/rolling report](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [postclose verifier](/home/ubuntu/KORStockScan/src/engine/verify_threshold_cycle_postclose_chain.py), [traceability](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md)
  - 완료 결과 (`2026-08-11`): `smoothing_source_only_rolling_decision_v1` consumer가 soft-stop 10건·OFI 20건 floor, rolling 5/10/20일 90초 source-quality-adjusted EV, downside p10, guarded-terminal 비율/EV, phase/exclusion을 함께 판정한다. 표본·EV·downside 근거가 모두 완성되고 세 window가 양수일 때만 `source_only_bounded_review_ready`를 내며, 이 상태도 단일 same-stage bounded-canary 설계 검토만 열고 runtime/PREOPEN 권한은 갖지 않는다. verifier는 ready/hold 상태 drift와 권한·metric contract 결손을 fail로 닫는다.
  - 리뷰/검증: 1차 리뷰에서 downside evidence 미완성 통과, 보수적 오분류 미검출, metric contract 누락, R1/R2 traceability 귀속 오류를 보완했다. 최종 리뷰에서 verifier의 metric contract 누락과 downside readiness 플래그 신뢰를 독립 재계산으로 보완했다. 재리뷰 미해결 finding=`0`; daily/verifier pytest=`294 passed`, smoothing runtime/source-quality integration pytest=`216 passed`, checklist parser pytest=`60 passed`, Ruff/Black/compileall/`git diff --check`=`pass`다. Bot/PID/runtime env와 실주문 상태는 변경하지 않았다.

- [x] `[SmoothingRollingDecisionConsumer0812] exact-path rolling 의사결정 consumer 최초 검증` (`Due: 2026-08-12`, `Slot: POSTCLOSE`, `TimeWindow: 21:00~21:20`, `Track: TuningAutomation`)
  - Source: [daily/rolling report](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [postclose verifier](/home/ubuntu/KORStockScan/src/engine/verify_threshold_cycle_postclose_chain.py), [threshold_cycle_cumulative_2026-08-11.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_cumulative/threshold_cycle_cumulative_2026-08-11.json)
  - 판정 기준: `smoothing_source_only_path_journal_v3`와 `smoothing_source_only_rolling_decision_v1`이 soft-stop 10건·OFI 20건 floor, rolling 5/10/20일 90초 source-quality-adjusted EV, downside p10, guarded-terminal 비율/EV, holding/revive/non-revive phase와 exclusion을 함께 보존하는지 확인한다. 세 rolling window가 모두 양수이면 `source_only_bounded_review_ready`, 일부만 양수이면 `hold_direction_conflict`, 전부 비양수이면 `hold_no_edge`, 표본 미달이면 `hold_sample`로 닫는다.
  - 금지: source-only decision을 standalone live promotion, PREOPEN env mutation, hard/protect/emergency 우회, provider/bot/cap/quantity 변경 근거로 사용하지 않는다. `source_only_bounded_review_ready`는 동일 holding/exit stage 단일 bounded-canary 설계 검토만 열 수 있다.
  - 완료 결과 (`2026-08-12`): 두 family 모두 rolling 5/10/20일 `hold_sample`이다. soft-stop은 grace 관측 47건이 있었지만 confirmation 시작·만료·soft-stop 완료가 모두 0건이라 source-only terminal opportunity가 없었고 journal arm도 0건이었다. OFI는 pre-smoothing hard guard 1건만 있었고 applied/debounce/confirm이 0건이라 smoothing 대안 대상이 없었으며 journal arm도 0건이었다. verifier의 journal/rolling contract는 `pass`, runtime effect와 apply 권한은 모두 false다.
  - 코드리뷰: report producer·rolling consumer·verifier·runtime hook을 대조했다. hard/emergency 및 pre-smoothing guard 우회, 0표본 승격, sim/source-only의 runtime 권한 누출은 없었고 미해결 finding=`0`이다. smoothing journal/daily/verifier pytest=`305 passed`, Ruff/Black/compile/checklist parser/`git diff --check`=`pass`다.

- [x] `[SamsungMorningOneShareMachineImplementation0811] 삼성전자 오전 1주 독립 상태기계와 추가 1개월 분석 구현·리뷰` (`Due: 2026-08-11`, `Slot: INTRADAY`, `TimeWindow: 09:30~11:30`, `Track: ScalpingLogic`)
  - Source: [samsung-morning-one-share-machine.md](/home/ubuntu/KORStockScan/docs/samsung-morning-one-share-machine.md), [machine.py](/home/ubuntu/KORStockScan/src/trading/samsung_morning_one_share/machine.py), [gateway.py](/home/ubuntu/KORStockScan/src/trading/samsung_morning_one_share/gateway.py), [test_samsung_morning_one_share.py](/home/ubuntu/KORStockScan/src/tests/test_samsung_morning_one_share.py)
  - 완료 결과 (`2026-08-11`, 후속 정정 반영): 기존 entry/holding/exit/ADM/LDM/AI/수량결정과 분리한 `005930` 1주 전용 NXT PREMARKET-first/SOR-regular-fallback 상태기계를 추가했다. 2026-05-06~08-10 공통 완전일 64일 중 고정 정책 진입 49일·+2호가 12분 내 관측 도달 49일이며, clean baseline 44일은 진입 35·도달 35, archive-only 추가월 20일은 진입 14·도달 14다. 12분은 과거 도달시간 진단값일 뿐 runtime 청산 제한이 아니며, pre-baseline은 archive/audit로만 유지한다.
  - 안전/권한: 기본 OFF, env+CLI 확인문구+production endpoint+명시적 manual-operator exclusion+당일 PREOPEN authority가 모두 있어야 broker write가 가능하다. 전일 전용기계 미해결 주문/보유, 수량·응답 계약 이상, write 중단·모호 응답은 fail-closed다. 모든 주문은 1주로 하드코딩했다. widget 자동매매와 전용기계는 서로의 주문을 차단하거나 청산하지 않고 각자 broker 주문번호·체결수량만 소유한다.
  - 리뷰/검증: write-ahead 중복주문 방지, 독립 전략 장부 경계, 라이브 `--once`/임의 state 경로 금지, 당일 authority gate를 보완했다. 전용 pytest=`23 passed`, 수동제외·기존 widget 회귀 포함 pytest=`79 passed`, checklist parser pytest=`58 passed`, Ruff/Black/compile/systemd/shell/`git diff --check`=`pass`다. 최종 재리뷰 미해결 finding=`0`이다.

- [x] `[SamsungMorningOneShareNoStopSorCorrection0811] 오전 1주 기계 무손절 보유·SOR 정규장 라우트 정정` (`Due: 2026-08-11`, `Slot: INTRADAY`, `TimeWindow: 11:40~12:20`, `Track: ScalpingLogic`)
  - Source: [samsung-morning-one-share-machine.md](/home/ubuntu/KORStockScan/docs/samsung-morning-one-share-machine.md), [policy.py](/home/ubuntu/KORStockScan/src/trading/samsung_morning_one_share/policy.py), [machine.py](/home/ubuntu/KORStockScan/src/trading/samsung_morning_one_share/machine.py), [gateway.py](/home/ubuntu/KORStockScan/src/trading/samsung_morning_one_share/gateway.py), [preflight.py](/home/ubuntu/KORStockScan/src/trading/samsung_morning_one_share/preflight.py)
  - 완료 결과 (`2026-08-11`): 사용자 정정에 따라 08:00 구간만 NXT PREMARKET으로 유지하고 09:00 정규장 fallback 주문은 `SOR`로 변경했다. 목표가 +2호가 주문은 시간 제한 없이 유지하며 브로커가 미체결 종료로 확정한 경우 `HELD`로 닫아 1주를 보유한다. 12분 목표 취소, 최우선 지정가 강제매도, 다음 날 자동 목표 재주문, 보유 중 신규매수 경로는 제거·금지했다.
  - 권한/참조: PREOPEN authority schema를 `v2`로 올리고 SOR 정규장·무시간청산 정책을 필수 검증한다. 키움 공식 `Kiwoom-Securities/Kiwoom-REST-API` commit `69642586f7d84ba9fd8a6faf1f1537c7fda6568b`의 `주문.md`, `계좌.md`, `차트.md`, API spec, Postman에서 `kt10000/kt10001/kt10003/kt00007`의 SOR 계약을 2026-08-11 11:43:54 KST에 재확인했다.
  - 리뷰/검증: 1차 리뷰에서 authority v2가 SOR·무시간청산 policy payload를 직접 검증하지 않던 누락을 보완했고, 2차 리뷰에서 전용 gateway의 KRX 직접 라우트 우회 가능성을 제거했다. 전용 pytest=`30 passed`, manual-exclusion/widget 독립성/live trade 회귀 포함 pytest=`122 passed`, checklist/Project·Calendar parser pytest=`84 passed`, checklist print-backlog parser, Ruff/Black/compileall/`git diff --check`=`pass`다. 최종 재리뷰 미해결 finding=`0`이며 Bot/PID/runtime env와 실주문 상태는 변경하지 않았다.

- [ ] `[SamsungMorningOneShareLiveStart0812] 삼성전자 오전 2-leg 독립 기계 최초 실운용·귀속 확인` (`Due: 2026-08-12`, `Slot: PREOPEN`, `TimeWindow: 07:55~09:35`, `Track: ScalpingLogic`)
  - Source: [samsung-morning-one-share-machine.md](/home/ubuntu/KORStockScan/docs/samsung-morning-one-share-machine.md), [preflight.py](/home/ubuntu/KORStockScan/src/trading/samsung_morning_one_share/preflight.py), [service.py](/home/ubuntu/KORStockScan/src/trading/samsung_morning_one_share/service.py)
  - 판정 기준: 새 unit 설치 뒤 07:57 PREOPEN authority v3가 당일 메인 봇·공유토큰·manual-operator exclusion, leg당 1주·총 2주 50:50, `SOR` 정규장 fallback, 무시간청산·미청산 보유 정책을 PASS하고 07:59 전용 서비스가 시작되는지 확인한다. 기존 widget과 전용기계가 모두 독립 활성 상태이며, NXT/SOR 각 leg가 자기 주문번호·체결수량만 취소·매도하는지 최초 episode attribution으로 확인한다. NXT는 base/base+1호가, SOR fallback도 미체결 leg별 base/base+1호가, 각 체결가 +2호가 목표 유지와 `HELD` 종결을 검증한다.
  - 금지: 코드 변경만으로 기존 unit을 재기동하거나 active legacy 1주 상태를 자동 변환하지 않는다. 목표가 미체결을 이유로 목표 주문을 시간 취소하거나 최우선 지정가로 강제매도하지 않는다. 한 전략의 삼성전자 총보유수량·주문을 다른 전략의 장부로 간주하거나 상대 주문을 취소·청산하지 않는다. widget 중지·삼성 제외·재기동, 총 2주·leg당 1주 상한 완화, threshold/provider/cap/hard-safety 변경을 열지 않는다.
  - 다음 액션: `parallel_independent_episode_pass`, `target_open_continues`, `target_closed_unfilled_position_held`, `no_entry_condition`, `opening_source_missing`, `preflight_authority_blocked`, `independent_ledger_breach_disable_one_share_only`, `state_or_lock_failure` 중 하나로 닫는다.

- [x] `[SamsungAfternoonOneShareMachineImplementation0811] 삼성전자 오후 SOR 1주 독립 기계 구현·리뷰` (`Due: 2026-08-11`, `Slot: INTRADAY`, `TimeWindow: 12:20~14:50`, `Track: ScalpingLogic`)
  - Source: [samsung-afternoon-one-share-machine.md](/home/ubuntu/KORStockScan/docs/samsung-afternoon-one-share-machine.md), [policy.py](/home/ubuntu/KORStockScan/src/trading/samsung_afternoon_one_share/policy.py), [machine.py](/home/ubuntu/KORStockScan/src/trading/samsung_afternoon_one_share/machine.py), [gateway.py](/home/ubuntu/KORStockScan/src/trading/samsung_afternoon_one_share/gateway.py), [preflight.py](/home/ubuntu/KORStockScan/src/trading/samsung_afternoon_one_share/preflight.py)
  - 완료 결과 (`2026-08-11`): 정규장 `005930_AL` SOR 완성 1분봉의 14:00~14:40 최신 봉만 평가하고, 최근 연속 30봉 고점 대비 -1.25% 이하·저점 대비 +0.20% 이내이면 신호 종가 -1호가에 1주를 하루 1회 주문한다. 신호 뒤 5개 완성봉 동안 미체결인 매수 주문만 자기 주문번호로 취소하며, 체결 뒤 실제 체결가 +2호가 목표 매도는 시간 취소·손절·강제청산 없이 유지하고 브로커 미체결 종료 시 `HELD`로 보유한다.
  - 독립성/권한: 오전·widget과 상태·lock·당일 authority·정확 주문번호 원장을 분리했고 계좌 총보유량을 매도 수량으로 사용하지 않는다. 기본 OFF이며 env+CLI 확인문구+production endpoint+manual-operator exclusion+당일 authority가 모두 있어야 broker write가 가능하다. 키움 공식 upstream commit `69642586f7d84ba9fd8a6faf1f1537c7fda6568b`의 차트·주문·계좌 계약을 12:04:45 KST에 확인했다.
  - 리뷰/검증: PREOPEN 메인 봇 실측 래퍼, 30봉 연속성, 상태 불변식, authority 전필드 검증, pagination fail-closed를 보완했다. 최초 배포 smoke에서 preflight `PrivateTmp`가 기존 tmux 소켓을 격리하는 결함을 확인해 preflight unit에서만 제거하고 live service 격리는 유지했다. 오후 전용 pytest=`33 passed`, 오전 회귀 포함 pytest=`63 passed`, manual-exclusion/widget 독립성 회귀 포함 pytest=`136 passed`, checklist parser pytest=`58 passed`, Ruff/Black/compile/systemd/shell/`git diff --check`=`pass`다. 최종 재리뷰 미해결 finding=`0`이며 결함 발견 시점에는 authority·live service·실주문이 열리지 않았다.

- [ ] `[SamsungAfternoonOneShareLiveStart0811] 삼성전자 오후 2-leg 독립 기계 재설치 후 최초 실운용·귀속 확인` (`Due: 2026-08-12`, `Slot: INTRADAY`, `TimeWindow: 13:50~15:20`, `Track: ScalpingLogic`)
  - Source: [samsung-afternoon-one-share-machine.md](/home/ubuntu/KORStockScan/docs/samsung-afternoon-one-share-machine.md), [preflight.py](/home/ubuntu/KORStockScan/src/trading/samsung_afternoon_one_share/preflight.py), [service.py](/home/ubuntu/KORStockScan/src/trading/samsung_afternoon_one_share/service.py)
  - 기동 상태 (`2026-08-11 12:29 KST`): PR `#33/#34` main 병합 후 오후 전용 systemd unit을 설치·enable했다. 당일 authority=`ready`, service PID=`223895`, state=`READY`, attempt_consumed=`false`, position_qty=`0`, buy/target order number=`empty`, last_action=`waiting_for_afternoon_scan_window`다. 14:00~14:40 최초 episode attribution은 계속 OPEN이다.
  - 판정 기준: 새 unit 설치 뒤 13:57 authority v2가 메인 봇·공유토큰·manual-operator exclusion, SOR 통합 정규장, 신호종가 1주+신호종가-1호가 1주, 5개 완성봉 유효기간, leg별 +2호가 무손절 보유 정책을 PASS하고 13:59 서비스가 시작되는지 확인한다. 오전·midday·widget·오후가 각자 주문번호와 체결수량만 소유하는지 최초 episode attribution으로 확인한다.
  - 금지: 코드 변경만으로 기존 unit을 재기동하거나 active legacy 1주 상태를 자동 변환하지 않는다. 오후 정규장을 KRX/NXT 별도 시장으로 분리하거나 목표 매도를 시간 취소·최우선 지정가 강제매도·손절하지 않는다. 다른 기계 주문 또는 계좌 총보유량을 오후 장부로 간주하지 않으며 총 2주·leg당 1주 상한·hard safety·provider·bot·cap을 변경하지 않는다.
  - 다음 액션: `parallel_independent_episode_pass`, `target_open_continues`, `target_closed_unfilled_position_held`, `no_signal`, `source_stale_or_gap`, `preflight_authority_blocked`, `independent_ledger_breach_disable_afternoon_only`, `state_or_lock_failure` 중 하나로 닫는다.

- [x] `[SamsungMiddayOneShareMachineImplementation0811] 삼성전자 13:15~13:55 SOR 1주 독립 기계 구현·리뷰` (`Due: 2026-08-11`, `Slot: INTRADAY`, `TimeWindow: 12:40~14:10`, `Track: ScalpingLogic`)
  - Source: [samsung-midday-one-share-machine.md](/home/ubuntu/KORStockScan/docs/samsung-midday-one-share-machine.md), [policy.py](/home/ubuntu/KORStockScan/src/trading/samsung_midday_one_share/policy.py), [machine.py](/home/ubuntu/KORStockScan/src/trading/samsung_midday_one_share/machine.py), [gateway.py](/home/ubuntu/KORStockScan/src/trading/samsung_midday_one_share/gateway.py), [preflight.py](/home/ubuntu/KORStockScan/src/trading/samsung_midday_one_share/preflight.py)
  - 완료 결과 (`2026-08-11`): clean baseline 46거래일 탐색에서 선택한 반개구간 `[13:15, 13:55)`를 runtime 13:15~13:54 완성봉으로 고정했다. 최근 연속 30봉 고점 대비 -1.25% 이하·저점 대비 +0.20% 이내이면 신호 종가 -1호가에 SOR 1주를 하루 1회 주문한다. 신호 뒤 5개 완성봉 동안 미체결인 자기 매수 주문만 취소하고, 체결 뒤 실제 체결가 +2호가 목표 매도는 손절·시간취소·강제청산 없이 유지하며 브로커 미체결 종료 시 `HELD`로 보유한다.
  - 독립성/권한: 오전·기존 오후·widget과 process/state/lock/당일 authority/정확 주문번호 원장을 모두 분리했고 계좌 총보유량을 청산 수량으로 사용하지 않는다. 기본 OFF이며 env+CLI 확인문구+production endpoint+manual-operator exclusion+당일 authority를 모두 통과해야 broker write가 가능하다. 키움 공식 upstream commit `69642586f7d84ba9fd8a6faf1f1537c7fda6568b`의 차트·주문·계좌·spec/core/Postman 계약을 12:44:57 KST에 재확인했다.
  - 리뷰/검증: 1차 리뷰에서 공식 참조 경로 provenance와 기존 오후 서비스 비변경 안내 누락을 보완하고 SOR 1주 매도·정확 매수취소·연속 체결조회·수량 위반 fail-closed 테스트를 추가했다. midday 전용 pytest=`39 passed`, 오전·오후·manual-exclusion·widget 독립성 회귀 포함 pytest=`158 passed`, checklist/Project·Calendar parser pytest=`84 passed`, checklist print-backlog parser, Ruff/Black/compile/systemd/shell/`git diff --check`=`pass`다. 최종 재리뷰 미해결 finding=`0`이며 기계는 설치·enable·start하지 않았고 runtime/order 상태를 변경하지 않았다.

- [ ] `[SamsungMiddayOneShareLiveStart0811] 삼성전자 midday 2-leg 독립 기계 재설치 후 최초 실운용·귀속 확인` (`Due: 2026-08-12`, `Slot: INTRADAY`, `TimeWindow: 13:05~14:05`, `Track: ScalpingLogic`)
  - Source: [samsung-midday-one-share-machine.md](/home/ubuntu/KORStockScan/docs/samsung-midday-one-share-machine.md), [preflight.py](/home/ubuntu/KORStockScan/src/trading/samsung_midday_one_share/preflight.py), [service.py](/home/ubuntu/KORStockScan/src/trading/samsung_midday_one_share/service.py)
  - 기동 상태 (`2026-08-11 12:58 KST`): PR `#37` main 병합 후 midday 전용 systemd unit을 설치·enable했다. 당일 authority=`ready`, service PID=`252969`, restart count=`0`, state=`READY`, attempt_consumed=`false`, position_qty=`0`, buy/target order number=`empty`, last_action=`waiting_for_midday_scan_window`다. 기존 오후 PID=`223895`는 재기동 없이 유지됐고, `[13:15,13:55)` 최초 episode attribution은 계속 OPEN이다.
  - 판정 기준: 새 unit 설치 뒤 13:12 authority v2가 메인 봇·공유토큰·manual-operator exclusion, SOR 통합 정규장, `[13:15,13:55)` 신호창, 신호종가 1주+신호종가-1호가 1주, 5개 완성봉 유효기간, leg별 +2호가 무손절 보유 정책을 PASS하고 13:14 서비스가 시작되는지 확인한다. 오전·오후·widget·midday가 각자 주문번호와 체결수량만 소유하는지 state와 최초 episode attribution으로 확인한다.
  - 금지: 코드 변경만으로 기존 unit을 재기동하거나 active legacy 1주 상태를 자동 변환하지 않는다. 설치·기동을 이유로 기존 오전·오후·widget·main bot을 중지·재기동하거나, 다른 기계 주문·계좌 총보유량을 midday 장부로 간주하지 않는다. midday 자체 재기동이 필요하면 미해결 write intent·owned order·position을 먼저 확인하고 상태 파일을 보존한 채 systemd graceful stop/start만 허용한다.
  - 다음 액션: `parallel_independent_episode_pass`, `target_open_continues`, `target_closed_unfilled_position_held`, `no_signal`, `source_stale_or_gap`, `preflight_authority_blocked`, `independent_ledger_breach_disable_midday_only`, `state_or_lock_failure` 중 하나로 닫는다.

- [x] `[SamsungThreeMachineTwoLegAllocation0811] 삼성전자 오전·midday·오후 2주 50:50 분할 진입 구현·리뷰` (`Due: 2026-08-11`, `Slot: INTRADAY`, `TimeWindow: 15:20~17:30`, `Track: ScalpingLogic`)
  - Source: [shared two-leg core](/home/ubuntu/KORStockScan/src/trading/order/regular_two_leg_machine.py), [morning policy](/home/ubuntu/KORStockScan/src/trading/samsung_morning_one_share/policy.py), [midday policy](/home/ubuntu/KORStockScan/src/trading/samsung_midday_one_share/policy.py), [afternoon policy](/home/ubuntu/KORStockScan/src/trading/samsung_afternoon_one_share/policy.py), [morning document](/home/ubuntu/KORStockScan/docs/samsung-morning-one-share-machine.md), [midday document](/home/ubuntu/KORStockScan/docs/samsung-midday-one-share-machine.md), [afternoon document](/home/ubuntu/KORStockScan/docs/samsung-afternoon-one-share-machine.md)
  - 완료 결과 (`2026-08-11`): 세 기계의 하루 1회 episode를 총 2주로 바꾸되 broker 주문은 leg당 1주로 분리했다. midday·오후는 신호종가 1주와 신호종가-1호가 1주, 오전은 기존 base 1주와 base+1호가 1주를 사용하며 NXT 미체결 leg만 동일 역할의 SOR fallback으로 넘긴다. 각 체결은 실제 체결가 +2호가 목표를 독립 소유하고 손절·시간청산·강제매도 없이 미청산 보유한다. package/unit 경로는 호환성을 유지하지만 state/authority/confirmation schema는 two-leg 버전으로 올렸다.
  - 안전/리뷰: active legacy state는 자동 변환하지 않고 manual reconciliation으로 차단하며, broker write intent는 leg와 집계 상태를 함께 원자 저장한다. leg별 정확 주문번호·1주 수량·중복 주문번호 금지·집계 수량/상태 정합성을 검증한다. 1차 재리뷰에서 오전 프로세스가 08:10 이후 최초 시작되면 SOR fallback 대신 NXT leg를 `NO_FILL`로 닫을 수 있는 결함을 찾아, 08:10~09:00에는 SOR leg를 대기시키고 09:00 이후에는 SOR 시가로 직접 제출하도록 보완했다. 기존 설치 unit은 자동 재기동하지 않았고 새 confirmation 계약으로 별도 재설치 전까지 fail-closed다. 공식 키움 upstream commit `69642586f7d84ba9fd8a6faf1f1537c7fda6568b`의 주문·계좌·spec/API/Postman 계약을 15:30:19 KST에 재확인했다.
  - 검증: 세 기계·preflight·gateway/service targeted pytest=`113 passed`, Samsung/widget/manual-exclusion 회귀 pytest=`352 passed`; Ruff/Black/compileall/systemd verify/shell syntax/checklist parser/`git diff --check`=`pass`, 최종 재리뷰 미해결 finding=`0`이다. runtime/order/provider/main bot/widget 상태는 변경하지 않았다.

- [x] `[SamsungMachineEntryPostcloseTuning0811] 삼성전자 세 독립 기계 실제 진입결과 장후 누적·리뷰` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 20:10~20:30`, `Track: ScalpingLogic`)
  - Source: [entry tuning producer](/home/ubuntu/KORStockScan/src/engine/monitoring/samsung_machine_entry_tuning.py), [shared two-leg core](/home/ubuntu/KORStockScan/src/trading/order/regular_two_leg_machine.py), [postclose wrapper](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh), [traceability](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md)
  - 완료 결과 (`2026-08-11`, 초기 구현 기록): 세 기계가 live episode를 arm할 때 현재 entry 판정 입력과 두 leg 가격을 `signal_features`로 고정하고, 20:10 장후 wrapper가 당일 durable state와 이 producer의 clean-baseline 이후 이전 일별 artifact만 읽어 당시 daily/rolling10/rolling20/cumulative report를 생성하도록 구현했다. 이후 현재 결정창 계약은 `[MachineActualCleanBaselineWindow0811]`에서 rolling gate를 제거한 단일 `clean_baseline_cumulative` actual-report 창으로 대체됐다. 과거 시세·API는 다시 조회하지 않으며 실제 broker fill/target 결과만 leg별로 인정하고 주문번호·audit는 report에서 제거한다. legacy 1주 state는 archive-only로 분리한다.
  - 판정/권한: 오전은 route별 현재 drawdown 정책 결과만, midday·오후는 현재 신호와 더 엄격한 drawdown/near-low subset만 관찰한다. 완화 threshold·대체 매수 유효시간·price-touch fill은 추정하지 않는다. v2 complete episode 20건, source-quality, rolling/cumulative 방향, 보유·미해결 상태를 operator review gate로 기록하되 `runtime_effect=false`, `allowed_runtime_apply=false`이며 threshold/order/provider/bot/cap 자동변경, 손절·강제청산, 기계 간 장부 통합 권한은 없다.
  - 검증: report producer·세 기계 targeted pytest=`80 passed`, postclose wrapper·detector·controller·checklist 회귀 pytest=`156 passed`; Ruff/Black/compileall/wrapper shell syntax/checklist parser/`git diff --check`=`pass`다. review-gate에서 오전 NXT/SOR 혼합 leg provenance, v2 source-gap 누락, legacy archive의 누적 차단 오염, prior report 계약/cost drift를 보완한 뒤 최종 재리뷰 미해결 finding=`0`이다. 과거 시세/API 조회, report 실생성, bot/service 재기동, runtime/order/provider/threshold 변경은 수행하지 않았다.

- [x] `[SamsungMachineEntryAutoApply0811] 삼성전자 장후 튜닝의 다음 PREOPEN 자동반영·유사종목 탐색` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 20:10~NEXT_PREOPEN`, `Track: ScalpingLogic`)
  - Source: [entry tuning producer](/home/ubuntu/KORStockScan/src/engine/monitoring/samsung_machine_entry_tuning.py), [PREOPEN apply](/home/ubuntu/KORStockScan/src/engine/automation/samsung_machine_entry_policy_apply.py), [policy contract](/home/ubuntu/KORStockScan/src/trading/order/samsung_entry_policy.py), [candidate scan](/home/ubuntu/KORStockScan/src/engine/monitoring/samsung_like_machine_candidate_scan.py), [candidate result](/home/ubuntu/KORStockScan/data/report/samsung_like_machine_candidate_scan/samsung_like_machine_candidate_scan_2026-08-10.json)
  - 자동반영 (초기 계약 기록, 현재 `[MachineActualCleanBaselineWindow0811]`로 대체): 장후 report의 target-date source-quality preflight, clean-baseline cumulative 20 complete episode·20 completed leg, 양수 notional EV, 보유·미해결 차단을 확인해 다음 거래일 candidate를 만든다. rolling10/20 각 3 completed leg gate는 현재 계약에서 제거됐다. 오전은 NXT 3.0%·SOR 0.75% baseline-only다. midday/오후는 drawdown `1.25→최대 1.50` 또는 near-low `0.20→최소 0.10` 중 전체 동일 entry stage에서 하루 정확히 한 기계·한 축만 tightening한다. 수량 2, 50:50 1주 leg, five-bar validity, +2틱, 무손절·미청산 보유는 immutable이다.
  - PREOPEN/rollback: 세 preflight wrapper는 최신 prior candidate를 정확일자 applied artifact로 최초 한 번만 고정하고 이후 같은 날에는 검증·재사용만 한다. 후보 없음/7일 초과는 baseline, 최신 후보 또는 당일 artifact schema/hash/lineage 손상은 이전 후보로 건너뛰지 않고 broker gateway 생성 전에 fail-closed한다. provider/bot/cap/account/order/broker guard 권한은 없다.
  - 종목 탐색 (`2026-06-05~2026-08-10`, 46거래일): 공식 Kiwoom `ka10080` SOR `_AL`과 NXT `_NX`를 각 후보별 continuation으로 완전 수집했고 여섯 종목·두 venue가 모두 source PASS다. 동일 2-leg/+2틱/비용 0.20%/same-bar target 제외/no-stop held 규칙에서 현대차 midday만 `15 episode, 30 attempted leg, 25 complete, 5 no-fill, 0 held, notional-weighted realized EV +0.052383%`로 source-only 구현 후보를 통과했다. NAVER morning/midday/afternoon, LG전자 morning/midday/afternoon, 현대차 morning/afternoon은 EV가 양수지만 held proxy가 `1~10`건 있어 연구 후보로만 남겼다. SK하이닉스·삼성전기·SK스퀘어의 주요 regular 조합은 비용차감 EV가 음수라 제외했다. 어떤 종목도 runtime/live symbol로 추가하지 않았다.
  - 공식 참조: `Kiwoom-Securities/Kiwoom-REST-API` commit `69642586f7d84ba9fd8a6faf1f1537c7fda6568b`, `kiwoom_docs/차트.md`, `kiwoom/_data/kiwoom_api_spec.json`, `kiwoom/specs.py`, `kiwoom/core/client.py`, Postman의 `POST /api/dostk/chart`, `api-id=ka10080`, continuation·종목 suffix 계약을 재확인했다. 기존 cache token을 읽기 전용으로 공유했고 발급·갱신·폐기·계좌·주문 API는 호출하지 않았다.
  - 리뷰/검증: 최종 review gate에서 동일-stage 복수 axis 가능성, source-quality preflight 누락, tightening 후 무신호 시 policy carry-forward, exact-date intraday overwrite 가능성, 오전 all-SOR provenance drift, 과거 1일 source gap의 영구 누적차단, same-bar fill/target 낙관 편향을 보완했다. Samsung/policy/scan/wrapper/detector/controller/checklist parser targeted pytest=`301 passed`; Black/Ruff/compileall/shell syntax/checklist print-backlog parser/`git diff --check`=`pass`, 최종 미해결 finding=`0`이다. runtime service 재기동과 실주문은 수행하지 않았다.

- [x] `[SamsungAdaptivePairabilityResearch0811] 삼성전자 적응형 매수 후보 pairability 판정기 구현·검증` (`Due: 2026-08-11`, `Slot: RESEARCH`, `TimeWindow: 12:00~15:00`, `Track: ScalpingLogic`)
  - Source: [adaptive opportunity replay](/home/ubuntu/KORStockScan/src/engine/monitoring/pure_market_adaptive_opportunity_replay.py), [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), [pure-market backtest contract](/home/ubuntu/KORStockScan/docs/pure-market-reversal-backtest.md)
  - 판정 기준: 46 coverage-qualified 거래일을 연구 표본 floor로 사용한다. KRX 매수 후보 시점의 prior-only 입력만으로 수익 가능한 매도 전환이 adverse transition보다 먼저 완성될 가능성을 평가하고, 현재 `prior_duration_cap_next_open` 손실 집단과 완성 pair 집단을 walk-forward로 분리한다. 약세 반전 lane과 강세 전환·추세 lane은 별도 attribution으로 유지하며 primary metric은 비용 차감 OOS EV다.
  - 금지: 평가일 미래가격, 같은 보고서 결과로 선택한 threshold, NXT partial context를 KRX 권한으로 사용하는 행위, 위젯·메인 봇·주문·계좌·provider·bot·runtime 변경을 금지한다. 표본 floor 통과만으로 live/runtime authority를 부여하지 않는다.
  - 다음 액션: `pairability_oos_positive`, `pairability_detected_execution_negative`, `insufficient_pairability_labels`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`pairability_detected_execution_negative`. 1차 buy/sell 모델이 이전 20일만 학습한 OOS 후보를 만들고, 2차 pairability 모델은 이전 base-OOS episode만 학습하도록 nested walk-forward를 구현했다. selection fraction은 이전 이력의 chronological validation suffix에서 EV 우선·누적 percentage sum 차선으로 선택하며, 거절된 후보가 포지션 슬롯을 점유하지 않고 다음 후보를 다시 탐색하는 causal 상태기계를 재생한다. KRX pairability OOS 18일 control=`276건, EV -0.183845%`, 선택=`151건, EV -0.165089%`, delta=`+0.018756%p`, 승률=`42.384%`로 개선됐으나 양수 전환에 실패했고 선택 건 중 duration expiry가 `91건` 남았다. weak-reversal EV=`-0.170575%`, bullish-transition EV=`-0.144688%`로 두 lane 모두 음수다. NXT는 `source_quality_blocked`다. runtime/order/provider/bot 변경은 없으며 `runtime_effect=false`다.
  - 리뷰 결과: 1차 리뷰에서 EV보다 누적합을 우선하던 selection 계약과 NXT partial context 오분류를 보완했다. 재리뷰에서 사후 subset 선택이 거절 후 후보 재탐색을 재현하지 못하는 결함과 학습/평가 특징 반올림 불일치를 찾아 causal gate 재실행과 동일 특징 정규화로 보완했다. 최종 미해결 finding=`0`이다.

- [x] `[SamsungAdaptiveCompetingRiskLaneReplay0811] 삼성전자 lane별 competing-risk·직접 EV replay 설계/구현` (`Due: 2026-08-11`, `Slot: RESEARCH`, `TimeWindow: 13:00~15:20`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), [adaptive replay](/home/ubuntu/KORStockScan/src/engine/monitoring/pure_market_adaptive_opportunity_replay.py)
  - 판정 기준: binary pair-completion classifier와 공통 5~6분 duration cap을 재조정하지 않는다. `weak_reversal`은 비용 회복 sell-transition과 새 저점/불리 전환의 first-event를, `bullish_transition`은 추세 지속과 bearish transition의 first-event를 별도 prior-only 모델로 평가한다. 분류 정확도보다 동일 OOS 날짜 비용 차감 EV와 누적 순성과를 우선하며 current-date outcome/threshold 누수를 금지한다.
  - 금지: NXT partial context의 KRX 전용, 고정 ENTRY/EXIT/widget label 복원, 같은 보고서 threshold 선택, runtime/widget/order/provider/bot 적용을 금지한다. `runtime_effect=false`를 유지한다.
  - 다음 액션: `lane_competing_risk_oos_positive`, `lane_ev_improved_but_negative`, `no_incremental_predictive_value`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`lane_ev_improved_but_negative`. base buy/sell 모델은 평가일 이전 20거래일만 사용하고, 확정 진입 후보 뒤 첫 causal `sell_transition`·`adverse_buy_transition`·session-end censor를 next-open으로 기록하는 무 duration-cap episode를 만들었다. `weak_reversal`과 `bullish_transition`은 서로 다른 prior-only event classifier와 직접 비용차감 EV regressor를 사용하며, 예측 EV가 0보다 큰 후보만 선택했다. KRX 동일 OOS 18일 control=`356건, EV -0.206315%`, 선택=`80건, EV -0.205076%`, delta=`+0.001239%p`, 승률=`43.750%`; weak-reversal EV=`-0.169575%`, bullish-transition EV=`-0.319053%`로 모두 음수다. NXT는 `source_quality_blocked`다. 이는 반전 기회 부재가 아니라 현재 base transition first-event가 비용을 회수할 만큼 지속되는 실행 구간을 정의하지 못한다는 결과이며 runtime/order/widget/provider/bot 변경 없이 `runtime_effect=false`로 종료했다.
  - 리뷰 결과: session-end censor가 마지막 의사결정 시각을 쓰던 1분 조기 종료 결함을 실제 마지막 execution 시각으로 보완했고, 최종 판정이 계약상 primary metric인 `source_quality_adjusted_ev_pct`만 사용하도록 fail-closed 처리했다. 현재 평가일 결과의 학습 유입, 두 lane 모델 공유, duration-cap 잔존, 선택 후보의 예측 EV 0 이하, NXT의 KRX 권한 전용 여부를 재검증했으며 최종 미해결 finding=`0`이다.

- [x] `[SamsungAdaptiveEconomicFirstPassageReplay0812] 삼성전자 lane별 경제적 first-passage utility replay` (`Due: 2026-08-12`, `Slot: RESEARCH`, `TimeWindow: 09:00~15:20`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungAdaptiveCompetingRiskLaneReplay0811]` 완료 결과
  - 판정 기준: 너무 잦은 base buy/sell transition을 경제적 청산 event로 재사용하지 않는다. 각 후보 뒤의 연속 비용차감 return path에서 비용 회수 가능한 유리 first-passage와 새 저점·추세 훼손의 불리 first-passage를 lane별 competing event로 구성하되, 공통 고정 ENTRY/EXIT 라벨을 복원하지 않고 후보 시점 이전 volatility·cost·prior distribution만으로 경계를 정한다. 동일 OOS 날짜 control 대비 `source_quality_adjusted_ev_pct`, 누적 순성과, event duration, MFE/MAE를 함께 판정한다.
  - 금지: 평가일 결과로 경계·holding window 선택, 미래가격의 진입 feature 사용, NXT partial context의 KRX 권한 전용, runtime/widget/order/provider/bot 적용을 금지하며 `runtime_effect=false`를 유지한다.
  - 다음 액션: `economic_first_passage_oos_positive`, `economic_first_passage_improved_but_negative`, `no_incremental_predictive_value`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`economic_first_passage_improved_but_negative`. 후보 시점의 trailing 20분 causal volatility와 비용으로 유리·불리 경계를 만들고, lane별 배수는 평가일보다 앞선 chronological validation suffix에서만 선택했다. KRX 동일 OOS 18일 control=`263건, EV -0.267165%`, 선택=`78건, EV -0.251125%`, delta=`+0.016040%p`, 승률=`43.590%`, 복리 순성과=`-18.012967%`다. 선택 78건 중 `60건/76.923%`는 이후 세션 MFE가 +0.5% 이상이고 평균 full-session MFE=`+1.916299%`였으나, adverse-first 43건 중 24건은 청산 뒤 자기 유리 경계까지 반등했다. weak-reversal EV=`-0.285194%`, bullish-transition EV=`-0.152328%`로 모두 음수이며 NXT는 `source_quality_blocked`다. 기회 부족은 기각됐고 사전 ranking의 작은 개선보다 recovery-aware exit 결손이 더 큰 병목으로 확인됐다. runtime/order/widget/provider/bot 변경 없이 `runtime_effect=false`로 종료했다.
  - 리뷰 결과: 청산 후 움직임이 episode MFE/MAE에 섞이던 결함을 pre-exit과 full-session counterfactual로 분리하고, 단순 수익률 합계를 복리 순성과로 교체했다. 한 번의 adverse touch가 조기 청산이 되던 결함은 weak-reversal 2개 확정봉 연속 이탈, bullish-transition 연속 이탈 또는 3·5분·가속도 동시 훼손으로 보완했다. 직접 EV 입력에 causal volatility 절대값을 추가하고 adverse confirmation reason을 기록했다. 최종 관련 pytest=`118 passed`, Ruff/Black/compile/checklist parser/`git diff --check`/artifact contract=`pass`, 미해결 finding=`0`이다.

- [x] `[SamsungAdaptiveRecoveryAwareExitReplay0812] 삼성전자 recovery-aware adverse exit·수익확장 replay` (`Due: 2026-08-12`, `Slot: RESEARCH`, `TimeWindow: 15:20~17:30`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungAdaptiveEconomicFirstPassageReplay0812]` 완료 결과
  - 판정 기준: selected KRX episode의 +0.5% full-session MFE 76.923%와 adverse-first 후 later-favorable 24건을 분리한다. 첫 adverse 구조 확인 시 즉시 종료와 prior-only recovery probability/time-to-recovery recheck를 lane별로 비교하고, favorable first-passage 뒤에는 prior-only peak/trailing utility로 수익확장 가능성을 평가한다. 동일 OOS 날짜 비용차감 EV·복리 순성과·MFE capture ratio·추가 MAE를 primary/diagnostic으로 분리한다.
  - 금지: adverse guard 전면 해제, 평가일 결과로 recovery/trailing 경계 선택, full-session MFE를 entry feature로 사용, NXT partial context의 KRX 권한 전용, runtime/widget/order/provider/bot 적용을 금지하며 `runtime_effect=false`를 유지한다.
  - 다음 액션: `recovery_aware_exit_oos_positive`, `recovery_aware_exit_improved_but_negative`, `no_incremental_predictive_value`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`no_incremental_predictive_value`. 기존 경제 first-passage가 선택한 entry timestamp를 먼저 고정하고 recovery-ready lane만 1:1 비교한 KRX 18 OOS일/76건에서 baseline EV=`-0.217520%`, recovery+trailing EV=`-0.221401%`, delta=`-0.003881%p`, 복리 순성과는 `-15.446255% -> -15.672494%`, pre-exit MAE는 `-0.423629% -> -0.470804%`로 악화했다. adverse checkpoint 41건 중 prior-only 예측 증분 EV가 양수인 18건만 bounded defer했고, favorable 회복 2건·deep-adverse 9건·timeout 6건이었다. 다만 defer 18건 자체의 entry-matched 평균 증분은 `+0.114623%p`였고, non-defer 집단의 손상은 favorable trailing 6건에서 발생했다. bullish-transition은 `+0.000539% -> +0.005077%`, weak-reversal은 `-0.285194% -> -0.291687%`다. 따라서 recovery 가능성은 기각하지 않고 profit-extension trailing과 축을 분리한다. NXT는 `source_quality_blocked`, runtime/order/widget/provider/bot 변경은 없고 `runtime_effect=false`다.
  - 리뷰 결과: 1차 리뷰에서 recovery-ready 후보를 다시 비중복 선택해 기존 경제모형 entry cohort가 달라질 수 있는 귀속 결함을 찾아, 경제모형이 선택한 entry 집합을 먼저 고정한 뒤 1:1 매핑하도록 보완했다. 전체 재생성 1차에서 recovery 행에 baseline first-passage provenance가 없어 최종 진단 조립이 실패한 결함을 발견했고 `baseline_exit_at/price/reason/event`를 명시적으로 보존해 보완했다. 재생성·artifact 검사에서 46일/schema v5, 모든 model/policy 날짜의 평가일 이전성, 동일 entry timestamp, defer의 예측 증분 EV `>0`, private path 비노출, KRX/NXT 분리와 권한 금지를 확인했다. 최종 관련 pytest=`131 passed`, Ruff/Black/compile/checklist parser/`git diff --check` 검증 대상이며 미해결 finding=`0`이다.

- [x] `[SamsungAdaptiveRecoveryTrailingAxisSeparation0812] 삼성전자 recovery와 favorable trailing 축 분리 OOS replay` (`Due: 2026-08-12`, `Slot: RESEARCH`, `TimeWindow: 09:00~12:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungAdaptiveRecoveryAwareExitReplay0812]` 완료 결과
  - 판정 기준: 같은 economic-selected entry cohort에서 `baseline`, `recovery-only`, `trailing-only`, `recovery+trailing`을 분리한다. recovery-only 모델·정책은 trailing outcome을 label에 섞지 않고, favorable trailing은 별도 prior-only incremental-EV 판정기가 양수인 경우만 적용한다. lane별 비용차감 EV, 복리 순성과, 추가 MAE, MFE capture와 정책 적용 건수를 비교하며 current-date outcome은 선택에 사용하지 않는다.
  - 금지: 이번 OOS 결과를 같은 보고서의 lane ON/OFF threshold로 사용, adverse guard 전면 해제, 공통 고정 holding/ENTRY/EXIT 라벨 복원, NXT partial context의 KRX 권한 전용, runtime/widget/order/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `recovery_only_oos_positive`, `trailing_incremental_ev_positive`, `axis_separation_improved_but_negative`, `no_incremental_predictive_value`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`axis_separation_improved_but_negative`. 경제모형이 선택한 동일 KRX 18 OOS일/76 entry에 네 arm을 1:1 적용했다. baseline EV=`-0.217520%`, recovery-only=`-0.193819%`/delta=`+0.023701%p`, trailing-only=`-0.236889%`/delta=`-0.019368%p`, recovery+trailing=`-0.221691%`/delta=`-0.004170%p`다. recovery-only 복리 순성과는 `-15.446255% -> -13.903154%`로 개선했지만 pre-exit MAE는 `-0.423629% -> -0.479910%`로 확대됐다. bounded defer 18건의 평균 증분은 `+0.100072%p`이고 8건 개선·9건 악화·1건 동일이다. trailing 적용 8건은 평균 `-0.183999%p`, 1건 개선·7건 악화로 별도 predictor도 OOS 일반화에 실패했다. recovery-only lane EV는 bullish-transition=`+0.050797%`, weak-reversal=`-0.269734%`이며 두 lane 모두 각 baseline보다는 개선됐다. NXT는 `source_quality_blocked`, runtime/order/widget/provider/bot 변경 없이 `runtime_effect=false`다.
  - 리뷰 결과: recovery-only 라벨과 정책에서 trailing outcome을 물리적으로 제외하고, trailing은 favorable checkpoint 전용 classifier·증분 EV regressor와 0배수 validation baseline으로 분리했다. 자체리뷰에서 direct-favorable 표본만 학습한 trailing 모델이 recovery 후 favorable 문맥에 적용되는 분포 불일치를 찾아 `favorable_after_adverse_checkpoint` causal feature와 회복 후 favorable prior 표본을 추가했다. 네 arm entry timestamp 일치, 모든 model/policy 날짜의 평가일 이전성, recovery/trailing 적용 시 각 예측 증분 EV `>0`, private path 비노출, KRX/NXT 분리, schema v6와 선행 economic 78건/EV `-0.251125%` 불변을 artifact로 검증했다. 리포트 중복 행을 제거해 JSON 증가는 v5 약 13MB에서 v6 약 15.1MB로 제한했다. 최종 관련 pytest=`141 passed`, Ruff/Black/compile/checklist parser/`git diff --check` 검증 대상이며 미해결 finding=`0`이다.

- [x] `[SamsungAdaptiveRecoveryAwareEntryUtility0812] 삼성전자 recovery-only outcome 직접 entry utility OOS replay` (`Due: 2026-08-12`, `Slot: RESEARCH`, `TimeWindow: 12:00~15:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungAdaptiveRecoveryTrailingAxisSeparation0812]` 완료 결과
  - 판정 기준: baseline first-passage outcome으로 학습한 기존 entry direct-EV selector를 그대로 재사용하지 않는다. 동일 causal entry feature와 lane 분리를 유지하되, 평가일보다 앞선 recovery-only bounded exit outcome의 비용차감 EV를 직접 학습해 untouched next-date 후보를 선택한다. 기존 economic-selected control과 recovery-aware-selected의 비용차감 EV·복리 순성과·추가 MAE·선택/기회 수를 비교하고, bullish/weak lane 결과를 사후 ON/OFF 기준으로 같은 보고서에 재사용하지 않는다.
  - 금지: current-date recovery outcome 또는 이번 axis 결과로 같은 보고서 threshold/lane switch 선택, trailing outcome의 recovery entry label 혼입, full-session MFE/MAE의 entry feature 사용, NXT partial context의 KRX 권한 전용, runtime/widget/order/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `recovery_entry_utility_oos_positive`, `recovery_entry_utility_improved_but_negative`, `no_incremental_predictive_value`, `insufficient_recovery_entry_labels`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`no_incremental_predictive_value`. 각 날짜의 prior-only recovery 모델·정책으로 먼저 OOS 생성된 recovery-only episode만 다음 날짜 이후 entry utility 학습에 넣었다. KRX model-ready 10일에서 prior OOS label=`350건`, eligible 후보=`184건`, 기존 economic selector raw/non-overlap=`39/33건`, recovery entry selector raw/non-overlap=`64/50건`이다. 동일 recovery-only exit 정책 아래 control EV=`-0.283379%`, 신규 EV=`-0.304573%`, delta=`-0.021194%p`, 복리 순성과=`-9.057505% -> -14.417204%`, pre-exit MAE=`-0.562593% -> -0.468294%`다. 신규 selector는 기회를 늘리고 MAE를 줄였지만 EV와 복리 성과를 악화시켜 일반화에 실패했다. 신규 lane EV는 weak-reversal=`-0.153762%`, bullish-transition=`-0.839266%`이나 같은 보고서 lane ON/OFF에는 재사용하지 않았다. NXT는 `source_quality_blocked`; runtime/order/widget/provider/bot 변경은 없고 `runtime_effect=false`다.
  - 리뷰 결과: current-date outcome이 history로 들어가기 전에 평가를 완료하도록 update 순서를 분리하고, 각 label의 recovery 모델 최종 학습일 `<` label 거래일을 강제했다. trailing 적용 또는 0이 아닌 trailing 배수가 섞인 label은 즉시 실패한다. 대조군과 신규 selector 모두 recovery-only exit로 비중복 재구성해 entry 선택 외 차이를 제거했다. 2차 리뷰에서 eligible 후보와 prior label 수가 혼동되는 보고 결함을 찾아 `184`와 `350`을 분리하고 raw/non-overlap 선택 수를 추가했다. schema v7, 기존 economic=`78건/EV -0.251125%`, axis=`76건` 네 arm 수치 불변, 모든 entry 모델 학습일과 recovery label 모델일의 평가일 이전성, predicted EV 양수 선택, private path 비노출을 artifact로 확인했다. 관련 pytest=`94 passed`, Black/Ruff/compile/checklist parser/`git diff --check` 검증 대상이며 미해결 finding=`0`이다.

- [x] `[SamsungAdaptiveRecoveryEntryCalibration0813] 삼성전자 recovery entry utility prior-only calibration·capacity OOS replay` (`Due: 2026-08-13`, `Slot: RESEARCH`, `TimeWindow: 09:00~12:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungAdaptiveRecoveryAwareEntryUtility0812]` 완료 결과
  - 판정 기준: recovery entry selector가 raw 선택을 `39 -> 64건`으로 늘리면서 EV를 악화시킨 원인을 prior-date prediction residual calibration, lane별 predicted-vs-realized EV bin, 날짜별 drift와 non-overlap capacity로 분리한다. 평가일보다 앞선 calibration history만 사용한 shrinkage 또는 uncertainty-adjusted utility를 untouched next date에 적용하고, 단순 후보 축소가 아니라 EV·복리 순성과·기회 수·MAE의 Pareto 개선 여부를 기존 recovery-entry selector 및 economic control과 비교한다.
  - 금지: 이번 보고서 bullish/weak 결과로 동일 보고서 lane OFF, current-date residual로 calibration 갱신 후 같은 날짜 재평가, full-session MFE/MAE feature 사용, positive lower-bound만 남기는 무표본형 과차단, NXT partial context의 KRX 권한 전용, runtime/widget/order/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `calibrated_recovery_entry_oos_positive`, `calibrated_recovery_entry_pareto_improved`, `no_incremental_predictive_value`, `insufficient_calibration_history`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`no_incremental_predictive_value`. 평가일보다 앞서 OOS 생성된 recovery-entry 예측·실현값만 lane별 reliability-shrunk linear calibration과 최근 3개 prior date bounded residual drift에 사용했다. KRX calibration-ready=`6일`, prior OOS prediction history=`184건`, eligible=`104건`이다. 동일 recovery-only exit에서 economic control=`14건, EV -0.210308%, 복리 -2.954557%, MAE -0.528282%`, raw recovery entry=`21건, EV -0.292606%, 복리 -6.021327%, MAE -0.543250%`다. calibrated mean 양수 후보/비중복은 `0/0건`이었다. 과차단 방지 capacity fallback이 6일 모두 raw arm으로 복원해 최종=`21건`, retention=`100%`, 성과는 raw와 동일하므로 개선으로 인정하지 않았다. raw prediction 상위 quartile 26건도 predicted EV=`+0.271045%` 대비 realized EV=`-0.186389%`여서 ranking/calibration만으로 해결되지 않는 입력·진입시점 병목이 확인됐다. calibrated 평균은 weak-reversal predicted/realized=`-0.272009%/-0.268952%`, bullish-transition=`-0.384864%/-0.518708%`였으나 같은 보고서 lane ON/OFF에는 사용하지 않았다. NXT는 `source_quality_blocked`, `runtime_effect=false`다.
  - 리뷰 결과: 1차 구현의 calibrated mean이 104개 후보를 모두 차단하는 결함을 찾아, 날짜별 mean-positive 비중복 수가 raw arm의 75% 미만이면 current outcome을 보지 않고 완전한 raw arm으로 복귀하는 capacity fallback을 추가했다. 이어 fallback arm이 raw와 동일해도 positive/Pareto 개선으로 오표기될 수 있는 결함을 찾아 raw 대비 최소 한 성과축의 엄격한 개선을 승격 조건으로 추가했다. 모든 calibration fit/recent drift 날짜 `<` 평가일, recovery entry/recovery exit 모델일 `<` label 거래일, 6일 raw/fallback entry identity 일치, uncertainty diagnostic-only, private path 비노출, 기존 v7 entry=`50건/EV -0.304573%`, economic=`78건/EV -0.251125%`, axis 네 arm 수치 불변을 artifact로 검증했다. 관련 pytest/정적 검증과 checklist parser를 통과하고 미해결 finding=`0`으로 닫는다.

- [x] `[SamsungAdaptiveRecoveryEntryTiming0813] 삼성전자 recovery entry 진입시점·체결 utility nested OOS replay` (`Due: 2026-08-13`, `Slot: RESEARCH`, `TimeWindow: 13:00~16:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungAdaptiveRecoveryEntryCalibration0813]` 완료 결과
  - 판정 기준: calibration-ready raw prediction 상위 quartile조차 realized EV가 음수인 원인을 candidate ranking과 entry execution timing으로 분리한다. 동일 causal candidate에서 현재 next-open 진입을 control로 두고, prior-only validation이 선택한 bounded `confirmation continuation`, `first non-chasing pullback`, `VWAP/reclaim 유지 후 next-open` entry timing arm을 untouched next date에 적용한다. 각 arm은 동일 recovery-only exit owner 아래 비용차감 EV·복리·MAE·기회 수·미진입 후 MFE를 비교하고, 현재 날짜에서 가장 좋아 보인 시점을 oracle로 선택하지 않는다.
  - 금지: full-session 저점/고점 또는 미래 MFE로 entry timestamp 선택, current-date arm 결과로 같은 날짜 timing policy 변경, 고정 수익라벨 복원, raw recovery selector보다 75% 미만 기회만 남기는 과차단, NXT partial context의 KRX 권한 전용, runtime/widget/order/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `entry_timing_oos_positive`, `entry_timing_pareto_improved`, `no_incremental_predictive_value`, `insufficient_timing_history`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`entry_timing_pareto_improved`. recovery-entry raw selector와 recovery-only exit owner를 고정하고 completed-bar `confirmation_continuation`, `first_non_chasing_pullback`, 2-bar `vwap_reclaim_hold`의 3/5/10/20분 bounded timing을 각 날짜 당시 prior-only recovery 모델로 먼저 OOS 생성했다. 다음 날짜 정책은 이 earlier-OOS arm outcome만 사용했다. KRX timing-ready=`6일`, prior OOS raw control history=`64건`, 동일 날짜 raw/timing 비중복=`18/18건`, retention=`100%`다. prior-selected weak-reversal 정책은 6일 모두 `confirmation_continuation/20분`이었고 raw 대비 비용차감 EV=`-0.244312% -> -0.094412%` (`+0.149900%p`), 복리 순성과=`-4.360088% -> -1.753384%`, pre-exit MAE=`-0.569936% -> -0.534256%`, 승률 진단=`44.444% -> 50.000%`로 세 축을 모두 개선했다. 다만 절대 EV는 아직 음수라 runtime 승격 근거는 아니다. trigger 미발생 raw 후보=`9건`, 해당 후보의 post-control MFE 평균/최대=`2.591630%/5.021834%`; 기회 과차단 방지를 위한 lane-level raw fallback은 `2일` 발생했다. pullback/VWAP arm은 6일 모두 capacity fallback으로 raw control과 동일했고, NXT는 `source_quality_blocked`, `runtime_effect=false`다.
  - 리뷰 결과: 1차 통합 실행에서 armed execution provenance를 competing-risk 후보에 잘못 추가하고 economic timing 후보에는 누락한 producer 결함을 찾아 소유 위치를 수정하고 회귀 테스트를 추가했다. 이어 75% 계약을 trigger 자체의 최소 발생률로 적용해 9일·49개 weak history가 있어도 모든 정책을 차단한 결함을 찾아, 날짜별 raw capacity fallback 후 prior policy를 선택하도록 고쳤다. arm별 current OOS 비교 누락, lane fallback의 상위 집계 누락, 과거 정책은 raw-selected 전체를 독립 non-overlap했지만 당일은 raw-control non-overlap만 timing하던 비대칭도 보완했다. 최종 artifact에서 모든 timing policy fit 최대일 `<` 평가일, 평가일별 retention `>=75%`, private path 비노출, KRX/NXT 분리, 기존 entry=`50건/EV -0.304573%`, economic=`78건/EV -0.251125%`, axis 네 arm 수치 불변을 확인했다. 관련 pytest=`97 passed`, Black/Ruff/compile/checklist parser/`git diff --check` 검증 대상이며 미해결 finding=`0`이다.

- [x] `[SamsungAdaptiveRecoveryEntryTimingUtility0814] 삼성전자 recovery entry 후보별 timing incremental-utility nested OOS replay` (`Due: 2026-08-14`, `Slot: RESEARCH`, `TimeWindow: 09:00~12:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungAdaptiveRecoveryEntryTiming0813]` 완료 결과
  - 판정 기준: blanket weak-reversal `confirmation_continuation/20분`이 raw 대비 Pareto 개선했지만 EV가 여전히 `-0.094412%`이고 trigger 미발생 후보의 post-control MFE가 평균 `2.591630%`인 병목을 후보별 wait-versus-enter-now 선택 문제로 분리한다. earlier-OOS 동일 후보의 raw control 대비 timing incremental recovery-only EV를 causal baseline/trigger context와 delay feature로 학습하고, untouched next date에서 `enter_now`, prior-selected timing, raw capacity fallback을 선택한다. 동일 recovery-only exit owner 아래 비용차감 EV·복리·MAE·기회 수와 missed-trigger MFE를 비교한다.
  - 금지: current-date arm outcome 또는 future MFE를 후보별 선택 feature/label로 사용, 같은 보고서 결과로 lane/threshold/최대대기 변경, 75% 미만 raw opportunity 보존, NXT partial context의 KRX 권한 전용, runtime/widget/order/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `candidate_timing_utility_oos_positive`, `candidate_timing_utility_pareto_improved`, `no_incremental_predictive_value`, `insufficient_timing_pair_history`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`no_incremental_predictive_value`. baseline `enter_now/wait` 모델은 원 후보 시점 feature만, wait 후 trigger 모델은 최초 completed-bar trigger context만 사용하고, trigger 미발생 또는 skip에서 이미 지난 raw next-open으로 복귀하지 않는 2단계 인과 실행으로 분리했다. KRX earlier-OOS pair history=`30건`, trigger pair=`21건`, model-ready=`2일`, 동일 날짜 control=`6건/EV -0.307658%`, 신규=`5건/EV -0.458520%`, delta=`-0.150862%p`, 복리=`-1.838699% -> -2.275189%`, MAE=`-0.360498% -> -0.389401%`, retention=`83.3333%`다. 누적 3:1 탐색 예산으로 `enter_now=5`, `wait=1`, trigger 관측=`1`, trigger 진입=`0`이었고 fallback=`0`이다. wait 1건은 trigger 모델이 net EV=`-0.692171%`로 예측해 skip했으나 post-OOS attribution에서 즉시/4분 지연 양쪽 recovery-only 실현이 모두 `+0.446652%`여서 수익 기회를 제거했다. NXT는 `source_quality_blocked`, runtime/order/widget/provider/bot 변경 없이 `runtime_effect=false`다.
  - 리뷰 결과: 1차 구현에서 날짜마다 3:1 탐색 예산을 초기화해 하루 비중복 후보가 3건인 실제 표본에서 wait가 영구히 0이 되는 상태 연속성 결함을 발견했다. lane별 prior OOS `enter_now/wait` 수를 다음 평가일로 인계하고 날짜 경계 테스트를 추가했다. 이어 skip 원인을 직접 감사할 수 없던 provenance 결손을 찾아 current decision과 분리된 `post_oos_outcome_attribution`을 추가했다. schema v10, 모든 utility model fit 최대일 `<` 평가일, 정책/recovery fit 최대일 `<` label 거래일, aggregate retention=`83.3333%`, fallback=`0`, private path 비노출, KRX/NXT 분리, 기존 v9 raw/timing=`18/18건`, EV=`-0.244312%/-0.094412%` 불변을 artifact로 확인했다. 관련 pytest=`125 passed`, Black/Ruff/compile/checklist parser/`git diff --check` 검증 대상이며 미해결 finding=`0`으로 닫는다.

- [x] `[SamsungAdaptiveTimingTriggerUtilityCalibration0815] 삼성전자 timing trigger utility prior-only calibration·bounded exploration replay` (`Due: 2026-08-15`, `Slot: RESEARCH`, `TimeWindow: 09:00~12:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungAdaptiveRecoveryEntryTimingUtility0814]` 완료 결과
  - 판정 기준: trigger 모델이 실제 `+0.446652%`인 timing 경로를 predicted net EV `-0.692171%`로 skip한 오차를 prior-only trigger prediction residual, rank calibration, lane/date drift와 trigger-available 표본으로 분리한다. untouched next date에서 calibrated trigger mean utility와 bounded trigger-entry exploration을 비교하되 동일 baseline wait 결정과 recovery-only exit owner를 유지한다. trigger가 있는 wait 후보의 최소 75%를 관측 실행해 overblocking을 방지하고 비용차감 EV·복리·MAE·전체 opportunity retention을 control 및 uncalibrated trigger gate와 비교한다.
  - 금지: current-date trigger outcome으로 같은 날짜 calibration/threshold 갱신, future MFE/MAE feature 또는 label 사용, trigger 미발생 시 과거 raw next-open 복귀, 동일 보고서 lane OFF, KRX/NXT 혼합, runtime/widget/order/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `calibrated_trigger_utility_oos_positive`, `calibrated_trigger_utility_pareto_improved`, `no_incremental_predictive_value`, `insufficient_trigger_history`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`calibrated_trigger_utility_pareto_improved`. 평가일보다 앞선 OOS trigger 예측·실현 residual만 lane별 affine shrink calibration과 최근 3개 prior date bounded drift에 사용하고, trigger 진입 3건당 최대 1건만 model skip할 수 있는 누적 탐색 예산을 추가했다. KRX calibration-ready=`1일`, prior prediction history=`7건/2일`, 2026-08-10 동일 control/raw gate/calibrated=`3/2/3건`이다. 비용차감 EV는 `-0.128383%/-0.415901%/-0.128383%`, 복리 순성과는 `-0.387128%/-0.830072%/-0.387128%`, pre-exit MAE는 `-0.216334%/-0.216509%/-0.144339%`, calibrated retention은 control·trigger 모두 `100%`다. 10:33 wait 후보의 raw/calibrated trigger 예측은 `-0.692171%/-0.122153%`로 여전히 음수였지만 초기 bounded exploration이 10:37 진입을 유지해 실현 `+0.446652%`를 회복했다. 따라서 개선은 calibration 단독 양수 판정이 아니라 calibration과 과차단 방지 탐색의 결합 효과이며, 절대 EV가 음수이고 평가일이 1일뿐이므로 runtime 승격 근거가 아니다. NXT는 `source_quality_blocked`, `runtime_effect=false`다.
  - 리뷰 결과: trigger prediction history가 model fit일만 보존하고 timing policy/recovery model fit일을 누락한 provenance 결함을 보완하고 세 날짜가 모두 label 거래일보다 앞서는지 강제했다. calibrated arm과 raw arm의 baseline `enter_now/wait` identity를 실행 중 assertion으로 고정하고 calibration fit 최대일 `<` 평가일을 decision마다 기록했다. schema v11, 46거래일, OOS provenance, fallback=`0`, private path 비노출, KRX/NXT 분리, 기존 v10 control/selected=`6/5건`, EV=`-0.307658%/-0.458520%`, 기존 v9 raw/timing=`18/18건`, EV=`-0.244312%/-0.094412%` 불변을 artifact로 확인했다. 관련 replay pytest=`143 passed`, Black/Ruff/compile/checklist parser/`git diff --check` 검증 대상으로 미해결 finding=`0`이다.

- [x] `[SamsungAdaptiveTimingWaitBudgetCalibration0815] 삼성전자 candidate timing wait 예산 prior-only OOS 비교` (`Due: 2026-08-15`, `Slot: RESEARCH`, `TimeWindow: 13:00~16:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungAdaptiveTimingTriggerUtilityCalibration0815]` 완료 결과
  - 판정 기준: 2026-08-10의 세 후보가 모두 positive predicted timing incremental EV였지만 누적 3:1 예산이 첫 후보만 wait하고 뒤의 두 손실 후보를 `enter_now`로 고정한 병목을 분리한다. calibrated trigger gate·bounded trigger exploration·recovery-only exit owner를 고정하고, earlier-OOS outcome만으로 3:1·2:1·1:1 wait 예산을 선택해 untouched next date에서 비용차감 EV·복리·MAE·전체 및 trigger opportunity retention을 비교한다. calibration-ready 표본이 얇더라도 arm 결과와 provenance를 누적하며 46거래일 research floor 외에 달성 불가능한 신규 최소 거래일 수를 만들지 않는다.
  - 금지: current-date arm outcome으로 같은 날짜 wait 예산 선택, future MFE/MAE feature 사용, trigger 미발생 시 과거 raw next-open 복귀, 75% 미만 opportunity 보존, calibration/exit owner 동시 변경, KRX/NXT 혼합, runtime/widget/order/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `wait_budget_oos_positive`, `wait_budget_pareto_improved`, `no_incremental_predictive_value`, `insufficient_wait_budget_history`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`insufficient_wait_budget_history`. calibrated trigger·3-entry/1-skip bounded exploration·recovery-only exit를 고정하고 KRX 2026-08-10 untouched arm을 독립 평가했다. 3:1/2:1/1:1은 모두 `3건`, opportunity 및 trigger retention=`100%`였다. 비용차감 EV는 `-0.128383%/-0.092497%/-0.092497%`, 복리 순성과는 `-0.387128%/-0.279438%/-0.279438%`, pre-exit MAE는 `-0.144339%/-0.180414%/-0.180414%`다. 12:53 후보는 12:54 timing 진입으로 손실이 `-0.415551% -> -0.307892%`로 줄었으나 MAE가 악화했고, 11:21 후보는 11:23까지 기다려도 동일 가격·동일 손익이라 1:1 추가 wait 이득이 없었다. 2:1과 1:1은 EV/복리/MAE가 동률이고 2:1의 MFE가 `0.288133%`로 1:1의 `0.252019%`보다 높다. current-date arm 결과는 같은 날짜 정책 선택에 사용하지 않아 prior-selected evaluation=`0일/0건`; 다음 untouched 날짜 selector는 이 earlier-OOS history에서 2:1을 우선한다. NXT는 `source_quality_blocked`, `runtime_effect=false`다.
  - 리뷰 결과: wait ratio를 parameterize하면서 첫 arm 이전 v10 3:1 seed 예산이 두 번째 평가일부터 유실될 수 있는 상태 연속성 결함을 찾아 pre-arm seed와 arm 이후 결정을 누적 결합했다. capacity 실패 arm 하나가 있으면 전체 selector가 영구 정지하거나 제외된 arm을 `max()`에서 다시 조회하는 결함도 capacity-safe subset 선택으로 보완했다. schema v12, 46거래일, 모든 model/calibration fit일 `<` 거래일, 3:1 arm과 v11 calibrated 거래 identity 일치, fallback=`0`, private path 비노출, KRX/NXT 분리, 기존 v9·v10·v11 수치 불변을 artifact로 확인했다. 관련 replay pytest=`154 passed`, Black/Ruff/compile/checklist parser/`git diff --check` 검증 대상으로 미해결 finding=`0`이다.

- [ ] `[SamsungAdaptiveTimingWaitBudgetNextDateOOS0812] 삼성전자 2:1 prior-selected wait 예산 다음 완결 거래일 OOS 귀속` (`Due: 2026-08-12`, `Slot: RESEARCH`, `TimeWindow: 09:00~12:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungAdaptiveTimingWaitBudgetCalibration0815]` 완료 결과
  - 판정 기준: 2026-08-11 삼성전자·KOSPI 전 세션 1분봉이 완결되고 source-quality PASS인 경우 clean-baseline backfill을 2026-08-11까지 확장한다. wait-budget policy fit 최대일이 `2026-08-10` 이하인지 확인하고, prior arm EV 동률 시 복리·MAE·보수적 enter-per-wait tie-break로 선택된 2:1을 2026-08-11 untouched KRX 후보에 적용해 fixed 3:1 및 arm 관측과 비용차감 EV·복리·MAE·기회 유지율을 비교한다. 기존 46거래일 floor 외 신규 최소 거래일 수를 추가하지 않는다.
  - 금지: 불완전한 당일 분봉 backfill, current-date outcome의 same-date policy 선택, 2026-08-11 outcome으로 fit_max_date를 2026-08-11까지 올린 뒤 재평가, NXT partial context의 KRX 권한 전용, runtime/widget/order/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 사전 실행 상태 (`2026-08-11 20:01 KST`): NXT 종료와 반영 유예 뒤 `2026-08-11` 단일일 backfill을 요청했으나 producer의 `fully completed prior KST trading date` guard가 당일 입력을 의도대로 거부했다. 대체 가능한 삼성전자·KOSPI 완결 1분봉 source는 로컬에 없고 신규 artifact도 생성되지 않았다. guard를 장중 완화하거나 위젯 10초 관측을 1분 OHLCV로 변환하지 않으며, 항목은 `complete_next_date_not_available` 상태로 다음 거래일 실행까지 OPEN 유지한다.
  - 다음 액션: `wait_budget_oos_positive`, `wait_budget_pareto_improved`, `no_incremental_predictive_value`, `source_quality_blocked`, `complete_next_date_not_available` 중 하나로 닫는다.

- [x] `[SamsungFixedTPSplitExecutionReplay0811] 고정 entry cohort 분할매수·평단 고정익절 causal replay` (`Due: 2026-08-11`, `Slot: RESEARCH`, `TimeWindow: 20:10~21:30`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), [adaptive replay](/home/ubuntu/KORStockScan/src/engine/monitoring/pure_market_adaptive_opportunity_replay.py), `[SamsungAdaptiveEconomicFirstPassageReplay0812]` 완료 결과
  - 판정 기준: economic first-passage가 prior-only로 선택한 KRX 78개 entry timestamp를 모든 arm에서 고정한다. 단일 100%, 40/60 2-leg, 20/30/50 3-leg를 평단 기준 0.4~0.5% 고정익절, 최초 진입가 대비 -2% 재난성 중단, 장종료 청산으로 비교한다. 분할 체결봉의 새 목표 체결을 금지하고 KRX 유효 호가단위를 적용하며, full planned budget EV를 primary로 두고 deployed-notional EV를 별도 진단한다. current-date outcome은 같은 날짜 arm 선택에 쓰지 않는다.
  - 금지: 고정 entry cohort 변경, future bar 기반 분할 여부 결정, 동일봉 분할·새 평단 목표 동시 체결, 미투입 예비현금을 무료 레버리지로 간주, NXT partial context의 KRX 권한 전용, runtime/widget/order/account/quantity/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 완료 결과 (`2026-08-11`): 판정=`fixed_tp_split_pareto_improved`. KRX 18일/78건에서 단일 0.5% arm은 planned-budget EV=`-0.244535%`, 승률=`71.795%`, 복리=`-17.786214%`, 재난성 중단=`17건`이다. best full-sample arm인 40/60·-0.8% add·평단 +0.5%는 EV=`-0.148918%`, 승률=`78.205%`, 복리=`-11.155254%`, 재난성 중단=`13건`, 평균 투입=`57.6923%`이고 deployed-notional EV=`+0.007613%`다. 20/30/50·-0.5/-1.0% arm은 EV=`-0.157403%`, 승률=`80.769%`, deployed-notional EV=`+0.049386%`다. earlier-date 누적 arm 결과로만 선택한 17일/73건은 동일 날짜 단일 control 대비 EV=`-0.235848% -> -0.215100%`, 복리=`-16.220157% -> -14.720868%`, planned-budget MAE=`-0.843274% -> -0.484426%`로 모두 개선했으나 절대 EV는 음수다. 분할매수·저율 고정익절은 손실과 노출을 줄이는 유효 실행축이지만 기존 entry cohort의 재난성 하락 13~17건을 상쇄하지 못해 runtime 승격 근거는 아니다. NXT는 `source_quality_blocked`다.
  - 리뷰 결과: 최초 구현의 비유효 목표 호가와 동일봉 분할/재난중단 순서가 분할 arm 손실을 과소평가할 수 있음을 찾아 목표가 tick 올림, add/stop tick 내림, 하락 경로에서 crossed add 체결 후 재난중단으로 보완했다. 최종 artifact 재리뷰에서는 시가 진입봉 전체를 건너뛰어 같은 봉의 add/재난중단을 누락한 결함을 추가로 찾아, 진입봉 목표만 금지하고 add/stop은 즉시 평가하도록 보완·재생성했다. planned/deployed return 분리와 prior policy fit 최대일 `<` 평가일을 강제했다. schema v13, 46거래일, `runtime_effect=false`; replay pytest=`135 passed`, checklist parser pytest=`60 passed`, Ruff/Black/compile/`git diff --check` 통과, 미해결 finding=`0`이다.

- [x] `[SamsungFixedTPSplitEntryQuality0813] 분할·고정익절 고정 후 재난성 하락 entry 품질 prior-only 분리` (`Due: 2026-08-13`, `Slot: RESEARCH`, `TimeWindow: 09:00~12:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungFixedTPSplitExecutionReplay0811]` 완료 결과
  - 판정 기준: 가장 안정적인 40/60·-0.8% add·평단 +0.5% 실행 owner를 고정하고, 재난성 중단 13건과 목표/장종료 65건을 entry 시점 causal feature로만 분리한다. 확률 hard gate 대신 prior-only expected loss와 bounded exploration으로 유효 기회 75% 이상을 유지하며, untouched date에서 planned-budget EV·복리·재난중단 빈도·기회 retention을 원 cohort와 비교한다. 46거래일 research floor 외 신규 최소 거래일 수를 추가하지 않는다.
  - 금지: current-date outcome으로 같은 날짜 entry 차단, future MFE/MAE·최저점·최고점 feature 사용, 재난성 손실 전부 회피를 목표로 한 과차단, split/TP/stop owner 동시 변경, NXT partial context의 KRX 권한 전용, runtime/widget/order/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `entry_quality_oos_positive`, `entry_quality_pareto_improved`, `no_incremental_predictive_value`, `insufficient_prior_failure_history`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`, causal 재생성): 판정=`entry_quality_pareto_improved`. 40/60·-0.8% add·평단 +0.5% 실행 owner를 고정하고 entry 시점 economic feature 및 earlier-date fixed-arm outcome만 사용했다. KRX prior-model-ready 17일 control=`73건, EV -0.147171%, 복리 -10.363790%, 재난중단 12건`, prior-only 선택=`61건, EV -0.098670%, 복리 -5.981592%, 재난중단 9건`으로 상대 개선했다. 승률은 `78.082% -> 81.967%`, retention=`83.5616%`, bounded exploration=`53건`이다. skip 12건 중 재난중단 `3건`과 non-catastrophic `9건`을 제거했고, 제거한 수익 기회는 `7건`이다. catastrophic AP/prevalence=`0.189041/0.164384`, Brier=`0.175918`로 약한 순위 신호만 확인됐고 선택 후 절대 EV도 음수이므로 runtime 승격 근거는 아니다. NXT는 `source_quality_blocked`, runtime/order/widget/provider/bot 변경 없이 `runtime_effect=false`다.
  - 리뷰 결과: 1차 구현에서 `class_weight=balanced` logistic 확률을 실제 재난확률로 간주해 prior 발생률과 예상 EV를 왜곡한 결함을 찾아 unweighted regularized logistic과 prior-rate shrinkage로 보완했다. 최종 재리뷰에서 당일 최종 candidate 수로 skip quota를 계산한 미래정보 결함을 추가로 찾아, 동일 날짜의 앞선 3회 진입이 이후 negative-EV 후보 1회 skip 권한을 얻는 prefix-safe `EEE -> S` 상태기계로 교체하고 artifact를 재생성했다. AP·발생률·Brier는 post-OOS diagnostic-only이며 current-date model fit 금지, 모든 관측 prefix 및 누적 retention `>=75%`, fixed execution owner 불변을 검증했다. schema v15에 보존, 46거래일, `runtime_effect=false`; 최종 검증 결과는 하단 recoverable-basin 완료 항목에 함께 기록한다.

- [x] `[SamsungRecoverableBasinEntryDiscovery0814] 고정 실행 owner 기반 recoverable-basin 진입조건 재탐색` (`Due: 2026-08-14`, `Slot: RESEARCH`, `TimeWindow: 09:00~12:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungFixedTPSplitEntryQuality0813]` 완료 결과
  - 판정 기준: 이미 economic-selected 된 78건 안에서 재난확률만 분류하는 축은 닫고, 더 넓은 causal armed-candidate universe에서 40/60·-0.8% add·평단 +0.5% 고정 실행 결과를 직접 label로 만든다. 저점 맞히기가 아니라 분할 후 목표 회복 가능한 basin의 causal 구조·regime·상대강도·거래량 문맥을 prior-only로 탐색하며, 원 후보의 최소 75% 관측과 bounded exploration을 유지한다. 비용차감 planned-budget EV·복리·재난중단·기회수로 기존 economic-selected cohort와 비교한다.
  - 금지: future MFE/MAE·세션 저고점으로 entry timestamp 선택, 고정 가격·고정 drawdown 라벨 복원, 재난손실 완전회피를 위한 과차단, current-date outcome의 same-date 조건 선택, fixed split/TP/stop owner 동시 변경, NXT partial context의 KRX 권한 전용, runtime/widget/order/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `recoverable_basin_oos_positive`, `recoverable_basin_pareto_improved`, `broader_universe_no_incremental_value`, `insufficient_prior_candidate_history`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`broader_universe_no_incremental_value`. KRX prior-model-ready 17일에서 fixed-arm broader control은 `191건, EV -0.112140%, 복리 -19.696423%, 재난중단 30건`, 기존 economic-selected baseline은 `73건, EV -0.147171%, 복리 -10.363790%, 재난중단 12건`, recoverable-basin prior-only 선택은 `170건, EV -0.090151%, 복리 -14.582740%, 재난중단 25건`이다. 선택은 broader control 대비 EV·MAE를 개선했지만 거래 확대 때문에 기존 economic baseline보다 누적손실이 컸고 절대 EV도 음수다. raw candidate=`356건`, broader control 대비 retention=`89.0052%`, model skip=`34건`, bounded exploration=`85건`, skip한 재난중단=`6건`, 함께 놓친 수익=`27건`이다. 직접 EV 예측은 Pearson=`-0.078347`, MAE=`0.582784%`로 일반화하지 못했다. NXT는 `source_quality_blocked`; runtime 승격 없이 research-only로 닫는다.
  - 리뷰 결과: wider candidate마다 동일 fixed arm을 독립 replay한 뒤 실제 포지션 종료 전 후보를 배제하고, skip은 슬롯을 점유하지 않아 후속 후보를 재검토하는 causal state machine을 적용했다. 날짜별 미래 candidate 수를 참조하지 않는 prefix-safe `EEE -> S` quota, prior fit 최대일 `<` 평가일, 관측 prefix·누적 retention `>=75%`, fixed execution owner 불변을 검증했다. 평가일별 full trade 배열이 source 1분봉과 중복되어 artifact를 약 `30MB`까지 키운 결함도 찾아 model·capacity·compact decision provenance와 trade count만 보존해 약 `19MB`로 줄였다. schema v15, 46거래일, `runtime_effect=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`; replay/checklist parser pytest=`198 passed`, checklist print-backlog parser, Ruff/Black/compile/`git diff --check` 통과, 최종 미해결 finding=`0`이다.

- [x] `[SamsungRecoverableBasinParentBucket0815] 고정 실행 기반 coarse parent archetype prior-only attribution` (`Due: 2026-08-15`, `Slot: RESEARCH`, `TimeWindow: 09:00~12:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungRecoverableBasinEntryDiscovery0814]` 완료 결과
  - 판정 기준: 음의 OOS 상관을 보인 고차원 Ridge의 추가 threshold 튜닝을 중지하고, wider causal candidate를 lane·세션 시간대·변동성 regime·상대강도·VWAP/20분 range 위치의 coarse parent archetype으로 한 축씩 분해한다. bucket 경계와 선택은 earlier-date 누적분만 사용하고 40/60·-0.8% add·평단 +0.5% fixed execution, prefix-safe bounded exploration, 기회 retention `>=75%`를 유지한다. broader control 및 기존 economic baseline과 planned-budget EV·복리·재난중단·기회수를 함께 비교하며, 일별 child combo EV가 아니라 rolling parent bucket의 재현성과 충돌 방향을 판정한다.
  - 금지: thin multi-feature combo bucket의 live authority 사용, current-date outcome으로 same-date bucket 생성·선택, future MFE/MAE·저고점 feature, fixed execution owner 동시 변경, runtime/widget/order/account/quantity/provider/bot 적용, NXT partial context의 KRX 권한 전용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `parent_bucket_oos_positive`, `parent_bucket_pareto_improved`, `parent_bucket_no_incremental_value`, `parent_bucket_conflict_only`, `insufficient_parent_history`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`parent_bucket_conflict_only`. KRX prior-axis-ready 16일에서 broader control=`180건, EV -0.121137%, 복리 -19.991865%, 재난중단률 16.111111%`, 기존 economic baseline=`68건, EV -0.173406%, 복리 -11.297196%, 재난중단률 17.647059%`, prior-selected parent axis=`157건, EV -0.093864%, 복리 -14.061252%, 재난중단률 15.286624%`다. parent 선택은 EV·MAE·재난중단률을 개선했고 기회 retention=`87.2222%`를 유지했지만 절대 EV가 음수이며 economic baseline보다 복리손실이 커 Pareto 개선이 아니다. 축 선택은 lane=`1일`, session-time=`2일`, volatility=`13일`이고 모든 16일에서 parent bucket 양·음 방향이 함께 관측됐다. prior-selected volatility bucket은 middle=`43건, EV +0.007563%`, low=`26건, -0.177516%`, high=`61건, -0.108403%`로 중간 변동성만 손익분기 수준이다. NXT는 `source_quality_blocked`; sim policy/PREOPEN/runtime에 적용되지 않았고 real runtime까지는 별도 양의 OOS EV·누적수익·source-quality closure가 남았다.
  - 리뷰 결과: lane·session-time·volatility·relative-strength·VWAP·20분 range 위치를 한 축씩만 평가하고 numeric 경계, bucket EV shrinkage, 날짜별 axis 선택을 모두 earlier-date 누적 outcome으로 제한했다. actual position이 종료될 때까지 슬롯을 점유하고 skip은 후속 후보를 재검토하며 `EEE -> S` prefix-safe quota로 모든 axis/date retention `>=75%`를 검증했다. axis/bucket current outcome은 post-OOS attribution 전용이고 same-date 선택에 쓰지 않으며 full trade 배열은 source bar 재생 대상으로 생략했다. schema v16, 46거래일, `runtime_effect=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`; replay/checklist parser pytest=`200 passed`, checklist print-backlog parser, Ruff/Black/compile/artifact causal invariant/`git diff --check` 통과, 최종 미해결 finding=`0`이다.

- [x] `[SamsungParentBucketConflictStability0816] prior-selected parent 충돌의 날짜 안정성·재난손실 집중도 재판정` (`Due: 2026-08-15`, `Slot: RESEARCH`, `TimeWindow: 12:00~14:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungRecoverableBasinParentBucket0815]` 완료 결과
  - 판정 기준: v16이 이미 생성한 prior-only OOS decision을 고정하고 volatility low/middle/high, session-time, lane parent의 날짜별 EV 기여·재난중단 집중·rolling sign persistence·leave-one-date 민감도를 post-OOS attribution으로 재판정한다. volatility middle의 `43건, +0.007563%`가 소수 날짜나 재난손실 부재에 의존하는지 먼저 확인하며, 새 threshold나 multi-axis combo를 같은 46거래일에서 재선택하지 않는다. 이후 완결 거래일이 추가되면 고정 parent 정의의 next-date attribution만 append한다.
  - 금지: 동일 표본에서 volatility middle을 새 hard gate로 승격, lane×time×volatility thin combo 생성, current-date outcome의 same-date 판단 사용, fixed execution owner 변경, NXT partial context의 KRX 권한 전용, runtime/widget/order/account/quantity/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `stable_parent_edge_needs_next_date_confirmation`, `parent_edge_concentrated_not_reproducible`, `catastrophic_loss_cluster_identified`, `no_stable_parent_edge`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`parent_edge_concentrated_not_reproducible`. v16의 KRX prior-selected parent 진입을 재선택 없이 고정한 `volatility_parent:middle`은 `43건/11일`, equal-weight·source-quality-adjusted EV=`+0.007563%`, 진단 승률=`86.047%`, 양의 날짜=`7/11(63.6364%)`였지만 3관측일 rolling 양수 비율은 `44.4444%`에 그쳤다. 전반부 EV=`-0.042464%`, 후반부=`+0.043583%`; leave-one-date EV 범위=`-0.037391%~+0.049382%`이고 all-positive=`false`라 날짜 제거에 따라 부호가 바뀐다. 재난중단 4건이 전체 음의 손익 규모의 `92.0388%`를 차지했으며, 단일 최악 날짜의 음의 손익 집중도는 `29.6999%`다. NXT는 기존대로 `source_quality_blocked`; volatility middle을 같은 표본의 hard gate로 승격하지 않고 runtime/sim policy/PREOPEN/order에는 반영하지 않는다.
  - 리뷰 결과: stability producer는 v16의 `evaluated_prior_only_parent_axis` 내 실제 enter decision만 읽고 boundary·bucket·axis·action·fixed execution owner를 다시 적합하지 않는다. 날짜별 EV, 3관측일 rolling sign, leave-one-date, 전·후반 EV, 재난손실 및 날짜별 gross-negative 집중도를 별도 post-OOS attribution으로 산출하도록 검증했으며, 양·음 trade를 날짜 net으로 상계해 손실 집중도를 축소하던 검토 중 결함은 trade-level gross-negative 합으로 보완했다. schema v17, 46거래일 source window, `runtime_effect=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`; replay/checklist parser pytest=`154 passed`, Ruff/Black/compile 및 artifact authority·causal invariant 검증을 통과했고 최종 미해결 finding=`0`이다.

- [x] `[SamsungParentCatastrophicEpisodeAudit0817] volatility-middle 재난손실 4건의 사전 causal episode 대조` (`Due: 2026-08-15`, `Slot: RESEARCH`, `TimeWindow: 14:00~16:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungParentBucketConflictStability0816]` 완료 결과
  - 판정 기준: 고정된 `volatility_parent:middle` OOS 진입 중 재난중단 4건(`2026-07-28 11:47`, `2026-07-30 13:02`, `2026-08-05 09:43`, `2026-08-07 10:09`)과 같은 bucket의 비재난·목표회복 episode를 원본 1분봉으로 재구성한다. 진입 시점 이전 1·3·5·10분의 가격/거래량 연속성, VWAP·20분 range 위치, KOSPI 상대강도, 세션 진행도와 결측 provenance를 한 차원씩 대조해 반복되는 사전 손실 signature가 있는지 판정한다. 발견한 signature는 별도 미래 완결 거래일 검증 후보로만 남기고 같은 46거래일의 threshold나 multi-axis hard gate로 선택하지 않는다.
  - 금지: 사후 저점·MFE·MAE·청산값을 진입 feature로 사용, 4건을 완전 회피하도록 조건 역설계, lane×time×volatility child combo 생성, fixed 40/60·-0.8% add·평단 +0.5% TP·재난 stop 변경, NXT partial context의 KRX 권한 전용, runtime/widget/sim policy/PREOPEN/order/account/quantity/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `repeatable_pre_entry_loss_signature_identified`, `loss_signature_not_separable`, `source_contract_gap`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`loss_signature_not_separable`. KRX `volatility_parent:middle`의 고정 OOS 진입 `43건`을 원 causal candidate와 직전 완성 1분봉에 identity join했고 source gap/duplicate=`0/0`, exact KOSPI context=`43/43`이다. outcome은 재난중단=`4건`, 평단 +0.5% 목표회복=`37건`, 장종료=`2건`이다. 재난군은 candidate age 중앙값=`1분` 대 목표회복군=`2분`, pairwise 방향확률=`82.432432%`; volatility scale 중앙값=`0.25618346%` 대 `0.22053949%`, 방향확률=`85.135135%`의 distribution shift를 보였다. 그러나 해당 방향을 단일 제외축으로 가정하면 목표회복 보존율이 각각 `64.864865%`, `51.351351%`로 75% 기회보존 기준을 위반한다. weak-reversal도 재난 `3/4`지만 목표회복 `22/37`과 겹쳤고, 진입 직전 1·3·5·10분 수익률, VWAP, KOSPI 상대강도, lane은 반복 손실을 단독 분리하지 못했다. 따라서 신규 entry blocker/threshold 없이 두 shift만 prospective 관측값으로 남긴다. NXT는 `source_quality_blocked`다.
  - 리뷰 결과: outcome은 비교군 label에만 사용하고 feature는 original causal candidate와 entry 직전 완성봉으로 제한했으며 `future_path_used_as_feature=false`를 episode별로 기록했다. 첫 검증에서 길이가 다른 인접봉 배열에 `zip(strict=True)`를 사용해 정상 sequence가 예외를 내던 결함을 수정했다. 이어 exact KOSPI context availability가 alpha signature로 오인될 수 있던 결함을 provenance source gap으로 분리하고, pairwise 분포차만으로 유효기회를 과차단하지 않도록 target-recovery retention `>=75%`를 signature 계약에 추가했다. schema v18, 46거래일, focus source-quality-adjusted EV=`+0.007563%`, `runtime_effect=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`; targeted replay/checklist parser, Ruff/Black/compile/artifact invariant/`git diff --check` 통과 후 최종 미해결 finding=`0`이다.

- [x] `[SamsungCatastrophicStopRecoveryPath0818] 사전 분리 불가 재난중단 4건의 post-stop 회복·추가하락 경로 판정` (`Due: 2026-08-15`, `Slot: RESEARCH`, `TimeWindow: 16:00~18:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungParentCatastrophicEpisodeAudit0817]` 완료 결과
  - 판정 기준: 진입 전 단일 축으로 재난 4건을 75% 기회보존 하에 분리하지 못했으므로 entry threshold 추가를 중지한다. 동일 fixed 40/60·-0.8% add·평단 +0.5% 실행에서 최초 진입가 -2% 재난중단 시각을 고정한 뒤, 중단 후 1·3·5·10·20·30·60분과 세션 종료까지 추가 MAE·평단 목표 recovery first-hit을 outcome-only로 측정한다. hard stop 체결 control과 단순 지속보유 counterfactual을 분리하고, 재난중단이 terminal loss를 제한했는지 또는 회복 가능한 adverse-first를 조기 확정했는지 판정한다.
  - 금지: post-stop 경로를 entry feature나 같은 표본 threshold로 역사용, stop 제거를 곧바로 runtime/order 정책으로 승격, 새로운 scale-in leg·수량·목표를 동시에 변경, 실현손익과 counterfactual 회복을 합산, NXT partial context의 KRX 권한 전용, runtime/widget/sim policy/PREOPEN/order/account/quantity/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `catastrophic_stop_terminal_loss_protection_supported`, `recoverable_adverse_first_dominates`, `mixed_post_stop_paths_no_owner_change`, `source_contract_gap`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`mixed_post_stop_paths_no_owner_change`. KRX 재난중단 4건의 고정 40/60·-0.8% add·평단 +0.5% 목표를 동일하게 replay했고 identity mismatch/source gap=`0/0`이다. hard-stop control EV=`-1.767802%`, 복리=`-6.885959%`; stop 이후 동일 수량 지속보유의 target-or-terminal-mark 단순 EV=`-0.472423%`, 복리=`-1.890903%`다. `2026-07-28`은 stop 후 9분, `2026-07-30`은 12분에 기존 평단 목표를 회복해 각각 `+0.339326%`, `+0.375540%`로 종료 가능했다. `2026-08-05`, `2026-08-07`은 목표를 회복하지 못했고 15:19 마지막 관측 mark가 각각 `-1.000818%`, `-1.603738%`다. 회복 first-hit 누계는 10분=`1/4`, 20분=`2/4`, 30·60분=`2/4`; stop 다음 봉 이후 추가 하락은 최악 `-0.865801%`다. 목표회복률이 `50%`라 75% dominance 기준에 미달하고, 미회복 2건은 15:30 exact close가 없어 continuation source-quality-adjusted EV=`null`, terminal-limited=`2`, decision evidence complete=`false`다. hard stop/무기한 보유 어느 owner도 변경하지 않는다. NXT는 `source_quality_blocked`다.
  - 리뷰 결과: stop 체결봉의 intrabar high/low 순서는 알 수 없으므로 다음 봉부터만 회복·추가하락을 관측하고 hard-stop control과 counterfactual 손익을 합산하지 않는다. 첫 산출물에서 15:19 마지막 연속매매 mark를 세션 종가로 오표기한 provenance 결함을 찾아 exact 15:30 bar가 없는 non-target episode의 adjusted EV를 차단했다. 저장된 6자리 반올림 평단으로 목표를 재계산해 호가 경계에서 1틱 차이가 날 수 있던 결함도 체결 leg의 정밀 수량·가격 평단 재계산으로 보완했다. schema v19, 46거래일, `runtime_effect=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`; targeted replay/checklist parser, Ruff/Black/compile/artifact invariant/`git diff --check` 통과 후 최종 미해결 finding=`0`이다.

- [x] `[SamsungPostStopBoundedGraceArms0819] 재난중단 직후 5·10·20분 bounded grace 독립 attribution` (`Due: 2026-08-15`, `Slot: RESEARCH`, `TimeWindow: 18:00~19:30`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungCatastrophicStopRecoveryPath0818]` 완료 결과
  - 판정 기준: 즉시 stop과 무기한 지속보유의 우세가 모두 닫히지 않았으므로 stop crossing 이후 기존 평단 +0.5% 목표는 유지하되 최대 5·10·20분까지만 grace를 주는 세 arm을 immediate-stop control과 독립 비교한다. 목표가 먼저 체결되면 기존 목표로 종료하고, 미회복이면 각 horizon의 완성봉 mark로 종료한다. 각 arm의 planned-budget EV·복리·추가 MAE·목표회복 수와 episode별 개선/악화를 보고하되 4건 같은 표본에서 best arm을 runtime owner로 선택하지 않고 미래 재난 episode prospective attribution 후보로만 고정한다.
  - 금지: arm 간 손익 합산, same-sample best horizon runtime 승격, stop crossing 이전 entry feature/threshold 역사용, 새 scale-in leg·수량·목표·emergency floor 동시 변경, 15:19 mark를 15:30 종가로 오표기, NXT partial context의 KRX 권한 전용, runtime/widget/sim policy/PREOPEN/order/account/quantity/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `bounded_grace_candidate_for_prospective_only`, `immediate_stop_retained`, `grace_tradeoff_mixed`, `source_contract_gap`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`bounded_grace_candidate_for_prospective_only`. 동일 KRX 재난 4건과 즉시 stop control EV=`-1.767802%`, 복리=`-6.885959%`를 고정하고 기존 체결수량·평단 +0.5% 목표를 바꾸지 않은 채 stop 다음 봉부터 독립 5·10·20분 grace를 replay했다. 5분 arm은 EV=`-1.070780%`, 복리=`-4.216988%`, 목표회복=`0/4`, episode 개선/악화=`4/0`, 평균/최악 추가 MAE=`-0.054113%/-0.216450%`; 10분은 EV=`-0.491882%`, 복리=`-1.958971%`, 목표회복=`1/4`, 개선/악화=`4/0`, 평균/최악 추가 MAE=`-0.054113%/-0.216450%`; 20분은 EV=`-0.756123%`, 복리=`-3.015503%`, 목표회복=`2/4`, 개선/악화=`3/1`, 평균/최악 추가 MAE=`-0.216450%/-0.865801%`다. 세 arm 모두 control 대비 EV·복리가 개선돼 prospective 후보 `[5,10,20]`으로 남겼지만 same-sample best arm은 선택하지 않았고, 특히 20분의 1건 손실 확대를 분리했다. source gap=`0`; NXT는 `source_quality_blocked`다.
  - 리뷰 결과: exact horizon 완성봉이 없으면 source contract를 닫고 모든 arm의 adjusted EV를 차단하도록 검증했다. 목표 체결봉의 intrabar 저가 순서를 알 수 없어 추가 MAE는 보수적 envelope로 명시하고, stop 이후 저가가 모두 높을 때 MAE가 양수로 오표기되던 결함을 `min(0, excursion)`으로 보완했다. 표본 0건 arm의 복리가 `0%`로 보이던 결함은 `null`로 수정했으며 JSON뿐 아니라 Markdown에도 12개 episode-arm 개선·악화 행을 추가했다. schema v20, `same_sample_best_arm_selected=false`, `runtime_effect=false`, `allowed_runtime_apply=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`; targeted replay/checklist parser pytest=`156 passed`, Black/Ruff/compile/artifact invariant/`git diff --check` 통과 후 최종 미해결 finding=`0`이다.

- [x] `[SamsungPostStopGraceProspectiveOOS0820] 고정 5·10·20분 grace 후보의 신규 재난 episode OOS 귀속` (`Due: 2026-08-15`, `Slot: RESEARCH`, `TimeWindow: 19:30~20:00`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungPostStopBoundedGraceArms0819]` 완료 결과
  - 판정 기준: 2026-08-10까지의 4건에서 정한 candidate set `[5,10,20]`을 재선택하지 않고 고정한다. `2026-08-11` 이후 새로 발생한 동일 `volatility_parent:middle` 재난 episode만 prospective OOS로 귀속해 arm별 기존 목표 first-hit, exact horizon mark, planned-budget EV·복리·추가 MAE·control 대비 개선/악화를 누적한다. 신규 표본이 없으면 정상 관찰대기로 남기며 과거 4건을 신규 OOS처럼 재사용하지 않는다.
  - 금지: prospective 표본을 과거 calibration과 혼합, 신규 표본 1건으로 best horizon 선택·runtime 승격, arm 손익 합산, 수량·평단목표·scale-in·emergency floor·entry threshold 동시 변경, NXT partial context의 KRX 권한 전용, runtime/widget/sim policy/PREOPEN/order/account/quantity/provider/bot 적용을 금지하고 `runtime_effect=false`를 유지한다.
  - 다음 액션: `prospective_grace_evidence_accumulating`, `no_new_catastrophic_episode_observe`, `prospective_grace_tradeoff_changed`, `source_contract_gap`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.
  - 완료 결과 (`2026-08-11`): 판정=`no_new_catastrophic_episode_observe`. 후보 horizon `[5,10,20]`은 `2026-08-10` 종료 시점 값으로 별도 불변 상수에 고정했고, prospective start=`2026-08-11`로 분리했다. KRX 과거 calibration 재난 episode `4건`은 OOS 지표에서 제외됐으며 신규 prospective episode=`0`, source gap=`0`, control 및 세 arm의 EV·source-quality-adjusted EV·복리=`null`이다. 이는 0% 수익이 아니라 신규 완결 표본이 아직 없다는 정상 관찰 상태다. NXT는 기존대로 `source_quality_blocked`다.
  - 리뷰 결과: 후보 set 재선택 금지, calibration/prospective identity 분리, arm 간 episode 및 control return 일치, exact candidate set, calibration 4건 보존, 신규 표본 0건·누적·tradeoff 변화·source gap·NXT block 경로를 검증했다. 일반 grace horizon 상수를 향후 변경하면 frozen set도 함께 바뀔 수 있던 결함은 전용 literal tuple로 분리했다. schema v21, `same_sample_best_arm_selected=false`, `calibration_and_prospective_returns_mixed=false`, `runtime_effect=false`, `allowed_runtime_apply=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`; targeted replay/checklist parser pytest=`156 passed`, Black/Ruff/compile/artifact invariant/`git diff --check` 통과 후 최종 미해결 finding=`0`이다.

- [ ] `[SamsungPostStopGraceFirstNewEpisode0821] frozen grace 후보의 첫 신규 재난 episode 장후 재귀속` (`Due: 2026-08-15`, `Slot: RESEARCH`, `TimeWindow: 20:00~20:20`, `Track: ScalpingLogic`)
  - Source: [adaptive report](/home/ubuntu/KORStockScan/data/report/pure_market_adaptive_opportunity_replay/pure_market_adaptive_opportunity_replay_2026-06-05_2026-08-10.json), `[SamsungPostStopGraceProspectiveOOS0820]` 완료 결과
  - 판정 기준: `2026-08-11` 이후 첫 완결 KRX `volatility_parent:middle` 재난 episode가 생긴 장후에만 v21 producer를 재실행한다. `[5,10,20]`을 그대로 적용해 control과 arm별 목표 first-hit·exact horizon return·추가 MAE를 귀속하며, 신규 표본이 없으면 `no_new_catastrophic_episode_observe`를 유지한다. 첫 1건 결과는 방향성 조기경보일 뿐 arm 선택이나 runtime 권한이 아니다.
  - 금지: 과거 4건을 신규 표본으로 재사용, 신규 1건으로 best horizon 선택·승격, arm/calibration/control 손익 합산, 수량·평단목표·scale-in·emergency floor·entry threshold 변경, NXT partial context의 KRX 권한 전용, runtime/widget/sim policy/PREOPEN/order/account/quantity/provider/bot 적용을 금지한다.
  - 다음 액션: `first_prospective_episode_attributed_observe_only`, `no_new_catastrophic_episode_observe`, `prospective_source_contract_gap`, `source_quality_blocked`, `implementation_defect_repair_required` 중 하나로 닫는다.

- [x] `[WidgetAutoTradeSymbolScope0811] 두산에너빌리티·한화오션 위젯 자동매매 중지` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 19:30~20:00`, `Track: RuntimeStability`)
  - Source: [widget auto-trade service](/home/ubuntu/KORStockScan/src/trading/widget_auto_trade/service.py), [systemd unit](/home/ubuntu/KORStockScan/deploy/systemd/korstockscan-widget-signal-auto-trader.service), [운영 계약](/home/ubuntu/KORStockScan/docs/widget-signal-auto-trading-runbook.md)
  - 판정 기준: collector·advisory·Telegram·장후 calibration은 유지하고 실주문 실행기만 `005930` strict allowlist로 제한한다. 적용 전 `034020`, `042660`의 활성 주문과 당일 위젯 원장을 확인하며 상태 파일을 삭제하거나 외부 보유수량을 자동 청산하지 않는다.
  - 완료 결과 (`2026-08-11`): 한화오션 위젯 원장은 매수·최종청산 체결로 open qty=`0`, 두산에너빌리티는 원장 open qty=`1`이지만 익절 재제출이 broker `매도가능수량 0주`로 명시 거절됐고 활성 주문은 없었다. 실행기 allowlist를 삼성전자 `005930`만으로 고정해 두산·한화의 신규주문·reconciliation·자동청산을 중지하고 수집·신호 계층은 유지한다. 상태 원장은 감사 근거로 보존한다.
  - 롤백: 활성 주문·원장·broker 보유를 먼저 대조한 후 승인된 code만 allowlist에 다시 추가하고 review gate 통과 unit을 재배포한다.

- [x] `[LowPriceTwoLegMachineImplementation0811] 저가 후보 3종목 5개 시간대 실기계와 종목별 튜닝 구현` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 18:10~20:10`, `Track: ScalpingLogic`)
  - Source: [lower-price two-leg machines](/home/ubuntu/KORStockScan/docs/low-price-two-leg-machines.md), [candidate scan](/home/ubuntu/KORStockScan/data/report/samsung_like_machine_candidate_scan_low_price/samsung_like_machine_candidate_scan_2026-08-10.json), [profiles](/home/ubuntu/KORStockScan/src/trading/low_price_two_leg/profiles.py), [tuning producer](/home/ubuntu/KORStockScan/src/engine/monitoring/low_price_two_leg_tuning.py)
  - 완료 결과 (`2026-08-11`): 삼성중공업 midday/오후, 대우건설 midday/오후, SK이터닉스 midday를 profile별 process/state/lock/authority/order ledger로 분리했다. 각 signal은 종가 1주와 종가-1틱 1주, +2틱 target, five-bar entry validity, SOR regular, 무손절·미청산 보유를 고정한다. postclose actual broker state는 종목·시간대별로만 누적하고 하루 전체에서 한 profile·한 tightening axis만 다음 PREOPEN exact-date policy 후보가 된다. 구현 자체는 timer 설치, process start, primary-bot manual exclusion을 수행하지 않았다.
  - 금지: profile 간 outcome/order/position 혼합, target timeout cancel, forced exit/stop loss, 수량·target·validity 변경, same-day tuning apply, provider/bot/cap/broker guard 변경을 금지한다.
  - 리뷰/검증: review gate에서 과거 `HELD` episode의 후일 실제 청산을 원 거래일 결과로 재귀속하고, axis feature가 손상된 보유 건이 tightening subset에서 빠져 후보가 열릴 수 있던 결함을 profile 전체 inventory hard gate로 차단했다. state의 leg status/position/target fill과 aggregate terminal 상태 모순을 source-quality gap으로 격리했으며 연구 report의 embedded hash만 믿지 않고 canonical 본문 hash를 PREOPEN·service 시작 시 재검증한다. 신규/삼성 세 기계·policy/tuning/candidate scan/manual exclusion/postclose wrapper/checklist parser 회귀 pytest=`291 passed`; Black/Ruff/compileall/shell syntax/systemd verify/checklist print-backlog parser/`git diff --check`=`pass`, 최종 미해결 finding=`0`이다. timer 설치, process 기동, primary-bot exclusion, 실주문은 수행하지 않았다.
  - 다음 액션: `implemented_review_pass_activation_pending`으로 닫고 `[LowPriceTwoLegActivationPreopen0812]`가 별도 기동 권한과 preflight를 소유한다.

- [ ] `[LowPriceTwoLegActivationPreopen0812] 저가 후보 2종목 primary-bot 제외 확인·systemd 설치·3개 profile 기동 검증` (`Due: 2026-08-12`, `Slot: PREOPEN`, `TimeWindow: 08:40~08:55`, `Track: RuntimeStability`)
  - Source: [lower-price two-leg machines](/home/ubuntu/KORStockScan/docs/low-price-two-leg-machines.md), [installer](/home/ubuntu/KORStockScan/deploy/install_low_price_two_leg_systemd.sh), [preflight](/home/ubuntu/KORStockScan/src/trading/low_price_two_leg/preflight.py)
  - 판정 기준: 사용자 기동 지시에 따라 `010140`, `475150`의 explicit `manual_operator` exclusion을 먼저 확인하고 installer를 실행한다. 삼성중공업 midday는 `13:20~13:29`, 30 bars, drawdown `0.75%`, near-low `0.35%`; afternoon은 기존 `14:00~14:40`, 30 bars, `1.25%/0.20%`; SK이터닉스 midday는 `13:30~13:54`, 20 bars, `2.00%/0.75%` exact baseline을 사용한다. 대우건설 두 profile은 allowlist·wrapper·timer에서 제외하고 legacy unit도 제거한다. exact-date applied policy, research evidence, shared token, main bot active, profile authority가 모두 PASS인지와 13:10/13:55 preflight 및 13:14/13:59 service timer를 확인한다.
  - 준비 결과 (`2026-08-11 19:27 KST`): `010140`, `475150`의 broker 잔고와 당일 미체결이 모두 `0`임을 공식 계좌 조회로 확인한 뒤 `manual_operator` exclusion을 적용했다. `2026-08-12` exact-date applied policy hash=`566828ca2cb28b5d1089cdd931acdf2917b69fc8afd047b5527dd7e71a8c67a3`, 세 profile의 source-quality/research hash/policy/token/main-bot/exclusion preflight blockers=`[]`이며 active preflight/service timer 6개는 `enabled`이다. 대우건설 unit file/timer는 `0`이다. 실제 scheduled service 시작과 당일 state/order 귀속 확인 전이므로 항목은 OPEN으로 유지한다.
  - 금지: review 미완료, manual exclusion 누락, 연구 report/hash 불일치, applied/authority artifact missing·stale, unresolved existing order/position 상태에서 timer를 설치·우회 기동하지 않는다. 기존 삼성전자/widget unit은 변경하지 않는다.
  - 다음 액션: `installed_enabled_preflight_ready`, `blocked_manual_exclusion_missing`, `blocked_exact_date_policy_or_authority`, `blocked_existing_order_or_position`, `activation_not_authorized` 중 하나로 닫는다.

- [x] `[LowPriceMachineCandidatePostcloseTelegram0811] 저가 실기계 후보 rolling 장후 추천·어드민 Telegram 연결` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 20:10~21:30`, `Track: ScalpingLogic`)
  - Source: [expanded candidate research](/home/ubuntu/KORStockScan/src/engine/monitoring/low_price_two_leg_expanded_candidate_research.py), [postclose wrapper](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh), [traceability](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md)
  - 완료 결과 (`2026-08-11`): 장후 actual-profile tuning 다음에 검토된 9종목×midday/afternoon source-only 탐색을 기본 ON으로 연결했다. target date마다 최근 46 KRX 거래일을 30일 calibration/16일 untouched holdout으로 분리하고 모든 종목의 integrated-SOR 분봉 거래일·source quality, 표본 floor, 양의 notional EV, held 0, 최신 종가 10만원 이하를 통과한 profile만 순위화한다. cached token 또는 공통 source 계약 차단은 `source_quality_blocked` 보고서와 추천 미산출 admin 안내로 격리한다. JSON/Markdown 원자 기록 뒤 `ADMIN_ONLY` Telegram을 최대 3회 전송하고 target-date state로 중복을 막는다. 전송 설정 누락·재시도 소진·authority 위반은 postclose `[FAIL]`로 표면화한다.
  - 권한/금지: `recommendation_only=true`, `runtime_effect=false`, `allowed_runtime_apply=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`, `machine_created=false`, `service_started=false`다. 추천만으로 기계 생성·timer 설치·PREOPEN/live policy·주문·threshold/provider/bot/cap/broker guard 변경을 금지한다.
  - 리뷰/검증: `$korstockscan-review-gate`의 wrapper/Telegram/source-quality/authority 재리뷰와 targeted pytest, Black/Ruff/compile, shell syntax, checklist parser, `git diff --check`를 통과한 뒤 닫는다.

- [x] `[LowPriceMachineExpandingWindowAndTimeLane0811] clean-baseline 전체 튜닝·기존종목 미구현시간대 탐색 레인 추가` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 21:20~22:20`, `Track: ScalpingLogic`)
  - Source: [expanded candidate research](/home/ubuntu/KORStockScan/src/engine/monitoring/low_price_two_leg_expanded_candidate_research.py), [entry-spot research](/home/ubuntu/KORStockScan/src/engine/monitoring/low_price_two_leg_entry_spot_research.py), [traceability](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md)
  - 완료 결과 (`2026-08-11`): 장후 후보 연구의 고정 rolling 46거래일을 제거하고 `2026-06-05` clean baseline부터 target date까지 전 KRX 거래일을 사용하도록 변경했다. 최신 16거래일은 untouched holdout으로 고정하고 이전 clean 구간 전체를 expanding calibration으로 사용한다. 신규 9종목×midday/afternoon과 별도로 기존 구현종목의 active symbol/session pair를 제외한 미구현시간대 lane을 추가했으며, 현재 대상은 SK이터닉스 afternoon 1개다. 보고서·Telegram은 신규종목과 기존종목 시간대 확장을 분리하고 lane별 최대 3개를 표시한다.
  - 권한/금지: active profile의 동일 symbol/session 재탐색은 fail-closed하며 source-only 추천은 기계 생성·기동·PREOPEN/live policy·주문·threshold/provider/bot/cap/broker guard 변경 권한이 없다. Kiwoom `ka10080` 요청 계약은 바꾸지 않고 예상 clean 거래일 수만 명시적으로 확장한다.
  - 리뷰/검증: `$korstockscan-review-gate`로 producer/consumer/wrapper/Telegram/source-quality/authority를 재검토하고 targeted pytest, Black/Ruff/compile, shell syntax, checklist parser, `git diff --check`를 통과한 뒤 최종 미해결 finding 0으로 닫는다.

- [x] `[MachineActualCleanBaselineWindow0811] 실기계 장후 튜닝을 clean-baseline actual-report 전체 창으로 전환` (`Due: 2026-08-11`, `Slot: POSTCLOSE`, `TimeWindow: 22:20~23:10`, `Track: ScalpingLogic`)
  - Source: [Samsung tuning producer](/home/ubuntu/KORStockScan/src/engine/monitoring/samsung_machine_entry_tuning.py), [lower-price tuning producer](/home/ubuntu/KORStockScan/src/engine/monitoring/low_price_two_leg_tuning.py), [traceability](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md)
  - 완료 결과 (`2026-08-11`): 삼성전자 오전·midday·오후 및 삼성중공업 midday·오후/SK이터닉스 midday 실제-state 튜너의 고정 rolling10/rolling20 후보 gate를 제거했다. 각 producer는 `2026-06-05` 이후 생성된 자기 actual-state observation 전부를 종목·시간대별 단일 `clean_baseline_cumulative` 창으로 누적하며, 저가 기계의 후일 자연청산 carry는 durable state로 원 거래일에 재귀속한다. source-quality·표본 floor·양의 EV·보유/미해결·동일 stage 하루 한 축 guard를 유지한다.
  - coverage/금지: 기계 도입 전 또는 report 미생성 거래일은 expected 대비 unobserved coverage로 명시하지만 outcome으로 보간하지 않고 후보 차단 표본으로도 사용하지 않는다. 과거 시세/API 재조회, historical replay 혼합, 종목·시간대 pooling, 수량·target·validity·무손절 보유·provider/bot/cap/broker guard 변경을 금지한다. 기존 report/candidate schema는 PREOPEN 호환 검증으로 계속 읽고 신규 report는 명시적으로 version-up한다.
  - 리뷰/검증: `$korstockscan-review-gate` 1차 재검토에서 휴장일 target이 전체 postclose를 실패시키는 문제, 저가 기계의 당일 source-gap 후보 개방 가능성, 이전 report 비용 계약 미검증, carried-state reconciliation을 report로만 표현한 provenance 오표기를 찾아 보완했다. producer/consumer·schema migration·same-stage owner·PREOPEN preflight/세 실기계 회귀 pytest=`205 passed`; Black/Ruff/compile, wrapper shell syntax, checklist parser(count=`46`), `git diff --check`=`pass`이며 최종 미해결 finding=`0`이다. report 재생성, PREOPEN apply, bot/service 재기동, 주문·수량·target·provider·cap 변경은 수행하지 않았다.

- [x] `[ErrorDetectorInvocationContract0811] 에러디텍터 silent fail·stale report DONE·장중 artifact 오탐 보완` (`Due: 2026-08-11`, `Slot: INTRADAY`, `TimeWindow: 16:10~17:00`, `Track: ScalpingLogic`)
  - Source: [error_detector.py](/home/ubuntu/KORStockScan/src/engine/error_detector.py), [run_error_detection.sh](/home/ubuntu/KORStockScan/deploy/run_error_detection.sh), [artifact_freshness.py](/home/ubuntu/KORStockScan/src/engine/error_detectors/artifact_freshness.py), [time-based operations runbook](/home/ubuntu/KORStockScan/docs/time-based-operations-runbook.md)
  - 완료 결과 (`2026-08-11`): detector 생성 예외와 필수 detector 미등록을 report의 FAIL result와 initialization accounting에 포함하고, 모든 report를 atomic replace로 기록한다. cron wrapper는 고유 `run_id` 임시 report의 schema/mode/date/detector 집합/runtime 권한을 검증한 뒤에만 canonical report 승격·Telegram 알림·`[DONE]`을 허용하며, 누락·이전 실행·부분 JSON은 `[FAIL]`로 닫는다. 전략 runtime mutation은 none으로 유지하면서 실제 bounded 운영 조치는 `operational_mutations`로 분리했다. 20:10 postclose wrapper가 생산하는 pattern-lab propagation artifact의 freshness window를 잘못된 16:10~17:10에서 20:10~21:40으로 정렬해 장중 반복 warning 오탐을 제거했다.
  - 권한/롤백: `runtime_effect=false`, `runtime_mutation=none`이며 threshold·주문·provider·bot 상태를 변경하지 않는다. rollback은 wrapper의 invocation validation 변경만 되돌리되 detector 생성 실패 FAIL 표면화와 atomic write는 유지한다.
  - 리뷰/검증: 에러디텍터 전체 회귀, report 계약·생성 실패·explicit invocation path·artifact window 테스트, shell syntax, compile, checklist parser, `git diff --check`를 통과한 뒤 닫는다.

- [x] `[SamsungWidgetEntryExitPolicyAttribution0822] 삼성전자 진입 시나리오별 고정목표·구조청산 관측 귀속` (`Due: 2026-08-11`, `Slot: RESEARCH`, `TimeWindow: 20:20~21:00`, `Track: ScalpingLogic`)
  - Source: [evaluation producer](/home/ubuntu/KORStockScan/src/engine/monitoring/samsung_widget_advisory_evaluation.py), [daily evaluation](/home/ubuntu/KORStockScan/data/report/samsung_widget_advisory_evaluation/samsung_widget_advisory_evaluation_2026-08-11.json), [observation source](/home/ubuntu/KORStockScan/data/report/samsung_widget_advisory_observation/samsung_widget_advisory_20260811.jsonl)
  - 판정 기준: 기존 actionable 진입을 immutable derived evidence로 `candidate_support_vwap_recovery`, `support_retest_*`, `resistance_reclaim_*`, `vwap_higher_low_recovery` 시나리오로 분리한다. exact entry touch 이후 동일 세션에서 `+0.5%`, `+1.0%` 고정목표와 exact `entry_episode_id`가 일치하는 `EXIT_READY`를 서로 독립 귀속하고 MFE/MAE·도달시간·gross return을 기록한다.
  - 금지: 고정목표 arm 간 손익 합산, stop/scale-in/position capacity가 없는 counterfactual을 실현 EV로 오표기, episode ID 없는 과거 청산 추정 연결, 두산·한화 평가 계약 변경, widget signal/calibration/자동주문/수량/target/provider/bot/runtime 변경을 금지한다. `runtime_effect=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`를 유지한다.
  - 완료 결과 (`2026-08-11`): 삼성전자 전용 additive 평가로 구현했다. source-quality PASS 관측률 80%를 충족한 exact-touch episode만 정책 요약에 포함하고, KRX 종가 단일가의 terminal gap은 diagnostic으로 보존하되 전체 장중 표본을 일괄 폐기하지 않는다. 당일 episode=`16`, eligible=`15`이며 KRX eligible=`13`에서 stop/scale-in/capacity 없는 독립 hold counterfactual의 +0.5% hit=`11/13`(평균 23.268분), +1.0% hit=`10/13`(평균 30.876분)였다. exact episode 구조청산은 `7건`, 단순 gross 평균=`-0.424087%`지만 arms overlap·미청산 episode·비용/수량/손절 미포함 때문에 정책 EV나 실현손익으로 쓰지 않는다. 구조청산은 exact episode ID와 exit authority/source-quality/time/price 계약이 모두 맞을 때만 귀속하며, ID 결손은 `UNATTRIBUTABLE_MISSING_EPISODE_ID`로 닫는다. 실시간 advisory·Telegram·자동매매·기존 +1% 주문·calibration 소비 필드는 변경하지 않았다.
  - 리뷰/검증: 공유 evaluation producer가 두산·한화에도 쓰이는 범위 결함을 삼성 코드 strict gate로 보완했고, 종일 고정목표에 단기 horizon max-gap 규칙을 잘못 적용해 KRX 표본을 전부 제외하던 결함을 ratio coverage 계약으로 분리했다. 세 위젯/advisory/Telegram/calibration 회귀 pytest=`215 passed`, 최종 targeted pytest=`29 passed`; Black/compile/실관측 replay/`git diff --check` 통과 후 미해결 finding=`0`이다.

- [x] `[SamsungWidgetCapacityConstrainedExitReplay0824] 중첩 신호 제거·세션별 단일 포지션 청산정책 재생` (`Due: 2026-08-11`, `Slot: RESEARCH`, `TimeWindow: 23:30~23:55`, `Track: ScalpingLogic`)
  - Source: [evaluation producer](/home/ubuntu/KORStockScan/src/engine/monitoring/samsung_widget_advisory_evaluation.py), [daily evaluation](/home/ubuntu/KORStockScan/data/report/samsung_widget_advisory_evaluation/samsung_widget_advisory_evaluation_2026-08-11.json), [rolling evaluation](/home/ubuntu/KORStockScan/data/report/samsung_widget_advisory_evaluation/samsung_widget_advisory_evaluation_rolling_60d.json)
  - 완료 결과 (`2026-08-11`): 동일 세션·청산정책마다 포지션 슬롯을 1개로 제한하고, 기존 포지션이 청산되기 전 후속 진입신호를 overlap으로 제외했다. 원 신호의 60초 유효시간 내 exact-touch와 source-quality 80% 관측만 진입 가능하며 미청산 포지션은 세션 관측 종료까지 슬롯을 점유한다. KRX `13`개 eligible 신호는 +0.5% arm에서 `6`개 선택/`5`개 완료/`1`개 미청산, +1.0% arm에서 `4`개 선택/`3`개 완료/`1`개 미청산, exact-episode 구조청산 arm에서 `2`개 선택/`1`개 완료/`1`개 미청산으로 축소됐다. 고정 0.20% 왕복비용 가정 후 완료 거래만의 진단 평균은 각각 `+0.436345%`, `+0.862587%`, `-1.284599%`지만 모든 arm에 미청산 포지션이 있어 `decision_evidence_complete=false`이며 정책 EV·승격 근거가 아니다.
  - 계약/금지: 고정목표는 분봉 high touch를 목표가 체결로 보는 시장 counterfactual이며 broker latency·실체결 증거가 아니다. 미청산 terminal mark는 diagnostic만 기록하고 완료수익에 포함하지 않는다. arm 간 수익 합산, 첫날 정책 선택, widget signal/calibration/자동주문/수량/target/stop/scale-in/provider/bot/runtime 변경을 금지하며 `runtime_effect=false`, `actual_order_submitted=false`, `broker_order_forbidden=true`를 유지한다.
  - 리뷰/검증: 구조청산 arm을 `observed_exact_episode_structural_exit_ready`로 명명해 임의 후속 EXIT와의 혼동을 제거하고, rolling consumer가 authority/cost/capacity/count 및 완료 거래의 시간·가격·수익 산식과 미청산 terminal mark를 재검증하도록 보완했다. 세 위젯/advisory/Telegram/calibration 회귀 pytest=`220 passed`, targeted pytest=`34 passed`; Black/Ruff/compile/실관측 report 재생성/checklist parser/`git diff --check` 통과 후 미해결 finding=`0`이다. 실시간 서비스·자동매매·정책값은 변경하지 않았다.

- [ ] `[SamsungWidgetEntryExitPolicyRolling0823] 신규 거래일 시나리오별 청산 정책 누적 관측` (`Due: 2026-08-18`, `Slot: POSTCLOSE`, `TimeWindow: 20:20~20:35`, `Track: ScalpingLogic`)
  - Source: [rolling evaluation](/home/ubuntu/KORStockScan/data/report/samsung_widget_advisory_evaluation/samsung_widget_advisory_evaluation_rolling_60d.json), `[SamsungWidgetEntryExitPolicyAttribution0822]` 완료 결과
  - 판정 기준: 일별 자동 평가가 새 additive episode와 capacity-constrained 필드를 생성하는지 확인하고, KRX/NXT·시나리오별 exact-touch/source-qualified 표본에서 +0.5%·+1.0% hit rate·도달시간과 exact-episode `EXIT_READY` gross return을 분리 누적한다. 동시에 세션·arm별 단일 포지션 선택/overlap 제외/완료/미청산/terminal diagnostic을 누적해 미청산 없는 완료 창이 생기는지 확인한다. 완료 거래 비용차감 평균은 diagnostic이며 실제 체결 EV가 아니다.
  - 금지: 첫날 또는 소수 표본으로 widget signal/threshold/calibration/자동주문/수량/target/stop/scale-in/runtime을 변경하거나 fixed-target counterfactual과 구조청산 손익을 합산하지 않는다.
  - 다음 액션: `episode_evidence_accumulating`, `source_coverage_insufficient`, `episode_provenance_gap`, `structural_exit_contract_gap`, `implementation_defect_repair_required` 중 하나로 닫는다.

<!-- AUTO_NEXT_STAGE2_CHECKLIST_END -->



## Project/Calendar 동기화

문서/checklist를 수정했으면 parser 검증은 실행하고, Project/Calendar 동기화는 사용자가 아래 명령으로 수동 실행한다.

```bash
PYTHONPATH=. .venv/bin/python -m src.engine.sync_docs_backlog_to_project && PYTHONPATH=. .venv/bin/python -m src.engine.sync_github_project_calendar
```
