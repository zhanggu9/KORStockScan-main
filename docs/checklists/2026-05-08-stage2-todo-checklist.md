# 2026-05-08 Stage2 To-Do Checklist

## 오늘 목적

- `statistical_action_weight` 2차 고급축 중 `SAW-4~SAW-6`의 적재 가능성과 리포트 확장 순서를 판정한다.
- 체결품질, 시장/종목 맥락, orderbook absorption 축을 행동가중치에 넣을 수 있는지 sample/readiness 기준으로 분리한다.
- OFI/QI가 P2 내부 live 입력 feature로 반영된 뒤 stale 되지 않도록 prompt/report/calibration 확장 ladder와 owner를 확정한다.

## 오늘 강제 규칙

- 기준선은 `main-only`, `normal_only`, `post_fallback_deprecation`이며 상세 기준은 `Plan Rebase` §1~§6을 따른다.
- `statistical_action_weight`는 report-only/decision-support 축이며 직접 runtime threshold나 주문 행동을 바꾸지 않는다.
- 체결품질 분석은 full/partial fill을 합치지 않는다.
- orderbook/microstructure 필드가 누락되면 추정값으로 손익 결론을 만들지 않는다.
- OFI/QI는 판단축이 아니라 입력데이터 품질개선축으로 관리한다. 현재 live 배선은 `entry-only` P2 내부 feature로 유지하되, `watching/holding/exit`에는 report-only context enrichment 설계 없이 live gate로 바로 연결하지 않는다.

## 장전 체크리스트 (08:50~09:00)

- 없음

## 장중 체크리스트 (09:00~15:20)

- 없음

## 장후 체크리스트 (16:00~20:15)

- [x] `[EntryFunnelRecoveryDecision0508] submitted/full/partial funnel 회복과 score65_74 probe 다음 enable 조건 잠금` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 18:35~18:55`, `Track: ScalpingLogic`)
  - Source: [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md), [sniper_missed_entry_counterfactual.py](/home/ubuntu/KORStockScan/src/engine/sniper_missed_entry_counterfactual.py), [2026-05-06-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-05-06-stage2-todo-checklist.md), [2026-05-07-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-05-07-stage2-todo-checklist.md)
  - 판정 기준: `BUY Funnel Sentinel`의 `UPSTREAM_AI_THRESHOLD/LATENCY_DROUGHT/PRICE_GUARD_DROUGHT`와 `missed_entry_counterfactual`, `submitted/full/partial/COMPLETED + valid profit_rate`를 한 owner에서 같이 본다. `score65_74 recovery probe`는 단독 MISSED_WINNER가 아니라 `submitted 회복`, `full/partial 체결 품질`, `soft_stop tail`까지 닫혀야 다음 장전 enable 후보가 된다.
  - 범위: broad score threshold 완화, fallback 재개, spread cap 재완화는 금지한다. 이번 owner는 `baseline vs blocked_ai_score65_74 vs would-have-submitted/filled`를 분리하는 report-only 판정이다.
  - 다음 액션: `enable`, `hold`, `drop` 중 하나로 닫고, enable이면 다음 장전 단일 canary load item을 새로 만든다. hold/drop이면 owner를 종료하거나 다른 병목축으로 전환한다.
  - 판정: `hold`. `score65_74_recovery_probe`는 threshold-cycle의 `adjust_up` 후보로 남기되, 5/8 장전/장중 실적만으로 다음 장전 수동 enable 항목은 만들지 않는다.
  - 근거: `BUY Funnel Sentinel`은 `UPSTREAM_AI_THRESHOLD` primary + `LATENCY_DROUGHT` secondary다. 세션 기준 `budget_pass=700`, `latency_pass=11`, `order_bundle_submitted=10`, `holding_started=3`, `submitted_to_budget_unique_pct=30.0`, `submitted_to_ai_unique_pct=7.8`로 submitted 회복은 제한적이다. `wait6579_ev_cohort`는 `total_candidates=262`, `entered_attempts=1`, `missed_attempts=261`, `avg_expected_ev_pct=3.4495`, `avg_close_10m_pct=4.1086`로 방향성은 있으나 `partial_samples=0`이다. `missed_entry_counterfactual`은 평가 10건 중 `MISSED_WINNER=6`, `AVOIDED_LOSER=3`, 10분 평균 종가수익률 `-0.263%`, 10분 MAE 평균 `-8.247%`라 단순 enable 근거로는 불충분하다.
  - 다음 액션: score 65~74는 broad threshold 완화가 아니라 `1주/5만원 cap`, latency `DANGER` 제외, 수급/가속/micro-VWAP 유지 조건의 bounded family로만 daily EV에서 추적한다. `partial=0`은 금지가 아니라 post-apply calibration target으로 남긴다.
  - 검증: `PYTHONPATH=. .venv/bin/python -m src.engine.daily_threshold_cycle_report --date 2026-05-08 --calibration-run-phase postclose --ai-correction-provider none` 실행 후 `threshold_cycle_calibration_2026-05-08_postclose.json`에서 `score65_74_recovery_probe.calibration_state=adjust_up`, `safety_revert_required=false` 확인.

- [x] `[LatencyDangerRootCause0508] BUY submitted 직전 latency danger root-cause 분해` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 18:55~19:20`, `Track: RuntimeStability`)
  - Source: [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [performance_tuning_2026-05-07.json](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/performance_tuning_2026-05-07.json), [missed_entry_counterfactual_2026-05-07.json](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/missed_entry_counterfactual_2026-05-07.json), [sentinel_followup_2026-05-07.md](/home/ubuntu/KORStockScan/data/report/sentinel_followup_2026-05-07.md), [sniper_performance_tuning_report.py](/home/ubuntu/KORStockScan/src/engine/sniper_performance_tuning_report.py)
  - 판정 기준: `budget_pass_events=1111`, `order_bundle_submitted_events=10`, `latency_block_events=1100`, `latency_pass_events=11`, `quote_fresh_latency_pass_rate=1.2%` 기준으로 `ws_jitter_too_high`, `ws_age_too_high`, `quote_stale`, `other_danger`, `spread_too_wide`를 분리 집계한다. `BUY Funnel Sentinel`의 `LATENCY_DROUGHT`와 `performance_tuning` slow lane을 같은 owner에서 묶고, root-cause를 `runtime feed health`, `threshold-family candidate`, `instrumentation gap`, `normal drift` 중 하나로 닫는다.
  - 범위: spread cap 재완화, score threshold 완화, fallback 재개, bot restart 자동화, live threshold mutation은 금지한다. 이번 owner는 latency danger의 귀속과 다음 조치 owner를 정하는 report-only 분해다.
  - 다음 액션: `runtime ops incident`, `threshold-family candidate`, `instrumentation fix`, `observe keep` 중 하나로 닫고, threshold-family 후보는 sample floor/rollback guard가 있을 때만 별도 workorder로 승격한다.
  - 판정: `instrumentation fix` 우선, `pre_submit_price_guard` threshold-family 후보로 흡수. 운영장애 restart 대상은 아니다.
  - 근거: 5/8 `performance_tuning`은 `budget_pass_events=368`, `order_bundle_submitted_events=6`, `latency_block_events=362`, `latency_pass_events=6`, `quote_fresh_latency_pass_rate=1.9%`다. latency danger breakdown은 `other_danger=152`, `ws_jitter_too_high=151`, `ws_age_too_high=100`, `quote_stale=59`, `spread_too_wide=1`로 spread cap 문제가 아니라 quote freshness/jitter 귀속이다. `missed_entry_counterfactual`의 상위 missed winner도 terminal `latency_block`이며 terminal fields가 `quote_stale, ws_age_too_high`를 반복한다.
  - 다음 액션: live threshold 완화 대신 `pre_submit_price_guard` family에서 submit revalidation warning, passive probe cancel/confirm, quote age/jitter reason을 daily EV에 연결한다. `other_danger`의 세부 source가 계속 남으면 다음 보강은 threshold 변경이 아니라 latency danger reason instrumentation이다.
  - 검증: 위 daily threshold report 재생성 후 `threshold_cycle_2026-05-08.json`의 `pre_submit_price_guard` 및 `trade_lifecycle_attribution.entry_price`가 생성됨을 확인했다.

- [x] `[PassiveEntryProbeLifecycle0508] 파두 미체결/취소 기반 WAIT+DANGER passive probe 주문관리 보강` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 19:00~19:15`, `Track: RuntimeStability`)
  - Source: [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md), [README.md](/home/ubuntu/KORStockScan/data/threshold_cycle/README.md), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [threshold_cycle_registry.py](/home/ubuntu/KORStockScan/src/utils/threshold_cycle_registry.py), [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py)
  - 판정 기준: 파두 1차 `91200원`/2차 `91700원` 주문은 `WAIT+score75+DANGER+USE_DEFENSIVE+1주 cap` 상태에서 bid보다 2~3틱 낮은 passive 지정가가 60초 동안 미체결 후 취소된 표본이다. 개선은 broad score threshold 완화가 아니라 `send/skip/reprice/cancel` lifecycle 분리다.
  - 실행 메모: `last_watching_ai_action/score` provenance를 저장하고, `WAIT+score>=75+DANGER+USE_DEFENSIVE+1주` 주문은 `entry_order_lifecycle=passive_probe`로 태그한다. 방어가가 best bid보다 깊으면 `SCALPING_ENTRY_PASSIVE_PROBE_MAX_BELOW_BID_TICKS=1` 기준 bid-1tick까지 보정하고, timeout은 `SCALPING_ENTRY_PASSIVE_PROBE_TIMEOUT_SEC=30`으로 제한한다. submit revalidation age와 cancel request/confirm/fail provenance를 `ENTRY_PIPELINE`에 남긴다.
  - 자동화: `entry_submit_revalidation_warning`, `entry_order_cancel_requested`, `entry_order_cancel_confirmed`, `entry_order_cancel_failed`를 `pre_submit_price_guard` threshold family로 편입했다. daily threshold report는 `passive_probe`, `entry_timeout_cancel_confirmed`, `submit_revalidation_warning` sample을 같이 출력한다.
  - 금지선: fallback 재개, score threshold broad 완화, 장중 threshold mutation, 주문 자동 재제출 loop는 열지 않는다. 이번 변경은 같은 entry price owner 내부 bounded lifecycle 보강이다.
  - 검증: `test_entry_ai_price_canary_clamps_wait_danger_passive_probe_to_one_tick`, `test_pending_entry_cancel_logs_receipt_provenance`를 추가했다.

- [x] `[HoldingAICacheLatency0508] holding/gatekeeper cache miss 100%와 slow path 원인 판정` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 19:20~19:45`, `Track: RuntimeStability`)
  - Source: [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [performance_tuning_2026-05-07.json](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/performance_tuning_2026-05-07.json), [sentinel_followup_2026-05-07.md](/home/ubuntu/KORStockScan/data/report/sentinel_followup_2026-05-07.md), [holding_exit_observation_2026-05-07.json](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/holding_exit_observation_2026-05-07.json), [ai_engine.py](/home/ubuntu/KORStockScan/src/engine/ai_engine.py), [ai_engine_gemini.py](/home/ubuntu/KORStockScan/src/engine/ai_engine_gemini.py)
  - 판정 기준: `holding_ai_cache_hit_ratio=0.0`, `gatekeeper_ai_cache_hit_ratio=0.0`, `gatekeeper_eval_ms_p95=12744`, `holding_review_ms_p95=2158`, `AI holding cache MISS=100%`를 기준으로 cache key/bucket/TTL/invalidation/tier provenance를 점검한다. `cache miss가 설계상 정상인지`, `reuse signature가 과도한지`, `slow path가 운영 허용 범위인지`를 분리 판정한다.
  - 범위: cache TTL 확대, model routing 변경, provider 승격, live AI 정책 변경은 금지한다. 이번 owner는 cache reuse 실패와 slow path의 원인/허용선 판정이다.
  - 다음 액션: `acceptable by design`, `instrumentation gap`, `flag-off remediation`, `runtime ops incident` 중 하나로 닫고, remediation이 필요하면 별도 checklist로 분리한다.
  - 판정: `flag-off remediation`. cache miss 자체는 stale 방지를 위한 설계 의도가 있으나, slow path p95는 운영 허용선을 초과해 그대로 정상 처리할 수 없다.
  - 근거: 5/8 `performance_tuning` 기준 `holding_ai_cache_hit_ratio=0.0`, `gatekeeper_ai_cache_hit_ratio=0.0`, `holding_review_ms_p95=17022ms`, `gatekeeper_eval_ms_p95=11433ms`다. lock/build 지연은 `0ms`이고 `gatekeeper_model_call_ms_p95=11433ms`로 slow path 원인은 모델 호출이다. reuse blocker는 holding 쪽 `시그니처 변경=17`, `가격 변화 확대=9`, gatekeeper 쪽 `재사용 창 만료=24`, `시그니처 변경=23`, `WS stale=4`라 cache key/TTL만 늘리면 stale 판단 재사용 리스크가 커진다.
  - 다음 액션: live cache TTL 확대나 model routing 변경 없이, flag-off 상태에서 cache signature bucket/coarsening 후보와 Tier2 slow-call timeout/provenance를 별도 remediation backlog로 둔다. sentinel의 `AI_HOLDING_OPS`는 restart가 아니라 daily EV warning으로 관리한다.
  - 검증: `performance_tuning_2026-05-08.json`의 `holding_ai_cache_modes=miss 17`, `gatekeeper_cache_modes=miss 24`, `gatekeeper_model_call_ms_p95=11433` 확인.

- [x] `[HoldingFlowDeferCost0508] holding_flow_override defer cost와 soft-stop tail 후속 owner 잠금` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 19:45~20:15`, `Track: ScalpingLogic`)
  - Source: [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [holding_exit_observation_2026-05-07.json](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/holding_exit_observation_2026-05-07.json), [trade_review_2026-05-07.json](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/trade_review_2026-05-07.json), [sentinel_followup_2026-05-07.md](/home/ubuntu/KORStockScan/data/report/sentinel_followup_2026-05-07.md), [2026-05-07-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-05-07-stage2-todo-checklist.md)
  - 판정 기준: `holding_flow_override_defer_exit=67`, `soft_stop_rebound` priority 1, `rebound_above_sell_10m_rate=90.0%`, `cooldown_would_block_rate=65.0%`, `trailing_continuation MISSED_UPSIDE=5 / GOOD_EXIT=7`을 같이 본다. `holding_flow_override` 현 owner 유지가 맞는지, `soft_stop whipsaw confirmation` 재판정이 우선인지, `trailing continuation`이 별도 owner로 분리돼야 하는지를 하나의 postclose 판정으로 잠근다.
  - 범위: live override mutation, trailing threshold mutation, same-symbol cooldown mutation, 자동 매도 로직 변경은 금지한다. 이번 owner는 defer cost와 soft-stop tail의 다음 guarded owner를 고정하는 report-only 판정이다.
  - 다음 액션: `owner keep`, `split owner`, `canary hold`, `drop candidate` 중 하나로 닫고, live 변경 필요 시 다음 checklist에 단일축 canary 항목만 생성한다.
  - 판정: `split owner`. `holding_flow_override`는 현 운영 owner로 유지하고, `soft_stop_whipsaw_confirmation`은 threshold-cycle bounded apply 후보, `trailing_continuation`은 freeze/report-only로 분리한다.
  - 근거: `HOLD/EXIT Sentinel`은 `HOLD_DEFER_DANGER` primary, `AI_HOLDING_OPS`/`SOFT_STOP_WHIPSAW` secondary다. 5/8 세션은 `holding_flow_override_defer_exit=59`, `force_exit=2`, `exit_confirmed=1`, unique sell sent/completed가 exit signal 대비 `100%`로 주문 안전 손상은 없다. 장후 post-sell은 한라캐스트 `soft_stop_missed_upside` 1건과 슈어소프트테크 `soft_stop_good_exit` 1건으로 혼재한다. rolling observation은 soft-stop 21건, 10분 sell가 상회 `90.5%`, 10분 buy가 상회 `33.3%`라 whipsaw confirmation은 후보지만 단일 당일 표본만으로 trailing까지 바꾸면 GOOD_EXIT 훼손 위험이 크다.
  - 다음 액션: 다음 자동 적용 후보는 `soft_stop_whipsaw_confirmation` 하나로 제한하고, `holding_flow_ofi_smoothing`은 현행값 유지, `protect_trailing_smoothing`은 sample floor 충족 전 hold, `trailing_continuation`은 freeze 유지한다.
  - 검증: `threshold_cycle_calibration_2026-05-08_postclose.json`에서 `soft_stop_whipsaw_confirmation=adjust_up`, `holding_flow_ofi_smoothing=hold`, `protect_trailing_smoothing=hold_sample`, `trailing_continuation=freeze`, 모두 `safety_revert_required=false` 확인.

- [x] `[ThresholdCalibrationLoop0508] threshold-cycle bounded calibration schema/apply/provenance 구현` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 20:15~20:35`, `Track: RuntimeStability`) (`실행: 2026-05-07 KST`)
  - Source: [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md), [README.md](/home/ubuntu/KORStockScan/data/threshold_cycle/README.md), [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [threshold_cycle_preopen_apply.py](/home/ubuntu/KORStockScan/src/engine/threshold_cycle_preopen_apply.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)
  - 판정 기준: `R5_live_threshold_apply`를 단발 적용/원복이 아니라 `R5_bounded_calibrated_apply`로 전환한다. 조건 미달은 `calibration_state=adjust_up|adjust_down|hold|hold_sample|freeze`로 남기고, safety breach만 `safety_revert_required=true`로 분리한다. 첫 family 우선순위는 `soft_stop_whipsaw_confirmation`, `holding_flow_ofi_smoothing`, `protect_trailing_smoothing`이며 `trailing_continuation`은 1차 loop에서 freeze/report-only다.
  - 실행 메모: `threshold_cycle_YYYY-MM-DD.json`에 `calibration_candidates`, `post_apply_attribution`, `safety_guard_pack`, `calibration_trigger_pack`을 추가했다. preopen apply manifest는 `manifest_only`와 `calibrated_apply_candidate`를 모두 허용하되 둘 다 `runtime_change=false`로 유지한다. soft-stop whipsaw runtime event에는 `threshold_family`, `threshold_version`, `threshold_calibration_state`, `threshold_applied_value` provenance를 남긴다.
  - 금지선: 장중 runtime threshold mutation, 자동 restart, safety guard 없는 env/code 반영은 금지한다. 목표 미달은 rollback 사유가 아니며 다음 장전 manifest calibration 대상이다.
  - 다음 액션: 5/8 장후 `HoldingFlowDeferCost0508` 판정에서 soft-stop tail/defer cost와 calibration candidate를 같이 보고, 승인된 경우에만 다음 장전 단일 family bounded apply 항목을 새로 만든다.

- [x] `[ThresholdCalibrationTwiceDaily0508] 보유/청산 report source bundle 및 장중/장후 2회 calibration 자동화` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 20:35~20:50`, `Track: RuntimeStability`) (`실행: 2026-05-07 KST`)
  - Source: [data/report/README.md](/home/ubuntu/KORStockScan/data/report/README.md), [README.md](/home/ubuntu/KORStockScan/data/threshold_cycle/README.md), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md), [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [run_threshold_cycle_calibration.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_calibration.sh), [install_threshold_cycle_cron.sh](/home/ubuntu/KORStockScan/deploy/install_threshold_cycle_cron.sh)
  - 판정 기준: calibration source를 compact threshold events에 한정하지 않고 `holding_exit_observation`, `post_sell_feedback`, `trade_review`, `holding_exit_sentinel`, `holding_exit_decision_matrix` 기존 report로 확장한다. 새 관찰축을 만들지 않고 `calibration_source_bundle`에 source path/metric만 요약한다. `preclose_sell_target`은 operator preclose review 산출물이며 tuning/calibration source에서 제외한다.
  - 실행 메모: `daily_threshold_cycle_report`에 `calibration_source_bundle`과 `--calibration-run-phase intraday|postclose`, `--calibration-only`를 추가했다. `deploy/run_threshold_cycle_calibration.sh`는 장중 artifact `threshold_cycle_calibration_YYYY-MM-DD_intraday.json`만 생성하고 canonical threshold report를 덮어쓰지 않는다. 장후 wrapper는 `threshold_cycle_calibration_YYYY-MM-DD_postclose.json`을 함께 생성한다.
  - 자동화: `install_threshold_cycle_cron.sh`에 `12:05 KST` `THRESHOLD_CYCLE_INTRADAY_CALIBRATION`을 추가했다. 기존 `16:10 KST` postclose threshold cycle은 postclose calibration도 같이 생성한다.
  - 금지선: 관찰축 신규 추가, 장중 runtime mutation, 자동 restart, report source 단독 live threshold 반영은 금지한다.
  - 보강 메모: family별 `sample_window/window_policy`를 분리한다. `holding_flow_ofi_smoothing`, `score65_74_recovery_probe`처럼 당일/장중 병목을 우선 볼 축과 `soft_stop_whipsaw_confirmation`, `bad_entry_refined_canary`, `scale_in_price_guard`처럼 4월 이후 rolling/cumulative 지속성이 필요한 축을 같은 기준으로 판단하지 않는다.
  - 다음 액션: 운영자는 5/8부터 intraday/postclose calibration artifact의 `source_sample_count`, `calibration_state`, `safety_revert_required`만 확인한다. 신규 이상치가 생겨도 새 관찰축을 열지 말고 기존 source bundle의 누락 필드 또는 safety incident로만 분류한다.

- [x] `[EfficientTradeoffCalibration0508] ADM/SAW, BUY 병목, bad-entry efficient trade-off calibration 전환 구현` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 20:50~21:20`, `Track: RuntimeStability`)
  - Source: [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [README.md](/home/ubuntu/KORStockScan/data/threshold_cycle/README.md), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md), [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [threshold_cycle_preopen_apply.py](/home/ubuntu/KORStockScan/src/engine/threshold_cycle_preopen_apply.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)
  - 판정 기준: 완벽한 spot 탐색 대신 `bounded live canary -> 장중/장후 attribution -> calibration` loop로 전환한다. sample 부족은 live 전면 금지가 아니라 `cap 축소`, `hold_sample`, `max_step_per_day 축소`로 처리하고, rollback은 safety breach에만 제한한다. 신규 관찰축은 추가하지 않는다.
  - 실행 메모: `threshold_cycle` calibration source bundle에 BUY source(`buy_funnel_sentinel`, `sentinel_followup`, `wait6579_ev_cohort`, `missed_entry_counterfactual`, `performance_tuning`)와 decision-support source(`statistical_action_weight`)를 추가했다. calibration family에 `score65_74_recovery_probe`, `bad_entry_refined_canary`, `holding_exit_decision_matrix_advisory`를 추가하고, preopen manifest mode에 `efficient_tradeoff_canary_candidate`를 추가했다.
  - BUY 판정: partial sample `0`은 전면 차단이 아니라 post-apply calibration target으로 이동한다. `score65_74_recovery_probe`는 broad score threshold 완화가 아니라 score 65~74, latency DANGER 제외, 수급/가속/micro-VWAP 유지, 1주/5만원 cap 후보로만 산출한다.
  - ADM/SAW 판정: `candidate_weight_source`가 나온 bucket만 matrix bias 후보로 연결한다. all `no_clear_edge`이면 perfect spot 대기가 아니라 최소 edge 부재로 `hold_no_edge` 처리하고 live AI 응답 변경은 하지 않는다.
  - bad-entry 판정: naive hard block은 재개하지 않고 `bad_entry_refined_candidate`만 soft-stop tail/defer cost 감소 후보로 calibration한다.
  - runtime provenance: `score65_74_recovery_probe`, `bad_entry_refined_candidate`, `bad_entry_refined_exit` 로그에 `decision_source`, `threshold_family`, `threshold_version`, `threshold_calibration_state`, `threshold_applied_value`를 남긴다.
  - 검증: `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/daily_threshold_cycle_report.py src/engine/threshold_cycle_preopen_apply.py src/engine/sniper_state_handlers.py` 통과. `PYTHONPATH=. .venv/bin/python -m pytest src/tests/test_daily_threshold_cycle_report.py src/tests/test_threshold_cycle_preopen_apply.py src/tests/test_state_handler_fast_signatures.py -q` 통과 (`38 passed`).
  - 다음 액션: 5/8 postclose calibration artifact에서 `score65_74_recovery_probe`, `bad_entry_refined_canary`, `holding_exit_decision_matrix_advisory`의 `calibration_state`를 확인하고, 승인된 family만 다음 장전 단일 bounded apply 항목으로 분리한다.

- [x] `[TradeLifecycleAttribution0508] 실적 전중후 lifecycle attribution 공통 계층 구현` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 21:05~21:15`, `Track: RuntimeStability`)
  - Source: [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [README.md](/home/ubuntu/KORStockScan/data/threshold_cycle/README.md), [README.md](/home/ubuntu/KORStockScan/data/report/README.md), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md)
  - 판정 기준: 특정 family의 runtime 후보 stage는 provisional signal로만 보고, 최종 유형은 entry submit/cancel, holding 후보 신호, exit rule/source, 장후 `post_sell_evaluations`를 `record_id`로 join한 lifecycle attribution에서 닫는다. `bad_entry_refined_candidate`뿐 아니라 entry price/passive probe, soft-stop, trailing, holding_flow, scale-in family가 같은 공통 계층을 참조한다.
  - 실행 메모: threshold-cycle report에 `trade_lifecycle_attribution`을 추가하고 `entry_unfilled_cancelled`, `soft_stop_good_exit`, `soft_stop_missed_upside`, `trailing_early_exit`, `holding_flow_*`, `bad_entry_refined` signal type, `scale_in` outcome view를 분리했다. `bad_entry_refined_canary` snapshot에는 post-sell join 기반 `lifecycle_attribution`을 별도로 연결했다.
  - 금지선: 사용자가 특정 종목을 물어본 시점의 부분 로그만으로 배드엔트리/whipsaw/trailing early exit 같은 최종 라벨을 확정하지 않는다. 장후 post-sell outcome이 붙지 않은 record는 `pending_post_sell_outcome`으로 유지한다.
  - 검증: `test_trade_lifecycle_attribution_splits_entry_holding_exit_and_post_sell_types`, `test_bad_entry_refined_candidate_waits_for_postclose_lifecycle_attribution`을 추가했다.

- [x] `[ThresholdAICorrection0508] threshold-cycle AI reviewer + anomaly corrector proposal layer 구현` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 22:05~22:20`, `Track: RuntimeStability`)
  - Source: [README.md](/home/ubuntu/KORStockScan/data/threshold_cycle/README.md), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md), [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [test_daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/tests/test_daily_threshold_cycle_report.py)
  - 판정 기준: AI 역할을 검토자에서 `검토자 + 이상치 수정안 제안자`로 확장하되, 최종 threshold state/value source of truth는 deterministic calibration guard로 유지한다. AI는 `adjust_up|adjust_down|hold|hold_sample|freeze`, 후보값, anomaly route, sample window만 제안할 수 있고 env/code/runtime 변경과 safety guard 우회는 금지한다.
  - 실행 메모: `threshold_cycle_ai_review_YYYY-MM-DD_{intraday,postclose}.json/md` artifact를 추가했다. 기본 실행은 provider 호출 없이 deterministic calibration과 `ai_status=unavailable`을 남긴다. 운영 cron은 OpenAI Responses API + `threshold_ai_correction_v1` strict JSON schema로 correction 생성을 시도하며, 기본 모델은 `gpt-5.5`, fallback은 `gpt-5.4 -> gpt-5.4-mini`다. 이미 생성된 strict JSON 응답은 `--ai-correction-response-json`으로 검증한다. family별 `ai_proposed_value/state/route/evidence`는 guard가 검증하며, parse 실패해도 deterministic calibration은 정상 생성된다.
  - guard: 제안값은 family bounds와 `max_step_per_day` 안으로 clamp한다. `sample_window/window_policy` 불일치는 reject/`hold_sample`로 처리하고, soft-stop/bad-entry/scale-in처럼 rolling/cumulative가 필요한 family는 당일 단일 이상치로 live 후보 승격을 막는다. `instrumentation_gap` route는 threshold candidate review에서 제외한다.
  - 금지선: 장중 threshold mutation, `safety_revert_required` 강제 변경, 단일 사례 live enable 확정, AI correction의 자동 적용은 금지한다.
  - 검증: `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/daily_threshold_cycle_report.py` 통과. `PYTHONPATH=. .venv/bin/python -m pytest src/tests/test_daily_threshold_cycle_report.py -q` 통과.
  - 다음 액션: 5/11 장전에는 전일 intraday/postclose `threshold_cycle_ai_review` artifact가 생성됐는지와 `guard_accepted/guard_reject_reason/runtime_change=false`를 calibration review에 같이 확인한다.

- [x] `[ThresholdAICorrectionCron0508] threshold-cycle cron AI correction 자동 연동` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 22:20~22:30`, `Track: RuntimeStability`)
  - Source: [README.md](/home/ubuntu/KORStockScan/data/threshold_cycle/README.md), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md), [run_threshold_cycle_calibration.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_calibration.sh), [run_threshold_cycle_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh), [install_threshold_cycle_cron.sh](/home/ubuntu/KORStockScan/deploy/install_threshold_cycle_cron.sh)
  - 판정 기준: `12:05` intraday calibration cron과 `16:10` postclose threshold-cycle cron에서 AI correction proposal 생성이 자동 시도되어야 한다. 단, AI correction은 proposal-only이며 deterministic guard가 최종 source of truth다.
  - 실행 메모: `run_threshold_cycle_calibration.sh`와 `run_threshold_cycle_postclose.sh`에 `THRESHOLD_CYCLE_AI_CORRECTION_PROVIDER`를 추가하고 기본값을 `openai`로 전환했다. `THRESHOLD_CYCLE_AI_CORRECTION_RESPONSE_JSON`이 있으면 provider 호출 대신 strict JSON 파일을 검증한다. cron installer는 intraday/postclose 라인에 `THRESHOLD_CYCLE_AI_CORRECTION_PROVIDER=openai`을 명시한다.
  - 금지선: cron AI correction은 env/code/runtime threshold를 직접 변경하지 않는다. API 실패/parse 실패 시 deterministic calibration artifact는 유지하고 `ai_provider_status`/`ai_status`로 실패 사유만 남긴다.
  - 검증: `bash -n deploy/run_threshold_cycle_calibration.sh deploy/run_threshold_cycle_postclose.sh deploy/install_threshold_cycle_cron.sh` 통과. `PYTHONPATH=. .venv/bin/python -m src.engine.sync_docs_backlog_to_project --print-backlog-only --limit 500` 통과.
  - 다음 액션: `ThresholdUnattendedApply0508`에서 PREOPEN auto apply 라인까지 포함해 cron 재설치를 완료한다.

- [x] `[ThresholdUnattendedApply0508] threshold-cycle 완전 무인 auto bounded runtime env apply 전환` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 22:30~22:50`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_preopen_apply.py](/home/ubuntu/KORStockScan/src/engine/threshold_cycle_preopen_apply.py), [run_threshold_cycle_preopen.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_preopen.sh), [run_bot.sh](/home/ubuntu/KORStockScan/src/run_bot.sh), [README.md](/home/ubuntu/KORStockScan/data/threshold_cycle/README.md), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md)
  - 판정 기준: 사람의 preopen 승인 없이 `deterministic calibration guard + AI correction guard + same-stage owner rule`을 통과한 family만 다음 장전 runtime env로 자동 반영한다. 장중 threshold mutation은 계속 금지한다.
  - 실행 메모: `threshold_cycle_preopen_apply`에 `auto_bounded_live` mode, `--auto-apply`, AI guard 필수 옵션, same-stage priority selector, runtime env writer를 추가했다. env 산출물은 `data/threshold_cycle/runtime_env/threshold_runtime_env_YYYY-MM-DD.{env,json}`이다. `src/run_bot.sh`는 당일 runtime env 파일이 있으면 봇 기동 직전에 source한다.
  - safety: `safety_revert_required=true`, `freeze/hold_sample/hold_no_edge`, AI `instrumentation_gap/incident` route, same-stage owner 충돌, env override 누락은 자동 차단한다.
  - 자동화: `deploy/run_threshold_cycle_preopen.sh` 기본 apply mode를 `auto_bounded_live`로 바꾸고, cron installer의 PREOPEN 라인에 `THRESHOLD_CYCLE_AUTO_APPLY=true`, `THRESHOLD_CYCLE_AUTO_APPLY_REQUIRE_AI=true`를 명시했다.
  - 검증: `test_threshold_cycle_preopen_apply.py`에 AI guard 통과 시 runtime env 생성, same-stage priority 충돌 차단, AI instrumentation gap 제외 테스트를 추가했다.

- [x] `[ThresholdDailyEVReport0508] daily EV 성과 제출 리포트 자동 생성` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 22:50~23:00`, `Track: RuntimeStability`)
  - Source: [threshold_cycle_ev_report.py](/home/ubuntu/KORStockScan/src/engine/threshold_cycle_ev_report.py), [run_threshold_cycle_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh), [README.md](/home/ubuntu/KORStockScan/data/threshold_cycle/README.md), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md)
  - 판정 기준: 무인 반영 후 수동 승인/검토 항목을 제출하지 않고, postclose에는 `daily EV` 성과 리포트만 제출한다. 수치는 `trade_review`, `performance_tuning`, postclose calibration, preopen apply manifest에서 가져온다.
  - 실행 메모: `data/report/threshold_cycle_ev/threshold_cycle_ev_YYYY-MM-DD.{json,md}`를 생성하는 `threshold_cycle_ev_report` 모듈을 추가하고, postclose wrapper 마지막 단계에 연결했다.
  - 포함 항목: selected family/runtime_change, completed/open, win/loss, avg profit rate, realized PnL, submitted funnel, latency pass/block, holding review p95, calibration decisions.
  - 검증: `test_threshold_cycle_ev_report.py`에 기존 report source 기반 EV report 생성 테스트를 추가했다.

- [x] `[PatternLabAutomation0508] Gemini/Claude scalping pattern lab improvement order 자동화 편입` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 23:00~23:20`, `Track: RuntimeStability`)
  - Source: [scalping_pattern_lab_automation.py](/home/ubuntu/KORStockScan/src/engine/scalping_pattern_lab_automation.py), [threshold_cycle_ev_report.py](/home/ubuntu/KORStockScan/src/engine/threshold_cycle_ev_report.py), [run_threshold_cycle_postclose.sh](/home/ubuntu/KORStockScan/deploy/run_threshold_cycle_postclose.sh), [README.md](/home/ubuntu/KORStockScan/analysis/README.md), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md)
  - 판정 기준: `gemini_scalping_pattern_lab`와 `claude_scalping_pattern_lab`를 postclose 자동화 입력으로 쓰되, runtime/env/code를 직접 변경하지 않고 `code_improvement_order(runtime_effect=false)`와 `auto_family_candidate(allowed_runtime_apply=false)`만 생성한다.
  - 실행 메모: postclose wrapper가 두 lab에 `ANALYSIS_START_DATE=2026-04-21`, `ANALYSIS_END_DATE=TARGET_DATE`를 주입한 뒤 `scalping_pattern_lab_automation_YYYY-MM-DD.{json,md}`를 생성한다. finding은 `score65_74_recovery_probe`, `bad_entry_refined_canary`, `soft_stop_whipsaw_confirmation` 등 기존 family에 우선 매핑하고, 미매핑 반복 패턴은 신규 family 설계 후보로만 남긴다.
  - EV report: `threshold_cycle_ev`에는 lab freshness, consensus count, auto family candidate count, code improvement order count, top finding/order 3개만 포함한다. 상세 backlog 전문은 별도 automation artifact를 참조한다.
  - 금지선: pattern lab 결과만으로 live threshold/order/exit 판단, repo code patch, runtime env 반영, bot restart를 수행하지 않는다.
  - 검증: aggregator/EV/wrapper 테스트를 추가해 fresh consensus, stale lab reject, gatekeeper latency instrumentation routing, missing artifact warning, wrapper 실행 순서를 확인한다.

- [x] `[ScaleInGuardCalibration0508] 물타기/불타기 scale-in price guard report-only calibration 편입` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 21:20~21:35`, `Track: RuntimeStability`)
  - Source: [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [README.md](/home/ubuntu/KORStockScan/data/threshold_cycle/README.md), [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [statistical_action_weight_2026-05-07.json](/home/ubuntu/KORStockScan/data/report/statistical_action_weight/statistical_action_weight_2026-05-07.json)
  - 판정 기준: `scale_in_price_guard`는 이미 `REVERSAL_ADD/PYRAMID` 주문 직전 가격·수량 safety threshold family로 수집된다. 다만 live threshold 완화가 아니라 기존 report source bundle과 `statistical_action_weight` 표본을 이용해 `report_only_calibration` 후보를 출력한다.
  - 실행 메모: `CALIBRATION_FAMILY_METADATA`에 `scale_in_price_guard`를 추가하고, source bundle에 `scale_in_price_resolved`, `scale_in_price_guard_block`, `scale_in_price_p2_observe`, `compact_scale_in_executed`, `avg_down_wait`, `pyramid_wait`를 연결했다. resolved/executed cohort가 없으면 `calibration_state=hold_sample`로 값 유지한다.
  - 금지선: 별도 승인 전 `SCALPING_SCALE_IN_MAX_SPREAD_BPS`, `SCALPING_PYRAMID_MAX_MICRO_VWAP_BPS`, `SCALPING_SCALE_IN_EFFECTIVE_QTY_CAP` 등 runtime/env 값을 변경하지 않는다. `1주 cap`과 P1 resolver safety는 유지한다.
  - 다음 액션: 5/8 postclose artifact에서 `scale_in_price_guard`의 `resolved_executed`, `guard_block`, `source_sample_count`를 확인한다. resolved/executed cohort가 계속 0이면 live 완화가 아니라 SAW-4~SAW-6 필드 보강으로 넘긴다.

- [x] `[SoftStopLongHorizonForensics0508] post-sell soft-stop 장기 회복 horizon 20/30/60분 확장` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 21:50~22:05`, `Track: ScalpingLogic`)
  - Source: [sniper_post_sell_feedback.py](/home/ubuntu/KORStockScan/src/engine/sniper_post_sell_feedback.py), [test_post_sell_feedback.py](/home/ubuntu/KORStockScan/src/tests/test_post_sell_feedback.py), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [data/report/README.md](/home/ubuntu/KORStockScan/data/report/README.md)
  - 판정 기준: soft-stop threshold를 30/40/90초와 10분 rebound만으로 고정하지 않는다. post-sell feedback은 기존 10분 중심 outcome 분류를 유지하되 `1/3/5/10/20/30/60분` forward horizon을 함께 남긴다.
  - 실행 메모: `POST_SELL_REPORT_SCHEMA_VERSION=3`으로 올리고 evaluation JSON/report row/soft_stop_forensics/top cases/meta에 `20m/30m/60m` MFE/MAE/close/rebound/extra-upside field를 추가했다.
  - 금지선: 장기 rebound 존재만으로 soft stop을 자동 완화하지 않는다. threshold-cycle에서는 late rebound, defer cost, GOOD_EXIT 훼손 허용범위, safety guard를 함께 본다.
  - 검증: `test_post_sell_feedback.py`에 `metrics_30m/metrics_60m`, legacy evaluation backfill, 30/60분 rebound rate, report meta horizon 검증을 추가했다. 5/8 실후보 2건 평가를 v3 schema로 실행했고 `post_sell_feedback_2026-05-08.json` 저장 기준 한라캐스트는 `MISSED_UPSIDE`, `mfe_10m=+2.879%`, `mfe_30m=+4.345%`, `mfe_60m=+4.345%`로 확인됐다.

- [x] `[ScaleInMultiArmCanary0508] 물타기/불타기 1주 cap multi-arm canary 설계 및 장전 적용 후보 분리` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 22:05~22:25`, `Track: ScalpingLogic`)
  - Source: [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [statistical_action_weight_2026-05-07.json](/home/ubuntu/KORStockScan/data/report/statistical_action_weight/statistical_action_weight_2026-05-07.json)
  - 판정 기준: 물타기/불타기는 실행 표본이 없으면 분석할 수 없으므로, live 전면 금지가 아니라 같은 `position-sizing/scale-in` family 안의 bounded multi-arm canary로 샘플을 만든다. 허용 arm은 `exit_only`, `avg_down_wait`, `pyramid_wait`이며 각 arm은 1주 cap, resolved price provenance, order receipt, post-add MFE/MAE, soft-stop/defer outcome을 남긴다.
  - 범위: 무제한 다축 실험, cap 확대, P1 price resolver safety 우회, hard/protect/order safety 우회는 금지한다. 같은 symbol/position에는 하나의 arm만 배정하고, arm allocation은 report에서 복원 가능해야 한다.
  - 판정: `동일 단계 1축` 문구를 그대로 유지하지 않고 `동일 단계 단일 owner + 같은 family 내부 bounded multi-arm`으로 전환한다. 실행 표본 부족은 live 금지 사유가 아니라 `1주 cap` arm 샘플 수집 사유다.
  - 실행 메모: Plan Rebase §0/§2/§7/§8에 `exit_only`, `avg_down_wait`, `pyramid_wait` arm별 provenance/safety guard 원칙을 반영했다.
  - 다음 액션: 2026-05-11 PREOPEN에서 `arm allocation`, `kill switch`, `daily severe loss guard`, `same-stage owner collision guard`, `post-apply calibration field`가 닫히면 live enable, 하나라도 누락되면 simulation/report-only로 판정한다.

- [x] `[SentinelTelegramRemoval0508] BUY/HOLD-EXIT Sentinel Telegram 알림 코드 및 기능 삭제` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 21:35~21:50`, `Track: RuntimeStability`)
  - Source: [buy_funnel_sentinel.py](/home/ubuntu/KORStockScan/src/engine/buy_funnel_sentinel.py), [holding_exit_sentinel.py](/home/ubuntu/KORStockScan/src/engine/holding_exit_sentinel.py), [run_buy_funnel_sentinel_intraday.sh](/home/ubuntu/KORStockScan/deploy/run_buy_funnel_sentinel_intraday.sh), [run_holding_exit_sentinel_intraday.sh](/home/ubuntu/KORStockScan/deploy/run_holding_exit_sentinel_intraday.sh), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md)
  - 판정 기준: Sentinel은 JSON/Markdown report와 권고 액션만 생성한다. Telegram admin alert, semantic dedupe notify state, `--notify-admin`, wrapper notify env는 제거한다.
  - 실행 메모: BUY/HOLD-EXIT Sentinel의 Telegram message builder, config loader, HTTP sender, abnormal dedupe state, `maybe_notify_admin`/`should_notify_admin` 경로를 삭제했다. cron wrapper의 `BUY_FUNNEL_SENTINEL_NOTIFY_ADMIN`, `HOLDING_EXIT_SENTINEL_NOTIFY_ADMIN`, `--notify-admin` 전달도 삭제했다.
  - 금지선: Sentinel 이상치를 근거로 자동 score/spread/threshold 변경, 자동 매도, AI cache TTL 변경, bot restart를 실행하지 않는다.
  - 검증: Sentinel 전용 파일 기준 `notify-admin/telegram_admin_alert/maybe_notify_admin/should_notify_admin/build_telegram_message/_send_telegram/notify_state` 경로가 남지 않음을 확인했다. `PYTHONPATH=. .venv/bin/python -m py_compile src/engine/buy_funnel_sentinel.py src/engine/holding_exit_sentinel.py` 통과. `PYTHONPATH=. .venv/bin/python -m pytest src/tests/test_buy_funnel_sentinel.py src/tests/test_holding_exit_sentinel.py -q` 통과 (`11 passed`). `bash -n deploy/run_buy_funnel_sentinel_intraday.sh deploy/run_holding_exit_sentinel_intraday.sh` 통과. `PYTHONPATH=. .venv/bin/python -m src.engine.sync_docs_backlog_to_project --print-backlog-only --limit 500` 통과.

- [x] `[StatActionAdvancedContext0508] SAW-4~SAW-6 체결품질/시장맥락/orderbook 축 적재 가능성 판정` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 16:00~16:45`, `Track: ScalpingLogic`)
  - Source: [2026-04-30-data-driven-threshold-inventory.md](/home/ubuntu/KORStockScan/docs/audit-reports/2026-04-30-data-driven-threshold-inventory.md), [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [sniper_execution_receipts.py](/home/ubuntu/KORStockScan/src/engine/sniper_execution_receipts.py)
  - 판정 기준: `SAW-4` full/partial/slippage/adverse fill, `SAW-5` market_regime/volatility/marcap/sector/VI freshness, `SAW-6` orderbook absorption/large sell print/micro VWAP 이탈을 action weight report에 넣을 수 있는지 확인한다. 각 축별 필드 존재율, join key, sample floor, compact stream 포함 여부, report-only 유지 조건을 표로 잠근다.
  - why: 1차 가격/거래량/시간대 축만으로는 행동 선택의 기대값 차이를 충분히 설명하지 못한다. 다만 체결품질과 orderbook 축은 필드 누락 시 왜곡 위험이 크므로 적재 가능성부터 닫아야 한다.
  - 다음 액션: readiness가 높은 축 1개만 다음 구현 항목으로 승격하고, 나머지는 누락 필드 보강 항목으로 분리한다.
  - 판정: `SAW-6 orderbook absorption/microstructure`를 다음 구현 항목으로 승격한다. `SAW-4`는 compact fill quality 필드는 있으나 full 2건/partial 0건으로 표본이 부족하고, `SAW-5`는 market regime freshness/join owner가 아직 약해 후순위다.
  - 근거: 5/8 `statistical_action_weight`는 `completed_valid=50`, `compact_decision_snapshot=89`, `compact_scale_in_executed=21`까지 생성됐다. `performance_tuning`에는 `ofi_orderbook_micro_samples=380`, state `neutral=357/bearish=22/bullish=1`, bucket 분포와 warning 0건이 있어 orderbook context는 존재율과 sample이 가장 높다. 반면 체결품질은 `full_fill_events=2`, `partial_fill_events=0`, full fill completed avg `-0.395%`라 slippage/adverse fill 결론에는 부족하다.
  - 다음 액션: 2026-05-11 장후에 `statistical_action_weight` report-only 확장으로 `SAW-6 orderbook context`를 먼저 구현한다. `SAW-4`는 fill 표본 누적 후, `SAW-5`는 regime snapshot freshness/join key가 닫힌 뒤 분리한다.
  - 검증: daily threshold report 실행 후 `data/report/statistical_action_weight/statistical_action_weight_2026-05-08.{json,md}`와 `holding_exit_decision_matrix_2026-05-08.{json,md}` 생성 확인. matrix는 전 entry가 `recommended_bias=no_clear_edge`라 live AI 응답 변경 없음.

- [x] `[OFIQExpansionLadder0508] OFI/QI P2 내부 feature 확대적용 ladder 및 stale 방지 owner 확정` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 16:45~17:15`, `Track: ScalpingLogic`)
  - Source: [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [2026-05-03-ofi-audit-response-result-report.md](/home/ubuntu/KORStockScan/docs/audit-reports/2026-05-03-ofi-audit-response-result-report.md), [README.md](/home/ubuntu/KORStockScan/data/report/README.md), [orderbook_stability_observer.py](/home/ubuntu/KORStockScan/src/trading/entry/orderbook_stability_observer.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [sniper_performance_tuning_report.py](/home/ubuntu/KORStockScan/src/engine/sniper_performance_tuning_report.py)
  - 판정 기준: OFI/QI 후속을 `P2 prompt contract 명문화`, `performance_tuning Markdown OFI/QI 섹션`, `bucket calibration ON 기준`, `symbol anomaly watch`, `prompt drift/stale guard` 5단계 ladder로 잠근다. 각 단계별 owner, 금지 범위, 필요한 로그 필드, sample floor, rollback owner를 표로 남긴다. 별도로 `holding/exit context enrichment`는 live 판단 변경이 아니라 입력 품질 보강 후보로 분리해 필요한 필드와 attribution owner만 정의한다.
  - prompt 기준: `entry_price_v1` prompt에서 OFI/QI의 사용 범위를 `submitted 직전 주문가/USE_DEFENSIVE/IMPROVE_LIMIT/SKIP 판단 보조`로 명시하고, `neutral/insufficient`이면 OFI/QI 단독 SKIP 금지를 유지한다. prompt 문자열 개선은 AIEngineFlagOffBacklog와 충돌하지 않도록 P2 entry_price contract 전용 workorder로만 연다.
  - report 기준: `performance_tuning_YYYY-MM-DD.md` 구현 후보에 OFI/QI 섹션을 포함한다. 최소 필드는 `ofi_orderbook_micro_states`, `ofi_orderbook_micro_threshold_sources`, `ofi_orderbook_micro_buckets`, `ofi_orderbook_micro_warnings`, `symbol_anomalies`, `entry_ai_price_skip_policy_warning/basis`다.
  - holding/exit 입력 품질 기준: 매도/보유 쪽 OFI/QI는 `exit_context.orderbook_micro`, `snapshot_age_ms`, `micro_state`, `ofi/qi threshold source`, `post_exit_mfe/mae`, `exit_rule` join이 report-only로 닫히기 전에는 live exit gate로 쓰지 않는다. 이 단계는 새 매도 canary가 아니라 입력 context 품질 보강이며, 이후 성과 귀속이 확인될 때만 별도 guarded canary 여부를 판단한다.
  - 민감도 기준: `bearish_supported SKIP`과 `neutral/insufficient warning SKIP`을 별도 cohort로 분리한다. `skip_without_bearish_ofi`가 반복되면 OFI/QI threshold 완화가 아니라 `SKIP -> USE_DEFENSIVE/P1 fallback` demotion guard 후보로 보고, missed upside와 adverse fill 회피 효과를 같이 비교한다.
  - 5/4 근거: [2026-05-04 checklist](./checklists/2026-05-04-stage2-todo-checklist.md) `OrderbookMicroP2Canary0504-Postclose` 기준 P2 stage 111건 중 `USE_DEFENSIVE=96`, `SKIP=8`, micro state는 `neutral=99`, `bearish=7`, `insufficient=3`이다. SKIP 8건 중 `ofi_bearish_supported=6`, `neutral=2`, `skip_without_bearish_ofi=4` warning이 있어 neutral/insufficient demotion guard 검토 근거가 있다.
  - calibration 기준: `SCALPING_ENTRY_PRICE_ORDERBOOK_MICRO_BUCKET_CALIBRATION_ENABLED`는 기본 OFF다. ON 후보는 `ThresholdOpsTransition0506` 이후에도 별도 workorder, manifest id/version, sample floor, fallback 급증 guard, restart 절차가 닫혀야 한다.
  - stale 방지: 2영업일 연속 OFI/QI 로그 표본이 0이거나, `snapshot_age_ms`/observer health/fallback reason이 report에 누락되거나, SKIP warning 분포가 Markdown에 나타나지 않으면 `stale_context`로 보고 다음 checklist에 보강 작업을 자동 생성한다.
  - 다음 액션: ladder가 잠기면 `performance_tuning Markdown 구현`, `entry_price_v1 prompt contract 보강`, `bucket calibration ON/OFF workorder` 중 readiness가 높은 1개만 다음 단일 작업항목으로 분리한다.
  - 판정: ladder 확정. 다음 단일 작업은 `performance_tuning Markdown OFI/QI section + stale guard` 구현이다. prompt contract 보강과 bucket calibration ON/OFF workorder는 그 다음이다.
  - 근거: P2 내부 OFI/QI는 5/8 `ofi_orderbook_micro_samples=380`, `ofi_orderbook_micro_threshold_sources=global`, warnings 0건으로 데이터 생성은 정상이다. 그러나 현재 Markdown/운영자가 읽는 performance report에는 stale guard와 SKIP warning 분포가 충분히 노출되지 않아, calibration ON/OFF 판단보다 report 표면화가 선행되어야 한다.
  - 다음 액션: 2026-05-11 장후 후속으로 `performance_tuning_YYYY-MM-DD.md`에 `ofi_orderbook_micro_states`, threshold source, bucket, warning, symbol anomaly, `entry_ai_price_skip_policy` 요약을 추가한다. `holding/exit` 연결은 live gate가 아니라 context enrichment/report-only로 유지한다.
  - 검증: 5/8 `performance_tuning_2026-05-08.json`에서 OFI/QI sample/state/bucket/warning 필드 존재 확인. 장중 runtime mutation, standalone OFI BUY/EXIT hard gate, bucket calibration ON은 열지 않았다.

- [x] `[Phase3Quality0508] counterfactual realism 3-mode 및 exit decision authority provenance 채택 여부 판정` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 17:15~17:35`, `Track: ScalpingLogic`) (`실행: 2026-05-07 16:57 KST`)
  - Source: [2026-04-10-scalping-ai-coding-instructions.md](/home/ubuntu/KORStockScan/docs/reference/2026-04-10-scalping-ai-coding-instructions.md), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [sniper_missed_entry_counterfactual.py](/home/ubuntu/KORStockScan/src/engine/sniper_missed_entry_counterfactual.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [sniper_trade_review_report.py](/home/ubuntu/KORStockScan/src/engine/sniper_trade_review_report.py)
  - 판정 기준: `Phase 3-1`은 현재 `missed_entry_counterfactual` 관찰축과 `counterfactual 손익 미합산` 원칙으로 부분 채택됐는지, 그리고 `optimistic/realistic/conservative` 3모드가 실제 구현됐는지 분리 판정한다. `Phase 3-2`는 `exit_decision_source` 필드와 `PRESET_HARD_STOP/PRESET_PROTECT/AI_REVIEW_EXIT/SOFT_STOP/TIMEOUT/MANUAL` authority taxonomy가 runtime log/report/test에 존재하는지 확인한다.
  - why: 초기 튜닝 문서의 `분석 품질 고도화`는 현재 Rebase에 원칙 단위로만 흡수돼 있고, 구현 단위로는 `partial adopted`와 `missing`이 섞여 있다. 이 상태를 그대로 두면 `counterfactual realism`과 `청산 authority provenance`가 다시 stale 된다.
  - 판정: `Phase 3-1`은 `partial adopted`다. `missed_entry_counterfactual` 관찰축과 `counterfactual 손익 미합산` 원칙은 반영됐지만 `optimistic/realistic/conservative` 3모드는 실제 구현되지 않았다. `sniper_missed_entry_counterfactual.py`는 단일 `metrics_5m/metrics_10m/outcome` 체계만 가진다.
  - 판정: `Phase 3-2`는 `missing`이다. `exit_decision_source` 문자열은 runtime/report/test 경로에 없고, 현재는 `exit_rule/reason/sell_reason_type/stage`가 암묵 provenance 역할만 한다. `PRESET_HARD_STOP/PRESET_PROTECT/AI_REVIEW_EXIT/SOFT_STOP/TIMEOUT/MANUAL` taxonomy를 잠근 단일 field가 없다.
  - 근거: repo grep 기준 `exit_decision_source`는 설계 문서에만 있고 코드에는 없다. 반면 `missed_entry_counterfactual`는 이미 monitor snapshot/report owner가 있으며 `counterfactual != realized PnL` 원칙도 Rebase에 반영돼 있다. 따라서 readiness가 더 높은 다음 단일 구현 owner는 `exit_decision_source provenance`다.
  - 다음 액션: `exit_decision_source provenance`를 다음 단일 구현 owner로 승격하고, `3-mode counterfactual`는 `report-only/설계 only`로 잠근다.

- [x] `[ExitDecisionSourceProvenance0508] exit_decision_source taxonomy runtime/report/test 반영` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 18:55~19:20`, `Track: ScalpingLogic`) (`실행: 2026-05-07 17:12 KST`)
  - Source: [2026-04-10-scalping-ai-coding-instructions.md](/home/ubuntu/KORStockScan/docs/reference/2026-04-10-scalping-ai-coding-instructions.md), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [sniper_trade_review_report.py](/home/ubuntu/KORStockScan/src/engine/sniper_trade_review_report.py)
  - 판정 기준: `PRESET_HARD_STOP`, `PRESET_PROTECT`, `AI_REVIEW_EXIT`, `SOFT_STOP`, `TRAILING`, `TIMEOUT`, `MANUAL`, `OVERNIGHT_FLOW`, `HOLDING_FLOW_OVERRIDE` 중 하나로 귀속되는 `exit_decision_source` field를 runtime log/report/test에 추가한다. `exit_rule`와 별도 field로 남기고 realized PnL 집계 기준을 바꾸지 않는다.
  - 금지선: 새 taxonomy를 근거로 live 청산 우선순위를 바꾸지 않는다. matrix advisory flag, soft stop rule, holding_flow_override action 자체를 이 항목에서 바꾸지 않는다.
  - 실행 메모 (`2026-05-07 KST`): `sniper_state_handlers.py`에 `exit_decision_source` resolver를 추가해 `exit_signal`, `sell_order_sent`, `sell_order_failed`, `sell_order_blocked_market_closed`에 authority taxonomy를 기록했다. `sniper_execution_receipts.py`는 `sell_completed`까지 같은 field를 넘기고, `sniper_trade_review_report.py`는 필드가 있으면 그대로 표기하고 과거 로그는 `exit_rule/reason`으로 fallback 추정한다. `sniper_post_sell_feedback.py` candidate payload에도 동일 provenance를 포함했다.
  - 판정: runtime/report/test provenance는 닫혔다. `PRESET_HARD_STOP/PRESET_PROTECT/AI_REVIEW_EXIT/SOFT_STOP/TRAILING/TIMEOUT/OVERNIGHT_FLOW/HOLDING_FLOW_OVERRIDE/MANUAL` taxonomy가 공통 field로 고정됐다.
  - 검증: `pytest src/tests/test_sniper_scale_in.py src/tests/test_holding_flow_override.py src/tests/test_trade_review_report_revival.py src/tests/test_post_sell_feedback.py -q` 통과 (`157 passed`). `py_compile` 통과.
  - 다음 액션: provenance가 닫혔으므로 이후 `ADM advisory enable`, `winner wide-window`, `bad-entry refined` 해석은 같은 authority field를 공통 근거로 사용한다.

- [x] `[SAW3EligibleOutcomeImplementation0508] eligible-but-not-chosen 후행 MFE/MAE report-only 구현` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 17:35~17:50`, `Track: ScalpingLogic`)
  - Source: [2026-05-07-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-05-07-stage2-todo-checklist.md), [postclose_decision_support_followup_2026-05-07.md](/home/ubuntu/KORStockScan/data/report/postclose_decision_support_followup_2026-05-07.md), [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)
  - 판정 기준: `stat_action_decision_snapshot`의 `chosen_action/eligible_actions/rejected_actions/record_id/emitted_at`를 후행 quote 또는 position outcome과 join해 `eligible_but_not_chosen`별 `post_decision_mfe`, `post_decision_mae`, `missed_upside`, `avoided_loss`를 계산하는 report-only 섹션을 구현한다. compact partition read cap, horizon, 중복 snapshot downsample, selection-bias caveat를 같이 잠근다.
  - 금지선: 선택하지 않은 action의 counterfactual을 realized PnL과 합산하지 않는다. live 주문/청산 threshold나 AI 응답을 직접 바꾸지 않는다.
  - 선반영: 2026-05-07 장후 `statistical_action_weight` Markdown/JSON에 `eligible_but_not_chosen` 섹션을 추가했다. 5/8에는 true 후행 quote join과 snapshot 중복 downsample 품질 보강만 별도 필요 시 분리한다.

- [x] `[ADMReportOnlyContract0508] holding_exit_decision_matrix report-only 계약 및 ADM-2 불허선 정리` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 17:50~18:05`, `Track: AIPrompt`)
  - Source: [workorder-shadow-canary-runtime-classification.md](/home/ubuntu/KORStockScan/docs/workorder-shadow-canary-runtime-classification.md), [2026-05-07-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-05-07-stage2-todo-checklist.md), [holding_exit_decision_matrix_2026-05-07.json](/home/ubuntu/KORStockScan/data/report/holding_exit_decision_matrix/holding_exit_decision_matrix_2026-05-07.json), [ai_engine.py](/home/ubuntu/KORStockScan/src/engine/ai_engine.py), [ai_engine_openai.py](/home/ubuntu/KORStockScan/src/engine/ai_engine_openai.py), [ai_engine_deepseek.py](/home/ubuntu/KORStockScan/src/engine/ai_engine_deepseek.py)
  - 판정 기준: 5/7 판정에 따라 ADM-2 runtime shadow prompt 주입은 불허하고, ADM-1 report-only matrix의 `matrix_version`, `prompt_hint`, `hard_veto`, provenance, offline operator review 계약만 정리한다. `prompt_profile=exit`는 holding route alias로 유지하되 matrix context를 runtime prompt/cache key에 넣지 않는다.
  - 금지선: ADM-2 shadow, ADM-3 advisory nudge, ADM-4 weighted live, ADM-5 policy gate는 별도 사용자 승인 전까지 OFF다. live AI 응답을 채택하지 않는다.
  - 선반영: 2026-05-07 장후 Plan Rebase/checklist/follow-up에 ADM-1 report-only 허용선과 ADM-2 불허선을 정리했다. 필요한 경우 matrix prompt_hint/token budget 축소만 별도 작업으로 분리한다.

- [x] `[ADMCanaryLivePivot0508] holding_exit_decision_matrix live canary pivot readiness 판정` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 18:35~18:55`, `Track: AIPrompt`) (`실행: 2026-05-07 16:50 KST`)
  - Source: [2026-05-07-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-05-07-stage2-todo-checklist.md), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [holding_exit_decision_matrix_2026-05-07.json](/home/ubuntu/KORStockScan/data/report/holding_exit_decision_matrix/holding_exit_decision_matrix_2026-05-07.json), [holding_exit_matrix_runtime.py](/home/ubuntu/KORStockScan/src/engine/holding_exit_matrix_runtime.py), [ai_engine.py](/home/ubuntu/KORStockScan/src/engine/ai_engine.py), [ai_engine_openai.py](/home/ubuntu/KORStockScan/src/engine/ai_engine_openai.py), [ai_engine_deepseek.py](/home/ubuntu/KORStockScan/src/engine/ai_engine_deepseek.py), [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py)
  - 판정 기준: `AIDecisionMatrixShadow0507`의 shadow naming을 폐기하고, 실제 다음 축을 `single-owner advisory/live canary`로 전환할지 판정한다. 최소 조건은 runtime matrix loader, feature flag, cache key 분리, matrix provenance logging, provider parity 범위 고정, rollback owner, baseline/candidate/excluded cohort 정의다.
  - 추가 기준: current matrix entry가 대부분 `no_clear_edge`이면 plumbing 구현과 live enable 판정을 분리한다. loader/flag/logging 구현이 완료되어도 `COMPLETED + valid profit_rate`, `GOOD_EXIT/MISSED_UPSIDE`, soft stop tail, holding defer cost 기준이 닫히지 않으면 live enable은 불허한다.
  - 금지선: matrix를 score override나 hard veto bypass 용도로 쓰지 않는다. holding/exit same-stage 다축 live 변경, self-updating matrix, bot restart 자동 연동은 금지한다.
  - 실행 메모 (`2026-05-07 KST`): `holding_exit_matrix_runtime.py`를 추가해 holding prompt/exit alias 전용 runtime loader를 구현했다. `ai_engine.py`, `ai_engine_openai.py`, `ai_engine_deepseek.py`는 `prompt_profile=holding|exit` 경로에서만 matrix provenance를 로드하고, `HOLDING_EXIT_MATRIX_ADVISORY_ENABLED` flag가 켜질 때만 prompt context를 붙인다. cache key는 `:adm:{cohort}:{matrix_version}:{price_bucket}:{volume_bucket}:{time_bucket}` suffix로 분리했고, result/log에는 `holding_exit_matrix_status/cohort/version/source_date/buckets/decision_alignment`를 남긴다.
  - 판정: readiness plumbing 구현은 완료다. baseline/candidate/excluded cohort도 정의했다. baseline은 `holding prompt` + flag OFF, candidate는 `holding prompt` + advisory flag ON, excluded는 non-holding/shared prompt 및 현 owner 범위 밖 surface다. 다만 5/7 matrix는 `recommended_bias=no_clear_edge` 비중이 높고 `holding_flow_override`는 현 owner 유지가 우선이라 same-day advisory/live enable은 보류한다.
  - 근거: `holding_exit_decision_matrix_2026-05-07.json`은 14개 entry 대부분이 `no_clear_edge`이며 `defensive_only_high_loss_rate`/`candidate_weight_source`가 다수다. 따라서 plumbing은 닫혀도 runtime AI 응답 변경까지 바로 올릴 edge가 아니다. provider parity는 관련 pytest가 통과했고 flag 기본값은 OFF다.
  - 다음 액션: next matrix에서 directional edge가 생기기 전까지 `HOLDING_EXIT_MATRIX_ADVISORY_ENABLED=False`를 유지한다. live enable 재판정은 다음 장후 matrix와 `GOOD_EXIT/MISSED_UPSIDE/holding defer cost`가 닫힐 때만 별도 checklist로 연다.

- [x] `[PrecloseSellTargetAIRecovery0508] preclose sell target AI/Telegram acceptance 재검증` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 18:05~18:20`, `Track: Plan`)
  - Source: [preclose-sell-target-revival-plan.md](/home/ubuntu/KORStockScan/docs/proposals/preclose-sell-target-revival-plan.md), [2026-05-07-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-05-07-stage2-todo-checklist.md), [preclose_sell_target_report.py](/home/ubuntu/KORStockScan/src/scanners/preclose_sell_target_report.py)
  - 판정 기준: 5/7 Gemini 503 `UNAVAILABLE` 후 별도 승인 시 AI 호출, 응답 JSON contract, schema parse, Telegram publish 대상을 각각 분리 검증한다. dry-run과 `--no-telegram`으로 실제 전송을 먼저 차단한다.
  - 금지선: AI/Telegram acceptance는 자동 주문, threshold mutation, bot restart와 연결하지 않는다.
  - 선반영: 2026-05-07 장후 Gemini key fallback을 구현했고 key2로 AI JSON schema parse와 report-only artifact 생성이 통과했다. 같은 날 사용자 승인으로 Telegram 실제 전송도 실행했다.

- [x] `[PrecloseSellTargetCronWrapper0508] preclose sell target cron wrapper lock/status/holiday skip 판정` (`Due: 2026-05-08`, `Slot: POSTCLOSE`, `TimeWindow: 18:20~18:35`, `Track: RuntimeStability`)
  - Source: [data/report/README.md](/home/ubuntu/KORStockScan/data/report/README.md), [report-based-automation-traceability.md](/home/ubuntu/KORStockScan/docs/report-based-automation-traceability.md), [run_preclose_sell_target_report.sh](/home/ubuntu/KORStockScan/deploy/run_preclose_sell_target_report.sh)
  - 판정 기준: cron 등록이 아니라 wrapper에 lock, status manifest, log path, failure alert, holiday skip, retry/cooldown이 필요한지 확인한다. report-only profile은 `--no-ai --no-telegram`을 기본으로 본다.
  - 금지선: cron 등록은 산출물 생성 정기화일 뿐 live threshold mutation, bot restart, 자동 주문 제출이 아니다.
  - 선반영: 2026-05-07 장후 wrapper에 lock/status manifest/log path/venv guard/weekend guard를 추가했고, 같은 날 사용자 승인으로 `15:00` cron 등록까지 반영했다. 다만 consumer 연결은 별도 owner 전까지 열지 않는다.

<!-- AUTO_SERVER_COMPARISON_START -->
### 본서버 vs songstockscan 자동 비교 (`2026-05-08 15:47:32`)

- 기준: `profit-derived metrics are excluded by default because fallback-normalized values such as NULL -> 0 can distort comparison`
- 상세 리포트: `data/report/server_comparison/server_comparison_2026-05-08.md`
- `Trade Review`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Performance Tuning`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Post Sell Feedback`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Entry Pipeline Flow`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
<!-- AUTO_SERVER_COMPARISON_END -->
