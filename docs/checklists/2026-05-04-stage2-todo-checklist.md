# 2026-05-04 Stage2 To-Do Checklist

## 오늘 목적

- `ai_engine_openai.py`가 `ai_engine.py`와 같은 endpoint schema registry/contract 기준을 따르는지 장전 로드로 닫는다.
- `OpenAI Responses WS`는 live 전환이 아니라 `shadow-first flag-off` 기준으로 queue/timeout/fallback/request_id 정합성만 관찰한다.
- phase1 WS 범위는 `analyze_target`, `analyze_target_shadow_prompt`로만 잠그고 `condition_entry/condition_exit`는 전용 프롬프트/endpoint 없이 기존 scalping 라우팅으로 위임한다. `realtime_report/gatekeeper/overnight/EOD`는 HTTP 유지로 분리한다.
- BUY-side timeout/parse failure/late response는 `DROP/SKIP` 보수 폴백으로만 처리하고, `previous_response_id`는 종목 간 상태 오염 방지 차원에서 금지한다.
- `soft_stop_expert_defense v2`는 `2026-04-30` same-day 수집 후 기본 OFF다. 다음 보유/청산 신규 owner는 v2 재가동이 아니라 `GOOD_EXIT` 제거를 피하는 refined `bad_entry` canary다.
- 스캘핑 신규 BUY 최대매수가능 주수는 `1주 cap`이다. 5/4 장전에는 `SCALPING_INITIAL_ENTRY_MAX_QTY=1` 로드와 env override 오염 여부를 확인한다.
- Dynamic 진입가 P1/P2는 운영 canary다. `target_buy_price`는 참고 기준가로만 쓰고, P1 `strategy-aware resolver`가 기본 제출가와 미체결 timeout을 결정한다. P2는 submitted 직전 `AI Tier2 entry_price canary`가 `USE_DEFENSIVE | USE_REFERENCE | IMPROVE_LIMIT | SKIP` 중 하나를 선택한다. 운영 규칙상 entry price shadow는 금지한다.
- 물타기/불타기 `MAX_*_COUNT`는 더 이상 runtime blocker가 아니라 attribution counter다. generic `AVG_DOWN` 평가는 제거했고, scalping `AVG_DOWN` add_type은 `REVERSAL_ADD` 귀속명으로만 남긴다. 반복 추가매수 리스크는 `REVERSAL_ADD`/`PYRAMID` enable, fill cooldown, cancel cooldown, pending order, position cap, protection 재설정, near-close gate로 제한한다.
- `stat_action_decision_snapshot`은 observe-only다. live 판단 변경 없이 HOLDING 의사결정 순간의 후보/선택/차단 행동을 compact stream에 남긴다.
- `holding_flow_override`는 기존 튜닝 관찰축과 별개인 운영 override다. 보유/청산 및 `15:20 KST` 오버나이트 `SELL_TODAY` 재검문에 적용하되, hard stop/protect hard stop/주문·잔고 안전장치는 즉시 실행을 유지한다.
- threshold cycle은 `07:35 PREOPEN apply manifest`, `16:10 POSTCLOSE collector/report`가 자동 실행된다. 5/6 운영전환 acceptance 전까지 live runtime mutation은 `manifest_only`다.

## 오늘 강제 규칙

- 기준선은 `main-only`, `normal_only`, `post_fallback_deprecation`이며 상세 기준은 `Plan Rebase` §1~§6을 따른다.
- live 변경은 동일 단계 내 `1축 canary`만 허용한다. 진입병목축과 보유/청산축은 별개 단계이므로 병렬 canary가 가능하지만, 같은 단계 안에서는 canary 중복을 금지한다.
- 동일 단계 replacement는 `기존 축 OFF -> restart.flag -> 새 축 ON` 순서만 쓴다.
- 관찰창이 끝나면 `즉시 판정 -> 다음 축 즉시 착수`를 기본으로 한다. 이미 수집된 데이터로 닫을 수 있는 판정은 장후/익일로 미루지 않는다.
- `장후/익일/다음 장전` 이관은 예외사유 4종(`단일 조작점 미정`, `rollback guard 미문서화`, `restart/code-load 불가`, `운영 경계상 same-day 반영 불가`) 중 하나로만 허용한다. 막힌 조건과 다음 절대시각이 없으면 이관 판정은 무효다.
- PREOPEN은 전일에 이미 `단일 조작점 + rollback guard + 코드/테스트 + restart 절차`가 준비된 carry-over 축만 받는다.
- 일정은 모두 `YYYY-MM-DD KST`, `Slot`, `TimeWindow`로 고정한다.
- 손익은 `COMPLETED + valid profit_rate`만 사용하고 `full fill`과 `partial fill`은 분리한다.
- live 승인, replacement, stage-disjoint 예외, 관찰 개시 판정에는 `cohort`를 같이 잠근다. 최소 `baseline cohort`, `candidate live cohort`, `observe-only cohort`, `excluded cohort`를 구분하고 `partial/full`, `initial/pyramid`, `fallback` 혼합 결론을 금지한다.
- `ApplyTarget`은 문서에 명시된 값만 사용하고, parser/workorder가 `remote`를 추정하지 않도록 유지한다.
- 다축 동시 변경 금지, 승인 전 `main` 실주문 변경 금지 규칙을 유지한다.
- `openai_responses_ws_shadow_flag_off`는 `observe-only`다. `request_id mismatch`, `late discard`, `http fallback`, `timeout reject`는 shadow 판정 근거로만 쓰고 실주문 go/no-go에는 직접 사용하지 않는다.
- `bad_entry` 신규 canary는 naive block 금지다. `2026-04-30` 장후에 코드/테스트는 준비했고, 5/4 장전에는 런타임 로드와 cohort tag만 확인한다.
- `holding_flow_override`는 단일 score cutoff로 판정하지 않는다. 최근 tick 30개, 분봉 60개, flow review history 기반으로 `flow_state/evidence/action`을 보고, 최초 후보 대비 추가악화 `0.80%p`, AI/parse/stale/context 실패, 누적 보류 `90초` 초과 시 기존 청산을 허용한다.
- `dynamic_entry_price_resolver_p1`은 `target_buy_price>0`을 예약주문으로 해석하지 않는다. 일반 스캘핑 timeout은 `90초`, `BREAKOUT 120초`, `PULLBACK 600초`, `RESERVE 1200초`로 분리하고, 기준가가 best bid 대비 `80bp` 초과 하향이면 방어가 제출을 유지한다. `dynamic_entry_ai_price_canary_p2`는 AI 실패/저신뢰/guard 위반 시 P1 가격으로 fail-closed한다.
- threshold 자동화는 장전 manifest와 장후 report 생성까지만 허용한다. `ThresholdOpsTransition0506` 전에는 자동 threshold live 적용이나 봇 재기동 정책 변경을 열지 않는다.

## 장전 체크리스트 (08:15~08:55)

- [x] `[RuntimeFlagInventory0504-Preopen] ON/OFF runtime flag inventory 및 OFF축 ON 기준 로드 확인` (`Due: 2026-05-04`, `Slot: PREOPEN`, `TimeWindow: 08:15~08:20`, `Track: RuntimeStability`)
  - Source: [workorder-shadow-canary-runtime-classification.md](/home/ubuntu/KORStockScan/docs/workorder-shadow-canary-runtime-classification.md), [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md)
  - 판정 기준: `2026-05-01` 기준 runtime ON/OFF 스냅샷과 실제 `TradingConfig`/env override를 비교한다. 실주문 영향 ON, observe/report-only ON, guarded-off/OFF를 구분하고, OFF 축은 owner/cohort/rollback/restart 기준 없이 장전 임의 ON 금지로 잠근다.
  - 현재 실주문 영향 ON: `mechanical_momentum_latency_relief`, `soft_stop_micro_grace`, `REVERSAL_ADD`, `bad_entry_refined_canary`, `initial_entry_qty_cap_1share`, `pre_submit_price_guard`, `dynamic_entry_price_resolver_p1`, `dynamic_entry_ai_price_canary_p2`, `partial_fill_ratio_guard`, `dynamic_vpw`, `dynamic_strength_relief`, `SCALPING_ENABLE_PYRAMID`, `SCALPING_PYRAMID_ZERO_QTY_STAGE1`.
  - 현재 observe/report-only ON: `stat_action_decision_snapshot`, `statistical_action_weight`, `holding_exit_decision_matrix`, `threshold_cycle manifest/report`, `SCALP_LOSS_FALLBACK observe-only`.
  - 현재 OFF/guarded-off: `soft_stop_expert_defense v2`, `soft_stop_micro_grace_extend`, `latency_quote_fresh_composite`, `latency_signal_quality_quote_composite`, `latency_spread_relief`, `latency_ws_jitter_relief`, `latency_other_danger_relief`, `latency_guard_canary`, `latency_fallback/split_entry`, `hard_time_stop_shadow`, `same_symbol_soft_stop_cooldown_shadow`, `partial_only_timeout_shadow`, `OpenAI Responses WS`, `OpenAI dual persona`, `OpenAI schema registry/deterministic config`. generic `AVG_DOWN`는 OFF가 아니라 runtime removed다.
  - `2026-05-02` live 보정 및 `2026-05-04` 과다호출 완화 로드 확인: prompt별 model tier routing은 `watching/holding/shared=Tier1`, `entry_price/holding_flow/overnight/exit/gatekeeper=Tier2`, `EOD=Tier3`이며, OpenAI 기본값은 `GPT_FAST_MODEL=gpt-5.4-nano`, `GPT_REPORT_MODEL=gpt-5.4-mini`, `GPT_DEEP_MODEL=gpt-5.4`다. 호출 interval은 `AI_WATCHING_COOLDOWN=90`, `AI_HOLDING_MIN_COOLDOWN=45`, `AI_HOLDING_MAX_COOLDOWN=180`, `AI_HOLDING_CRITICAL_MIN_COOLDOWN=20`, `AI_HOLDING_CRITICAL_COOLDOWN=45` 로드 여부를 확인한다.
  - 실행 메모 (`2026-05-04 07:50:38 KST`): `bot_main.py` PID `3257`가 `2026-05-04 07:40:01 KST`부터 실행 중임을 확인했다. `PYTHONPATH=. .venv/bin/python`으로 로드한 `TRADING_RULES`와 `/proc/3257/environ` 기준 `KORSTOCKSCAN_*`/`OPENAI_*`/`GPT_*` 런타임 override는 없었고, ON 축(`mechanical_momentum_latency_relief`, `soft_stop_micro_grace`, `REVERSAL_ADD`, `bad_entry_refined_canary`, `initial_entry_qty_cap_1share`, `pre_submit_price_guard`, `dynamic_entry_price_resolver_p1`, `dynamic_entry_ai_price_canary_p2`, `partial_fill_ratio_guard`, `dynamic_vpw`, `dynamic_strength_relief`, `SCALPING_ENABLE_PYRAMID`)과 OFF 축(`soft_stop_expert_defense v2`, `OpenAI Responses WS`, `OpenAI schema registry/deterministic config`)이 workorder 스냅샷과 일치했다. `SCALPING_PYRAMID_ZERO_QTY_STAGE1`은 이후 `2026-05-04 10:05 KST` 사용자 지시로 ON 전환했다.
  - 실행 메모 (`2026-05-04 KST`): 사용자 지시에 따라 원인귀속 여부와 무관하게 AI 호출 주기를 상향했다. [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py)는 `AI_WATCHING_COOLDOWN 45->90`, `AI_HOLDING_MIN/MAX 20/90->45/180`, `AI_HOLDING_CRITICAL_MIN/MAX 8/20->20/45`로 조정했고, `KORSTOCKSCAN_AI_*` env override를 추가했다. entry price canary와 provider routing은 변경하지 않았다.
  - 런타임 반영 (`2026-05-04 13:10 KST`): `restart.flag` 우아한 재시작을 수행했고 main `bot_main.py` PID가 `24069 -> 43934`로 교체됐다. `restart.flag`는 소모됐고 코드 기본값 import check는 `90/45/180/20/45`로 확인했다.
  - 다음 액션: OFF 축을 켜야 한다면 이 항목에서 바로 ON하지 않고, 해당 축별 ON 시점/기준을 [workorder-shadow-canary-runtime-classification.md](/home/ubuntu/KORStockScan/docs/workorder-shadow-canary-runtime-classification.md)의 `0.1 Runtime ON/OFF 스냅샷`과 대조해 단일축 checklist 항목으로 분리한다.

- [x] `[StatActionDecisionSnapshot0504-Preopen] 행동 후보 decision snapshot observe-only 로드 확인` (`Due: 2026-05-04`, `Slot: PREOPEN`, `TimeWindow: 08:20~08:25`, `Track: ScalpingLogic`)
  - Source: [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [threshold_cycle_registry.py](/home/ubuntu/KORStockScan/src/utils/threshold_cycle_registry.py), [2026-04-30-data-driven-threshold-inventory.md](/home/ubuntu/KORStockScan/docs/audit-reports/2026-04-30-data-driven-threshold-inventory.md)
  - 판정 기준: `STAT_ACTION_DECISION_SNAPSHOT_ENABLED=True`, `STAT_ACTION_DECISION_SNAPSHOT_MIN_INTERVAL_SEC=30` 기본 로드와 env override 오염 여부를 확인한다. stage `stat_action_decision_snapshot`은 family `statistical_action_weight`로 compact 적재되어야 한다.
  - why: 통계 행동가중치는 실제 선택 행동만으로는 selection bias가 커진다. HOLDING 판단 순간의 `eligible_actions`, `rejected_actions`, `chosen_action`, 수익률/고점/AI/수급/호가 상태를 같이 남겨야 `exit_now`, `avg_down_wait`, `pyramid_wait`, `hold_wait`의 counterfactual 근거가 생긴다.
  - 실행 메모 (`2026-05-04 07:50:38 KST`): `TRADING_RULES.STAT_ACTION_DECISION_SNAPSHOT_ENABLED=True`, `STAT_ACTION_DECISION_SNAPSHOT_MIN_INTERVAL_SEC=30`을 확인했다. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)에서 `_emit_stat_action_decision_snapshot()`가 `stat_action_decision_snapshot` stage를 적재하고 `8 passed` targeted pytest에 snapshot observe-only 경로가 포함됐다.
  - 다음 액션: 장중에는 live 행동 변경 없이 snapshot 적재 여부와 IO 증가만 확인한다. 과다 적재가 보이면 env로 interval을 늘리거나 snapshot을 OFF한다.

- [x] `[ScaleInCountGateRemoval0504-Preopen] 물타기/불타기 count gate 제거 로드 확인` (`Due: 2026-05-04`, `Slot: PREOPEN`, `TimeWindow: 08:25~08:30`, `Track: ScalpingLogic`)
  - Source: [sniper_scale_in.py](/home/ubuntu/KORStockScan/src/engine/sniper_scale_in.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md)
  - 판정 기준: `avg_down_count`/`pyramid_count`가 주문 가능 여부를 막지 않고 집계/귀속값으로만 남는지 확인한다. generic `AVG_DOWN` 평가 경로가 runtime에서 제거됐는지, scalping `AVG_DOWN` add_type이 `REVERSAL_ADD` 귀속명으로만 남는지, `SCALPING_ENABLE_PYRAMID`/`REVERSAL_ADD_ENABLED`가 실제 enable owner인지 확인한다.
  - why: `REVERSAL_ADD`와 `PYRAMID` 실행 표본이 count cap에 막히면 데이터 기반 threshold/weight 산정이 공회전한다. 다만 반복 주문 리스크는 count가 아니라 fill cooldown, cancel cooldown, pending order, position cap, protection 재설정 실패 fail-closed로 제한한다.
  - 실행 메모 (`2026-05-04 07:50:38 KST`): [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py)에서 `SCALPING_MAX_AVG_DOWN_COUNT=0`, `SCALPING_MAX_PYRAMID_COUNT=0`, `SWING_MAX_*_COUNT=0`이 모두 attribution counter로 고정돼 있고, [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py) `can_consider_scale_in()`은 count 대신 `REVERSAL_ADD_ENABLED`/`SCALPING_ENABLE_PYRAMID`와 `fill cooldown/cancel cooldown/pending/position cap/near-close` 게이트만 사용한다. generic `AVG_DOWN` 평가는 runtime removed 상태다.
  - 추가 실행 메모 (`2026-05-04 09:02 KST`): `456040`에서 `PYRAMID` 추가매수 접수 후 미체결 취소가 반복되는 런타임 루프를 확인했다. 원인은 pending add timeout 취소 후 pending 메타만 해제되고 재접수 쿨다운이 없어 같은 `scalping_pyramid_ok` 신호가 바로 재통과한 것이다. [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py)에 `SCALE_IN_CANCEL_COOLDOWN_SEC=120`을 추가하고, [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)에서 취소 성공 시 `last_add_cancel_at`을 기록해 `scale_in_cancel_cooldown`으로 재접수를 차단한다.
  - 다음 액션: 로드 확인 후 후보가 계속 0이면 count나 legacy avg-down이 아니라 `pnl/hold/supply/qty/position_cap/cooldown/pending/protection` blocker로 분해한다.

- [x] `[BadEntryRefinedCanary0504-Preopen] refined bad_entry canary 설계/로드 승인` (`Due: 2026-05-04`, `Slot: PREOPEN`, `TimeWindow: 08:30~08:40`, `Track: ScalpingLogic`)
  - Source: [2026-04-30-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-04-30-stage2-todo-checklist.md), [personal-decision-flow-notes.md](/home/ubuntu/KORStockScan/docs/personal/personal-decision-flow-notes.md), [workorder-shadow-canary-runtime-classification.md](/home/ubuntu/KORStockScan/docs/workorder-shadow-canary-runtime-classification.md)
  - 판정 기준: `soft_stop_expert_defense v2` 기본 OFF를 확인하고, `bad_entry_block_observed` 2026-04-30 최종 outcome(`unique 32`, 후행 sell completed `30`, 평균 `-0.961%`, 손실 `22/30`, soft stop `20/30`, GOOD_EXIT `13`)을 기준으로 naive block이 아니라 refined canary 조건을 잠근다.
  - 구현 상태 (`2026-04-30 장후`): `src/utils/constants.py` 기본값으로 `SCALP_BAD_ENTRY_REFINED_CANARY_ENABLED=True`, `held_sec>=180`, `profit_rate<=-1.16`, `peak_profit<=+0.05`, `AI<=45`, `recovery_prob_shadow<=0.30`을 반영했다. `src/engine/sniper_state_handlers.py`는 `scalp_bad_entry_refined_canary`를 `soft_stop` 전 조기정리 rule로 적용하고, `bad_entry_refined_candidate`/`bad_entry_refined_exit` stage를 남긴다.
  - refined canary 확정조건: `held_sec>=180`, `profit_rate<=-1.16`, `peak_profit<=+0.05`, `AI<=45`를 기본 anchor로 두고, `recovery_prob_shadow<=0.30` 또는 thesis invalidation/adverse fill 동반 시에만 live 후보로 본다. `current_ai_score` 회복, positive peak 확대, `REVERSAL_ADD/POST_ADD_EVAL`, active sell pending, emergency/hard stop, 기존 soft stop zone은 제외한다.
  - why: v2는 absorption 유예 성공 표본이 없어 지속하지 않는다. 그러나 bad-entry 후보군은 비후보보다 손익과 soft stop 전환이 나빠 EV 개선 후보가 맞으므로, `GOOD_EXIT` 제거를 피하는 더 좁은 단일축 canary가 시급하다.
  - rollback guard: canary 적용 cohort의 `COMPLETED + valid profit_rate` 평균이 비적용 후보보다 `-0.20%p` 이상 악화, `GOOD_EXIT/MISSED_UPSIDE` would-have-been 증가, `sell_order_failed`, `reversal_add_used` 혼입 1건 이상이면 즉시 OFF한다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_sniper_scale_in.py -k 'bad_entry_refined or bad_entry_block_observe or soft_stop_micro_grace'` -> `6 passed`
  - 실행 메모 (`2026-05-04 07:50:38 KST`): 런타임 값 `SCALP_SOFT_STOP_EXPERT_DEFENSE_ENABLED=False`, `SCALP_BAD_ENTRY_REFINED_CANARY_ENABLED=True`를 확인했다. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)에는 `bad_entry_refined_candidate`/`bad_entry_refined_exit` stage가 남아 있고, targeted pytest 확장 검증(`bad_entry_refined`, `bad_entry_block_observe`, `soft_stop_micro_grace`, `entry_price`, `stat_action`)은 `8 passed`로 통과했다.
  - 다음 액션: 장전에는 코드 구현이 아니라 `TradingConfig`/env override 로드, `soft_stop_expert_defense=False`, `bad_entry_refined=True`, cohort tag/event stage 적재 여부만 확인한다. 미승인 시 observe-only 유지가 아니라 즉시 threshold/refined rule을 다시 좁힌다.

- [x] `[OpenAIParity0504-Preopen] OpenAI schema registry/transport flag 로드 확인` (`Due: 2026-05-04`, `Slot: PREOPEN`, `TimeWindow: 08:40~08:50`, `Track: ScalpingLogic`)
  - Source: [2026-04-30-openai-enable-acceptance-spec.md](/home/ubuntu/KORStockScan/docs/ai-acceptance/2026-04-30-openai-enable-acceptance-spec.md), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [workorder-shadow-canary-runtime-classification.md](/home/ubuntu/KORStockScan/docs/workorder-shadow-canary-runtime-classification.md)
  - 판정 기준: `OPENAI_RESPONSE_SCHEMA_REGISTRY_ENABLED`, `OPENAI_JSON_DETERMINISTIC_CONFIG_ENABLED`, `OPENAI_TRANSPORT_MODE`, `OPENAI_RESPONSES_WS_ENABLED`, `OPENAI_PREVIOUS_RESPONSE_ID_ENABLED=False`가 코드/런타임 provenance로 확인되고, endpoint별 schema 매핑(`entry/holding_exit/overnight/condition/eod`)이 테스트 기준과 일치한다.
  - why: OpenAI parity는 live alpha 확장이 아니라 계약 정합성과 transport provenance 잠금이 먼저다.
  - cohort: `baseline cohort=OpenAI HTTP live contract`, `candidate live cohort=none`, `observe-only cohort=openai_responses_ws_shadow_flag_off`, `excluded cohort=realtime_report/gatekeeper/overnight/EOD text path`, `rollback owner=OPENAI_TRANSPORT_MODE`, `cross-contamination check=entry transport 결과를 gatekeeper/eod 판정에 합산 금지`
  - 실행 메모 (`2026-05-04 07:50:38 KST`): 런타임 값은 `OPENAI_RESPONSE_SCHEMA_REGISTRY_ENABLED=False`, `OPENAI_JSON_DETERMINISTIC_CONFIG_ENABLED=False`, `OPENAI_TRANSPORT_MODE=http`, `OPENAI_RESPONSES_WS_ENABLED=False`, `OPENAI_PREVIOUS_RESPONSE_ID_ENABLED=False`로 flag-off 기준과 일치했다. [ai_engine_openai.py](/home/ubuntu/KORStockScan/src/engine/ai_engine_openai.py)는 schema registry와 WS 사용을 모두 해당 flag로 게이팅하고, `test_ai_engine_openai_transport.py`는 `8 passed`로 통과했다.
  - 다음 액션: 로드 확인 후 장중에는 WS enable이 아니라 shadow-only queue/timeout/fallback 관찰로 이어가고, `JSON deterministic config`/`schema registry`는 계속 flag-off acceptance로만 유지한다.

- [x] `[HoldingFlowOverride0504-Preopen] holding/overnight flow override 장전 로드 확인` (`Due: 2026-05-04`, `Slot: PREOPEN`, `TimeWindow: 08:40~09:00`, `Track: ScalpingLogic`)
  - Source: [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [ai_engine.py](/home/ubuntu/KORStockScan/src/engine/ai_engine.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [sniper_overnight_gatekeeper.py](/home/ubuntu/KORStockScan/src/engine/sniper_overnight_gatekeeper.py), [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py)
  - 판정 기준: `HOLDING_FLOW_OVERRIDE_ENABLED=True`, `HOLDING_FLOW_OVERRIDE_WORSEN_PCT=0.80`, `HOLDING_FLOW_OVERRIDE_MAX_DEFER_SEC=90`, `HOLDING_FLOW_REVIEW_TICK_LIMIT=30`, `HOLDING_FLOW_REVIEW_CANDLE_LIMIT=60`, `SCALPING_OVERNIGHT_DECISION_TIME=15:20:00` 로드와 env override 오염 여부를 확인한다.
  - why: 보유/청산 AI가 진입 직후 단일 순간 점수로 너무 조급하게 전량청산하는 문제를 운영 override로 막고, flow가 깨지는 경우에만 기존 청산을 통과시켜 기대값을 높인다.
  - rollback guard: hard stop/protect/order safety 우회 1건, AI/parse 실패 시 보류 1건, 추가악화 `0.80%p` 미작동 1건, 오버나이트 `15:20` 미실행 1건이면 즉시 OFF 또는 이전 코드 경로 복귀한다.
  - 실행 메모 (`2026-05-04 07:50:38 KST`): 런타임 값 `HOLDING_FLOW_OVERRIDE_ENABLED=True`, `HOLDING_FLOW_OVERRIDE_WORSEN_PCT=0.8`, `HOLDING_FLOW_OVERRIDE_MAX_DEFER_SEC=90`, `HOLDING_FLOW_REVIEW_TICK_LIMIT=30`, `HOLDING_FLOW_REVIEW_CANDLE_LIMIT=60`, `SCALPING_OVERNIGHT_DECISION_TIME=15:20:00`를 확인했다. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)와 [sniper_overnight_gatekeeper.py](/home/ubuntu/KORStockScan/src/engine/sniper_overnight_gatekeeper.py)에 `holding_flow_override_*`, `overnight_flow_override_*` stage가 구현돼 있다.
  - 다음 액션: 장중에는 `holding_flow_override_review`, `holding_flow_override_defer_exit`, `holding_flow_override_exit_confirmed`, `holding_flow_override_force_exit`, 오버나이트는 `overnight_flow_override_hold/revert_sell_today` stage 적재를 확인한다.

- [x] `[DynamicEntryResolverP10504-Preopen] dynamic entry price resolver P1 장전 로드 확인` (`Due: 2026-05-04`, `Slot: PREOPEN`, `TimeWindow: 08:50~09:00`, `Track: ScalpingLogic`)
  - Source: [2026-04-29-daehan-cable-entry-price-audit-followup.md](/home/ubuntu/KORStockScan/docs/audit-reports/2026-04-29-daehan-cable-entry-price-audit-followup.md), [sniper_entry_latency.py](/home/ubuntu/KORStockScan/src/engine/sniper_entry_latency.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py)
  - 판정 기준: `SCALPING_ENTRY_PRICE_RESOLVER_ENABLED=True`, `SCALPING_ENTRY_PRICE_RESOLVER_MAX_BELOW_BID_BPS=80`, `SCALPING_ENTRY_TIMEOUT_SEC=90`, `SCALPING_BREAKOUT_ENTRY_TIMEOUT_SEC=120`, `SCALPING_PULLBACK_ENTRY_TIMEOUT_SEC=600`, `SCALPING_RESERVE_ENTRY_TIMEOUT_SEC=1200` 기본 로드와 env override 오염 여부를 확인한다.
  - why: `target_buy_price`가 낮다는 이유만으로 실주문가를 비체결권으로 끌어내리거나, `target_buy_price>0` 때문에 일반 스캘핑 미체결 주문을 20분 방치하는 문제를 막는다.
  - rollback guard: `pre_submit_price_guard` 우회 1건, 기준가 괴리 `80bp` 초과인데 `reference_target_cap` 적용 1건, 일반 스캘핑 timeout이 `90초`를 초과해 미체결 방치 1건이면 `KORSTOCKSCAN_SCALPING_ENTRY_PRICE_RESOLVER_ENABLED=false`로 즉시 OFF한다.
  - 실행 메모 (`2026-05-04 07:50:38 KST`): 런타임 값 `SCALPING_ENTRY_PRICE_RESOLVER_ENABLED=True`, `SCALPING_ENTRY_PRICE_RESOLVER_MAX_BELOW_BID_BPS=80`, `SCALPING_ENTRY_TIMEOUT_SEC=90`, `SCALPING_BREAKOUT_ENTRY_TIMEOUT_SEC=120`, `SCALPING_PULLBACK_ENTRY_TIMEOUT_SEC=600`, `SCALPING_RESERVE_ENTRY_TIMEOUT_SEC=1200`을 확인했다. [sniper_entry_latency.py](/home/ubuntu/KORStockScan/src/engine/sniper_entry_latency.py)에는 `reference_target_rejected_reason`, `target_below_bid_bps_exceeded`, `reference_target_cap` 경로가 남아 있어 rollback guard 관측 필드가 확보돼 있다.
  - 다음 액션: 장중에는 `resolution_reason`, `reference_target_applied`, `reference_target_rejected_reason`, `submitted_order_price`, `defensive_order_price`, `price_below_bid_bps` 분포를 확인한다.

- [x] `[DynamicEntryAIPriceCanary0504-Preopen] AI Tier2 entry price canary 설계/로드 승인` (`Due: 2026-05-04`, `Slot: PREOPEN`, `TimeWindow: 08:55~09:00`, `Track: ScalpingLogic`)
  - Source: [2026-04-29-daehan-cable-entry-price-audit-followup.md](/home/ubuntu/KORStockScan/docs/audit-reports/2026-04-29-daehan-cable-entry-price-audit-followup.md), [ai_engine.py](/home/ubuntu/KORStockScan/src/engine/ai_engine.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [sniper_entry_latency.py](/home/ubuntu/KORStockScan/src/engine/sniper_entry_latency.py)
  - 판정 기준: `SCALPING_ENTRY_AI_PRICE_CANARY_ENABLED=True`, `SCALPING_ENTRY_AI_PRICE_MIN_CONFIDENCE=60`, `SCALPING_ENTRY_AI_PRICE_SKIP_MIN_CONFIDENCE=80`, `entry_price_v1` schema 로드와 Gemini/OpenAI/DeepSeek `evaluate_scalping_entry_price` method 존재를 확인한다. submitted 신호 이후, 실제 주문 전 `AI Tier2`가 reference target/defensive price/live quote/latency/체결강도/호가 depth를 보고 `USE_DEFENSIVE | USE_REFERENCE | IMPROVE_LIMIT | SKIP` 중 하나와 `order_price`를 반환한다. shadow/observe-only 적용은 금지한다.
  - rollback guard: AI timeout/parse fail/stale context는 P1 resolver로 fail-closed, `pre_submit_price_guard` 우회 1건, AI 가격이 best bid 대비 허용 괴리 초과 1건, 일반 스캘핑 미체결 방치 1건이면 즉시 OFF한다.
  - 실행 메모 (`2026-05-04 07:50:38 KST`): 런타임 값 `SCALPING_ENTRY_AI_PRICE_CANARY_ENABLED=True`, `SCALPING_ENTRY_AI_PRICE_MIN_CONFIDENCE=60`, `SCALPING_ENTRY_AI_PRICE_SKIP_MIN_CONFIDENCE=80`, `SCALPING_ENTRY_PRICE_ORDERBOOK_MICRO_ENABLED=True`를 확인했다. [ai_engine.py](/home/ubuntu/KORStockScan/src/engine/ai_engine.py), [ai_engine_openai.py](/home/ubuntu/KORStockScan/src/engine/ai_engine_openai.py), [ai_engine_deepseek.py](/home/ubuntu/KORStockScan/src/engine/ai_engine_deepseek.py)에 `evaluate_scalping_entry_price()`와 `entry_price_v1` schema 경로가 모두 존재하며, 관련 targeted pytest와 OpenAI transport pytest는 각각 `8 passed`, `8 passed`로 통과했다.

## 장중 체크리스트 (10:00~10:20)

- [x] `[ProtectTrailingSmoothing0504-Intraday] PYRAMID 이후 보호트레일링 단일 tick 민감도 평탄화` (`Due: 2026-05-04`, `Slot: INTRADAY`, `TimeWindow: 09:05~09:20`, `Track: ScalpingLogic`)
  - Source: [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [sniper_execution_receipts.py](/home/ubuntu/KORStockScan/src/engine/sniper_execution_receipts.py), [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py), [pipeline_events_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-04.jsonl), [post_sell_candidates_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/post_sell/post_sell_candidates_2026-05-04.jsonl)
  - 판정 기준: `protect_trailing_stop`은 단일 현재가 tick 이탈만으로 즉시 청산하지 않고, 최근 가격 샘플의 중앙값/이탈비율/관측기간으로 평탄화된 이탈을 확인한다. 단, emergency 손실폭과 `protect_hard_stop`은 평탄화 대상이 아니다.
  - 근거: `리노공업(058470)`은 `09:00:32` PYRAMID 체결 후 보호선 `119,825원` 아래 단일 tick 이탈로 `09:03:53` 청산됐지만 이후 `118,800원`까지 회복했다. 기존 `pyramid_post_add_trailing_grace`는 판단을 미룰 뿐 민감도를 낮추거나 가격 노이즈를 평탄화하지 못한다.
  - 실행 메모 (`2026-05-04 09:20 KST`): [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py)에 `SCALP_PROTECT_TRAILING_SMOOTH_*` 계열 기본값을 추가했다. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 `holding_price_samples`를 rolling window로 유지하고, 보호트레일링 후보는 `protect_trailing_smooth_hold` 또는 확정 exit로 분기한다. `SCALP_PYRAMID_POST_ADD_TRAILING_GRACE_SEC`는 일반 `scalp_trailing_take_profit` 억제에는 남기되, 보호트레일링 민감도 완화의 주수단으로 보지 않는다.
  - 추가 실행 메모 (`2026-05-04 09:55 KST`): 보호트레일링 평탄화 수치를 `protect_trailing_smoothing` threshold family로 편입했다. `protect_trailing_smooth_hold`/`protect_trailing_smooth_confirmed`가 compact threshold-cycle에 적재되고, `window_sec`, `min_span_sec`, `min_samples`, `below_ratio`, `buffer_pct`, `emergency_pct`는 장후 report와 다음 장전 `manifest_only` apply plan 추천값으로 산출된다. env override 키는 `KORSTOCKSCAN_SCALP_PROTECT_TRAILING_*` 계열로 열었지만, `ThresholdOpsTransition0506` 전 live runtime mutation은 계속 금지한다.
  - OCI 추가 분석 메모 (`2026-05-04 09:56 KST`): OCI(`456040`, id `4694`)는 `09:46:29` peak `+1.00%`까지 갔지만 `SCALPING_PYRAMID_MIN_PROFIT_PCT=1.5%`에 못 닿아 `PYRAMID` 실행 대상이 아니었다. `09:47:37`에는 `peak_profit=+1.00`, `profit_rate=+0.59`, `drawdown_from_peak=0.41%`, `AI=62`로 weak trailing limit `0.4%`를 막 넘겨 `scalp_trailing_take_profit`이 실행됐다. [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py)에 일반 `scalp_trailing_take_profit` family를 추가해 `weak_borderline`과 `would_hold_if_weak_limit_plus_10bp`를 장후 표본으로 분리한다.
  - 비츠로테크 추가 분석 메모 (`2026-05-04 10:03 KST`): 비츠로테크(`042370`, id `4864`)는 `09:54:22` `+2.37%`, `09:55:12` `+2.68%`, `09:55:35` `+3.21%`에서 `PYRAMID` 신호가 났지만 실제 추가매수는 `zero_qty`로 불발했다. `09:56:09`에는 `peak_profit=+3.48`, `profit_rate=+2.79`, `drawdown_from_peak=0.69%`, `AI=74`라 현행 weak limit `0.4%` 기준으로 `scalp_trailing_take_profit`이 실행됐다. 다만 AI strong 경계 `75`에 1점 모자란 사례라 strong limit `0.8%`를 적용했다면 같은 tick 청산은 보류됐을 표본으로 분리한다.
  - PYRAMID zero_qty 수정 메모 (`2026-05-04 10:05 KST`): 원인은 `buy_qty=1` cap에서 `int(1 * 0.50)=0`으로 `template_qty=0`이 되는 구조였고, 이를 보정하는 `SCALPING_PYRAMID_ZERO_QTY_STAGE1_ENABLED`가 기본 OFF였기 때문이다. 사용자 지시로 스캘핑 `PYRAMID` 1주 floor를 기본 ON으로 전환해, 예수금/position cap이 1주 이상 허용하는 경우 `buy_qty=1`에서도 `template_qty=1`, `floor_applied=True`로 주문 가능하게 했다. 이는 신규 진입 수량 cap 확대가 아니라 `PYRAMID` 신호 실행불능 제거다.
  - 검증: `PYTHONPATH=. .venv/bin/python -m pytest src/tests/test_sniper_scale_in.py -q` -> `118 passed`
  - 추가 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_daily_threshold_cycle_report.py` -> `8 passed`; `PYTHONPATH=. .venv/bin/python -m src.engine.daily_threshold_cycle_report --date 2026-05-04`로 `scalp_trailing_take_profit.sample.exit_signal=5`, `weak_borderline=1`, `would_hold_if_weak_limit_plus_10bp=1`, `would_hold_if_strong_ai_score_relaxed_5pt=2`, `pyramid_signaled_not_executed=4`, `pyramid_executed=0` 확인.
  - 다음 액션: 장후에는 `protect_trailing_smooth_hold`, `protect_trailing_stop`, `scalp_trailing_take_profit`을 `initial-only`와 `pyramid-activated`로 분리해 missed upside와 loss tail을 비교한다.

- [x] `[HoldingFlowOverride0504-Intraday] holding flow override 장중 후보/보류 health check` (`Due: 2026-05-04`, `Slot: INTRADAY`, `TimeWindow: 09:00~11:30`, `Track: ScalpingLogic`)
  - Source: [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [ai_engine.py](/home/ubuntu/KORStockScan/src/engine/ai_engine.py), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md)
  - 판정 기준: `scalp_soft_stop_pct`, `scalp_ai_momentum_decay`, `scalp_trailing_take_profit`, `scalp_bad_entry_refined_canary` 후보에서 flow `EXIT`만 전량청산으로 이어지고, `HOLD/TRIM`은 `holding_flow_override_defer_exit`로 보류되는지 확인한다. 점수 낮음만으로 EXIT가 나가지 않는지, 30~90초 review cadence와 `0.35%p` 변화 트리거가 지켜지는지도 같이 본다.
  - why: 조급한 단일 판단 대신 흐름을 보되, 보류가 무기한으로 늘어나 실현 손실을 키우지 않게 `0.80%p` 추가악화와 `90초` 상한을 동시에 검증한다.
  - 장중 분석 메모 (`2026-05-04 09:45 KST`): `SK네트웍스(001740)`와 `하나마이크론(067310)` 모두 최초 `scalp_soft_stop_pct` 후보에서 `holding_flow_override_defer_exit`가 발생한 뒤 수익률이 soft stop 위로 회복했지만, `holding_flow_override_candidate_key/started_at`이 clear되지 않았다. 이후 재차 soft stop 구간에 들어오자 `candidate_profit`이 최초 후보값(`SK -2.18%`, `하나 -2.08%`)으로 남은 상태에서 `elapsed_sec=421/663`의 `max_defer_sec` 강제청산이 발생했다. 이는 soft stop 임계값 자체보다 후보 lifecycle stale 여부를 우선 보정해야 하는 케이스다.
  - 롯데에너지머티리얼즈 재진입 분석 메모 (`2026-05-04 10:03 KST`): 롯데에너지머티리얼즈(`020150`) 1차 포지션 id `4734`는 `09:48:50` `peak_profit=+2.21`, `profit_rate=+1.57`, `drawdown_from_peak=0.64%`, `AI=63`에서 `scalp_trailing_take_profit`이 나갔고 영수증 기준 `+1.69%`로 완료됐다. 이후 id `4928` 재진입은 `09:57:23` `77,900원`에 체결됐으나 `09:59:57` `-1.25%`, `10:00:24` `-1.51%`까지 악화된 뒤 `10:01:24` `+0.67%`로 회복했다. 1차 진입가 `77,800원`을 계속 보유했다고 가정하면 재진입 직후 `77,600원/77,100원` 구간은 원포지션도 대략 `-0.26%/-0.90%` 구간으로 밀렸으므로, "익절하지 말고 계속 보유"보다 "익절은 맞고 재진입 품질을 별도 검문" 쪽으로 본다.
  - 장중 health 판정 (`2026-05-04 10:08 KST`): raw `pipeline_events_2026-05-04.jsonl` 기준 `holding_flow_override_review=9`, `holding_flow_override_defer_exit=9`, `holding_flow_override_exit_confirmed=3`, `holding_flow_override_force_exit=11`이다. force reason은 `ws_stale=8`, `max_defer_sec=3`이고, override 대상 exit_rule은 `scalp_soft_stop_pct`, `scalp_trailing_take_profit`, `scalp_ai_momentum_decay`로 한정됐다. hard/protect safety 우회 로그는 확인되지 않았다.
  - 코드 보정 (`2026-05-04 10:08 KST`): 보류 후 exit 후보가 소멸하면 [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)의 `holding_flow_override_candidate_*`와 마지막 review 메타를 `holding_flow_override_candidate_cleared` stage로 clear하도록 수정했다. 새 후보가 다시 발생하면 `candidate_profit/started_at`을 새 시점에서 재앵커하므로 stale `max_defer_sec` 강제청산을 막는다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_sniper_scale_in.py -k 'holding_flow_override or protect_trailing or pyramid or scale_in_qty_stage1'` -> `14 passed, 106 deselected`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_daily_threshold_cycle_report.py` -> `8 passed`.
  - 다음 액션: 장후 `[HoldingFlowOverride0504-Postclose]`에서는 `holding_flow_override_candidate_cleared`가 새로 적재되는지와 보류 후 `COMPLETED + valid profit_rate`를 full/partial, initial/pyramid, REVERSAL_ADD 혼입 없이 분리한다.

- [x] `[DynamicEntryResolverP10504-Intraday] dynamic entry price resolver P1 장중 health check` (`Due: 2026-05-04`, `Slot: INTRADAY`, `TimeWindow: 09:00~11:30`, `Track: ScalpingLogic`)
  - Source: [sniper_entry_latency.py](/home/ubuntu/KORStockScan/src/engine/sniper_entry_latency.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [2026-04-29-daehan-cable-entry-price-audit-followup.md](/home/ubuntu/KORStockScan/docs/audit-reports/2026-04-29-daehan-cable-entry-price-audit-followup.md)
  - 판정 기준: `reference_target_cap` 적용 주문과 `scalping_reference_rejected_defensive` 주문을 분리해 체결률, 미체결 취소, `COMPLETED + valid profit_rate`를 본다. 일반 스캘핑이 `target_buy_price>0` 때문에 `1200초` timeout으로 남지 않는지도 확인한다.
  - 장중 health 판정 (`2026-05-04 10:08 KST`): unique `order_bundle_submitted=22`, `filled_or_holding=14`, `completed_after_submit=10`이다. `reference_target_applied=True` 10건, rejected 12건(`target_below_bid_bps_exceeded=11`, `not_better_than_defensive=1`)으로 80bp anchor가 작동했다. 제출가의 `price_below_bid_bps` 평균은 `23.55bp`, 최대 `48bp`라 guard 초과 제출은 없었다.
  - timeout/체결 품질: `ai_entry_price_canary_max_wait_sec`는 `60초=15`, `45초=3`, `30초=2`, `20초=2`로 분포했고 일반 스캘핑이 `target_buy_price>0` 때문에 `1200초` 예약 timeout으로 남은 흔적은 없었다. timeout/cancel stage는 같은 raw 집계에서 0건이다.
  - P2 보조 판정: `entry_ai_price_canary_applied=23`(`USE_DEFENSIVE=22`, `IMPROVE_LIMIT=1`), `entry_ai_price_canary_skip_order=2`다. skip 2건 중 1건은 `entry_ai_price_skip_policy_warning=skip_without_bearish_ofi`, basis `neutral`이라 P1 OFF 근거가 아니라 POSTCLOSE `OrderbookMicroP2Canary0504`에서 OFI/QI skip policy를 분리할 후속 근거다.
  - 다음 액션: resolver는 유지한다. 장후에는 rejected 12건을 `target_below_bid_bps_exceeded`와 `not_better_than_defensive`로 나눠 full/partial fill, missed upside, soft stop 전환을 비교한다.

- [x] `[BadEntryRefinedCanary0504-1030] refined bad_entry canary 1차 health check` (`Due: 2026-05-04`, `Slot: INTRADAY`, `TimeWindow: 10:30~10:45`, `Track: ScalpingLogic`)
  - Source: [2026-05-04-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-05-04-stage2-todo-checklist.md), [2026-04-30-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-04-30-stage2-todo-checklist.md)
  - 판정 기준: 적용 시 `bad_entry_refined_candidate`, `bad_entry_refined_exit`, 후보별 `exclusion_reason`, 후속 `soft_stop/trailing/COMPLETED + valid profit_rate`, `GOOD_EXIT/MISSED_UPSIDE` would-have-been, `REVERSAL_ADD` 혼입을 확인한다. 미적용 시 후보가 왜 0인지 threshold/gate/blocker를 분해한다.
  - why: 2026-04-30에 `REVERSAL_ADD` 체결 0과 v2 OFF가 확인됐으므로, 다음 보유/청산 owner가 또 관찰만 반복되면 soft stop tail을 줄이지 못한다.
  - 장중 health 판정 (`2026-05-04 10:45 KST`): `pipeline_events_2026-05-04.jsonl`의 `10:30~10:45` 구간 기준 `bad_entry_refined_candidate=12`, unique record `4`(`SK네트웍스`, `미래에셋증권`, `한국항공우주`, `크래프톤`), `bad_entry_refined_exit=0`이다. `exclusion_reason`은 전부 `loss_too_shallow`였고, 계산상 12건 모두 `loss_too_shallow`, `peak_too_high`, `ai_too_high`가 동시에 걸렸다. 즉 미적용 원인은 이벤트 누락이 아니라 refined gate가 의도대로 보수적으로 막은 것이다.
  - gate 분해: 같은 구간 최저 수익률은 `SK네트웍스 -0.53%`, `크래프톤 -0.40%`, `미래에셋증권 +0.06%`, `한국항공우주 +0.11%`로 현행 `min_loss_pct=-1.16%`에 닿지 않았다. peak도 `+0.33~+0.99%`로 `max_peak_profit_pct=+0.05%`보다 높고, AI 점수도 대체로 `48~66`으로 `ai_score_limit=45`보다 높아 GOOD_EXIT 제거 위험이 남는다.
  - threshold-cycle 검증: `PYTHONPATH=. .venv/bin/python -m src.engine.daily_threshold_cycle_report --date 2026-05-04` 결과 [threshold_cycle_2026-05-04.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_2026-05-04.json)의 `bad_entry_block.sample`은 `refined_candidate=800`, `refined_exit=0`, `soft_stop_zone_candidate=26`, `early_capture_candidate=0`, `exclusion_top.loss_too_shallow=626`이다. 장중 1차 health에서는 canary OFF도 threshold 완화도 하지 않고, 장후 `[BadEntryRefinedCanary0504-Postclose]`에서 soft stop 후행 outcome과 `GOOD_EXIT/MISSED_UPSIDE` would-have-been을 분리 판정한다.
  - 주문상태 로그 보정 (`2026-05-04 10:45 KST`): `매수취소` WS 통보가 문자열 포함 판정 때문에 `BUY`로 분류되던 위험을 [kiwoom_websocket.py](/home/ubuntu/KORStockScan/src/engine/kiwoom_websocket.py)에서 `BUY_CANCEL`/`SELL_CANCEL`로 분리했고, [sniper_execution_receipts.py](/home/ubuntu/KORStockScan/src/engine/sniper_execution_receipts.py)는 `BUY`/`SELL` 통보만 active target에 바인딩하도록 좁혔다. 콘솔 `WS 주문상태` 로그에는 `order_no`를 추가해 신규 매수와 취소주문을 즉시 구분 가능하게 했다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_sniper_scale_in.py -k 'cancel_order_notice or order_execution_notice_parses_cancel or bad_entry_refined or bad_entry_block_observe or soft_stop_micro_grace'` -> `8 passed, 114 deselected`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_daily_threshold_cycle_report.py` -> `8 passed`; `PYTHONPATH=. .venv/bin/python -m src.engine.daily_threshold_cycle_report --date 2026-05-04` -> `data/report/threshold_cycle_2026-05-04.json` 갱신.
  - 다음 액션: 장후에는 `soft_stop_zone_candidate=26`이 refined canary가 너무 늦게 보는 표본인지, 아니면 RF머트리얼즈처럼 이미 soft stop zone 진입 후라 조기정리 불가능 표본인지 나누고, `COMPLETED + valid profit_rate`만으로 `GOOD_EXIT/MISSED_UPSIDE` would-have-been을 분리한다.

- [x] `[SameSymbolLossReentryCooldown0504-Intraday] 손실 청산 후 동일종목 재진입 live cooldown 긴급 가드` (`Due: 2026-05-04`, `Slot: INTRADAY`, `TimeWindow: 14:30~14:45`, `Track: ScalpingLogic`)
  - Source: [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py), [pipeline_events_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-04.jsonl)
  - 판정 기준: `유안타증권(003470)`은 id `4902`가 `12:01:37` `protect_trailing_stop -1.20%`로 손실 청산된 뒤, 일반 20분 쿨다운이 끝난 후 id `5008`에서 `12:42`, `13:07`, `13:51` 재주문 시도가 발생했고 최종 `13:51:48` `6130원` 체결 후 `14:28:58` `scalp_soft_stop_pct -2.02%`로 다시 손절됐다. 이후 id `5037` revive 직후에는 `same_symbol_soft_stop_cooldown_shadow would_block=True`가 찍혔지만 shadow-only라 정책 증적일 뿐 live 차단 owner가 아니었다.
  - 코드 조치 (`2026-05-04 KST`): [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py)에 `SCALP_SAME_SYMBOL_LOSS_REENTRY_COOLDOWN_ENABLED=True`, `SCALP_SAME_SYMBOL_LOSS_REENTRY_COOLDOWN_SEC=3600`을 추가했다. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 `scalp_soft_stop_pct`, 음수 `protect_trailing_stop`, `scalp_bad_entry_refined_canary`, `reversal_add_post_eval_fail` 손실 청산 시 일반 20분 대신 60분 동일종목 신규 BUY 쿨다운을 걸고 `same_symbol_loss_reentry_cooldown` stage를 남긴다. 양수 trailing/protect 익절은 이 가드 대상이 아니다.
  - why: 손실 청산 후 동일종목이 revived watch로 남아 다시 BUY를 시도하면 soft/protect stop 손실이 반복된다. 이 가드는 손실 억제 자체보다 같은 종목 반복 손실로 좋은 trailing 수익을 잠식하는 구조를 끊기 위한 운영 가드다.
  - 임시성: `SCALP_SAME_SYMBOL_LOSS_REENTRY_COOLDOWN_SEC=3600`은 장중 반복손실을 즉시 막기 위한 운영 가드다. 최종 형태는 단독 시간 쿨다운이 아니라 `직전 동일종목 손실`, `entry_vs_last_loss_price_bps`, `recent lower-high/lower-low`, `strength_relief/strong_absolute_override`, `OFI/QI`, `AI score 50 fallback 여부`를 묶은 복합 threshold로 전환해야 한다.
  - 유안타 root cause: id `4902/5008` 모두 AI가 `BUY`를 낸 것이 아니라 `AI score=50` 특수값이 AI threshold 차단을 우회했고, `strong_absolute_override` 및 `latency_mechanical_momentum_relief_normal_override`가 제출을 허용했다. 특히 id `5008`은 직전 손실 후 `12:42/13:07/13:51`에 `current_vpw 133.9~128.3`, `buy_pressure_10t 57.24~56.20`, `orderbook_micro_state=neutral`, `OFI/QI` 혼재 상태였는데도 하향 가격구조/직전 손실 기억이 entry gate에 없어서 재진입됐다.
  - 판단 단절: 논리적으로는 `다시 살 만한 thesis`가 유지되면 기존 보유에서 `REVERSAL_ADD/POST_ADD_EVAL`로 처리하고, thesis가 깨져 손절했다면 신규 watch revive 후에는 `same-symbol post-loss` entry가 막혀야 한다. 현재 구조는 HOLDING의 물타기/손절 판단과 WATCHING의 신규 진입 판단이 서로 다른 단계에서 실행되고, `sell_completed -> new_watch_id` revive가 발생하면 직전 포지션의 thesis invalidation/downtrend state가 entry gate의 필수 입력으로 넘어가지 않았다. 그래서 "손절했지만 다시 BUY 후보"라는 모순이 코드상 가능했다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_sniper_scale_in.py -k 'same_symbol_loss_reentry_cooldown or same_symbol_soft_stop_cooldown_shadow or resolve_sell_order_sign_trailing_negative_treated_as_loss'` -> `3 passed, 120 deselected`.
  - 다음 액션: 장후에는 `same_symbol_loss_reentry_cooldown`이 missed winner를 과도하게 막았는지 `same-symbol post-loss cooldown` cohort로 분리하고, 5/6에는 단독 쿨다운을 복합 threshold 후보로 전환할지 판정한다. 단, 5/4 남은 장중에는 유안타증권형 반복손실 차단을 우선한다.

- [x] `[AIScore50BuyHoldOverride0504-Intraday] AI score 50 fallback 매수보류 운영 override` (`Due: 2026-05-04`, `Slot: INTRADAY`, `TimeWindow: 15:00~15:10`, `Track: ScalpingLogic`)
  - Source: [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py), [pipeline_events_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-04.jsonl)
  - 판정 기준: `AI score=50`은 더 이상 mechanical fallback 신규 BUY 제출로 내려보내지 않는다. `AI_SCORE_50_BUY_HOLD_OVERRIDE_ENABLED=True`일 때 score가 50이거나 `ai_fallback_score_50=True`이면 `blocked_ai_score`로 기록하고 `AI_WAIT_DROP_COOLDOWN`을 부여한다.
  - 코드 조치 (`2026-05-04 KST`): [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py)에 `AI_SCORE_50_BUY_HOLD_OVERRIDE_ENABLED=True`와 env rollback key `KORSTOCKSCAN_AI_SCORE_50_BUY_HOLD_OVERRIDE_ENABLED`를 추가했다. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 AI parse/error/neutral score 50과 기존 entry arm score 50을 모두 `blocked_ai_score`로 차단하고 `blocked_reason=ai_score_50_buy_hold_override`, `ai_score_50_buy_hold_override=True`를 남긴다.
  - why: 유안타증권처럼 AI가 확정 BUY를 주지 않았는데도 score 50 fallback이 `strong_absolute_override`와 latency mechanical relief를 타면, "손절했지만 다시 매수" 모순이 entry 단계에서 반복된다. score 50은 매수판단 품질이 확인된 신호가 아니므로 보수 fallback은 신규 BUY가 아니라 보류가 맞다.
  - rollback guard: 남은 장중 `BUY drought`가 과도하게 발생해 `blocked_ai_score`가 급증하더라도 score 50만 별도 해제하지 않는다. 해제가 필요하면 5/6 `DowntrendReentryComposite0506`에서 `feature conflict + score provenance` 복합 threshold로 대체한다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_constants.py src/tests/test_state_handler_fast_signatures.py -k 'score_50_buy_hold or ai_cadence'` -> `4 passed, 19 deselected`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_constants.py src/tests/test_state_handler_fast_signatures.py` -> `23 passed`; parser 검증 `DOC_BACKLOG_TODAY=2026-05-04 ... --print-backlog-only --limit 500` -> `count=48`, `YuantaDowntrendReentry0504-Postclose=True`, `DowntrendReentryComposite0506=True`, `CooldownPolicyInventory0506=True`.
  - 다음 액션: 봇 재기동 후 runtime import와 신규 `blocked_ai_score` 필드를 확인한다. 장후에는 `score50_blocked`가 막은 missed winner와 avoided loser를 분리해 5/6 복합 threshold 설계 입력으로 넘긴다.

- [x] `[RuntimeShadowPurge0504-Intraday] runtime shadow 기본 ON 잔재 전수확인 및 OFF 정리` (`Due: 2026-05-04`, `Slot: INTRADAY`, `TimeWindow: 14:45~15:00`, `Track: RuntimeStability`)
  - Source: [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [workorder-shadow-canary-runtime-classification.md](/home/ubuntu/KORStockScan/docs/workorder-shadow-canary-runtime-classification.md)
  - 판정 기준: runtime에서 실주문/실청산에 연결되지 않는 `shadow_only` 후보 로그가 기본 ON으로 남아 있으면 OFF로 내리고, live fast-reuse telemetry가 shadow 명칭으로 남아 있으면 명칭을 분리한다.
  - 코드 조치 (`2026-05-04 KST`): `SCALP_COMMON_HARD_TIME_STOP_SHADOW_ONLY=False`, `SCALP_SOFT_STOP_SAME_SYMBOL_COOLDOWN_SHADOW_ENABLED=False`, `SCALP_PARTIAL_ONLY_TIMEOUT_SHADOW_ENABLED=False`, `OPENAI_DUAL_PERSONA_SHADOW_MODE=False`로 기본값을 정리했다. `same_symbol_soft_stop_cooldown_shadow`는 live `same_symbol_loss_reentry_cooldown` 전환으로 관찰축을 닫고, HOLDING fast reuse 경계 로그는 `ai_holding_shadow_band`가 아니라 `ai_holding_fast_reuse_band`로 기록한다.
  - 남은 허용 범위: `split_entry_*_shadow`와 OpenAI dual/Responses WS는 guarded-off 상태의 historical/transport 코드 경로로만 남고, 기본 runtime에서는 호출되지 않는다. 향후 켜려면 새 checklist/rollback owner가 필요하다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_constants.py src/tests/test_sniper_scale_in.py -k 'runtime_shadow_defaults_are_off or same_symbol_loss_reentry_cooldown or holding_fast_signature or hard_time_stop_shadow or same_symbol_soft_stop_cooldown_shadow or partial_only_timeout_shadow'` -> `5 passed, 125 deselected`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_constants.py src/tests/test_sniper_scale_in.py` -> `130 passed`.
  - 다음 액션: shadow 명칭이 report/UI에 남아 있는 historical label은 `CodeDebt0506`에서 rename/backward-compat 정리 대상으로만 본다. 신규 alpha 검증은 shadow가 아니라 canary-only 또는 report-only 산출물로 분리한다.

- [x] `[OpenAIResponsesWS0504-Intraday] OpenAI Responses WS shadow queue/timeout/fallback 1차 판정` (`Due: 2026-05-04`, `Slot: INTRADAY`, `TimeWindow: 10:00~10:20`, `Track: ScalpingLogic`)
  - Source: [2026-04-30-openai-enable-acceptance-spec.md](/home/ubuntu/KORStockScan/docs/ai-acceptance/2026-04-30-openai-enable-acceptance-spec.md), [workorder-shadow-canary-runtime-classification.md](/home/ubuntu/KORStockScan/docs/workorder-shadow-canary-runtime-classification.md), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md)
  - 판정 기준: 현재 `main` 스캘핑 live routing은 Gemini 고정이며 OpenAI HTTP/WS는 실전 매매판단 owner가 아니다. 따라서 이 항목은 `ai_engine_openai.py` transport readiness/contract parity를 future backlog 후보로 유지할지 보는 점검이다. 만약 별도 replay/diagnostic 실행으로 `openai_ws_requests`, `openai_ws_completed`, `openai_ws_timeout_reject`, `openai_ws_late_discard`, `openai_ws_parse_fail`, `openai_ws_http_fallback`, `openai_ws_request_id_mismatch`, `openai_ws_queue_wait_ms`, `openai_ws_roundtrip_ms`가 생성되면 shadow-only로 확인하고, 생성되지 않으면 `현재 runtime 미사용`으로 닫는다. `request_id_mismatch=0`, `late_discard=0`이 아니면 same-day live 검토 금지다.
  - why: 지금 필요한 것은 OpenAI를 live/shadow로 실제 매매호출하는 것이 아니라, Gemini live routing을 건드리지 않고 OpenAI transport 경로를 후반 acceptance backlog로 추적 가능한 상태로 보존하는 것이다.
  - cohort: `baseline cohort=Gemini live routing`, `candidate live cohort=none`, `observe-only cohort=openai_responses_ws_shadow_flag_off`, `excluded cohort=full/partial fill 및 COMPLETED 손익 직접 판정`, `rollback owner=OPENAI_TRANSPORT_MODE + OPENAI_RESPONSES_WS_ENABLED`, `cross-contamination check=OpenAI transport 준비도와 실전 scalping EV를 직접 합산 금지`
  - 장중 판정 (`2026-05-04 10:08 KST`): `pipeline_events_2026-05-04.jsonl`와 `logs/`에서 `openai_ws_*`/`responses_ws` runtime metric은 0건이다. 현재 `TRADING_RULES` 기준 `OPENAI_TRANSPORT_MODE=http`, `OPENAI_RESPONSES_WS_ENABLED=False`이며, main 스캘핑 live routing은 Gemini baseline이다. 따라서 이 항목은 `runtime 미사용/backlog only`로 닫고, 실전 scalping EV와 합산하지 않는다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_ai_engine_openai_transport.py` -> `8 passed, 1 warning(pandas_ta Pandas4Warning)`.
  - 다음 액션: POSTCLOSE에는 live/WS 전환 없이 `[OpenAIResponsesWS0504-Postclose]`에서 `runtime 미사용 backlog 유지`로 재분류하고, 실제 transport acceptance는 `AIEngineFlagOffBacklog0506`에서 다시 본다.

## 오버나이트 체크리스트 (15:20~15:35)

- [x] `[HoldingFlowOverride0504-Overnight] 15:20 SELL_TODAY flow override 재검문` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 15:20~15:35`, `Track: ScalpingLogic`)
  - Source: [sniper_overnight_gatekeeper.py](/home/ubuntu/KORStockScan/src/engine/sniper_overnight_gatekeeper.py), [sniper_time.py](/home/ubuntu/KORStockScan/src/engine/sniper_time.py), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md)
  - 판정 기준: `SCALPING_OVERNIGHT_DECISION_TIME=15:20:00`에 `evaluate_scalping_overnight_decision`이 1회 실행되고, 결과가 `SELL_TODAY`일 때 `evaluate_scalping_holding_flow(..., decision_kind=overnight_sell_today)` 재검문이 실행되는지 확인한다. 장후 보정 후 기준은 flow `EXIT`/`TRIM`은 `SELL_TODAY` 유지, flow `HOLD`만 `HOLD_OVERNIGHT` 전환과 `overnight_flow_override_hold` 적재다.
  - why: 장마감 직전 유동성 리스크를 남겨두지 않으면서도, `SELL_TODAY` 단일 판정이 회복/흡수 흐름을 끊는 경우를 줄인다.
  - 다음 액션: `15:20~15:30` 사이 최초 후보 대비 추가악화 `0.80%p` 도달 시 `overnight_flow_override_revert_sell_today`와 실제 당일청산 복귀가 발생하는지 확인한다. hard stop/잔고/주문 정합성 문제가 있으면 override 없이 `SELL_TODAY` 유지가 맞다.
  - 장후 판정 (`2026-05-04 16:20 KST`): `pipeline_events_2026-05-04.jsonl` 기준 `overnight_flow_override_review=2`, `overnight_flow_override_hold=2`, `overnight_flow_override_revert_sell_today=0`이다. `한국항공우주(047810)`는 `15:20:13` `HOLD/흡수/score=72/profit=+0.90%`로 `HOLD_OVERNIGHT` 전환했고, `16:05:56` 최근 관측 `+0.78%`로 최초 대비 악화가 `0.12%p`라 복귀 조건 `0.80%p`에 닿지 않았다.
  - 문제 판정: `쏠리드(050890)`는 `15:20:28` `TRIM/소강/score=42/profit=-1.43%`였는데도 v1 구현이 `HOLD_OVERNIGHT`로 전환했다. `TRIM`은 실주문 부분청산이 아니라 리스크 축소 선호 라벨이므로, 부분청산 구현이 없으면 `SELL_TODAY` 유지가 논리적으로 맞다.
  - 코드 조치: [sniper_overnight_gatekeeper.py](/home/ubuntu/KORStockScan/src/engine/sniper_overnight_gatekeeper.py)는 오버나이트 flow 재검문에서 `TRIM`을 `flow_trim_unsupported`로 기록하고 원래 `SELL_TODAY`를 유지하도록 보정했다. `HOLD`만 `HOLD_OVERNIGHT`로 승격한다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_holding_flow_override.py` -> `8 passed`.

## 장후 체크리스트 (16:20~16:40)

- [x] `[CumulativeThresholdCycleReport0504-Postclose] daily threshold cycle 누적/rolling report-only 산출물 생성` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 16:40~17:00`, `Track: ScalpingLogic`)
  - Source: [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py), [README.md](/home/ubuntu/KORStockScan/data/threshold_cycle/README.md), [README.md](/home/ubuntu/KORStockScan/data/report/README.md), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md)
  - 판정 기준: daily threshold report와 별도로 `threshold_cycle_cumulative_YYYY-MM-DD.{json,md}`를 생성하되, runtime threshold mutation, bot restart, preopen apply 자동반영과 연결하지 않는다. 손익은 `COMPLETED + valid profit_rate`만 쓰고, `all_completed_valid`, `normal_only`, `initial_only`, `pyramid_activated`, `reversal_add_activated` cohort를 분리한다.
  - 실행 메모 (`2026-05-04 KST`): [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py)에 `build_cumulative_threshold_cycle_report`, Markdown renderer, saver를 추가했다. 기존 `deploy/run_threshold_cycle_postclose.sh`는 daily builder를 호출하므로 장후 실행 시 daily JSON, statistical action weight, holding/exit decision matrix와 함께 `data/report/threshold_cycle_cumulative/threshold_cycle_cumulative_YYYY-MM-DD.{json,md}`가 생성된다.
  - 금지선: 누적 평균 단독으로 live threshold mutation을 수행하지 않는다. daily, rolling, cumulative 방향성이 일치하고 full/partial fill, runtime flag/source cohort 누락 위험이 해소된 뒤에만 별도 checklist에서 단일 owner/rollback guard/manifest-only 추천값으로 넘긴다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_daily_threshold_cycle_report.py` -> `10 passed`; `PYTHONPATH=. .venv/bin/python -m src.engine.daily_threshold_cycle_report --date 2026-05-04` -> `threshold_cycle_cumulative_2026-05-04.{json,md}` 생성 확인. DB 포함 누적 표본은 cumulative completed `146`, avg `-0.3992%`, rolling_5d completed `91`, avg `-0.5784%`, warnings `0`이다.
  - 다음 액션: `ThresholdOpsTransition0506` 전까지 이 산출물은 장후 판정 입력으로만 사용하고, Project/Calendar 동기화 후 5/6 threshold 운영전환 항목에서 report-only 유지 여부를 재확인한다.

- [x] `[G2PowerReceiptMismatch0504-Postclose] 지투파워 0주 매도가능/중복 COMPLETED 분리 및 원인 격리` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 17:00~17:20`, `Track: RuntimeStability`)
  - Source: [2026-05-04-symbol-followup-risk-report.md](/home/ubuntu/KORStockScan/docs/audit-reports/2026-05-04-symbol-followup-risk-report.md), [trade_review_2026-05-04.json](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/trade_review_2026-05-04.json), [sniper_execution_receipts_info.log](/home/ubuntu/KORStockScan/logs/sniper_execution_receipts_info.log), [kiwoom_orders_info.log](/home/ubuntu/KORStockScan/logs/kiwoom_orders_info.log), [sniper_state_handlers_error.log](/home/ubuntu/KORStockScan/logs/sniper_state_handlers_error.log)
  - 판정 기준: `지투파워(388050)` `ID 4799`와 `ID 4988`을 분리한다. `ID 4799`는 `10:50:32` 1주 정상 체결 후 `10:56:36` `scalp_soft_stop_pct -1.78%` 완료 표본으로 인정한다. `ID 4988`은 DB에는 `COMPLETED -0.23%`로 남았지만 신규 `ENTRY_FILL/holding_started`가 없고 `10:56:41` `0주 매도가능` 거절 및 `10:57:29` 취소 원주문번호 없음 롤백이 있으므로 `receipt_mismatch_zero_sellable` 후보로 손익 표본에서 분리한다.
  - why: 1주 cap 환경이라 손실 규모는 작았지만, cap이 없었다면 대수량 포지션에서 `0주 매도가능`/상태 롤백/중복 completed가 손익 왜곡과 주문 재시도 리스크로 커질 수 있다. 원인 격리 전에는 이 표본을 threshold 자동화의 실현손익 입력으로 쓰면 안 된다.
  - 다음 액션: report builder 또는 장후 분석에서 `COMPLETED`라도 receipt-matched `holding_started/ENTRY_FILL` 없는 same-symbol revived row를 제외/flag 처리하는 follow-up을 만든다. 원인은 `sell_completed revive`, `new_watch_id` 생성, HOLDING rollback, DB completed 전환 경로 중 어디인지 분해한다.
  - 장후 판정 (`2026-05-04 18:40 KST`): `ID 4799`는 정상 손실 표본으로 유지하고, `ID 4988`은 `receipt_mismatch_zero_sellable` excluded cohort로 분리한다. `ID 4988`은 pipeline event에 신규 `holding_started`가 없고, `10:56:40` `exit_signal`, `10:56:41` `sell_order_sent`만 남아 DB `COMPLETED -0.23%`와 raw receipt가 맞지 않는다.
  - 근거: `ID 4799`는 `10:50:32` `holding_started buy_price=16730 qty=1`, `10:56:36` `holding_flow_override_force_exit max_defer_sec`, `10:56:37` `sell_completed -1.78%`로 정상 lifecycle이 닫혔다. 반면 `ID 4988`은 `held_sec=0`, `scalp_preset_hard_stop_pct`, 신규 fill 부재로 같은 포지션 상태가 revive row에 잘못 바인딩된 것으로 본다.
  - 후속 연결: [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `ExecutionReceiptsThreadSafety0506`에서 지투파워 `ID 4988`을 `active order binding / zero_sellable / completed truth drift` anchor로 추가해 대수량 cap 해제 전 반드시 닫는다.

- [x] `[SKNetworksDowntrendReentry0504-Postclose] SK네트웍스 하향 재진입 bad-entry 방어 가능성 점검` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 17:20~17:40`, `Track: ScalpingLogic`)
  - Source: [2026-05-04-symbol-followup-risk-report.md](/home/ubuntu/KORStockScan/docs/audit-reports/2026-05-04-symbol-followup-risk-report.md), [trade_review_2026-05-04.json](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/trade_review_2026-05-04.json), [pipeline_events_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-04.jsonl)
  - 판정 기준: 첫 매매 `ID 4738`은 `09:15:06` `6,650원` 진입 후 `09:27:53` `6,530원` `-2.03%` soft stop, 두 번째 `ID 4884`는 `10:03:00` `6,560원` 재진입 후 `11:01:36` `6,460원` `-1.75%` soft stop이다. 첫 손절 이후 같은 종목이 하향 가격 구조에서 재진입된 이유를 `same_symbol_repeat_loss`, `downtrend_reentry`, `post_soft_stop_reentry`로 분리한다.
  - why: `bad_entry_refined`는 보유/청산 canary라 제출 전 재진입 자체를 막지 못한다. 향후 방어 가능성은 entry-side report에서 same-symbol post-loss cooldown, downtrend reentry gate, entry price trend veto가 관측되는지에 달려 있다.
  - 다음 액션: 현재 report에 하락 재진입 blocker가 없으면 `ThresholdOpsTransition0506` 전 live 변경 없이 report-only feature로 `previous_same_symbol_completed_loss`, `last_same_symbol_sell_price`, `entry_vs_last_loss_price_bps`, `same_symbol_loss_cooldown_sec`를 후보화한다.
  - 장후 판정 (`2026-05-04 18:40 KST`): 방어는 `bad_entry_refined`가 아니라 entry-side 복합 threshold가 맡아야 한다. 첫 손절 후 두 번째 진입가는 `6,560원`으로 직전 sell `6,530원` 대비 높았지만 첫 buy `6,650원` 대비 낮고, 두 거래 모두 `strong_absolute_override + USE_DEFENSIVE + orderbook_micro_state=neutral`로 들어왔다.
  - 근거: `ID 4738`은 entry 전 `AI 75` 뒤 `AI score 50 fallback=True`가 섞였고 `peak_profit=-0.23%`, `soft_stop -2.03%`로 종료했다. `ID 4884`는 `10:11:17` trailing 후보 `+0.53%`를 flow가 HOLD로 보류했지만 최종 `11:01:36` `ws_stale` soft stop `-1.75%`로 끝났다. 두 건의 post-sell 평가는 모두 `MISSED_UPSIDE`이나 20분 MAE가 `-11.026%`, `-9.288%`라 단순 보유 연장만으로는 결론을 낼 수 없다.
  - 후속 연결: [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `DowntrendReentryComposite0506`에 `SK네트웍스 4738/4884`를 anchor로 남기고, `same-symbol 손실 이후 revive 신규 WATCHING`과 `기존 보유 중 REVERSAL_ADD`를 상태전이 기준으로 분리한다.

- [x] `[YuantaDowntrendReentry0504-Postclose] 유안타증권 손실 후 하향 재진입 root-cause 및 복합 threshold 전환 판정` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 18:20~18:40`, `Track: ScalpingLogic`)
  - Source: [pipeline_events_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-04.jsonl), [post_sell_candidates_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/post_sell/post_sell_candidates_2026-05-04.jsonl), [sniper_strength_momentum.py](/home/ubuntu/KORStockScan/src/engine/sniper_strength_momentum.py), [sniper_entry_latency.py](/home/ubuntu/KORStockScan/src/engine/sniper_entry_latency.py)
  - 판정 기준: `유안타증권(003470)` id `4902`는 `11:56:03` `6,080원` 체결, `11:59:01` PYRAMID 후 `12:01:37` `protect_trailing_stop -1.20%`로 종료했다. revive된 id `5008`은 `12:42` `5,920원`, `13:07` `6,060원`, `13:51` `6,130원`으로 재주문됐고 최종 `13:51:48` 체결 후 `14:28:58` `scalp_soft_stop_pct -2.02%`로 종료했다. 각 재진입 시점의 `AI score=50 fallback`, `strong_absolute_override`, `latency_mechanical_momentum_relief_normal_override`, `entry_price_canary USE_DEFENSIVE`, `OFI/QI neutral/mixed`, `entry_vs_last_loss_price_bps`, `recent lower-high/lower-low`를 함께 본다.
  - why: 이번 live 60분 cooldown은 반복손실을 즉시 막는 임시 운영 가드다. 근본 원인은 하향 가격구조와 직전 동일종목 손실 정보가 entry gate의 복합 feature로 들어가지 않고, `AI score=50` 특수값과 mechanical strength/latency relief가 BUY 제출을 허용하는 구조다.
  - 다음 액션: 장후 판정에서 `same_symbol_loss_reentry_cooldown` 유지/OFF만 판단하지 말고, 5/6 `DowntrendReentryComposite0506`로 `previous_same_symbol_completed_loss + trend path + OFI/QI + AI fallback provenance` 기반 복합 threshold 설계 여부를 넘긴다. live 변경은 5/4에 추가로 열지 않는다.
  - 장후 판정 (`2026-05-04 18:40 KST`): 사용자의 지적대로 논리가 끊겼다. 다시 살 근거가 충분했다면 기존 보유 중 `REVERSAL_ADD`/보유 연장으로 처리했어야 하고, 손절 thesis invalidation이 맞았다면 revive 신규 진입은 회복 근거가 확인될 때까지 막았어야 한다.
  - 근거: `ID 4902`는 `11:59:01` PYRAMID 후 `12:01:37` protect trailing `-1.20%`로 닫혔고 post-sell은 `GOOD_EXIT`로 20분 MAE `-13.300%`라 손실 확대 차단 근거가 있다. 그러나 revive `ID 5008`은 `12:42/13:07/13:51` 세 차례 모두 `USE_DEFENSIVE + orderbook_micro_state=neutral`, 최종 `13:51:48` 체결 후 `14:28:58` soft stop `-2.02%`로 종료했다. 이는 동일종목 손실 상태가 entry gate에 충분히 반영되지 않은 재진입 churn이다.
  - 후속 연결: `AI_SCORE_50_BUY_HOLD_OVERRIDE_ENABLED=True`는 유지하되 최종 형태는 hard cooldown 단독 blocker가 아니다. [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `DowntrendReentryComposite0506`에서 `previous_same_symbol_completed_loss + lower-high/lower-low + AI fallback provenance + OFI/QI neutral/bearish + entry_vs_last_loss_price_bps`를 복합 feature로 잠근다.

- [x] `[PinoSoftStopPostclose0504] 피노 soft stop 완료 및 REVERSAL_ADD 미도달 장후 분석` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 17:40~18:00`, `Track: ScalpingLogic`)
  - Source: [2026-05-04-symbol-followup-risk-report.md](/home/ubuntu/KORStockScan/docs/audit-reports/2026-05-04-symbol-followup-risk-report.md), [pipeline_events_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-04.jsonl), [trade_review_2026-05-04.json](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/trade_review_2026-05-04.json)
  - 판정 기준: `피노(033790)` `ID 4810`은 DB 기준 `10:46:12` `15,820원` 진입, `11:17:20` `15,550원` 청산, `-1.93%` soft stop 완료다. `REVERSAL_ADD` 미도달 사유는 초반 `57~88초` 구간의 `ai_score_too_low(55/53)` 및 수급/AI 회복 부족, 이후 `pnl_out_of_range(-1.11~-1.55)`와 `hold_sec_out_of_range(300초+)`로 분리한다.
  - why: flow override가 `흡수/HOLD`로 soft stop 청산을 보류했지만 최종 손실이 확대됐다. 장후에는 물타기 완화보다 먼저 `flow defer`가 손실 확대에 기여했는지, 그리고 `REVERSAL_ADD` 시간창이 기대값 관점에서 너무 짧은지 분리해야 한다.
  - 다음 액션: `HoldingFlowOverride0504-Postclose`에 피노를 손실 확대 표본으로 합류시키고, `ReversalAddDynamicQty0506`/`PyramidDynamicQty0506`와 혼합하지 않는다. `REVERSAL_ADD` 시간창 완화는 별도 단일축 후보로만 검토한다.
  - 장후 판정 (`2026-05-04 18:40 KST`): `REVERSAL_ADD` 미도달은 단일 threshold 문제가 아니라 `AI 회복 부족 + 시간창 이탈 + flow defer 손실 확대`의 복합 표본이다. 물타기 완화를 바로 열기보다 `would_allow_if_hold_window_extended`류 report-only 분해가 먼저다.
  - 근거: 초반 `10:47:01~10:47:32`에는 손실폭이 `-0.67~-0.61%`로 창 안에 있었지만 AI가 `55/53`, buy pressure가 `7.55 -> 84.94`로 불안정했고 `add_judgment_locked`도 겹쳤다. 이후 `10:49:04`부터 손실은 `-1.11%` 이상으로 깊어지고, 보유시간은 `300초+`로 늘어 REVERSAL_ADD 기본 창 밖으로 밀렸다. `11:14:50~11:17:09` flow HOLD 반복 뒤 최종 `-1.93%`, post-sell은 `GOOD_EXIT`와 20분 MAE `-15.498%`라 늦은 청산 자체는 손실 확대 차단 근거도 있다.
  - 후속 연결: [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `ReversalAddDynamicQty0506`에서 수량 산식과 섞지 말고, 시간창/AI회복 blocker counterfactual은 별도 report field 후보로만 본다.

- [x] `[ZaramSoftStopPositivePeak0504-Postclose] 자람테크놀로지 positive peak 후 soft stop/체결 슬리피지 분석` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 18:00~18:20`, `Track: ScalpingLogic`)
  - Source: [pipeline_events_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-04.jsonl), [post_sell_candidates_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/post_sell/post_sell_candidates_2026-05-04.jsonl:29)
  - 판정 기준: `자람테크놀로지(389020)` `ID 4982`는 `12:38:20` `buy_price=59300`, `12:54:20` `profit_rate=+0.95`, `12:55:54` 첫 `soft_stop_micro_grace`, `12:57:27` `holding_flow_override_force_exit/scalp_soft_stop_pct`, `12:57:28` `sell_completed sell_price=58100 profit_rate=-2.25%`로 완료됐다. exit signal 관측가는 `58400` 수준이어서 체결가는 신호 대비 약 `300원` 불리했다.
  - why: 이 표본은 순수 bad-entry 즉시손절이 아니라 `+0.95%` positive peak 이후 `-2.25%` 체결까지 peak-to-exit giveback이 커진 케이스다. `bad_entry_refined`는 `peak_recovered`/`soft_stop_zone`으로 배제됐고, REVERSAL_ADD는 초반 `AI/supply/pnl` 및 이후 `hold_sec_out_of_range`로 미도달했다.
  - 다음 액션: 장후에는 `post_sell_evaluation` 누락 여부를 먼저 확인하고, `profit protection/trailing`, `soft_stop_micro_grace 재시작 여부`, `signal-to-fill slippage`를 분리한다. `bad_entry_refined` 완화나 REVERSAL_ADD 확대를 같은 축에서 바로 열지 않는다.
  - 장후 판정 (`2026-05-04 18:40 KST`): peak 이후 수익 보전 실패 표본으로 확정한다. `bad_entry_refined`를 넓혀 해결할 케이스가 아니라, positive peak 이후 trailing/protect와 soft stop 전환 민감도를 분리해야 한다.
  - 근거: `12:54:20` `+0.95%`까지 갔고, `12:55:33`에는 `profit=-1.24%`, `peak=+0.95%`, `curr_vs_micro_vwap_bp=-171.13`, `spread_bps=153.32`로 급격히 얇은 호가/마이크로 이탈이 발생했다. 최종 신호는 `-1.74%`였지만 체결은 `-2.25%`로 약 `0.51%p` 불리해 signal-to-fill slippage도 크다. post-sell 20분 MFE `+2.410%`와 MAE `-9.466%`가 모두 커서 noise와 true adverse flow가 섞여 있다.
  - 후속 연결: [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `TrailingProtectSensitivity0506`에 `positive_peak -> soft_stop` anchor로 연결하고, `bad_entry_refined`/`REVERSAL_ADD`와 같은 축에서 조정하지 않는다.

- [x] `[DaehanOpticalReversalAddPostEval0504-Postclose] 대한광통신 물타기 직후 reversal_add post-eval 손절 분석` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 18:20~18:40`, `Track: ScalpingLogic`)
  - Source: [pipeline_events_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-04.jsonl), [post_sell_candidates_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/post_sell/post_sell_candidates_2026-05-04.jsonl:31)
  - 판정 기준: `대한광통신(010170)` `ID 4805`는 `13:21:59` `REVERSAL_CANDIDATE -> AVG_DOWN`으로 1주를 `17600원`에 추가 체결해 `buy_price=17615`, `buy_qty=2`가 됐고, 약 `7초` 뒤 `13:22:00` `reversal_add_post_eval_fail`로 전량 청산 신호가 발생했다. 최종 체결은 `sell_price=17580`, `profit_rate=-0.43%`, `exit_rule=reversal_add_post_eval_fail`이다.
  - why: 손실폭 자체는 soft stop 수준이 아니며, `REVERSAL_ADD_POST_EVAL_SEC=25` 안의 실패 가드가 즉시 작동한 표본이다. 추가 직전 `AI=72`, `buy_pressure_10t=96.77`, `curr_vs_micro_vwap_bp=28.44`였지만 `tick_acceleration_ratio=0.0`, `spread_bps=34.09`, `top3_depth_ratio=2.0855`라 단기 수급 quality가 혼재했다.
  - 다음 액션: 장후에는 `post_add 0~30초` 가격 경로와 post-sell rebound를 보고 `POST_EVAL 7초 즉시청산`이 손실 확대 차단인지 missed recovery인지 분리한다. 단일 tick `tick_acceleration_ratio<0.90`만으로 post-eval fail이 나는지, `sample_count/span`, spread/top3 depth, AI 유지 조건을 복합축으로 바꿔야 하는지 `ReversalAddDynamicQty0506`와 별도 민감도 항목으로 연결한다.
  - 장후 판정 (`2026-05-04 18:40 KST`): `POST_EVAL 7초 즉시청산`은 손실폭을 작게 제한했지만 missed recovery 가능성이 있다. live 변경은 보류하고, `post_add_eval` 자체의 sample/span/호가품질 민감도 보고축이 필요하다.
  - 근거: `13:21:53` `reversal_add_candidate`, `13:21:59` `scale_in_executed add_type=AVG_DOWN`, `13:22:00` `reversal_add_post_eval_fail`로 7초 만에 전량청산 신호가 났다. 최종 손실은 `-0.43%`로 작지만 post-sell 평가는 `NEUTRAL`, 5분 MFE vs buy `+0.710%`, 20분 MFE vs buy `+0.766%`라 즉시청산이 일부 회복 여지를 버렸을 가능성이 있다.
  - 후속 연결: [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `ReversalAddDynamicQty0506`의 수량 산식과 분리해, `reversal_add_post_eval_sensitivity` report-only 후보를 별도 메모로 남긴다. 수량을 늘리는 문제와 post-eval 즉시청산 민감도는 같은 날 live로 동시에 열지 않는다.

- [x] `[KAIWideWindowExit0504-Postclose] 한국항공우주 스캘핑/스윙 윈도우 전환 필요성 점검` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 18:40~19:00`, `Track: ScalpingLogic`)
  - Source: [pipeline_events_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-04.jsonl), [post_sell_evaluations_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/post_sell/post_sell_evaluations_2026-05-04.jsonl), [trade_review_2026-05-04.json](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/trade_review_2026-05-04.json)
  - 판정 기준: `한국항공우주(047810)` 당일 completed `4932`, `4976`과 open `5019`를 분리하고, 스캘핑 trailing 청산 후 같은 종목 재진입/보유 전환이 기대값을 깎았는지 본다. open/HOLDING과 `overnight_flow_override`는 실현손익에 합산하지 않는다.
  - 장후 판정 (`2026-05-04 18:40 KST`): 사용자의 우려는 타당하다. 첫 번째 `ID 4932`는 단순 스캘핑 익절로 닫기보다 `winner wide-window` 후보로 감시했어야 하는 표본이다. 다만 두 번째 `ID 4976`은 고점 이후 재진입 손실 표본이라, 모든 KAI를 1일 이상 swing으로 보자는 결론은 아니다.
  - 근거: `ID 4932`는 `10:10:55` `178,400원` 체결, peak `+1.45%`, `10:43:43` trailing signal `+0.89%`, 체결 `+0.78%`였다. post-sell 20분 기준 `mfe_vs_buy=+1.962%`, `close_vs_buy=+1.850%`로 매도 후에도 매수가 대비 우위가 유지됐다. 이후 `ID 4976`은 `12:01:23` `182,300원`에 더 높은 가격으로 재진입해 `12:50:40` refined bad-entry `-1.16%`로 닫혔고, post-sell 20분 `close_vs_buy=-2.194%`라 재진입 시점은 실패였다.
  - open 분리: `ID 5019`는 `14:09:56` `178,200원` 체결 후 checklist snapshot 기준 HOLDING이다. pipeline에는 `16:12:12` `overnight_flow_override_review flow_action=TRIM profit_rate=+191.31`이 남아 있는데, `trade_review`의 buy price/holding 상태와 맞지 않는 비정상 profit_rate라 수익 판단 근거로 쓰지 않는다.
  - 다음 액션: [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `SwingTrailingPolicy0506`와 `TrailingProtectSensitivity0506`에 `winner wide-window transition` anchor로 연결한다. 후보 조건은 `peak_profit>=+1.0`, `current_ai_score>=70`, `post_sell 5~20m close_vs_buy 양수`, `same-symbol 재진입이 더 높은 가격에서 발생`이며, live 변경 전에는 report-only로만 `would_extend_window`를 산출한다.

- [x] `[HoldingFlowOverride0504-Postclose] holding/overnight flow override 장후 판정` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 16:00~16:20`, `Track: ScalpingLogic`)
  - Source: [2026-05-04-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-05-04-stage2-todo-checklist.md), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md)
  - 판정 기준: `holding_flow_override_defer_exit` 이후 후행 `COMPLETED + valid profit_rate`, `GOOD_EXIT/MISSED_UPSIDE`, hard/protect bypass, `force_reason` 분포를 full/partial, initial/pyramid, REVERSAL_ADD 혼입 여부로 분리한다. `+0.8% preset TP AI 1회 검문`은 `SCALPING_HOLDING_SYSTEM_PROMPT`를 사용하지만 normal `ai_holding_review` refresh와 합치지 않고 익절 후보 표본으로 `scalp_preset_tp_ai_exit_action/hold_action`, `scalp_preset_ai_review_exit`를 따로 본다. 오버나이트는 `SELL_TODAY -> HOLD_OVERNIGHT` 전환과 `0.80%p` 악화 복귀를 별도 표본으로 본다.
  - why: 목적은 손실 억제가 아니라 조급한 청산으로 인한 missed upside를 줄이고, 붕괴 흐름은 더 명확하게 청산해 순기대값을 높이는 것이다.
  - 장중 근거 메모 (`2026-05-04 10:43 KST snapshot`): [trade_review_2026-05-04.json](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/trade_review_2026-05-04.json) 기준 completed `14`, open `4`, `COMPLETED + valid profit_rate` 평균 `+0.26%`, 실현 `+7,204원`이다. `scalp_trailing_take_profit`은 `8/8` 승, 평균 `+1.431%`, 합계 `+12,611원`으로 수익의 주축이고, `scalp_soft_stop_pct`는 `4/4` 패, 평균 `-1.91%`, 합계 `-3,519원`으로 손실 tail의 주축이다.
  - flow override 장중 근거: `SK네트웍스(001740)` id `4738`은 `09:21:26` `candidate_profit=-2.18%`에서 `HOLD` 보류 후 `09:27:53` `max_defer_sec`로 `-2.03%` 청산됐다. `하나마이크론(067310)` id `4829`는 `09:21:02` `candidate_profit=-2.08%` 보류 후 `09:31:48` `max_defer_sec`로 `-1.62%` 청산됐다. 두 표본은 보류 자체의 유효성보다 후보 lifecycle/stale anchor와 회복 후 재진입 여부를 먼저 분해해야 한다.
  - open risk 메모: 같은 snapshot 이후 마지막 pipeline 관측 기준 `크래프톤(259960)` id `4824`는 약 `-1.40%`, `SK네트웍스(001740)` id `4884`는 약 `-0.99%`로 soft stop tail 후보이고, `한국항공우주(047810)` id `4932`는 약 `+1.39%`, `미래에셋증권(006800)` id `4817`은 약 `+0.94%`로 이익 보전/추세 유지 분기 후보다. 장후 판정에서는 open 상태를 실현손익에 합산하지 말고 별도 risk bucket으로 둔다.
  - 다음 액션: 보류 후 추가악화가 잦거나 hard/protect safety 우회가 보이면 즉시 OFF한다. 보류 후 회복/흡수가 확인되면 다음 운영일에도 운영 override를 유지하되, canary 성과와 합산하지 않고 별도 cohort로 판정한다.
  - 장후 판정 (`2026-05-04 16:20 KST`): keep은 하되, 오버나이트 `TRIM -> HOLD_OVERNIGHT` 승격은 즉시 폐기한다. 당일 stage는 `holding_flow_override_review=70`, `defer_exit=439`, `candidate_cleared=48`, `exit_confirmed=4`, `force_exit=138`이다. `force_exit` 상위는 `ai_engine_unavailable + scalp_soft_stop_pct=113`, `ws_stale + scalp_trailing_take_profit=8`, `max_defer_sec + scalp_soft_stop_pct=7`이다.
  - 근거: flow defer가 일부 후보를 `candidate_cleared`로 회복시킨 점은 유지 근거지만, `쏠리드`는 `15:16` refined bad-entry 조기정리 후보를 `HOLD`로 유예한 뒤 `15:31~15:32` 장마감 주문불가 `[2000](999999:주문 불가능합니다.)`가 `98건` 반복됐다. 이는 hard/protect bypass는 아니지만 close 이후 주문 재시도/상태복구 safety gap이다.
  - 코드 조치: 오버나이트 `TRIM` semantics는 [sniper_overnight_gatekeeper.py](/home/ubuntu/KORStockScan/src/engine/sniper_overnight_gatekeeper.py)에서 보정했다. 장중 `HOLD/TRIM` 보류 semantics는 유지하되, 장마감 주문불가 반복 방지는 5/6 별도 safety item으로 분리한다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_holding_flow_override.py` -> `8 passed`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_holding_flow_override.py src/tests/test_constants.py src/tests/test_state_handler_fast_signatures.py -k 'overnight_flow or bad_entry_refined or score_50_buy_hold or ai_cadence'` -> `5 passed, 26 deselected`.
  - 추가 보정 (`2026-05-04 16:30 KST`): 5/6로 미뤘던 장마감 주문불가 반복 방지는 현재 장후에도 처리 가능하므로 즉시 반영했다. [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 `15:30 KST` 이후 SCALPING 매도 신호가 발생하면 실주문을 보내지 않고 `sell_order_blocked_market_closed`를 1회만 남기며 `market_closed_sell_pending=True`로 상태를 유지한다. 이는 주문 실패를 숨기는 것이 아니라 `after-close impossible`을 별도 stage로 분리해 다음 장전/리포트 truth를 보호하는 조치다.
  - 추가 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_sniper_scale_in.py -k 'market_close_blocks_order_once or sell_reject_with_zero_sellable_qty or sell_reject_with_positive_sellable_qty'` -> `3 passed, 121 deselected`.

- [x] `[BadEntryRefinedCanary0504-Postclose] refined bad_entry canary keep/OFF 판정` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 16:00~16:20`, `Track: ScalpingLogic`)
  - Source: [2026-05-04-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-05-04-stage2-todo-checklist.md), [2026-04-30-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-04-30-stage2-todo-checklist.md)
  - 판정 기준: canary 적용/비적용 refined bad-entry 후보를 `COMPLETED + valid profit_rate`, `soft_stop`, `trailing`, `GOOD_EXIT`, `MISSED_UPSIDE`, `same-symbol reentry`, `sell_order_failed`로 비교한다. `full/partial`, `initial/pyramid`, `REVERSAL_ADD` 체결 포지션은 합산하지 않는다.
  - why: refined canary는 v2 계층화 전략의 다음 실행축이다. 성공 여부는 단순 손실 축소가 아니라 winner 제거 없이 bad-entry 손실 tail을 줄였는지로 닫아야 한다.
  - 장후 표본 보강 (`2026-05-04 09:56 KST`): [daily_threshold_cycle_report.py](/home/ubuntu/KORStockScan/src/engine/daily_threshold_cycle_report.py)에 `bad_entry_block.sample.refined_candidate/refined_exit/soft_stop_zone_candidate/early_capture_candidate/exclusion_top`을 추가했다. RF머트리얼즈(`327260`, id `4895`)는 `09:42:41` `profit_rate=-1.77`, `peak_profit=+0.31`, `AI=54`, `held_sec=276`에서 `bad_entry_refined_candidate exclusion_reason=soft_stop_zone`으로 이미 soft stop zone 진입 뒤였고, 그 전 후보들은 `hold_too_short` 또는 `loss_too_shallow`였다. 즉 현재 rule 기준으로는 soft stop threshold 자체보다 앞서 refined bad-entry가 잡을 수 있었던 케이스가 아니다.
  - 손실 tail 근거 메모 (`2026-05-04 10:43 KST snapshot`): `scalp_soft_stop_pct` 완료 4건은 `RF머트리얼즈 -2.32%/-2,547원`, `SK네트웍스 -2.03%/-135원`, `하나마이크론 -1.62%/-698원`, `아톤 -1.67%/-139원`이다. post-sell 후보까지 포함한 당일 soft stop 표본은 6건 평균 `-1.97%`로, protect/trailing보다 손실 최소화 우선순위가 높다.
  - refined gate 근거: 장중 `10:30~10:45` health에서는 `bad_entry_refined_candidate=12`, unique record `4`, `bad_entry_refined_exit=0`, 전부 `exclusion_reason=loss_too_shallow`였다. threshold-cycle report 기준 `bad_entry_block.sample`은 `refined_candidate=800`, `refined_exit=0`, `soft_stop_zone_candidate=26`, `early_capture_candidate=0`, `exclusion_top.loss_too_shallow=626`이다. 장후에는 `soft_stop_zone_candidate=26`이 조기 포착 실패인지, 이미 soft stop zone 진입 후 후보인지 먼저 나눈다.
  - REVERSAL_ADD 보강 (`2026-05-04 09:56 KST`): 같은 리포트에 `reversal_add.sample.blocker_top/predicate_pass_counts/near_miss_all_but_hold/near_miss_all_but_ai_recovery`를 추가했다. RF머트리얼즈는 `09:38:40` `ai_score_too_low(57)`, `09:39:32` `ai_not_recovering`, `09:41:51` `pnl_out_of_range(-0.05)`, `09:42:24` `hold_sec_out_of_range(246s)`, `09:42:58` `pnl_out_of_range(-1.95)`로 막혔다. 핵심은 단일 soft stop threshold 문제가 아니라 `REVERSAL_ADD`의 시간창/AI회복/수익률창이 동시에 어긋난 복합 blocker다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_daily_threshold_cycle_report.py` -> `8 passed`; `PYTHONPATH=. .venv/bin/python -m src.engine.daily_threshold_cycle_report --date 2026-05-04 --print`로 report 재생성 완료.
  - 다음 액션: keep이면 다음 운영일 baseline 승격이 아니라 1일 추가 canary로 유지하고, OFF면 `adverse fill detector` 또는 `MAE/MFE quantile stop` 중 다음 계층을 단일축 후보로 재선정한다.
  - 장후 판정 (`2026-05-04 16:20 KST`): OFF. `bad_entry_refined_candidate=2628`, `bad_entry_refined_exit=37`까지 발생했지만, exit 후 실제 `COMPLETED + valid profit_rate` 성과로 닫힌 clean cohort가 아니라 `holding_flow_override` 유예와 교차됐다. exit 발생 종목은 `롯데에너지머티리얼즈(020150) 32건`, `쏠리드(050890) 3건`, `한국항공우주(047810) 1건`, `크래프톤(259960) 1건`이다.
  - 롤백 근거: `쏠리드`는 `15:16:19` `scalp_bad_entry_refined_canary`가 먼저 발생했고 flow override가 `HOLD`로 보류했다. 이후 `15:31~15:32` `scalp_soft_stop_pct` 주문이 장마감 이후 반복 거절(`sell_order_failed=98`, 에러 `[2000](999999:주문 불가능합니다.)`)됐다. 직접 exit_rule은 soft stop으로 바뀌었지만 canary-applied cohort가 주문 실패/flow-defer safety gap에 섞였으므로, 문서 rollback guard의 `sell_order_failed` 및 원인귀속 실패 조건에 해당한다.
  - 코드 조치: [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py) 기본값을 `SCALP_BAD_ENTRY_REFINED_CANARY_ENABLED=False`로 내렸다. env `KORSTOCKSCAN_SCALP_BAD_ENTRY_REFINED_CANARY_ENABLED=true` 없이는 다음 재기동부터 refined bad-entry 실청산은 발생하지 않는다.
  - 검증: runtime import `SCALP_BAD_ENTRY_REFINED_CANARY_ENABLED=False`, `AI_SCORE_50_BUY_HOLD_OVERRIDE_ENABLED=True`, `HOLDING_FLOW_OVERRIDE_ENABLED=True`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_sniper_scale_in.py -k 'bad_entry_refined'` -> `2 passed, 121 deselected`.
  - 다음 단일축 즉시 판정 (`2026-05-04 16:30 KST`): 5/6 장후로 넘겼던 다음 축 설계는 현재 데이터로 닫을 수 있으므로 `bad-entry report-only counterfactual enrichment`를 선택했다. `adverse_fill_detector`와 `MAE/MFE quantile stop`은 아직 live canary로 열지 않는다. 이유는 5/4 cohort가 flow defer/order failure와 교차돼 live 조작점보다 먼저 arbitration 순서와 excluded cohort를 복원해야 하기 때문이다.
  - 코드 조치: [constants.py](/home/ubuntu/KORStockScan/src/utils/constants.py)에 `SCALP_BAD_ENTRY_REFINED_OBSERVE_ENABLED=True`를 추가했고, [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)는 canary OFF 상태에서도 `bad_entry_refined_candidate`를 report-only로 남긴다. 이때 `canary_enabled=False`, `observe_only=True`, `should_exit=False`, `would_exit=True/False`를 분리해 live 실청산과 counterfactual 후보를 섞지 않는다.
  - 추가 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_sniper_scale_in.py -k 'bad_entry_refined'` -> `3 passed, 121 deselected`; `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_constants.py -k 'runtime_shadow_defaults_are_off'` -> `1 passed, 6 deselected`.
  - 재기동 반영 (`2026-05-04 16:40 KST`): `restart.flag` 우아한 재시작으로 main `bot_main.py` PID가 `60353 -> 62056`으로 교체됐고 flag는 소모됐다. import 기준 `SCALP_BAD_ENTRY_REFINED_CANARY_ENABLED=False`, `SCALP_BAD_ENTRY_REFINED_OBSERVE_ENABLED=True`, `HOLDING_FLOW_OVERRIDE_ENABLED=True`를 재확인했다.

- [x] `[TrailingProfitSensitivity0504-Postclose] trailing/protect 익절 민감도와 PYRAMID 후 수익 보전 판정` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 16:20~16:40`, `Track: ScalpingLogic`)
  - Source: [2026-05-04-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-05-04-stage2-todo-checklist.md), [trade_review_2026-05-04.json](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/trade_review_2026-05-04.json), [post_sell_candidates_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/post_sell/post_sell_candidates_2026-05-04.jsonl), [pipeline_events_2026-05-04.jsonl](/home/ubuntu/KORStockScan/data/pipeline_events/pipeline_events_2026-05-04.jsonl), [threshold_cycle_2026-05-04.json](/home/ubuntu/KORStockScan/data/report/threshold_cycle_2026-05-04.json)
  - 판정 기준: `scalp_trailing_take_profit`, `scalp_preset_protect_profit`, `protect_trailing_stop`, `protect_trailing_smooth_hold`를 `initial-only`와 `pyramid-activated`로 분리하고, `COMPLETED + valid profit_rate`, 청산 후 max/min 관측가, missed upside, 손실 회피, `PYRAMID` 실행/불발 여부를 합산하지 않고 본다.
  - 매도 민감도 원인귀속 메모 (`2026-05-04 KST`): OFI/QI `orderbook_micro`는 신규 매수/매도 판단축이라기보다 입력데이터 품질개선축으로 보는 것이 맞다. 다만 현재 구현은 그 품질개선 입력이 `dynamic_entry_ai_price_canary_p2`에만 배선돼 있고, 매도/보유 경로에는 아직 `exit_context.orderbook_micro`, exit prompt contract, post-sell MFE/MAE attribution이 없다. 따라서 매도 쪽 후속은 `OFI/QI를 live exit gate로 즉시 적용`이 아니라 `holding/exit 입력 context 품질 보강 및 report-only attribution`으로 시작한다. 현재 매도 민감도 후보는 [kiwoom_sniper_v2.py](/home/ubuntu/KORStockScan/src/engine/kiwoom_sniper_v2.py)의 `time_decay`, `weak_vpw_count`, `soft_stop_pct`, `scalp_trailing_take_profit` drawdown limit, [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py)의 `holding_flow_override_*`, `protect_trailing_smooth_*`에서 먼저 분리한다. OFI/QI 기반 입력 보강을 매도에 확장하려면 장후 판정 후 별도 `exit_orderbook_micro_context` workorder로 `report-only context enrichment -> attribution -> guarded canary 여부` 순서를 새로 열어야 한다.
  - 장중 근거 메모 (`2026-05-04 10:43 KST snapshot`): completed 기준 trailing은 `8/8` 승, 평균 `+1.431%`, 합계 `+12,611원`이다. 다만 수익 극대화 관점에서는 `파마리서치(214450)`가 청산 후 관측가 기준 추가 `+1.17%`, `롯데에너지머티리얼즈(020150)` 1차가 청산 후 추가 `+2.40%`, `산일전기(062040)`은 `scalp_preset_protect_profit`으로 `+0.09%` 본전권 청산 후 추가 `+0.81%`가 관측됐다.
  - PYRAMID 근거 메모: `비츠로테크(042370)`는 `09:54~09:55` PYRAMID 신호가 있었지만 10:05 이전 `zero_qty` 문제로 추가매수 불발 후 `+3.58%` 완료됐다. `머큐리(100590)`는 10:20/10:23 PYRAMID 2회 체결 후 최종 `+0.38%` 완료로, 불타기 후 trailing이 수익을 충분히 보전했는지 별도 확인 대상이다. `유안타증권(003470)`과 `한화투자증권(003530)`은 PYRAMID 이후 soft stop 판단까지 너무 빠르게 이어진 anchor로 추가하고, `scale_in_executed(PYRAMID)` 시각, `last_add_at/last_add_type`, post-add high rebase, `pyramid_post_add_trailing_grace`, `soft_stop_micro_grace`, `scalp_soft_stop_pct`, `holding_flow_override_*`, `bad_entry_refined_*`, 매도 후 `post_sell MFE/MAE`를 같은 record 기준으로 묶어 어떤 조건이 조급하게 반응했는지 장후 판정한다. `리노공업(058470)`/`한화비전(489790)`은 PYRAMID 후 protect trailing 민감도 표본으로 유지하되, 단일 tick 평탄화 hotfix 이후 표본과 전후를 섞지 않는다.
  - 불타기 후 물타기 bypass 점검 메모 (`2026-05-04 KST`): 코드상 `last_add_type=PYRAMID` 자체가 `REVERSAL_ADD`를 영구 차단하는 명시 분기는 없다. 다만 추가매수 레이어는 `SELL` 신호가 없을 때만 실행되고, PYRAMID 체결 후 공통 `SCALE_IN_COOLDOWN_SEC=180` 및 `ADD_JUDGMENT_LOCK_SEC=20`이 먼저 적용된다. 또한 `REVERSAL_ADD`는 원 보유 기준 `held_sec 20~180`, `profit_rate -0.70~-0.10`, `AI>=60`, AI 회복, 수급 3/4를 요구하므로 불타기 후 3분 내 하락하거나 원 보유시간이 이미 긴 표본은 사실상 물타기 평가/실행이 막힌다. 유안타증권은 `12:01:07` `reversal_add_gate_blocked gate_reason=scale_in_cooldown`, 이후 `12:01:37` `protect_trailing_stop` 완료로 확인됐다. 한화투자증권은 `12:02:54` `hold_sec_out_of_range(3220s)`, `12:03:27` `add_judgment_locked + hold_sec_out_of_range`, `12:03:50` `protect_trailing_stop` 완료로 확인됐다. 장후에는 이를 `명시 bypass 아님 / 쿨다운+시간창+SELL 우선순위에 의한 실질 bypass`로 분리한다.
  - 급락 신호 왜곡 점검 메모 (`2026-05-04 KST`): 위 표본을 `급락`으로 단정하지 않는다. PYRAMID 직후 가격 하락 판단은 단일 tick, spread 확대, thin depth, 체결강도 급변, `buy_pressure_10t`/`tick_acceleration_ratio`의 짧은 window 왜곡, protect trailing smoothing sample 수 부족으로 과민하게 보일 수 있다. 장후에는 `post_add_0~30/30~90/90~180초` 구간의 가격 경로, top3 depth, spread bps, large sell print, buy pressure, micro VWAP 이탈, protect smoothing `below_ratio/sample_count/sample_span_sec`를 함께 보고 `true_adverse_flow`와 `micro_noise/temporary_liquidity_vacuum`을 분리한다.
  - 롯데에너지머티리얼즈 추가 샘플 (`2026-05-04 13:20 KST`): `020150` id `4950`은 앞선 id `4928` `scalp_trailing_take_profit` 완료 후 revived watch로 이어진 최근 표본이며, `pyramid_count=1`, `last_add_type=PYRAMID`, `buy_qty=2` 상태에서 `protect_trailing_stop`으로 완료됐다. exit signal은 `13:20:33` `curr_price=78700`, `buy_price=79550`, `profit_rate=-1.30`, `peak_profit=+0.21`, `AI=54`, `drawdown_from_peak=1.51`, `spread_bps=63.53`, `top3_depth_ratio=0.2078`, `tick_acceleration_ratio=0.667`, `curr_vs_micro_vwap_bp=-38.18`였고, protect smoothing은 `sample_count=4`, `sample_span_sec=8`, `below_ratio=1.00`으로 확정됐다. 최종 체결은 `sell_price=78800`, `profit_rate=-1.17%`, `exit_rule=protect_trailing_stop`이다.
  - 롯데 샘플 판정 기준: 기존 롯데 1차/2차 익절 표본과 합치지 않고 `pyramid-activated + protect_trailing_stop loss` anchor로 본다. 장후에는 `PYRAMID` 체결 시점/가격, post-add high rebase, `protect_trailing_smoothing`이 손실 확대 차단으로 작동했는지 또는 `spread/thin depth/tick_accel` 왜곡에 과민 반응했는지, 청산 후 `post_sell MFE/MAE`가 생성됐는지를 확인한다.
  - 다음 액션: weak trailing limit `+10bp`, strong AI 경계 `75 -> 70`, protect trailing smoothing 적용 전/후를 report-only로 비교한다. `ThresholdOpsTransition0506` 전 live threshold mutation은 금지하고, 추천값은 manifest/report 근거로만 남긴다.
  - 장후 판정 (`2026-05-04 16:45 KST`): live 즉시 변경은 보류하고, 5/6 단일 owner에서 `scalp_trailing_take_profit` weak limit 완화와 `protect_trailing_smoothing` sample/span 보강을 report-only 후보로 재판정한다. trailing은 수익 실현 자체는 양호하지만 약한 구간이 너무 빨리 닫힌 표본이 많고, protect는 손실 확대 차단과 조급한 손절이 섞여 단일 수치 변경 근거가 부족하다.
  - 근거: `threshold_cycle_2026-05-04.json` 기준 `scalp_trailing_take_profit`은 `exit_signal=34`, `completed=34`, 평균 완료수익 `+1.2403%`, 평균 peak drawdown `0.6376%`다. 그러나 `weak_borderline=13`, `would_hold_if_weak_limit_plus_10bp=13`, `would_hold_if_strong_ai_score_relaxed_5pt=12`라 현행 weak limit `0.4%`와 strong AI 경계 `75`가 기대값을 깎는 후보로 남았다. `protect_trailing_smoothing`은 `smooth_hold=641`, `smooth_confirmed=27`, `protect_trailing_completed=16`, 완료 평균 `-0.9744%`이며 추천값은 `min_span_sec 8->18`, `min_samples 3->8`이다.
  - post-sell 근거: `post_sell_evaluations_2026-05-04.jsonl` 기준 `scalp_trailing_take_profit` 12건은 `MISSED_UPSIDE=4`, `GOOD_EXIT=4`, `NEUTRAL=4`, 평균 `+1.221%`다. `LS마린솔루션`은 청산 후 20분 MFE `+10.702%`, `머큐리`는 `+2.486%`로 수익 극대화 missed upside가 크다. 반면 `protect_trailing_stop` 6건은 `MISSED_UPSIDE=1`, `GOOD_EXIT=3`, `NEUTRAL=2`, 평균 `-0.998%`이고, `유안타증권/한화투자증권`은 20분 MAE가 각각 `-13.300%/-10.012%`라 손실 확대 차단 표본도 명확하다.
  - PYRAMID 분리: threshold report상 `pyramid_signaled_not_executed=16`, `pyramid_executed=3`으로 trailing 익절 표본 상당수가 불타기 실행/불발 상태와 얽혀 있다. `비츠로테크`, `롯데에너지머티리얼즈`, `머큐리`는 winner size-up과 trailing giveback을 분리해야 하므로 [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `PyramidDynamicQty0506` 및 신규 `TrailingProtectSensitivity0506`으로 넘긴다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_daily_threshold_cycle_report.py src/tests/test_sniper_scale_in.py -k 'trailing or protect_trailing or entry_price or orderbook_micro'` -> `7 passed, 128 deselected`.

- [x] `[OpenAIResponsesWS0504-Postclose] OpenAI transport shadow 유지/교체 판정` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 16:20~16:40`, `Track: Plan`)
  - Source: [2026-04-30-openai-enable-acceptance-spec.md](/home/ubuntu/KORStockScan/docs/ai-acceptance/2026-04-30-openai-enable-acceptance-spec.md), [workorder-shadow-canary-runtime-classification.md](/home/ubuntu/KORStockScan/docs/workorder-shadow-canary-runtime-classification.md)
  - 판정 기준: 현재 `main` runtime owner는 Gemini이므로 `OpenAI HTTP baseline` 자체가 실전 scalping baseline은 아니다. 따라서 장후에는 `1) 별도 diagnostic/shadow 실행으로 OpenAI WS 메트릭이 실제 수집됐는지`, `2) 수집됐다면 request_id/late_discard/fallback/timeout 기준을 만족하는지`, `3) 수집되지 않았다면 현재 runtime 미사용/backlog only로 분류할지`를 닫는다. 하나라도 미충족이면 `observe-only 유지` 또는 `runtime 미사용 backlog 유지`로 고정한다.
  - why: 이번 change set의 목적은 live 전환이 아니라 동일 계약 parity와 shadow transport 안정성 확인이며, 현재 실전 라우팅 owner와 혼동하지 않게 backlog 성격을 잠그는 데 있다.
  - cohort: `baseline cohort=Gemini live routing`, `candidate live cohort=none`, `observe-only cohort=openai_responses_ws_shadow_flag_off`, `excluded cohort=Gemini/DeepSeek routing 우열 비교 및 strategy alpha 판정`, `rollback owner=OPENAI_TRANSPORT_MODE + OPENAI_RESPONSES_WS_ENABLED`, `cross-contamination check=transport 판정과 strategy alpha 판정 분리`
  - 다음 액션: 변경이 있으면 checklist와 [2026-04-30-openai-enable-acceptance-spec.md](/home/ubuntu/KORStockScan/docs/ai-acceptance/2026-04-30-openai-enable-acceptance-spec.md), [workorder-shadow-canary-runtime-classification.md](/home/ubuntu/KORStockScan/docs/workorder-shadow-canary-runtime-classification.md)를 같이 갱신하고, 런타임 미사용이면 `AIEngineFlagOffBacklog0506` owner로 이관한다.
  - 장후 판정 (`2026-05-04 16:45 KST`): `runtime 미사용 backlog 유지`. OpenAI Responses WS는 유지/교체 판정을 할 실측 shadow 표본이 없다. 따라서 live/WS 전환은 열지 않고, 5/6 `AIEngineFlagOffBacklog0506`에서 `route open 여부`, `diagnostic/shadow metric 존재 여부`, `backlog only 유지 여부`를 다시 분류한다.
  - 근거: runtime import 기준 `OPENAI_RESPONSES_WS_ENABLED=False`, `OPENAI_TRANSPORT_MODE=http`, `OPENAI_RESPONSE_SCHEMA_REGISTRY_ENABLED=False`, `OPENAI_JSON_DETERMINISTIC_CONFIG_ENABLED=False`, `OPENAI_PREVIOUS_RESPONSE_ID_ENABLED=False`다. `pipeline_events_2026-05-04.jsonl`에서 `openai`/`responses_ws` 관련 stage는 0건으로, request_id mismatch/late discard/http fallback/timeout rate를 계산할 수 없다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_ai_engine_openai_transport.py src/tests/test_ai_engine_api_config.py` -> `16 passed, 1 warning`. 이는 코드 계약 회귀검증이며, 실측 transport acceptance 통과 근거로 쓰지 않는다.

- [x] `[OrderbookMicroP2Canary0504-Postclose] OFI/QI P2 내부 feature provenance 및 bucket calibration 점검` (`Due: 2026-05-04`, `Slot: POSTCLOSE`, `TimeWindow: 16:20~16:40`, `Track: ScalpingLogic`)
  - Source: [2026-05-02-stage2-todo-checklist.md](/home/ubuntu/KORStockScan/docs/checklists/2026-05-02-stage2-todo-checklist.md), [2026-05-03-ofi-audit-response-result-report.md](/home/ubuntu/KORStockScan/docs/audit-reports/2026-05-03-ofi-audit-response-result-report.md), [plan-korStockScanPerformanceOptimization.rebase.md](/home/ubuntu/KORStockScan/docs/plan-korStockScanPerformanceOptimization.rebase.md), [sniper_state_handlers.py](/home/ubuntu/KORStockScan/src/engine/sniper_state_handlers.py), [orderbook_stability_observer.py](/home/ubuntu/KORStockScan/src/trading/entry/orderbook_stability_observer.py)
  - 판정 기준: `SCALPING_ENTRY_PRICE_ORDERBOOK_MICRO_ENABLED=True` runtime provenance와 `captured_at_ms/snapshot_age_ms`, `observer_healthy/missing_reason`, `observer_last_quote_age_ms/trade_age_ms`, `micro_window_sec/micro_z_min_samples/micro_lambda`, `ofi_threshold_source/bucket_key/manifest_id/version/fallback_reason`, `entry_ai_price_skip_policy_warning/basis` 적재를 확인한다.
  - bucket calibration 기준: `SCALPING_ENTRY_PRICE_ORDERBOOK_MICRO_BUCKET_CALIBRATION_ENABLED` 기본값은 `False`이며, ON이면 `data/config/ofi_bucket_threshold_manifest.json`의 bucket threshold만 적용한다. manifest missing/invalid/bucket miss/sample 부족은 global threshold fallback으로 닫고 `ofi_threshold_fallback_reason`을 남긴다.
  - 장후 분포 확인: `ofi_orderbook_micro_states`, `ofi_orderbook_micro_threshold_sources`, `ofi_orderbook_micro_buckets`, `ofi_orderbook_micro_warnings`, `symbol_anomalies`를 확인한다. 이는 bucket runtime calibration 품질과 종목별 anomaly watch를 보기 위한 것이며, 종목별 runtime threshold 적용 근거로 쓰지 않는다.
  - 민감도 점검 메모 (`2026-05-04 KST`): 장중 P2 표본은 `entry_ai_price_canary_applied=46`, `entry_ai_price_canary_skip_order=4`, `entry_ai_price_canary_skip_followup=2`였고, SKIP 4건 중 2건은 `entry_ai_price_skip_policy_basis=neutral` 및 `skip_without_bearish_ofi`였다. `USE_DEFENSIVE=44`라 OFI/QI 도입 후 전반적 민감도는 주문가 방어 쪽으로 완화됐지만, neutral OFI/QI 상태에서 AI가 latency/depth 이유로 SKIP을 낸 경우 현재 runtime은 경고만 남기고 차단을 그대로 허용한다. 장후에는 `bearish_supported SKIP`과 `neutral warning SKIP`을 분리하고, neutral/insufficient는 `USE_DEFENSIVE` 또는 P1 fallback으로 demote할지 별도 단일축 후보로만 검토한다.
  - 적용 검토 보류 메모 (`2026-05-04 KST`): 사용자 결정에 따라 `SKIP -> USE_DEFENSIVE/P1 fallback` demotion guard 적용 여부는 장중 즉시 반영하지 않고 장후 판정 후 다시 검토한다. 장후 전에는 코드/env/restart 변경을 열지 않는다.
  - 진입 기회비용 근거 메모 (`2026-05-04 10:43 KST snapshot`): [wait6579_ev_cohort_2026-05-04.json](/home/ubuntu/KORStockScan/data/report/monitor_snapshots/wait6579_ev_cohort_2026-05-04.json) 기준 후보 `113`, entered attempts `2`, missed attempts `111`, entered rate `1.8%`다. terminal blocker는 `blocked_ai_score=62`, `blocked_strength_momentum=42`, `blocked_overbought=4`, `blocked_liquidity=2`, `latency_block=1`이다. 평균 기대 EV는 `blocked_overbought +14.5158%`, `blocked_liquidity +16.4522%`, `blocked_strength_momentum +4.5769%`, `blocked_ai_score +4.4282%`로 찍혀 있어, 장후에는 손실회피보다 기대값 회수 가능성이 큰 blocker부터 MFE/MAE를 재확인한다.
  - 금지 범위: `OFI standalone hard gate`, `watching/holding/exit` 확장, symbol-level runtime threshold, threshold 자동 live mutation은 별도 workorder 없이는 금지한다. manifest 적용은 env/code 반영과 restart 기반으로만 한다.
  - rollback guard: bucket calibration 이상 시 `SCALPING_ENTRY_PRICE_ORDERBOOK_MICRO_BUCKET_CALIBRATION_ENABLED=False`가 owner다. observer health 미수신, stale snapshot, fallback 급증, `SKIP`의 `ofi_not_ready`/`skip_without_bearish_ofi` 급증은 bucket calibration OFF 또는 manifest 보류 사유로 기록한다. P2 parse fail/stale context/pre-submit guard 위반은 기존 P1 resolver fail-closed를 유지한다.
  - 다음 액션: OFI/QI는 P2 내부 live 입력 feature로 유지한다. 장후 해석은 provenance와 calibration 품질 확인이며, 도입효과 측정 기반 live 승인/keep/OFF 결정으로 사용하지 않는다.
  - 장후 판정 (`2026-05-04 16:45 KST`): OFI/QI P2 내부 feature는 유지한다. 다만 bucket calibration은 계속 OFF이고, neutral/insufficient SKIP은 live demotion 없이 5/8 `OFIQExpansionLadder0508`의 `SKIP -> USE_DEFENSIVE/P1 fallback` 후보 근거로만 남긴다.
  - provenance 근거: runtime import 기준 `SCALPING_ENTRY_PRICE_ORDERBOOK_MICRO_ENABLED=True`, `SCALPING_ENTRY_PRICE_ORDERBOOK_MICRO_BUCKET_CALIBRATION_ENABLED=False`다. `pipeline_events_2026-05-04.jsonl` 기준 P2 관련 stage는 111건이고, action 분포는 `USE_DEFENSIVE=96`, `SKIP=8`, `USE_REFERENCE=3`, `IMPROVE_LIMIT=2`, 기타 2건이다. `orderbook_micro_ready=True` 106건, `False` 3건, observer health는 `True` 109건이며 missing reason은 대부분 `ok`다.
  - bucket/calibration 근거: threshold source는 `global=109`, `-` 2건이며 manifest id/version은 비어 있다. 이는 bucket calibration이 켜진 것이 아니라 global threshold fallback으로 정상 동작한 것이다. bucket key는 `spread=wide|price=high|depth=normal|sample=rich` 35건, `spread=wide|price=mid|depth=thick|sample=rich` 16건, `spread=wide|price=low|depth=thick|sample=rich` 14건 등으로 분포는 적재됐다.
  - 민감도 근거: micro state는 `neutral=99`, `bearish=7`, `insufficient=3`, `-` 2건이다. SKIP 8건 중 `ofi_bearish_supported=6`, `neutral=2`이고 `skip_without_bearish_ofi=4` warning이 남았다. 따라서 OFI/QI 완화로 전반적 주문가 선택은 `USE_DEFENSIVE` 중심으로 안정화됐지만, neutral 상태에서 AI가 latency/depth 사유로 SKIP을 유지한 표본은 아직 불필요하게 민감할 수 있다.
  - 검증: `PYTHONPATH=. .venv/bin/pytest -q src/tests/test_daily_threshold_cycle_report.py src/tests/test_sniper_scale_in.py -k 'trailing or protect_trailing or entry_price or orderbook_micro'` -> `7 passed, 128 deselected`.

<!-- AUTO_SERVER_COMPARISON_START -->
### 본서버 vs songstockscan 자동 비교 (`2026-05-04 15:47:07`)

- 기준: `profit-derived metrics are excluded by default because fallback-normalized values such as NULL -> 0 can distort comparison`
- 상세 리포트: `data/report/server_comparison/server_comparison_2026-05-04.md`
- `Trade Review`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Performance Tuning`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Post Sell Feedback`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
- `Entry Pipeline Flow`: status=`remote_error`, differing_safe_metrics=`0`
  - safe 기준 차이 없음
<!-- AUTO_SERVER_COMPARISON_END -->
