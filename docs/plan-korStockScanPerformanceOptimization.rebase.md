# KORStockScan Plan Rebase Core Document

Baseline date: `2026-06-05 KST`
Role: This is the core document that pins only the current tuning principles, automation-chain decision contracts, and active/open state.
Note: This document does not own auto-parsed checklist items. Executable work items are owned by the daily `stage2 todo checklist`.

Tuning data decision baseline: clean tuning data starts at `2026-06-05 KST`, with full-day start timestamp `2026-06-05T00:00:00+09:00`. Raw/report/analytics data before this baseline is `archive_only_not_allowed_for_clean_tuning` and must not be used for EV, rolling/MTD/cumulative tuning, live-auto promotion, runtime approval, pattern lab promotion, or real execution quality approval. The policy artifact is `data/source_quality/clean_baseline_policy.json`; report and analytics residue enforcement is owned by [report-based-automation-traceability](./report-based-automation-traceability.md) and `threshold_cycle_postclose_verification`.

Closed manual execution maps, deprecated latency/fallback/shadow axes, expired owner-decision notes, and runtime override migration records are archive evidence only. Current operating decisions stay in this document's active/open state, the current daily checklist, and target-date artifacts; historical detail belongs in `docs/archive/` or execution-delta records.

---

## 1. Current Decision State

1. The current phase is not loss-suppression micro-tuning. It is automation-chain tuning for `expected value and net-profit maximization`.
2. The central loop is `R0_collect -> R1_daily_report -> R2_cumulative_report -> R3_manifest_only -> R4_preopen_apply_candidate -> R5_bounded_calibrated_apply -> R6_post_apply_attribution`. Detailed artifact and consumer contracts follow [report-based-automation-traceability](./report-based-automation-traceability.md).
3. The runtime/strategy period baseline is `main-only`, `normal_only`, and `post_fallback_deprecation`.
4. The tuning data decision baseline is `clean_tuning_baseline_date=2026-06-05` and `clean_tuning_baseline_ts_kst=2026-06-05T00:00:00+09:00`. For target dates on or after `2026-06-05`, automation-chain tuning decisions may consume only clean-baseline-allowed raw/report/analytics inputs. Pre-baseline raw, report, DuckDB, and Parquet artifacts are archive/audit evidence only and cannot support EV, live-auto, runtime approval, pattern lab promotion, or real execution quality approval. Postclose `observation_source_quality_audit` is a preflight hard gate for contract gaps at the defective row/window level: required-field missing, invalid label, or contract warning/fail rows must be excluded from tuning input through `raw_row_exclusion` before EV, rolling/MTD/cumulative tuning, live-auto promotion, or runtime approval continues. The whole target date is blocked only when the preflight artifact is missing/invalid, row/window exclusion cannot be produced, or high-volume no-contract coverage makes the affected rows non-isolatable. Producer fixes and workorder handoff remain required after exclusion. Unknown-token findings alone remain review warnings, not immediate tuning stops.
5. Real trading changes default to a single owner canary within the same stage. Stage-disjoint concurrent canaries are allowed only when stage, manipulation point, cohort tag, and rollback guard are separated.
6. Operational overrides are recognized only when they leave an explicit user instruction, source/provenance, rollback env, and safety priority. The scheduled automation-chain path is postclose artifact -> PREOPEN `auto_bounded_live`; an explicit intraday override follows the `runtime_mutation_guard` contract. Closed override records are archive evidence only.
7. `lifecycle_decision_matrix_runtime` is the ADM expansion umbrella owner. It wraps existing Entry ADM, Holding/Exit ADM, submit observation, and scale-in bias adapter as `entry/submit/holding/scale_in/exit` stage-level weighted ADM policy. Its scheduled policy file/version/promote-cap handoff uses the PREOPEN env; an explicit intraday override may modify one existing bounded axis under `runtime_mutation_guard`. Stage runtime effect remains bounded by hard-safety priority.
8. The default scalping live AI route remains OpenAI except for explicit endpoint-specific overrides. `entry_price` routes to Bedrock Qwen3 32B primary with Nova Lite v2 failback and no OpenAI third fallback; if both Bedrock calls fail, the existing defensive entry-price fallback closes the call. `holding_flow` routes to Bedrock Nova Lite v2 primary with OpenAI failback. Removed Bedrock shadow/comparison surfaces are not active report inventory; current Bedrock evidence is limited to primary/failback provider audit rows. Other Tier2 endpoints stay on OpenAI Responses WS. Gemini/DeepSeek fallback is treated only as explicit env behavior, OpenAI initialization failure, or a non-operational analysis path.
9. Swing is a sim-first LDM self-improvement chain. The fixed flow is `swing_strategy_discovery_sim -> labels / EV -> swing_lifecycle_decision_matrix -> swing_lifecycle_bucket_discovery -> swing_sim_auto_approval / swing_sim_policy_catalog -> PREOPEN sim policy env -> runtime sim/probe observation -> postclose attribution -> final live conversion approval only when complete LDM parent bucket evidence is closed`. An `arm` is a strategy recipe unit such as `arm05_breakout_conf_trailing` and remains the source-only EV and active sim priority unit. A `bucket` is an LDM attribution unit such as `entry_bucket`, `holding_exit_bucket`, `scale_in_bucket`, or complete `swing_lifecycle_flow_bucket`; live conversion judgment uses complete parent flow-bucket evidence, not a thin stage-only bucket. `swing_runtime_approval` may surface dry-run env pre-final candidates and final full-live approval requests, but there is no current one-share or scale-in broker-submit trial path. If swing entry satisfies `entry_unique >=10`, `submitted=0`, a major blocker floor, or a probe/sim conversion gap, surface `SWING_ENTRY_DROUGHT_CRITICAL` automatically and hand it off as source-only to `swing_improvement_automation`, Swing LDM, `swing_lifecycle_bucket_discovery`, `code_improvement_workorder`, and the postclose verifier. This path has `operator_action_required=false`, `runtime_effect=false`, and `allowed_runtime_apply=false`; it must not execute real orders, provider route changes, bot restarts, or threshold live mutation. Postclose `swing_threshold_ai_review` defaults to OpenAI through its individual `gpt-5.4-mini` medium reviewer config; the 20:15 lightweight wrapper defaults to provider `none` to avoid postclose resource contention before the main chain starts.
9a. `entry_cancel_wait_runtime` is a standalone operational tuning family, separate from ADM/LDM, lifecycle buckets, general threshold EV, and runtime apply bridge. It owns BUY-order cancel timeout only. Postclose deterministic counterfactual EV may adjust `standard/breakout/pullback/reserve` thresholds within daily bounds, and the next PREOPEN applies them. The family activates on `2026-06-15`, remains ON across hold/warning/missing-report states, and may turn OFF only through an explicit operator lock. Entry-price AI `max_wait_sec` is advisory and cannot directly override the live timeout.
10. Sim/probe/counterfactual data can support source bundles and approval requests, but it is not standalone evidence for real execution quality or real-order conversion.
11. Sentinel, panic sell, and system error detector are operational/attribution/source-quality inputs. The panic-buying feature family was removed on `2026-08-08`; its historical artifacts are archive/audit evidence only and have no current producer, consumer, runtime, or reactivation path. Report-only inputs must not directly change thresholds, providers, orders, automated selling, or bot restarts.
12. Submit drought is forcibly surfaced by `BUY Funnel Sentinel`. If `submitted/ai < 20.0%` or `submitted/budget <= 10.0%`, with floors of `ai_confirmed unique >=20` and `budget_pass unique >=3` respectively, close it as `SUBMIT_DROUGHT_CRITICAL` and automatically hand it off to postclose code-improvement workorder and LDM attribution. This path has `operator_action_required=false` and creates downstream workorder/attribution without separate approval. It still must not bypass threshold/order/provider/bot restart, broker/stale/account/order/quantity/cooldown guards. `latency_classifier_runtime_profile` is not the CAUTION submit-block owner; it audits DANGER/stale/broker submit quality. Runtime `EntryPolicy` sends `SAFE` and `CAUTION` to normal submit after slippage check, and blocks only `DANGER`, stale quote, and broker/account/order/quantity/cooldown guards. Adaptive latency env apply defaults to `allowed_runtime_apply=false` until separate proof/workorder exists.
13. `lifecycle_bucket_discovery` owns postclose discovery and classification of all lifecycle buckets. Bucket tuning is not a finer-and-finer combo split exercise; its purpose is to preserve child pattern detail as dimensions/provenance while widening canonical parent buckets enough to accumulate usable sim samples, detect new daily patterns, confirm parent EV through rolling/MTD windows, isolate only conflicting child patterns as exclusion candidates, and narrow parent-level sim/live candidates faster. Existing buckets are refined through parent/child refinement, and combinations not explained by the existing taxonomy are registered as new bucket candidates only when they cannot be absorbed into an existing parent/dimension contract. After deterministic first-pass output, an AI Tier2 OpenAI reviewer validates with an English prompt through `first interpretation -> second review -> final conclusion`. The AI reviewer cannot newly promote deterministic live candidates, but it also must not block live conversion merely due to about-1% effects, low confidence, new buckets, or ambiguity. AI unavailable/parse fail is fail-closed for pre-final live auto-promotion and downgrades deterministic live candidates to `runtime_blocked_contract_gap` until the next parsed Tier2 review. Parsed ambiguous/non-contract objections do not block; block only explicit source-quality/schema/env mapping/runtime hook/post-apply attribution/safety/broker/stale/quantity/cooldown/provider/cap/forbidden-use gaps as `runtime_blocked_contract_gap` or `code_patch_required`. `sim_auto_approved` applies to the next PREOPEN sim policy without human approval, and a scale-in or complete-lifecycle bridge candidate closed as `live_auto_apply_ready` is consumed as a next PREOPEN live auto env candidate without the old human approval artifact only when Tier2 status is `parsed`. It still must not bypass hard safety, stale quote, broker/account/order/quantity/cooldown guards, provider route, bot restart, or position cap release.
14. The current focus is `Active Sim Priority`: rare positive scalping parent buckets and swing superior arms/buckets must move from postclose discovery into next-day sim sample collection priority through the existing threshold-cycle chain. The success criterion is not finding a single attractive bucket in a report; it is closing the same key through producer catalog -> PREOPEN apply plan/runtime policy file -> intraday event/sim target observation -> postclose verifier. Natural match count of 0 is a warning, not a strategy failure, when catalog and PREOPEN handoff are intact.
15. Active Sim Priority is sim-only. It must keep `actual_order_submitted=false`, `broker_order_forbidden=true`, `runtime_effect=false`, and no provider/bot/cap/order/threshold authority. Scalping runtime matching uses only observable prefix fields such as `entry_score_parent`, `entry_source_parent`, and optionally `submit_quality_parent`; posterior labels such as `exit_outcome_parent`, `major_holding_parent`, and `scale_in_parent` are validation dimensions only. Swing priority tags matching arms/buckets without creating duplicate arms or changing real-order authority.
16. Internal runtime labels must use canonical English contract values. Legacy Korean labels may be normalized only through an explicit compatibility map; a newly generated undefined label is a source-quality defect, not a new bucket by itself. Unknown `flow_state` or gatekeeper action labels are surfaced by `observation_source_quality_audit` as `invalid_label_findings`, route to `source_quality_blocker`, and fail the relevant contract until the producer prompt/schema/parser is fixed.
17. The current daily execution owner is the current dated stage2 checklist. The retired WAIT6579 entry-only bridge has no producer, approval contract, PREOPEN consumer, or reactivation path. `wait6579_ev_cohort` remains an entry-provenance dimension inside LDM and source-quality analysis only. Real runtime judgment uses selected probe families plus complete LDM lifecycle buckets; `greenfield_real_environment_authority` and any full-live/cap/provider/bot changes remain blocked unless a separate approved artifact exists.

## 2. Terms And Decision Contracts

| Term | Current Meaning | Decision Rule |
| --- | --- | --- |
| `auto_bounded_live` | Mode that applies only families that passed deterministic guard, AI correction guard, and same-stage owner rule to the next preopen runtime env. | Current threshold apply boundary |
| `manifest_only` | State that creates candidates and recommendations without changing runtime env. | Default when approval/guard requirements are not met |
| `report_only` / `observe_only` | State used only for source bundles, workorders, approval requests, and operator review without changing runtime decisions. | No live-change authority |
| `operator_runtime_override` | State where the user explicitly instructed real application through runtime env and bot restart. | Must leave source/provenance and rollback env; must not bypass safety guards |
| `lifecycle_matrix_runtime_policy` | Umbrella runtime policy that interprets each candidate lifecycle row as a stage-level weighted ADM proposal. | Must not bypass hard safety or broker guards; no runtime effect before selected PREOPEN env |
| `fixed_threshold_contract` | Contract that reclassifies existing thresholds as `hard_safety`, `baseline_prior`, `bounded_tunable`, and `legacy_archive`. | Score alone must not decide BUY/WAIT/DROP; no use outside assigned role |
| `approval_required` | Legacy/manual state where the automation chain created a final-stage candidate that still requires user approval. | Use only for final full-live conversion, cap release beyond bounded limits, provider/bot changes, or hard/protect/emergency safety relaxation |
| `sim_auto_approved` | State where lifecycle bucket discovery automatically applies the item to the next PREOPEN sim policy without human approval. | Real order/provider/bot/cap changes are forbidden |
| `active_sim_priority` | Dynamic sim-only priority generated from positive scalping parent buckets and swing superior arms/buckets, then consumed by next PREOPEN sim policy catalog and runtime observation. | Must use catalog keys, not code constants. Runtime may reserve sim sample quota but must not bypass `max_daily`, `max_open`, hard safety, broker/order guards, real orders, provider, bot, cap, or threshold authority |
| `live_auto_apply_ready` | State where discovery/bridge closed deterministic contract, env mapping, runtime hook, post-apply attribution, at least 1% primary EV uplift, and parsed AI Tier2 2-pass review found no explicit gap, so the item is passed as a next PREOPEN live auto-apply candidate. | AI has no new promotion authority. AI unavailable/parse fail blocks pre-final auto-promotion; parsed ambiguity alone does not block. Hard/broker/stale/quantity/cooldown guard bypass is forbidden |
| `safety_veto` | Immediate block condition such as severe loss, order failure, provenance damage, or hard/protect/emergency stop delay. | Daily-only evidence may block |
| `source_quality_blocker` | State where stale/missing/duplicate/provenance defects block edge judgment. | Not a threshold candidate |
| `instrumentation_gap` | State where required observation fields or contracts are missing and the decision cannot be closed. | Instrumentation must be improved first |
| `invalid_label_findings` | Source-quality finding raised when a producer emits a label outside the canonical contract, such as unknown holding `flow_state` or gatekeeper action values. | Fail the source contract; fix producer prompt/schema/parser instead of treating it as a valid new tag |
| `fallback_scout/main`, `fallback_single`, `latency fallback split-entry` | Historical exceptional entry/split-entry paths. | Permanently deprecated. Reopening requires a new workorder and rollback guard |
| `shadow` | Former validation mode that computed in parallel without affecting real orders. | Forbidden for new/supplemental alpha axes |

New observation metrics consumed by the automation chain must declare `metric_role`, `decision_authority`, `window_policy`, `sample_floor`, `primary_decision_metric`, `source_quality_gate`, and `forbidden_uses` at creation time. Undeclared metrics route only to `instrumentation_gap` or `source_quality_blocker`.

| Metric Role | Purpose | Forbidden Use |
| --- | --- | --- |
| `primary_ev` | Primary decision metric for expected value and net-profit improvement. | Must not be replaced by `win_rate` or simple sums |
| `diagnostic_win_rate` | Supporting diagnostic for directionality, consistency, and tail risk. | Must not approve live/canary by itself |
| `funnel_count` | Participation, blocker, coverage, and submitted-drought judgment. | Must not be used alone as PnL edge evidence |
| `safety_veto` | Severe loss/order/provenance safety block. | Must not be used as an expected-value improvement metric |
| `source_quality_gate` | Stale/missing/duplicate/provenance quality gate. | Must not be used as a threshold recommendation value |
| `active_unrealized` | Open sim/probe/position context. | Must not be summed with closed EV |
| `execution_quality_real_only` | Real broker execution/receipt quality. | Must not be replaced by sim/probe |
| `sim_probe_ev` | Sim/probe equal-weight expected value observation. | Must not be standalone evidence for real-order conversion |
| `risk_regime_state` | State value that interprets allowed action scope for panic/market regimes. | Must not directly change orders, exits, thresholds, providers, or bot state without approval artifact and rollback guard |

## 3. Tuning Principles

### 3.1 Performance Decisions And Data Criteria

1. The final goal is not loss suppression, but expected value and net-profit maximization.
2. PnL decisions use only `COMPLETED + valid profit_rate`. Exclude `NULL`, incomplete, and fallback-normalized values from PnL criteria.
3. Do not merge `full fill` and `partial fill`. Fill quality, downstream exit, and PnL are separated.
4. Comparison priority is `trade_count -> funnel -> blocker -> fill_quality -> missed_upside -> PnL`. Do not sum `counterfactual` values with directly realized PnL.
5. Win rate is `diagnostic_win_rate`, not standalone live/canary approval criteria. `primary_ev` owns expected value and net-profit decisions.
6. Simple PnL sum is not EV. Name it `simple_sum_profit_pct`; EV fields must be one of `equal_weight_avg_profit_pct`, `notional_weighted_ev_pct`, or `source_quality_adjusted_ev_pct`.
7. Daily-only values may be used for incidents, safety vetoes, freshness/source-quality, and intraday operational triggers. Edge apply approval must also check rolling/cumulative or `post_apply_version_window`.
8. If attribution is unclear, inspect report consistency, event reconstruction, and aggregation quality before changing strategy thresholds.
9. User-facing tuning performance reports must not lead with a thin headline count as if the count alone were success or failure. For LDM, bucket, active-sim-priority, scalp sim, and swing sim reports, present the result in this order: `decision -> evidence -> next action`. The decision must answer whether the flow/bucket/arm was identified with valid source quality, whether it was actually handed off to the sim policy catalog/PREOPEN env/runtime observation, and what exact blockers remain before real runtime reflection. Evidence must separate complete lifecycle flows, stage-only buckets, sim-auto approvals, active priority matches, EV fields, sample floors, source-quality warnings, submit drought, bridge/live-auto contract status, AI review status, and post-apply attribution. A small `complete lifecycle flow` count is not automatically a failure when catalog/PREOPEN handoff and natural-match semantics are intact; it is early evidence that must be reported with sim application status and real-runtime remaining work. Source-quality unknowns are not described as a current real-trading blocker when the candidate has not reached real-trading authority; they are evidence-quality warnings that must be resolved, excluded, or explicitly marked not-applicable before the same data can support live-auto, real runtime reflection, or full-live approval. Bridge/live-auto reports must not stop at "contract not closed"; they must enumerate each blocker by class (`source_quality`, `sample_floor`, `submit_drought`, `env_mapping`, `runtime_hook`, `post_apply_attribution`, `AI_review`, `safety_or_broker_guard`, `user_authority`), owner artifact, observed evidence, next repair action, and the acceptance test that would move the blocker forward.

### 3.2 Automation-Chain Decision Principles

1. The scheduled automation-chain unit is `postclose report/calibration/AI review -> PREOPEN runtime env -> post-apply attribution`. An explicit operator-directed intraday override may change exactly one existing `bounded_tunable` axis for one explicit KRX or NXT cohort only with fresh conflict-free source quality, effective price, target-first or sufficient post-block MFE, a single-blocker causal link, and no same-stage owner/canary conflict. The task instruction must fix the stage owner, evidence, before/after values, runtime env/PID provenance, rollback value/trigger, and immediate post-apply attribution. It must not create a competing owner or alter a common/KRX surface from NXT evidence.
2. Unmet conditions close as calibration states, not rollback: `adjust_up`, `adjust_down`, `hold`, `hold_sample`, `hold_no_edge`, or `freeze`.
3. Rollback/safety revert is used only for hard/protect/emergency stop delay, order failure, provenance damage, same-stage owner conflict, or severe loss guard breach.
4. AI correction and pattern lab automation are reviewer/proposer/work-instruction layers. They have no standalone runtime apply authority without deterministic guard.
5. Runtime-loaded AI system/user prompts, reviewer prompts, JSON classifier prompts, and schema instructions use English ASCII as the default contract. Internal decision role descriptions, rules, JSON field descriptions, and fallback reasons remain English; Korean user-facing text is separated into a formatting contract. Korean briefing/Telegram text shown directly to users and contract-required canonical labels are exceptions. Future edits to prompt-contract areas must keep this English standard and must not reintroduce Korean internal prompt wording.
6. Code-improvement workorders may be consumed by unattended implementation automation when the work stays in source-quality, parser/schema, report, test, documentation, instrumentation, or sim/source-only improvement scope. A `runtime_effect=false` workorder is allowed to produce repo changes and validation artifacts without separate user approval, provided it does not change real runtime authority. User approval and real-runtime guards are required only when the implementation would convert sim/probe/source-only evidence into real order authority, PREOPEN live env selection, provider route, bot process state, cap release, broker/order guard behavior, or hard/protect/emergency safety behavior.
7. Existing fixed thresholds are not deleted; their roles change. Broker submit/stale quote/price freshness/stop/account/order/quantity/cooldown guards are `hard_safety`; `BUY_SCORE_THRESHOLD` and entry score cutoff/VPW/strength/momentum families are candidate-generation `baseline_prior`; score65_74/soft stop/holding/scale-in price guard are `bounded_tunable`; latency DANGER/stale/broker submit block is `hard_safety_submit_quality`; fallback/legacy latency/shadow axes are `legacy_archive`.
8. The runtime decision contract removes the assumption that higher score monotonically increases EV. Matrix may consume score as a feature, but score bucket itself must not be a fixed policy or standalone action authority.
9. LDM/ADM and `lifecycle_bucket_discovery` must not silently pass when bucket, runtime candidate, code-improvement workorder, or sim-auto approval is created but downstream consumers miss the handoff. If any surfaced bucket handoff is missing from `threshold_cycle_ev`, `runtime_approval_summary`, `code_improvement_workorder`, `runtime_apply_bridge`, or `threshold_cycle_postclose_verification`, postclose verification closes as `automation_handoff_gap`. When collection-layer source, bucket field/type, or dimension key is added/deleted/changed, discovery compares the LDM source contract snapshot and surfaces `source_contract_changes`; additions route to `new_bucket_candidate`, while deletions/field loss route to `code_patch_required` or source-quality blocker.
10. Canonical runtime label contracts are part of source quality. Prompt/schema producers may emit only declared English labels, and consumers must normalize only documented legacy aliases. Any undefined label is an explicit fail condition that blocks downstream promotion until the contract or producer is deliberately updated with tests.
11. Bucket tuning follows a daily-plus-cumulative separation: daily buckets are for new-pattern/source-quality/conflict-child detection, while parent-level sim/live authority must use rolling/MTD or post-apply version confirmation. Child combo buckets remain available for diagnosis, dominant pattern explanation, and exclusion-dimension candidates, but daily-only child EV or a single thin child sample must not become live authority. Parent granularity should avoid both one super-bucket and thin over-fragmentation; target-pass parent buckets are the promotion unit, and conflict children narrow the policy instead of blocking the entire parent by default.

### 3.3 Canary, Cohort, And Live Change Principles

1. Only one live canary is allowed within the same stage. Bounded multi-arm canaries within the same family are allowed only when arm-level provenance, allocation, and rollback guards are closed.
2. Canary-to-live conversion must close `N_min`, primary EV improvement, no rollback guard breach, applied/not-applied cohort comparison, no cross-contamination, and restart/rollback path.
3. New/supplemental alpha axes must not open as shadow-only. Support axes that do not change real order/exit decisions, such as transport/schema, report-only decision support, and counterfactual enrichment, are allowed only when documented as observe/report-only.
4. Sim/probe/combined/counterfactual authority must not be standalone evidence for real execution quality or broker order enablement. Swing has no direct broker-submit trial approval path; actual trading reflection opens only through final full-live conversion approval after complete LDM parent bucket evidence, parsed review, source-quality gates, and explicit user approval artifact are closed.

### 3.4 Document And Execution Ownership

1. Plan Rebase owns only current principles and active/open decisions.
2. Daily checklists own executable work, absolute times, `Due`, `Slot`, `TimeWindow`, and `Track`.
3. Report traceability and threshold README own automation artifacts, consumer contracts, and apply contracts.
4. Completed old checklist items are evidence, not current OPEN owners.
5. Baseline documents such as README, runbook, Plan Rebase, prompt, and AGENTS are updated only when the user explicitly asks.
6. Keep those document updates separate from runtime/order/provider/bot/threshold changes. When needed, close them through first edit, second review, final supplement, and parser validation.
7. Historical content is not accumulated in the current snapshot; preserve it in archive or execution-delta.
8. AI runs parser validation after document changes. The user manually runs Project/Calendar sync with the standard command.

## 4. Automation-Chain Baseline

| Stage | Current Baseline | Live Impact |
| --- | --- | --- |
| `R0_collect` | Collect pipeline event, threshold compact event, DB completed trade, and monitor snapshot. | None |
| `R1_daily_report` | Generate Sentinel, panic, performance, and daily threshold reports. | None |
| `R2_cumulative_report` | Generate rolling/cumulative cohort and owner baseline. | None |
| `R3_manifest_only` | Create candidate families and source bundles without env mutation. | None |
| `R4_preopen_apply_candidate` | Check deterministic, AI, source-quality, and same-stage guards. | Pre-change stage |
| `R5_bounded_calibrated_apply` | Apply only guard-passing families to the next preopen runtime env. | Yes |
| `R6_post_apply_attribution` | Submit selected/applied/not-applied cohort, daily EV, and approval summary. | None |

The automation chain reuses existing source bundles instead of adding observation axes indefinitely. Preferred sources are `buy_funnel_sentinel`, `wait6579_ev_cohort`, `missed_entry_counterfactual`, and `performance_tuning` for BUY; `lifecycle_decision_matrix`, `scalp_entry_action_decision_matrix`, `holding_exit_decision_matrix`, `sim_post_sell_evaluations`, and `statistical_action_weight` for ADM/lifecycle; `holding_exit_observation`, `post_sell_feedback`, `trade_review`, and `holding_exit_sentinel` for holding/exit; `institutional_flow_context` for supply-demand features; and `panic_sell_defense` for panic context. The removed panic-buying artifacts are archive-only and cannot be reopened through an operator override. One-off follow-up records remain archive/reference material, not current automation-chain source bundle owners.

Active Sim Priority is not a separate chain. Its producer/consumer contract is the existing chain: `lifecycle_bucket_discovery.active_sim_priority_seeds`, `scalp_sim_auto_approval`, `swing_sim_auto_approval`, `threshold_cycle_preopen_apply`, intraday sim/runtime events, and `threshold_cycle_postclose_verification`. The priority key, not the report headline, is the unit of control. If a key is active in catalog and PREOPEN but has no natural runtime match that day, close it as observation warning and keep the next decision on match coverage, prefix quality, EV persistence, and source-quality status.

Unorganized report-only/legacy artifacts are managed by `calibration_source_bundle.report_only_cleanup_audit`. This audit is a `source_quality_gate`; it surfaces cleanup candidates through `cleanup_candidate_count`, but because it is `source_quality_only`, it has no authority to change threshold/env/order/bot/provider.

`Metric Decision Contract` is owned by [report-based-automation-traceability](./report-based-automation-traceability.md#26-metric-decision-contract). A new report or metric that does not satisfy this contract is source-quality/instrumentation backlog, not a threshold candidate.

## 5. Current Runtime And Observation Axes

| Area | Current State | Forbidden Use |
| --- | --- | --- |
| scalping entry | Entry ADM can correct AI `BUY` into `WAIT`/`DROP` through `BUY_NOW/WAIT_REQUOTE/SKIP_STALE/BUY_DEFENSIVE/NO_BUY_AI/SKIP_SOURCE_QUALITY/SKIP_PRE_SUBMIT_SAFETY` buckets and hypothesis fallback. Score 50 fallback/neutral is held as `blocked_ai_score`. Existing score cutoff is only a matrix feature and baseline prior. Selected `score65_74_recovery_probe` keeps its family id but adjusts the runtime floor to score 60, sending only feature-qualified score60-74 candidates that pass supply-demand/acceleration/micro-VWAP conditions to entry unlock. The current `WAIT6579_PROBE_CANARY` cap defaults are unlimited for separate probe budget/quantity (`0` means use normal new-buy sizing), so the old one-share/50,000 KRW cap is not the active runtime description. OpenAI analyze_target engine-level min-interval must not create a score50 fallback blocker; it waits briefly and then makes the real model call. Submit drought is surfaced by `BUY Funnel Sentinel` as `SUBMIT_DROUGHT_CRITICAL`, and `latency_classifier_runtime_profile` owns only DANGER/stale/broker submit quality audit. Runtime `EntryPolicy` sends `SAFE` and `CAUTION` to normal submit after slippage check. | Must not bypass threshold/provider/broker submit guards. Stale/liquidity/overbought/DANGER latency/price freshness pre-submit safety outranks ADM. Latency quality audit cannot bypass hard safety, stale quote, broker/account/order/quantity/cooldown guards. |
| Opening Rotation one-share | Permanently retired by explicit operator direction on 2026-08-14 under `opening_rotation_full_retirement_20260814`. Scanner ownership, protected watch capacity, new mechanical BUY authority, margin exception, postclose tuning, and PREOPEN policy production/verification are removed. Existing reports, runtime artifacts, and events are archive/audit evidence only. Receipt/exit parsing for an already-owned legacy position remains custody compatibility and grants no new entry or reactivation authority. | A former env key, dated policy, candidate artifact, or archived report cannot reactivate the family. Any future rotation concept requires a new workorder, namespace, evidence contract, runtime guard, and explicit operator authority; reuse of `opening_rotation_*` live authority is forbidden. |
| lifecycle decision matrix | `lifecycle_decision_matrix_runtime` is the ADM expansion umbrella owner. It wraps existing Entry ADM/Holding-Exit ADM plus submit/scale-in adapters and sim/counterfactual sources into `entry/submit/holding/scale_in/exit` stage-level weighted ADM policy, and connects policy file/version/promote cap to the next PREOPEN env only when selected. `entry_bucket_attribution` automatically buckets entry rows by `score_band/source_stage/stale/liquidity/strength/overbought/time_bucket/combo_entry_spot`; `scale_in_bucket_attribution` separates `AVG_DOWN/PYRAMID` arms from blocker namespace/reason. `lifecycle_bucket_discovery` deterministically classifies these buckets across the whole lifecycle, preserves child pattern dimensions/provenance, selects a target-pass parent granularity, then AI Tier2 2-pass review finalizes `existing_bucket_refinement`, `new_bucket_candidate`, `source_only_keep_collecting`, `sim_auto_approved`, `live_auto_apply_ready`, `runtime_blocked_contract_gap`, `code_patch_required`, or `automation_handoff_gap`. Daily discovery is new-pattern detection; rolling/MTD parent discovery is promotion confirmation. AI review uses English prompts and must not block deterministic live candidates due to ambiguity or small effects; AI unavailable/parse fail is fail-closed for pre-final live-auto, and only parsed explicit contract/safety gaps block. The LDM source contract snapshot surfaces collection-layer additions/deletions/changes as `source_contract_changes` and closes downstream handoff/code patch need. `lifecycle_ai_context` supplies this policy and postclose attribution only as OpenAI entry/holding/exit prompt context, and context contribution feeds back into LDM as bounded auxiliary weight. | Must not bypass hard safety, account/order/broker guards, stale quote/price freshness/stop/quantity guards. Do not use score buckets as fixed policy or assume monotonic score EV. Child combo buckets are diagnosis/exclusion dimensions, not default live owners. `sim_auto_approved` means sim policy only. `live_auto_apply_ready` means a deterministic parent-level live candidate with parsed Tier2 validation, rolling/MTD confirmation, and no explicit AI-found gap that inherits existing guards/caps as a next PREOPEN live auto env candidate. |
| greenfield real environment authority | `greenfield_real_environment_authority` is a next-PREOPEN runtime authority that makes the real environment follow only promoted/allowed lifecycle buckets across `entry/submit/holding/scale_in/exit`. `runtime_apply_bridge` creates `greenfield_real_env_policy_YYYY-MM-DD.json` from `lifecycle_bucket_discovery.live_auto_apply_ready` rows that passed deterministic contract and parsed AI Tier2 review. When enabled by PREOPEN env, legacy live BUY/holding/exit/scale-in logic is only baseline prior/source/provenance unless a matching promoted bucket allows the stage/action. Stage Telegram is emitted once to `VIP_ALL` for real `entry`, `submit`, `holding`, `scale_in`, and `exit`; sim/probe/source-only rows are excluded. | Hard safety, broker/account/order/cooldown/quantity, stale quote, price freshness, hard/protect/emergency stops still outrank greenfield. Greenfield itself does not change provider route, bot process, or cap release; a separately recorded operator-directed intraday override follows the single-axis cohort/rollback contract. Missing policy or missing stage bucket fail-closes real action instead of falling back to legacy authority; hard safety remains passthrough. |
| institutional flow context | `institutional_flow_context` is a source-only axis that normalizes Kiwoom REST `ka10059/ka10061/ka10064` helpers and WS `0F/0w` cache into standard features for lifecycle matrix. Kiwoom access is owned by `kiwoom_utils.py` and `KiwoomWSManager`; new modules own only normalize/report. | Must not decide BUY/scale-in/exit alone, bypass broker guards, or change threshold/provider/runtime env. Stale/missing/token error remains source-quality provenance only. |
| entry price | `dynamic_entry_price_resolver_p1` plus `dynamic_entry_ai_price_canary_p2`; if passive probe submit revalidation is stale, block before submission. | Direct `ws_data.curr` chase and stale quote submit are forbidden. |
| holding/exit | `soft_stop_micro_grace`, `holding_flow_override`, and `holding_exit_matrix_runtime_bias_p1` are operating overrides. `soft_stop_whipsaw_confirmation` is currently OFF and remains a `bounded_tunable` source-only exact-path observation owner until rolling evidence closes; `holding_flow_ofi_smoothing` remains ON inside the bounded holding-flow path. Holding/Exit ADM can correct flow AI action to `HOLD`/`EXIT` and propose scale-in action through `holding_exit_matrix_avg_down_bias`/`holding_exit_matrix_pyramid_bias`. SCALP preset TP is no longer a profit-taking owner: new/recovered scalping holdings must not create a +1.5% preset TP sell order, and legacy preset TP order refs are canceled/disabled before profit-side evaluation continues through `scalp_trailing_take_profit`. Legacy `SCALP_PRESET_TP` hard-stop compatibility remains only as stop-safety provenance. | Must not bypass hard/protect/emergency/order/account/cooldown/quantity safety. Source-only smoothing evidence cannot directly enable a live family. Preset TP disabled/sync events are operational diagnostics, not EV approval or threshold-apply evidence. |
| scale-in/position sizing | Keep scale-in price resolver and dynamic quantity safety. `position_sizing_dynamic_formula` remains the upstream affordability owner for every active `SCALPING/SCALP` new buy, Rising Missed Scout, AVG_DOWN/PYRAMID, and scalping simulation. Source-count/time/venue classification selects fixed tiers `10%/15%/20%/25%/25%` under `entry_type_5stage_cap25_v1`; NXT, unknown venue, invalid/missing source, and unrecoverable original-entry context fail closed to tier 1 (10%). Scale-in reuses the initial tier. The 95% safe budget, minimum one-share floor, and downstream position/stage/broker caps remain. | Score-linear 10-30%, retired Opening Rotation margin/private-ratio authority, Rising Missed 400,000 KRW cap, sim 100% default, and `entry_armed_ratio` have no current sizing authority. Sim/probe real-order authority and hard-safety bypass are forbidden. An unapproved bot restart remains forbidden. |
| sim lifecycle | Sim-first lifecycle exploration is not a new report chain; it is the input/interpretation scope of the existing threshold-cycle automation chain. Scalping `scalp_ai_buy_all`, `scalp_sim_candidate_window_expansion`, `scalp_sim_ai_budget_manager`, wait/missed-entry counterfactual, and swing dry-run/probe all enter daily/cumulative EV, runtime approval summary, and code-improvement workorder as `actual_order_submitted=false` sources. Active Sim Priority now promotes rare positive scalping parent bucket prefixes and swing superior arms/buckets into next PREOPEN sim policy catalogs so the runtime can preferentially collect more samples when those observable prefixes naturally appear. | Must not be interpreted as real order or real-like execution quality. Sim exploration must not narrow candidates because of cash availability, real-order possibility, or currently selected runtime family. Sim-only candidates do not send BUY analysis/order Telegram alerts and leave only `buy_analysis_telegram_suppressed` provenance. Sim-only AI budget controls calls and deferred source generation only; it must not change provider route, BUY/submit threshold, or broker guard. Active Sim Priority must not bypass total sim `max_daily`/`max_open`, hard safety, broker/order guards, real orders, provider route, bot restart, cap release, or threshold authority. |
| swing dry-run | Lifecycle audit, threshold AI review, improvement automation, runtime approval summary, Swing LDM, and swing sim-auto policy catalog are generated postclose and become swing source-bundle inputs for the existing threshold-cycle automation chain. Postclose `SWING_THRESHOLD_AI_REVIEW_PROVIDER` defaults to `openai` with the individual `gpt-5.4-mini` medium config; the 20:15 lightweight path remains `none`. | General dry-run env apply can be auto-approved after parsed Tier2 validation and hard-floor/source-quality pass. Full live conversion, dry-run removal, cap release, provider/bot changes, and hard/protect/emergency safety relaxation remain final user-approval-only. Direct one-share or scale-in broker-submit trial stages are not current operating paths. |
| swing strategy discovery sim | `swing_strategy_discovery_sim_v1` targets the whole safe pool, uses existing ML only as a low-weight feature/cohort, selects candidates with lifecycle rank 60% + diversity exploration 30% + legacy ML 10%, and creates 8 virtual arms per candidate. The label builder updates `1d/5d/10d/policy_exit` labels and arm state, and the EV report aggregates arm/entry/sizing/exit/selection/sector/theme axes as source-only. Surviving arms and Swing LDM sim candidates can become active sim priority policies in the daily swing sim catalog without creating duplicate arms. | `actual_order_submitted=false`, `broker_order_forbidden=true`, `runtime_effect=false`, and `decision_authority=swing_sim_exploration_only` are fixed. Broker orders, Telegram BUY alerts, replacing `recommendation_history`, threshold-cycle live runtime apply, real execution quality claims, and full-live authority are forbidden. Active priority only changes sim observation focus. |
| swing live conversion | There is no current swing one-share or scale-in broker-submit trial path. Dry-run/sim candidates can become pre-final env candidates, but broker-submit authority opens only through final full-live conversion approval. | Swing actual trading reflection requires final full-live conversion approval after complete LDM parent bucket evidence is closed. |
| AI route | Main live AI route is OpenAI except explicit endpoint-specific overrides. `entry_price` uses Bedrock Qwen3 32B primary with Nova Lite v2 failback and defensive close if both Bedrock calls fail. `holding_flow` keeps Bedrock Nova Lite v2 primary with OpenAI failback. Removed Bedrock shadow/comparison surfaces are not active report inventory. AI input v2/call-policy surfaces remain disabled-by-default builders and bounded refresh flags unless a later owner enables them. | Provider route confirmation and AI input/cadence instrumentation must not be used as evidence to change thresholds, order price, quantity, or bot state. Endpoint allowlist, failback, audit rows, and disabled v2 input flags must remain separate from strategy edge judgment. |
| Sentinel/panic | BUY/HOLD/EXIT/panic sell are default report-only source bundles. `scalp_sim_panic_lifecycle` is a scalping sim-only actuator with `actual_order_submitted=false` and `broker_order_forbidden=true` that consumes panic-sell risk-regime context. It can apply `panic_level>=2` entry block and holding partial/full exit, with scale-in block/holding defense kept as separate sim actuators. `panic_level=1` market breadth risk-off entry is not a fixed hard gate; it is an observation regime, and bottoming state remains a `BOTTOMING`/`WEAK` feature. | Real automated selling, TP/trailing/threshold/provider/bot restart changes are forbidden. Level-1 risk-off/bottoming observation is sim-only and has no authority to change broker submit, Telegram BUY alerts, or real-entry relaxation. |
| System Error Detector | Report-only detector and gated filesystem maintenance detector are separated. | Strategy threshold/order changes are forbidden. |

## 6. Quantitative Targets And Guards

| Metric/Guard | Baseline | Action |
| --- | --- | --- |
| `N_min` | Below family-specific hard decision sample floor. | Hard pass/fail is forbidden; use `hold_sample` or cap reduction. |
| `primary_ev` | Family contract's EV primary metric. | Primary evidence for apply/promotion. |
| `diagnostic_win_rate` | Supporting win-rate/consistency metric. | Standalone apply is forbidden. |
| `simple_sum_profit_pct` | Simple PnL sum. | Must not be named or used as EV. |
| `source_quality_gate` | Stale/missing/duplicate/provenance gap. | Exclude from threshold candidates if gate is not met. |
| `canonical_label_contract` | Internal labels emitted by prompts, schemas, JSONL/log-derived reports, and source-quality audits. | Unknown labels fail source quality; do not silently bucket or auto-promote them. |
| `execution_quality_real_only` | Real broker order/receipt/fill quality. | Must not be replaced by sim/probe/combined. |
| `safety_veto` | Severe loss, order failure, provenance damage, stop delay, or same-stage conflict. | May block/rollback even when daily-only. |
| `window_policy` | Separation of `daily_only`, rolling, cumulative, and post-apply version windows. | Daily-only edge apply is forbidden. |
| `runtime_mutation_guard` | An operator-directed intraday change is limited to one existing `bounded_tunable` axis, one explicit KRX or NXT cohort, one stage owner, recorded values/provenance, rollback, and immediate attribution. | Reject hard-safety, stale/conflict, price freshness, broker/account/order/quantity/cooldown, provider, bot, cap, requested-quantity increase, or cross-venue/common-runtime changes; otherwise apply the recorded operator override contract immediately. |

## 7. Current Open State Summary

| Workstream | Current Owner/State | Next Decision Path |
| --- | --- | --- |
| threshold auto apply | Scheduled `auto_bounded_live` uses postclose source bundle plus AI/deterministic guard and PREOPEN runtime env. User operational overrides are recorded separately with selected family, explicit cohort, before/after values, runtime provenance, rollback, and post-apply attribution. Target-date PREOPEN apply plans and runtime env files own the scheduled scalp/swing sim policy handoff. | An explicit intraday override applies through the `runtime_mutation_guard` contract; blocked families still require their owner, evidence, and rollback fields before any override. Before judging strategy outcome, confirm that target-date active priority catalog keys remain present in PREOPEN policy file paths and runtime/sim event outputs. |
| Samsung independent machine entry tuning | `samsung_machine_entry_tuning` owns postclose attribution for the separate morning, midday, and afternoon Samsung machines. The report and candidate remain non-runtime artifacts; exact-date PREOPEN `samsung_machine_entry_policy_apply` owns bounded reflection. Morning remains baseline-only. Midday/afternoon may tighten drawdown `1.25→1.50` or near-low `0.20→0.10`, with at most one machine and one entry axis changed per candidate. By explicit operator direction on 2026-08-13, every new episode uses two independent 10-share legs (20 shares maximum); legacy owned one-share state remains custody-compatible and is not resized. A partial buy cancels only its exact order remainder before targeting the confirmed filled quantity. The 50:50 split, five-bar validity, +2 ticks, no stop, held inventory, provider/bot/cap, and broker guards are immutable. | Require clean-baseline actual leg outcomes, target-date observation source-quality allow, cumulative episode and completed-leg floors, positive rolling10/20 and cumulative notional EV, no held/unresolved inventory, candidate lineage hash, and immutable exact-date PREOPEN apply. Missing/stale candidate writes baseline; invalid latest candidate or applied artifact blocks before gateway construction. Post-apply `signal_features` provenance owns the next attribution cycle. |
| lifecycle matrix runtime | `lifecycle_decision_matrix_runtime` has Full runtime structure implemented, and `lifecycle_bucket_discovery` automatically discovers/classifies the whole lifecycle bucket set. Fixed thresholds are reinterpreted through `hard_safety`, `baseline_prior`, `bounded_tunable`, and `legacy_archive` contracts. LDM `entry_bucket_attribution`, `scale_in_bucket_attribution`, `overnight_bucket_attribution`, and stage policy buckets are standardized by discovery into sim-auto or live-auto candidates. Live-auto checks deterministic contract and absence of explicit gaps in parsed AI Tier2 2-pass review. AI unavailable/parse fail blocks pre-final live-auto until the next parsed review. Parsed ambiguity without an explicit contract/safety gap is not an automatic block. Collection-layer source contract drift and downstream handoff misses are postclose verification fail/warning reasons. | The scheduled route uses `lifecycle_decision_matrix`, `lifecycle_bucket_discovery`, `runtime_apply_bridge`, `threshold_cycle_ev`, `runtime_approval_summary`, `code_improvement_workorder`, and PREOPEN `threshold_cycle_preopen_apply`. An explicit intraday override may immediately modify one existing bounded axis under the `runtime_mutation_guard` contract. |
| active sim priority | Implemented as sim-only key handoff. The current strategic question is no longer "did we find a rare positive bucket?" but "does the same active key get observed by runtime/postclose, or close as natural-match warning with preserved catalog handoff?" Target-date postclose discovery and PREOPEN apply artifacts own the active seed/policy counts. | Check `scalp_sim_active_priority_seed_matched`, `active_seed_id`, and swing `priority_policy_id` in target-date runtime/sim event outputs. Postclose verifier should close active priority as `pass` when observed, `warning` when natural match is 0, and `fail` only for key/schema/contract mismatch, unknown key, inactive key consumption, or forbidden contract violation. Do not add hardcoded bucket constants or manual allow/block lists. |
| greenfield real environment | Greenfield real authority is implemented as a PREOPEN-only runtime env candidate generated by `runtime_apply_bridge` from promoted lifecycle buckets. The current live BUY/holding/scale-in/exit code remains present, but when this authority is enabled it no longer has independent real action authority outside the promoted bucket allowlist. Liquidity/overbought submit blockers become greenfield tunable prior observations; stale quote, price freshness, broker/account/order/quantity/cooldown and hard/protect/emergency stops remain hard safety. `greenfield_real_environment_authority` remains blocked unless target-date artifacts close the live-auto contract, policy file, promoted bucket, runtime hook, and rollback/post-apply attribution. | Next PREOPEN `threshold_cycle_preopen_apply` may enable `KORSTOCKSCAN_GREENFIELD_REAL_ENV_AUTHORITY_*` only from a `live_auto_apply_ready` bridge candidate. Stage Telegram sends one `VIP_ALL` notice per real entry/submit/holding/scale_in/exit occurrence. Missing policy or promoted bucket is a real drought/blocked-action condition, not a reason to fall back to legacy live authority. |
| institutional flow context | `institutional_flow_context` is implemented. Actual Kiwoom token smoke checks required `ka10059/ka10061` and intraday `ka10064` parsing. Postclose artifacts expose source-only join rate/source mix/top net buy in `threshold_cycle_ev` and `runtime_approval_summary`. | Allowed only as lifecycle matrix runtime feature/provenance input. `institutional_accumulation_score` is not a standalone BUY/additional-buy condition. |
| fixed threshold migration | Existing thresholds are not deleted; their roles change to `hard_safety`, `baseline_prior`, `bounded_tunable`, and `legacy_archive`. Lifecycle matrix may propose only next PREOPEN candidates for bounded tunables. The 2026-06-30 cleanup removes the legacy price-field protect-hard runtime/taxonomy path; `hard_stop_price` remains only as schema/S15 TTL compatibility and must not create exit, scale-in, overnight, report-taxonomy, or live-auto authority. | Hard safety override, baseline-prior standalone action, unauthorized legacy archive reopening, and reintroducing legacy price-field protect-hard authority are forbidden. |
| entry funnel | `BUY Funnel Sentinel` + `wait6579_ev_cohort` + `score65_74_recovery_probe` + `scalp_entry_adm_runtime_bias_p1` + `latency_classifier_runtime_profile`. | BUY/submitted drought and forced WAIT/DROP are judged by daily/rolling/cumulative EV, blocker attribution, missed-upside, and avoided-loss. `BUY Funnel Sentinel` automatically surfaces `SUBMIT_DROUGHT_CRITICAL` and creates postclose workorder/LDM handoff when the submitted ratio crosses the critical threshold. `wait6579` evidence is preserved as entry provenance inside LDM complete lifecycle buckets and source-quality analysis, but the retired entry-only bridge family has no runtime candidate or PREOPEN env path. Current real runtime uses selected probe families and normal new-buy sizing when separate `WAIT6579_PROBE_CANARY` budget/quantity caps are `0`. It is not broad BUY threshold relaxation and cannot bypass hard safety, stale quote, broker/account/order/quantity/cooldown guards. `latency_classifier_runtime_profile` splits latency blocks by reason/profile but treats CAUTION as normal submit semantics and audits only DANGER/stale/broker safety plus counterfactual proof. Rising-missed filters use the balanced 3-layer contract: `scanner_watch_budget` owns only watch-budget/source freshness pruning such as `not_rising_missed_without_recovery_signal`; `candidate_gate` owns scout/normal-bridge candidate eligibility such as source marker, positive delta, AI action, upper-limit, open pending, holding, and price cap; `submit_safety` owns pre-broker vetoes such as stale quote with weak evidence, weak same-symbol micro reentry, and tick-speed guards. Submit-safety feedback backoff is scanner budget reallocation only and does not relax or strengthen broker-submit criteria. Market-data freshness envelope is a scanner/pre-AI/submit common input supplement; REST orderbook and signed tape add freshness/provenance/negative-veto context only and never create BUY support or safety bypass. Post-block submit retry paths such as secondary submit recheck, direct-canary REST tape recheck, rising-missed REST estimator/REST AI recheck, and latency false-negative repass are removed; refreshed inputs must enter through the envelope or the next scanner loop. `rising_missed_tick_speed_entry_guard` is a real-submit safety veto for rising_missed lineage only: `tick_window_span_sec >= 60`, `tick_acceleration_ratio < 1.0`, or missing tick context after pre-submit refresh blocks before broker submit and must not be generalized into a broad SCALPING threshold/provider/cap change. |
| Opening Rotation one-share | `opening_rotation_full_retirement_20260814`; no current runtime or automation owner. Historical source/report/policy artifacts remain archive/audit evidence and legacy receipt/exit parsing remains custody-only. | No further observation, tuning, PREOPEN apply, scanner slot, margin exception, or BUY submit path. Reopening the retired namespace is forbidden. |
| sim-first lifecycle threshold scope | This is the scope rule of the existing `R0_collect -> R1_daily_report -> R2_cumulative_report -> R3_manifest_only -> R4_preopen_apply_candidate -> R5_bounded_calibrated_apply -> R6_post_apply_attribution` chain, with `lifecycle_bucket_discovery` as the owner for full lifecycle bucket discovery/classification. All scalping and swing BUY/selection candidates run broadly through a virtual `selection -> entry -> submit -> holding -> scale_in -> exit -> overnight/carry` lifecycle to find sim-auto candidates, live-auto candidates, active priority keys, and hook/code gaps. `scalp_sim_candidate_window_expansion`, `scalp_sim_ai_budget_manager`, and `scalp_sim_scale_in_window_expansion` keep widening sim sources, and discovery automatically creates next PREOPEN sim policy plus scale-in or complete-lifecycle live-auto bridge candidates. | Real order/cash/cap/selected runtime family is a provenance tag, not sim exclusion. `sim_auto_approved` and `active_sim_priority` keep `actual_order_submitted=false`, `broker_order_forbidden=true`, and `decision_authority=sim_observation_only`. `live_auto_apply_ready` also cannot bypass hard safety, stale quote, broker/account/order/quantity/cooldown guard, provider route, bot restart, or cap release. |
| entry price quality | P1/P2 price resolver, passive probe lifecycle, and submit revalidation block. | `pre_submit_price_guard` family and daily EV attribution. `pre_submit_price_guard` is not the primary latency submit-drought owner; it is downstream quote/price quality guard after latency classifier. |
| holding/exit | `soft_stop_micro_grace`, `holding_flow_override`, `holding_exit_matrix_runtime_bias_p1`, and profit-side `scalp_trailing_take_profit` after SCALP preset TP removal are active. `soft_stop_whipsaw_confirmation` is OFF/source-only; `holding_flow_ofi_smoothing` remains ON and bounded by pre-smoothing hard guards. Exact-path rolling decisions use 5/10/20-day 90-second source-quality-adjusted EV with guarded terminal downside, but remain in source-only bounded review with no direct runtime authority. | HOLD/EXIT Sentinel, post-sell feedback, smoothing exact-path rolling decision, scale-in ADM bias attribution, trade/performance preset-disabled diagnostics, and threshold-cycle EV. A positive rolling decision may open one same-stage bounded-canary design review only; it cannot directly mutate PREOPEN env. |
| position sizing | `position_sizing_dynamic_formula` is the single sizing owner. The selected formula is `entry_type_5stage_cap25_v1`; the report compares only that selected formula with `flat_10_fallback`. The former seven score-linear/correction candidates are archive/parser compatibility only. Source changes are implemented but not reflected in the currently running process. | Future formula changes are reviewed by `notional_weighted_ev_pct` or `source_quality_adjusted_ev_pct`; report candidates cannot mutate the runtime. The next process start requires explicit permission and validation handoff. |
| panic lifecycle | `panic_sell_defense` remains a report-only source bundle and `risk_regime_context` input. The panic-buying detector/report, wrapper, postclose source, approval/workorder bridge, sim euphoria actuator, and real-entry price modifier were removed on `2026-08-08`; existing artifacts are archive-only. `scalp_sim_panic_lifecycle` separates `market_regime` and `symbol_regime` as a panic-sell sim-only actuator. `PANIC_CONFIRMED`/`LIQUIDITY_BROKEN` prioritize defense; level-1 `market breadth risk-off` entry is not a hard gate but a `BOTTOMING`/`WEAK` observation feature in lifecycle matrix entry rows. | `panic_lifecycle_actuator` and `panic_entry_freeze_guard` are not standalone live-selectable threshold families. Removed panic-buying code and families have no operator override or reactivation path; a future rebound/exhaustion strategy must use a new workorder, new namespace, new evidence, and new guards. Preopen standalone env, threshold mutation, real orders, real-account open-order cancellation, forced real holding reduction/exit, TP/trailing/provider/bot restart changes are forbidden. Sim-only actuator must stay within `actual_order_submitted=false`, `broker_order_forbidden=true`, and `decision_authority=sim_observation_only`. |
| swing lifecycle | Swing dry-run recommendation, audit, AI review, improvement automation, runtime approval, Swing LDM, lifecycle bucket discovery, and swing control-tower pre-final sim artifact. Postclose `SWING_THRESHOLD_AI_REVIEW_PROVIDER=openai` uses individual `gpt-5.4-mini` medium reviewer config; 20:15 lightweight execution keeps `none`. The control tower has sim-auto policy authority only and no final full-live conversion authority. | Parsed Tier2 + hard-floor/source-quality pass can auto-apply dry-run/sim policy env only. Full live conversion, dry-run removal, cap release, provider/bot changes, and hard/protect/emergency safety relaxation require user approval. |
| swing strategy discovery sim | First follow-up implementation exists. `swing_strategy_discovery_candidates`, `swing_strategy_discovery_arms`, and `swing_strategy_discovery_labels` schema, safe-pool candidate builder, bounded 8-arm builder, sector/theme cache, label builder, EV report, and postclose source-only connection exist. Existing ML enters only as feature/comparison cohort. Surviving arms and Swing LDM sim candidates can now become active sim priority policies in `swing_sim_policy_catalog_v1` without duplicating the 8-arm structure. | Next step is postclose cumulative sample review, sector/theme source-quality improvement, and active priority observation. Current table/report is sim exploration source and does not connect to real order, runtime env, or threshold apply without approval artifact. Active swing priority raises sim collection focus only; it does not create real orders or full-live authority. |
| AI transport | OpenAI Responses WS provenance plus endpoint-specific Bedrock overrides: `entry_price` is Qwen3 32B primary with Nova Lite v2 failback and defensive close on double Bedrock failure; `holding_flow` remains Nova Lite v2 primary with OpenAI failback. Bedrock Nova shadow comparison code is removed; provider/failback audit rows remain. Entry/price/holding v2 input builders and state-change refresh controls are implementation surfaces, default OFF unless a later checklist owner enables them. | Separate transport and input/cadence incidents from strategy threshold effects. |
| scalping AI multi-timeframe context | The 3/5/15-minute completed-bar bundle, session VWAP, 5/15-minute opening range, previous-day lines, and market/sector context remain source-only observation and external-validation inputs until the next `PREMARKET_KRX_LIKE` final validation. The live integration owners are the existing stage-specific schemas: `entry_candle_context_v1` for entry-side calls and `holding_decision_context_v1` for holding/scale-in/exit/overnight calls. Both consume the same venue/session-normalized source and shared derived-feature version `scalping_multi_timeframe_context_v1`; that version is not a third parallel model payload. The user-directed target is one binary validation followed immediately by full live promotion across every scalping symbol, every active market session (`PREMARKET_KRX_LIKE`, `KRX_REGULAR`, `NXT_REGULAR_OVERLAP`, `NXT_AFTERMARKET`), and every applicable live scalping AI endpoint. This is not a canary, session-limited, partial-cohort, endpoint-limited, or call-rate rollout. | The gate is binary: final PREMARKET validation pass means global full application from the validation completion timestamp; failure means no promotion. The reviewed target-date promotion artifact records both context schemas, their shared `input_bundle_version`, exact payload/trace provenance, `provider!=none`, comparable required-field `MISMATCH=0`, completed-bar-only aggregation, fresh conflict-free source quality, and rollback. Once passed, no additional KRX, NXT, stage, or endpoint promotion gate is required. Each call still uses its own venue/session and stage-appropriate context without cross-venue or cross-stage substitution. Threshold, provider route, order price/quantity, broker/account/order/cooldown, hard/protect/emergency safety, and bot-state authority are unchanged. |
| AI decision-quality improvement | `baseline_v1` is a protective input-preflight policy built from legacy source-quality proxies; it is not a prompt version or proof of intelligent decisions. `exact_v2` is the exact snapshot/venue/session/payload trust gate around the stage-specific context payload. After multi-timeframe global promotion, the unchanged current stage prompts, models, and provider routes become the control on `input_bundle_version=scalping_multi_timeframe_context_v1`, with `entry_candle_context_v1` and `holding_decision_context_v1` retained as the canonical context-schema owners. A separate `decision_quality_v1` offline loop owns mature outcome labels, control error taxonomy, stage-specific Prompt V2 candidates, and same-payload paired replay. | Keep the layers ordered as `baseline_v1 protective evidence -> exact_v2 trusted input -> stage-specific context plus shared multi-timeframe feature bundle -> frozen current-prompt control -> mature stage/venue outcomes -> Prompt V2 paired replay -> reviewed stage-level runtime candidate`. Entry Prompt V2 replay uses the exact `entry_candle_context_v1` payload and holding/exit Prompt V2 replay uses the exact `holding_decision_context_v1` payload; do not mix the two cohorts. Pre-`exact_v2` rows may support defect discovery but not authoritative paired comparison. Candidate prompts have `runtime_effect=false` and no order authority during replay. Model/provider changes are evaluated only after prompt candidates fail to improve `source_quality_adjusted_ev_pct` and net profit on the same eligible payload cohort. |
| system health | System Error Detector, postclose verification, and wrapper `[START]/[DONE]/[FAIL]` markers. | Separate operational incidents/playbook from strategy tuning. |
| metric contract | Required onboarding contract for new observation metrics. | Route missing contracts to `instrumentation_gap`/`source_quality_blocker`. |
| canonical label quality | Observation source-quality audit normalizes documented legacy Korean aliases for holding `flow_state` and gatekeeper action values, then fails unknown labels as `invalid_label_findings`. | Unknown labels are producer/schema drift, not valid discovery output. Fix prompt/schema/parser and tests before using affected rows for promotion. |

## 8. Excluded From Current Baseline

1. `fallback_scout/main`, `fallback_single`, `latency fallback split-entry`, legacy latency composite, and closed shadow axes are archive-based historical/reference.
2. Completed `[x]` items in old daily checklists are not current owners. Current owners are owned by §7 of this document and open items/runtime state in the current daily checklist.
3. Operational automation evidence such as cron, wrapper, manifest, parser, and report freshness is separate from strategy effect or live approval evidence.
5. `combined` sim+real diagnostics may be used for operational observation, but not for real execution quality or real-order approval.
6. New report-only artifacts are not automation-chain inputs unless they have source bundle consumer, metric contract, and forbidden uses. If the user declares a separate `operator_runtime_override`, record artifact output and runtime env override separately, with rollback env and safety guard priority.
7. The previous-limit-up rotation observation family (`upper_limit_watch`, its candidate/report/counterfactual/bounded-live artifacts, and `UPPER_LIMIT_LIVE_RECLAIM`) was retired on `2026-08-14` by explicit operator direction. Historical artifacts are archive/audit evidence only and have no producer, verifier, runtime slot, scanner handoff, live-auto promotion, or reactivation path. A future previous-limit-up strategy requires a new workorder, namespace, evidence contract, and runtime guards.

## 9. Delta/Q&A Routing

| Document | What It Stores | Why It Is Excluded Here |
| --- | --- | --- |
| [plan prompt](./plan-korStockScanPerformanceOptimization.prompt.md) | Lightweight pointer for next session entry. | Rebase owns the core baseline. |
| [execution-delta](./plan-korStockScanPerformanceOptimization.execution-delta.md) | Date-based task registry, expired schedules, same-day pivots, and effect records. | Rebase does not accumulate historical progress. |
| [qna](./plan-korStockScanPerformanceOptimization.qna.md) | FAQ to prevent automation-chain misjudgment, metric authority, and proposal/apply separation. | Stores repeated policy only. |
| Daily checklist | Time-specific work, Due/Slot/TimeWindow, and complete/incomplete state. | Auto-parsing and Project/Calendar owner document. |
| audit/report | Externalized copies, detailed numeric evidence, and review-perspective explanations. | Rebase keeps approval criteria only. |
| [archive](./archive/) | Closed observation axes, old workorders, and pre-renewal original text. | Excluded from current active/open decisions. |

## 10. Key Reference Documents

| Document | Role |
| --- | --- |
| Current daily stage2 checklist (`docs/checklists/YYYY-MM-DD-stage2-todo-checklist.md`) | Current daily preopen/intraday/postclose execution table and postclose auto-generated items. |
| [report-based-automation-traceability.md](./report-based-automation-traceability.md) | R0-R6 ladder, source bundle, Metric Decision Contract, and forbidden-use lines. |
| [data/source_quality/clean_baseline_policy.json](../data/source_quality/clean_baseline_policy.json) | Clean tuning data baseline policy. Data before `2026-06-05T00:00:00+09:00` is archive-only and forbidden as current tuning decision input. |
| [data/threshold_cycle/README.md](../data/threshold_cycle/README.md) | Threshold collector/report/apply plan/runtime env operating method. |
| [data/report/README.md](../data/report/README.md) | Regular report inventory and Markdown-missing candidates. |
| [plan-korStockScanPerformanceOptimization.prompt.md](./plan-korStockScanPerformanceOptimization.prompt.md) | Lightweight session-start pointer. |
| [plan-korStockScanPerformanceOptimization.execution-delta.md](./plan-korStockScanPerformanceOptimization.execution-delta.md) | Changes from the original plan, date-based history, and closed-axis records. |
| [plan-korStockScanPerformanceOptimization.qna.md](./plan-korStockScanPerformanceOptimization.qna.md) | Repeated decision criteria and review Q&A. |
| [archive](./archive/) | Closed observation axes, pre-renewal original text, runtime override records, and other historical evidence excluded from current active/open decisions. |
