# KORStockScan Plan Rebase 중심 문서

기준일: `2026-05-01 KST`
역할: 현재 튜닝 원칙, 판정축, 정량 목표, active/open 상태만 고정하는 중심축 문서다.
주의: 이 문서는 자동 파싱용 체크리스트를 소유하지 않는다. Project/Calendar 동기화 대상 작업항목은 날짜별 `stage2 todo checklist`가 소유한다.

---

## 1. 현재 판정

1. 현재 단계는 손실 억제형 미세조정이 아니라 `기대값/순이익 극대화`를 위한 `Plan Rebase`다.
2. 현재 기준선은 `main-only`, `normal_only`, `post_fallback_deprecation`이다.
3. 현재 폐기/종료 확정 축은 [closed observation archive](./archive/closed-observation-axes-2026-05-01.md)로 내린다. rebase 본문은 active/open 축과 재개 금지 원칙만 유지한다.
4. 현재 live 운용 원칙은 `동일 단계 내 단일 owner canary`다. 진입병목축과 보유/청산축은 서로 다른 단계이므로 양쪽 canary 동시 존재가 가능하다. 같은 단계 안에서도 `REVERSAL_ADD/PYRAMID/exit_only`처럼 같은 position-sizing family에 속하고 arm별 provenance, safety guard, cohort allocation이 고정된 경우에는 하나의 owner 안에서 bounded multi-arm canary로 표본을 수집할 수 있다.
5. 현재 entry owner는 `mechanical_momentum_latency_relief` 운영 override와 `dynamic_entry_price_resolver_p1`/`dynamic_entry_ai_price_canary_p2` 가격축이다. P2 내부에서는 OFI/QI가 stale/unhealthy/insufficient가 아니고 `micro_state in {neutral,bullish}`일 때 confidence `80~89` raw `SKIP`만 P1 `USE_DEFENSIVE` 경로로 demotion하는 운영 override를 허용한다. 종료된 latency composite 세부 경과는 archive에서만 본다.
6. 현재 보유/청산 owner는 `soft_stop_micro_grace`, `REVERSAL_ADD`, `holding_flow_override`다. `bad_entry_refined_canary`는 `2026-05-04` 장후 OFF됐고, refined 후보는 `SCALP_BAD_ENTRY_REFINED_OBSERVE_ENABLED=True` report-only counterfactual로만 남긴다. `soft_stop_expert_defense v2`는 종료했고, naive `bad_entry_block`은 observe-only 근거로만 남긴다.
7. `2026-05-04 KST` 장전부터 `holding_flow_override`를 기존 튜닝 관찰축과 별개인 운영 override로 둔다. 적용 대상은 `AI/soft stop/trailing/bad-entry refined` 청산 후보와 오버나이트 `SELL_TODAY` 후보이며, hard stop/protect hard stop/주문·잔고 안전장치는 즉시 실행을 유지한다. 단일 점수구간 컷 대신 최근 tick 30개, 분봉 60개, 최근 flow review history를 넣어 `flow_state/evidence/action` 흐름으로 최종 판단하고, 최초 후보 대비 추가악화 `0.80%p` 또는 최대 보류 `90초` 도달 시 기존 청산을 허용한다. OFI smoother는 이 override 내부 postprocessor로만 허용하며 `EXIT + stable_bullish`는 기존 guard 범위 안에서 debounce, `HOLD/TRIM + stable_bearish + 후보 이후 추가악화 >=0.30%p`는 기존 청산 confirm으로만 쓴다. `2026-05-04` 장후부터 오버나이트 flow `TRIM`은 부분청산 구현이 없으면 `HOLD_OVERNIGHT`가 아니라 원래 `SELL_TODAY` 유지로 본다.
8. `2026-05-11 KST` 사용자 운영 지시로 main live AI routing은 OpenAI로 고정한다. 스캘핑 `watching/holding/shared/gatekeeper/overnight` 등 runtime router를 타는 live 판단은 `KORSTOCKSCAN_SCALPING_AI_ROUTE=openai`를 기준으로 실행하며, 라우터 기본값도 OpenAI다. OpenAI runtime tier는 `FAST=gpt-5-nano`, `REPORT=gpt-5.4-mini`, `DEEP=gpt-5.4`로 둔다. `src/run_bot.sh`는 OpenAI live route 안정화를 위해 `KORSTOCKSCAN_OPENAI_TRANSPORT_MODE=responses_ws`, `KORSTOCKSCAN_OPENAI_RESPONSES_WS_ENABLED=true`, `KORSTOCKSCAN_OPENAI_RESPONSES_WS_POOL_SIZE=2`, `KORSTOCKSCAN_OPENAI_RESPONSES_WS_TIMEOUT_MS=15000`, `KORSTOCKSCAN_OPENAI_RESPONSES_MAX_OUTPUT_TOKENS=512`, `KORSTOCKSCAN_OPENAI_REASONING_EFFORT=auto`을 export한다. Responses WS는 `analyze_target` 계열 hot path에만 적용하고, HTTP fallback과 일반 HTTP 경로는 기존 key rotation lock을 유지한다. Gemini/DeepSeek는 별도 명시 env나 OpenAI 키/초기화 실패 시의 fallback/비운영 분석 경로로만 본다.
9. Threshold AI correction은 runtime routing과 분리된 proposal layer이며 `gpt-5.5 -> gpt-5.4 -> gpt-5.4-mini` fallback과 strict JSON schema를 사용한다. 장중 runtime threshold mutation 금지는 유지하고, provider route 전환은 threshold 값/주문가/수량 guard 변경과 분리해 provenance로 판정한다.

## 2. 용어 범례

| 표현 | 한글 설명 | 현재 판정 |
| --- | --- | --- |
| `fallback` | 정상 진입이 막힌 상황에서 보조 주문 경로로 진입을 시도하던 예외 경로 | 신규 사용 금지, 폐기 유지 |
| `fallback_scout/main` | `fallback_scout` 탐색 주문과 `fallback_main` 본 주문이 같은 축에서 함께 나가던 2-leg fallback 분할진입 | 영구 폐기. 재개/승격/canary 대상 아님 |
| `fallback_single` | scout/main으로 나뉘지 않은 단일 fallback 진입 경로 | 영구 폐기. 재개/승격/canary 대상 아님 |
| `latency fallback split-entry` | latency 상태가 `CAUTION/DANGER`일 때 fallback 허용으로 분할진입을 시도하던 경로 | 영구 폐기. latency bugfix 대상과 분리 |
| `main-only` | `songstock`/remote 비교를 제외하고 메인서버 실전 로그만 기준으로 보는 방식 | Plan Rebase 기간의 기준선 |
| `normal_only` | fallback 태그와 예외 진입이 섞이지 않은 정상 진입 표본 | 손익/성과 비교의 우선 기준 |
| `post_fallback_deprecation` | `2026-04-21 09:45 KST` fallback 폐기 이후 새로 쌓인 표본 | 폐기 이후 효과 확인 기준 |
| `canary` | 작은 범위로 실전에 적용해 성과와 리스크를 검증하는 단일 owner 변경 | 동일 단계 안에서는 단일 owner를 기본으로 하되, 같은 family 안의 bounded multi-arm canary는 arm별 provenance와 safety guard가 닫힌 경우 허용 |
| `stage-disjoint concurrent canary` | 진입병목과 보유/청산처럼 단계, 조작점, 적용시점, cohort tag, rollback guard가 분리되는 병렬 canary | 단계가 다르면 동시 존재 가능. 단, 동일 단계 내 canary 중복은 금지하고, 두 축 중 하나라도 cohort 혼선이 생기면 해당 단계 단일축 원칙으로 복귀 |
| `shadow` | 실전 주문에는 반영하지 않고 병렬 계산만 하던 검증 방식 | 신규/보완축에서는 금지 |
| `buy_recovery_canary` | Gemini `WAIT 65~79` 과밀 구간을 2차 재평가해 BUY 회복 여부를 보던 진입축 | 현재 live owner 아님. `2026-04-23` 이후 downstream 제출병목 축으로 교체됐고, 재개 시 새 checklist 승인 필요 |
| `entry_filter_quality` | 불량 진입을 줄이고 제출/체결 품질을 높이는 정식 튜닝 후보 | submitted 병목 해소 전까지 parking. 재개는 현재 entry owner와 충돌하지 않는 단일축으로만 가능 |
| `latency_quote_fresh_composite` | `ws_age`, `ws_jitter`, `spread`, `other_danger`가 단일 사유가 아니라 quote freshness family로 겹쳐 제출을 막는 복합축 | `2026-04-29 08:29 KST` OFF + restart 완료. 현재 standby/off이며, `signal>=88`, `ws_age<=950ms`, `ws_jitter<=450ms`, `spread<=0.0075`, `quote_stale=False` 묶음은 historical/reference 축으로만 남긴다 |
| `latency_signal_quality_quote_composite` | `latency_quote_fresh_composite` 미회복 시 검토한 예비 복합축. quote freshness 완화폭을 넓히는 대신 `signal>=90`, `latest_strength>=110`, `buy_pressure_10t>=65`를 요구했다 | `2026-04-29 12:50 KST` 운영 override로 OFF. post-restart `budget_pass=972`, `submitted=0`, 후보 통과 0건으로 효과 미약 판정 |
| `mechanical_momentum_latency_relief` | score 70대 mechanical fallback 상태에서 `budget_pass` 이후 수급/강도와 quote freshness 조건이 충분하면 latency DANGER를 normal 주문으로 넘기는 entry replacement 축 | `2026-04-29 12:50 KST` 운영 override로 live ON. 조건은 `signal_score<=75`, `latest_strength>=110`, `buy_pressure_10t>=50`, `ws_age<=1200ms`, `ws_jitter<=500ms`, `spread<=0.0085`, `quote_stale=False`다. `2026-05-06` 장중 submitted drought 대응으로 active owner 내부 단일 파라미터 `spread 0.0085 -> 0.0100`을 적용했으나, post-restart submitted cohort에서 두산(000150) `COMPLETED + valid profit_rate=-2.19%`가 발생해 rollback guard 기준으로 `0.0085`에 복귀했다. 단, 두산은 `latency_canary_applied=False`, `safe_normal_entry_allowed`인 non-mechanical 표본이므로 다음 원인축은 mechanical cap이 아니라 `SAFE normal submit 직전 음수 수급/strength fade`와 `holding_flow_override defer cost`로 분리한다. `2026-05-04`부터 `AI_SCORE_50_BUY_HOLD_OVERRIDE_ENABLED=True`라 score 50 또는 `ai_fallback_score_50=True`는 신규 BUY로 내려보내지 않고 `blocked_ai_score`로 보류한다 |
| `dynamic_entry_price_resolver_p1` | `signal_radar.target_buy_price`를 참고 기준가로만 쓰고, strategy-aware resolver가 defensive price와 timeout profile을 결정하는 진입가 기본 경로 | `2026-05-01` 구현 완료. `SCALPING_ENTRY_PRICE_RESOLVER_ENABLED=True`, best bid 대비 `80bp` 초과 하향 기준가는 거부하고, 일반 스캘핑 `90초`, `BREAKOUT 120초`, `PULLBACK 600초`, `RESERVE 1200초` timeout을 분리한다. entry price canary의 fallback baseline으로도 사용한다 |
| `dynamic_entry_ai_price_canary_p2` | submitted 직전 Tier2 AI가 reference target, defensive price, live quote, spread/latency, 호가/체결강도와 OFI/QI orderbook micro feature를 보고 최종 주문가를 재결정하는 진입가 canary | `2026-05-01` 구현 완료, `2026-05-02` OFI/QI P2 내부 feature 확장 완료. `SCALPING_ENTRY_AI_PRICE_CANARY_ENABLED=True`, `SCALPING_ENTRY_PRICE_ORDERBOOK_MICRO_ENABLED=True`, `entry_price_v1` schema, min confidence `60`, skip min `80`. `USE_DEFENSIVE | USE_REFERENCE | IMPROVE_LIMIT | SKIP` 중 하나를 선택하며 AI 실패/parse fail/guard 위반은 P1 resolver로 fail-closed한다. `SCALPING_ENTRY_AI_PRICE_OFI_SKIP_DEMOTION_ENABLED=True`일 때 confidence `80~89` raw `SKIP`은 OFI stale/unhealthy/insufficient가 아니고 `micro_state in {neutral,bullish}`이면 `entry_ai_price_ofi_skip_demoted` stage를 남기고 P1 `USE_DEFENSIVE` 경로로 demotion한다. confidence `>=90`, `stable_bearish`, stale/unhealthy/insufficient는 기존 `SKIP`을 유지한다. `2026-05-08` 파두 미체결/취소 표본 이후 `WAIT+score>=75+DANGER+USE_DEFENSIVE`는 `passive_probe` lifecycle로 태그하고 bid 대비 최대 1틱 아래로 보정, 30초 timeout, submit revalidation/cancel provenance를 `pre_submit_price_guard` family에 남긴다. `2026-05-11` 세아베스틸지주 표본 이후 passive probe submit revalidation이 `stale_context_or_quote`이면 브로커 제출 전 `entry_submit_revalidation_block`으로 차단한다 |
| `ofi_qi_expansion_ladder` | OFI/QI가 P2 내부 입력으로 반영된 뒤 stale 되지 않게 prompt/report/calibration 후속 owner를 묶는 확장 계획 | P2 내부 low-confidence SKIP demotion과 `holding_flow_override` 내부 postprocessor까지만 운영 override로 허용한다. `OFI standalone BUY/EXIT hard gate`, watching 단계 live gate, symbol-level runtime threshold는 별도 workorder 없이는 금지한다. 3/5/10분 wide OFI persistence는 스윙 전환 단독 gate가 아니라 `winner_wide_window_candidate` report-only 미시구조 보조 증거로만 허용한다. bucket calibration ON은 기존대로 기본 OFF이며 별도 workorder, manifest id/version, sample floor, fallback 급증 guard가 필요하다. [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `OFIQExpansionLadder0508`은 prompt/report/calibration 확장계획 owner이고, [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `OFIAISmoothing*`가 live override 로드와 manifest_only threshold family 판정을 소유한다 |
| `holding_exit_observation` | 보유/청산 후보를 saved snapshot, post-sell, pipeline event로 분해하는 리포트 축 | live canary가 아니라 관찰/후보 고정용. `partial/full`, `initial/pyramid` 합산 결론 금지. `+0.8% preset TP AI 1회 검문`은 `SCALPING_HOLDING_SYSTEM_PROMPT`를 사용하지만 익절 후보 표본으로 별도 분리해 `scalp_preset_tp_ai_exit_action/hold_action`, `scalp_preset_ai_review_exit`를 본다 |
| `holding_exit_sentinel` | 장중 보유/청산 이상치를 자동 감지·분류·알림하는 report-only 운영 감시축 | `2026-05-06`부터 `09:05~15:30 KST` 5분 cron으로 실행한다. `SELL_EXECUTION_DROUGHT`, `HOLD_DEFER_DANGER`, `AI_HOLDING_OPS`, `SOFT_STOP_WHIPSAW`, `TRAILING_EARLY_EXIT`, `RUNTIME_OPS`, `NORMAL`을 분류하고 `NORMAL`은 무음이다. 자동 매도, 보유/청산 threshold mutation, AI cache TTL mutation, bot restart는 금지한다 |
| `panic_sell_defense` | 패닉셀 급변 구간의 stop-loss cluster, active sim/probe 회복률, post-sell rebound를 한 산출물에서 분리하는 report-only attribution 축 | `NORMAL/PANIC_SELL/RECOVERY_WATCH/RECOVERY_CONFIRMED`를 분류하지만 runtime mutation 권한은 없다. hard/protect/emergency stop 지연, score threshold 완화, stop-loss 완화, 자동매도, bot restart, approval artifact 없는 스윙 실주문 전환은 금지한다. V2 1차 후보는 기존 보유 청산 변경이 아니라 신규 long pre-submit을 방어적으로 막는 `panic_entry_freeze_guard`로 고정하며, approval artifact/rollback guard/runtime env key가 닫힌 뒤에만 다음 장전 bounded canary 후보가 된다. `panic_stop_confirmation`, `panic_rebound_probe`, `panic_attribution_pack`은 장후 attribution이 더 닫힌 뒤 후순위로 검토한다 |
| `panic_buying` | 패닉바잉 급등/소진 구간을 탐지해 fixed TP 전량청산 대비 runner opportunity를 분리하는 report-only attribution 축 | `NORMAL/PANIC_BUY_WATCH/PANIC_BUY/EXHAUSTION_WATCH/BUYING_EXHAUSTED`를 분류하지만 runtime mutation 권한은 없다. TP 정책 변경, trailing 강화, 자동매도/자동매수, threshold/provider 변경, bot restart는 금지한다. 장후 attribution이 닫힌 뒤에만 `panic_buy_runner_tp_canary`를 별도 checklist/approval/rollback guard가 필요한 미래 bounded canary 후보로 검토한다 |
| `swing_blocked_origin_quality_guard` | 스윙 probe/dry-run에서 `blocked_swing_gap`, `blocked_swing_score_vpw`, `blocked_gatekeeper_reject`가 만든 손실 cluster와 missed rebound를 한 family로 묶어 보는 blocked-origin 품질 guard 후보 | 세 origin을 각각 별도 threshold/family로 미세조정하지 않는다. origin은 `attribution/cohort tag`로만 유지하고, 장후 자동화는 손익, 반복손실, active recovery, missed rebound, source-quality를 단일 guard 후보의 cap 축소/real-like EV 제외/counterfactual 보존 판단으로 집계한다. 다음 장전 후보가 필요하면 1개 family, 1개 ON/OFF, 제한된 cap 조정으로만 넘기며, origin별 gap %, score floor, gatekeeper reject reason을 당일 표본만으로 각각 runtime mutation 후보화하지 않는다 |
| `sentinel_threshold_feedback` | Sentinel 이상치를 threshold-cycle 후보, incident playbook, instrumentation gap, normal drift로 라우팅하는 운영 피드백 루프 | 튜닝 완료의 정의는 이상치가 사라지는 상태가 아니라 `반복 이상치가 owner와 전환 경로를 가진 상태`다. 반복 이상치가 sample floor, daily/rolling/cumulative 방향 일치, safety guard owner를 만족하면 `R3_manifest_only` 또는 `calibrated_apply_candidate` 후보로 연결한다. 조건 미달은 rollback이 아니라 calibration trigger이며, safety breach만 revert 후보로 분리한다. 1회성 운영장애는 playbook, 로그 누락은 instrumentation backlog, 정상 변동은 no-action으로 분리한다. Sentinel 자체는 live score/spread/청산/재시작 mutation을 수행하지 않는다 |
| `soft_stop_micro_grace_extend` | soft stop 최초 유예 20초가 너무 짧을 때 threshold 근처에서 1회 추가 유예하는 보조 파라미터 | standby/off. `soft_stop_micro_grace` 20초 축의 hard stop/동일종목 손실/미체결 비악화가 확인되고도 반등 포착이 부족할 때만 검토한다 |
| `soft_stop_expert_defense` | `soft_stop_micro_grace v2`로 `stop arbitration layer`, `thesis invalidation veto`, `orderbook absorption stop`을 live에 묶고, `MAE/MFE quantile`, `recovery probability`, `partial de-risk`, `adverse fill`은 shadow/observe로 분리한 보유/청산 방어망 | `2026-04-30 12:00~15:30 KST` same-day 수집 축으로 종료. 다음 재승인 전 기본 OFF이며 로그는 다음 방어망 설계 근거로만 유지한다 |
| `valid_entry_reversal_add` | 진입 판단은 유효했지만 초반 눌림이 발생한 표본에서 저점 미갱신, AI 회복, 수급 재개가 확인될 때 평단을 낮추는 소형 추가매수 canary | `2026-04-30` 소형 canary. 기본 조건은 `REVERSAL_ADD_ENABLED=True`, `profit_rate -0.70%~-0.10%`, `held_sec 20~180`, `AI>=60`, bottom 대비 `+15pt` 또는 연속회복, 수급 3/4 충족이다. 동일 포지션에서도 count/one-shot semantic으로 막지 않고, `POST_ADD_EVAL`, `scale_in_cooldown`, `pending_add_order`, `position_cap`, protection fail-closed가 반복 사용 가드다. `2026-04-30 10:15 KST` intraday override로 오전 blocker의 대부분을 차지한 `pnl_out_of_range`, `hold_sec_out_of_range`만 완화하고 AI/supply 조건은 유지한다 |
| `scale_in_price_resolver_p1` / `scale_in_dynamic_qty` | 수익/손실 중인 포지션의 `PYRAMID`/`REVERSAL_ADD` 주문 직전 가격·수량을 신규 BUY와 분리해 재결정하는 scale-in 경로 | `2026-05-06` 삼성생명 PYRAMID 추격 지정가 표본 후 live replacement로 적용. P1 가격은 `best_bid` 또는 defensive tick 지정가만 허용하고 `ws_data.curr` 직접 제출은 금지한다. P2 `scale_in_price_v1`은 observe-only 로그이며 live 주문가/주문 여부를 바꾸지 않는다. `2026-05-09` 후속 사용자 지시로 추가매수 1주 수량 cap은 ON으로 복귀했고, 해제는 `position_sizing_cap_release` 기준 충족 시 사용자 승인 요청으로만 승격한다 |
| `statistical_action_weight` | 가격대/거래량/시간대별로 `exit_only`, `avg_down_wait`, `pyramid_wait`의 후행 성과를 비교해 threshold weight와 동적 수량화 근거를 만드는 decision-support 축 | report-only. `exit_signal`, `sell_completed`, `scale_in_executed` compact 표본과 completed trade를 연결해 장후에만 본다. 단순 평균이 아니라 empirical-bayes shrinkage와 lower-confidence score를 사용하며, live runtime threshold나 주문 행동을 직접 바꾸지 않는다 |
| `stat_action_decision_snapshot` | HOLDING 판단 순간의 후보/선택/차단 행동과 포지션·수급·호가 상태를 남기는 statistical action weight용 수집 이벤트 | observe-only. 기본 30초 rate-limit로 IO를 제한하며 `chosen_action`, `eligible_actions`, `rejected_actions`를 남겨 selection bias를 줄인다. live 행동 변경 없음 |
| `bad_entry_block` | soft stop으로 이어질 가능성이 큰 never-green/AI fade 유형을 조기정리 후보로 분류하는 observe-only classifier | `2026-04-30` observe-only. 표본 수는 충분하지만 `GOOD_EXIT` 제거 위험이 남아 단순 live block은 금지한다. `bad_entry_refined_canary`는 `2026-05-04` 장후 canary-applied cohort가 flow defer와 장마감 `sell_order_failed` 반복에 섞여 rollback guard로 OFF됐다. 재개는 기존 keep이 아니라 새 단일축 workorder와 rollback guard가 필요하다. `bad_entry_refined_candidate` 자체는 runtime provisional signal이며, 최종 유형은 진입부터 청산, 장후 `post_sell_evaluations`까지 record_id로 join한 lifecycle attribution에서만 닫는다 |
| `holding_flow_override` | 보유/청산과 오버나이트 `SELL_TODAY` 후보를 단일 점수 컷이 아니라 긴 입력 윈도와 AI flow summary로 재검문하는 운영 override | `2026-05-04` 장전부터 적용. 기존 튜닝 관찰축과 별개이며 장중 `HOLD/TRIM`은 v1에서 전량청산 보류만 뜻한다. `EXIT`, AI/parse/stale/context 실패, 보류 90초 초과, 최초 후보 대비 추가악화 `0.80%p` 도달은 기존 청산 허용이다. `HOLDING_FLOW_OFI_SMOOTHING_OVERRIDE_ENABLED=True`일 때 OFI smoother는 flow 결과 후처리로만 적용한다. `EXIT + stable_bullish`는 `holding_flow_ofi_smoothing_applied(DEBOUNCE_EXIT)`로 보류하고, `HOLD/TRIM + stable_bearish + worsen>=0.30%p`는 `CONFIRM_EXIT`로 기존 청산을 확정한다. hard stop/protect hard stop/주문·잔고 safety, max defer, worsen floor가 항상 OFI보다 우선한다. 오버나이트 판정은 `15:20 KST`로 앞당기되, flow `HOLD`만 `HOLD_OVERNIGHT` 전환 가능하고 `TRIM`은 부분청산 구현 전까지 원래 `SELL_TODAY`를 유지한다. `15:20~15:30` 추가악화 `0.80%p` 시 `SELL_TODAY`로 복귀한다 |
| `nan_cast_guard_followup` | 주문·체결·DB 복원 숫자 필드에 `NaN/inf`가 유입될 때 런타임 중단과 상태전이 실패를 막기 위한 숫자 정규화/업스트림 source 재분해 계획 | live canary 아님. 런타임 안정화/집계 품질 보강용 follow-up으로만 관리하고, 기대값 해석 입력은 재발건수·영향경로·미진입/미청산 기회비용 분해를 함께 남긴다 |
| `openai_transport_parity_flag_off` | OpenAI가 Gemini와 같은 endpoint schema registry/contract 기준을 공유하되, transport는 HTTP baseline과 WS shadow를 분리 관찰하는 acceptance 축 | `2026-04-30` 기준 flag-off observe-only. `response schema registry`, `deterministic JSON config`, `Responses WS transport`는 모두 rollback owner와 cohort를 잠근 뒤에만 다음 슬롯으로 넘긴다 |

### 2.1 Shadow / Canary / Cohort 운영 정의

1. `shadow`는 실주문, 실청산, 실판단을 바꾸지 않고 병렬 계산과 로그만 남기는 관찰 경로다. 현재 운영 원칙상 신규 alpha 튜닝축에는 쓰지 않는다.
2. `canary`는 ON/OFF 가능한 단일 조작점이 실주문 또는 실판단을 실제로 바꾸는 제한적 live 변경이다. 적용 대상, rollback owner, cohort tag가 raw event에서 복원 가능해야 한다.
3. `cohort`는 live/observe/excluded 모집단을 분리해 성과와 rollback을 섞이지 않게 잠그는 판정 단위다. 최소 `baseline cohort`, `candidate live cohort`, `observe-only cohort`, `excluded cohort`를 함께 기록한다.
4. `운영 override`는 canary와 달리 튜닝 가설 검증보다 실전 보호/보정 목적이 우선인 runtime 우선순위 변경이다. `holding_flow_override`, `mechanical_momentum_latency_relief`처럼 same-day 적용이 가능하지만, 이 경우에도 cohort와 rollback guard는 동일하게 잠근다.
5. `baseline-promote`는 이름에 canary가 남아 있어도 실질적으로 기본 운영경로로 굳은 상태를 뜻한다. baseline-promote 후보는 신규 canary로 세지지 않지만, rename과 문서 정리는 별도 change set으로 닫아야 한다.

## 3. 튜닝 원칙

### 3.1 성과 판정과 데이터 기준

1. 최종 목표는 손실 억제가 아니라 기대값/순이익 극대화다.
2. 손익 판단은 `COMPLETED + valid profit_rate`만 사용한다. `NULL`, 미완료, fallback 정규화 값은 손익 기준에서 제외한다.
3. `full fill`과 `partial fill`은 합산하지 않는다. 체결 품질, 후행 청산, 손익은 각각 분리해 해석한다.
4. 비교 우선순위는 `거래수 -> 퍼널 -> blocker -> 체결품질 -> missed_upside -> 손익`이다. `counterfactual` 수치는 직접 실현손익과 합산하지 않고 우선순위 판단 자료로만 쓴다.
5. BUY 후 미진입은 `latency guard miss`, `liquidity gate miss`, `AI threshold miss`, `overbought gate miss`로 분리한다.
6. `ADM/SAW`, BUY 병목, bad-entry 차단은 완벽한 spot 탐색이 아니라 `efficient_tradeoff_gate`를 통과한 bounded live canary를 먼저 만들고, 장중/장후 attribution으로 calibration한다.
7. sample 부족은 live 전면 금지가 아니라 `cap 축소`, `hold_sample`, `max_step_per_day 축소` 중 하나로 처리한다. 다만 최소 edge가 없는 `no_clear_edge`, defensive-only/high-loss, safety guard 미정은 live 변경이 아니라 calibration/readiness 보류다.
8. 원인 귀속이 불명확하면 전략 threshold를 먼저 바꾸지 않고 리포트 정합성, 이벤트 복원, 집계 품질을 먼저 점검한다.
9. 승률은 `diagnostic_win_rate`이며 단독 live/canary 승인 기준이 아니다. 기대값/순이익 판정은 `primary_ev` 지표가 맡고, 단순 합산값은 EV가 아니라 `simple_sum_profit_pct`로 표시한다.
10. EV 필드는 `equal_weight_avg_profit_pct`, `notional_weighted_ev_pct`, `source_quality_adjusted_ev_pct` 중 하나로 명명하고, `sim/probe/counterfactual/combined` 권한 지표는 real execution 품질이나 실주문 전환 근거로 단독 사용하지 않는다.
11. 자동화 체인이 소비하는 새 관찰지표는 생성 시점에 `metric_role`, `decision_authority`, `window_policy`, `sample_floor`, `primary_decision_metric`, `source_quality_gate`, `forbidden_uses`를 함께 선언한다. 미선언 지표는 `instrumentation_gap` 또는 `source_quality_blocker`로만 라우팅한다.
12. daily-only 수치는 incident, safety veto, freshness/source-quality, 장중 운영 trigger에는 쓸 수 있지만, edge apply 승인에는 rolling/cumulative 또는 `post_apply_version_window`와 함께 확인해야 한다.

### 3.2 AI/entry 평가 원칙

1. A/B 테스트의 목적은 단일 모델 우열 확인이 아니라 진입/보유/청산 각 단계에서 `EV`를 높여 기준 조합 대비 `최소 +10%` 개선되는 엔진 조합을 찾는 것이다.
2. AI `fast -> deep` 재판정 트리거는 엔진별 하드코딩 점수대가 아니라 `feature conflict predicates + 최근 실거래 EV 근거`로 관리한다.
3. 재판정 구조를 공통축으로 승격할 때는 `submitted/full_fill/partial_fill/COMPLETED + valid profit_rate` 기준으로 `재판정 코호트 vs 비재판정 코호트`를 먼저 비교하고, EV 개선이 확인될 때만 live 공통구조로 올린다.
4. `gatekeeper_fast_reuse_ratio`, `gatekeeper_eval_ms_p95`, `signature/window blocker`는 제출병목의 보조 진단 지표다. `submitted/full/partial` 회복 또는 `latency_state_danger` 감소 없이 이 지표만으로 entry live 후보를 승격하지 않는다.

### 3.3 Canary, Cohort, Live 변경 원칙

1. live 변경은 동일 단계 내 단일 owner canary를 기본으로 한다. 복합 entry canary는 단일축 원칙의 예외가 아니라 `단일 가설을 구성하는 묶음 축`이며, 묶음 파라미터는 개별 ON/OFF 효과로 분리 판정하지 않는다. 물타기/불타기처럼 실제 체결 표본이 부족한 family는 `exit_only`, `avg_down_wait`, `pyramid_wait` arm을 같은 owner 안에서 safety guard와 cohort allocation을 고정한 multi-arm canary로 배정해 샘플을 만들 수 있다.
2. canary를 live로 열기 전에는 `분포 기준`, `예상 기각률`, `효과 부족 시 fallback 임계값`, `candidate cohort`, `rollback guard`, `OFF 조건`, `판정 시각`, `same-stage owner 충돌 여부`를 같은 문서 또는 checklist에서 모두 닫는다. 하나라도 비어 있으면 observe-only 또는 guarded-off로 둔다.
3. active canary의 다음 판정 baseline은 가능하면 같은 bundle 내 `canary_applied=False + normal_only + post_fallback_deprecation` 표본으로 고정한다. baseline이 `N_min` 미달이거나 `ShadowDiff` 같은 집계 경로 차이가 남아 있으면 hard pass/fail 대신 방향성 판정으로 격하한다.
4. canary를 `live 유지`에서 `baseline 운영`으로 전환할 때는 최소 `N_min`, 핵심 metric, canary-applied vs baseline cohort 비교, cross-contamination 부재, restart/rollback 경로를 함께 닫는다. 단순히 “문제 없었다”만으로는 승격하지 않는다.
5. 예비축/예비 파라미터는 active 축의 실패, 표본부족 재판정, 또는 별도 workorder가 닫히기 전에는 live ON 하지 않는다. 활성화하려면 기존 동일 단계 canary OFF, rollback guard, restart 필요 여부, baseline cohort를 같은 checklist에 잠근다.

### 3.4 폐기축과 Shadow 금지선

1. `fallback_scout/main`, `fallback_single`, `latency fallback split-entry`는 폐기 축으로만 남긴다. 실전 경로 재개, replacement 후보, observe-only runtime shadow 재가동은 새 workorder와 새 rollback guard 없이 허용하지 않는다.
2. 신규/보완 alpha 튜닝축은 shadow-only로 열지 않는다. 단, transport/schema, report-only decision support, counterfactual enrichment처럼 실주문·실청산 판단을 바꾸지 않는 지원축은 문서에 observe/report-only로 명시된 경우에만 허용한다.

### 3.5 장중 판정과 운영 안정화

1. 장중 판정 시각에 fresh 로그가 작업환경에 아직 없으면 판정을 빈칸으로 남기지 않는다. 같은 시각 기준 lightweight/offline bundle을 생성하고, 산출물로 same-slot 판정을 닫는다.
2. offline/live canary bundle은 장중 과부하 방지용 판정 입력이다. heavy snapshot/report builder를 반복 호출하지 않고, hard pass/fail 전제 충족 여부와 direction-only 사유를 확인하는 입력으로만 사용한다.
3. `offline_live_canary_bundle`은 standby diagnostic/report-only 표준 경로다. retired `offline_gatekeeper_fast_reuse_bundle` 전용 codebase의 legacy `gatekeeper_fast_reuse`/`entry_latency_offline` summary compatibility는 이 경로에서 생성하며, live threshold/order/exit 판단을 직접 변경하지 않는다.
4. `NaN cast` 계열 오류는 손익 축이 아니라 런타임 안정화 축으로 분리한다. 판정은 `재발 건수`, `루프 중단 여부`, `BUY_ORDERED->HOLDING/청산 상태전이 실패`, `미진입/미청산 기회비용` 기준으로 남기고, 메인 코드베이스 기준의 최소 안전 캐스팅/업스트림 source 추적 계획을 우선한다.

## 4. 작업 규칙

| 규칙 | 기준 | 위반 시 처리 |
| --- | --- | --- |
| 동일 단계 단일 live canary | 단일 live canary 원칙은 동일 단계 안에서만 적용한다. 진입병목축과 보유/청산축은 별개 단계이므로 양측에 canary가 동시에 존재할 수 있다. | 동일 단계 안에서 동시 2축 live 금지. 같은 단계 안에서 교체가 필요하면 `기존 축 OFF -> restart.flag -> 새 축 ON` 순서를 강제한다. |
| stage-disjoint 병렬 canary | 진입병목 canary와 보유/청산 canary는 조작점, 적용 시점, cohort tag, rollback guard가 완전히 분리되면 병렬 live가 가능하다. 이 경우 전체 성과 합산 판정은 금지하고 단계별로 분리 판정한다. | entry canary가 유입 cohort를 크게 바꿔 보유/청산 판정이 오염되거나 rollback guard가 공유되면 병렬 판정 무효. 해당 단계의 단일축 원칙으로 복귀한다. |
| shadow 금지 | 신규/보완축은 shadow 없이 canary-only | shadow 항목은 폐기 또는 코드정리로 격하 |
| canary -> live 전환 | canary를 기본 live owner 또는 운영 기본값으로 승격할 때는 `N_min`, 주요 metric 개선, rollback guard 무위반, applied/not-applied cohort 비교, cross-contamination 부재, restart 가능 여부를 checklist와 report에 함께 잠근다. | 수치 기준 미달, applied cohort 복원 불가, same-stage owner 충돌, `COMPLETED + valid profit_rate` 또는 체결품질 악화가 있으면 live 전환 금지. 기존 canary 유지 또는 OFF로 닫는다. |
| 문서 참조 방향 | `docs/personal-decision-flow-notes.md`는 개인판단 정합성 기록용 노트다. 다른 문서에서 `Source` 또는 판정 근거 링크로 사용하지 않는다. 개인문서는 checklist/report/plan 문서나 코드 레퍼런스를 참조해도 된다. | 개인문서가 판정 근거 링크로 쓰이면 링크를 checklist에서 제거하고 기준문서/감사문서 근거로 대체한다. |
| 원격 비교 제외 | Plan Rebase 기간은 main-only 기준 | songstock/remote 비교는 의사결정 입력에서 제외 |
| 라우팅 고정 | live 스캘핑 AI는 Gemini 고정 | A/B는 `entry_filter_quality` 1차 판정 후 별도 판단. 병목 완화 중에는 원격 전체 엔진 라우팅만 GPT로 바꾸는 단순 교체를 금지하고, 진입/보유/청산 단계별 코호트 비교와 품질 가드(`submitted/full/partial/soft_stop/COMPLETED + valid profit_rate`)를 함께 고정한다. |
| 문서 기준 | 중심 문서는 기준, checklist는 실행, report는 근거 | 중복 작업항목 생성 금지 |
| report README 기반 리포트 보강 | [data/report/README.md](../data/report/README.md)는 정기 JSON/JSONL 산출물과 Markdown 누락 후보의 inventory다. 운영/감리 중 사람이 매일 읽어야 하는 canonical JSON/JSONL이 탐지되면 먼저 이 README의 누락 후보에 반영하고, 구현이 필요하면 날짜별 checklist에 `Due/Slot/TimeWindow/Track`이 있는 Markdown 생성 작업계획을 만든다. | JSON/JSONL만 있는 항목을 장후 판정 근거로 계속 쓰거나, 누락 후보를 답변에만 남기면 무효다. 단 raw stream/manifest처럼 사람이 직접 판정하지 않는 항목은 README에 Markdown 제외 사유를 남긴다. |
| threshold 운영문서 | threshold compact collector, POSTCLOSE report, PREOPEN apply plan/runtime env 운영방법은 [data/threshold_cycle/README.md](../data/threshold_cycle/README.md)를 따른다. 현재 apply mode는 완전 무인 `auto_bounded_live`이며, deterministic guard + AI correction guard + same-stage owner rule을 통과한 family만 다음 장전 runtime env로 자동 반영한다. 장중 runtime threshold 자동 변경은 계속 금지한다. Sentinel 이상치는 threshold-cycle 입력 후보가 될 수 있지만, `incident/playbook`, `threshold-family 후보`, `instrumentation gap`, `normal drift` 라우팅을 먼저 통과해야 한다. | README와 다른 wrapper/cron/apply mode로 운영하거나 sample floor/safety guard owner 없이 runtime threshold를 변경하면 safety revert 대상으로 본다. 목표 미달은 rollback이 아니라 calibration trigger로 처리하고, 이상치 발생만으로 threshold를 바꾸거나 장중 runtime mutation을 적용하면 safety 위반이다. daily EV 제출은 `threshold_cycle_ev_YYYY-MM-DD` artifact로 통일한다. |
| prompt/rebase 동기화 | [plan-korStockScanPerformanceOptimization.prompt.md](./plan-korStockScanPerformanceOptimization.prompt.md)는 세션 시작용 경량 포인터지만, Plan Rebase의 active owner, 핵심 참조문서, 문서 운영 규칙이 바뀌면 같은 변경 세트에서 현행화한다. | rebase만 바꾸고 prompt가 과거 일정/owner를 계속 가리키면 다음 문서 변경 때 prompt 현행화를 선행한다. |
| 일정 날짜 고정 | 모든 작업일정은 `YYYY-MM-DD KST`와 `Slot/TimeWindow`로 고정 | `후보비교 완료시`, `관찰 후`, `다음주` 같은 상대 일정은 무효. 당일 안에 절대 날짜/시각으로 재작성 |
| checklist 상단 공통화 | 날짜별 checklist 상단은 [stage2-todo-checklist-template.md](./stage2-todo-checklist-template.md) 형식을 기본으로 사용한다. `오늘 목적`/`오늘 강제 규칙` 공통 bullet는 템플릿에서 복사하고, 날짜별 파일에는 당일 예외만 최소 추가한다. | 미래 checklist가 약한 규칙셋으로 되돌아가면 템플릿 위반으로 보고 같은 턴에 상단 규칙을 재정렬한다. |
| 관찰 반나절 제한 | live 영향 여부 관찰은 오전/오후 반나절을 넘기지 않음 | 반나절에 미관측이면 관찰축 오류, live 영향 없음, 또는 그대로 진행 가능 중 하나로 판정 |
| 관찰 후 즉시 판정/즉시 착수 | 관찰창이 끝나면 같은 거래일 안에서 `즉시 판정 -> 다음 축 즉시 착수`를 기본으로 한다. 이미 수집된 데이터만으로 볼 수 있는 판정은 장후/익일로 미루지 않고 그 자리에서 닫는다. 추가 데이터가 아니라 코드베이스 수정이 필요한 경우에도 same-day 형상/가드/재시작 가능 여부를 먼저 판단하고, 가능하면 즉시 반영 후 새 관찰창을 연다. | `장후 보자`, `다음 장전에 보자` 식 이관은 금지한다. 미룰 수 있는 예외는 `1) 단일 조작점이 아직 정의되지 않음`, `2) rollback guard가 문서화되지 않음`, `3) restart/code-load가 운영 경계상 같은 턴에 불가능함` 뿐이며, 이때도 why와 막힌 조건, 다음 절대시각을 checklist에 함께 고정한다. |
| 장후/익일 이관 무효화 | 관찰축/보조축/승격후보를 장후·익일·다음 장전으로 넘기려면 같은 턴에 `지금 닫을 수 있는가`, `추가 데이터가 필요한가 vs 코드수정이 필요한가`, `same-day 단일 조작점/rollback guard/restart 가능 여부`, `불가하면 막힌 조건과 다음 절대시각` 4가지를 문서에 모두 남겨야 한다. | 위 4개가 없으면 이관 판정은 무효로 본다. 무효 항목은 자동으로 `same-day 재분해/즉시 착수 미이행`으로 간주하고, 다음 응답에서 먼저 재개한다. |
| PREOPEN 이관 제한 | PREOPEN은 전일에 이미 `단일 조작점`, `rollback guard`, `코드/테스트`, `restart 절차`가 고정된 carry-over 축만 받는다. PREOPEN을 `다음에 생각해볼 후보 검토 슬롯`으로 쓰지 않는다. | same-day에 설계/분해 가능한 축을 PREOPEN으로 넘기면 규칙 위반이다. PREOPEN checklist에는 전일 준비완료 증적과 승인/롤백 판정만 남긴다. |
| 판정 근거 서술 | `유지/보류/미완/폐기/완료` 모두 수치 + why(기대값 영향/원인귀속/표본충분성)를 함께 남긴다. 수치만 나열하고 해석이 없으면 근거 미충족으로 본다. | checklist/report에 why를 추가 기재하고, why가 없으면 완료/보류 판정을 다시 연다 |
| 봇 재실행 | 검증/반영에 봇 재실행이 필요하고 권한/안전 조건이 맞으면 AI가 표준 wrapper로 직접 실행 | 토큰/계정/운영 승인 등 보안 경계가 있으면 실행하지 않고 필요한 1개 명령을 사용자에게 요청 |
| 장중 스냅샷 운용 | 장중 판단용 스냅샷은 `12:00~12:20 full` 1회를 기준으로 하고, 그 외 장중은 `intraday_light` 증분(지연/jitter 포함)으로만 갱신한다. `server_comparison`은 Plan Rebase 기본 입력에서 제외하며, 명시 플래그(`KORSTOCKSCAN_ENABLE_SERVER_COMPARISON`)가 있을 때만 생성한다. `performance_tuning` 거래일 이력은 고정 3일이 아니라 상황별 가변 window(`trend_max_dates` 또는 env)로 운용한다. 체크리스트에 스냅샷 생성 작업을 둘 때는 `max_date_basis`, `trend_max_dates`, `evidence_cutoff`를 함께 기재한다. 12시 full 스냅샷 완료 후에는 admin Telegram 완료 알림을 발송해 일일 작업지시서 전달 기준으로 삼는다. | full 다중 실행, 고정 기간 강제, max date/cutoff 미기재, 원격 비교값을 기준 판단에 혼입하면 규칙 위반으로 간주하고 main-only 기준으로 재집계한다. |
| 보유/청산 관찰 리포트 | `holding_exit_observation`은 saved monitor snapshots, `data/post_sell/*.jsonl`, `data/pipeline_events/*.jsonl*`만 입력으로 쓰고, fresh snapshot이 필요하면 safe wrapper/cron만 사용한다. 출력 필드는 `readiness`, `cohorts`, `exit_rule_quality`, `trailing_continuation`, `soft_stop_rebound`, `same_symbol_reentry`, `opportunity_cost`, `load_distribution_evidence`로 고정한다. `soft_stop_rebound` 하위에는 `hard_stop_auxiliary`를 포함해 휩쏘/하드스탑 보조축을 함께 잠근다. `+0.8% preset TP AI 1회 검문`은 동일 holding prompt를 쓰더라도 normal `ai_holding_review` 점수 refresh와 합치지 않고 익절 관찰축에서 별도 판정한다. | foreground direct builder 호출, `partial/full` 합산 결론, `initial/pyramid` 합산 결론, counterfactual 기회비용을 실현손익과 합산하면 판정 무효 |
| PREOPEN 판정 범위 | 장전에는 `restart.flag` 반영, 신규 계측 필드 기록 여부, 전일 carry-over snapshot/로그 존재 여부만 확인한다. 같은 거래일의 `submitted/fill/completed` 발생을 장전 통과조건으로 쓰지 않는다. | 장전 항목에 same-day `submitted/fill/completed`를 완료기준으로 넣으면 무효로 보고, `INTRADAY/POSTCLOSE` 판정으로 재배치한다. |
| canary ON/OFF 반영 | canary flag는 `TRADING_RULES` 생성 시 env/code에서 읽히므로 hot-reload 기준이 아니다. OFF/ON 변경은 env/code 반영 후 `restart.flag` 기반 우아한 봇 재시작이 표준이다 | rollback guard 발동 시 canary OFF 값을 먼저 고정하고 `restart.flag`로 재시작한다. 목표 소요시간은 5분 이내, 토큰/운영 승인 경계가 있으면 사용자 실행 명령을 남김 |
| Kiwoom REST 인증 장애 | `8005 Token이 유효하지 않습니다`처럼 런타임 토큰 무효화가 발생하면 실전 중 토큰 rebind/hot-refresh를 시도하지 않고, `restart.flag` 기반 우아한 봇 재시작만 표준 복구 경로로 쓴다. | 주문가능금액 0원 fail-closed 상태가 반복되면 인증 장애로 분리하고 즉시 우아한 재시작을 수행한다. 장중 hot-patch/re-auth 실험은 canary 판정과 원인귀속을 깨므로 금지한다. |
| runtime env 증적 | canary 판정/재교정/롤백 전후에는 main bot PID의 `/proc/<pid>/environ`에서 핵심 `KORSTOCKSCAN_*` 값을 확인하고 checklist 또는 report에 남긴다. 최소 대상은 해당 축 enable flag, threshold/prompt split, runtime route다. | `/proc/<pid>/environ` 증적 없이 "env 혼선 없음"을 가정해 판정한 항목은 조건부로 취급하고, 다음 판정 전에 provenance 점검을 보강한다. |
| 운영 자동화 증적 분리 | cron/runbook/parquet/manifest/동기화 정상화는 운영 증적이다. 이는 튜닝 효과나 전략 승인 근거와 분리한다. | 운영 정상화 항목을 중심 문서의 active/open 판정으로 승격하지 않는다. 필요 시 `execution-delta`, audit/report 또는 별도 운영문서에만 남긴다. |
| 문서/동기화 | 문서 변경 후 parser 검증은 AI가 실행한다. GitHub Project / Google Calendar 동기화는 AI가 직접 실행하지 않고, 반드시 사용자가 수동 실행한다. | AI는 토큰/캘린더 자격증명 확인과 실제 Project/Calendar 동기화를 시도하지 않는다. 변경 후 사용자에게 실행 명령 `PYTHONPATH=. .venv/bin/python -m src.engine.sync_docs_backlog_to_project && PYTHONPATH=. .venv/bin/python -m src.engine.sync_github_project_calendar` 1개를 남긴다. |
| 환경 변경 | 패키지 설치/업그레이드/제거 전 사용자 승인 | 승인 전 대안 경로 사용 |

## 5. 완료 기준

| 항목 | 완료 기준 |
| --- | --- |
| 튜닝축 선정 | pain point, 실행과제, 정량 목표, rollback guard, 판정시각, 상태가 한 줄로 연결됨 |
| live canary | `N_min`, 주요 metric, rollback guard, OFF 조건, 판정시각이 문서와 로그에 고정됨 |
| 성과판정 | `COMPLETED + valid profit_rate`, full/partial 분리, blocker 분포, 체결품질이 함께 제시됨 |
| 보류/미착수 | 보류 사유, 기대값 영향, 표본충분성 또는 관측 누락 이유, 다음 판정시각 또는 폐기/코드정리 판정이 명시됨 |
| 런타임 안정화 후속 | same-day hotfix를 장기 과제로 넘길 때는 `재발건수`, `영향 경로`, `업스트림 source 후보`, `메인 코드베이스 최소 수정범위`, `다음 절대시각`이 함께 남음 |
| 장후/익일 이관 | `same-day 불가 이유`, `추가 데이터 vs 코드수정` 구분, `단일 조작점`, `rollback guard`, `restart 가능 여부`, `다음 절대시각`이 한 묶음으로 남음 |
| 판정 근거 | 수치, 기준선 비교, why(왜 그 수치가 유지/보류/미완/폐기로 이어지는지)가 한 묶음으로 남음 |
| 폐기 | 재개 조건이 없으면 폐기 문서/부속문서로 내리고 중심 문서에는 요약만 유지 |
| 하위 참조 | 일일 체크리스트, 감사보고서, Q&A, 폐기과제가 역할별로 분리됨 |
| 실제 효과 갱신 | 일일 체크리스트 완료 시 `execution-delta`의 날짜형 효과 기록 또는 audit/report를 같은 턴에 갱신함 |

## 6. 정량 목표와 가드

`latency_quote_fresh_composite`의 기준선과 도달목표는 아래처럼 잠근다.

- 5-parameter bundle rule: `signal/ws_age/ws_jitter/spread/quote_stale` 5개는 묶음 단위로만 ON/OFF하고, 묶음 효과만 판정한다. `ws_age만 유지` 같은 부분 적용은 단일축 원칙 위반으로 금지한다.
- primary baseline: 같은 bundle 안의 `quote_fresh_composite_canary_applied=False`, `normal_only`, `post_fallback_deprecation` 표본
- fallback reference baseline: `2026-04-27 15:00` offline bundle (`budget_pass=7568`, `submitted=11`, `budget_pass_to_submitted_rate=0.1%`, `latency_state_danger=7178`, `full_fill=7`, `partial_fill=0`)
- `ShadowDiff0428`: submitted/full/partial 집계의 live runtime 경로와 offline bundle 경로 간 차이가 `1건 이내`로 좁혀진 상태를 뜻한다.
- hard pass/fail preconditions: `submitted_orders >= 20`, baseline 표본이 `N_min` 이상, `ShadowDiff0428` 해소
- direction-only gate: primary baseline 표본 부족, `submitted_orders < 20`, 또는 shadow diff 미해소 시 방향성 판정으로만 유지/종료를 닫음
- direction-only expiry: direction-only 판정은 발생일로부터 `2영업일 이내` 재판정 필수이며, 미재판정 시 canary를 자동 OFF 한다.

| 지표 | 목표/발동 조건 | 적용축 | 조치 |
| --- | --- | --- | --- |
| `N_min` | 판정 시점 `trade_count < 50`이고 `submitted_orders < 20` | 모든 canary | hard pass/fail 금지, 방향성 판정 |
| `entry_hard_pass_fail_prereq` | `submitted_orders >= 20`, baseline 표본 `>= N_min`, `ShadowDiff0428` 해소 | `latency_quote_fresh_composite` | 전제 충족 시에만 hard pass/fail 허용 |
| `entry_composite_baseline` | 같은 bundle 내 `quote_fresh_composite_canary_applied=False`, `normal_only`, `post_fallback_deprecation` 표본 확보. `ShadowDiff0428` 미해소 시 hard baseline 승격 금지 | `latency_quote_fresh_composite` | baseline 부족/오염 시 direction-only 판정 |
| `entry_arrival_primary` | `budget_pass_to_submitted_rate >= baseline + 1.0%p` | `latency_quote_fresh_composite` | keep 또는 expand 검토 |
| `entry_arrival_secondary` | `latency_state_danger / budget_pass` 비율이 baseline 대비 `-5.0%p` 이상 개선 | `latency_quote_fresh_composite` | 기대값 개선 후보로 승격 검토 |
| `entry_fill_quality_non_worse` | `full_fill + partial_fill`의 `submitted` 대비 전환율이 baseline 대비 `-2.0%p` 이내 | `latency_quote_fresh_composite` | fill quality 비악화로 판정 |
| `loss_cap` | canary cohort 일간 합산 실현손익이 당일 스캘핑 배정 NAV 대비 `<= -0.35%` | live canary | canary OFF, 전일 설정 복귀 |
| `reject_rate` | `normal_only` baseline 대비 `+15.0%p` 이상 증가 | entry canary | canary OFF |
| `latency_p95` | `gatekeeper_eval_ms_p95 > 15,900ms`, 샘플 `>=50` | entry/latency | canary OFF, latency 경로 재점검 |
| `partial_fill_ratio` | baseline 대비 `+10.0%p` 이상 증가 | entry canary | 경고. `loss_cap` 또는 soft-stop 악화 동반 시 OFF |
| `fallback_regression` | `fallback_scout/main`(탐색/본 주문 동시 fallback) 또는 `fallback_single`(단일 fallback) 신규 1건 이상 | 전체 | 즉시 OFF, 회귀 조사 |
| `composite_no_recovery` | `latency_quote_fresh_composite` 적용 표본의 `budget_pass_to_submitted_rate`가 같은 bundle 내 `canary_applied=False` baseline 대비 `+1.0%p` 이상 개선하지 못함 | entry composite canary | canary OFF, 다음 독립축 또는 새 묶음축으로 교체 |
| `direction_only_expiry` | direction-only 판정 후 `2영업일` 내 재판정 미실시 | `latency_quote_fresh_composite` | canary 자동 OFF, 새 판정창 재개 전 재승인 필요 |
| `trailing_exit_rollback` | trailing canary cohort `avg_profit_rate <= 0`, soft_stop 전환율 baseline 대비 `+5.0%p`, 또는 GOOD_EXIT rate 추가 악화 `+15.0%p` 중 하나 충족 | 보유/청산 canary | canary OFF, 전일 설정 복귀 |
| `buy_drought_persist` | canary 후에도 BUY count가 baseline 하위 3분위수 미만이고 `blocked_ai_score_share` 개선 없음 | `buy_recovery_canary` | canary 유지 금지, score/prompt 재교정 |
| `recovery_false_positive_rate` | canary로 회복된 BUY 중 soft_stop 비율이 `normal_only` baseline 대비 `+5.0%p` 이상 증가 | `buy_recovery_canary` | canary OFF, score/prompt 재교정 |
| `initial_entry_qty_cap` | prompt 재교정 이후 신규 BUY 변동성이 커질 때 스캘핑 초기 진입 수량을 제한하는 운영가드 | active/on | `2026-05-09` 후속 사용자 지시로 신규 BUY 1주 수량 cap은 기본 ON으로 복귀했다. `PYRAMID zero_qty`는 신규 진입 cap 확대가 아니라 스캘핑 PYRAMID 최소 1주 floor bugfix로 처리한다. cap 해제는 완벽한 spot 탐색이 아니라 `position_sizing_cap_release` family의 efficient trade-off 기준으로 본다. hard floor는 `normal_only COMPLETED valid >=30`, cap-reduced sample `>=5`, submitted `>=15`, avg profit `>=+0.10%`, downside p10 `>=-2.00%`, order failure rate `<=10%`만 둔다. 그 위에서 avg profit, win rate, full-fill quality, downside tail, order failure, soft-stop tail, cap opportunity를 가중한 `tradeoff_score >=0.70`이면 daily EV/preopen apply artifact의 `approval_requests`로 사용자 승인을 요청한다. 자동 runtime apply는 금지한다. |
| `reversal_add_loss_cap` | `REVERSAL_ADD` 체결 cohort의 당일 `COMPLETED + valid profit_rate` 평균이 `<= -0.30%`, 또는 `reversal_add_used` 후 soft stop 전환율이 baseline 대비 `+5.0%p` 이상 | `valid_entry_reversal_add` | canary OFF, `bad_entry_block` 관찰만 유지 |
| `soft_stop_expert_defense_loss_cap` | guarded cohort의 `COMPLETED + valid profit_rate` 평균이 `<= -0.30%`, guarded 후 hard/protect stop 전이, `sell_order_failed`, 또는 `REVERSAL_ADD` 체결 포지션 적용 cross-contamination 1건 이상 | `soft_stop_expert_defense` | canary OFF, `soft_stop_micro_grace v1`만 유지하고 shadow/observe 로그는 계속 남김 |
| `bad_entry_block_promote_gate` | observe-only 표본 `>=10`에서 classifier 후보의 soft stop/하드스탑 전환율이 비후보 대비 `+10.0%p` 이상이고 missed winner 비율이 낮음 | `bad_entry_refined_canary` | 단순 block이 아니라 refined 조기정리 canary로만 승격하고, `GOOD_EXIT/MISSED_UPSIDE` would-have-been 증가 시 OFF |

## 7. 매매단계별 Open Pain Point

| 단계 | 현재 pain point | 기대값 영향 | 현재 owner / 다음 판정 |
| --- | --- | --- | --- |
| 진입 | BUY 후 제출 전 병목과 mechanical fallback score 표본 처리 | 제출 drought가 지속되면 보유/청산 개선 표본도 고갈된다 | `mechanical_momentum_latency_relief`는 [2026-04-30 checklist](./checklists/2026-04-30-stage2-todo-checklist.md) `MechanicalMomentumLatencyRelief0430-*`에서 반영/판정 완료. 현재는 신규 OPEN 작업이 아니라 운영 override ON 상태다. 다만 `AI score 50`은 [2026-05-04 checklist](./checklists/2026-05-04-stage2-todo-checklist.md) `AIScore50BuyHoldOverride0504-Intraday` 이후 매수보류 override가 우선한다. 장중 `BUY Funnel Sentinel`은 BUY/submitted 병목을 자동 감지·분류하는 detection/report-only owner이며, `2026-05-06`부터 `09:05~15:20 KST` 5분 cron으로 JSON/Markdown 리포트와 권고 액션만 생성한다. `2026-05-08`부터 Sentinel Telegram 알림 코드는 삭제됐고, 이상치도 Telegram으로 전송하지 않는다. score threshold/spread cap/fallback/live threshold/runtime restart는 자동 변경하지 않는다. `2026-05-07` 성과 기준 `budget_pass_events=1111`, `order_bundle_submitted_events=10`, `latency_block_events=1100`, `latency_pass_events=11`, `quote_fresh_latency_pass_rate=1.2%`라 score65~74 표본 외에 latency danger root-cause 분해가 별도 필요하다. 다음 명시 owner는 [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `EntryFunnelRecoveryDecision0508`, `LatencyDangerRootCause0508`이며, 전자는 `BUY Funnel Sentinel + missed_entry_counterfactual + submitted/full/partial/COMPLETED valid PnL`을 한 owner에서 묶어 `score65_74 recovery probe` 다음 장전 enable/hold/drop을 닫고, 후자는 `ws_jitter/ws_age/quote_stale/other_danger/spread` 귀속을 `runtime ops / threshold-family / instrumentation / observe` 중 하나로 잠근다 |
| 진입가 | reference target이 실주문가/timeout을 과도하게 왜곡하는 문제와 submitted 직전 불량 호가 흐름 진입 | 미체결 기회비용과 불리한 추격진입이 동시에 생길 수 있다 | `dynamic_entry_price_resolver_p1`, `dynamic_entry_ai_price_canary_p2`는 [2026-05-04 checklist](./checklists/2026-05-04-stage2-todo-checklist.md)가 로드/health check를 소유한다. OFI/QI P2 내부 feature는 `OrderbookMicroP2Canary0504-Postclose`에서 provenance와 global-threshold fallback을 확인했고, low-confidence SKIP demotion은 P2 내부 운영 override로만 본다. [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `OFIAISmoothingOverride0506-Preopen`이 로드를 소유하고 `ThresholdOpsTransition0506`의 OFI smoothing integration 메모가 manifest_only family 판정을 소유하며, [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `OFIQExpansionLadder0508`은 prompt/report/calibration 확장계획을 소유한다 |
| AI 엔진 | Gemini/DeepSeek/OpenAI 계약 안정화와 live enable 금지선 분산 | parse/contract drift가 생기면 BUY/WAIT/DROP, HOLD/TRIM/EXIT 분포가 바뀌어 원인귀속이 깨진다 | 4/29~4/30 acceptance는 완료. [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `AIEngineFlagOffBacklog0506`이 잔여 flag-off/backlog 재분류를 소유 |
| 보유/청산 | soft stop tail, never-green/AI fade, 조급한 전량청산, winner를 너무 짧은 스캘핑 호흡으로 닫은 뒤 더 높은 가격에 재진입하는 churn | 손실 축소보다 missed upside 회복과 bad-entry tail 절단의 순EV가 핵심이다 | `soft_stop_micro_grace`, `REVERSAL_ADD`, `holding_flow_override`를 분리. OFI smoother는 `holding_flow_override` 내부 postprocessor이며 standalone EXIT gate가 아니다. `bad_entry_refined_canary`는 [2026-05-04 checklist](./checklists/2026-05-04-stage2-todo-checklist.md) `BadEntryRefinedCanary0504-Postclose`에서 OFF됐다. 다음 축은 같은 checklist에서 `bad-entry report-only counterfactual enrichment`로 조기 판정했고, [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `BadEntryRefinedRollback0506-Preopen`이 로드 확인만 소유한다. `한국항공우주` `ID 4932` 같은 winner wide-window 후보는 [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `SwingTrailingPolicy0506`/`TrailingProtectSensitivity0506`에서 report-only 전환 조건으로만 보며, 3/5/10분 wide OFI persistence는 보조 feature일 뿐 live swing 승격 gate가 아니다. `HOLD/EXIT Sentinel`은 장중 `SELL_EXECUTION_DROUGHT`, `HOLD_DEFER_DANGER`, `AI_HOLDING_OPS`, `SOFT_STOP_WHIPSAW`, `TRAILING_EARLY_EXIT`를 감지·분류하는 report-only owner이며 자동 청산/threshold 변경/Telegram 알림을 수행하지 않는다. `2026-05-07` 성과 기준 `holding_flow_override_defer_exit=67`, `soft_stop_rebound priority=1`, `rebound_above_sell_10m_rate=90.0%`, `trailing_continuation MISSED_UPSIDE=5 / GOOD_EXIT=7`가 남아 있어 current owner 유지 vs split owner vs canary hold를 다시 잠가야 한다. 다음 명시 owner는 [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `HoldingFlowDeferCost0508`이다 |
| 포지션 증감 | `REVERSAL_ADD`와 `PYRAMID` 실제 체결 표본이 부족해 물타기/불타기 EV가 report-only 추정에 머문다 | 유효 진입 회수와 winner size-up 기대값이 제한되고, 미실행 arm은 분석 신뢰도가 낮다 | 2026-05-09 후속 사용자 지시로 신규 BUY/REVERSAL_ADD/PYRAMID 1주 수량 cap은 ON으로 복귀했다. 같은 family 안에서 `exit_only`, `avg_down_wait`, `pyramid_wait`를 arm별 provenance로 분리하는 bounded multi-arm canary는 허용하되, 수량 cap 해제는 `position_sizing_cap_release`의 사용자 승인 요청 이후에만 검토한다. [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `ReversalAddDynamicQty0506`, `PyramidDynamicQty0506`의 observe-only `would_qty`는 다음 live micro-canary arm 설계의 입력으로 쓴다. |
| 의사결정 지원 | 가격대/거래량/시간대별 행동가중치와 AI decision matrix가 runtime 판단과 분리됨 | 청산/물타기/불타기/오버나이트/스윙 전환 선택의 기회비용을 놓치면 기대값 개선축 선정이 늦어진다 | `statistical_action_weight`, `stat_action_decision_snapshot`, `holding_exit_decision_matrix`는 [2026-05-04 checklist](./checklists/2026-05-04-stage2-todo-checklist.md) `StatActionDecisionSnapshot0504-Preopen`, [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `StatActionWeight0506`, `StatActionMarkdown0506`, `StatActionAdvancedAxes0506`, `AIDecisionMatrix0506`, [2026-05-07 checklist](./checklists/2026-05-07-stage2-todo-checklist.md) `StatActionEligibleOutcome0507`, `AIDecisionMatrixShadow0507`, [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `StatActionAdvancedContext0508`가 report-only/ADM ladder로 소유한다. `preclose_sell_target`은 2026-05-10 제거되어 현재 owner와 calibration source에서 제외한다 |
| 분석 품질 | missed-entry counterfactual는 아직 `optimistic/realistic/conservative` 3모드로 분리되지 않았지만, 청산 authority provenance는 `exit_decision_source` field taxonomy로 잠겼다 | 기회비용 해석과 청산 원인귀속을 한 문서에서 혼동하면 기대값 개선축 우선순위가 다시 흐려진다 | [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `Phase3Quality0508`, `ExitDecisionSourceProvenance0508`이 `3-mode counterfactual` 보류와 authority provenance 구현 완료를 분리해 닫는다 |
| 스캐너/구조 | candidate/enrich/promote 경계, DB/WS 경계, state handler 단일 모듈 비대 | 좋은 후보를 놓치거나 WS 부하/부분 커밋으로 미진입 기회비용이 커질 수 있다 | [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `ScalpingScanner*`, `StateHandlers*`, `CodeDebt0506`이 설계/분해 순서를 소유 |
| 운영/데이터 | NaN, receipt binding, threshold collector IO, historical aggregation 품질 | 집계/truth 품질이 흔들리면 잘못된 승격/롤백이 발생한다 | [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `NaNCastGuard*`, `ExecutionReceiptsThreadSafety*`, `Threshold*`가 소유 |

## 8. 현재 Open 상태 요약

| 영역 | 현재 상태 | 체크리스트/소유 문서 | 누락 여부 |
| --- | --- | --- | --- |
| entry operating override | `mechanical_momentum_latency_relief` ON. `AI_SCORE_50_BUY_HOLD_OVERRIDE_ENABLED=True`가 score 50 fallback/neutral 신규 BUY를 `blocked_ai_score`로 보류한다. `2026-05-06` `entry_armed` 급감 재분해에서 score 50뿐 아니라 `WAIT 65~74`/`ai_score 65~74 blocked_ai_score`가 budget/pass 이전 병목으로 확인됐으므로, missed-winner/avoided-loser 장후 판정 범위에 `wait65_74_*` report-only 후보를 포함한다. `AI_SCORE65_74_RECOVERY_PROBE_ENABLED=False`는 5/6 장후 구현된 기본 OFF 단일축 canary이며, 기존 `buy_recovery_canary` 재개가 아니라 score65~74, latency DANGER 제외, 수급/가속/micro-VWAP gate, 예산 guard, `score65_74_recovery_probe` cohort log로 분리한다. 5/9 후속 사용자 지시로 신규 BUY 1주 수량 cap은 기본 ON이다. 5/7 PREOPEN 로드 확인에서는 env override 미설정과 5/6 `probe_applied_candidates=0`, `submitted_candidates=0`을 근거로 live enable을 보류했다. `BUY Funnel Sentinel`은 장중 5/10/30분과 전 영업일 동시간대 기준으로 `UPSTREAM_AI_THRESHOLD`, `LATENCY_DROUGHT`, `PRICE_GUARD_DROUGHT`, `RUNTIME_OPS`, `NORMAL`을 분류하는 report-only 자동 점검이며, `09:05~15:20 KST` 5분 cron으로 리포트와 권고 액션만 만든다. `2026-05-08`부터 Sentinel Telegram 알림 기능은 삭제됐다. dynamic action은 리포트/권고 명령까지만 허용한다. 다만 `2026-05-07` 성과 기준 `budget_pass_to_submitted_rate=0.9%`, `latency_block_events=1100`, `latency_pass_events=11`이라 score65~74 판정만으로는 진입병목이 닫히지 않는다 | [2026-04-30 checklist](./checklists/2026-04-30-stage2-todo-checklist.md) `MechanicalMomentumLatencyRelief0430-*`, [2026-05-04 checklist](./checklists/2026-05-04-stage2-todo-checklist.md) `RuntimeFlagInventory0504-Preopen`, `AIScore50BuyHoldOverride0504-Intraday`; [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `Score50BlockedTriage0506-Intraday`, `BuyFunnelSentinel0506-Intraday`, `CooldownPolicyInventory0506`, `DowntrendReentryComposite0506`, `Wait6579RecoveryProbeImplementation0506`; [2026-05-07 checklist](./checklists/2026-05-07-stage2-todo-checklist.md) `Wait6579RecoveryProbePreopen0507`; [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `EntryFunnelRecoveryDecision0508`, `LatencyDangerRootCause0508`, `SentinelTelegramRemoval0508`; [2026-05-11 checklist](./checklists/2026-05-11-stage2-todo-checklist.md) `PositionSizingCapRemoval0509`, `PositionSizingCapRestore0509` | score 50 fallback은 유안타증권 반복손실 대응으로 신규 BUY 보류 override가 우선한다. sentinel이 병목을 감지해도 score threshold 완화, spread cap 재완화, fallback 재개, live threshold runtime mutation, bot restart는 별도 단일축 workorder와 rollback guard 없이는 자동 실행하지 않는다. score65~74 probe와 latency danger root-cause는 5/8 장후 owner 둘로 분리해 다시 본다 |
| entry price baseline/live | `dynamic_entry_price_resolver_p1` baseline 경로 + `dynamic_entry_ai_price_canary_p2` active canary. P2 실패는 P1로 fail-closed. OFI/QI orderbook micro는 P2 내부 입력으로만 쓰며 신규 standalone entry canary로 세지 않는다. confidence `80~89` raw `SKIP` 중 non-bearish OFI만 `entry_ai_price_ofi_skip_demoted`로 P1 defensive 경로에 남긴다. `2026-05-06`에는 latency entry/exit price guard 재튜닝, `80bps` 분포 재앵커, 비-SCALPING observe-only guard, `submitted_order_price` canonical 승격, resolver ingress gate를 carry-over follow-up으로 닫는다. `2026-05-08`에는 파두 `91200/91700` passive 미체결 반복을 근거로 `passive_probe` lifecycle을 추가해 `WAIT+score>=75+DANGER+1주`는 bid-1tick 방어가, 30초 timeout, submit revalidation warning, cancel receipt provenance로 자동 귀속한다. `2026-05-11`부터 stale passive probe는 제출 전 차단하고 세아베스틸지주(001430) 손실은 `mechanical_momentum_latency_relief` 표본이되 `stale_context_or_quote + late_fill + receipt_ignored` 운영 결함 태그로 분리한다 | [2026-05-02 checklist](./checklists/2026-05-02-stage2-todo-checklist.md) `OrderbookMicroP2Canary0502-DesignApply`, [2026-05-04 checklist](./checklists/2026-05-04-stage2-todo-checklist.md) `DynamicEntryResolverP10504-*`, `DynamicEntryAIPriceCanary0504-*`, `OrderbookMicroP2Canary0504-Postclose`; [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `OFIAISmoothingOverride0506-Preopen`, `ThresholdOpsTransition0506`, `LatencyEntryPriceGuard0506HolidayCarry`, `LatencyEntryPriceGuardV2_0506HolidayCarry`, `LatencyExitPriceGuardReview0506HolidayCarry`, `PreSubmitGuardDist0506HolidayCarry`, `PreSubmitGuardObserve0506HolidayCarry`, `BuyPriceSchemaSplitP1_0506HolidayCarry`, `DynamicEntryResolverIngress0506HolidayCarry`; [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `OFIQExpansionLadder0508`, `PassiveEntryProbeLifecycle0508`; [2026-05-11 checklist](./checklists/2026-05-11-stage2-todo-checklist.md) `PassiveProbeLifecycleGuard0511` | entry price follow-up은 `pre_submit_price_guard` family와 daily EV로 자동 관리한다. broad score threshold 완화나 fallback 재개는 여전히 금지한다 |
| AI engine: Gemini | P0 JSON fast-path는 반영됨. P1 `system_instruction`, P1 deterministic JSON config, P2 schema registry는 flag-off 준비/관찰 승인이고 live enable 미승인. `2026-05-06` 삼성생명 보유감시 표본에서 `ai_holding_review` cache MISS 100%와 `holding_flow_override_review` Tier2 provenance 필드 누락이 확인되어, [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `HoldingAICacheAndTierLogging0506-Intraday`가 로깅 보강과 `15:10 KST` 1차 / `17:45 KST` 최종 판정을 소유했다. 그러나 `2026-05-07` 성과 기준 `holding_ai_cache_hit_ratio=0.0`, `gatekeeper_ai_cache_hit_ratio=0.0`, `gatekeeper_eval_ms_p95=12744`, `holding_review_ms_p95=2158`이라 운영 허용성 판정은 아직 닫히지 않았다 | [workorder_gemini_engine_review](./archive/workorders/workorder_gemini_engine_review.md), [2026-04-29 Gemini spec](./ai-acceptance/2026-04-29-gemini-enable-acceptance-spec.md), [2026-04-30 checklist](./checklists/2026-04-30-stage2-todo-checklist.md) `GeminiSchemaIngress0430`, `GeminiSchemaContractCarry0430`; [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `AIEngineFlagOffBacklog0506`, `HoldingAICacheAndTierLogging0506-Intraday`; [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `HoldingAICacheLatency0508` | 5/6 재점검 항목은 로깅 보강으로 닫혔다. cache reuse 실패와 slow path 허용성은 5/8 새 owner로 다시 판정한다 |
| AI engine: DeepSeek | context-aware backoff/retry acceptance는 flag-off 관찰성 보강. gatekeeper structured-output, holding cache bucket, Tool Calling은 backlog 유지 | [2026-04-29 DeepSeek spec](./ai-acceptance/2026-04-29-deepseek-enable-acceptance-spec.md), [2026-04-30 checklist](./checklists/2026-04-30-stage2-todo-checklist.md) `DeepSeekRemoteAcceptance0430`, `DeepSeekAcceptanceCarry0430`, `DeepSeekInterfaceGap0430`; [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `AIEngineFlagOffBacklog0506` | 5/6 재점검 항목으로 보강 완료 |
| AI engine: OpenAI | `2026-05-11 KST` 사용자 운영 지시로 main live AI routing은 OpenAI로 고정한다. `src/run_bot.sh`가 `KORSTOCKSCAN_SCALPING_AI_ROUTE=openai`, `KORSTOCKSCAN_OPENAI_TRANSPORT_MODE=responses_ws`, `KORSTOCKSCAN_OPENAI_RESPONSES_WS_ENABLED=true`, `KORSTOCKSCAN_OPENAI_RESPONSES_WS_POOL_SIZE=2`, `KORSTOCKSCAN_OPENAI_RESPONSES_WS_TIMEOUT_MS=15000`, `KORSTOCKSCAN_OPENAI_RESPONSES_MAX_OUTPUT_TOKENS=512`, `KORSTOCKSCAN_OPENAI_REASONING_EFFORT=auto`을 export하고, 라우터 기본값도 OpenAI다. Responses WS는 `analyze_target`/`analyze_target_shadow_prompt` hot path에만 적용하며, WS 직접 성공 경로는 전역 HTTP API lock 밖에서 pool worker로 처리하고 HTTP fallback/비-WS endpoint는 기존 lock과 key rotation을 유지한다. OpenAI 호출 prompt는 `English control prompt + Korean glossary + raw label preservation` contract를 중앙 wrapper에서 적용한다. 스캘핑 hot path input은 자연어 파생 브리핑을 제거한 numeric compact JSON을 기본으로 쓰며, OpenAI `invalid_prompt` 400은 키/쿼터 문제가 아니라 prompt/input precheck로 보고 최소 numeric JSON prompt 1회 재시도 후 fail-closed한다. Tier 기본값은 `FAST=gpt-5-nano`, `REPORT=gpt-5.4-mini`, `DEEP=gpt-5.4`다. Threshold AI correction은 runtime tier와 분리된 proposal layer로 `gpt-5.5`를 기본 사용하고 `gpt-5.4`, `gpt-5.4-mini`로 fallback한다 | [2026-04-30 OpenAI spec](./ai-acceptance/2026-04-30-openai-enable-acceptance-spec.md), [2026-05-04 checklist](./checklists/2026-05-04-stage2-todo-checklist.md) `OpenAIParity0504-Preopen`, `OpenAIResponsesWS0504-*`; [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `AIEngineFlagOffBacklog0506`; [2026-05-11 checklist](./checklists/2026-05-11-stage2-todo-checklist.md) `OpenAILiveRuntimeRouting0511`, `OpenAIResponsesWSLiveCanary0511`, `OpenAIThresholdCorrection0511` | OpenAI key/engine 초기화 실패 시 Gemini fallback 가능성을 오류로 보고 startup 로그 `main_scalping_openai=ON`과 pipeline event `openai_transport_mode`, `openai_ws_used`, `openai_ws_http_fallback`, `openai_ws_roundtrip_ms`를 운영 증적으로 확인한다. `2026-05-11` 장후 `OpenAIWSPostcloseStabilityReport0511`은 unique WS `analyze_target` 569건, fallback 0건, p95 2742ms, HTTP late baseline 대비 median 29.54% 개선으로 `keep_ws` 판정했다. `entry_price`는 canary 적용 이벤트 3건이 있었지만 당시 `entry_ai_price_canary_applied` 이벤트에 OpenAI transport provenance가 누락되어 WS 적용 여부를 확정할 수 없는 instrumentation gap으로 정정했고, 이후 `entry_ai_price_canary_*` 이벤트에 `openai_*` provenance를 같이 남기도록 보강했다. OpenAI correction caller는 strict JSON schema 기반 proposal만 만들며 deterministic guard가 최종 source of truth다 |
| holding/exit live canary | `soft_stop_micro_grace`, `REVERSAL_ADD`, `holding_flow_override`가 현재 보유/청산 live owner다. OFI smoother는 `holding_flow_override` 내부에서만 `EXIT` debounce와 bearish confirm으로 쓰며 hard/protect/order safety, `max_defer_sec`, `worsen_floor`를 넘지 않는다. `SCALP_SOFT_STOP_WHIPSAW_CONFIRMATION_ENABLED=False`는 5/6 장후 구현된 기본 OFF 단일축 canary이며, 단순 micro grace 연장이 아니라 base grace 종료 후 emergency/hard/protect 우선, 1회 confirmation cap, rebound/additional-worsen 로그를 별도 stage로 남긴다. 5/7 PREOPEN 로드 확인에서는 env override 미설정과 같은 holding/exit 단계 `protect_trailing_smoothing` 후보 충돌을 근거로 live enable을 보류했다. `bad_entry_refined_canary`는 5/4 장후 OFF, refined 후보는 `SCALP_BAD_ENTRY_REFINED_OBSERVE_ENABLED=True` report-only counterfactual로만 유지한다. `same_symbol_loss_reentry_cooldown`은 유안타증권 반복손실 대응으로 연 임시 운영가드이며 최종 threshold 축이 아니다. `2026-05-06`에는 REVERSAL_ADD/PYRAMID의 신호 조건 대규모 재튜닝은 하지 않고 주문 직전 scale-in P1 가격 resolver와 0/1 동적 수량 safety만 live로 교체했다. `HOLD/EXIT Sentinel`은 `09:05~15:30 KST` 5분 cron으로 보유/청산 이상치를 감지·분류하는 report-only 자동점검이며 Telegram 알림 기능은 삭제됐다. trailing/protect 민감도, winner wide-window 전환 후보, 손실 후 하향 재진입 복합 threshold는 observe/report-only 설계 owner로 분리한다. `protect_trailing_stop`은 단일 tick 이탈이 아니라 rolling price smoothing으로 확정하며, `protect_hard_stop`은 평탄화 대상이 아니다. `2026-05-07` 성과 기준 `holding_flow_override_defer_exit=67`, `soft_stop_rebound priority=1`, `rebound_above_sell_10m_rate=90.0%`, `trailing_continuation MISSED_UPSIDE=5 / GOOD_EXIT=7`이 남아 있어 다음 owner를 명시적으로 잠가야 한다 | [2026-05-04 checklist](./checklists/2026-05-04-stage2-todo-checklist.md) `BadEntryRefinedCanary0504-Postclose`, `HoldingFlowOverride0504-*`, `ProtectTrailingSmoothing0504-Intraday`, `TrailingProfitSensitivity0504-Postclose`, `SameSymbolLossReentryCooldown0504-Intraday`, `YuantaDowntrendReentry0504-Postclose`, `KAIWideWindowExit0504-Postclose`; [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `OFIAISmoothingOverride0506-Preopen`, `ThresholdOpsTransition0506`, `BadEntryRefinedRollback0506-Preopen`, `OvernightFlowTrimSemantics0506-Preopen`, `ScaleInPriceResolverP1_0506`, `ScaleInDynamicQty0506`, `HoldingExitSentinel0506-Intraday`, `SwingTrailingPolicy0506`, `TrailingProtectSensitivity0506`, `CooldownPolicyInventory0506`, `DowntrendReentryComposite0506`, `SoftStopWhipsawConfirmationImplementation0506`; [2026-05-07 checklist](./checklists/2026-05-07-stage2-todo-checklist.md) `SentinelDiscoveryLoop0507-Intraday`, `SoftStopWhipsawConfirmationPreopen0507`; [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `HoldingFlowDeferCost0508`, `SentinelTelegramRemoval0508` | `2026-05-04` 리노공업/한화비전 보호트레일링 이탈 대응으로 단일 tick 민감도 평탄화 hotfix를 반영했고, 유안타증권 반복손실 대응으로 손실 청산 후 동일종목 60분 재진입 cooldown을 임시로 열었다. `bad_entry_refined_canary`는 flow defer/sell failure 혼입으로 OFF, 다음 축은 report-only counterfactual enrichment로 즉시 전환했다. trailing/protect 민감도는 5/4 수익 실현은 양호하나 weak borderline, winner wide-window 후보, PYRAMID 교차가 커서 5/6 단일 owner로 재판정한다. 오버나이트 `TRIM`은 `SELL_TODAY` 유지로 보정했고, 장마감 후 SCALPING 매도 신호는 `sell_order_blocked_market_closed`로 주문 전송을 막는다. Sentinel이 이상치를 감지해도 자동 매도, Telegram 알림, 보유/청산 threshold mutation, AI cache TTL mutation, bot restart는 별도 workorder 없이는 실행하지 않는다. whipsaw confirmation은 5/7 PREOPEN live hold 판정 이후 `HOLD/EXIT Sentinel`과 `SentinelThresholdFeedback0507-Intraday`에서 report-only로 다시 라우팅한다. 5/8에는 defer cost/soft-stop tail 후속 owner를 `HoldingFlowDeferCost0508`에서 다시 잠근다 |
| position sizing / scale-in semantics | 2026-05-09 후속 사용자 지시로 신규 BUY, wait6579 probe, REVERSAL_ADD/AVG_DOWN, PYRAMID의 1주 수량 cap은 기본 ON으로 복귀했다. `MAX_*_COUNT`는 runtime blocker가 아니라 attribution counter로만 본다. generic `AVG_DOWN` 평가는 제거했고, scalping `AVG_DOWN` add_type은 `REVERSAL_ADD` 귀속명으로만 남긴다. 추가매수 리스크 제한은 `REVERSAL_ADD`/`SCALPING_ENABLE_PYRAMID`와 fill cooldown/cancel cooldown/pending/position cap/protection 기반 가드가 맡는다. `2026-05-06` 삼성생명 PYRAMID 추격 지정가 표본 이후, PYRAMID 임시 가격가드는 scale-in P1 resolver hard safety로 흡수했고 `SCALPING_SCALE_IN_PRICE_RESOLVER_ENABLED=True`, `SCALPING_SCALE_IN_DYNAMIC_QTY_ENABLED=True`, `SCALPING_SCALE_IN_EFFECTIVE_QTY_CAP=1`, spread `80bp`, PYRAMID AI/수급/가속 threshold `70/60/0.5`로 REVERSAL_ADD/PYRAMID 주문 직전 가격·수량 경로를 유지한다. P2 `scale_in_price_v1`은 observe-only다. 수량 cap 해제는 `position_sizing_cap_release`가 EV 중심 trade-off score를 충족해 `approval_requests`를 생성한 뒤 사용자 승인으로만 진행한다 | [2026-05-04 checklist](./checklists/2026-05-04-stage2-todo-checklist.md) `ScaleInCountGateRemoval0504-Preopen`; [workorder-shadow-canary-runtime-classification](./workorder-shadow-canary-runtime-classification.md) `initial_entry_qty_cap_1share`, `SCALPING_ENABLE_PYRAMID`; [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `SamsungLifePyramidPriceGuard0506-Intraday`, `ScaleInPriceResolverP1_0506`, `ScaleInDynamicQty0506`; [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `ScaleInMultiArmCanary0508`; [2026-05-11 checklist](./checklists/2026-05-11-stage2-todo-checklist.md) `PositionSizingCapRemoval0509`, `PositionSizingCapRestore0509` | `2026-05-04` `456040` PYRAMID 접수/취소 반복 대응으로 pending add 취소 성공 후 `SCALE_IN_CANCEL_COOLDOWN_SEC=120` 재접수 가드를 추가했다. 삼성생명 id `5132`에서 PYRAMID 직전 `spread_bps 83.19/82.64`, `distance_to_buy_bps 203.74/176.62`, `tick_acceleration_ratio 0.0/0.0` 상태로 `ws_data.curr` 지정가가 전송된 것을 가격품질 결함으로 보고, 다음 scale-in 주문부터 `request_price == ws_data.curr` 직접 제출 재발을 rollback guard로 본다. 수량 cap 해제는 무제한 주문 허용이 아니라 기존 예산/position/protection guard 안의 수량 산출 복귀지만, 현재는 사용자 승인 전까지 1주 cap을 유지한다 |
| scalping live simulator | `SCALP_LIVE_SIMULATOR_ENABLED=True`가 기본이며 스캘핑 AI/Gatekeeper BUY 확정 지점에서 `scalp_ai_buy_all` sim 포지션을 종목당 1개까지 생성한다. 브로커 `send_buy_order`/`send_smart_sell_order`/추가매수 접수는 전부 차단하고, `signal_inclusive_best_ask_v1` full-fill로 BUY 신호 표본을 즉시 가상 체결한다. sim 초기 진입 수량은 `SCALP_LIVE_SIMULATOR_QTY<=0` 기본값에서 실주문 1주 cap을 적용하지 않고 예산/전략비중 산식의 uncapped 수량을 사용하며, sim 추가매수도 `SCALPING_SCALE_IN_EFFECTIVE_QTY_CAP=1` 실주문 cap을 무시하고 `would_qty`/`effective_qty`를 동적 산식대로 남긴다. quote touch/timeout은 진입 허들이 아니라 `would_limit_fill`, `fill_source`, `limit_fill_price` 진단 필드로만 남긴다. open sim state는 `data/runtime/scalp_live_simulator_state.json`에 저장해 재시작 후 복원하며, Kiwoom WS 구독은 active sim target이 있으면 prune하지 않는다. `scalp_sim_*` stage는 `simulation_book=scalp_ai_buy_all`, `actual_order_submitted=false`, `calibration_authority=equal_weight` provenance를 남긴다 | [2026-05-11 checklist](./checklists/2026-05-11-stage2-todo-checklist.md) `ScalpAiBuyAllLiveSimulator0511`, `ScalpSimEVMidcheck0511` | sim completed rows는 threshold-cycle/daily EV에서 `real`, `sim`, `combined`로 분리 표시하되, 튜닝 후보 산출에는 combined를 실매매 동급 권한으로 사용한다. `scalp_sim_ev_midcheck`는 `sim_record_id` 기준으로 `exit_only`/`avg_down`/`pyramid` arm, scale-in filled/unfilled, post-add MFE/MAE, final exit, 초기 수량 provenance, `actual_order_submitted=false` 검증을 분리한다. 초기 수량은 실제 sim 수량이 이미 uncapped 산식이므로 별도 1주 PnL counterfactual 축을 만들지 않는다. 장중 runtime threshold mutation 금지와 deterministic calibration guard는 유지한다 |
| swing dry-run simulator | 스윙은 BUY 확정 hook이 드물어 `gatekeeper_pass` 표본만 보면 gap/gatekeeper가 버린 후보의 후행 EV가 사라진다. `swing_daily_simulation_report`는 최신 추천일 carryover CSV 후보와 `final_ensemble_scanner`가 `recommendation_history`에 직접 적재한 `DB_EOD_TOP5`/`DB_FINAL_ENSEMBLE` 후보를 병합해 `selection_only`, `gap_pass`, `gatekeeper_pass` observation arm을 분리하고, 같은 리포트에 장중 `blocked_swing_gap`/`blocked_gatekeeper_reject`/`swing_sim_*` entry funnel raw/unique count를 붙인다. `SWING_INTRADAY_LIVE_EQUIV_PROBE_ENABLED=True`는 `blocked_swing_score_vpw`, gap, gatekeeper reject, market regime block, budget/latency/order bundle block에서 `swing_probe_*` virtual holding을 생성해 live 보유/청산 경로와 같은 tick 데이터를 observe-only로 수집한다. `SWING_ENABLE_AVG_DOWN_SIMULATION=True`, `SWING_SCALE_IN_DYNAMIC_QTY_ENABLED=True`, `SWING_SCALE_IN_EFFECTIVE_QTY_CAP=0`은 dry-run/probe 내부에서 AVG_DOWN/PYRAMID 후보와 `would_qty`/`effective_qty`를 수집하는 관찰축이며, 별도 `swing_scale_in_real_canary_phase0` approval artifact 전까지 브로커 추가매수 실주문은 차단한다. `SWING_SAME_SYMBOL_LOSS_REENTRY_GUARD_ENABLED=True`는 stop-loss 계열 `<= -2.5%` 또는 당일 연속 손실 2회 이후 동일 종목/전략 신규 dry-run BUY·probe를 60분 차단하는 mandatory safety다. 막힌 후보는 `swing_reentry_counterfactual_after_loss`로 남기되 real-like EV에는 합산하지 않는다. probe state는 `data/runtime/swing_intraday_probe_state.json`에 저장/복원하며 `actual_order_submitted=false`, `broker_order_forbidden=true`, open 10/일 30/종목 1개 cap과 손실 후 재진입 cooldown을 유지한다. 동일 종목/전략이 CSV와 DB에 모두 있으면 CSV를 canonical로 둔다. `swing_lifecycle_audit`는 daily simulation arm과 intraday probe evidence quality를 분리해 읽고, `panic_sell_defense`의 `panic_state`, active sim/probe 회복률, origin별 panic-context outcome을 `panic_context`로 함께 고정한다. 닫힌 winner 표본은 새 family를 만들지 않고 `swing_selection_top_k`, `swing_market_regime_sensitivity`, `swing_gatekeeper_reject_cooldown` 기존 family의 `swing_improvement_automation` order 후보로 매핑한다. 이 arm은 모두 `actual_order_submitted=false`이며 broker 주문/수량/가격 guard를 변경하지 않는다 | [2026-05-11 checklist](./checklists/2026-05-11-stage2-todo-checklist.md) `SwingRuntimeDryRunSimulation0509`, `SwingLiveOrderDryRunRuntime0511`, `SwingIntradayLiveEquivalentProbe0511`, `SwingSimScaleInArms0511`, `SwingScaleInRealCanaryPhase0Policy0511`; [2026-05-12 checklist](./checklists/2026-05-12-stage2-todo-checklist.md) `SwingSameSymbolLossReentryGuard0512` | `selection_only`는 추천 후보 자체의 opportunity cost, `gap_pass`는 gap guard 기회비용, `gatekeeper_pass`는 기존 dry-run proxy를 본다. `swing_probe_*`는 장중 block-stage 이후 live-equivalent opportunity cost를 보고, `daily_next_open_proxy`보다 높은 evidence quality로 분리한다. 손실 후 cooldown counterfactual은 missed rebound 관찰에는 쓰지만 active probe/real-like EV에는 섞지 않는다. 일봉 quote가 아직 없으면 `PENDING_ENTRY`로 남기고 장중 차단 병목은 runtime entry funnel/probe로 따로 판정한다. `panic_context`는 스윙 approval/source bundle 입력이지만 단독 live gate가 아니며, runtime threshold mutation, gate 완화, 일반 스윙 실주문 전환은 이 리포트 권한 밖이다 |
| swing one-share real canary | 스윙은 계속 `SWING_LIVE_ORDER_DRY_RUN_ENABLED=True`가 기본이지만, sim/combined가 채우지 못하는 broker execution 품질 수집을 위해 `swing_one_share_real_canary`를 별도 approval-required 후보로 정의한다. 이 축은 스윙 전체 실매매 전환이 아니라 승인된 극소수 후보만 `qty=1`, `actual_order_submitted=true`로 BUY/SELL receipt, order number binding, fill ratio, slippage, cancel/timeout, sell receipt를 수집하는 real-canary다. phase0에서는 `max_new_entries_per_day=1`, `max_open_positions=3`, `max_total_notional_krw=300000`, same-symbol active 1개, 손실 후 동일종목 재진입 guard mandatory, 추가매수/AVG_DOWN/PYRAMID 실주문 금지, 청산 SELL만 실제 주문 허용으로 잠근다 | [2026-05-11 checklist](./checklists/2026-05-11-stage2-todo-checklist.md) `SwingOneShareRealCanaryPolicy0511`; [workorder-shadow-canary-runtime-classification](./workorder-shadow-canary-runtime-classification.md) `swing_one_share_real_canary`; [2026-05-12 checklist](./checklists/2026-05-12-stage2-todo-checklist.md) `SwingSameSymbolLossReentryGuard0512` | 시작 조건은 `swing_runtime_approval` hard floor + EV trade-off 통과, 별도 사용자 approval artifact, 장전 preopen apply, DB load/instrumentation/fallback contamination/severe downside/same-stage conflict 없음, same-symbol loss reentry cooldown 미해당이다. rollback guard는 approval artifact 밖 실주문 1건, qty > 1, global dry-run 해제, receipt/order number mismatch, sell failure, price guard breach, daily/open/notional cap 초과, `actual_order_submitted` provenance 누락, loss reentry guard 우회다. 1주 real canary 결과는 combined EV에 들어갈 수 있지만 broker execution 품질은 real-only로만 판정하고, 스윙 전체 실주문 전환은 별도 2차 계획/승인 없이는 금지한다 |
| swing scale-in real canary | `swing_scale_in_real_canary_phase0`는 initial BUY/SELL real canary와 분리된 별도 approval-required 축이다. 기본값은 `KORSTOCKSCAN_SWING_SCALE_IN_REAL_CANARY_ENABLED=false`이고, `data/threshold_cycle/approvals/swing_scale_in_real_canary_YYYY-MM-DD.json` artifact가 있어야만 승인된 real swing holding의 `PYRAMID`/`AVG_DOWN` 추가매수를 `qty=1` limit order로 허용한다. 동적수량은 `would_qty`/`effective_qty` provenance로만 남기고 실제 주문 수량은 `real_canary_actual_qty=1`, `real_canary_qty_cap=1`, `qty_cap_reason=swing_scale_in_real_canary_phase0`로 고정한다. sim/probe/dry-run 포지션, 승인되지 않은 arm, stale quote, `orderbook_micro_ready=false`, OFI/QI `RISK_BEARISH`, pending order, 일/포지션/notional cap 초과는 fail-closed block이다 | [2026-05-11 checklist](./checklists/2026-05-11-stage2-todo-checklist.md) `SwingScaleInRealCanaryPhase0Policy0511` | 승인 기준은 최근 10거래일 arm별 closed sim/probe scale-in 표본(`PYRAMID>=5`, `AVG_DOWN>=8`), OFI/QI 표본 `>=5`, instrumentation/db/fallback/provenance gap 없음, `p10_profit_rate>=-4.0`, post-add outcome coverage, 평균 `final_exit_return_pct>0`, `post_add_delta_vs_exit_only>=0`, MAE p50/p90 `>=-2.0%/-4.0%`, loser extension `<=35%`다. rollback은 승인 밖 주문, 1건 초과 제출, sim/probe real order attempt, receipt/order lifecycle mismatch, stale submit, bearish submit, 추가매수 후 MAE `<=-4.0%`, exit-only 대비 악화다 |
| swing ML retrain automation | 스윙 ML v2 재학습은 `auto_retrain_pipeline.sh`가 매 장후 진단, 상승장 특화모델 적용/기간 검토, staging 학습, 평가, artifact 승격/rollback을 수행한다. `bull_specialist_mode`는 `enabled/disabled/hold_current` 중 하나이며, `disabled`일 때도 `bx/bl`은 `hx/hl`로 중립화해 `META_FEATURES` schema를 유지한다. AI 제안 기간은 DB 범위, 최소 기간/표본, label safety guard를 통과해야 하며 실패 시 `hold_current`로 닫는다. 자동 승격은 모델 artifact 교체만 뜻하고 `SWING_LIVE_ORDER_DRY_RUN_ENABLED=True`, `actual_order_submitted=false`는 유지한다 | [2026-05-11 checklist](./checklists/2026-05-11-stage2-todo-checklist.md) `SwingModelRetrainAutomation0511` | 조건 미달은 `hold_sample/rejected`로 닫고 active artifact를 유지한다. smoke 실패 시 backup에서 즉시 rollback한다. 스윙 실주문 전환, one-share real canary 승인, OpenAI/Gemini live routing 변경은 이 체인의 권한 밖이다 |
| holding/overnight operating override | `holding_flow_override`는 튜닝 관찰축이 아니라 운영 override. hard/protect/order safety는 우회하지 않는다. 5/4 장후 `TRIM -> HOLD_OVERNIGHT` semantics는 폐기했고, flow `HOLD`만 오버나이트 보유로 승격한다 | [2026-05-04 checklist](./checklists/2026-05-04-stage2-todo-checklist.md) `HoldingFlowOverride0504-*`, [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `OvernightFlowTrimSemantics0506-Preopen`, [2026-05-01 review](./code-reviews/2026-05-01-holding-flow-override-code-review-report.md) | 운영 override 자체는 ON/반영 완료지만, 5/6 PREOPEN의 `TRIM=SELL_TODAY` 로드 확인은 OPEN이다 |
| threshold cycle / action weight | threshold compact collector, 장중/장후 calibration, 누적/rolling cohort report, 다음 장전 `auto_bounded_live` runtime env apply, `statistical_action_weight`, daily EV report를 하나의 무인 loop로 관리한다. Gemini/Claude pattern lab은 postclose에 `scalping_pattern_lab_automation`으로 집계해 기존 family 입력, `auto_family_candidate(allowed_runtime_apply=false)`, `code_improvement_order(runtime_effect=false)`만 생성하며 repo/runtime을 직접 바꾸지 않는다. 장중 live threshold 자동변경은 금지하고, 장중 calibration artifact -> 장후 canonical report/calibration/AI correction/pattern-lab order -> 다음 장전 bounded runtime env -> 장후 attribution/daily EV/calibration loop로 고정한다. calibration은 매일 `intraday`, `postclose` 2회 실행한다. 스윙 runtime approval은 별도 보수 ladder로 `proposal -> approval_required -> approved_live(dry-run)`만 허용한다. `swing_runtime_approval`은 hard floor 통과 후 `overall_ev 45% + downside_tail 20% + participation/funnel 15% + regime_robustness 10% + attribution_quality 10%` trade-off score `>=0.68`일 때 요청을 만들고, 수동 approval artifact가 있어야 preopen apply가 env를 쓴다. 반영 후에도 `SWING_LIVE_ORDER_DRY_RUN_ENABLED=True`, `actual_order_submitted=false`를 강제한다. `threshold_cycle_YYYY-MM-DD.json`과 `threshold_cycle_calibration_YYYY-MM-DD_{intraday,postclose}.json`은 `calibration_source_bundle`, `trade_lifecycle_attribution`, `calibration_candidates`, `safety_guard_pack`, `calibration_trigger_pack`, `post_apply_attribution`을 포함한다. `trade_lifecycle_attribution`은 entry submit/cancel, holding 후보 신호, exit rule/source, 장후 post-sell outcome을 `record_id`로 join해 family 공통 전중후 유형을 닫고, runtime 후보 stage만으로 최종 라벨을 확정하지 않는다. `threshold_cycle_ai_review_YYYY-MM-DD_{intraday,postclose}.{json,md}`는 AI를 검토자 + 이상치 수정안 제안자로 쓰는 보조 layer이며, cron wrapper는 기본 `THRESHOLD_CYCLE_AI_CORRECTION_PROVIDER=openai`로 OpenAI `gpt-5.5` strict JSON correction proposal 생성을 자동 시도하고 실패 시 `gpt-5.4`, `gpt-5.4-mini`로 fallback한다. 최종 threshold state/value는 deterministic calibration guard가 결정하고, preopen apply는 AI guard accepted, family bounds/max-step/sample-window/safety/same-stage owner guard를 통과한 family만 runtime env에 쓴다. family별 `sample_window/window_policy`를 분리해 당일 운영 데이터로 판단할 축과 4월 이후 누적/rolling 지속성이 필요한 축을 섞지 않는다. source bundle은 새 관찰축을 만들지 않고 BUY 쪽 `buy_funnel_sentinel`, `sentinel_followup`, `wait6579_ev_cohort`, `missed_entry_counterfactual`, 보유/청산 쪽 `holding_exit_observation`, `post_sell_feedback(1/3/5/10/20/30/60분 forward horizon)`, `trade_review`, `holding_exit_sentinel`, `panic_sell_defense`, `panic_buying`, decision-support 쪽 `holding_exit_decision_matrix`, `statistical_action_weight` 기존 report를 재사용한다. `score65_74_recovery_probe`는 daily_intraday sample floor를 기본 20으로 유지하되, 전일 panic/recovery-watch 상태에서 표본이 floor의 70% 이상이고 EV/close 우위 및 submitted drought guard를 통과하면 `panic_adjusted_ready`로 다음 장전 bounded canary 유지 후보가 될 수 있다. `panic_sell_defense`와 `panic_buying`은 simulation/counterfactual source가 생기면 threshold-cycle EV lineage를 통해 `code_improvement_workorder`에 `runtime_effect=false` 설계 order를 만들고, `runtime_approval_summary`에는 `approval_required` 후보로만 표시한다. 두 panic 축 모두 `allowed_runtime_apply=false`이며 approval artifact, runtime env key, rollback guard, same-stage owner rule이 별도 checklist로 닫히기 전에는 preopen apply가 live threshold/TP/exit 변경을 만들 수 없다. `preclose_sell_target`은 2026-05-10 제거되어 operator review, tuning/calibration source, cron inventory에서 제외한다. 첫 efficient trade-off family는 `score65_74_recovery_probe`, `bad_entry_refined_canary`, `soft_stop_whipsaw_confirmation`, `holding_flow_ofi_smoothing`, `protect_trailing_smoothing`, `holding_exit_decision_matrix_advisory`다. 스윙 1차 env 대상은 `swing_model_floor`, `swing_selection_top_k`, `swing_gatekeeper_reject_cooldown`, `swing_market_regime_sensitivity`로 제한하고, `AVG_DOWN`, `PYRAMID`, exit OFI/QI smoothing, AI contract 변경은 별도 family guard 전까지 approval request까지만 허용한다. `trailing_continuation`은 GOOD_EXIT 훼손 리스크 때문에 1차 loop에서는 freeze/report-only다. `scale_in_price_guard`는 물타기/불타기 resolved/executed cohort가 없으면 calibration state를 `hold_sample`로 유지하고, sample floor 전에는 `report_only_calibration`만 허용한다. Sentinel/패닉 이상치는 신규 관찰축으로 늘리지 않고 기존 report source bundle과 incident/instrumentation/no-action 라우팅으로 흡수한다. 누적/rolling report는 추천값 확정이 아니라 방향성과 step size 산정 입력이며, 누적 평균 단독으로 threshold를 자동 적용하지 않는다. 자동 threshold 적용, version attribution, daily EV 제출 추적성은 [report-based-automation-traceability](./report-based-automation-traceability.md)의 `R0~R6` ladder로 관리한다 | [2026-05-01 checklist](./checklists/2026-05-01-stage2-todo-checklist.md) `ThresholdBootstrap0501-AM`, `StatActionMatrixReport0501-Maintenance`; [2026-05-04 checklist](./checklists/2026-05-04-stage2-todo-checklist.md) `CumulativeThresholdCycleReport0504-Postclose`; [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `OFIAISmoothingOverride0506-Preopen`, `ScaleInPriceGuardThresholdCycle0506-Intraday`, `ThresholdCollectorIO0506`, `ReportAutomationTraceability0506`, `ThresholdOpsTransition0506`, `StatActionWeight0506`, `StatActionMarkdown0506`, `StatActionAdvancedAxes0506`; [2026-05-07 checklist](./checklists/2026-05-07-stage2-todo-checklist.md) `SentinelThresholdFeedback0507-Intraday`, `StatActionEligibleOutcome0507`; [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `ThresholdCalibrationLoop0508`, `ThresholdCalibrationTwiceDaily0508`, `EfficientTradeoffCalibration0508`, `TradeLifecycleAttribution0508`, `ThresholdAICorrection0508`, `ThresholdAICorrectionCron0508`, `ThresholdUnattendedApply0508`, `ThresholdDailyEVReport0508`, `PatternLabAutomation0508`, `ScaleInGuardCalibration0508`, `ScaleInMultiArmCanary0508`, `SoftStopLongHorizonForensics0508`, `StatActionAdvancedContext0508`; [2026-05-11 checklist](./checklists/2026-05-11-stage2-todo-checklist.md) `OpenAIThresholdCorrection0511`, `SwingRuntimeApproval0511`, `PrecloseSellTargetDecommission0510`; [2026-05-12 checklist](./checklists/2026-05-12-stage2-todo-checklist.md) `PanicSellDefenseReportOnly0512`; [2026-05-13 checklist](./checklists/2026-05-13-stage2-todo-checklist.md) `PanicBuyingReportOnly0513`, `PanicLifecycleAutomationReview0513` | 조건 미달은 rollback이 아니라 `adjust_up/adjust_down/hold/hold_sample/hold_no_edge/freeze` calibration trigger다. sample 부족은 live 금지가 아니라 cap 축소/hold_sample/max_step_per_day 축소로 처리한다. safety revert는 hard/protect/emergency stop 지연, 주문 실패, provenance 손상, same-stage owner 충돌, severe loss guard 초과에만 쓴다. AI correction과 pattern lab automation은 자동 적용의 검증/작업지시 layer지만 단독 적용 권한은 없다. daily EV 성과 제출은 `data/report/threshold_cycle_ev/threshold_cycle_ev_YYYY-MM-DD.{json,md}`를 기준으로 보며, 스윙 approval request/approved dry-run 상태와 panic approval-required 후보도 같은 EV/runtime approval summary에 포함한다. |
| holding/exit decision support | `stat_action_decision_snapshot`, `holding_exit_decision_matrix`는 report-only/observe-only에서 출발하되, 목표는 완벽한 matrix spot이 아니라 `candidate_weight_source -> recommended_bias -> bounded advisory canary -> calibration` loop다. 5/7 `SAW-3`는 `eligible_but_not_chosen` MFE/MAE proxy 섹션을 `statistical_action_weight` JSON/Markdown에 구현했다. `post_decision_*_proxy`는 post_sell 10분 지표를 record_id로 붙인 report-only proxy이며 realized PnL과 합산하지 않는다. `AIDecisionMatrixShadow0507`의 naming은 stale이고, 다음 유효 축은 `single-owner advisory/live canary` readiness다. 5/7 장후에는 `holding_exit_matrix_runtime.py`와 provider 3종 parity patch로 runtime loader / feature flag / cache-key 분리 / provenance logging / baseline-candidate-excluded cohort를 구현했다. 다만 5/7 matrix는 entry 대부분이 `no_clear_edge`라 same-day live AI 응답 변경 근거가 약하고, `HOLDING_EXIT_MATRIX_ADVISORY_ENABLED`는 기본 OFF로 유지한다 | [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `AIDecisionMatrix0506`; [2026-05-07 checklist](./checklists/2026-05-07-stage2-todo-checklist.md) `StatActionEligibleOutcome0507`, `AIDecisionMatrixShadow0507`; [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `SAW3EligibleOutcomeImplementation0508`, `ADMReportOnlyContract0508`, `ADMCanaryLivePivot0508`, `EfficientTradeoffCalibration0508` | 현재 허용선은 ADM-1 report-only + flag-off readiness plumbing + threshold-cycle `holding_exit_decision_matrix_advisory` calibration candidate다. 다음 진행은 shadow가 아니라 advisory/live canary pivot으로 본다. all `no_clear_edge`는 perfect spot 대기가 아니라 최소 edge 부재로 `hold_no_edge` 처리하며, live AI 응답 변경은 matrix edge/rollback owner가 닫히기 전까지 금지한다 |
| counterfactual / exit authority provenance | `missed_entry_counterfactual` 리포트는 운영 중이지만 3모드 realism은 아직 없다. 반면 `exit_decision_source` taxonomy는 runtime/report/post-sell payload에 구현돼 common provenance field로 고정됐다 | [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `Phase3Quality0508`, `ExitDecisionSourceProvenance0508` | `Phase3Quality0508` 판정으로 `3-mode counterfactual`은 report-only/설계 only로 잠갔고, `ExitDecisionSourceProvenance0508`으로 `PRESET_HARD_STOP/PRESET_PROTECT/AI_REVIEW_EXIT/SOFT_STOP/TRAILING/TIMEOUT/OVERNIGHT_FLOW/HOLDING_FLOW_OVERRIDE/MANUAL` taxonomy를 구현했다 |
| scanner/residual architecture | 스캐너 3단 분해, DB/WS 경계, source/gate/composite, state handler context/split은 설계/정리 단계 | [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `ScalpingScanner*`, `StateHandlers*`, `SwingTrailingPolicy0506`, `CodeDebt0506` | 반영 완료 |
| runtime stabilization | `nan_cast_guard_followup`, execution receipt binding/thread safety, `holding_state net-profit sell decision` 회귀 격리는 EV 판정 전제 품질축이다. threshold IO/ops는 위 `threshold cycle / action weight` 행에서 별도 관리한다 | [2026-05-06 checklist](./checklists/2026-05-06-stage2-todo-checklist.md) `NaNCastGuard0506HolidayCarry`, `ExecutionReceiptsThreadSafety0506`, `HoldingNetProfitExitRegression0506` | `2026-05-05` 휴장일 OPEN 누락 없음. `HoldingNetProfitExitRegression0506`까지 반영 완료. `2026-05-04` 지투파워 `ID 4988` 0주 매도가능/중복 completed는 `ExecutionReceiptsThreadSafety0506`의 receipt binding anchor로 추가됐다 |

### 8.1 2026-05-08 장후 owner 판정 메모

- `EntryFunnelRecoveryDecision0508`: `score65_74_recovery_probe`는 `hold`로 닫는다. 5/8 `wait6579_ev_cohort` 방향성은 있으나 `partial_samples=0`, submitted/full/partial 회복과 soft-stop tail이 아직 동시에 닫히지 않았다. family는 `adjust_up` 후보로 daily EV/calibration에 남기되 장전 수동 enable 항목은 만들지 않는다.
- `LatencyDangerRootCause0508`: latency danger는 spread cap이 아니라 `quote freshness/ws jitter/ws age/other_danger` instrumentation owner로 닫는다. `pre_submit_price_guard` family에 submit revalidation/passive probe/cancel provenance를 연결하고, score threshold/spread/fallback/restart 자동화는 열지 않는다.
- `HoldingAICacheLatency0508`: holding/gatekeeper cache miss 100%는 stale 방지를 위한 signature 변경 성격이 있으나 `holding_review_ms_p95=17022`, `gatekeeper_eval_ms_p95=11433`라 `flag-off remediation`으로 닫는다. cache TTL 확대나 provider 승격 대신 slow-call provenance와 cache signature coarsening 후보만 follow-up한다.
- `HoldingFlowDeferCost0508`: `split owner`로 닫는다. `holding_flow_override`는 현 운영 owner로 유지하고, `soft_stop_whipsaw_confirmation`은 bounded apply 후보, `holding_flow_ofi_smoothing`은 hold, `protect_trailing_smoothing`은 hold_sample, `trailing_continuation`은 GOOD_EXIT 훼손 리스크로 freeze/report-only다.
- `StatActionAdvancedContext0508`: SAW-4~SAW-6 중 readiness 1순위는 `SAW-6 orderbook context`다. `SAW-4`는 full 2/partial 0으로 보류하고, `SAW-5`는 market regime freshness/join key가 닫힌 뒤 진행한다.
- `OFIQExpansionLadder0508`: ladder는 `performance_tuning Markdown OFI/QI section + stale guard`를 다음 단일 작업으로 확정한다. prompt contract 보강과 bucket calibration ON/OFF는 Markdown 표면화 이후에만 연다.

### 8.2 튜닝 후속 작업 자동화체인 편입 원칙

- 남은 튜닝 작업은 수동 관찰축/후속계획을 계속 늘리지 않고 `threshold_cycle`, `statistical_action_weight`, `performance_tuning`, `pattern_lab_automation`, `threshold_cycle_ev` 체인의 입력/검증/경고로 편입한다.
- `SAWOrderbookContext0511`은 독립 report-only 축이 아니라 `statistical_action_weight -> holding_exit_decision_matrix_advisory -> threshold_cycle_calibration -> daily EV`로 이어지는 source metric 확장이다.
- `OFIQPerformanceMarkdown0511`은 사람이 읽는 Markdown만이 아니라 OFI/QI freshness, stale warning, SKIP policy evidence를 `performance_tuning`과 daily EV가 소비하게 만드는 source quality 확장이다.
- 새 조정축이 필요하면 날짜별 수동 workorder를 먼저 늘리지 않고 pattern lab `auto_family_candidate(allowed_runtime_apply=false)` 또는 threshold-cycle `calibration_candidates`에 machine-readable 후보로 남긴다. runtime 적용은 기존 `auto_bounded_live` guard와 safety policy를 통과한 family만 가능하다.
- 순수 report-only로 남길 수 있는 것은 감리/운영 제출물, 사후 설명 문서, 외부 operator review뿐이다. 기대값 튜닝에 쓰이는 report-only 산출물은 자동화체인의 source bundle 또는 EV report input으로 연결되어야 한다.

종료/폐기 축과 historical 품질 gate(`ShadowDiff0428` 등)는 이 표에서 제외하고 [closed observation archive](./archive/closed-observation-axes-2026-05-01.md) 또는 관련 Q&A/체크리스트에서만 참조한다. `2026-05-07` 성과 기준으로 드러난 owner 공백은 [2026-05-08 checklist](./checklists/2026-05-08-stage2-todo-checklist.md) `LatencyDangerRootCause0508`, `HoldingAICacheLatency0508`, `HoldingFlowDeferCost0508`으로 메웠다.

## 9. 델타/Q&A 라우팅

| 문서 | 무엇을 남기나 | 이 문서에서 뺀 이유 |
| --- | --- | --- |
| [plan prompt](./plan-korStockScanPerformanceOptimization.prompt.md) | 다음 세션 진입용 경량 포인터, 현재 active owner와 핵심 참조문서 | 중심 기준은 rebase가 소유하되, rebase 변경 시 prompt도 같은 변경 세트에서 현행화한다 |
| [execution-delta](./plan-korStockScanPerformanceOptimization.execution-delta.md) | 날짜형 과제 레지스터, 지나간 일정, same-day pivot, 효과 기록, 폐기/종료 이력 | rebase에 남기면 현재 원칙보다 과거 경과가 더 커져 active 판정이 흐려진다 |
| [qna](./plan-korStockScanPerformanceOptimization.qna.md) | baseline 해석, direction-only 규칙, 감리 확인 포인트, 반복 질의 | 규칙은 중요하지만 매번 중심 문서 본문에 장문 설명으로 둘 필요는 없다 |
| 날짜별 checklist | 특정 시각 작업, Due/Slot/TimeWindow, 완료/미완 상태 | 자동 파싱과 Project/Calendar 소유 문서는 checklist다 |
| audit/report | 외부 반출본, 세부 수치 근거, 감리 관점 해설 | rebase는 승인 기준만 남기고 수치 근거 전문은 분리한다 |
| [closed observation archive](./archive/closed-observation-axes-2026-05-01.md) | 종료/폐기/역사참조 관찰축, 재개 금지선 | 중심 문서에는 active/open 판단만 남기기 위함 |

## 10. 핵심 참조문서

| 문서 | 역할 |
| --- | --- |
| [2026-05-04-stage2-todo-checklist.md](./checklists/2026-05-04-stage2-todo-checklist.md) | 다음 KRX 운영일 장전/장중/장후 실행 작업항목 |
| [2026-05-06-stage2-todo-checklist.md](./checklists/2026-05-06-stage2-todo-checklist.md) | 휴장 이월 후속, 스캐너/threshold/AI 엔진/보유청산 잔여작업 |
| [2026-05-07-stage2-todo-checklist.md](./checklists/2026-05-07-stage2-todo-checklist.md) | `SAW-3`, `ADM-2` 후속 설계 |
| [2026-05-08-stage2-todo-checklist.md](./checklists/2026-05-08-stage2-todo-checklist.md) | `SAW-4~SAW-6` 체결품질/시장맥락/orderbook readiness |
| [plan-korStockScanPerformanceOptimization.execution-delta.md](./plan-korStockScanPerformanceOptimization.execution-delta.md) | 원안 대비 변경, 날짜형 이력, 종료된 축 기록 |
| [plan-korStockScanPerformanceOptimization.prompt.md](./plan-korStockScanPerformanceOptimization.prompt.md) | 다음 세션 시작용 경량 포인터. rebase 변경 시 함께 현행화 |
| [plan-korStockScanPerformanceOptimization.qna.md](./plan-korStockScanPerformanceOptimization.qna.md) | 반복 판단 기준과 감리 Q&A |
| [plan-korStockScanPerformanceOptimization.performance-report.md](./plan-korStockScanPerformanceOptimization.performance-report.md) | 정기 성과 기준선과 반복 성과값 |
| [data/report/README.md](../data/report/README.md) | 정기 report inventory, Markdown 누락 후보, report 생성 작업계획 진입 기준 |
| [data/threshold_cycle/README.md](../data/threshold_cycle/README.md) | threshold collector/report/apply plan 운영방법과 금지선 |
| [report-based-automation-traceability.md](./report-based-automation-traceability.md) | report 기반 자동화의 산출물/소비자/apply 단계/R5-R6 gap 추적 registry |
| [workorder-shadow-canary-runtime-classification.md](./workorder-shadow-canary-runtime-classification.md) | shadow/canary/historical 분류와 코드베이스 정렬 기준 |
| [archive/closed-observation-axes-2026-05-01.md](./archive/closed-observation-axes-2026-05-01.md) | 종료된 관찰축 archive |
| [archive/](./archive/) | 폐기 과제, 과거 workorder, legacy shadow/fallback 경과 |
